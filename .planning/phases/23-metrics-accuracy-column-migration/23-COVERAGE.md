# Phase 23: API Coverage Declaration

No external API integration: this phase relocates existing internal per-day TIF window rendering (min/max/confidence per event type) from `js/ui/metrics-screen.js` to `js/ui/accuracy-screen.js`, both already-existing internal DOM-rendering modules. It consumes `computeTifBoundsHistory()` from `js/lib/accuracy-tif.js`, a pure internal function already called by `accuracy-screen.js` today — no new external API, SDK, network call, or third-party service is introduced or touched. The deterministic `api-coverage.cjs` probe produced no output in this environment (probe likely unavailable); this declaration substitutes per the contribution's fallback instruction rather than fabricating a coverage matrix for a capability that does not exist in this phase.
