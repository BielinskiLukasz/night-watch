---
phase: 23-metrics-accuracy-column-migration
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - js/ui/metrics-screen.js
  - tests/e2e/metrics.spec.js
autonomous: true
requirements: [UI-11]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The Metrics screen's .metricsTable header row has exactly 19 <th> elements (down from 31) in both Classic and TIF modes — the TIF inline columns are gone entirely, not merely hidden (D-09)."
    - "None of the 12 former TIF column labels (W-min, W-max, W-conf, NS-min, NS-max, NS-conf, NE-min, NE-max, NE-conf, B-min, B-max, B-conf) appear anywhere in .metricsTable's text content, in any algorithm mode."
    - "js/ui/metrics-screen.js no longer defines TIF_COLUMNS and no longer imports computeTifBoundsHistory; buildDayRow, buildRollingSection call sites, the thead header-building loop, buildTifAggregateRow, and the render() all-time summary section contain zero references to TIF_COLUMNS or tifBoundsMap (D-09, D-10)."
    - "isTif and activityLog remain defined and used in metrics-screen.js's render() — they still gate the min-TIF/median-TIF/max-TIF aggregate rows and the tifForecast historic-band override block, which are unrelated to the removed columns and are explicitly out of scope for this phase (D-10)."
    - "buildDayRow's signature is (dayMetrics, snap) — the tifBoundsMap and isTif parameters that existed solely to render the removed columns are gone, and its sole call site in render() passes exactly two arguments."
    - "buildRollingSection's signature drops the isTif parameter (its only use was the now-deleted TIF placeholder-cell loop); both call sites (7-day and 14-day) pass one fewer argument."
    - "buildSectionHeaderRow's colCount argument at both call sites (buildRollingSection's section header, render()'s All-time section header) is COLUMNS.length (19), not COLUMNS.length + TIF_COLUMNS.length."
  artifacts:
    - js/ui/metrics-screen.js (TIF_COLUMNS constant, computeTifBoundsHistory import, and all 6 TIF-cell/TIF-placeholder render call sites deleted)
    - tests/e2e/metrics.spec.js (column-count assertion updated 31→19, negative label assertions added, obsolete hidden-cell test removed)
  key_links:
    - "render() → buildDayRow(dayMetrics, snap) [TIF args removed] → daysTbody"
    - "render() → buildRollingSection(nDays, label, nonRejectedDays, snap) [isTif arg removed] → buildSectionHeaderRow(headerLabel, COLUMNS.length)"
  prohibitions:
    - "Never remove the isTif variable, the activityLog variable, or the tifForecast()-based historic-band override block from metrics-screen.js's render() — they are still consumed by computeTifTrimmedStats, the min-TIF/median-TIF/max-TIF aggregate rows, and the override that keeps those rows matching the Today screen's historic band; only the tifBoundsMap-specific plumbing that fed the now-removed columns is in scope (D-10)."
    - "Never add a 4th 'window width' column/field to any table in this phase — exactly 3 fields (min, max, confidence) x 4 events = 12 columns, per D-04; width remains reader-derivable (max - min), not computed."
    - "Never modify computeTifBoundsHistory or computeTifAccuracy's computation logic in js/lib/accuracy-tif.js — this phase only relocates rendering call sites; the shared data function is reused unchanged by accuracy-screen.js."
    - "Never leave metrics-screen.js in a state where the TIF_COLUMNS identifier still appears anywhere in the file (constant, comment referencing it as live code, or stale JSDoc @param entries for tifBoundsMap/isTif on buildDayRow/buildRollingSection) — full clean removal per D-09, not a hide-only toggle."
    - "Never delete or weaken the existing 19 base-column assertions in metrics.spec.js while updating the TIF-column-count assertion — Date/Wake/Sleep/S.Debt(7d) and the cold-start S.Debt em-dash check (td index 9) must keep passing unchanged, since none of the base columns move index position (TIF columns were always appended after them)."
---

<objective>
Delete the 12 per-event TIF window columns and their entire rendering/data-prep pipeline from the Metrics screen, now that plan 23-01 has given them a new home on the Accuracy screen. This is the "remove the old home" half of the UI-11 migration.

Purpose: clean separation per UI-11 — Metrics shows "what happened," Accuracy shows "how well predicted." Full clean removal (D-09), not a hide toggle.
Output: `js/ui/metrics-screen.js` shrinks from a 30/31-effective-column table to 19 columns; `tests/e2e/metrics.spec.js` reflects the new column count and asserts the old TIF labels are gone.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-metrics-accuracy-column-migration/23-CONTEXT.md
@.planning/phases/23-metrics-accuracy-column-migration/23-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts This Phase Produces (this plan's half)

**Deleted:**
- `TIF_COLUMNS` constant in `js/ui/metrics-screen.js` (12-entry frozen array, with its preceding doc comment block).
- `import { computeTifBoundsHistory } from '../lib/accuracy-tif.js';` in `js/ui/metrics-screen.js`.
- The TIF-cell rendering loop inside `buildDayRow` (~22 lines: `tifBoundsMap`/`TIF_COLUMNS` split-key parsing and cell formatting).
- The TIF placeholder-cell loop inside `buildTifAggregateRow` (12 `'—'` cells).
- The TIF placeholder-cell loop inside `buildRollingSection` (applied to each of its Min/Average/Max rows — runs once per call, i.e. once for the 7-day section and once for the 14-day section).
- The TIF placeholder-cell loop inside `render()`'s All-time summary section (applied to its Min/Average/Max rows).
- The TIF column-header-building loop in `render()`'s thead construction.
- The `tifBoundsArray`/`tifBoundsMap` construction lines in `render()`.
- The obsolete `MET-10/boundary: TIF placeholder cells hidden when TIF is off` test in `tests/e2e/metrics.spec.js` — its premise (hidden TIF placeholder `<td>`s) no longer exists.

**Modified (signatures narrowed, not deleted):**
- `buildDayRow(dayMetrics, snap, tifBoundsMap, isTif)` → `buildDayRow(dayMetrics, snap)`; its one call site in `render()`'s per-day loop updated to match.
- `buildRollingSection(nDays, label, nonRejectedDays, snap, isTif)` → `buildRollingSection(nDays, label, nonRejectedDays, snap)`; both call sites (7-day, 14-day) updated to drop the trailing arg.
- `buildSectionHeaderRow(label, colCount)` call sites: `COLUMNS.length + TIF_COLUMNS.length` → `COLUMNS.length` (two call sites: inside `buildRollingSection`, and in `render()`'s All-time section).
- `tests/e2e/metrics.spec.js`'s `MET-02/MET-03` test: header count assertion `31` → `19`, plus new negative assertions that none of the 12 former TIF labels appear.

**Explicitly NOT touched:** `isTif`, `activityLog`, `computeTifTrimmedStats`, the `tifForecast()` historic-band override block, `minTifRow`/`medianTifRow`/`maxTifRow` (min-TIF/median-TIF/max-TIF aggregate rows) — all remain, per D-10, because they serve TIF functionality unrelated to the removed inline columns.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Delete TIF_COLUMNS and every rendering call site from metrics-screen.js</name>
  <files>js/ui/metrics-screen.js</files>
  <read_first>
    js/ui/metrics-screen.js (whole file — re-open before editing to get current line numbers, since Task 1 of plan 23-01 does not touch this file so line numbers should match this planning session's read, but confirm before each edit)
    .planning/phases/23-metrics-accuracy-column-migration/23-CONTEXT.md — D-09 (full clean removal, explicit list of 6 call sites), D-10 (isTif/activityLog retained, only tifBoundsMap plumbing removed)
    .planning/phases/23-metrics-accuracy-column-migration/23-PATTERNS.md — "D-09 Removal Checklist (metrics-screen.js)" section, items 1-10
  </read_first>
  <action>
    In `js/ui/metrics-screen.js`, in this order (later edits shift line numbers, so work top-to-bottom or re-grep after each edit):
    1. Delete the `computeTifBoundsHistory` import line (`import { computeTifBoundsHistory } from '../lib/accuracy-tif.js';`).
    2. Delete the `TIF_COLUMNS` constant and its preceding JSDoc comment block (the "TIF inline column definitions (MET-08, D-11)" comment through the closing `]);`).
    3. In `buildDayRow`: delete the "TIF inline cells (MET-08, D-11)" loop (`const tifEntry = tifBoundsMap...` through the closing `}` of the `for (const col of TIF_COLUMNS)` block). Change the function signature from `function buildDayRow(dayMetrics, snap, tifBoundsMap, isTif)` to `function buildDayRow(dayMetrics, snap)`. Update its JSDoc to remove the `@param {Map} tifBoundsMap` and `@param {boolean} isTif` lines.
    4. In `buildTifAggregateRow`: delete the "TIF inline columns — render '—' in aggregate rows" loop (`for (let j = 0; j < TIF_COLUMNS.length; j++) { ... }`).
    5. In `buildSectionHeaderRow`'s JSDoc: update the `@param {number} colCount` comment to no longer claim "= 30" (the exact new value depends on where it's called from; leave the description generic or say "= COLUMNS.length = 19" if a concrete example is wanted).
    6. In `buildRollingSection`: change the function signature from `function buildRollingSection(nDays, label, nonRejectedDays, snap, isTif)` to `function buildRollingSection(nDays, label, nonRejectedDays, snap)`. Update the `buildSectionHeaderRow(headerLabel, COLUMNS.length + TIF_COLUMNS.length)` call to `buildSectionHeaderRow(headerLabel, COLUMNS.length)`. Delete the `for (const row of [minRow, avgRow, maxRow]) { for (let j = 0; j < TIF_COLUMNS.length; j++) { ... } }` TIF placeholder-cell block. Update its JSDoc to remove the `@param {boolean} isTif` line and the sentence about "TIF placeholder cells (12) are appended...".
    7. In `render()`: delete the block computing `tifBoundsArray`/`tifBoundsMap` (`const tifBoundsArray = isTif ? computeTifBoundsHistory(days, snap, activityLog) : [];` and `const tifBoundsMap = new Map(...)`) — keep the preceding `const isTif = snap.forecastAlgorithm === 'tif';` and `const activityLog = isTif ? eventLog.getActivityLog() : {};` lines exactly as they are (D-10 — still needed by `computeTifTrimmedStats` and the `tifForecast` override block further down).
    8. In `render()`'s thead-building block: delete the "TIF inline column headers (MET-08, D-11)" loop (`for (const col of TIF_COLUMNS) { ... }`).
    9. In `render()`'s "7-day rolling"/"14-day rolling" calls: `buildRollingSection(7, '7-day rolling', nonRejectedDays, snap, isTif)` → `buildRollingSection(7, '7-day rolling', nonRejectedDays, snap)`, same for the 14-day call.
    10. In `render()`'s All-time summary section: change `buildSectionHeaderRow('All-time', COLUMNS.length + TIF_COLUMNS.length)` to `buildSectionHeaderRow('All-time', COLUMNS.length)`. Delete the `for (const row of [minRow, avgRow, maxRow]) { for (let j = 0; j < TIF_COLUMNS.length; j++) { ... } }` TIF placeholder-cell block that follows it (this is a SEPARATE block from the one in step 6 — same shape, different function).
    11. In `render()`'s per-day-rows loop: change `const dayRow = buildDayRow(rows[i], snap, tifBoundsMap, isTif);` to `const dayRow = buildDayRow(rows[i], snap);`.
    12. Do NOT touch: `isTif`, `activityLog`, `computeTifTrimmedStats` and its call, the `tifForecast(reversedDays, snap, activityLog)` historic-band override block, `minTifRow`/`medianTifRow`/`maxTifRow` construction and their `.hidden = !isTif` lines — all of these remain exactly as-is (D-10).
  </action>
  <verify>
    <automated>grep -c "TIF_COLUMNS\|tifBoundsMap\|tifBoundsArray" js/ui/metrics-screen.js</automated>
    <fails_when>the count is anything other than 0 — any remaining reference means D-09's "full clean removal" is incomplete</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "TIF_COLUMNS\|tifBoundsMap\|tifBoundsArray" js/ui/metrics-screen.js` returns 0
    - `grep -c "computeTifBoundsHistory" js/ui/metrics-screen.js` returns 0
    - `grep -c "isTif" js/ui/metrics-screen.js` returns a count >= 5 (still used by computeTifTrimmedStats gating, the historic-band override, and the three min/median/max-TIF row `.hidden` assignments — confirms D-10's "not removed" requirement)
    - `node --check js/ui/metrics-screen.js` (or loading the app via `npm run serve`) shows no syntax errors and the Metrics tab renders without console errors
  </acceptance_criteria>
  <done>metrics-screen.js contains zero references to TIF_COLUMNS/tifBoundsMap/tifBoundsArray/computeTifBoundsHistory, while isTif/activityLog/the TIF aggregate rows/the historic-band override remain fully intact and functional.</done>
</task>

<task type="auto">
  <name>Task 2: Update metrics.spec.js for the 19-column table and prove TIF labels are gone</name>
  <files>tests/e2e/metrics.spec.js</files>
  <read_first>
    tests/e2e/metrics.spec.js lines 22-82 (the MET-02/MET-03 test with the header-count assertion) and lines 423-466 (the now-obsolete "TIF placeholder cells hidden" test)
    js/ui/metrics-screen.js (as modified by Task 1) — confirm the final base `COLUMNS` array still has 19 entries and TIF_COLUMNS no longer exists
  </read_first>
  <action>
    In `tests/e2e/metrics.spec.js`:
    1. In the `'MET-02/MET-03: Table renders with correct columns and data'` test: change the comment block and assertion from `expect(count).toBe(31); // 19 base + 12 TIF inline columns` to `expect(count).toBe(19); // 19 base columns only — TIF columns moved to Accuracy screen per UI-11/D-09 (Phase 23)`.
    2. In the same test, immediately after the existing `headerTexts` assertions (Date/Wake/Sleep/S.Debt(7d)), add negative assertions that none of the 12 former TIF labels appear: `expect(headerTexts).not.toContain('W-min')`, and likewise for `W-max`, `W-conf`, `NS-min`, `NS-max`, `NS-conf`, `NE-min`, `NE-max`, `NE-conf`, `B-min`, `B-max`, `B-conf` (a `for...of` loop over an array of the 12 labels is acceptable instead of 12 separate lines).
    3. Delete the entire `'MET-10/boundary: TIF placeholder cells hidden when TIF is off'` test (lines ~423-466) — its premise (12 hidden `<td>` elements per row) no longer applies since those cells no longer exist at all, hidden or otherwise.
    4. Add one new test (e.g. `'UI-11: Metrics table has no TIF columns even when TIF algorithm is active'`) that seeds `forecastAlgorithm: 'tif'` with enough days for a normal render, navigates to Metrics, and asserts the same 19-column count and the same 12 negative label assertions as step 2 — proving removal holds in TIF mode too, not just Classic (the pre-existing MET-02/MET-03 seed uses `forecastAlgorithm: 'classic'`).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/metrics.spec.js</automated>
    <fails_when>non-zero exit code, or the summary line does not report every test in the file passing (including the updated MET-02/MET-03 assertion, the new UI-11 TIF-mode test, and confirmation that the deleted obsolete test no longer runs/fails)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "toBe(31)" tests/e2e/metrics.spec.js` returns 0
    - `grep -c "toBe(19)" tests/e2e/metrics.spec.js` returns >= 2 (Classic-mode and TIF-mode assertions)
    - `grep -c "TIF placeholder cells hidden when TIF is off" tests/e2e/metrics.spec.js` returns 0 (obsolete test removed)
    - `npx playwright test tests/e2e/metrics.spec.js` reports all tests passing
  </acceptance_criteria>
  <done>metrics.spec.js asserts 19 columns (not 31) in both Classic and TIF modes, proves none of the 12 former TIF labels remain anywhere in the Metrics table, and no longer contains a test whose premise (hidden TIF placeholder cells) was deleted in Task 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| localStorage-persisted event/settings data → DOM rendering in metrics-screen.js | Unchanged by this plan — this is a pure code-deletion plan touching only which columns render, not what data flows in. No new trust boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-04 | Tampering | `js/ui/metrics-screen.js` signature changes to `buildDayRow`/`buildRollingSection` | low | accept | Pure removal of now-unused parameters (`tifBoundsMap`, `isTif`-for-placeholder-cells); all remaining cell content continues to render via `textContent` only (T-11-05), unchanged from before this plan. |
| T-23-05 | Information Disclosure | Removal of TIF window data from the Metrics screen | low | accept | Reduces the surface where this data is rendered (now only on Accuracy screen, per plan 23-01) — a net reduction in duplication, not a new exposure. |
| T-23-06 | Denial of Service | Incomplete removal leaving a dangling reference to the deleted `TIF_COLUMNS` constant | medium | mitigate | Task 1's `<verify>` is a grep gate (`TIF_COLUMNS\|tifBoundsMap\|tifBoundsArray` count == 0) run before the E2E suite, catching a `ReferenceError` that would otherwise crash the Metrics screen's `render()` at runtime for every user. |

</threat_model>

<verification>
- `grep -c "TIF_COLUMNS\|tifBoundsMap\|tifBoundsArray\|computeTifBoundsHistory" js/ui/metrics-screen.js` returns 0.
- `npx playwright test tests/e2e/metrics.spec.js` — all tests pass, including the updated 19-column assertions and the new TIF-mode absence test.
- Manually confirm (via `npm run serve`) the Metrics screen renders without console errors in both Classic and TIF algorithm modes, and its table has no TIF-labeled columns in either mode.
</verification>

<success_criteria>
- `js/ui/metrics-screen.js`'s Metrics table has exactly 19 columns in all modes.
- `isTif`/`activityLog`/the TIF aggregate rows/the historic-band override remain functional and untouched.
- `tests/e2e/metrics.spec.js` proves the 12 TIF labels are absent in both Classic and TIF modes.
- No syntax errors, no console errors, full metrics.spec.js suite green.
</success_criteria>

<output>
Create `.planning/phases/23-metrics-accuracy-column-migration/23-02-SUMMARY.md` when done
</output>
