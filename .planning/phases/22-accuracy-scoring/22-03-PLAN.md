---
phase: 22-accuracy-scoring
plan: 03
type: execute
wave: 3
depends_on: ["22-01", "22-02"]
files_modified:
  - js/ui/accuracy-screen.js
  - tests/e2e/accuracy-screen.spec.js
autonomous: true
requirements: [ACC-04]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The Accuracy screen's classic grid renders a single score column (D-11: plain numbers, no percent suffix, no color-coded thresholds) across 6 rows: wake, napStart, napEnd, bedtime, bedtimeNapDay, bedtimeNoNapDay (D-09)."
    - "An overall headline score (D-10) is rendered above the per-event grid, reading computeAccuracy()'s overallScore field verbatim — never recomputed independently in the UI layer."
    - "A row whose approximatedCount is greater than zero is visually distinguished from a row with zero approximated scores (D-07); a napStart/napEnd row with total below settings.minDays still shows the existing cold-data dash (carried forward from D7-15)."
    - "All dynamic cell content in accuracy-screen.js is set via textContent or replaceChildren only — no innerHTML is introduced anywhere in the file (CLAUDE.md XSS guard, T-07-06-01 precedent)."
    - "The TIF accuracy table gains bedtimeNapDay/bedtimeNoNapDay rows sourced from computeTifAccuracy()'s extended result, rendered through the same buildTifAccuracyGrid helper used for the original 4 rows (D-05)."
  artifacts:
    - path: "js/ui/accuracy-screen.js"
      provides: "Rewritten ACCURACY_ROWS (6 rows)/ACCURACY_COLS (1 score column)/buildAccuracyGrid/renderAccuracy for the avgScore+overallScore shape, with an approximated-score visual marker; TIF_ACCURACY_ROWS extended with the bedtime split"
    - path: "tests/e2e/accuracy-screen.spec.js"
      provides: "Deterministic E2E coverage proving per-event scores, the bedtime nap-day split, and the overall headline render correctly from real seeded history, for both the classic and TIF algorithms"
  key_links:
    - from: "js/ui/accuracy-screen.js (render)"
      to: "js/lib/accuracy.js (computeAccuracy)"
      via: "direct function call; renders result.overallScore verbatim"
      pattern: "computeAccuracy\\("
    - from: "js/ui/accuracy-screen.js (renderTifAccuracy)"
      to: "js/lib/accuracy-tif.js (computeTifAccuracy)"
      via: "TIF_ACCURACY_ROWS iterating the 6-key extended result"
      pattern: "bedtimeNapDay"
  prohibitions:
    - statement: "MUST NOT use innerHTML anywhere in accuracy-screen.js for dynamic content (CLAUDE.md XSS guard, T-07-06-01 precedent, carried forward unchanged by this plan)."
      status: resolved
      verification: test
    - statement: "MUST NOT recompute the overall headline score independently inside accuracy-screen.js — it must read AccuracyResult.overallScore verbatim from computeAccuracy(), keeping the scoring arithmetic single-sourced in js/lib/accuracy.js (D-09/D-10's 'UI stays dumb' principle, carried from D-07)."
      status: resolved
      verification: judgment
---

<!-- planner-discipline-allow: innerHTML -->

<objective>
Rewrite `js/ui/accuracy-screen.js` to render the new score-based `AccuracyResult`/`TifAccuracyResult` shapes produced by Plans 22-01 and 22-02: a single "Avg Score" column across 6 rows (wake/napStart/napEnd/bedtime/bedtimeNapDay/bedtimeNoNapDay) replacing the old three-metric (within-max-delta / within-half-delta / inside-band) grid, a new overall headline score displayed above the grid (D-10), a visual marker distinguishing band-approximated scores (D-07), and the matching two new rows on the TIF accuracy table (D-05).

Purpose: this is the only user-visible surface of the whole phase — until this plan lands, the new scoring engines exist but nobody can see their output. `accuracy-screen.js` must stay "dumb" (render only, no scoring logic of its own) so the single source of truth for every number on screen remains `js/lib/accuracy.js`/`js/lib/accuracy-tif.js`.

Output: a rewritten Accuracy screen showing per-event-type average scores, the bedtime nap-day/no-nap-day split, an overall headline score, and an approximated-score visual marker — proven with real, deterministic E2E assertions against seeded history.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-accuracy-scoring/22-CONTEXT.md
@.planning/phases/22-accuracy-scoring/22-PATTERNS.md
@js/ui/accuracy-screen.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Classic accuracy grid rewrite — avgScore column, bedtime-split rows, overall headline, approximated-score marker (D-08/D-09/D-10/D-11)</name>
  <files>js/ui/accuracy-screen.js, tests/e2e/accuracy-screen.spec.js</files>
  <read_first>
    js/ui/accuracy-screen.js (full file, ~450 lines — ACCURACY_ROWS/ACCURACY_COLS at lines 38-59, buildAccuracyGrid at lines 146-208, mountAccuracyScreen/renderAccuracy at lines 334-437)
    tests/e2e/accuracy-screen.spec.js (full file, ~55 lines — the existing cold-start test this task must not break)
    tests/e2e/forecast.spec.js lines 15-96 (makeDb/makeEvents/makeBaselineDb/seedAndReload — the seed-helper pattern to duplicate locally in accuracy-screen.spec.js; this project has no shared e2e helper module, each spec file duplicates its own)
    .planning/phases/22-accuracy-scoring/22-CONTEXT.md D-08, D-09, D-10, D-11 (old-shape removal, per-event-type aggregate rendering, overall headline placement, plain-number display, Claude's-discretion note on the approximated marker's exact visual treatment)
    js/lib/accuracy.js (as rewritten by Plan 22-01 — the exact `AccuracyResult` shape: `{ wake, bedtime, bedtimeNapDay, bedtimeNoNapDay, napStart, napEnd: {total, avgScore, approximatedCount}, overallScore }`)
  </read_first>
  <behavior>
    RED — write this E2E test first, confirm it fails against the pre-rewrite screen:
    Seed `makeBaselineDb()` (32 consecutive days, all 4 event types at constant times: wake 06:30, napStart 13:00, napEnd 14:30, bedtime 21:00 — the existing `makeDb`/`makeEvents` helper pattern from tests/e2e/forecast.spec.js, duplicated locally) with default settings (`minDays: 7, maxDelta: 30, windowDays: 7`). Because every day is identical, `computeAccuracy` will retroactively predict every scored day (index 7..31) with D=0 for every event type, so this fixture is fully deterministic:
    1. Navigate to the Accuracy tab. Assert `.accuracyGrid` is visible (not cold-start).
    2. Assert a headline element is visible showing "100" somewhere in its text (the overall headline score — every scored day's per-day mean is exactly 100 since every event matches its forecast exactly).
    3. Assert the row labeled for `wake` shows a cell containing "100" with no approximated marker.
    4. Assert the row labeled for `bedtimeNapDay` shows a cell containing "100" — because `makeBaselineDb`'s every day has a nap (napStart is always set), so every scored bedtime day is classified nap-day.
    5. Assert the row labeled for `bedtimeNoNapDay` shows the existing dash/insufficient-data marker (its `total` is 0 — the fixture has zero no-nap days).
    6. Assert there is only ONE data column per row for the score (not three, as in the old withinDelta/withinHalfDelta/insideBand layout).
    Do not delete the existing cold-start test in tests/e2e/accuracy-screen.spec.js — it must keep passing unmodified (no data seeded, `.coldStartNote` still shown, `.accuracyGrid` still absent).
  </behavior>
  <action>
    Replace `ACCURACY_ROWS` with 6 entries in this order: `wake`/'Wake', `napStart`/'Nap Start', `napEnd`/'Nap End', `bedtime`/'Bedtime (combined)', `bedtimeNapDay`/'Bedtime (nap day)', `bedtimeNoNapDay`/'Bedtime (no nap)'.

    Replace `ACCURACY_COLS` with a single entry: `{ key: 'avgScore', header: 'Avg Score' }` (D-09/D-11 — one score metric, not three).

    Keep `NAP_TYPES` as `Set(['napStart', 'napEnd'])` unchanged (the existing D7-15 dash-gating for insufficient nap data still applies only to these two rows).

    Rewrite `buildAccuracyGrid(gridEl, result, snap)`: the header row now has one empty top-left cell plus exactly one column header cell (`ACCURACY_COLS[0].header`, currently "Avg Score") instead of three. For each of the 6 `ACCURACY_ROWS`, render the row label cell as today, then one data cell: when `showDash || rowResult.total === 0` (unchanged D7-15/zero-total logic), render the existing dash marker; otherwise render `rowResult.avgScore` as plain text via `pctEl.textContent = String(rowResult.avgScore)` (D-11 — no `%` suffix; `avgScore` is a decayed score, not a hit-rate percentage) followed by the existing `n=` sample-count sub-label (`countEl`) unchanged. When `rowResult.approximatedCount > 0`, append a small marker element (e.g. a `<span class="accApprox">*</span>`, muted/superscript styling left to your judgment per D-07's Claude's-discretion note) immediately after the score text, and set a flag so a single footnote line — e.g. "* n score(s) approximated from a wide probability-band midpoint" with the total approximated count summed across all 6 rows — is appended once below the grid when any row has `approximatedCount > 0` (never per-row duplicated footnotes).

    In `mountAccuracyScreen`, add a new fixed DOM element `headlineEl` (e.g. `document.createElement('p')`, `className = 'overallScoreHeadline'`) created once alongside `stageBadge`/`gridRoot` and inserted into the permanent structure between them (`root.replaceChildren(stageBadge, headlineEl, gridRoot)`). In `renderAccuracy(root, accuracy, snap)`, after restoring the permanent structure (if needed) and before `buildAccuracyGrid`, set `headlineEl.textContent = 'Overall accuracy: ' + accuracy.overallScore;` (D-10 — reads `overallScore` verbatim off the `computeAccuracy()` result; the UI performs no averaging of its own).

    Update the module-level JSDoc comment block (lines 1-25) to describe the new grid shape (1 score column, 6 rows including the bedtime split) and the new headline element; drop the stale "D7-14 — 4x3 grid" reference in favor of ACC-01..04/D-08..D-11.

    Add the new `<behavior>` E2E test to tests/e2e/accuracy-screen.spec.js, duplicating the `makeDb`/`makeEvents`/`makeBaselineDb`/`seedAndReload` helpers locally at the top of the file (matching tests/e2e/forecast.spec.js's exact pattern — this project has no shared e2e helper module).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/accuracy-screen.spec.js</automated>
    <fails_when>non-zero exit code, or the Playwright summary line reports any failed test</fails_when>
  </verify>
  <acceptance_criteria>
    - `js/ui/accuracy-screen.js`'s classic grid renders exactly one score column across the 6 rows wake/napStart/napEnd/bedtime/bedtimeNapDay/bedtimeNoNapDay
    - An overall headline element is present above the grid and its text is derived from `accuracy.overallScore` with no independent recomputation in this file
    - On the deterministic 32-day baseline fixture, the wake and bedtimeNapDay rows both show "100" and the bedtimeNoNapDay row shows the insufficient-data dash (zero no-nap days in the fixture)
    - The pre-existing cold-start E2E test still passes unmodified
    - `npx playwright test tests/e2e/accuracy-screen.spec.js` passes
  </acceptance_criteria>
  <done>The classic Accuracy screen renders per-event average scores (including the bedtime nap-day split) and a top-line overall headline score, proven end-to-end against real seeded history; the old three-metric grid is fully gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TIF accuracy table — bedtime nap-day split rows (D-05)</name>
  <files>js/ui/accuracy-screen.js, tests/e2e/accuracy-screen.spec.js</files>
  <read_first>
    js/ui/accuracy-screen.js (as rewritten by Task 1 — TIF_ACCURACY_ROWS at lines 65-70, TIF_ACCURACY_COLS at lines 77-81, buildTifAccuracyGrid at lines 227-305, renderTifAccuracy at lines 373-393)
    js/lib/accuracy-tif.js (as rewritten by Plan 22-02 — `computeTifAccuracy()`'s 6-key result including `bedtimeNapDay`/`bedtimeNoNapDay`)
    .planning/phases/22-accuracy-scoring/22-CONTEXT.md D-05
  </read_first>
  <behavior>
    RED — write this E2E test first, confirm it fails against the pre-Task-2 TIF table:
    Seed a fixture identical to Task 1's `makeBaselineDb()` but with `forecastAlgorithm: 'tif'` and the additional TIF-required settings fields already used elsewhere in this project's e2e specs for the TIF path (mirror `tests/e2e/tif.spec.js`'s settings-override pattern for `tifRollingDays`/`trimPct`/`precisionTarget` if that spec seeds them — read it to confirm the minimal required field set before writing this fixture). Navigate to the Accuracy tab. Assert the TIF accuracy table (`js/ui/accuracy-screen.js`'s `<table>` output) contains a row whose header cell text is "Bedtime (nap day)" and a row whose header cell text is "Bedtime (no nap)", in addition to the pre-existing Wake/Nap Start/Nap End/Bedtime rows. Do not assert exact numeric values for the TIF table in this test (TIF's window-bound arithmetic is not as trivially deterministic as the classic path's point-delta scoring) — only assert the two new rows are structurally present with the correct labels, proving D-05's split reached the rendered table.
  </behavior>
  <action>
    Extend `TIF_ACCURACY_ROWS` with two new entries appended after the existing `bedtime` row: `{ key: 'bedtimeNapDay', label: 'Bedtime (nap day)' }` and `{ key: 'bedtimeNoNapDay', label: 'Bedtime (no nap)' }`. Do not change `TIF_ACCURACY_COLS` (windowHit/avgWidthMin/highConf stay exactly as they are — D-05 only adds rows, the TIF metric shape itself is unchanged). `buildTifAccuracyGrid` already reads `stats[row.key]` generically via its existing loop over `TIF_ACCURACY_ROWS`, so no other code change is required in that function — the new rows render through the exact same cell-formatting logic (`—` for null/missing, `±N min` for avgWidthMin, `N%` for windowHit/highConf) as the original 4 rows.

    Update the module-level JSDoc comment describing `TIF_ACCURACY_ROWS` to note the D-05 bedtime split (now 6 rows, not 4).

    Add the new `<behavior>` E2E test to tests/e2e/accuracy-screen.spec.js.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/accuracy-screen.spec.js</automated>
    <fails_when>non-zero exit code, or the Playwright summary line reports any failed test</fails_when>
  </verify>
  <acceptance_criteria>
    - `TIF_ACCURACY_ROWS` has 6 entries including `bedtimeNapDay`/`bedtimeNoNapDay` with the labels "Bedtime (nap day)"/"Bedtime (no nap)"
    - The rendered TIF table (algorithm set to `'tif'` in settings) shows both new row labels alongside the original 4
    - The full accuracy-screen.spec.js suite (Task 1's classic-path test, the new TIF-path test, and the pre-existing cold-start test) passes
    - `npx playwright test tests/e2e/accuracy-screen.spec.js` passes
  </acceptance_criteria>
  <done>The TIF accuracy table renders the D-05 bedtime nap-day/no-nap-day split using the same rendering helpers as the original 4 rows, proven end-to-end with a seeded TIF-algorithm fixture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `js/lib/accuracy.js`/`js/lib/accuracy-tif.js` computed values -> DOM text | Every number/flag rendered by this plan (avgScore, overallScore, approximatedCount, windowHit/avgWidthMin/highConf) is a derived value computed over the app's own already-validated in-memory day-record/settings state; no new external input reaches accuracy-screen.js directly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-05 | Tampering | `js/ui/accuracy-screen.js` dynamic cell rendering | low | mitigate | All rendering in this task continues to use `textContent`/`replaceChildren`/`createElement`+`appendChild` exclusively (CLAUDE.md's dom.js guard) — no `innerHTML` is introduced anywhere in this plan's changes. |
| T-22-06 | Information Disclosure | Rendered accuracy scores / overall headline | low | accept | Scores describe only the user's own locally-stored prediction accuracy, already visible elsewhere in the app; no new PII surface and no network transmission (offline-first PWA). |
| T-22-SC | Tampering (Supply Chain) | npm/pip/cargo installs | n/a | accept | No package installs occur in this phase — Nightwatch is a vanilla-JS, no-dependency project (CLAUDE.md); the package-legitimacy gate does not apply. |
</threat_model>

## Artifacts this phase produces

- `ACCURACY_ROWS` (js/ui/accuracy-screen.js) — extended to 6 rows: wake, napStart, napEnd, bedtime, bedtimeNapDay, bedtimeNoNapDay
- `ACCURACY_COLS` (js/ui/accuracy-screen.js) — reduced to a single `avgScore` column (was 3: withinDelta/withinHalfDelta/insideBand)
- `headlineEl` (js/ui/accuracy-screen.js, `mountAccuracyScreen`) — new fixed DOM element rendering `computeAccuracy()`'s `overallScore` verbatim
- `TIF_ACCURACY_ROWS` (js/ui/accuracy-screen.js) — extended to 6 rows including `bedtimeNapDay`/`bedtimeNoNapDay`

<verification>
Run `npx playwright test tests/e2e/accuracy-screen.spec.js` after each task — must exit 0 with zero failures, including the pre-existing cold-start test. Optionally run the full `npm run test:e2e` once after Task 2 to confirm no unrelated regression (e.g. tests/e2e/tif.spec.js, tests/e2e/metrics.spec.js, which both touch computeTifBoundsHistory/computeTifAccuracy indirectly).
</verification>

<success_criteria>
- The classic Accuracy screen renders per-event-type average scores, the bedtime nap-day split, and an overall headline score sourced verbatim from computeAccuracy() (D-08/D-09/D-10/D-11).
- Band-approximated scores are visually distinguished from point-prediction scores (D-07).
- The TIF accuracy table renders the same bedtime split (D-05).
- No innerHTML is introduced; the XSS guard is preserved.
- The full tests/e2e/accuracy-screen.spec.js suite passes.
</success_criteria>

<output>
Create `.planning/phases/22-accuracy-scoring/22-03-SUMMARY.md` when done
</output>
