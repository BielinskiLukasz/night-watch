---
phase: 22-accuracy-scoring
plan: 02
type: tdd
wave: 2
depends_on: ["22-01"]
files_modified:
  - js/lib/accuracy-tif.js
  - tests/unit/accuracy-tif.test.js
autonomous: true
requirements: [ACC-04]

estimate:
  tokens: 27000
  raw_tokens: 27000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "computeTifBoundsHistory's per-day entry shape is unchanged — still exactly the wake/napStart/napEnd/bedtime keys tifForecast() actually returns. The D-05 bedtime split is applied only in computeTifAccuracy's counting stage, never in bounds history."
    - "computeTifAccuracy classifies a scored day's bedtimeNapDay vs bedtimeNoNapDay bucket using that day's own actual napStart != null — the identical rule accuracy.js's D-03 uses — never by which series tifForecast() happened to predict from."
    - "accuracy-tif.js does not import from js/lib/metrics.js (the pre-existing circular-import guard from CLAUDE.md is unchanged by this plan)."
    - "Every pre-existing test in tests/unit/accuracy-tif.test.js (12-day fixture, hit/miss, highConf, avgWidthMin, null-entry exclusion) still passes unmodified — the D-05 extension is additive only."
  artifacts:
    - path: "js/lib/accuracy-tif.js"
      provides: "ACCURACY_TIF_CONFIG.EVENT_TYPES extended with bedtimeNapDay/bedtimeNoNapDay; a new ACCURACY_TIF_CONFIG.BASE_EVENT_TYPES; computeTifAccuracy() fans bedtime bounds into both nap-day sub-buckets"
    - path: "tests/unit/accuracy-tif.test.js"
      provides: "New coverage for the bedtime nap-day/no-nap-day split and null-bounds exclusion on the new sub-buckets"
  key_links:
    - from: "js/lib/accuracy-tif.js (computeTifAccuracy)"
      to: "js/lib/accuracy-tif.js (ACCURACY_TIF_CONFIG.EVENT_TYPES)"
      via: "for...of loop over the frozen 6-key config array for counter init/result-building"
      pattern: "ACCURACY_TIF_CONFIG.EVENT_TYPES"
  prohibitions:
    - statement: "MUST NOT let computeTifBoundsHistory iterate the extended 6-key EVENT_TYPES list (it would look up entry.bedtimeNapDay/entry.bedtimeNoNapDay, which tifForecast() never returns, silently producing null bounds for real bedtime data) — it must keep iterating the 4-key BASE_EVENT_TYPES list only (D-05)."
      status: resolved
      verification: test
    - statement: "MUST NOT reintroduce an import from js/lib/metrics.js into accuracy-tif.js (circular-import guard, CLAUDE.md)."
      status: resolved
      verification: test
---

<objective>
Extend `js/lib/accuracy-tif.js`'s TIF retroactive backtesting engine with the same D-03 bedtime nap-day/no-nap-day split Plan 22-01 added to `js/lib/accuracy.js`, per D-05's explicit scope extension. TIF's `windowHit`/`avgWidthMin`/`highConf` metric is structurally different from `eventAccuracyScore` (interval containment, not point-distance decay) and is NOT changed by this plan — only `ACCURACY_TIF_CONFIG` and `computeTifAccuracy()`'s counting stage gain the two new bucket keys, fanned out from the existing single `bedtime` TIF bounds using the identical actual-day nap-status classification rule.

Purpose: keeps the two backtesting engines (classic and TIF) consistent — a user comparing the two algorithms' bedtime accuracy sees the same nap-day/no-nap-day breakdown in both, letting `accuracy-screen.js` (Plan 22-03) render both tables with the same row set.

Output: `ACCURACY_TIF_CONFIG.EVENT_TYPES` extended to 6 keys; `computeTifAccuracy()` returning `bedtimeNapDay`/`bedtimeNoNapDay` entries alongside the existing 4; `computeTifBoundsHistory()`'s entry shape untouched.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-accuracy-scoring/22-CONTEXT.md
@.planning/phases/22-accuracy-scoring/22-PATTERNS.md
@js/lib/accuracy-tif.js
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: accuracy-tif.js bedtime nap-day split (D-05) — bedtimeNapDay/bedtimeNoNapDay fan-out from the existing bedtime TIF bounds</name>
  <files>js/lib/accuracy-tif.js, tests/unit/accuracy-tif.test.js</files>
  <read_first>
    js/lib/accuracy-tif.js (full file, ~220 lines — ACCURACY_TIF_CONFIG at lines 36-38, computeTifBoundsHistory's entry-building loop at ~lines 97-137, computeTifAccuracy's counting loop at ~lines 162-220)
    tests/unit/accuracy-tif.test.js (full file, ~380 lines — the existing describe blocks this task extends, especially "null entry exclusion" at lines 348-380)
    .planning/phases/22-accuracy-scoring/22-CONTEXT.md D-05 (the scope-extension decision and its exact classification rule)
    .planning/phases/22-accuracy-scoring/22-PATTERNS.md (accuracy-tif.js section — EVENT_TYPES extension and nap-day classification pseudocode)
    js/lib/metrics.js line 467 (`const isNapDay = day.napStart != null;` — the precedent this plan's classification must match)
  </read_first>
  <behavior>
    RED — write these tests first, confirm they fail against the pre-existing implementation:
    1. Config shape: `ACCURACY_TIF_CONFIG.EVENT_TYPES` has exactly 6 entries including `bedtimeNapDay`/`bedtimeNoNapDay`; `ACCURACY_TIF_CONFIG.BASE_EVENT_TYPES` has exactly the original 4 (`wake`, `napStart`, `napEnd`, `bedtime`).
    2. Fan-out independence: one TIF bounds-history entry with `bedtime: { algMin: '21:00', algMax: '22:00', central: '21:30', precisionScore: 85 }` reused against TWO day records sharing that date's bounds — day A has `napStart` non-null (nap day) with bedtime actual inside the window (hit), day B (a different date, same bounds shape) has `napStart` null (no-nap day) with bedtime actual outside the window (miss). Assert `result.bedtime.windowHit.count === 1` (one hit total across both), `result.bedtimeNapDay.windowHit.count === 1` and `result.bedtimeNapDay.windowHit.pct === 100`, `result.bedtimeNoNapDay.windowHit.count === 0` and `result.bedtimeNoNapDay.windowHit.pct === 0`.
    3. avgWidthMin fan-out: extend the existing two-width fixture pattern (widths 120 and 60 -> avg 90) so one width belongs to a nap-day bedtime entry and the other to a no-nap-day entry; assert `result.bedtimeNapDay.avgWidthMin` and `result.bedtimeNoNapDay.avgWidthMin` are each computed independently over just their own subset (not blended with each other or with the combined `bedtime` average).
    4. Null-bounds exclusion (extends the existing describe block at lines 348-380): a history entry with `bedtime: null` — assert it is excluded from `bedtime`, `bedtimeNapDay`, AND `bedtimeNoNapDay` totals alike.
    5. computeTifBoundsHistory regression: reuse the existing 12-day-fixture test (buildFixture12) and add one assertion that every non-null `entry` in the result has exactly the 4 own keys `wake`/`napStart`/`napEnd`/`bedtime` — no `bedtimeNapDay`/`bedtimeNoNapDay` key ever appears on a bounds-history entry (pins that the split lives only in computeTifAccuracy).
  </behavior>
  <action>
    In `ACCURACY_TIF_CONFIG`: add `BASE_EVENT_TYPES: Object.freeze(['wake', 'napStart', 'napEnd', 'bedtime'])` (the four keys `tifForecast()` actually returns bounds for — used unchanged by `computeTifBoundsHistory`'s entry-building loop). Change the existing `EVENT_TYPES` key's value to `Object.freeze(['wake', 'napStart', 'napEnd', 'bedtime', 'bedtimeNapDay', 'bedtimeNoNapDay'])` — used only by `computeTifAccuracy` for counter initialization and result-building.

    In `computeTifBoundsHistory`'s entry-building loop, change the iteration source from `ACCURACY_TIF_CONFIG.EVENT_TYPES` to `ACCURACY_TIF_CONFIG.BASE_EVENT_TYPES` — this is the only change needed in this function; it keeps producing exactly the same 4-key entry shape it does today.

    In `computeTifAccuracy`: initialize the `counters` object over `ACCURACY_TIF_CONFIG.EVENT_TYPES` (now 6 keys), each `{ total: 0, windowHitCount: 0, widthSum: 0, highConfCount: 0 }` (unchanged per-key shape). In the per-entry loop, keep iterating `ACCURACY_TIF_CONFIG.BASE_EVENT_TYPES` (the 4 real keys present on `entry`) to fetch `bounds = entry[type]` and `actualSlot = actualDay[type]` exactly as today, with the existing null-bounds/null-actualSlot/null-actualMinutes/null-algMin-algMax guards unchanged. Immediately after the existing `type === 'bedtime'` increments (`c.total++`, conditional `windowHitCount++`, `widthSum +=`, conditional `highConfCount++`) succeed for a given day, compute `const isNapDay = actualDay.napStart != null;` (once per day, D-05's classification rule matching `js/lib/metrics.js`'s precedent exactly) and duplicate the SAME already-computed hit/width/highConf values into `counters[isNapDay ? 'bedtimeNapDay' : 'bedtimeNoNapDay']` (same `total++`, same conditional `windowHitCount++`, same `widthSum +=`, same conditional `highConfCount++` — do not recompute any of these values a second time, just apply them to the second counter object too).

    Build the returned result object by iterating `ACCURACY_TIF_CONFIG.EVENT_TYPES` (6 keys) — the existing `pct()` helper and per-key `{ windowHit: {count, pct}, avgWidthMin, highConf: {count, pct} }` shape stay exactly as they are today.

    Update the file's header comment to mention the D-05 bedtime split and the `BASE_EVENT_TYPES` vs `EVENT_TYPES` distinction; keep the existing circular-import guard comment (the "Do NOT import from metrics.js" note) exactly as-is.

    Rewrite tests/unit/accuracy-tif.test.js per the `<behavior>` block above, adding the new describe blocks WITHOUT deleting or modifying any pre-existing test — `computeTifBoundsHistory`'s entry shape and `computeTifAccuracy`'s base-4-type behavior are both unchanged by this task, so every existing assertion must keep passing verbatim.
  </action>
  <reversibility rating="costly">D-05 touches a module (accuracy-tif.js) not otherwise named in ACC-01..04's literal scope; reverting means removing the added buckets and their tests from a file this phase would not otherwise change. Already explicitly confirmed in CONTEXT.md D-05 as a deliberate scope extension — per planner-reversibility.md's "decision already made" rule, no runtime checkpoint is inserted.</reversibility>
  <verify>
    <automated>node --test tests/unit/accuracy-tif.test.js</automated>
    <fails_when>non-zero exit code, or the node:test end-of-run summary line reports a fail count greater than 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `ACCURACY_TIF_CONFIG.EVENT_TYPES` has exactly 6 entries; `ACCURACY_TIF_CONFIG.BASE_EVENT_TYPES` has exactly the original 4
    - `computeTifBoundsHistory`'s result entries still have exactly 4 own keys each (wake/napStart/napEnd/bedtime) — the split never leaks into bounds history
    - The nap-day/no-nap-day fan-out test proves independent bucketing: `bedtimeNapDay`'s and `bedtimeNoNapDay`'s counts do not overlap, and their totals sum to `bedtime.total` when every scored day is unambiguously classified
    - Every pre-existing test in tests/unit/accuracy-tif.test.js still passes unmodified
    - `node --test tests/unit/accuracy-tif.test.js` passes with zero failures
  </acceptance_criteria>
  <done>accuracy-tif.js's ACCURACY_TIF_CONFIG and computeTifAccuracy() support the D-05 bedtime nap-day/no-nap-day split using the identical D-03 classification rule; computeTifBoundsHistory's entry shape is unchanged; the metrics.js circular-import guard is preserved (still only `./forecast-tif.js` and `./forecast.js` imports).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | `computeTifAccuracy`/`computeTifBoundsHistory` remain pure functions over the app's own already-validated, locally-stored `dayRecords`/`settings`/`activityLog` state (offline-first, no network I/O). No new trust boundary is crossed by adding two counter buckets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-03 | Tampering | `ACCURACY_TIF_CONFIG` (js/lib/accuracy-tif.js) | low | accept | `Object.freeze()` prevents runtime mutation of both frozen arrays; no external input can reach this constant. |
| T-22-04 | Information Disclosure | `computeTifAccuracy()` return object | low | accept | The result describes only the user's own locally-stored TIF prediction accuracy, already visible elsewhere in the app; no new PII surface and no network transmission. |
| T-22-SC | Tampering (Supply Chain) | npm/pip/cargo installs | n/a | accept | No package installs occur in this phase — Nightwatch is a vanilla-JS, no-dependency project (CLAUDE.md); the package-legitimacy gate does not apply. |
</threat_model>

## Artifacts this phase produces

- `ACCURACY_TIF_CONFIG.BASE_EVENT_TYPES` (js/lib/accuracy-tif.js) — new frozen array, the 4 keys `tifForecast()` actually returns
- `ACCURACY_TIF_CONFIG.EVENT_TYPES` (js/lib/accuracy-tif.js) — extended frozen array, now 6 keys including `bedtimeNapDay`/`bedtimeNoNapDay`
- `computeTifAccuracy()` (js/lib/accuracy-tif.js) — result object gains `bedtimeNapDay`/`bedtimeNoNapDay` entries alongside the existing 4, each with the unchanged `{windowHit, avgWidthMin, highConf}` shape

<verification>
Run `node --test tests/unit/accuracy-tif.test.js` — must exit 0 with zero failures, including every pre-existing test.
</verification>

<success_criteria>
- `computeTifAccuracy()` reports `bedtimeNapDay`/`bedtimeNoNapDay` using the identical actual-day classification rule as accuracy.js's D-03.
- `computeTifBoundsHistory()`'s output is byte-identical to before this plan (same 4-key entry shape).
- The metrics.js circular-import guard is preserved.
- The full tests/unit/accuracy-tif.test.js suite passes.
</success_criteria>

<output>
Create `.planning/phases/22-accuracy-scoring/22-02-SUMMARY.md` when done
</output>
