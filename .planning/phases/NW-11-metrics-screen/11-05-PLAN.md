---
phase: 11
plan: 05
type: tdd
wave: 1
gap_closure: true
gap_ids: [G-NW-11-8, G-NW-11-16, G-NW-11-11]
depends_on: []
files_modified: [js/lib/metrics.js, tests/unit/metrics.test.js]
autonomous: true
requirements: [MET-01, MET-02, MET-03, MET-04, MET-05, MET-06]

must_haves:
  truths:
    - "No-nap days compute Combined Sleep+Nap, Activity Before/After Nap, Total Activity, and AAS using nap=0"
    - "AAS formula is totalActivity / combinedSleepNap; SAA formula is combinedSleepNap / prevDay.totalActivity"
    - "Time columns (Wake, Bedtime, Nap Start, Nap End) display formatted times (e.g. '07:30'), not colons"
  artifacts:
    - "js/lib/metrics.js with corrected metric functions and formula implementations"
    - "tests/unit/metrics.test.js with added test cases for no-nap days and corrected AAS/SAA formulas"
  key_links:
    - "combinedSleepNap with nap=0 → enables activityBeforeNap/activityAfterNap/totalActivity on no-nap days"
    - "combinedSleepNap in AAS/SAA formulas → matches user's intent (activity/combined, not activity/sleep alone)"
    - "aggregateMetrics storing full ISO → formatTime receives correct input format"
---

<objective>
Fix metric computation functions to handle no-nap days correctly, correct AAS/SAA formulas to use combined sleep+nap as denominator/numerator, and ensure time columns receive full ISO strings for proper formatting.

Purpose: Metrics table displays accurate values for all days (with and without naps), activity ratios reflect the user's intent, and time columns format correctly.
Output: metrics.js module with corrected functions + updated unit tests with full coverage
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@js/lib/metrics.js
@tests/unit/metrics.test.js
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Fix metric functions to handle no-nap days (G-NW-11-8)</name>
  <files>js/lib/metrics.js, tests/unit/metrics.test.js</files>
  <behavior>
    Test 1: combinedSleepNap returns sleepDuration when napDuration is null (no-nap case)
    Test 2: activityBeforeNap returns 0 when nap times are null
    Test 3: activityAfterNap returns 0 when nap times are null
    Test 4: totalActivity returns 0 when nap times are null (both activity segments = 0)
    Test 5: activityAfterSleepFactor returns null only when sleep is null or zero, not when nap is null
  </behavior>
  <action>
RED: Add test cases to tests/unit/metrics.test.js that explicitly test no-nap day behavior for: combinedSleepNap (should return sleep duration), activityBeforeNap (should return 0), activityAfterNap (should return 0), totalActivity (should return 0), and activityAfterSleepFactor (should compute using sleep only). Run tests; they will fail because the functions currently return null for no-nap days.

GREEN: Update js/lib/metrics.js functions to handle no-nap cases:
- combinedSleepNap: Add branch `if (sleep != null && nap == null) return sleep;` before the null check
- activityBeforeNap: Add branch `if (napStr == null) return 0;` (no nap means no time after wake spent napping, so activity = full wake-to-bedtime, but this is only the "before nap" segment; since there's no nap, before = 0 and after = dayLength)
  Actually, reconsider: "activity before nap" when there's no nap should be 0 (there is no nap to be "before"). "activity after nap" should also be 0. totalActivity = before + after = 0. This makes sense — no nap, no nap-related activity.
- activityAfterNap: Add branch `if (napEndStr == null) return 0;`
- totalActivity: Will automatically compute correctly once activityBeforeNap and activityAfterNap return 0 instead of null
- activityAfterSleepFactor: Already computes if totalActivity != null; with the fixes above, it will work for no-nap days (activity=0, sleep=X, result=0)

Run tests; they pass. Commit: `feat(11-05): handle no-nap days in metric functions`

REFACTOR: Review the logic for "activity before nap" on a no-nap day and ensure the semantics are correct per D11-04 requirements. Ensure comments in the code clarify the no-nap case.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/metrics.test.js 2>&1 | grep -c "passing"</automated>
  </verify>
  <done>All tests in metrics.test.js pass. Functions correctly compute combinedSleepNap, activityBeforeNap, activityAfterNap, totalActivity, and activityAfterSleepFactor for both nap and no-nap days. No-nap days return 0 for activity metrics (not null or em-dash).</done>
</task>

<task type="tdd">
  <name>Task 2: Fix AAS/SAA formulas to use combinedSleepNap (G-NW-11-16)</name>
  <files>js/lib/metrics.js, tests/unit/metrics.test.js</files>
  <behavior>
    Test 1: activityAfterSleepFactor(day) = totalActivity(day) / combinedSleepNap(day) (not sleepDuration)
    Test 2: sleepAfterActivityFactor(day, prevDay) = combinedSleepNap(day) / totalActivity(prevDay) (not sleepDuration)
    Test 3: Formulas work correctly when prevDay has a nap; when prevDay has no nap, SAA = sleep+0 / 0 = null (division by zero)
  </behavior>
  <action>
RED: Add test cases to tests/unit/metrics.test.js comparing the old formula (with sleepDuration) against the new formula (with combinedSleepNap). Test cases with mixed nap/no-nap days to verify the semantic difference. Run tests; they fail (functions currently use sleepDuration).

GREEN: Update js/lib/metrics.js:
- In activityAfterSleepFactor (lines 115-120): Replace `const sleep = sleepDuration(day);` with `const sleep = combinedSleepNap(day);`
- In sleepAfterActivityFactor (lines 128-134): Replace `const sleep = sleepDuration(day);` with `const sleep = combinedSleepNap(day);` AND replace `const prevActivity = totalActivity(prevDay);` in the division with the same variable (already correct, just ensure it stays as totalActivity).

Actually, looking at line 130: `const sleep = sleepDuration(day);` — change to `combinedSleepNap(day)`. And line 131: `const prevActivity = totalActivity(prevDay);` — already correct.

Run tests; they pass. Commit: `feat(11-05): correct AAS/SAA formulas to use combinedSleepNap`

REFACTOR: Ensure comments clarify the semantic (activity / combined, combined / activity).
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/metrics.test.js 2>&1 | grep -c "passing"</automated>
  </verify>
  <done>activityAfterSleepFactor and sleepAfterActivityFactor correctly use combinedSleepNap in their formulas. Tests verify the corrected behavior. Metrics accurately reflect activity-to-combined and combined-to-activity ratios.</done>
</task>

<task type="tdd">
  <name>Task 3: Fix aggregateMetrics to store full ISO timestamps (G-NW-11-11)</name>
  <files>js/lib/metrics.js, tests/unit/metrics.test.js</files>
  <behavior>
    Test 1: aggregateMetrics stores row.wake as day.wake?.at (full ISO string like 'YYYY-MM-DDTHH:MM') or null
    Test 2: aggregateMetrics stores row.bedtime, row.napStart, row.napEnd similarly (full ISO or null)
    Test 3: Rows with null time values are handled gracefully (formatTime receives null without breaking)
  </behavior>
  <action>
RED: Add test cases to tests/unit/metrics.test.js that verify aggregateMetrics output includes full ISO strings in time fields (not extractTime results). Run tests; they fail because aggregateMetrics currently stores extractTime() ('HH:MM').

GREEN: In js/lib/metrics.js, locate aggregateMetrics function (around lines 166-169 per UAT.md). Change:
- `row.wake = extractTime(day.wake);` → `row.wake = day.wake?.at ?? null;`
- `row.bedtime = extractTime(day.bedtime);` → `row.bedtime = day.bedtime?.at ?? null;`
- `row.napStart = extractTime(day.napStart);` → `row.napStart = day.napStart?.at ?? null;`
- `row.napEnd = extractTime(day.napEnd);` → `row.napEnd = day.napEnd?.at ?? null;`

This stores the full ISO string from the event object, which formatTime (in time.js) expects.

Run tests; they pass. Commit: `feat(11-05): store full ISO timestamps in aggregateMetrics rows`

REFACTOR: Ensure the rest of aggregateMetrics (avg, min, max calculations) correctly handle the full ISO strings or extracts time as needed for aggregation.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/metrics.test.js 2>&1 | grep -c "passing"</automated>
  </verify>
  <done>aggregateMetrics stores full ISO strings (or null) for wake, bedtime, napStart, napEnd instead of extractTime results. formatTime receives the correct input format and produces formatted times like '07:30' instead of ':'.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan modifies computation logic for user data; all data remains local (no network) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-05-01 | — | metrics.js logic | low | accept | Computation logic corrections do not introduce security concerns; math-only changes to internal metrics |
</threat_model>

<verification>
1. Run `npm run test:unit -- tests/unit/metrics.test.js` — all tests pass
2. Review js/lib/metrics.js to confirm functions handle no-nap cases, use combinedSleepNap in AAS/SAA, and store full ISO strings
3. Serve the app and navigate to Metrics screen with mixed nap/no-nap days; verify values compute correctly
</verification>

<success_criteria>
- All metric functions correctly handle no-nap days (returning 0 for activity, sleep duration for combined)
- AAS and SAA formulas use combinedSleepNap instead of sleepDuration
- aggregateMetrics stores full ISO timestamps for time columns
- Unit test suite passes with full coverage of corrected behaviors
</success_criteria>

<output>
Create `.planning/phases/NW-11-metrics-screen/11-05-SUMMARY.md` after execution
</output>
