---
phase: 11
slug: metrics-screen
status: draft
shadcn_initialized: false
preset: none
created: 2026-07-28
---

# Phase 11 — UI Design Contract

> Visual and interaction contract for the Metrics Screen — dedicated 5th-tab analytics dashboard showing per-day duration/activity/ratio metrics and historical aggregates.
> Generated by gsd-ui-researcher from upstream CONTEXT.md, RESEARCH.md, and REQUIREMENTS.md decisions.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none — vanilla HTML/CSS |
| Icon library | inline SVG in bottom-nav.js |
| Font | system-ui, -apple-system, "Segoe UI", Roboto, sans-serif |

**Rationale:** Project constraint (CLAUDE.md) — zero npm dependencies, no build step, no frameworks. Design system established in Phase 1–8 (calm palette, monospace numbers, sticky tables). Metrics Screen extends existing patterns from accuracy-screen.js and charts-screen.js (stageChip badge, filterDayRecordsByStage, subscribe/unsubscribe mount pattern).

---

## Visual Hierarchy

**Primary focal point:** Metrics table with sticky column headers and first-column anchor

The metrics table is the dominant visual element. Its persistent header (position: sticky; top: 0) and sticky first column (position: sticky; left: 0) establish clear navigation anchors for users scanning both vertically and horizontally. Summary rows (Average/Min/Max) sit above per-day rows in the same scroll container, creating a secondary hierarchy: aggregates first, then historical detail. Rejected days are visually muted (opacity 0.5) to de-emphasize outliers without hiding them. The stage filter badge appears above the table as a tertiary element, indicating active scope without competing for attention.

---

## Spacing Scale

Declared values (all in rem, matching existing CSS patterns — NOT strict 8-point):

| Token | Value | CSS | Usage |
|-------|-------|-----|-------|
| xs | 4px | 0.25rem | Icon gaps, cell padding micro-adjustments |
| sm | 8px | 0.5rem | Compact element spacing, badge padding |
| md | 12px | 0.75rem | Default cell padding |
| lg | 16px | 1rem | Section spacing, major container padding |
| xl | 24px | 1.5rem | Page margin-bottom, major section breaks |

Exceptions: 
- Sticky header/column z-index layering: use fixed `position: sticky; top: 0` (headers) and `position: sticky; left: 0` (first column) with `z-index: 2` (headers), `z-index: 1` (first col), `z-index: 3` (top-left corner cell when both sticky)
- Table cell padding: 8px 8px per existing historyTable pattern (D4-01, updated to 4-point grid alignment)
- Summary row separator: 2px solid border (visually distinct from 1px data row borders)

---

## Typography

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 0.9rem (14.4px) | 400 | 1.4 | Table cell text (times, durations, metrics) |
| Label | 0.8rem (12.8px) | 600 | 1.4 | Table column headers (abbreviated) |
| Subtext | 0.75rem (12px) | 400 | 1.4 | Min/Max cell date lines (2-line layout) |
| Display | 1.25rem (20px) | 600 | 1.4 | Screen heading (if used above table) |

**Special treatment:**
- **Monospace numbers:** All time and duration cells use `font-variant-numeric: tabular-nums` for vertical alignment (existing pattern from historyTable, forecast cards)
- **First column (row labels):** sticky, 0.9rem regular weight, color #334155
- **Summary rows:** font-weight 600 for avg/min/max labels in first column, distinguishing them from per-day rows

---

## Color

| Role | Value | Hex | Usage |
|------|-------|-----|-------|
| Dominant (60%) | Off-white background | #fafafa | Page body background |
| Dominant (60%) | White surfaces | #fff | Table background, cell fill |
| Secondary (30%) | Light slate | #f1f5f9 | Table header background (D4-01 pattern) |
| Secondary (30%) | Border color | #e2e8f0 | Table borders (1px), cell dividers |
| Accent (10%) | Indigo | #4f46e5 | Stage badge background, focus outlines |
| Accent muted | Indigo light | #eef2ff | Stage badge background (lighter variant) |
| Text primary | Dark gray | #1a1a1a | Cell text (times, durations, metrics) |
| Text secondary | Slate | #475569 | Column header text, row labels |
| Text tertiary | Light slate | #64748b | Subtext (date lines in Min/Max cells) |
| Text muted | Very light slate | #94a3b8 | Em-dash for missing slots (existing pattern) |
| Rejected row | Slight opacity | opacity 0.5 + rgba(0,0,0,0.025) bg | Outlier-flagged days (D11-05, D4-10) |
| Focus | Indigo outline | #4f46e5 | 2px solid outline with 2px offset |

**Accent reserved for:**
- Stage filter badge (stageChip background)
- Focus outlines on interactive elements
- Hover states on compact buttons (if added)

---

## Copywriting Contract

| Element | Copy | Decision |
|---------|------|----------|
| **Stage badge label** | "Viewing: {Stage Name}" | D11-09: read-only badge showing active stage; mirrors accuracy-screen pattern |
| **Screen heading (if added above table)** | "Metrics" | Not strictly required by MET-01..MET-06; added for clarity |
| **Tab label (bottom-nav)** | "Metrics" | 5th tab icon + label in bottom-nav.js |
| **Column headers (abbreviated, left-to-right)** | Date, Wake, Bedtime, Nap Start, Nap End, Sleep, Nap, Comb, Day Len, →Nap, Nap→, Act, AAS, SAA | D11-06 (Claude's discretion on exact abbreviations); this is the chosen set for conciseness |
| **Summary row labels (first column)** | "Average", "Min", "Max" | D11-12: clearly label aggregate rows |
| **Empty state (no data)** | "No logged days yet. Start logging sleep events to see metrics." | Applies when eventLog is empty; mirrors cold-start pattern |
| **Rejected day row hover/accessibility** | aria-label: "Rejected (outlier): {date}" | Assists screenreaders; rejected rows are dimmed but visible (D11-05) |
| **Missing nap cell content** | "—" (em-dash, color #94a3b8) | D11-04 + existing pattern: days with no nap show em-dash in nap-dependent columns |
| **Min/Max cell layout** | Line 1: value (e.g., "7h 30m"); Line 2: date in smaller text (0.75rem) | D11-14: both lines set via textContent in separate <span> elements within <td> |

---

## UI Considerations

Applicable state considerations resolved: **7 covered, 0 backstop, 0 unresolved**

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| Empty | Event log (zero days logged) | ✅ covered | "No logged days yet. Start logging sleep events to see metrics." rendered in cold-start note; table is hidden. No cold-start gate (D11-07) — render from day 1, but if day 1 is missing, empty state triggers. |
| No-nap day | Nap-dependent columns (Nap, Combined, Act→Nap, Nap→Bed, Activity, AAS, SAA) | ✅ covered | Em-dash ("—") rendered; nap-dependent rows excluded from Avg/Min/Max aggregates (D11-15). |
| Rejected day | Per-day row styling | ✅ covered | Dimmed opacity (0.5) + light background tint (rgba(0,0,0,0.025)); row remains visible and selectable (D11-05, D4-10 pattern). Excluded from aggregates (D11-13) but count as "previous day" anchor for SAA. |
| Stage filter active | Summary + per-day rows | ✅ covered | Both scoped to stage data via filterDayRecordsByStage(allDays, snap.stages \|\| [], snap.activeStageId) — 3-arg form (D11-10, Research Pitfall 1). Aggregates recomputed to stage scope. |
| Stage filter inactive | Summary + per-day rows | ✅ covered | All logged days included; no stage badge shown. |
| Sticky header scroll | Column headers remain pinned | ✅ covered | position: sticky; top: 0; z-index: 2; stays fixed during vertical scroll (D11-18). |
| Sticky first column scroll | Date + row labels remain pinned | ✅ covered | position: sticky; left: 0; z-index: 1; stays fixed during horizontal scroll (D11-17). Top-left corner cell (header or summary) uses z-index: 3 for both-axes stickiness (D11-19). |
| Long text (metric name) | Column headers | ✅ covered | Headers abbreviated to 3–4 chars (e.g., "Sleep", "Nap", "Comb", "Day Len", "Act", "AAS", "SAA"); monospace body text wraps naturally in cells if needed (rare — values are short durations/ratios). |
| Overflow (wide table) | Entire table container | ✅ covered | #metrics-screen uses overflow-x: auto (matching #history-screen pattern, D4-01). Horizontal scroll allowed on narrow screens (<640px). Summary rows scroll together with per-day rows (same scroll container, D11-11). |
| First day (no previous-day context) | SAA (Sleep-After-Activity) column | ✅ covered | Shows "—"; excluded from SAA aggregates (D11-16). First day has no ActivityPrevDay anchor. |

---

## Component Inventory

### Main Components

| Component | Role | Constraints |
|-----------|------|-------------|
| `.stageChip` badge | Stage filter indicator | Read-only; hidden when no stage active; uses existing CSS class from Phase 7 (D7-18). Renders "Viewing: {stageName}" via textContent only (T-07-06-01 security invariant). |
| `.metricsTable` (or `<table>`) | Per-day metrics grid | Single `<table>` element (not two separate tables) for column-width alignment (D11-01). Separate `<tbody>` for summary rows and per-day rows; CSS class distinguishes. |
| Summary rows (thead + tbody) | Aggregates (Avg/Min/Max) | Three rows: "Average", "Min", "Max" in first column; same columns as per-day rows. Rendered above per-day rows in visual order but inside same scroll container (D11-11). |
| Per-day rows | Day-level metrics | One row per logged day; most-recent-first order (D11-03). |
| Min/Max cell layout | Dual-line value + date | `<td>` contains value on line 1 (e.g., "7h 30m"), date on line 2 in `<small>` or `<span>` (0.75rem, color #64748b) via textContent (D11-14). |

### Styling Classes (CSS selectors)

| Class | Purpose | Inheritance |
|-------|---------|-------------|
| `.metricsScreen` (or #metrics-screen) | Screen root container | Inherits #app max-width 32rem, margin 0 auto; padding-bottom 64px (bottom-nav compensation, D7-04). |
| `.metricsToolbar` or similar | Optional toolbar above table (if added) | Flex row; padding-bottom 0.5rem (existing pattern from historyToolbar, D5-02). |
| `.metricsTable` | Main table element | border-collapse: collapse; font-size: 0.9rem; font-variant-numeric: tabular-nums (D11-08, D11-20). |
| `.metricsTable th` | Column header cells | background: #f1f5f9; border-bottom: 2px solid #e2e8f0; text-align: left; padding: 8px 8px; font-weight: 600; font-size: 0.8rem; position: sticky; top: 0; z-index: 2. (D11-18, D4-01 pattern) |
| `.metricsTable td` | Data cells | padding: 8px 8px; border-bottom: 1px solid #f1f5f9; color: #1a1a1a; vertical-align: middle. |
| `.metricsTable .col-date` (or equivalent for each column) | Date column (sticky) | text-align: left; white-space: nowrap; font-weight: 500; color: #334155; position: sticky; left: 0; z-index: 1; background: #fff. |
| `.metricsTable .col-{event-or-metric}` | Time/duration/ratio columns | text-align: right; white-space: nowrap; color: #1a1a1a. (Right-aligned for numeric values, D4-01 pattern) |
| `.metricsTable tr.metrics-summary-row` | Summary rows (Avg/Min/Max) | font-weight: 600 (row label in first col); background-color: #f8fafc or #fafafa (subtle, distinct from white data rows); border-top: 2px solid #e2e8f0 (separator, D11-11). |
| `.metricsTable tr.metrics-summary-row td` | Summary cells | font-weight: 600 (first column only); others: 400. |
| `.metricsTable tr.rejected td` | Rejected day styling | opacity: 0.5; background-color: rgba(0, 0, 0, 0.025). (D11-05, D4-10) |
| `.metricsTable td.min-max-cell` (or implicit) | Min/Max value-date cells | display: flex; flex-direction: column; gap: 0.1rem (or use line-break via `<br>` or `<small>` on line 2). |
| `.metricsTable .min-max-date` | Date text in Min/Max cell | font-size: 0.75rem; color: #64748b; font-weight: 400. |
| `.metricsTable td:empty::before` | Missing slot em-dash | content: '—'; color: #94a3b8. (Existing pattern from D4-01) |

### Special CSS Rules

- **Sticky corner (top-left header or summary label):** `.metricsTable th:first-child` or `.metricsTable td.col-date[data-row-type="summary"]` must use `position: sticky; left: 0; top: 0; z-index: 3`.
- **Responsive (mobile <640px):** Reduce padding to 6px 6px and font-size to 0.82rem (matching historyTable media query, D4-01).
- **Stage badge:** Use existing `.stageChip` class (Phase 7, D7-18); margin-bottom: 1rem; display: inline-flex.

---

## Data Formatting

### Durations (Sleep, Nap, Combined, Day Length, Act→Nap, Nap→Bed, Activity columns)

**Format:** `Xh Ym` (e.g., "7h 30m", "0h 45m", "0h 0m")

**Implementation (D11-20, D11-22):**
```js
function formatDuration(minutes) {
  const hours = Math.floor(minutes / 60);
  const mins = minutes % 60;
  return `${hours}h ${mins}m`;
}
```
- Location: new export in `js/lib/time.js` OR local to `js/ui/metrics-screen.js` (Claude's discretion)
- Apply to all duration columns in summary + per-day rows
- Average durations in summary row also formatted this way, rounded to nearest minute

### Ratios (AAS, SAA columns)

**Format:** Two decimal places (e.g., "1.85", "0.42")

**Implementation (D11-21):**
```js
function formatRatio(value) {
  return value === null ? '—' : value.toFixed(2);
}
```
- No unit suffix
- Null values show em-dash ("—")

### Times (Wake, Bedtime, Nap Start, Nap End columns)

**Format:** Respects user's timeFormat setting (12h/24h)

**Implementation (D11-08):**
```js
// Use existing formatTime() from js/lib/time.js
formatTime(eventTime, settings.timeFormat)  // e.g., "07:30" or "7:30 AM"
```

---

## Mount Pattern

The `mountMetricsScreen({ root, eventLog, settings })` function follows the established pattern from accuracy-screen.js and charts-screen.js:

### Signature
```js
export function mountMetricsScreen({ root, eventLog, settings }) {
  // Returns { unsubscribe() { sub1(); sub2(); } }
}
```

### Key Behaviors
1. **Adapter injection:** No direct `new Date()` or `localStorage` calls; uses injected adapters (eventLog, settings)
2. **Reactive render:** Subscribe to both eventLog and settings; re-render on any mutation
3. **XSS guard:** All dynamic content (stage name, formatted times, durations) set via textContent, never innerHTML (T-07)
4. **Stage filter:** Use `filterDayRecordsByStage(allDays, snap.stages || [], snap.activeStageId)` from day-bucket.js (3-arg form, Research Pitfall 1)
5. **Return value:** `{ unsubscribe() { ... } }` for lifecycle management

### Render Flow
1. Get day records: `eventLog.getAllDayRecords()` (or equivalent getter)
2. Get settings snapshot: `settings.getSnapshot()`
3. Filter by stage: `filterDayRecordsByStage(allDays, snap.stages || [], snap.activeStageId)`
4. Call `aggregateMetrics(filteredDays)` from `js/lib/metrics.js` to compute summary rows
5. Build table DOM:
   - `.stageChip` badge (if stage active)
   - Summary rows (Avg, Min, Max)
   - Per-day rows (most-recent-first)
6. Update root via `replaceChildren()`

---

## Edge Cases & Backstop Conditions

| Edge Case | Handling | Verification |
|-----------|----------|--------------|
| Day 1 (no previous-day context for SAA) | SAA shows "—"; excluded from SAA aggregates | aggregateMetrics() returns null for day 0's SAA |
| Days with no nap logged | Nap-dependent columns show "—"; excluded from nap aggregates (Nap, Combined, Act→Nap, Nap→Bed, AAS, SAA) | No-nap days identified by `napDuration(day) === null` |
| Rejected day (outlier flag) | Dimmed row (opacity 0.5, faint bg); excluded from all aggregates EXCEPT SAA computation (D11-13, may vary) | `day.rejected === true` check; filtered before aggregate math |
| All days rejected | Summary row hidden (or shown with all "—" values); per-day rows still visible but all dimmed | aggregateMetrics() returns empty/fallback aggregates |
| No days logged | Cold-start note + table hidden (D11-07: no gate, but empty state still rendered) | eventLog.getAllDayRecords().length === 0 |
| Very long stage name | Truncate or wrap in badge (clip-path / text-overflow) | Badge uses inline-flex, text-overflow: ellipsis if needed |
| Horizontal overflow on narrow screens | Table scrolls; sticky header + first col remain fixed (D11-17, D11-18) | CSS position: sticky + z-index layering |
| Vertical scroll (many days) | Sticky header remains at top (D11-18); summary rows scroll with data | position: sticky; top: 0 on `<th>` elements |

---

## Integration Checklist

**Files that must be created/modified (passed to planner):**

1. `js/ui/metrics-screen.js` — new file (mount function + render helpers)
2. `js/lib/metrics.js` — extend with `totalActivity()`, `activityAfterSleepFactor()`, `sleepAfterActivityFactor()`, `aggregateMetrics()` (if not already added in Phase 10)
3. `js/lib/time.js` — add `formatDuration(minutes)` export (or inline in metrics-screen.js per Claude's discretion)
4. `js/ui/bottom-nav.js` — add 'metrics' to VALID_TABS, add 5th entry to TABS array with SVG icon + label
5. `index.html` — add `<section id="metrics-screen" class="screen-section" hidden aria-label="Metrics"></section>`
6. `js/app.js` — add metricsScreenEl query, add to SCREENS map, import + call mountMetricsScreen()
7. `sw.js` — add `js/ui/metrics-screen.js` to PRECACHE_LIST
8. `tests/unit/sw-precache.test.js` — add js/ui/metrics-screen.js to expected precache list
9. `style.css` — add `.metricsScreen`, `.metricsTable *`, `.metrics-summary-row`, sticky column/header rules (if not covered by generic historyTable rules)
10. (Optional) `tests/unit/metrics.test.js` — unit tests for new metrics.js helpers
11. (Optional) `tests/e2e/metrics.spec.js` — E2E test for tab nav + table rendering

---

## Design Decisions Summary

| Decision ID | Decision | Source |
|-------------|----------|--------|
| D11-01 | Single wide horizontally-scrollable table (not named sections) | Phase 11 CONTEXT.md |
| D11-02 | Column order: Date \| Wake \| Bedtime \| Nap Start \| Nap End \| Sleep \| Nap \| Combined \| Day Length \| Act→Nap \| Nap→Bed \| Activity \| AAS \| SAA | Phase 11 CONTEXT.md |
| D11-03 | Rows ordered most-recent-first | Phase 11 CONTEXT.md |
| D11-04 | No-nap days render em-dash in nap-dependent columns | Phase 11 CONTEXT.md |
| D11-05 | Rejected days appear but dimmed (opacity 0.5) | Phase 11 CONTEXT.md |
| D11-06 | Column headers abbreviated (exact text: Claude's discretion) | Phase 11 CONTEXT.md — chosen: "Sleep", "Nap", "Comb", "Day Len", "→Nap", "Nap→", "Act", "AAS", "SAA" |
| D11-07 | No cold-start gate (render from day 1) | Phase 11 CONTEXT.md |
| D11-08 | Raw event times (Wake, Bedtime, Nap Start, Nap End) formatted via existing formatTime() | Phase 11 CONTEXT.md |
| D11-09 | Stage filter is read-only stageChip badge (same as Charts & Accuracy screens) | Phase 11 CONTEXT.md |
| D11-10 | When stage active, both summary + per-day rows scoped to stage data | Phase 11 CONTEXT.md |
| D11-11 | Summary mini-table above per-day rows in same scroll container | Phase 11 CONTEXT.md |
| D11-12 | Summary row labels: "Average", "Min", "Max" in first column | Phase 11 CONTEXT.md |
| D11-13 | Rejected days excluded from all aggregate calculations | Phase 11 CONTEXT.md |
| D11-14 | Min/Max cells show value line 1 + date line 2 (smaller text) | Phase 11 CONTEXT.md |
| D11-15 | Nap-dependent columns exclude no-nap days from Avg; Avg computed over nap-days only | Phase 11 CONTEXT.md |
| D11-16 | First day shows em-dash for SAA (no previous-day context); excluded from SAA aggregates | Phase 11 CONTEXT.md |
| D11-17 | First column sticky (left: 0) for horizontal scroll | Phase 11 CONTEXT.md |
| D11-18 | Column headers sticky (top: 0) for vertical scroll | Phase 11 CONTEXT.md |
| D11-19 | Top-left corner cell (summary header or per-day header) sticky both axes (z-index: 3) | Phase 11 CONTEXT.md |
| D11-20 | Duration format: `Xh Ym` (e.g., "7h 30m") | Phase 11 CONTEXT.md |
| D11-21 | Ratio format: 2 decimal places (e.g., "1.85") | Phase 11 CONTEXT.md |
| D11-22 | Average durations in summary rows also formatted as `Xh Ym`, rounded to nearest minute | Phase 11 CONTEXT.md |
| D11-23..26 | New metrics.js exports: totalActivity(), activityAfterSleepFactor(), sleepAfterActivityFactor(), aggregateMetrics() | Phase 11 CONTEXT.md |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

*UI-SPEC generated: 2026-07-28*  
*Phase: 11 — Metrics Screen*  
*Pre-populated from:** CONTEXT.md (23 decisions), REQUIREMENTS.md (6 requirements), existing style.css design system
