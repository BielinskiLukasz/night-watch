---
phase: NW-14
plan: 04
type: execute
wave: 2
depends_on:
  - 14-02
files_modified:
  - js/ui/accuracy-screen.js
autonomous: true
requirements:
  - TIF-14

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "when snap.forecastAlgorithm==='tif', accuracy-screen render() calls computeTifBoundsHistory + computeTifAccuracy and renders a TIF accuracy grid (TIF-14)"
    - "when snap.forecastAlgorithm==='classic', accuracy-screen renders the existing classic accuracy grid unchanged (TIF-14 non-regression)"
    - "TIF accuracy grid has 4 data rows (Wake, Nap Start, Nap End, Bedtime) and 3 stat columns (Window Hit %, Avg Width, High Conf %) (TIF-14)"
    - "Avg Width cells show '±N min' format; Window Hit and High Conf cells show 'N%' format (D-11)"
    - "null/absent stats render as '—' (D-08)"
    - "all cell content set via textContent — never innerHTML (T-11-05)"
    - "[ASSUMPTION TIF-14 activityLog] activityLog obtained via eventLog.getActivityLog() inside render() — same pattern as today-screen.js; no app.js changes needed"
    - "[ASSUMPTION TIF-14 no-nap] computeTifAccuracy on a dataset with no nap events returns null for napStart/napEnd windowHit, avgWidthMin, highConf; cells render as '—'"
    - "[ASSUMPTION TIF-14 window] computeTifAccuracy uses accuracy-tif.js with the same tifRollingDays window that computeTifBoundsHistory uses"
  artifacts:
    - "isTif branch in render() calling computeTifBoundsHistory + computeTifAccuracy + renderTifGrid"
    - "buildTifAccuracyGrid(stats, snap) helper function"
    - "computeTifBoundsHistory and computeTifAccuracy imported from ../lib/accuracy-tif.js"
  prohibitions:
    - "No fenced code blocks (```) in any <action> field"
    - "No innerHTML with computed values — textContent only (T-11-05)"
    - "Do not modify the classic accuracy grid code path — isTif branch is additive"
    - "Do not modify app.js or event-log.js — activityLog obtained inside render() from eventLog.getActivityLog()"

artifacts_produced:
  - "isTif branch in accuracy-screen.js render() — calls computeTifBoundsHistory and computeTifAccuracy, renders TIF accuracy grid"
  - "buildTifAccuracyGrid(stats, snap) private helper in accuracy-screen.js"
  - "computeTifBoundsHistory and computeTifAccuracy imported at top of accuracy-screen.js"
---

<objective>
Update `js/ui/accuracy-screen.js` to add a TIF accuracy grid when the TIF algorithm is active. When `snap.forecastAlgorithm === 'tif'`, the screen calls `computeTifBoundsHistory` and `computeTifAccuracy` from `accuracy-tif.js`, then renders a 4×3 grid (4 event types × 3 stats). The classic accuracy grid is shown when the algorithm is 'classic'. This plan addresses TIF-14.

Purpose: Users who opted into TIF need to evaluate how well the TIF algorithm performs — they need hit rate, width, and high-confidence percentage for each event type.

Output: accuracy-screen.js with isTif branch; buildTifAccuracyGrid helper; imports wired.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/NW-14-tif-metrics-accuracy-chart-fixes/14-CONTEXT.md
@.planning/phases/NW-14-tif-metrics-accuracy-chart-fixes/14-PATTERNS.md
@js/ui/accuracy-screen.js
@js/lib/accuracy-tif.js
@.planning/phases/NW-14-tif-metrics-accuracy-chart-fixes/14-02-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: isTif branch in render() — computeTifBoundsHistory + computeTifAccuracy wired end-to-end</name>
  <files>js/ui/accuracy-screen.js</files>

  <read_first>
    - js/ui/accuracy-screen.js — full file (289 lines): ACCURACY_ROWS/ACCURACY_COLS constants (lines 37–52); buildAccuracyGrid function (lines 123–185) as model for structure; render() function (lines 238–274); computeAccuracy call at line 270; mountAccuracyScreen signature at line 276
    - 14-CONTEXT.md §D-11 — TIF accuracy column names and data shape
    - 14-PATTERNS.md §"js/ui/accuracy-screen.js (MODIFY)" — isTif branch pattern and import additions
    - js/lib/accuracy-tif.js — computeTifBoundsHistory and computeTifAccuracy signatures (from Plan 02 output)
  </read_first>

  <action>
    Add TIF accuracy rendering to accuracy-screen.js via a minimal isTif branch. Wire it end-to-end so TIF accuracy data flows from computeTifBoundsHistory all the way to the DOM.

    1. ADD two imports at the top of accuracy-screen.js, after the existing import statements:
       import { computeTifBoundsHistory, computeTifAccuracy } from '../lib/accuracy-tif.js';

    2. IN render(), identify where computeAccuracy is called (line 270) and replace the single call with an isTif branch:
       const isTif = snap.forecastAlgorithm === 'tif';
       if (isTif) {
         const activityLog = eventLog.getActivityLog();
         const tifBoundsHistory = computeTifBoundsHistory(days, snap, activityLog);
         const tifStats = computeTifAccuracy(tifBoundsHistory, days);
         renderTifAccuracy(root, tifStats, snap);
       } else {
         // existing classic path — call computeAccuracy as before
         const accuracy = computeAccuracy(days, snap);
         renderAccuracy(root, accuracy, snap);
       }

       NOTE: 'renderTifAccuracy' and 'renderAccuracy' are refactored helpers you will extract in this task. The current render() body already has the DOM building logic — wrap it in a function renderAccuracy(root, accuracy, snap) that does what the current render() does after computing accuracy. renderTifAccuracy is built in Task 2.

       For this tracer, renderTifAccuracy can be a stub:
         function renderTifAccuracy(root, tifStats, snap) {
           const section = document.createElement('section');
           const h2 = document.createElement('h2');
           h2.textContent = 'TIF Accuracy';
           section.appendChild(h2);
           root.replaceChildren(section);
         }
       This stub lets the tracer prove the isTif wiring works end-to-end without building the full grid yet. Task 2 replaces the stub with the full buildTifAccuracyGrid.

    3. EXTRACT the current DOM-building logic from render() into renderAccuracy(root, accuracy, snap) so the classic path still works. The render() function now just: calls eventLog/settingsStore reads, branches on isTif, delegates to renderTifAccuracy or renderAccuracy.

    This tracer is production code (the isTif wiring and renderAccuracy extraction are permanent); only the renderTifAccuracy stub is temporary (replaced in Task 2).
  </action>

  <verify>
    npm run test:unit
  </verify>

  <acceptance_criteria>
    - import of computeTifBoundsHistory and computeTifAccuracy from '../lib/accuracy-tif.js' is present
    - render() contains `const isTif = snap.forecastAlgorithm === 'tif'` branch
    - when isTif: eventLog.getActivityLog() called inside render(); computeTifBoundsHistory called; computeTifAccuracy called
    - when classic: computeAccuracy called (existing path preserved)
    - renderAccuracy helper function exists and encapsulates the existing DOM logic
    - renderTifAccuracy stub function exists (temporary — to be replaced in Task 2)
    - npm run test:unit exits 0 (no existing tests broken)
  </acceptance_criteria>

  <done>
    The isTif wiring is end-to-end: TIF active → computeTifBoundsHistory → computeTifAccuracy → stub renders "TIF Accuracy" heading. Classic → existing accuracy grid unchanged. All unit tests pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>buildTifAccuracyGrid — full TIF accuracy grid replacing the renderTifAccuracy stub (TIF-14, D-11)</name>
  <files>js/ui/accuracy-screen.js</files>

  <read_first>
    - js/ui/accuracy-screen.js — current state after tracer task; focus on: buildAccuracyGrid function as model for table structure; ACCURACY_ROWS/ACCURACY_COLS constants as reference for frozen-constant pattern; renderTifAccuracy stub to be replaced
    - 14-CONTEXT.md §D-11 — 3 stat column labels and cell format rules (±N min, N%, '—')
    - 14-PATTERNS.md §"js/ui/accuracy-screen.js (MODIFY)" — buildTifAccuracyGrid pattern and TIF_ACCURACY_ROWS/TIF_ACCURACY_COLS constants
  </read_first>

  <action>
    Replace the renderTifAccuracy stub with a full implementation.

    1. ADD two frozen constants before buildTifAccuracyGrid (model them on ACCURACY_ROWS/ACCURACY_COLS):
       TIF_ACCURACY_ROWS — 4 entries: { key: 'wake', label: 'Wake' }, { key: 'napStart', label: 'Nap Start' }, { key: 'napEnd', label: 'Nap End' }, { key: 'bedtime', label: 'Bedtime' }
       TIF_ACCURACY_COLS — 3 entries: { key: 'windowHit', label: 'Win Hit %' }, { key: 'avgWidthMin', label: 'Avg Width' }, { key: 'highConf', label: 'High Conf %' }
       Apply Object.freeze to each.

    2. ADD buildTifAccuracyGrid(stats, snap) function:
       stats is the return value of computeTifAccuracy: { wake: { windowHit: number|null, avgWidthMin: number|null, highConf: number|null }, napStart: {...}, napEnd: {...}, bedtime: {...} }
       Build a table element. thead row: first th = 'Event', then one th per TIF_ACCURACY_COLS column.
       tbody: one tr per TIF_ACCURACY_ROWS row.
         First td = row.label; textContent only.
         Then one td per TIF_ACCURACY_COLS column:
           stats[row.key] may be null/undefined (no data for that event type).
           eventStats = stats[row.key] ?? null
           cellValue = eventStats ? eventStats[col.key] : null
           if cellValue is null: td.textContent = '—'
           else if col.key === 'avgWidthMin': td.textContent = '±' + Math.round(cellValue) + ' min'
           else: td.textContent = cellValue + '%'
           All via textContent — T-11-05.
       Return the table element.

    3. REPLACE renderTifAccuracy stub with full implementation:
       function renderTifAccuracy(root, tifStats, snap) {
         const section = document.createElement('section');
         section.className = 'accuracy-section';
         const h2 = document.createElement('h2');
         h2.textContent = 'TIF Accuracy';
         section.appendChild(h2);
         const grid = buildTifAccuracyGrid(tifStats, snap);
         section.appendChild(grid);
         root.replaceChildren(section);
       }

    The snap parameter is accepted in buildTifAccuracyGrid signature for future extension (e.g. time format) but not currently used.
  </action>

  <verify>
    npm run test:unit
  </verify>

  <acceptance_criteria>
    - TIF_ACCURACY_ROWS and TIF_ACCURACY_COLS frozen constants exist in accuracy-screen.js
    - buildTifAccuracyGrid function exists and returns a table element
    - renderTifAccuracy is no longer a stub — it calls buildTifAccuracyGrid
    - avgWidthMin cells formatted as '±N min' (Math.round then prepend '±', append ' min')
    - windowHit and highConf cells formatted as 'N%'
    - null values render as '—'
    - no innerHTML used for cell content (all textContent)
    - npm run test:unit exits 0
  </acceptance_criteria>

  <done>
    When TIF is active, the Accuracy screen shows a 4×3 TIF accuracy grid with Window Hit %, Avg Width (±N min), and High Conf % per event type. Null stats show '—'. Classic path unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| computeTifAccuracy output → DOM cells | Computed numbers/nulls flow into textContent-only cell setters |
| eventLog.getActivityLog() → computeTifBoundsHistory | Internal store data; already validated by event-log.js schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04-01 | XSS (Injection) | TIF accuracy grid cell rendering | low | mitigate | All cell values set via td.textContent only; windowHit/highConf are integers, avgWidthMin is rounded integer — no user-controlled string in DOM |
| T-14-04-02 | Tampering | isTif branch bypasses computeAccuracy when algorithm swapped | low | mitigate | Branch is clean: exactly one of computeAccuracy / computeTifBoundsHistory+computeTifAccuracy runs per render(); settings subscription triggers re-render on algorithm change |
| T-14-04-03 | Tampering | renderAccuracy refactor breaks classic accuracy path | low | mitigate | renderAccuracy extraction is a pure move of existing code; no logic change; existing test suite validates classic path behavior |
| T-14-04-SC | Tampering | npm installs | low | accept | No new packages — vanilla JS constraint |
</threat_model>

<verification>
npm run test:unit
</verification>

<success_criteria>
- When snap.forecastAlgorithm==='tif': computeTifBoundsHistory called, computeTifAccuracy called, TIF accuracy grid rendered with 4 event-type rows and 3 stat columns
- When snap.forecastAlgorithm==='classic': existing computeAccuracy path runs unchanged
- Avg Width cells show '±N min'; Win Hit % and High Conf % cells show 'N%'
- Null stats render as '—'
- All cell content via textContent — no innerHTML
- npm run test:unit exits 0
</success_criteria>

<output>
Create .planning/phases/NW-14-tif-metrics-accuracy-chart-fixes/14-04-SUMMARY.md when done.
</output>
