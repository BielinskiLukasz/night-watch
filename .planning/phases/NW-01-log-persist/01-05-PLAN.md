---
plan_id: 01-05
phase: 1
plan: 5
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
files_modified:
  - tests/integration/persistence.test.js
  - tests/integration/security-smoke.test.js
  - README.md
  - .github/workflows/ci.yml
autonomous: false
requirements:
  - DATA-04
  - PLAT-08
  - PLAT-09
  - PLAT-10
  - PLAT-11
mitigates:
  - T-01
  - T-02
  - T-03
  - T-04
  - T-05
  - T-06
  - T-07
  - T-08

must_haves:
  truths:
    # Phase gate hardening — every truth from Plans 01..04 must have a regression guard
    - "After app reload the persisted blob round-trips losslessly (D-04 + D-05 invariant guarded by integration test)"
    - "A corrupted (non-JSON) blob in localStorage produces console.warn + null and the app behaves as if empty (T-03 mitigation tested)"
    - "Zero runtime dependencies: package.json dependencies field is literal {} (T-08 mitigation tested)"
    - "No fetch(, XMLHttpRequest, or external <script src=> references in js/ (T-04 mitigation tested)"
    - "No new Date() outside js/adapters/clock-system.js + the one documented exemption in js/ui/manual-entry.js (T-04 clock-seam invariant tested)"
    - "No localStorage references outside js/adapters/storage-local.js (D-07 storage-seam invariant tested)"
    - "No .innerHTML = with non-empty-string interpolation in js/ (T-07 XSS-prevention tested)"
    - "D-22 coverage audit: every LOG-01..09, DATA-04 requirement has at least one passing automated test referenced in 01-05-SUMMARY.md (PLAT-11 discipline)"
    - "CI workflow is green on push AND uploads playwright-report artifact on failure for triage (D-21)"
    - "README.md describes how to run the app locally + run the test suite (developer dogfooding readiness)"
  artifacts:
    - path: "tests/integration/persistence.test.js"
      provides: "Round-trip lossiness check (JSON.parse(JSON.stringify(db)) === db) + corrupted-blob graceful handling + storage seam check"
      min_lines: 40
    - path: "tests/integration/security-smoke.test.js"
      provides: "Repo-wide invariant smokes: zero deps, no fetch/XHR in js/, no new Date() outside adapters, no localStorage outside adapter, no innerHTML with user data"
      min_lines: 60
    - path: "README.md"
      provides: "Local dev instructions: node scripts/serve.js, npm test, test layout overview, REQ-ID commit convention"
      min_lines: 40
    - path: ".github/workflows/ci.yml"
      provides: "Same workflow extended with explicit dependency-count assertion step before running tests"
  key_links:
    - from: "tests/integration/security-smoke.test.js"
      to: "package.json"
      via: "fs.readFileSync('./package.json') → JSON.parse → assert Object.keys(parsed.dependencies).length === 0"
      pattern: "dependencies"
    - from: "tests/integration/security-smoke.test.js"
      to: "js/"
      via: "fs walk + grep for fetch(, XMLHttpRequest, <script src=, new Date(), localStorage, innerHTML"
      pattern: "readdirSync|walkSync"
    - from: ".github/workflows/ci.yml"
      to: "tests/integration/security-smoke.test.js"
      via: "CI runs the smoke before functional tests (fail-fast on supply-chain or seam violation)"
      pattern: "node --test"
---

<objective>
Close Phase 1 with a hardening pass: write regression-guard tests for every architectural invariant Plans 01-04 introduced, audit D-22 coverage (every LOG-* and DATA-04 requirement has at least one automated test), update CI to fail-fast on supply-chain violations, and write a developer README. After this plan, Phase 1 is shippable: the user can dogfood locally; the test scaffold catches regressions before they reach `main`.

Purpose: Prove the Walking-Skeleton invariants from `01-SKELETON.md` are still true after Plans 02-04 extended the slice. Specifically guard the six mitigations T-01..T-08 with explicit assertions in `tests/integration/security-smoke.test.js`. Verify D-22 coverage via a meta-audit captured in the summary. This plan does NOT introduce new user-visible behavior — it locks in what's already shipped.

Output: Two new integration test files (persistence + security-smoke), an extended CI workflow with a supply-chain assertion step, a developer README, and a final human-verify checkpoint that the phase is shippable.
</objective>

## Phase Goal

**As a** Nightwatch maintainer (the user, wearing a different hat), **I want to** have automated regression guards for every Phase 1 architectural decision so future plans cannot silently regress the zero-deps invariant, the storage/clock seams, or the XSS-prevention convention.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/NW-01-log-persist/01-CONTEXT.md
@.planning/phases/NW-01-log-persist/01-RESEARCH.md
@.planning/phases/NW-01-log-persist/01-PATTERNS.md
@.planning/phases/NW-01-log-persist/01-VALIDATION.md
@.planning/phases/NW-01-log-persist/01-SKELETON.md
@.planning/phases/NW-01-log-persist/01-01-SUMMARY.md
@.planning/phases/NW-01-log-persist/01-02-SUMMARY.md
@.planning/phases/NW-01-log-persist/01-03-SUMMARY.md
@.planning/phases/NW-01-log-persist/01-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: tests/integration/persistence.test.js (D-04/D-05 round-trip + T-03 corrupted blob handling)</name>
  <files>tests/integration/persistence.test.js</files>
  <read_first>
    - .planning/phases/NW-01-log-persist/01-CONTEXT.md §decisions D-04 (canonical JSON shape), D-05 (localStorage byte-for-byte equals canonical JSON — load-bearing invariant)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Common Pitfalls #4 (QuotaExceededError + corrupted blob handling)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Failure Categories #6 (Round-trip lossiness) + #7 (localStorage corruption tolerance)
    - .planning/phases/NW-01-log-persist/01-VALIDATION.md §Failure Categories Sampled
    - js/adapters/storage-memory.js (Plan 01 — for round-trip test setup)
    - js/store/event-log.js (current Plan 04 state — all CRUD methods)
  </read_first>
  <behavior>
    Create tests/integration/persistence.test.js with these assertions:

    Round-trip lossiness (D-04 + D-05 invariant — RESEARCH §Failure Categories #6):
    - 'JSON.parse(JSON.stringify(db)) deep-equals db for a populated event log of 10 events spanning 3 days' (canonical JSON contract; Phase 5 import/export must round-trip this)
    - 'storage-memory snapshot after 10 mutations exactly equals {version:1, events:[...]}' (D-04 shape)
    - 'after a fresh createEventLog reads from the same storage, every method (listEvents, daysByCalendar, daysBySubjectiveNight) returns identical output to the pre-reload state' (D-05 invariant)

    Corrupted blob tolerance (T-03 mitigation):
    - 'createEventLog over a memory adapter pre-seeded with a non-JSON string in its blob produces an empty event log (no throw)'
       - Setup: createStorageMemory with `blob` set to a raw string (bypass JSON.parse normally) — actually, since storage-memory uses JSON.parse(JSON.stringify) it can't hold a non-JSON string. Use a small custom test-only adapter that returns the raw string from load(): `{ load: () => '{ bad json }', save: () => {} }`. Verify createEventLog throws? OR document that storage-local catches this — the corruption resilience is in storage-local, not the store.
       - PIVOT: this test belongs in a unit test on storage-local, using a fake `localStorage` global. node:test supports mock.method to stub localStorage. Test that `createStorageLocal('key').load()` with localStorage.getItem returning a non-JSON string emits a console.warn (via mock.method(console, 'warn')) AND returns null.
    - 'createStorageLocal load() with corrupted blob returns null AND calls console.warn' (T-03)
    - 'createStorageLocal save() throws translated Error on QuotaExceededError' (Pitfall #4 — Phase 1 acceptable behavior is loud failure)

    Schema version guard (D-04):
    - 'createEventLog over a storage with version:99 in its blob throws /Unsupported schema version/' (forward-compat guard)

    For the localStorage stubbing, use:
    ```javascript
    import { test, mock } from 'node:test';
    test('...', () => {
      const fakeLS = { _store: { 'k': 'not-json' }, getItem(k) { return this._store[k] ?? null }, setItem(k, v) { this._store[k] = v } };
      // inject by passing fakeLS as a constructor arg if createStorageLocal supports it,
      // OR use globalThis.localStorage = fakeLS (Node 22+ allows global override)
    });
    ```
    The Plan 01 createStorageLocal might use `localStorage.getItem` referring to the global; if so, set `globalThis.localStorage = fakeLS` before each test and restore after.

    Decision point for executor: if Plan 01's storage-local closes over the global `localStorage` and can't be tested in pure Node without setting `globalThis.localStorage`, choose ONE of:
      (a) refactor createStorageLocal to accept an optional `{ ls }` second argument that defaults to globalThis.localStorage
      (b) use globalThis.localStorage = fakeLS in the test
    Recommend (a) — keeps the seam testable without polluting globals; documents the dependency injection cleanly. Add a small note to the integration test header.
  </behavior>
  <action>
    Read the current js/adapters/storage-local.js to determine whether it accepts an injected `localStorage` or relies on the global. If global-only, refactor to accept `createStorageLocal(key, ls = globalThis.localStorage)` — a backward-compatible signature change that takes the global by default but allows tests to inject a fake.

    Create tests/integration/persistence.test.js with the assertions above. Use the fakeLS approach via injected `ls` if the refactor was applied.

    Run `node --test tests/integration/persistence.test.js`. Must be green.

    Commit: `test(NW-01): DATA-04 + T-03 persistence round-trip + corrupted blob + schema-version guard per D-04,D-05`.

    Anti-patterns to avoid:
    - DO NOT swallow the QuotaExceededError in this test — Phase 1's contract is loud failure (Pitfall #4 + RESEARCH §Anti-Patterns)
    - DO NOT skip the schema-version check — Phase 5 import will need this guard
    - DO NOT mutate globalThis.localStorage permanently (use beforeEach/afterEach if you must)
  </action>
  <verify>
    <automated>
      node --test tests/integration/persistence.test.js
      AND grep -q "Unsupported schema version\|version:" tests/integration/persistence.test.js
      AND grep -q "QuotaExceededError\|Storage full" tests/integration/persistence.test.js
      AND grep -q "console.warn\|mock.method" tests/integration/persistence.test.js
    </automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/integration/persistence.test.js` exits 0 with at least 5 assertions passing
    - Test file covers: round-trip lossiness (D-04/D-05), corrupted blob (T-03), QuotaExceededError translation (Pitfall #4), schema version guard (D-04)
    - If storage-local was refactored, signature remains backward compatible (the no-arg call site in js/app.js still works)
    - All previous tests still pass (`npm test` chained suite green)
  </acceptance_criteria>
  <done>
    `npm test` green; D-04 + D-05 invariants explicitly guarded; T-03 mitigation observable.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: tests/integration/security-smoke.test.js (T-04, T-07, T-08, clock-seam, storage-seam)</name>
  <files>tests/integration/security-smoke.test.js</files>
  <read_first>
    - .planning/phases/NW-01-log-persist/01-CONTEXT.md §decisions D-07 (adapter seams), D-09 (no framework / no build), D-20 (package.json is dev-only)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Security Domain (V5 + V7 + V8 + V14 ASVS L1 controls)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Anti-Patterns (textContent over innerHTML, new Date() only in clock adapters, localStorage only in storage adapter)
    - .planning/phases/NW-01-log-persist/01-VALIDATION.md §Security Validation
    - .planning/phases/NW-01-log-persist/01-SKELETON.md "What's NOT in the Skeleton" (Plan 05 hardens these via smoke)
    - package.json (verify "dependencies": {} invariant; T-08)
    - All files under js/ (the smoke walks this directory)
  </read_first>
  <behavior>
    Create tests/integration/security-smoke.test.js as a single test file using `node:test` + node built-ins (fs/promises, path, child_process). Six assertions:

    1. **T-08 / D-20 — Zero runtime dependencies**
       - Read package.json; assert `JSON.parse(...).dependencies` is `{}` (literal empty object) — NOT undefined, NOT missing, explicitly empty
       - Assert `JSON.parse(...).devDependencies['@playwright/test']` is a non-empty version string
       - Assert there are no other devDependencies in v1 (just Playwright)

    2. **T-04 — No external network in js/**
       - Walk `js/` recursively reading every `.js` file
       - Assert no file contains `fetch(`, `XMLHttpRequest`, `new WebSocket(`, `new EventSource(`, `import(` (dynamic), or `<script src=` (this last is more for HTML — also check `index.html`)
       - Allow exemption-tagged lines: any line containing the literal comment `// gsd:allow-network` is skipped (Phase 1 should have ZERO such tags, but the gate supports future opt-out with justification)

    3. **T-04 (clock-seam invariant) — No new Date() outside clock adapters**
       - Walk `js/`; for each file NOT in `js/adapters/clock-system.js` or `js/adapters/clock-fixed.js`, assert no regex `/\bnew Date\(\)/` match
       - Allow one greppable exemption: lines containing `// gsd:allow-ui-clock` (Plan 04's manual-entry.js prefill is the documented exemption — verify it carries this exact tag)

    4. **D-07 (storage-seam invariant) — No localStorage outside storage-local.js**
       - Walk `js/`; for each file NOT `js/adapters/storage-local.js`, assert no regex `/\blocalStorage\b/` match

    5. **T-07 (XSS-prevention) — No .innerHTML = with non-empty data**
       - Walk `js/`; for each `.js` file, search for `.innerHTML\s*=\s*` and inspect each match
       - Allow only literal empty string assignments: `.innerHTML = ""` or `.innerHTML = ''`
       - Reject any `.innerHTML = ` followed by a template literal, variable, or non-empty string literal
       - Recommend `node.replaceChildren()` in violation messages

    6. **T-01 / Phase 1 invariants — adapter file boundary stats**
       - Assert `js/adapters/clock-system.js` is the ONLY file in `js/` matching `/\bnew Date\(\)/` regex outside of test files
       - Assert `js/adapters/storage-local.js` is the ONLY file in `js/` matching `/\blocalStorage\b/` regex

    Implementation hint: use `fs.promises.readdir({ withFileTypes: true, recursive: true })` (Node 22+) to walk js/. Filter for `.endsWith('.js')`. Read each file as utf-8. Apply the regex tests.

    Each assertion failure should print the offending file + line for easy fixup.
  </behavior>
  <action>
    Create tests/integration/security-smoke.test.js implementing the six assertions above. Use `node:test`, `node:assert/strict`, `node:fs/promises`, `node:path`. Walk `js/` recursively.

    Run `node --test tests/integration/security-smoke.test.js`. Expect:
    - Test 1 (T-08): passes if Plan 01 set dependencies: {} correctly
    - Test 2 (T-04 network): passes — Phase 1 has zero network code
    - Test 3 (clock-seam): passes if Plan 04's manual-entry.js exemption is tagged correctly (verify the tag exists in manual-entry.js; if not, this test will fail until the exemption tag is added — fix by editing manual-entry.js's prefill comment to read `// gsd:allow-ui-clock — UI default-prefill of today's date is non-domain`)
    - Test 4 (storage-seam): passes — only storage-local.js touches localStorage after Plan 01
    - Test 5 (T-07): passes — Plans 01..04 all use textContent
    - Test 6 (adapter boundary): mirrors tests 3 and 4

    If test 3 fails, edit js/ui/manual-entry.js to add the exact tag `// gsd:allow-ui-clock` on the line where `new Date()` is used for the default-date prefill. Re-run.

    Commit: `test(NW-01): T-04,T-07,T-08 security smoke + clock+storage seam invariants per D-07,D-09,D-20`.
  </action>
  <verify>
    <automated>
      node --test tests/integration/security-smoke.test.js
      AND grep -q "dependencies" tests/integration/security-smoke.test.js
      AND grep -q "gsd:allow-ui-clock" js/ui/manual-entry.js
      AND grep -q "innerHTML" tests/integration/security-smoke.test.js
    </automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/integration/security-smoke.test.js` exits 0 with all 6 assertions passing
    - tests/integration/security-smoke.test.js source contains: `dependencies`, `fetch(`, `new Date`, `localStorage`, `innerHTML`, `gsd:allow-ui-clock` (the six invariants are observable in the test source itself)
    - js/ui/manual-entry.js contains `// gsd:allow-ui-clock` on the line of its `new Date()` exemption (if present)
    - `npm test` exits 0 (full suite still green)
  </acceptance_criteria>
  <done>
    Security smoke green; every Phase 1 architectural invariant has an explicit regression guard.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend CI workflow with fail-fast supply-chain step + write README.md</name>
  <files>.github/workflows/ci.yml, README.md</files>
  <read_first>
    - .planning/phases/NW-01-log-persist/01-CONTEXT.md §decisions D-21 (one GitHub Action runs whole suite on push/PR)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Code Examples §.github/workflows/ci.yml (current state)
    - .github/workflows/ci.yml (Plan 01 — extend)
    - 01-01-SUMMARY.md, 01-02-SUMMARY.md, 01-03-SUMMARY.md, 01-04-SUMMARY.md (referenced in the README for "what each plan shipped")
    - CLAUDE.md (REQ-ID commit convention to document for future contributors)
  </read_first>
  <behavior>
    Extend .github/workflows/ci.yml: insert a new step BEFORE the `node --test` step, named "Supply-chain check", that runs:
      `node -e "const p=require('./package.json'); const n=Object.keys(p.dependencies||{}).length; if(n>0){console.error('Runtime deps not allowed (D-20, T-08): '+n+' found'); process.exit(1)} else console.log('OK: zero runtime deps')"`
    This is fail-fast: if someone adds a runtime dep, CI rejects the PR before running tests.

    Create README.md with these sections:
    - Title + one-paragraph project description (mirror PROJECT.md "Core value")
    - "Run locally" — `node scripts/serve.js` → open http://localhost:8080 → click "Woke up" → ...
    - "Run tests" — `npm test`, `npm run test:unit`, `npm run test:e2e`
    - "Project layout" — brief tree mirroring 01-SKELETON.md §Directory Layout
    - "Architectural invariants" — link to 01-SKELETON.md; list the four invariants tested by security-smoke (zero deps, no fetch, clock seam, storage seam, no innerHTML with user data)
    - "Commit message convention" — `<type>(NW-NN): REQ-IDs short description per D-XX`. List REQ-ID prefixes (LOG, CFG, PRED, UI, DATA, STAGE, PLAT)
    - "Phase 1 status" — link to the four summary files in `.planning/phases/NW-01-log-persist/`
    - "Roadmap" — `.planning/ROADMAP.md` link

    Keep README under 100 lines — Phase 1 deliverable, not a full project doc. Phase 8 will polish.
  </behavior>
  <action>
    Edit .github/workflows/ci.yml: locate the existing `- name: Run unit + integration tests` step. Insert a new step immediately BEFORE it:
    ```yaml
          - name: Supply-chain check (T-08, D-20)
            run: node -e "const p=require('./package.json'); const n=Object.keys(p.dependencies||{}).length; if(n>0){console.error('Runtime deps not allowed: '+n+' found'); process.exit(1)} else console.log('OK: zero runtime deps')"
    ```

    Create README.md with the sections in `<behavior>`. Use plain Markdown. Reference the four SUMMARY files by path.

    Run the full local suite one more time: `npm test`. All green.

    Commit: `docs(NW-01): PLAT-11 add README + CI supply-chain check per D-20,D-21,T-08`.
  </action>
  <verify>
    <automated>
      grep -q "Supply-chain check" .github/workflows/ci.yml
      AND grep -q "Runtime deps not allowed" .github/workflows/ci.yml
      AND test -f README.md
      AND grep -q "npm test" README.md
      AND grep -q "node scripts/serve.js" README.md
      AND grep -q "Architectural invariants\|zero deps" README.md
    </automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml contains a "Supply-chain check" step that runs before `node --test`
    - The supply-chain check command exits 1 if any runtime dependency is present
    - README.md exists and includes: project description, run-locally instructions, test commands, project layout tree, architectural-invariants section, commit-message convention, Phase 1 status with SUMMARY links
    - README.md is fewer than 100 non-blank lines (Phase 1 scope; Phase 8 will polish)
    - `npm test` exits 0 (no regression)
  </acceptance_criteria>
  <done>
    CI workflow updated and pushed; first green run with the supply-chain step observable in Actions tab. README renders correctly on GitHub.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Phase 1 shippable checkpoint — D-22 coverage audit + dogfooding readiness</name>
  <what-built>
    Plans 01-05 together deliver Phase 1: a working, tested, hardened sleep-event logging app. Plan 05's persistence + security-smoke tests lock in every architectural invariant. The CI workflow is green with a supply-chain fail-fast step. README is in place.

    Before declaring Phase 1 complete, audit D-22 coverage and confirm the user can dogfood the app on their own machine.
  </what-built>
  <how-to-verify>
    **D-22 coverage audit** (CONTEXT.md: "Every LOG-* and DATA-04 requirement landing in Phase 1 has at least one automated test"):

    Confirm for each REQ-ID, at least one test exists and passes. The summary file (01-05-SUMMARY.md) must list this matrix:

    | REQ-ID | Test File(s) | Test Name(s) | Status |
    |--------|--------------|--------------|--------|
    | LOG-01 | tests/integration/event-log.test.js, tests/e2e/reload.spec.js, tests/e2e/quick-log.spec.js | (specific test names) | ✅ |
    | LOG-02 | tests/integration/event-log.test.js, tests/e2e/quick-log.spec.js | ... | ✅ |
    | LOG-03 | ... | ... | ✅ |
    | LOG-04 | ... | ... | ✅ |
    | LOG-05 | tests/integration/manual-entry.test.js, tests/e2e/manual-entry.spec.js | ... | ✅ |
    | LOG-06 | tests/integration/manual-entry.test.js, tests/e2e/manual-entry.spec.js | ... | ✅ |
    | LOG-07 | tests/unit/time.test.js, tests/integration/manual-entry.test.js | ... | ✅ |
    | LOG-08 | tests/unit/day-bucket.test.js | ... | ✅ |
    | LOG-09 | tests/unit/day-bucket.test.js, tests/e2e/quick-log.spec.js | ... | ✅ |
    | DATA-04 | tests/integration/persistence.test.js, tests/e2e/reload.spec.js | ... | ✅ |
    | PLAT-08 | (smoke: node --test runs in CI) | n/a | ✅ |
    | PLAT-09 | (smoke: integration tests compose adapters; passing suite proves) | n/a | ✅ |
    | PLAT-10 | (smoke: Playwright runs in CI; package.json scopes to devDependencies) | n/a | ✅ |
    | PLAT-11 | (meta: all of the above + TDD commits in Plans 02+04) | n/a | ✅ |

    **Manual dogfooding readiness check:**

    1. Fresh-clone simulation: `cd /tmp && rm -rf nightwatch-clone && git clone <repo> nightwatch-clone && cd nightwatch-clone && npm ci && npm test` — full suite green from a clean checkout.
    2. Open `http://localhost:8080` after `node scripts/serve.js`. Click each of the four buttons. Use `+ Add event` to add a past-day event. Edit one event. Delete one event. Reload. Verify all changes persist.
    3. Open DevTools → Application → Local Storage → `http://localhost:8080`. Confirm `nightwatch:db` value is `{"version":1,"events":[...]}` with the events you just logged (D-04 + D-05 visible).
    4. Confirm GitHub Actions on the latest push has a green run including the "Supply-chain check" step in the log.
    5. Read 01-05-SUMMARY.md and confirm the D-22 coverage matrix is filled in.

    **Approval criteria:**
    - All boxes in the D-22 coverage matrix are ✅
    - The fresh-clone simulation succeeds
    - All manual dogfooding steps work as expected
    - CI is green
  </how-to-verify>
  <resume-signal>
    Type "approved" if all D-22 boxes are ✅, the fresh-clone simulation succeeds, manual dogfooding works, and CI is green. If anything fails, describe the failing item (which REQ-ID lacks a test, which step in the dogfooding flow broke, which CI run failed) and the executor will revise. Do NOT auto-advance — this is a blocking gate before Phase 2.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Future contributor commits → main branch | CI must reject any commit that adds a runtime dependency (T-08) OR breaks an architectural invariant (T-04 clock/storage seam, T-07 innerHTML) |
| Disk / package.json → CI smoke | Supply-chain check reads package.json; a malicious PR can edit package.json — fail-fast catches this |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01 | Tampering (V5) | js/store/event-log.js | mitigate (regression guard) | Plan 05 does not introduce new code for T-01; the integration tests from Plans 01/03/04 already cover VALID_TYPES enforcement. Plan 05's job is to confirm those tests run in CI. |
| T-02 | Tampering (V5) | js/lib/time.js + js/store/event-log.js | mitigate (regression guard) | Plan 02's parseLocalISO tests + Plan 04's addEventAt parse-and-reject test together cover T-02. |
| T-03 | Information disclosure / DoS (V7) | js/adapters/storage-local.js | mitigate | Plan 05 Task 1 explicitly tests: corrupted blob → console.warn + null + empty event log. |
| T-04 | Information disclosure (V8) + clock-seam invariant | Repo-wide js/ tree | mitigate | Plan 05 Task 2 walks js/ asserting no fetch/XHR/WebSocket/<script src=, and no new Date() outside clock adapters. |
| T-05 | Business Logic (V11) | js/store + js/ui | mitigate (regression guard) | Plan 03 + Plan 04 E2E tests for double-click and edit-no-duplicate are now in CI. |
| T-06 | Business Logic (V11) | js/lib/day-bucket.js + js/ui/today-screen.js | mitigate (regression guard) | Plan 02 unit test + Plan 03 E2E test cover the extraNaps read-side filter. |
| T-07 | Tampering / XSS (V5) | Repo-wide js/ tree | mitigate | Plan 05 Task 2 walks js/ asserting no `.innerHTML = ` with non-empty data. Phase 1 has low risk (data is enum + regex'd) but the convention is locked in for Phase 5's CSV import. |
| T-08 | Tampering / Supply-chain (V14) | package.json + CI | mitigate | Plan 05 Task 2 asserts dependencies === {}; Plan 05 Task 3 CI step fails fast if any runtime dep is added. |
</threat_model>

<verification>
- `npm test` exits 0 (full suite: 3 unit + 4 integration + 3 E2E = ~10+ files, ~25+ specs)
- CI workflow green on main with "Supply-chain check" step visible in the log
- `node --test tests/integration/security-smoke.test.js` exits 0 (all 6 invariants pass)
- `node --test tests/integration/persistence.test.js` exits 0 (D-04 + D-05 round-trip + T-03 corrupted blob handled)
- README.md exists and includes the architectural-invariants section
- 01-05-SUMMARY.md includes the D-22 coverage matrix with all REQ-IDs ✅
- Fresh-clone dogfooding simulation passes (Task 4 manual check)
</verification>

<success_criteria>
After Plan 05 (Phase 1 complete):
1. Every Phase 1 REQ-ID (LOG-01..09, DATA-04, PLAT-08..11) has at least one automated test (D-22 audit).
2. Every architectural invariant from `01-SKELETON.md` has a regression guard in `tests/integration/security-smoke.test.js`.
3. CI fails fast on supply-chain violations (T-08).
4. The user has confirmed (Task 4 checkpoint) that the app dogfoods correctly: log 4 button types, add past event via modal, edit, delete, reload — all work and persist.
5. Phase 1 is shippable. Ready for `/gsd-verify-work` and the Phase 2 planning that builds on this foundation.
</success_criteria>

<output>
Create `.planning/phases/NW-01-log-persist/01-05-SUMMARY.md` when done, including:
- The full D-22 coverage matrix (REQ-ID → test files → test names → status) — REQUIRED for the checkpoint to pass
- The list of T-XX threats with disposition + mitigation evidence (test file + line)
- Confirmation that all `must_haves.truths` from Plans 01-05 are satisfied
- Phase 1 final commit SHA and CI run URL (last green run)
- "Lessons for Phase 2 planning" — patterns established here that Phase 2 settings work should follow (composition root extension point, adapter pattern for the settings store, etc.)
</output>
