---
phase: NW-13
plan: 03
type: tdd
wave: 3
depends_on:
  - 13-02
files_modified:
  - js/lib/forecast-tif.js
  - tests/unit/forecast-tif-ratio.test.js
autonomous: true
requirements:
  - TIF-12

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "When TIF is active and today's sleepDuration is non-null, nap-start sourceWindows includes a window labeled 'MA/sleep ratio band' (D-01, D-05)"
    - "When today's sleepDuration is null (bedtime not yet logged), 'MA/sleep ratio band' is absent from nap-start sourceWindows (D-04)"
    - "When TIF is active and todayMA is derivable (actual or predicted napStart − wake), nap-end sourceWindows includes a window labeled 'MA/nap ratio band' (D-02, D-05)"
    - "When todayMA is null (neither actual napStart nor wakeAnchor available), 'MA/nap ratio band' is absent from nap-end sourceWindows (Claude discretion: skip when todayMA null)"
    - "Nap-start ratio window is AFTER existing Historic nap-start band and Activity-before-nap band in labelledWindows (D-05)"
    - "Nap-end ratio window is AFTER existing Historic nap-end band and Nap-length band in labelledWindows (D-05)"
    - "Ratio windows carry median from buildDurationBand (inherits TIF-15 extension from Plan 02)"
    - "Ratio computation: for each day in window, abn = actBeforeNapPerDay[i]; when abn is null, the day is excluded from ratios array (not treated as zero)"
    - "Division-by-zero guard: days with sleepDuration=0 or napDuration=0 are excluded from the ratio array"
  artifacts:
    - "js/lib/forecast-tif.js — todaySleepDuration helper computation (sleepDuration of last day record)"
    - "js/lib/forecast-tif.js — todayMA helper computation (D-03 priority chain)"
    - "js/lib/forecast-tif.js — nap-start ratio window logic after Activity-before-nap band"
    - "js/lib/forecast-tif.js — nap-end ratio window logic after Nap-length band"
    - "tests/unit/forecast-tif-ratio.test.js — new test file covering ratio window presence/absence cases"
  prohibitions:
    - "No new npm dependencies"
    - "actBeforeNapPerDay must NOT be re-computed inside the ratio block — use the index-aligned array from Plan 01"
    - "Division by zero: guard sd > 0 and nd > 0 before computing ratios"
    - "Time strings remain local wall-clock 'HH:MM' — no UTC parsing"
    - "todayMA resolution must follow D-03 priority: actual first, prediction-derived second, null third"

artifacts_produced:
  - "todaySleepDuration — derived from sleepDuration(dayRecords[last]) inside tifForecast"
  - "todayMA — derived from (actualNapStart − actualWake) or (napStartPred.central − wakeAnchorForNap) inside tifForecast"
  - "nap-start ratio window: label 'MA/sleep ratio band', appended after 'Activity-before-nap band'"
  - "nap-end ratio window: label 'MA/nap ratio band', appended after 'Nap-length band'"
  - "tests/unit/forecast-tif-ratio.test.js — RED/GREEN test coverage for TIF-12"
---

<objective>
TDD plan — add two ratio-based source windows to tifForecast: a nap-start window derived from activityBeforeNap/sleepDuration ratios anchored to wake time, and a nap-end window derived from activityBeforeNap/napDuration ratios anchored to nap-start time.

Purpose: TIF-12 makes nap-start and nap-end predictions more data-rich by incorporating the proportional relationship between morning activity and sleep/nap durations. This is separate from the existing historic-time-band and duration-band windows.

Output: Two new labelled windows appended to napStartLabelledWindows and napEndLabelledWindows respectively; both carry medians from buildDurationBand (via Plan 02); both skip gracefully when required anchors or durations are unavailable.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/NW-13-tif-algorithm-extensions/13-CONTEXT.md
@.planning/phases/NW-13-tif-algorithm-extensions/13-PATTERNS.md
@js/lib/forecast-tif.js
@js/lib/metrics.js
@tests/integration/forecast-tif.integration.test.js
</context>

<feature>
  <name>Ratio-based windows for nap-start and nap-end (TIF-12)</name>
  <files>js/lib/forecast-tif.js, tests/unit/forecast-tif-ratio.test.js</files>

  <behavior>
    Fixture setup used in tests: 10-day records where each day has wake='07:30', bedtime='22:00', napStart='13:00', napEnd='14:30', date='2024-01-{01..10}'. All events as bare HH:MM strings so metrics functions can compute durations. Last record is "today".

    sleepDuration per day ≈ 570 min (wake 7:30 minus bedtime 22:00 the night before — metrics.js handles cross-midnight). napDuration per day = 90 min. activityBeforeNap per day = 330 min (13:00 − 07:30).

    Nap-start ratio window (MA/sleep ratio band):
      ratio_i = actBeforeNapPerDay[i] / sleepDuration(window[i])
      For uniform fixture: all ratios ≈ 330/570 ≈ 0.579.
      projectedDurations = ratios.map(r => r * todaySleepDuration).
      todaySleepDuration = sleepDuration(dayRecords[last]).
      buildDurationBand(projectedDurations, wakeAnchorForNap, trimPct, 0) produces a non-null band.
      result.napStart.sourceWindows contains window with label 'MA/sleep ratio band'.

    When today's bedtime not logged (sleepDuration returns null for last record):
      todaySleepDuration = null → nap-start ratio window skipped entirely.
      result.napStart.sourceWindows does NOT contain 'MA/sleep ratio band'.

    Nap-end ratio window (MA/nap ratio band):
      ratio_i = actBeforeNapPerDay[i] / napDuration(window[i])
      For uniform fixture: all ratios ≈ 330/90 ≈ 3.67.
      projectedNapDurations = ratios.map(r => r * todayMA).
      todayMA: if today's actual napStart and wake are logged → timeToMinutes(actualNapStart) − timeToMinutes(actualWake).
      If actual not available → timeToMinutes(napStartPred.central) − wakeAnchorForNap (if both non-null).
      For the fixture where napStart and wake are logged: todayMA = timeToMinutes('13:00') − timeToMinutes('07:30') = 330.
      buildDurationBand(projectedNapDurations, napStartAnchor, trimPct, 0) produces a non-null band.
      result.napEnd.sourceWindows contains window with label 'MA/nap ratio band'.

    When todayMA is null:
      result.napEnd.sourceWindows does NOT contain 'MA/nap ratio band'.

    Both ratio windows carry median field (from buildDurationBand + trimmedMinMax chain, TIF-15).
  </behavior>

  <implementation>
    RED: Create tests/unit/forecast-tif-ratio.test.js. Import tifForecast from '../../js/lib/forecast-tif.js'. Build a 10-day fixture function makeRatioFixture() producing day records with date fields ('2024-01-01' through '2024-01-10'), wake='07:30', bedtime='22:00', napStart='13:00', napEnd='14:30', allEvents=[], rejected=false. Create defaultRatioSettings = { minDays:3, tifRollingDays:10, trimPct:0, precisionTarget:60, forecastAlgorithm:'tif' }.
    Write test: '10-day fixture with consistent durations → napStart sourceWindows includes MA/sleep ratio band'. Call tifForecast(makeRatioFixture(), defaultRatioSettings, {}, false). Assert result.napStart.sourceWindows.some(w => w.label === 'MA/sleep ratio band') is true.
    Write test: 'last record missing bedtime → no MA/sleep ratio band'. Fixture where last record has bedtime=null. Assert sourceWindows label absent.
    Write test: '10-day fixture → napEnd sourceWindows includes MA/nap ratio band'. Assert result.napEnd.sourceWindows.some(w => w.label === 'MA/nap ratio band') is true.
    Write test: 'MA/sleep ratio band and MA/nap ratio band both have median field'. Assert typeof result.napStart.sourceWindows.find(w=>w.label==='MA/sleep ratio band').median === 'string'.
    Run node --test tests/unit/forecast-tif-ratio.test.js — all 4 tests MUST fail (windows not yet added).
    Commit: test(NW-13-03): add failing tests for TIF-12 ratio windows

    GREEN: In js/lib/forecast-tif.js, after the actBeforeNapPerDay refactor from Plan 01, add the following logic inside tifForecast.

    Nap-start ratio window (after the actBeforeBand push, before the napStartPred = buildPrediction(...) call):
    Compute todaySleepDuration by calling sleepDuration(dayRecords[dayRecords.length - 1]). If wakeAnchorForNap is non-null and todaySleepDuration is non-null and greater than zero, build the ratio array: for each index i in the window slice, access actBeforeNapPerDay[i] and sleepDuration(window[i]); include the ratio actBeforeNapPerDay[i] / sleepDuration(window[i]) only when both values are non-null and sleepDuration(window[i]) > 0. Map the ratio array to projectedDurations by multiplying each ratio by todaySleepDuration. Call buildDurationBand(projectedDurations, wakeAnchorForNap, trimPct, 0); if result is non-null, push { label: 'MA/sleep ratio band', ...result } onto napStartLabelledWindows (D-01, D-04, D-05).

    Nap-end ratio window (after the napLenBand push, before the napEndPred = buildPrediction(...) call):
    Compute todayMA using the D-03 priority chain: check the last day record for both actualNapStart (extractTime on today.napStart) and actualWake (extractTime on today.wake); if both are non-null, todayMA = timeToMinutes(actualNapStart) − timeToMinutes(actualWake). If either is null, check napStartPred.central and wakeAnchorForNap; if both are non-null, todayMA = timeToMinutes(napStartPred.central) − wakeAnchorForNap. Otherwise todayMA = null. If napStartAnchor is non-null and todayMA is non-null, build the nap-end ratio array: for each index i, ratio = actBeforeNapPerDay[i] / napDuration(window[i]) when both are non-null and napDuration(window[i]) > 0; null otherwise. Filter nulls. Map to projectedNapDurations = ratios.map(r => r * todayMA). Call buildDurationBand(projectedNapDurations, napStartAnchor, trimPct, 0); if non-null, push { label: 'MA/nap ratio band', ...result } onto napEndLabelledWindows (D-02, D-03, D-05).

    Run node --test tests/unit/forecast-tif-ratio.test.js — all 4 tests MUST pass.
    Commit: feat(NW-13-03): add MA/sleep and MA/nap ratio windows to tifForecast (TIF-12)

    REFACTOR: Add JSDoc comment block before the nap-start ratio window section explaining the formula: ratio_i = actBeforeNap_i / sleepDuration_i; projected = ratio_i * todaySleepDuration; anchored to wakeAnchorForNap. Same for nap-end. Run npm run test:unit — MUST pass.
    Commit: refactor(NW-13-03): document ratio window formulas in tifForecast JSDoc
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| actBeforeNapPerDay values → ratio computation | Per-day numeric values (null or minutes) from activityLog or metrics.js |
| sleepDuration / napDuration results → ratio denominator | Numeric values that must be guarded against zero |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-03-01 | Tampering | Division by zero in ratio (sleepDuration=0 or napDuration=0) | medium | mitigate | Guard `sd > 0` and `nd > 0` before computing ratio; zero-duration days excluded from ratio array |
| T-13-03-02 | Tampering | Negative ratio when actBeforeNap or duration is negative | low | mitigate | activityBeforeNap returns null for invalid records; null guard already excludes them |
| T-13-03-03 | Information Disclosure | projectedDurations exposes activity-pattern ratios in sourceWindows | low | accept | sourceWindows is already an intentional transparency feature of TIF |
</threat_model>

<verification>
node --test tests/unit/forecast-tif-ratio.test.js
node --test tests/integration/forecast-tif.integration.test.js
npm run test:unit
</verification>

<success_criteria>
- result.napStart.sourceWindows includes a window with label === 'MA/sleep ratio band' when sleepDuration is available
- 'MA/sleep ratio band' is absent when today's sleepDuration is null
- result.napEnd.sourceWindows includes a window with label === 'MA/nap ratio band' when todayMA is derivable
- Both ratio windows carry a median: 'HH:MM' field (from buildDurationBand inheriting TIF-15)
- Ratio windows appear after the existing bands in labelledWindows (ordering: historic → activity/nap-len → ratio)
- Division by zero never occurs: sleepDuration=0 and napDuration=0 days are excluded
- All previous tests still pass (npm run test:unit exits 0)
</success_criteria>

<output>
Create .planning/phases/NW-13-tif-algorithm-extensions/13-03-SUMMARY.md when done.
</output>
