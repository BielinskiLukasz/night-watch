---
phase: NW-08
plan: 05
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
  - 08-03
  - 08-04
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-03
  - PLAT-04
  - PLAT-05
  - PLAT-06
  - PLAT-07

must_haves:
  truths:
    - All five Phase 8 success criteria from ROADMAP.md are verified as TRUE
    - PLAT-01 (vanilla JS only) confirmed via grep — no import statements from npm packages in any js/ file
    - PLAT-02 (multi-file split) confirmed — separate index.html, style.css, js/ directory structure exists
    - PLAT-05 (English UI) confirmed — no non-English user-facing copy in any js/ui/ file
    - PLAT-07 (in-app notifications only) confirmed — no Notification API or push registration in any js/ file
    - PLAT-03 active work confirmed complete — manifest.json, sw.js, icons/ all present and structurally valid
    - PLAT-04 active work confirmed complete — .github/workflows/ci.yml present with deploy job
    - PLAT-06 active work confirmed complete — chart animations in style.css; settings modal regrouped; icon style unified
    - Full test suite passes — node --test and npx playwright test exit 0
    - REQUIREMENTS.md PLAT-01 through PLAT-07 rows updated from Pending to Complete
  artifacts:
    - .planning/REQUIREMENTS.md (PLAT-01 through PLAT-07 status rows updated)
  key_links:
    - All seven PLAT requirements must be marked Complete in REQUIREMENTS.md before phase closes
    - Test suite results must be green before requirements are marked Complete
---

<objective>
Phase gate — verify all seven PLAT requirements (PLAT-01, PLAT-02, PLAT-03, PLAT-04, PLAT-05, PLAT-06, PLAT-07) and all five ROADMAP Phase 8 success criteria are met. Run the full test suite. Document the four already-satisfied requirements (PLAT-01, PLAT-02, PLAT-05, PLAT-07) as verified-in-phase. Update REQUIREMENTS.md status rows.

Purpose: Phase 8 completion gate. PLAT-01, PLAT-02, PLAT-05, PLAT-07 were satisfied in prior phases and are being formally verified and closed here. PLAT-03, PLAT-04, PLAT-06 are active work items from Plans 08-01 through 08-04.

Output: Verified green test suite; REQUIREMENTS.md updated with all seven PLAT rows marked Complete
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/sleep-tracker/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/sleep-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/NW-08-pwa-platform-hardening/08-CONTEXT.md
@.planning/phases/NW-08-pwa-platform-hardening/08-01-SUMMARY.md
@.planning/phases/NW-08-pwa-platform-hardening/08-02-SUMMARY.md
@.planning/phases/NW-08-pwa-platform-hardening/08-03-SUMMARY.md
@.planning/phases/NW-08-pwa-platform-hardening/08-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and verify all seven PLAT requirements</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (PLAT-01 through PLAT-07 section at lines 66–72 and status rows near line 153–160 — confirm current checkbox state and row format before editing)
    - .planning/ROADMAP.md (Phase 8 success criteria at §Phase 8 — the five criteria that must be TRUE)
    - .planning/phases/NW-08-pwa-platform-hardening/08-CONTEXT.md (domain section — "Already satisfied" list for PLAT-01, PLAT-02, PLAT-05, PLAT-07; active work items for PLAT-03, PLAT-04, PLAT-06)
    - manifest.json (confirm exists and has start_url "./" per PLAT-03)
    - sw.js (confirm exists with CACHE_NAME and PRECACHE_LIST per PLAT-03)
    - .github/workflows/ci.yml (confirm exists with deploy job per PLAT-04)
  </read_first>
  <action>
    Execute the following verification sequence. Each check must pass before proceeding.

    **Check 1 — PLAT-01 (vanilla JS only):**
    Run: node -e "const fs=require('fs'),path=require('path'); function walk(d){return fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(d,e.name)):[path.join(d,e.name)])} const files=walk('js').filter(f=>f.endsWith('.js')); const violations=files.flatMap(f=>{const src=fs.readFileSync(f,'utf8');return src.split('\n').filter(l=>l.match(/^import .+ from ['\"](react|vue|angular|lodash|moment|axios|express)/)).map(l=>f+': '+l.trim())}); if(violations.length){console.error('PLAT-01 violations:',violations);process.exit(1)}else{console.log('PLAT-01 OK — no npm runtime imports found')}"
    Expected: "PLAT-01 OK — no npm runtime imports found"

    **Check 2 — PLAT-02 (multi-file split):**
    Run: node -e "const fs=require('fs'); ['index.html','style.css','js/app.js','js/lib/time.js','js/ui/today-screen.js'].forEach(f=>{if(!fs.existsSync(f)){console.error('PLAT-02 missing:',f);process.exit(1)}}); console.log('PLAT-02 OK — multi-file split confirmed')"
    Expected: "PLAT-02 OK — multi-file split confirmed"

    **Check 3 — PLAT-03 (PWA manifest + SW + file://):**
    Run: node -e "const fs=require('fs'); const m=JSON.parse(fs.readFileSync('manifest.json','utf8')); console.assert(m.start_url==='./','manifest start_url'); console.assert(m.scope==='./','manifest scope'); const sw=fs.readFileSync('sw.js','utf8'); console.assert(sw.includes('nightwatch-v1'),'SW cache name'); console.assert(fs.existsSync('icons/icon-192.png'),'icon 192'); console.assert(fs.existsSync('icons/icon-512.png'),'icon 512'); const app=fs.readFileSync('js/app.js','utf8'); console.assert(app.includes(\"location.protocol !== 'file:'\"),'file: guard'); console.log('PLAT-03 OK')"
    Expected: "PLAT-03 OK"

    **Check 4 — PLAT-04 (GitHub Pages deployable):**
    Run: node -e "const fs=require('fs'); const ci=fs.readFileSync('.github/workflows/ci.yml','utf8'); console.assert(ci.includes('actions/deploy-pages@v4'),'deploy-pages v4'); console.assert(ci.includes('needs: test'),'needs test'); console.log('PLAT-04 OK')"
    Expected: "PLAT-04 OK"

    **Check 5 — PLAT-05 (English UI):**
    Run: node -e "const fs=require('fs'),path=require('path'); function walk(d){return fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(d,e.name)):[path.join(d,e.name)])} const files=walk('js/ui').filter(f=>f.endsWith('.js')); const nonAscii=files.flatMap(f=>{const src=fs.readFileSync(f,'utf8'); return src.split('\n').filter((l,i)=>l.match(/[^\x00-\x7F]/)&&!l.trim().startsWith('//')).map(l=>f+': '+l.trim())}); if(nonAscii.length){console.warn('Possible non-ASCII in UI:',nonAscii.slice(0,5))} console.log('PLAT-05 check complete — review any warnings above')"
    Expected: "PLAT-05 check complete" with no warning lines, or warnings only in comments (Polish column names are in csv-parse.js comments, which is acceptable per CLAUDE.md).

    **Check 6 — PLAT-06 (visual identity — chart animations + settings modal):**
    Run: node -e "const fs=require('fs'); const css=fs.readFileSync('style.css','utf8'); console.assert(css.includes('@keyframes drawLine'),'drawLine keyframes'); console.assert(css.includes('@keyframes fadeInEl'),'fadeInEl keyframes'); const html=fs.readFileSync('index.html','utf8'); console.assert(html.includes('Subject')&&html.includes('Day Structure'),'settings groups'); const cs=fs.readFileSync('js/ui/charts-screen.js','utf8'); console.assert(cs.includes('pathLength'),'pathLength'); console.assert(cs.includes('chart-line'),'chart-line'); console.log('PLAT-06 OK')"
    Expected: "PLAT-06 OK"

    **Check 7 — PLAT-07 (in-app notifications only):**
    Run: node -e "const fs=require('fs'),path=require('path'); function walk(d){return fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(d,e.name)):[path.join(d,e.name)])} const files=walk('js').filter(f=>f.endsWith('.js')); const push=files.filter(f=>fs.readFileSync(f,'utf8').includes('PushManager')||fs.readFileSync(f,'utf8').includes('showNotification')); if(push.length){console.error('PLAT-07 violation — push found in:',push);process.exit(1)} console.log('PLAT-07 OK — no push/OS notification APIs found')"
    Expected: "PLAT-07 OK — no push/OS notification APIs found"

    **Check 8 — Full test suite:**
    Run: node --test tests/unit/*.test.js tests/integration/*.test.js
    Expected: all tests pass (same count as Phase 7 baseline + new sw-precache.test.js assertions).

    **After all checks pass:** Update .planning/REQUIREMENTS.md. Use the Edit tool to change the status rows for PLAT-01 through PLAT-07 from "Pending" to "Complete" in the requirements status table (around lines 153–160). Also update the checkbox markers at lines 66–72: change "- [ ] **PLAT-01**" through "- [ ] **PLAT-07**" to "- [x] **PLAT-01**" through "- [x] **PLAT-07**". Use separate Edit calls for each change to keep diffs minimal.
  </action>
  <verify>
    <automated>node --test tests/unit/*.test.js tests/integration/*.test.js</automated>
    <automated>node -e "const src=require('fs').readFileSync('.planning/REQUIREMENTS.md','utf8'); ['PLAT-01','PLAT-02','PLAT-03','PLAT-04','PLAT-05','PLAT-06','PLAT-07'].forEach(id=>{const statusMatch=src.match(new RegExp(id+'.*\\|.*Complete'));if(!statusMatch){console.error(id,'not marked Complete');process.exit(1)}});console.log('All PLAT-01..07 marked Complete in REQUIREMENTS.md')"</automated>
  </verify>
  <done>All seven PLAT requirement checks pass; full test suite green; REQUIREMENTS.md status rows for PLAT-01 through PLAT-07 updated to Complete; checkbox markers updated to [x]</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — PWA install, SW, animations, Settings modal, file:// note</name>
  <what-built>
    Plans 08-01 through 08-04 have built: manifest.json, sw.js, icons/, .github/workflows/ci.yml (PLAT-03/PLAT-04), plus the update banner, file:// note, tab-switch fade, chart SVG animations, SVG icon style pass, and Settings modal group reorganization (PLAT-06). The full test suite passes. REQUIREMENTS.md marks all seven PLAT requirements Complete.
  </what-built>
  <how-to-verify>
    1. Open the app in a browser (run `node tests/e2e/serve.js` or equivalent local server, then navigate to http://localhost:PORT/).

    2. **PWA install prompt (PLAT-03):** In Chrome DevTools → Application → Manifest — confirm manifest.json loads without errors. Check start_url is "./" and scope "./". Under Service Workers, confirm sw.js is registered and active (status: "running"). Under Cache Storage, confirm "nightwatch-v1" cache exists with all expected files listed.

    3. **Offline support (PLAT-03):** In DevTools → Network → check "Offline". Reload the page. The app should load fully from cache (no network error). Uncheck Offline to restore.

    4. **Tab-switch fade (PLAT-06):** Click each of the four bottom-nav tabs. Each screen should fade in smoothly (~150ms). The transition should feel subtle, not jarring.

    5. **Chart animations (PLAT-06):** Navigate to Charts tab. If sleep data exists, the sleep-length line chart polyline should draw in from left to right. Heatmap cells should fade in. Switch stages (if any) to see the animation replay.

    6. **Settings modal groups (PLAT-06):** Open Settings. Confirm the five groups: "Subject & Display", "Day Structure", "Forecast & Prediction", "Data", "Stages". All existing fields should be present and functional. Save settings and confirm they persist on reload.

    7. **SVG icon style (PLAT-06):** Inspect the bottom nav icons and the Settings gear in the header. All icons should use the thin stroke-based line art (no filled paths). The gear icon should look like a consistent style with the nav icons.

    8. **file:// note (PLAT-03):** Open index.html directly as a file (e.g., File → Open or drag to browser). A muted note should appear: "Running from local file — install from the web version for offline support." Clicking × should dismiss it and not show it again on reload.

    9. **App fully functional (PLAT-03, PLAT-01, PLAT-02, PLAT-05):** Log a quick event on Today screen. Navigate all four tabs. Confirm everything works as it did before Phase 8. No regressions.
  </how-to-verify>
  <action>Human verification step — no automated action. Review the how-to-verify steps above in a browser.</action>
  <verify>
    <human-check>All nine verification steps above confirmed manually in browser</human-check>
  </verify>
  <done>User types "approved" — all Phase 8 visual and functional checks confirmed in browser</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found for gap-closure planning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REQUIREMENTS.md edit | Planner-controlled documentation update — no user input, no runtime execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05-01 | Repudiation | Phase gate approves with failing tests | high | mitigate | Task 1 runs full test suite before marking requirements complete; automated checks must pass before checkpoint |
| T-08-05-02 | Tampering | REQUIREMENTS.md incorrectly marks requirements complete | low | mitigate | Automated check verifies all seven PLAT IDs show "Complete" in status table after edit |
</threat_model>

<verification>
## Phase 8 Success Criteria Checklist (from ROADMAP.md)

1. App source is split into separate HTML, CSS, and JS files — VERIFIED by PLAT-02 check (multi-file structure exists since Phase 1)
2. App includes a valid web manifest and service worker; installs to home screen and can be launched offline — VERIFIED by manifest.json + sw.js + chrome DevTools checkpoint
3. Opened from file:///: app works fully — VERIFIED by file:// guard in app.js + file-note display + manual smoke in checkpoint
4. App is deployed to GitHub Pages via public URL — VERIFIED by .github/workflows/ci.yml deploy job structure
5. UI text is entirely in English, calm dark/minimal theme, in-app notifications only — VERIFIED by PLAT-05 + PLAT-07 checks + human visual confirm in checkpoint

All seven PLAT requirement status rows in REQUIREMENTS.md are "Complete".
Full test suite (node --test + playwright) exits 0.
</verification>

<success_criteria>
- All seven PLAT requirement check scripts exit 0
- node --test tests/unit/*.test.js tests/integration/*.test.js exits 0 with all assertions passing
- REQUIREMENTS.md: PLAT-01 through PLAT-07 checkboxes are [x] and status rows show "Complete"
- Human checkpoint: PWA installs, service worker is active, tab-switch fade visible, chart animations visible, settings modal has five groups, file:// note appears from file:// origin
- Phase 8 is complete
</success_criteria>

<output>
Create `.planning/phases/NW-08-pwa-platform-hardening/08-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Modified files:
- `.planning/REQUIREMENTS.md` — PLAT-01 through PLAT-07 checkbox markers changed to [x]; status rows changed to "Complete"

No new source files. This plan only verifies and documents completion.

## Already-Satisfied Requirements (documented here, verified in-phase)

| Requirement | Satisfied Since | Evidence |
|-------------|----------------|----------|
| PLAT-01 (vanilla JS only) | Phase 1 | No npm runtime imports in any js/ file; CLAUDE.md hard constraint enforced throughout |
| PLAT-02 (multi-file split) | Phase 1 | index.html + style.css + js/**/*.js structure in place since Phase 1, Plan 01-01 |
| PLAT-05 (English UI) | Phase 1 | All user-facing copy in English; Polish column names are code comments only (csv-parse.js) |
| PLAT-07 (in-app notifications only) | Phase 3 | Today screen next-event card is the notification surface; no PushManager or showNotification calls anywhere |
