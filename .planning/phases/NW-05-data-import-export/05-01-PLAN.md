---
phase: NW-05-data-import-export
plan: 01
type: auto
wave: 1
depends_on: []
files_modified:
  - js/lib/csv-parse.js
  - js/lib/db-shape.js
  - tests/unit/csv-parse.test.js
  - tests/unit/db-shape.test.js
autonomous: true
requirements: [DATA-03, DATA-05]
user_setup: []

must_haves:
  truths:
    - "parseCSV(text) returns events, rejectedDays, activityLog, and skipped arrays from a valid CSV string (D5-07, D5-17)"
    - "Delimiter is auto-detected (semicolon vs comma) per D5-09"
    - "Date format is auto-detected (DD.MM.YYYY dots vs YYYY-MM-DD dashes) per D5-08"
    - "Nap columns are optional — rows without nap times produce no nap events (D5-05)"
    - "Aggregate/computed columns are silently ignored (D5-06)"
    - "Bad rows are skipped with row number and reason recorded in the skipped array (D5-10)"
    - "db.activityLog: {} is included in fresh-install, v1-migration, and v2-passthrough outputs from migrateV1ToV2 (D5-17)"
    - "All imported event at-strings are 5-minute-aligned via roundTo5 (LOG-07)"
  artifacts:
    - path: "js/lib/csv-parse.js"
      provides: "Pure CSV parser — parseCSV(text) with Polish column mapping, delimiter/date-format auto-detection"
      exports: ["parseCSV"]
      contains: "export function parseCSV"
    - path: "js/lib/db-shape.js"
      provides: "migrateV1ToV2 updated to inject activityLog: {} in fresh/v1/v2-passthrough paths"
      contains: "activityLog"
    - path: "tests/unit/csv-parse.test.js"
      provides: "Unit tests (RED→GREEN) for parseCSV — all parsing branches covered"
      contains: "parseCSV"
    - path: "tests/unit/db-shape.test.js"
      provides: "Unit tests for activityLog injection in all three migration paths"
      contains: "activityLog"
  key_links:
    - from: "js/lib/csv-parse.js"
      to: "js/lib/time.js"
      via: "parseLocalISO, roundTo5, formatLocalISO for canonical at-string rounding"
      pattern: "parseLocalISO|roundTo5|formatLocalISO"
    - from: "js/store/event-log.js"
      to: "js/lib/db-shape.js"
      via: "migrateV1ToV2 called at construction and in replace() (Plan 05-02)"
      pattern: "migrateV1ToV2"
---

<objective>
**Wave 1a: CSV Parser + activityLog Schema Foundation**

Implement the pure CSV parsing module (js/lib/csv-parse.js) using strict TDD (RED→GREEN→refactor) and patch js/lib/db-shape.js to inject the new db.activityLog field into all three migration paths (fresh install, v1 migration, v2 passthrough). These two modules are the data-layer foundation that the import UI in Waves 2–3 will call.

**Purpose:** DATA-03 (CSV import) requires a pure, testable parser that maps Polish column headers (from sen.xlsx), auto-detects delimiters and date formats, skips bad rows, and produces canonical event at-strings rounded to 5 minutes. DATA-05 (file-as-truth) requires that the canonical JSON blob always carries db.activityLog so activity data round-trips through export and JSON import without loss.

**Output:** js/lib/csv-parse.js (pure function, no DOM), updated js/lib/db-shape.js (activityLog in all paths), passing unit tests for both modules.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/projects/sleep-tracker/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/projects/sleep-tracker/.claude/get-shit-done/templates/summary.md

**Key Integration Points:**
- js/lib/time.js — reuse parseLocalISO, roundTo5, formatLocalISO for time parsing (no new time logic)
- js/lib/db-shape.js — migrateV1ToV2 and DEFAULT_SETTINGS; patch to add activityLog to all three output paths
- tests/unit/db-shape.test.js — already exists; add activityLog test cases without breaking existing tests
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/NW-05-data-import-export/05-CONTEXT.md
@.planning/phases/NW-05-data-import-export/05-RESEARCH.md
@.planning/phases/NW-04-history-screen-edit-delete/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write failing unit tests for csv-parse.js</name>
  <files>tests/unit/csv-parse.test.js</files>
  <behavior>
    - Test: semicolon-delimited Polish-header CSV with dots date → events array contains wake + bedtime events with correct 5-min-aligned at-strings
    - Test: comma-delimited CSV → delimiter auto-detected, events parsed correctly
    - Test: DD.MM.YYYY date format → converted to YYYY-MM-DD ISO prefix in at-strings
    - Test: YYYY-MM-DD date format → accepted as-is in at-strings
    - Test: row with empty nap columns → no napStart/napEnd events in output; wake/bedtime present
    - Test: row with nap columns populated → napStart and napEnd events in output
    - Test: row missing required wake time → recorded in skipped array with row number and reason 'missing wake time'
    - Test: row missing required date → recorded in skipped array with reason 'missing date'
    - Test: odrzucone column truthy (value '1') → date appears in rejectedDays array
    - Test: odrzucone column falsy (empty or '0') → date absent from rejectedDays
    - Test: Aktywnosc column with numeric value → date/value pair in activityLog
    - Test: Aktywnosc column empty → date absent from activityLog
    - Test: unrecognized column headers are silently ignored; recognized columns still parse
    - Test: event at-strings are 5-minute-aligned (roundTo5 applied — e.g., '07:32' rounds to '07:30')
    - Test: Excel seconds suffix in time ('07:30:00') — only HH:MM taken, parses correctly
    - Test: empty trailing lines in CSV are skipped without error
  </behavior>
  <action>
    Create tests/unit/csv-parse.test.js. Import parseCSV from ../../js/lib/csv-parse.js (which does not exist yet — tests will fail with MODULE_NOT_FOUND, satisfying RED). Use node:test and node:assert/strict.

    Write one describe block 'parseCSV'. Include all behavior cases listed above as individual test() calls. Use a minimal fixture CSV string as input for each test — keep fixtures inline in each test for readability.

    Sample fixture for semicolon + Polish + dot-date:
    'Data;Pobudka;Zasniecie;Drzemka start;Drzemka stop;Aktywnosc;odrzucone\n28.06.2026;07:00;22:00;;;3.5;\n29.06.2026;07:30;22:30;14:00;15:00;4.0;1'

    Note: the actual Polish diacritics in column headers (Zaśnięcie, Aktywność) must appear in the fixture string as UTF-8. The COL map in csv-parse.js must match exactly. Use both plain-ASCII fallback versions (Zasniecie, Aktywnosc) in test fixtures if needed to avoid encoding issues in the test file itself — the COL map should accept both with and without diacritics where practical, or tests should use the exact UTF-8 header values.

    Run after writing: node --test tests/unit/csv-parse.test.js
    Expected outcome: all tests FAIL with MODULE_NOT_FOUND or import error. Commit: test(05-01): add failing unit tests for csv-parse.js [RED]
  </action>
  <verify>
    <automated>node --test tests/unit/csv-parse.test.js 2>&1 | grep -E "(FAIL|fail|Error|not ok)" | head -5</automated>
  </verify>
  <done>
    tests/unit/csv-parse.test.js exists; running it produces failures (not a syntax error in the test file itself); RED phase confirmed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement csv-parse.js and patch db-shape.js; run tests to pass</name>
  <files>js/lib/csv-parse.js, js/lib/db-shape.js, tests/unit/db-shape.test.js</files>
  <behavior>
    - parseCSV(text) returns { events, rejectedDays, activityLog, skipped }
    - COL map is Object.freeze'd with Polish and English header aliases (per D5-06)
    - migrateV1ToV2 fresh-install path returns { version: 2, settings: {...}, events: [], activityLog: {} }
    - migrateV1ToV2 v1 migration path returns blob with activityLog: {}
    - migrateV1ToV2 v2 passthrough path injects activityLog: {} when field is absent or not an object
    - Existing db-shape.test.js tests continue to pass (no regressions)
  </behavior>
  <action>
    **Part A — js/lib/csv-parse.js (new file):**

    Create the module with these exports: parseCSV(text). Import parseLocalISO, roundTo5, formatLocalISO from './time.js'.

    Implement the COL map as a frozen object mapping Polish headers (Data, Pobudka, Zaśnięcie, Drzemka start, Drzemka stop, Aktywność, odrzucone) and English aliases (Date, Wake, Bedtime, Nap start, Nap end, Activity, Rejected) to field names (date, wake, bedtime, napStart, napEnd, activity, rejected). Use Object.freeze per D5-06.

    Implement detectDelimiter(headerLine): count semicolons vs commas; return ';' if semis >= commas, else ','.

    Implement detectDateFormat(sampleDate): regex test for DD.MM.YYYY dots → return 'dmy-dot'; YYYY-MM-DD dashes → return 'iso'; else return null.

    Implement parseDate(raw, fmt): convert dmy-dot to YYYY-MM-DD ISO date string; iso → trim and return.

    Implement parseEventAt(dateStr, timeStr): slice timeStr to first 5 chars (HH:MM), build YYYY-MM-DDTHH:MM, call parseLocalISO then roundTo5 then formatLocalISO. Throws on malformed input.

    Implement parseCSV(text): split on /\r?\n/, detect delimiter from header line, build colIdx map (header index → field name via COL), iterate data rows. For each non-empty row: require date cell and wake cell (skip with reason if missing); detect date format from first parseable row; build wake event; optionally build bedtime, napStart, napEnd events (per D5-05 — skip if cell empty, swallow parse errors for these optional fields); process rejected column (truthy and not '0' and not 'false' → push to rejectedDays); process activity column (parseFloat, push to activityLog if not NaN). Return { events, rejectedDays, activityLog, skipped }.

    Note on encoding: the Polish column header strings in COL must be the exact UTF-8 character sequences. The js/lib/csv-parse.js file must be saved as UTF-8. The test fixtures must use the same UTF-8 strings for headers.

    **Part B — js/lib/db-shape.js patch (per D5-17 and Pattern F from RESEARCH.md):**

    In migrateV1ToV2, apply three patches:
    1. Fresh-install path (blob === null/undefined): add activityLog: {} to the returned object alongside version/settings/events.
    2. v1 migration path (blob.version === 1): add activityLog: {} to the returned object.
    3. v2 passthrough path (blob.version === 2): after the existing rejectedDays injection, add: if (!blob.activityLog || typeof blob.activityLog !== 'object' || Array.isArray(blob.activityLog)) { blob.activityLog = {}; }

    **Part C — tests/unit/db-shape.test.js additions:**

    Add three test cases to the existing test file:
    - migrateV1ToV2(null, defaults) returns blob with activityLog: {}
    - migrateV1ToV2({ version: 1, events: [] }, defaults) returns blob with activityLog: {}
    - migrateV1ToV2({ version: 2, settings: {...}, events: [] }, defaults) injects activityLog: {} when absent

    Run after implementation:
    node --test tests/unit/csv-parse.test.js
    node --test tests/unit/db-shape.test.js
    Expected: all tests PASS. Commit: feat(05-01): implement csv-parse.js and activityLog schema injection [GREEN]
  </action>
  <verify>
    <automated>node --test tests/unit/csv-parse.test.js && node --test tests/unit/db-shape.test.js</automated>
  </verify>
  <done>
    All csv-parse unit tests pass (all parsing branches covered); all db-shape tests pass including three new activityLog injection tests; no regressions in existing db-shape tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV file content → parseCSV() | User-supplied file bytes; untrusted text crosses the FileReader boundary and enters the parser |
| Parsed events → event store | Parser output (at-strings, type strings) flows into store replace() in Wave 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | parseCSV — malformed time strings | mitigate | parseEventAt wraps parseLocalISO which throws on malformed input; catch in row loop → row added to skipped[] with reason; no crash, no silent bad data |
| T-05-01-02 | Tampering | parseCSV — excessively large CSV (memory) | accept | Use case is seeding from months of daily records (~hundreds of rows); linear split + parse is safe. File size limit is a Phase 8 concern. |
| T-05-01-03 | Information Disclosure | activityLog injected into every saved blob | accept | Activity data is user's own data, stored locally. No PII beyond what's already in events. |
| T-05-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new runtime dependencies added in this plan; csv-parse.js is pure vanilla JS |
</threat_model>

<verification>
**Per-task verification:**
1. Task 1 (RED): test file exists; running it produces import/module errors (not syntax errors) — RED confirmed
2. Task 2 (GREEN): node --test tests/unit/csv-parse.test.js all pass; node --test tests/unit/db-shape.test.js all pass including new activityLog cases

**Wave 1a gate:**
- js/lib/csv-parse.js exports parseCSV with correct return shape
- db-shape.js fresh/v1/v2 paths all produce activityLog: {}
- Zero regressions in existing unit tests
- No DOM, no I/O in csv-parse.js or db-shape.js (pure modules only)
</verification>

<success_criteria>
- [ ] parseCSV correctly parses semicolon-delimited Polish-header CSV with dot dates
- [ ] parseCSV correctly parses comma-delimited English-header CSV with ISO dates
- [ ] Nap columns optional: empty → no nap events (D5-05)
- [ ] odrzucone column → rejectedDays array (D5-07)
- [ ] Aktywnosc column → activityLog map (D5-17)
- [ ] Bad rows skipped with row number + reason (D5-10)
- [ ] All event at-strings are 5-min-aligned (LOG-07)
- [ ] migrateV1ToV2 injects activityLog: {} in all three paths (D5-17)
- [ ] All unit tests pass; no regressions
</success_criteria>

<output>
Create `.planning/phases/NW-05-data-import-export/05-01-SUMMARY.md` when Wave 1a execution completes.
</output>
