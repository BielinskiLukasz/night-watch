---
phase: 21-prediction-normalization
plan: 03
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - tests/e2e/next-reachable-event.spec.js
autonomous: true
requirements: [PRED-24, UI-13]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "E2E coverage exists for every one of the 5 nextReachableEvent paths: bedtime->wake, wake(open)->[napStart,bedtimeAfterWake], wake(napWindowClosed or past eveningHour)->[bedtimeAfterWake] with napStart fully absent, napStart->napEnd, napEnd->bedtime (ROADMAP Phase 21 Success Criterion 7)"
    - "The 'Later today' section is collapsed on initial page load in every one of the above states (D-11), and clicking its summary reveals the non-hero cards (D-10/D-12)"
    - "A TIF-shaped card nested inside 'Later today' auto-expands once the section is opened, without an additional click on the card itself (D-13)"
  artifacts:
    - tests/e2e/next-reachable-event.spec.js
  key_links: []
---

<objective>
Deliver the phase's final Success Criterion (ROADMAP Phase 21, #7): a dedicated E2E spec proving event-card visibility for every one of the 5 `nextReachableEvent` paths built in 21-01/21-02, plus the Later-Today collapse/expand and TIF auto-expand behaviors.

Purpose: 21-01 proved the architecture on one path (napStart-hidden); 21-02 built the full dual-hero/Later-Today rendering. This plan is the production-polish/validation slice per the tracer-first task-order pattern — it does not change any implementation, it locks the full state-space in place with regression coverage so a future phase cannot silently regress any of the 5 paths.
Output: `tests/e2e/next-reachable-event.spec.js`, covering all 5 paths plus the collapse/expand/auto-expand behaviors.
</objective>

<artifacts>
## Artifacts This Phase Produces (phase-wide list; this plan's addition marked (P03))

- (P01, prior plan) `js/lib/forecast-utils.js`, `napProbability()`'s `napWindowClosed` field, `sw.js` precache registration
- (P02, prior plan) `predictions.bedtimeAfterWake`, dual-hero rendering, "Later today" section
- (P03) `tests/e2e/next-reachable-event.spec.js` — the full 5-path E2E coverage matrix
</artifacts>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-prediction-normalization/21-CONTEXT.md
@.planning/phases/21-prediction-normalization/21-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E coverage for the 4 single-hero paths + Later Today default-collapsed state</name>
  <files>tests/e2e/next-reachable-event.spec.js</files>
  <read_first>
    tests/e2e/forecast.spec.js:1-96 (seed helpers to reuse verbatim: makeDb, makeEvents, seedAndReload)
    tests/e2e/forecast.spec.js (as updated by 21-02) — the current hero/Later-Today container conventions (`#next-event-card .next-event-hero`, `#forecast-cards .later-today-section`), and the `data-event-type` attribute added in 21-01
    js/lib/forecast-utils.js (21-01's output — the exact 5-path return values, for picking fixtures that land in each branch)
  </read_first>
  <action>
    Create `tests/e2e/next-reachable-event.spec.js`. Reuse `makeDb`/`makeEvents`/`seedAndReload` from `tests/e2e/forecast.spec.js`'s pattern (either import them if exported, or copy the same three small helpers locally — match whichever convention 21-02 left in place). For each of the 4 single-hero paths below, pin `page.clock.setFixedTime` to a deterministic time consistent with the fixture (before any relevant P90/eveningHour boundary unless the test is specifically about the closed state), seed the fixture so the LAST logged event by timestamp matches the branch under test, then assert on `[data-event-type="..."]` and the `.next-event-hero`/`.later-today-section` containers:

    1. `bedtime -> wake`: seed a stable 32-day cycle (wake/napStart/napEnd/bedtime daily) — last logged event is that day's bedtime. Assert `#next-event-card .next-event-hero [data-event-type="wake"]` (or the hero's own type indicator) is visible and there is exactly ONE `.next-event-hero` element (no `.hero-row` dual wrapper). Open `.later-today-section` and assert it contains cards for `napStart`, `napEnd`, and `bedtime` (3 cards).

    2. `napStart -> napEnd`: seed a fixture whose last logged event is a `napStart` (truncate the final day after napStart, no napEnd/bedtime yet that day). Assert the single hero is `napEnd`; Later Today (opened) contains `wake`, `napStart`, `bedtime`.

    3. `napEnd -> bedtime`: seed a fixture whose last logged event is a `napEnd`. Assert the single hero is `bedtime`; Later Today (opened) contains `wake`, `napStart`, `napEnd`.

    4. `wake (window closed) -> bedtimeAfterWake only, napStart fully absent`: seed a fixture whose last logged event is `wake` (today's wake logged, nothing else yet), with napStart history flat at an early time (e.g. `'08:00'`, so P90 = `'08:00'`); pin the clock past that P90 (e.g. `'20:00'` the same day, also past the default `eveningHour` of 18 — this exercises both halves of the D-05 OR-condition at once, which is acceptable for this state's test; a boundary-specific test isolating just one half of the OR is not required here since 21-01's unit tests already isolate them individually). Assert exactly ONE `.next-event-hero`, and `page.locator('[data-event-type="napStart"]')` has count 0 ANYWHERE on the page (not hero, not in Later Today once opened) — reuse the same assertion shape as 21-01's tracer E2E test, now as part of the phase's permanent coverage matrix rather than a one-off.

    For every one of the 4 tests above, additionally assert `.later-today-section` has no `open` attribute (or equivalently, its contained cards are not visible) immediately after `seedAndReload`, BEFORE clicking its `<summary>` — proving D-11's collapsed-by-default behavior holds across every state, not just one.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/next-reachable-event.spec.js --grep "bedtime -> wake|napStart -> napEnd|napEnd -> bedtime|window closed"</automated>
    <fails_when>non-zero exit code, or any of the 4 grepped tests fails or reports 0 matched tests (grep pattern typo)</fails_when>
  </verify>
  <acceptance_criteria>
    - 4 passing tests, one per single-hero path, each asserting the correct hero type and the correct 3-card Later-Today contents
    - Every test confirms `.later-today-section` starts collapsed before any interaction
    - The window-closed test confirms `napStart` is absent from the entire page, not merely absent from the hero slot
  </acceptance_criteria>
  <done>All 4 single-hero nextReachableEvent paths have permanent E2E regression coverage.</done>
</task>

<task type="auto">
  <name>Task 2: E2E coverage for the dual-hero ambiguous path + TIF auto-expand-in-Later-Today</name>
  <files>tests/e2e/next-reachable-event.spec.js</files>
  <read_first>
    js/lib/forecast-utils.js (21-01's nextReachableEvent — the wake-branch's 2-element array case)
    tests/e2e/tif.spec.js (as updated by 21-02) — makeWakeOnlyDb and the TIF-card container conventions this plan's second test extends
  </read_first>
  <action>
    In the same `tests/e2e/next-reachable-event.spec.js` file, add:

    5. `wake (window open) -> dual hero [napStart, bedtimeAfterWake]`: seed a fixture whose last logged event is `wake` (today's wake logged, nothing else yet) with a full napStart/napEnd/bedtime history on all prior days so `predictions.napStart` and `predictions.bedtimeAfterWake` both resolve to real data; pin the clock BEFORE both the napStart P90 and the default `eveningHour` (e.g. `'10:00'` the same day). Assert `#next-event-card .hero-row` is visible, contains exactly 2 `.next-event-hero` children, one with `data-event-type="napStart"` and one with `data-event-type="bedtimeAfterWake"`, each showing a distinct (non-identical) predicted time. Open `.later-today-section` and assert it contains `wake` and `napEnd` only (2 cards — `napStart` and `bedtimeAfterWake`'s underlying `bedtime` type are both excluded because they're hero types).

    6. `TIF auto-expand inside Later Today (D-13)`: switch algorithm to `'tif'` (seed `forecastAlgorithm: 'tif'` in settings, reusing `tif.spec.js`'s `makeWakeOnlyDb`-style seed but with enough event-type variety that at least one TIF-rendered event type lands in Later Today rather than the hero slot — verify which type that is by running the test and reading the actual DOM, per 21-02 Task 3's already-established pattern for this exact fixture). Before opening `.later-today-section`, assert the TIF card inside it (once located) has NOT yet had its own manual-click applied. Click `.later-today-section > summary` to open the section. Assert the TIF card no longer has the `.collapsed` class and its `.card-full` content (source windows, precision badge) is now visible — without any additional click on the card itself.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/next-reachable-event.spec.js --grep "dual hero|auto-expand"</automated>
    <fails_when>non-zero exit code, or either grepped test fails or reports 0 matched tests</fails_when>
  </verify>
  <acceptance_criteria>
    - The dual-hero test confirms exactly 2 side-by-side hero cards with distinct predicted times and the correct Later-Today remainder
    - The TIF auto-expand test confirms a card transitions out of its `.collapsed` state purely from the outer section opening, with no direct click on the card
  </acceptance_criteria>
  <done>The ambiguous dual-hero path and the TIF auto-expand interaction both have permanent E2E regression coverage, completing ROADMAP Phase 21 Success Criterion 7.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds test-only code; it introduces no new production trust boundary beyond what 21-01/21-02 already registered.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-06 | n/a | `tests/e2e/next-reachable-event.spec.js` | low | accept | Test-only file; seeds only synthetic in-memory fixtures via `localStorage`/`page.evaluate`, exercises no new production code path beyond what 21-01/21-02 already shipped and threat-modeled. |
| T-21-SC | Tampering (Supply Chain) | npm/pip/cargo installs | n/a | accept | No package installs in this phase (vanilla-JS, no-dependency project per CLAUDE.md). |
</threat_model>

<verification>
- `npx playwright test tests/e2e/next-reachable-event.spec.js` — all 6 tests green
- `npm test` — full suite green (final phase-wide regression check)
</verification>

<success_criteria>
- All 5 `nextReachableEvent` paths (bedtime->wake, wake-open dual-hero, wake-closed single-hero with napStart absent, napStart->napEnd, napEnd->bedtime) have passing, deterministic E2E coverage
- Later Today's collapsed-by-default state and TIF auto-expand-on-open are both covered
- ROADMAP Phase 21 Success Criterion 7 ("E2E tests cover event-card visibility for each reachable-event state") is satisfied
</success_criteria>

<output>
Create `.planning/phases/21-prediction-normalization/21-03-SUMMARY.md` when done
</output>
