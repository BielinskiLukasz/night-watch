---
phase: 21-prediction-normalization
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - js/lib/forecast.js
  - js/ui/today-screen.js
  - style.css
  - tests/unit/forecast.test.js
  - tests/integration/today-hero-later-today.test.js
  - tests/e2e/forecast.spec.js
  - tests/e2e/tif.spec.js
autonomous: true
requirements: [PRED-24, UI-13]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "forecast()'s predictions.bedtimeAfterWake is the raw, unblended buildBedtimeSeriesNoNapDay(window, settings) result — independent of predictions.bedtime, which keeps its current PRED-19-blended meaning (D-09)"
    - "When the last logged event is wake and nap remains reachable (window open, before eveningHour), the Today screen shows napStart and bedtimeAfterWake as two equally-prominent hero cards side by side, each with its own predicted time (D-08)"
    - "Non-hero predictions render inside a single collapsed-by-default 'Later today' <details> section (no open attribute, matching the Phase 17 convention) that wraps the existing renderPredictionCard/renderTifNormalCard/renderTifLowConfidenceCard renderers unchanged (D-10/D-11/D-12)"
    - "A TIF card nested inside 'Later today' auto-expands (its own .collapsed class is removed) once the outer <details> is opened (D-13)"
    - { statement: "napProbability() and nextReachableEvent() together produce identical hero/Later-Today placement decisions for two different currentHour values that both fall before the nap window closes and before eveningHour (clock-invariance carries through to the UI layer, not just the pure functions)", verification: backstop }
  artifacts:
    - "js/ui/today-screen.js renderNextEventCard (exported, dual-hero capable)"
    - "js/ui/today-screen.js renderForecastSection (Later-Today reorg, replaces the flat EVENT_TYPES grid loop from 21-01)"
    - "style.css .hero-row / .later-today-section rules"
    - tests/integration/today-hero-later-today.test.js
  key_links:
    - "forecast()'s predictions.bedtimeAfterWake -> renderNextEventCard's dual-hero branch"
    - "nextReachableEvent(lastEvent, currentHour, {eveningHour, napWindowClosed}) called directly (not through selectNextEvent) -> renderForecastSection's hero-vs-Later-Today split"
---

<objective>
Expand the tracer slice from 21-01 into the full UI capability: add `predictions.bedtimeAfterWake` to `forecast()`'s output (D-09), render two equally-prominent hero cards when nap status is genuinely undetermined (D-08), and replace the flat 4-card grid with a collapsed-by-default "Later today" section wrapping the existing card renderers (D-10/D-11/D-12/D-13). Fix the pre-existing E2E assertions that this reorg structurally breaks.

Purpose: 21-01 proved the domain-logic contract end-to-end on the thinnest possible slice (single-hero, grid-skip only). This plan builds the actual user-facing capability the phase goal describes — "shows only the next realistically-reachable sleep event prominently" — on top of that already-proven foundation.
Output: A Today screen that shows 1 or 2 hero cards per the 5-path model, with everything else tucked into a collapsed "Later today" section.
</objective>

<artifacts>
## Artifacts This Phase Produces (phase-wide list; this plan's additions marked (P02))

- (P01, prior plan) `js/lib/forecast-utils.js`, `napProbability()`'s `napWindowClosed` field, `sw.js` precache registration
- (P02) `js/lib/forecast.js` — `forecast()` gains a new top-level `predictions.bedtimeAfterWake` field (D-09)
- (P02) `js/ui/today-screen.js` — `renderNextEventCard` exported and extended for dual-hero rendering; `renderForecastSection` rewritten to call `nextReachableEvent` directly and build the hero-row + "Later today" `<details>` structure
- (P02) `style.css` — `.hero-row` (side-by-side dual-hero layout) and `.later-today-section` rules
- (P02) `tests/integration/today-hero-later-today.test.js` — fast Node-only DOM-mock test for the new render logic (mirrors `tests/integration/today-card-collapse.test.js`'s pattern)
- (P03, next plan) Full E2E coverage matrix for all 5 `nextReachableEvent` paths
</artifacts>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-prediction-normalization/21-CONTEXT.md
@.planning/phases/21-prediction-normalization/21-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: forecast() gains predictions.bedtimeAfterWake (D-09)</name>
  <files>js/lib/forecast.js, tests/unit/forecast.test.js</files>
  <read_first>
    js/lib/forecast.js:748-808 (bedtimePred IIFE — the exact block that computes predictions.bedtime; bedtimeAfterWake is added as a sibling top-level field, not nested inside this IIFE's return)
    js/lib/forecast.js:556-561 (buildBedtimeSeriesNoNapDay — the function bedtimeAfterWake reuses directly)
    .planning/phases/21-prediction-normalization/21-CONTEXT.md D-09
  </read_first>
  <behavior>
    RED: in `tests/unit/forecast.test.js`, add a new describe block for `predictions.bedtimeAfterWake`. Test 1: with a window containing both nap-day and no-nap-day bedtime records (>= minDays each), `forecast()`'s result has `bedtimeAfterWake` as a `{ central, min, max }` object whose values equal calling `buildBedtimeSeriesNoNapDay(window, settings)` directly and running it through the same probability-band check `forecast()` uses elsewhere. Test 2: `bedtimeAfterWake` is present and non-null even when `predictions.bedtime` differs from it (e.g. when `napStartLogged: true` routes `predictions.bedtime` through the nap-day series per PRED-18 — `bedtimeAfterWake` must still reflect ONLY the no-nap-day series, proving independence). Test 3: when the no-nap-day sub-window is thin (< minDays), `buildBedtimeSeriesNoNapDay` returns null, so `predictions.bedtimeAfterWake` is `null` (not an empty object, not `{central:null,...}` — genuinely `null`, matching `buildBedtimeSeriesNoNapDay`'s own null-on-thin-history contract). Run and confirm RED (field does not exist yet).
  </behavior>
  <action>
    In `js/lib/forecast.js`'s `forecast()` function, immediately after the `bedtimePred` IIFE closes (~808) and before the `napGaps`/`napStartPred` block, compute: `const bedtimeAfterWakeSeries = buildBedtimeSeriesNoNapDay(window, settings);` then `const bedtimeAfterWakePred = bedtimeAfterWakeSeries !== null ? selectBedtime(bedtimeAfterWakeSeries) : null;` — reusing the `selectBedtime` helper already defined inside the `bedtimePred` IIFE requires either lifting `selectBedtime` out of that IIFE to module-function scope (preferred — it only closes over `bedtimeTimes`/`maxDelta`, both derivable from `window`/`settings` at the top level) or duplicating its two-line body inline; prefer lifting it out since it is now used twice. Add `bedtimeAfterWake: bedtimeAfterWakePred` to the final returned object (~845-851), as a sibling of `bedtime`, `wake`, `napStart`, `napEnd`. Update the `forecast()` JSDoc's return-shape description to document the new field.
  </action>
  <verify>
    <automated>node --test tests/unit/forecast.test.js</automated>
    <fails_when>non-zero exit code, or "0 passed" appears for the new bedtimeAfterWake describe block, or any pre-existing forecast() test regresses</fails_when>
  </verify>
  <acceptance_criteria>
    - `forecast()`'s return object always includes a `bedtimeAfterWake` key (object or `null`), independent of `bedtime`'s value
    - `predictions.bedtimeAfterWake` is `null` exactly when `buildBedtimeSeriesNoNapDay(window, settings)` is `null`, never a partial/empty object
  </acceptance_criteria>
  <done>forecast() exposes the raw no-nap-day bedtime prediction as its own field, ready for dual-hero rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Dual-hero rendering + "Later today" section</name>
  <files>js/ui/today-screen.js, style.css, tests/integration/today-hero-later-today.test.js</files>
  <read_first>
    js/ui/today-screen.js:118-192 (renderNextEventCard — currently single-prediction, not exported), 494-547 (renderForecastSection as left by 21-01, including the napStartHiddenToday skip), 844-969 (render() — where forecastDaysOldestFirst/currentHour/eveningHour/lastEvent-relevant context is already computed)
    js/lib/forecast-utils.js (21-01's output — nextReachableEvent's exact signature and 5-path return values)
    style.css:848-1000 (existing .next-event-hero / .forecast-grid / .prediction-card rules — insert new rules adjacent to these)
    .planning/phases/17-day-of-week-patterns (or 21-PATTERNS.md's citation of it) — the no-open-attribute `<details>` convention (native collapse resets on every replaceChildren rebuild)
    .planning/phases/21-prediction-normalization/21-CONTEXT.md D-08, D-09, D-10, D-11, D-12, D-13
  </read_first>
  <action>
    Export `renderNextEventCard` (drop the `function` keyword's lack of `export`, add it — matches the existing `export function renderPredictionCard` precedent). Extend its signature to accept EITHER a single prediction (existing behavior, unchanged output) OR an array of 1-2 predictions; when given a 2-element array, build a wrapper `el('div', { className: 'hero-row' })` containing two independently-rendered `.next-event-hero` cards (reuse the existing single-prediction rendering logic for each one — do not duplicate the body, extract it into a private `renderOneHeroCard(prediction, timeFormat)` helper that `renderNextEventCard` calls once or twice).

    Rewrite `renderForecastSection` (~494-547, superseding 21-01's flat EVENT_TYPES loop and its `napStartHiddenToday` skip — the same drop condition is now computed once and reused by both the hero-selection and the Later-Today loop):
    1. Determine `lastEvent` (same allEvents scan `forecast-utils.js` performs internally — add a small local helper or accept it as a new parameter threaded from `render()`, since `render()` already has `dayRecords`/`forecastDaysOldestFirst` in scope).
    2. Compute `currentHour` (already available in `render()`'s scope) and `napWindowClosed` from `predictions.napStart?.napProbabilityScore?.napWindowClosed === true`.
    3. Call `nextReachableEvent(lastEvent, currentHour, { eveningHour: settingsSnap.eveningHour, napWindowClosed })` directly (NOT `selectNextEvent` — the array-based dual-hero case needs the full array, and `selectNextEvent`'s singular-result contract from 21-01 stays available for any other caller needing the old single-result shape).
    4. Map the returned array to hero predictions using the SAME `PREDICTION_FIELD`/fallback rules 21-01 built into `selectNextEvent` (predictions.bedtimeAfterWake ?? predictions.bedtime for the `'bedtimeAfterWake'` entry; predictions.bedtime for `'bedtimeAfterNap'`; direct lookup otherwise) — build an array of `{ type, ...prediction, isMissed }` hero objects (isMissed computed the same way as before, only when `.central` is set). Pass this array to `renderNextEventCard`.
    5. Compute `napStartHiddenToday` exactly as 21-01 defined it: `(lastEvent === null || lastEvent.type === 'wake') && (napWindowClosed || currentHour >= (settingsSnap.eveningHour ?? 18))` — D-04's carve-out means this does NOT apply once `lastEvent.type` is `'napStart'`/`'napEnd'`/`'bedtime'`.
    6. Build the "Later today" section: `const laterToday = el('details', { className: 'later-today-section' });` (no `open` attribute — D-11, matches Phase 17's convention) with `laterToday.appendChild(el('summary', { textContent: 'Later today' }));`. Iterate the fixed `EVENT_TYPES = ['wake', 'napStart', 'napEnd', 'bedtime']` order: skip any type that is one of the hero result types (compare against the hero array's `.type` values — note `'bedtimeAfterWake'` maps back to skipping `'bedtime'` from this loop too, since they represent the same logged event type); skip `napStart` entirely when `napStartHiddenToday` is true (D-04/D-05, regardless of whether it would have been a hero type); otherwise append its card via the EXISTING unchanged logic (`renderTifLowConfidenceCard`/`renderTifNormalCard`/`renderPredictionCard`, same `precisionScore`/`isLowConfidence` branching as before) into `laterToday`. Append `laterToday` to `forecastCards` (only element now appended there — hero cards go to `nextEventCard`, never `forecastCards`).
    7. D-13 auto-expand: add a `'toggle'` event listener on `laterToday` (native `<details>` fires `toggle` when opened/closed) that, when `laterToday.open` is true, finds every `.tif-card.collapsed`/`.probability-band.collapsed` child inside `laterToday` and removes their `.collapsed` class plus flips their `.card-chevron` text to `'↑'` (mirroring the existing click-toggle behavior in `renderTifNormalCard`/`renderTifLowConfidenceCard`/`renderPredictionCard`'s probability-band branch) — this only needs to run once per open (re-running on an already-expanded card is a harmless no-op, but guard with a check on the `.collapsed` class to avoid re-triggering an already-manually-collapsed card the user just closed).

    Add CSS: `.hero-row { display: flex; gap: 0.75rem; margin-bottom: 1rem; }` with each `.hero-row .next-event-hero { flex: 1; margin-bottom: 0; }` (override the standalone `.next-event-hero`'s `margin-bottom: 1rem` when nested), plus a `@media (max-width: 480px) { .hero-row { flex-direction: column; } }` stacking rule (matches the existing `.forecast-grid` mobile breakpoint at ~944-948). Add `.later-today-section { margin-top: 0.5rem; } .later-today-section summary { cursor: pointer; font-weight: 600; color: #475569; padding: 0.5rem 0; } .later-today-section > *:not(summary) { margin-top: 0.75rem; }` adjacent to the existing `.forecast-grid` rules (~935-948).

    Create `tests/integration/today-hero-later-today.test.js`, mirroring `tests/integration/today-card-collapse.test.js`'s MockNode DOM-mock setup (install the mock on `globalThis` before importing `today-screen.js`). Cover: (a) `renderNextEventCard` given a 2-element prediction array produces a `.hero-row` with exactly 2 `.next-event-hero` children; (b) given a single prediction, output is unchanged from the pre-existing single-hero shape (no `.hero-row` wrapper); (c) the `toggle` listener removes `.collapsed` from a nested TIF-shaped card once `laterToday.open` is simulated true (dispatch a synthetic `'toggle'` event on the mock `<details>` node — extend `MockNode` with a minimal `open` property + `dispatchEvent`/listener-invocation if the existing mock does not already support it).
  </action>
  <verify>
    <automated>node --test tests/integration/today-hero-later-today.test.js</automated>
    <fails_when>non-zero exit code, or "0 passed" in the summary, or the dual-hero/auto-expand assertions fail</fails_when>
  </verify>
  <acceptance_criteria>
    - `renderNextEventCard` is exported and handles both single-prediction and 2-element-array inputs
    - `renderForecastSection` calls `nextReachableEvent` directly and no longer contains the flat 21-01 EVENT_TYPES grid loop
    - `#forecast-cards` contains exactly one `.later-today-section` `<details>` element with no `open` attribute in its initial HTML
    - A TIF-shaped card nested in Later Today loses its `.collapsed` class when the outer `<details>` fires `toggle` in the open state
  </acceptance_criteria>
  <done>The Today screen renders 1 or 2 hero cards per the 5-path model and tucks everything else into a collapsed-by-default Later Today section, with existing per-card renderers reused unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Fix pre-existing E2E assertions broken by the hero/Later-Today DOM split</name>
  <files>tests/e2e/forecast.spec.js, tests/e2e/tif.spec.js</files>
  <read_first>
    tests/e2e/forecast.spec.js:100-238 (all 7 existing tests — Tests 2, 4, 5, and 7 assert directly against `#forecast-cards .prediction-card`, which after Task 2 only ever contains the `.later-today-section` wrapper, never a hero)
    tests/e2e/tif.spec.js:98-173 (Tests 2 and 3 assert directly against `#forecast-cards .tif-card`)
  </read_first>
  <action>
    Apply these invariant rules to every impacted assertion (do not change fixture seed data unless a test's original intent can no longer be exercised any other way):
    1. Hero-card content (whichever event type(s) `nextReachableEvent` selects) lives under `#next-event-card`, rendered via `renderNextEventCard`'s hero-card markup (`.next-event-hero`, no `.collapsed`/`.card-summary`/`.card-full` mechanism) — it never appears under `#forecast-cards` anymore.
    2. Every non-hero-non-dropped card lives under `#forecast-cards .later-today-section`, which starts closed. Any assertion checking a non-hero card's visibility or collapsed state must first open the section: locate `page.locator('.later-today-section summary')` and `.click()` it (this also triggers D-13's TIF auto-expand for any TIF card inside).
    3. `forecast()`/`napProbability()` always produce a truthy per-event-type object (even `{central:null,...}`) for `wake`/`bedtime`/`napStart`/`napEnd` — so a card renders for every one of those 4 types unless it is the hero or is the napStart-drop special case; use `page.clock.setFixedTime(...)` to pin a deterministic `currentHour` for any test whose outcome depends on which type becomes hero or whether napStart drops (the existing Test 7 already does this — extend the same pattern to Tests 2, 4, and 5, which currently do not pin the clock).
    Concretely, for `tests/e2e/forecast.spec.js`: Test 2 (~100-122) — pin the clock to a morning hour on the fixture's last seeded date (before `eveningHour` and before the seeded napStart P90), assert `#next-event-card .next-event-hero` is visible, then open `.later-today-section` and assert the remaining `.prediction-card` count matches whatever is NOT the hero type (3, given the 32-day baseline fixture's last logged event is a same-day bedtime -> hero is wake -> Later Today holds napStart/napEnd/bedtime). Test 4/5 (~144-200, probability-band collapse/expand) — after opening `.later-today-section`, re-locate the probability-band card inside it (it may or may not be the hero depending on whether any other event type resolves to real data first — run the test and adjust the locator's scope to `.later-today-section .prediction-card.probability-band` if it lands there, or to `#next-event-card` if it becomes the hero; if it becomes the hero, note in this test that the collapse/expand mechanism does not apply to hero cards — adjust the fixture to add a second, stable-time event type with real data so the probability-band wake card is NOT selected as hero, preserving this test's original intent of exercising grid-level collapse/expand). Test 7 (~219-238, missed predictions) — the missed prediction becomes the hero in this fixture (bedtime -> wake); update the locator from `#forecast-cards .prediction-card.missed` to `#next-event-card .next-event-hero.missed`.
    For `tests/e2e/tif.spec.js`: Tests 2 and 3 seed wake-only data, so `wake` itself never appears as a hero candidate for a `lastEvent.type === 'wake'` scan (the wake-branch's own candidates are `napStart`/`bedtimeAfterWake`, not `wake`) — run the test to confirm which type actually becomes hero for this fixture and adjust the `.tif-card` locator's container scope accordingly (either `#next-event-card` with the TIF precision-badge assertions adapted to the hero markup, or `.later-today-section .tif-card` with the section opened first, and Test 2's "starts collapsed" assertion adjusted to "starts collapsed until Later Today is opened, then auto-expands per D-13" rather than "collapsed after page load").
    Do not weaken coverage: every currently-tested behavior (probability-band collapse/expand, missed-label rendering, TIF precision badge, TIF card removal on algorithm switch) must still have a passing assertion somewhere after this task, just via the correct container.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/forecast.spec.js tests/e2e/tif.spec.js</automated>
    <fails_when>non-zero exit code, or any test in either file fails or is skipped</fails_when>
  </verify>
  <acceptance_criteria>
    - All 7 existing tests in `tests/e2e/forecast.spec.js` pass against the new hero/Later-Today DOM structure
    - All 3 existing tests in `tests/e2e/tif.spec.js` pass against the new hero/Later-Today DOM structure
    - No assertion was deleted without a replacement covering the same behavior
  </acceptance_criteria>
  <done>The full pre-existing E2E suite is green against the new DOM structure, with no coverage silently dropped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `forecast()`'s computed prediction values → dual-hero / Later-Today DOM | New `bedtimeAfterWake` field and the hero/Later-Today split are pure presentation reorganizations of already-validated in-memory prediction objects |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-04 | Information Disclosure | `js/ui/today-screen.js` `.hero-row` / `.later-today-section` new DOM writes | low | mitigate | Every new element (`hero-row` wrapper div, `later-today-section` `<details>`/`<summary>`, the `'Later today'` label) is created via `el()` with `textContent`/attribute-only props — no `innerHTML` is introduced anywhere in this task's new rendering code (CLAUDE.md's dom.js XSS guard). |
| T-21-05 | Tampering | `js/lib/forecast.js` `predictions.bedtimeAfterWake` | low | accept | Purely derived from existing in-memory day-record percentile math (`buildBedtimeSeriesNoNapDay`) — no new external input path, nothing newly persisted. |
| T-21-SC | Tampering (Supply Chain) | npm/pip/cargo installs | n/a | accept | No package installs in this phase (vanilla-JS, no-dependency project per CLAUDE.md). |
</threat_model>

<verification>
- `node --test tests/unit/forecast.test.js tests/integration/today-hero-later-today.test.js` — green
- `npx playwright test tests/e2e/forecast.spec.js tests/e2e/tif.spec.js` — green
- `npm test` — full suite green, confirming no other spec (accuracy/metrics/charts/history/etc.) references `#forecast-cards`/`#next-event-card` in a way this reorg breaks
</verification>

<success_criteria>
- `forecast()` exposes `predictions.bedtimeAfterWake` independently of `predictions.bedtime`
- Dual hero cards render side by side exactly when nap status is genuinely undetermined
- Every non-hero, non-dropped prediction lives inside a single collapsed-by-default "Later today" section
- All pre-existing E2E coverage still passes, relocated to the correct container
</success_criteria>

<output>
Create `.planning/phases/21-prediction-normalization/21-02-SUMMARY.md` when done
</output>
