---
phase: 21-prediction-normalization
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - js/lib/forecast.js
  - js/lib/forecast-utils.js
  - js/ui/today-screen.js
  - sw.js
  - tests/unit/forecast.test.js
  - tests/unit/forecast-utils.test.js
  - tests/unit/sw-precache.test.js
  - tests/e2e/forecast.spec.js
autonomous: true
requirements: [PRED-23, PRED-24]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "napProbability()'s score never coerces to 0 solely because napWindowClosed is true — the real weighted score persists unchanged across the window-close boundary (Phase 20 D-11/D-12, this phase's D-01/D-02)"
    - "At the exact P90-minute boundary, napWindowClosed is false — only strictly after P90 does it become true (the strict > comparison is inherited unchanged from the removed hard-collapse branch, D-03)"
    - "At the exact eveningHour boundary (currentHour === settings.eveningHour), napStart is already excluded from nextReachableEvent's wake-branch candidates (>= comparison, D-05)"
    - "nextReachableEvent(lastEvent, currentHour, settings) exists as a pure export of js/lib/forecast-utils.js and selectNextEvent delegates all priority logic to it (D-06/D-07) — forecast.js no longer defines selectNextEvent"
    - "When the last logged event is wake (today's nap has not started) and the nap window has closed (napWindowClosed true or currentHour >= eveningHour), the napStart card is absent from every container on the Today screen — not a hero, not in the grid (D-01/D-04/D-05)"
  artifacts:
    - js/lib/forecast-utils.js
    - tests/unit/forecast-utils.test.js
    - "js/lib/forecast.js napProbability() return object gains napWindowClosed"
  key_links:
    - "napProbability()'s napWindowClosed field -> selectNextEvent's read of predictions.napStart.napProbabilityScore.napWindowClosed -> nextReachableEvent's D-05 drop condition -> today-screen.js napStart-card suppression"
---

<objective>
Establish the phase's domain-logic foundation: decouple `napWindowClosed` from `napProbability()`'s score (finishing Phase 20's D-11/D-12 amendment, which shipped without this piece — see STATE.md Phase 20 note and `.planning/phases/20-nap-probability-redesign/20-CONTEXT.md`), and extract `nextReachableEvent`/`selectNextEvent` out of `js/lib/forecast.js` into a new `js/lib/forecast-utils.js` implementing the 5-path event model (D-06/D-07). Wire the minimum real UI change end-to-end so the capability is observable: the `napStart` card fully disappears from the Today screen once the nap window has closed. Dual hero cards, the "Later today" section, and `predictions.bedtimeAfterWake` are Plan 21-02's job — this plan proves the domain-logic contract and the file-extraction architecture first.

Purpose: `nextReachableEvent`'s `string[]` return contract (D-07) is a one-way public-API change — every later task in this phase, and every future caller of `selectNextEvent`, is built on top of it. Proving it end-to-end (through a real render + a real E2E check) before building the fuller dual-hero/Later-Today UI on top catches an architectural mistake here, on the cheapest possible slice, instead of after three more layers are already built on it.
Output: `js/lib/forecast-utils.js` (new), `napProbability()`'s corrected return shape, `sw.js` PRECACHE_LIST registration, and a Today screen that already hides `napStart` correctly when the window closes.
</objective>

<artifacts>
## Artifacts This Phase Produces (phase-wide list; this plan's additions marked (P01))

- (P01) `js/lib/forecast-utils.js` — new module exporting `nextReachableEvent(lastEvent, currentHour, settings)` and `selectNextEvent(predictions, dayRecords, settings)`
- (P01) `js/lib/forecast.js` — `napProbability()`'s return object gains `napWindowClosed: boolean`; the old hard-collapse-to-0 branch is removed; `selectNextEvent` and its JSDoc are removed (moved to forecast-utils.js)
- (P01) `js/ui/today-screen.js` — import split (`selectNextEvent` now from `../lib/forecast-utils.js`), the two nap-probability-percentage ternary render sites fixed (no more `score === 0` special case), a `data-event-type` attribute added to prediction/hero card root elements for E2E targetability, and the napStart-drop skip wired into the existing 4-card grid loop
- (P01) `sw.js` `PRECACHE_LIST` gains `./js/lib/forecast-utils.js`; `tests/unit/sw-precache.test.js` gains the matching assertion
- (P02, next plan) `predictions.bedtimeAfterWake` on `forecast()`'s output; dual-hero rendering; "Later today" `<details>` section
- (P03, next plan) Full E2E coverage matrix for all 5 `nextReachableEvent` paths
</artifacts>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-prediction-normalization/21-CONTEXT.md
@.planning/phases/21-prediction-normalization/21-PATTERNS.md
@.planning/phases/20-nap-probability-redesign/20-CONTEXT.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: napWindowClosed decoupling + nextReachableEvent/selectNextEvent extraction, wired to hide napStart on the Today screen</name>
  <reversibility rating="one-way">D-07's `nextReachableEvent` array-return contract (string[], 1-2 elements) replaces the old single-type priority switch and touches every existing caller of `selectNextEvent` (Phase 3/8/12 code + its own test suite). This exact contract was explicitly decided by the user during /gsd-discuss-phase (confirmed twice per 21-CONTEXT.md &lt;specifics&gt;) — per planner-reversibility.md's "gating a decision already made" anti-pattern, no checkpoint is inserted here; the rating is recorded so the weight of this door is visible in the plan.</reversibility>
  <files>js/lib/forecast.js, js/lib/forecast-utils.js, js/ui/today-screen.js, tests/unit/forecast.test.js, tests/unit/forecast-utils.test.js, tests/e2e/forecast.spec.js</files>
  <read_first>
    js/lib/forecast.js:992-1130 (NAP_SCORE_WEIGHTS + napProbability, the hard-collapse branch to remove is at ~1123-1129)
    js/lib/forecast.js:854-990 (selectNextEvent — the exact function body being extracted)
    js/lib/forecast.js:1-58 (file header's "Exported Functions" list — must drop the selectNextEvent entry)
    js/ui/today-screen.js:41-46 (current import line), 118-192 (renderNextEventCard), 166-172 and 284-290 (the two nap-probability ternary sites), 494-547 (renderForecastSection's EVENT_TYPES loop)
    tests/unit/forecast.test.js:925-1189 (all selectNextEvent + PRED-08 describe blocks — these move to the new file), 2410-2642 (PRED-12 napProbability describe block — 3 tests reference the window-closed collapse and must be updated: "window already passed" ~2539, "window-closed returns 0" ~2552, "window-closed exact shape" ~2625)
    tests/integration/today-card-collapse.test.js:1-60 (MockNode DOM-mock pattern — not used directly by this task, but establishes the project's precedent for testing today-screen.js render helpers without a browser; informs Task naming for Plan 21-02's later integration test)
    tests/e2e/forecast.spec.js:1-96 (seed helpers `makeDb`/`makeEvents`/`seedAndReload` — reuse verbatim for the new E2E test)
    .planning/phases/21-prediction-normalization/21-CONTEXT.md D-01 through D-09, D-14 (napWindowClosed semantics, the 5-path model, file placement)
  </read_first>
  <behavior>
    RED — write failing tests first, across three layers:
    1. In `tests/unit/forecast.test.js`'s PRED-12 describe block: update the three existing window-closed tests (~2539 "window already passed", ~2552 "window-closed returns 0", ~2625 "window-closed exact shape") so each asserts `result.napWindowClosed === true` AND `result.score` equals the SAME non-collapsed value napProbability() would compute for an identical dayRecords/settings/context but with `currentHour` set to an hour BEFORE the nap window closes (i.e. score is clock-invariant across the close boundary, not forced to 0). Also update the cold-start tests (~2487 "empty dayRecords", ~2493 "dayRecords below minDays") to expect `napWindowClosed: false` in the returned object (the cold-start object gains this key too). Add one new boundary test: `currentMinute`/`currentHour` set to exactly the napStart P90 minute → `napWindowClosed === false` (strict `>` semantics, not `>=`); one minute later → `napWindowClosed === true`.
    2. Create `tests/unit/forecast-utils.test.js` (new file, import `nextReachableEvent`/`selectNextEvent` from `../../js/lib/forecast-utils.js`). Move the entire content of the three describe blocks at forecast.test.js:925-1189 into this new file (delete them from forecast.test.js once moved). Update every assertion whose expected `.type` was reached via the wake-branch's second/fallback candidate: per the mapping table below, those now resolve to `'bedtimeAfterWake'` instead of `'bedtime'`. Assertions reached via the napEnd-branch (napEnd -> bedtime) are unaffected (bedtimeAfterNap normalizes back to 'bedtime', see below) and keep their existing expected values unchanged. Add new tests for `nextReachableEvent` directly (not through the wrapper): the 5-path table (bedtime->['wake'], wake(open)->['napStart','bedtimeAfterWake'], wake(napWindowClosed=true)->['bedtimeAfterWake'], wake(currentHour>=eveningHour)->['bedtimeAfterWake'], napStart->['napEnd'], napEnd->['bedtimeAfterNap'], unknown-type->['wake']); one boundary test for currentHour === eveningHour exactly (drop fires, >= not >) and currentHour === eveningHour - 1 (does not fire).
    3. In `tests/e2e/forecast.spec.js`, add a new test: seed a fixture where the most recent logged event is `wake` (e.g. 7+ days of a stable wake/napStart/napEnd/bedtime cycle, with the FINAL day truncated to only a `wake` event — "today's wake logged, nothing else yet"), with napStart history flat at `'08:00'` (so its P90 is `08:00`). Pin `page.clock.setFixedTime` to a time well past both that P90 and the default `eveningHour` (e.g. `'2026-06-08T20:00:00'`, matching whatever the fixture's last day's date is +1). Assert `page.locator('[data-event-type="napStart"]')` has count 0 anywhere on the page, while `[data-event-type="wake"]`, `[data-event-type="napEnd"]`, `[data-event-type="bedtime"]` each have count >= 1 (proves the removal is specific to napStart, not a rendering breakage).
    Run all three test files/commands and confirm they fail for the right reason (napWindowClosed undefined, forecast-utils.js module not found, data-event-type attribute absent) before writing implementation.
  </behavior>
  <action>
    GREEN — implement in this order:

    1. `js/lib/forecast.js` napProbability() (~1048-1130): replace the cold-start early return's object literal to add `napWindowClosed: false`. Replace the hard-collapse branch (~1123-1129, `if (napStartResult !== null && napStartResult.max !== null && nowMins > napStartResult.max) { return { score: 0, signalsUsed, confidence }; }`) with a `const napWindowClosed = napStartResult !== null && napStartResult.max !== null && nowMins > napStartResult.max;` computed BEFORE the final return, then a single `return { score, signalsUsed, confidence, napWindowClosed };` — the weighted `score` computation above is untouched and always runs to completion regardless of `napWindowClosed`. Update the function's JSDoc (~1021-1047) to document the new field and remove references to score:0 meaning "window closed".

    2. Remove `selectNextEvent` entirely from `js/lib/forecast.js` (~854-990) and its JSDoc; remove its entry from the file-header "Exported Functions" list (~34-53).

    3. Create `js/lib/forecast-utils.js`. Header comment matches the `js/lib/time.js`/`js/lib/accuracy.js` module-doc convention (pure logic, no side effects, no DOM). Import `timeToMinutes` from `./forecast.js` (one-directional — forecast.js never imports from forecast-utils.js).

       `nextReachableEvent(lastEvent, currentHour, settings = {})`: pure function. Destructure `{ eveningHour = 18, napWindowClosed = false } = settings`. Compute `napStartDropped = napWindowClosed === true || currentHour >= eveningHour` (>= per D-05, matching the existing PRED-08 override's own `>=` comparison). Switch on `lastEvent.type`:
       - `'bedtime'` -> `['wake']`
       - `'wake'` -> `napStartDropped ? ['bedtimeAfterWake'] : ['napStart', 'bedtimeAfterWake']`
       - `'napStart'` -> `['napEnd']`
       - `'napEnd'` -> `['bedtimeAfterNap']`
       - default (unknown type) -> `['wake']`

       `selectNextEvent(predictions, dayRecords, settings = {})`: thin wrapper preserving the exact existing external contract (`{ type, isMissed, ...prediction } | null`). Step 1 (unchanged): scan `dayRecords` for the most-recent `lastEvent` via `allEvents`; return `null` if none. Step 2: `const nowHour = new Date().getHours(); // gsd:allow-ui-clock` — the ONE UI-clock read this wrapper performs on nextReachableEvent's behalf. Step 3: `const napWindowClosed = predictions.napStart?.napProbabilityScore?.napWindowClosed === true;` (safe optional-chain; defaults false when the field or the whole score object is absent — e.g. TIF algorithm active, or forecast() called directly in a unit test without napProbabilityScore threaded). Step 4: `const reachable = nextReachableEvent(lastEvent, nowHour, { eveningHour: settings?.eveningHour, napWindowClosed });`. Step 5: define a `PREDICTION_FIELD` map (`wake:'wake', bedtime:'bedtime', napStart:'napStart', napEnd:'napEnd', bedtimeAfterWake:'bedtimeAfterWake', bedtimeAfterNap:'bedtime'`) and a `RESULT_TYPE` map identical except it keeps `bedtimeAfterWake` as its own literal (needed so Plan 21-02's dual-hero code can distinguish it) while `bedtimeAfterNap` also normalizes to `'bedtime'` (per D-07's own parenthetical: these are prediction-calculation branches only, not new loggable event types — the user only ever logs a single `bedtime` event, so the RESULT label should read `'bedtime'` for that branch). Walk `reachable` in order: for each entry, resolve `fieldKey = PREDICTION_FIELD[entry]`, then `predEntry = predictions[fieldKey] ?? (entry === 'bedtimeAfterWake' ? predictions.bedtime : undefined)` (graceful fallback when `bedtimeAfterWake` doesn't exist yet — e.g. before Plan 21-02 adds it, or when TIF is active and never computes it); if `predEntry` is truthy, compute `isMissed` exactly as the old `buildResult` did (only when `predEntry.central` is set, using `new Date()` — `// gsd:allow-ui-clock`) and return `{ type: RESULT_TYPE[entry], isMissed, ...predEntry }`. Step 6: if nothing in `reachable` resolved, fall back to a private (non-exported) `LEGACY_FALLBACK_PRIORITY` table — exactly the OLD priority arrays this replaces (`bedtime:['wake','napStart','napEnd','bedtime']`, `wake:['napStart','bedtime','napEnd','wake']`, `napStart:['napEnd','bedtime','wake','napStart']`, `napEnd:['bedtime','wake','napStart','napEnd']`, default:`['wake','bedtime','napStart','napEnd']`) — walk it with the plain type names directly against `predictions` (no field-mapping needed, these are all standard keys) and return the first truthy match with the same `isMissed`/spread shape. Step 7: return `null` if nothing resolves.

    4. `js/ui/today-screen.js`: split the import at line 44 into `import { forecast, napProbability } from '../lib/forecast.js';` and `import { selectNextEvent } from '../lib/forecast-utils.js';`. Fix the two nap-probability ternary sites (~167-171, ~285-289): remove the `prediction.napProbabilityScore.score === 0 ? '0% — nap window closed' : ...` conditional entirely; always render `` `${prediction.napProbabilityScore.score}% chance of nap today` `` (per Phase 20 D-04's amendment note — the window-closed special text case no longer applies since score never collapses to 0 for that reason). Add `'data-event-type': type` (or the hero prediction's `.type`) to the root element created in `renderNextEventCard` (the `card` div), `renderPredictionCard` (the `card` div), `renderTifNormalCard` (the `card` div), and `renderTifLowConfidenceCard` (the `card` div) — a plain hardcoded literal from the fixed `EVENT_TYPES`/`prediction.type` set, never derived from imported/user JSON. In `renderForecastSection`'s EVENT_TYPES loop (~530-546): compute `const napStartHiddenToday = (predictions.napStart?.napProbabilityScore != null) && (lastEventTypeIsWakeOrNone) && (predictions.napStart.napProbabilityScore.napWindowClosed === true || currentHourFromSettingsSnap >= (settingsSnap.eveningHour ?? 18));` — you will need to thread the same `lastEvent` determination `selectNextEvent` uses (either by calling a small local helper that scans `dayRecords` for the latest event, mirroring forecast-utils.js's Step 1 logic, or by reading it off the hero result: `nextEvt` is already computed by `selectNextEvent` a few lines above — the simplest correct approach is to add a one-line local re-scan for `lastEvent` here since `renderForecastSection` already receives `dayRecords`). `lastEventTypeIsWakeOrNone` is true when `lastEvent === null || lastEvent.type === 'wake'` (D-04's carve-out: once `napStart` is already logged for today — i.e. `lastEvent.type` is `'napStart'`/`'napEnd'`/`'bedtime'` — this special full-hide does NOT apply; the card just follows normal grid rendering). When `napStartHiddenToday` is true, `continue` past `napStart` in the loop (skip appending its card entirely) — this is the minimal, real, observable capability this tracer proves; the fuller "Later today" wrapper is Plan 21-02's job and will supersede this loop's structure while keeping this exact skip condition.

    5. Add `./js/lib/forecast-utils.js` to `sw.js`'s `PRECACHE_LIST` (Task 2 of this plan does this — do not duplicate here).
  </action>
  <verify>
    <automated>node --test tests/unit/forecast.test.js tests/unit/forecast-utils.test.js && npx playwright test tests/e2e/forecast.spec.js</automated>
    <fails_when>non-zero exit code from either command, or "0 passed" / any "fail" count greater than 0 in the node --test or Playwright summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `node --test tests/unit/forecast.test.js` passes with the 5 updated PRED-12 tests reflecting the decoupled napWindowClosed/score contract
    - `node --test tests/unit/forecast-utils.test.js` passes, covering all 5 `nextReachableEvent` paths plus the eveningHour boundary and the P90 boundary
    - `grep -c "^export function selectNextEvent" js/lib/forecast.js` returns 0 (function fully removed, anchored to avoid matching a stray comment reference)
    - `grep -c "^export function selectNextEvent" js/lib/forecast-utils.js` returns 1
    - The new E2E test in `tests/e2e/forecast.spec.js` passes, proving `[data-event-type="napStart"]` is absent from the DOM once the window closes while the other three event types remain present
  </acceptance_criteria>
  <done>napProbability() returns napWindowClosed alongside a never-collapsed score; forecast-utils.js exports nextReachableEvent + selectNextEvent implementing the 5-path model; today-screen.js's grid genuinely hides the napStart card end-to-end when the window closes, proven by a real E2E check.</done>
</task>

<task type="auto">
  <name>Task 2: Register forecast-utils.js in the service-worker precache</name>
  <files>sw.js, tests/unit/sw-precache.test.js</files>
  <read_first>
    sw.js:17-64 (PRECACHE_LIST — alphabetical ordering within the js/lib/ section)
    tests/unit/sw-precache.test.js:117-127 (existing per-file assertion pattern, e.g. the forecast-tif.js and metrics.js tests)
  </read_first>
  <action>
    In `sw.js`'s `PRECACHE_LIST`, add `'./js/lib/forecast-utils.js'` immediately after `'./js/lib/forecast-tif.js'` and before `'./js/lib/forecast.js'` (ASCII ordering: `-` (0x2D) sorts before `.` (0x2E), so both hyphenated names precede the bare `forecast.js`, and `-tif` sorts before `-utils` alphabetically). In `tests/unit/sw-precache.test.js`, add a new `test('contains forecast-utils.js (event-reachability utilities module)', ...)` block immediately after the existing `forecast-tif.js` test (~117-119), asserting `precacheList.includes('./js/lib/forecast-utils.js')`.
  </action>
  <verify>
    <automated>node --test tests/unit/sw-precache.test.js</automated>
    <fails_when>non-zero exit code, or the new forecast-utils.js assertion is missing/failing, or any existing sw-precache test regresses (e.g. the "has at least 32 entries" count check)</fails_when>
  </verify>
  <acceptance_criteria>
    - `sw.js`'s PRECACHE_LIST contains `'./js/lib/forecast-utils.js'` positioned alphabetically between `forecast-tif.js` and `forecast.js`
    - `tests/unit/sw-precache.test.js` has a passing assertion naming forecast-utils.js explicitly
  </acceptance_criteria>
  <done>The app continues to work fully offline after this phase: forecast-utils.js is precached and pinned by a regression test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Imported JSON (`import-export.js`) / localStorage → `today-screen.js` rendering | Event `type`/`at` strings from a user-supplied JSON import flow into card rendering, including the new `data-event-type` attribute |
| `napProbability()`/`nextReachableEvent()` internal computed values → DOM text | Computed booleans/scores/labels feed into `textContent`-based card text; no external input reaches these functions directly (they consume already-validated in-memory day-record/settings data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Tampering | `js/ui/today-screen.js` new `data-event-type` attribute | low | accept | Attribute value is always one of the hardcoded literal strings (`wake`/`bedtime`/`napStart`/`napEnd`/`bedtimeAfterWake`) from the fixed `EVENT_TYPES`/mapping tables — never derived from imported/user-controlled JSON, so there is no injection surface to mitigate. |
| T-21-02 | Tampering | `js/lib/forecast.js` `napProbability()` / `js/lib/forecast-utils.js` `nextReachableEvent()` | low | accept | Both remain pure functions over already-schema-validated in-memory day-record/settings data (no new external input path introduced); `napWindowClosed` is a derived boolean, never persisted or round-tripped through import/export. |
| T-21-03 | Information Disclosure | `js/ui/today-screen.js` napStart-drop skip logic | low | mitigate | The skip condition only ever omits a card; it never introduces a new text sink. All rendering in this task continues to use `el()`/`textContent` exclusively (CLAUDE.md's dom.js guard) — no `innerHTML` is introduced anywhere in this task's changes. |
| T-21-SC | Tampering (Supply Chain) | npm/pip/cargo installs | n/a | accept | No package installs occur in this phase — Nightwatch is a vanilla-JS, no-dependency project (CLAUDE.md); the package-legitimacy gate does not apply. |
</threat_model>

<verification>
- `node --test tests/unit/forecast.test.js tests/unit/forecast-utils.test.js tests/unit/sw-precache.test.js` — all green
- `npx playwright test tests/e2e/forecast.spec.js` — all green, including the new napStart-hidden-when-closed test
- `npm run test:unit` — full unit+integration suite still green (no regression in unrelated modules that might reference `selectNextEvent` from `forecast.js`)
</verification>

<success_criteria>
- `napProbability()` never returns `score: 0` purely because the nap window closed; `napWindowClosed` is reported as an independent boolean
- `js/lib/forecast-utils.js` exists, exports `nextReachableEvent` and `selectNextEvent`, and `js/lib/forecast.js` no longer defines `selectNextEvent`
- The Today screen's `napStart` card is genuinely absent from the DOM (not just relabeled) once the nap window has closed, proven by a real E2E assertion
- `forecast-utils.js` is registered in the service worker's precache list and pinned by a unit test
</success_criteria>

<output>
Create `.planning/phases/21-prediction-normalization/21-01-SUMMARY.md` when done
</output>
