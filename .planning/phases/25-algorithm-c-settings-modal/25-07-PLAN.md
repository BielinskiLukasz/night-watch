---
phase: 25-algorithm-c-settings-modal
plan: 07
type: tdd
wave: 1
depends_on: ["25-02"]
files_modified:
  - js/lib/forecast-blend.js
  - tests/unit/forecast-blend.test.js
autonomous: true
requirements: [PRED-16, PRED-17]
gap_closure: true

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "napStartModel1 (the wake-anchored gap band), napEndModel1 (the fully-chained wake-anchored band), and napEndModel2 (the nap-start-anchored duration band) each wrap their min/max/median through wrapToDay() into [0,1440) before being combined via combineModels()/stabilityCheck() — matching the pattern already applied at wake's A2 and bedtime's Models 2/3 — so a late wake time plus a historic gap/duration that sums past midnight no longer produces a false 'no overlap' verdict or a central prediction that drifts by roughly half a day (WR-01 / 25-VERIFICATION.md gap 2 / 25-REVIEW.md WR-01, closed)."
    - "wrapToDay(m) is exported from js/lib/forecast-blend.js (previously private) so its [0,1440) wrapping contract is directly unit-testable at the exact 1440-minute boundary and one step either side (PRED-13 boundary/precision edge-probe rows, resolved for this module)."
    - statement: "napStart's Model 1 and Model 2 intervals are correctly detected as overlapping (not falsely disjoint) once both share the same wrapped [0,1440) reference frame — the reported napStart.central lies within the reported [napStart.min, napStart.max] band, treating that band as wrap-aware (min > max means the band crosses midnight) — for a fixture where wake is late enough that the unwrapped anchor+gap sum exceeds 1440 (PRED-17 adjacency edge-probe row, resolved for nap-start/nap-end)."
      verification: explicit
  artifacts:
    - path: "js/lib/forecast-blend.js"
      provides: "wrapToDay() exported; napStartModel1/napEndModel1/napEndModel2 all wrap their min/max/median before combineModels()"
    - path: "tests/unit/forecast-blend.test.js"
      provides: "New 'WR-01 midnight-wrap normalization' describe block: direct wrapToDay() boundary tests, plus late-wake napStart and napEnd integration tests that fail before the fix and pass after"
  key_links:
    - "napStartModel1/napEndModel1/napEndModel2's raw anchor+gap/duration sums (js/lib/forecast-blend.js) <-> wrapToDay() (same file, now exported) <-> combineModels()/stabilityCheck() (same file, UNMODIFIED) — this plan changes only the three call sites' inputs, never stabilityCheck's or combineModels' own comparison/shrinkage logic."
  prohibitions:
    - statement: "MUST NOT modify the already-correct wrapToDay() call sites for wake's A2 band (~lines 349-355) or bedtime's Models 2/3 (~lines 406-409, 428-431) — only the three identified nap-start/nap-end call sites (napStartModel1 ~242-246, napEndModel1 ~286-289, napEndModel2 ~301-304) are in scope for this fix."
      category: safety
      verification: judgment
    - statement: "MUST NOT change stabilityCheck()'s or combineModels()'s own comparison, intersection, union, or shrinkage algorithm — the fix is confined to wrapping the three model input objects BEFORE they reach combineModels(), not the combiner's logic, which Plan 25-01/25-02 already implemented and verified correctly for every other event."
      category: correctness
      verification: judgment
    - statement: "MUST NOT address IN-01 (the duplicate trimmedBand computation for gapBand/gapBand2 in 25-REVIEW.md) in this plan — it is an Info-level code-review finding, not one of 25-VERIFICATION.md's two gaps; reviewed and explicitly deferred, not silently dropped."
      category: scope
      verification: judgment
---

<objective>
Close 25-VERIFICATION.md gap 2 (= 25-REVIEW.md WR-01): apply `wrapToDay()` to `napStartModel1`'s, `napEndModel1`'s, and `napEndModel2`'s raw min/max/median sums in `js/lib/forecast-blend.js`, matching the normalization pattern already correctly applied at wake's A2 band and bedtime's Models 2/3.

Purpose: these three nap-anchored models compute `anchor + gap/duration` sums that can legitimately exceed 1440 minutes (a late wake plus a multi-hour historic gap or duration), but are combined via `combineModels()`/`stabilityCheck()` directly against sibling models built from already-wrapped `'HH:MM'` clock-time strings. Comparing an unwrapped (>1440-capable) interval against a wrapped ([0,1440)) interval produces false "no overlap" verdicts and a raw `central` average that can land roughly 12 hours away from the correct prediction — silently, since existing test fixtures only use moderate (06:00-ish) wake times that never trigger the edge case.

Output: `js/lib/forecast-blend.js` with the fix applied at all three call sites and `wrapToDay()` exported for direct testing, plus new RED->GREEN unit test coverage in `tests/unit/forecast-blend.test.js` that fails before the fix and passes after.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-algorithm-c-settings-modal/25-VERIFICATION.md
@.planning/phases/25-algorithm-c-settings-modal/25-REVIEW.md
@js/lib/forecast-blend.js
@tests/unit/forecast-blend.test.js
</context>

## Source Coverage Audit

This is a gap-closure plan (`/gsd-plan-phase 25 --gaps`). The source set is 25-VERIFICATION.md's gap 2 entry, not a fresh requirements/research pass — PRED-13..17/UI-12 were all already marked VERIFIED by Plans 25-01..25-05 and are not reopened here except where this fix's own correctness needs re-asserting.

| SOURCE | ID | Item | Plan | Status | Notes |
|--------|-----|------|------|--------|-------|
| GAP | gap 2 (= WR-01) | napStart/napEnd wake-anchored models skip day-wrap normalization before stabilityCheck | 25-07 | COVERED | The one task below fixes all three call sites and adds regression coverage. |
| REQ | PRED-16 | Interval stability check (overlap/union/shrinkage) for all events | 25-01 (stabilityCheck itself), 25-07 (this plan — corrects the INPUTS napStart/napEnd feed into it) | ALREADY VERIFIED, NOW HARDENED | `stabilityCheck()`'s own algorithm is untouched (see kept prohibition); this plan only fixes what napStart/napEnd feed into it. |
| REQ | PRED-17 | Algorithm C covers all 4 events (wake, bedtime, nap-start, nap-end) | 25-02 (already VERIFIED), 25-07 (this plan — closes the one edge-case correctness gap in nap-start/nap-end) | ALREADY VERIFIED, NOT REOPENED | The four events already produce real predictions per 25-VERIFICATION.md; this plan does not change that shape, only a midnight-crossing edge case's correctness. |
| Edge probe (specless-probe-fallback) | PRED-13 boundary | "What happens exactly at each min/max/threshold — and one step either side?" | 25-07 | RESOLVED — explicit | The 1440-minute midnight threshold IS the exact boundary this gap concerns. Resolved via direct `wrapToDay()` unit tests at 0, 1439, 1440, 1441, and 2880 (see Task 1 behavior). |
| Edge probe | PRED-13 precision | "Where can precision loss, overflow, or rounding/tie-breaking occur — and what is the exact contract?" | 25-07 | RESOLVED — explicit | `wrapToDay()`'s exact contract (`((m % 1440) + 1440) % 1440`, canonical range [0,1440), including negative-input defensiveness) is now directly unit-tested rather than only implicitly exercised through the full `blendForecast()` pipeline. |
| Edge probe | PRED-17 adjacency | "When two things are exactly equal or just touch, do they merge, collide, or separate?" | 25-07 | RESOLVED — explicit | The napStart/napEnd late-wake integration tests (Task 1) prove Model 1 and Model 2 are now correctly detected as overlapping once both share the wrapped [0,1440) frame — the exact WR-01 defect (false non-overlap). Folded into `must_haves.truths` above. |
| Edge probe | PRED-13/14/16/17 adjacency/empty/ordering/encoding (all other rows) | — | — | REVIEWED — OUT OF SCOPE | These rows concern wake (already VERIFIED, untouched by this plan), bedtime (already VERIFIED and already wraps correctly), or generic empty/ordering/encoding properties of `stabilityCheck`/`trimmedBand` that Plan 25-01's own `<flagged_assumptions>` block already addressed and this plan does not reopen. Recorded here rather than silently dropped. |
| Prohibition recall (specless-probe-fallback §B, scoped to "what could this gap silently become") | — | Adversarial pass over gap 2 | 25-07 | RESOLVED — 3 prohibitions kept | (1) Scope creep into the already-correct wake/bedtime wrap call sites. (2) Scope creep into `stabilityCheck()`/`combineModels()`'s own algorithm. (3) Scope creep into IN-01 (an unrelated Info-level duplicate-computation finding). All three captured in `must_haves.prohibitions` above. |

<tasks>

<task type="tdd">
  <name>Task 1: Wrap napStart/napEnd anchor sums into [0,1440) before stabilityCheck (WR-01)</name>
  <files>js/lib/forecast-blend.js, tests/unit/forecast-blend.test.js</files>
  <read_first>
    js/lib/forecast-blend.js (lines 138-148: the existing private `wrapToDay(m)` helper and its header comment, currently scoped as "used by Plan 25-02's wake-/nap-end-anchored bedtime duration bands"; lines 239-248: `napStartModel1` — builds `{min, max, median}` from `wakeAnchorMin + gapBand.{min,max,median}` with NO wrap; lines 279-291: `napEndModel1` — builds from `wakeAnchorMin + gapBand2.{...} + durBand2.{...}` with NO wrap; lines 293-305: `napEndModel2` — builds from `napStartAnchorMin + durBand2.{...}` with NO wrap; lines 344-356: wake's A2 band — the CORRECT reference pattern, `min: wrapToDay(anchor + durBand.min)` etc., with its explanatory comment "Wrap into [0, 1440) so A2 shares the same numeric reference frame as A1 before stabilityCheck's min/max comparisons and central averaging"; lines 400-411 and 421-433: bedtime's Models 2/3 — the same correct pattern already applied twice more. This task's fix makes napStartModel1/napEndModel1/napEndModel2 follow the IDENTICAL pattern, word-for-word, as these three already-correct sites.)
    tests/unit/forecast-blend.test.js (lines 24-33: `makeDay`/`fmt` fixture helpers this task's new fixtures reuse unchanged; lines 65-70: `buildNapStartFixture` — the existing wake=06:00-fixed nap-start fixture shape this task's new late-wake fixture mirrors structurally, just with a later wake and no `lastWakeNull` option; lines 185-216: the existing `describe('blendForecast — nap-start (D-07/D-08)', ...)` block — this task's new describe block is a sibling, added immediately after it; lines 218-245: the existing `describe('blendForecast — nap-end (D-09/D-10)', ...)` block — same sibling relationship for the napEnd fixture.)
    .planning/phases/25-algorithm-c-settings-modal/25-VERIFICATION.md (gap 2's frontmatter entry, lines 21-30 — the exact three line ranges and fix instructions this task implements verbatim).
    .planning/phases/25-algorithm-c-settings-modal/25-REVIEW.md (WR-01 section, lines 126-163 — the code reviewer's worked explanation of exactly how the false-non-overlap/12-hour-drift bug manifests and the exact fix diff, which this task's fix matches).
  </read_first>
  <behavior>
    Direct `wrapToDay()` boundary tests (export it first so these are callable; each is a one-line assertion):
    - `wrapToDay(0)` returns `0`.
    - `wrapToDay(1439)` returns `1439`.
    - `wrapToDay(1440)` returns `0` — the exact midnight-rollover boundary.
    - `wrapToDay(1441)` returns `1` — one step past the boundary.
    - `wrapToDay(2880)` returns `0` — two full days.
    - `wrapToDay(-5)` returns `1435` — defensive negative-input wrap (not exercised by any current call site, but documents the existing `((m % 1440) + 1440) % 1440` contract precisely now that it is a public export).

    napStart late-wake integration test — fixture `buildLateWakeNapStartFixture(n = 30)`: every day (including the last/"today" day) has `wake: '23:00'` (1380 raw minutes) and `napStart: fmt(timeToMinutes('23:00') + 60 + (i % 12) * 5)` (raw sums 1440-1495, so `fmt()`'s built-in `% 24` hour wrap stores these as `'00:00'` through `'00:55'` — a realistic "napped just after midnight" data set); `bedtime: null`, `napEnd: null`, `rejected: false`. Call `blendForecast(dayRecords, BLEND_SETTINGS)`. Add a local helper `centralWithinBand(minStr, maxStr, centralStr)` that converts all three to minutes via `timeToMinutes` and returns `min <= max ? (central >= min && central <= max) : (central >= min || central <= max)` (the second branch handles a band that itself crosses midnight, e.g. min='23:30', max='00:30'). Assert `centralWithinBand(result.napStart.min, result.napStart.max, result.napStart.central)` is `true`. Before the fix, `napStartModel1`'s unwrapped raw min/max (~1445-1485) is compared against `napStartModel2`'s wrapped raw min/max (~5-50) inside `stabilityCheck`, which finds no overlap and unions them; the raw `central` (average of an ~1465 and a ~27 median, never wrapped) becomes ~746 minutes -> displays as roughly `'12:25'` after `minutesToTime`'s trailing `% 1440` — nowhere near the reported `[min,max]` band, so this assertion FAILS pre-fix. After the fix, both models share the wrapped [0,1440) frame, `stabilityCheck` correctly finds overlap, and `central` lands inside the reported band.

    napEnd late-wake integration test — fixture `buildLateWakeNapEndFixture(n = 30)`: every day has `wake: '22:00'` (1320 raw minutes); `napStart: fmt(timeToMinutes('22:00') + 90 + (i % 12) * 5)` (raw 1410-1465, so roughly half the 12-value jitter cycle wraps past midnight and half does not — a realistic mixed data set); `napEnd: fmt(timeToMinutes(<that day's napStart string>) + 90 + (i % 4) * 5)` (duration 90-105, always a positive elapsed time — do not compute this from raw pre-wrap minutes, use the already-`fmt()`-wrapped napStart string exactly as `buildNapDurationSeries` itself would read it); `bedtime: null`, `rejected: false`. After building the array, override the LAST day's `napStart` to the literal string `'00:15'` (today's actual logged nap-start, small and already in [0,1440) — this is `napStartAnchorHHMM`'s D-10 "actual" precedence input for `napEndModel2`) while leaving that day's `wake: '22:00'` unchanged (still feeds `napEndModel1`'s `wakeAnchorMin`). Call `blendForecast(dayRecords, BLEND_SETTINGS)`. Assert `centralWithinBand(result.napEnd.min, result.napEnd.max, result.napEnd.central)` is `true`. Before the fix: `napEndModel1` (chained: `1320 + gap(~90-145) + duration(~90-115)`, raw ~1500-1580, unwrapped) and `napEndModel2` (`15 + duration(~90-115)`, raw ~105-130, already small) do not overlap as raw numbers; the union's `central` (average of an ~1540 and a ~117 median) lands around 828 minutes, which after `minutesToTime`'s rounding and `% 1440` displays outside the reported `[min,max]` band — this assertion FAILS pre-fix. After the fix, `napEndModel1`'s wrapped min/max (~60-140) correctly overlaps `napEndModel2`'s (~105-130), and central lands inside the band.
  </behavior>
  <action>
    RED — add all six behavior cases above to `tests/unit/forecast-blend.test.js` in a new `describe('blendForecast — WR-01 midnight-wrap normalization (gap-closure Plan 25-07)', ...)` block, placed immediately after the existing `describe('blendForecast — nap-end (D-09/D-10)', ...)` block. Add the `centralWithinBand` helper near the file's other local helpers (`makeDay`/`fmt`). Import `wrapToDay` alongside the file's existing `trimmedBand, stabilityCheck, blendForecast` import from `../../js/lib/forecast-blend.js` (this import will be `undefined` until the GREEN step exports it, so the direct boundary tests fail for that reason too). Run `node --test tests/unit/forecast-blend.test.js` and confirm every new test in this describe block fails. Commit `test(25-07): add failing tests for napStart/napEnd midnight-wrap normalization`.

    GREEN — in `js/lib/forecast-blend.js`: change `function wrapToDay(m) {` to `export function wrapToDay(m) {` and update its header comment (lines 138-142) to drop the "private" framing, noting it is now also used by `napStartModel1`/`napEndModel1`/`napEndModel2` (this plan) and is directly unit-tested at its [0,1440) boundary. Change `napStartModel1`'s assignment (lines 242-246) so `min`/`max`/`median` are each wrapped: `min: wrapToDay(wakeAnchorMin + gapBand.min)`, `max: wrapToDay(wakeAnchorMin + gapBand.max)`, `median: wrapToDay(wakeAnchorMin + gapBand.median)`. Change `napEndModel1`'s assignment (lines 286-289) the same way: `min: wrapToDay(wakeAnchorMin + gapBand2.min + durBand2.min)`, `max: wrapToDay(wakeAnchorMin + gapBand2.max + durBand2.max)`, `median: wrapToDay(wakeAnchorMin + gapBand2.median + durBand2.median)`. Change `napEndModel2`'s assignment (lines 301-304) the same way: `min: wrapToDay(napStartAnchorMin + durBand2.min)`, `max: wrapToDay(napStartAnchorMin + durBand2.max)`, `median: wrapToDay(napStartAnchorMin + durBand2.median)`. Do not touch wake's A2 block or bedtime's Model 2/Model 3 blocks — they already call `wrapToDay()` correctly. Do not touch `stabilityCheck()` or `combineModels()`. Run `node --test tests/unit/forecast-blend.test.js` until every test (new and pre-existing) is green. Commit `feat(25-07): wrap napStart/napEnd anchor sums into [0,1440) before combining`.

    No REFACTOR step is needed — the fix is three mechanical additions of an already-proven wrap call, matching an established in-file pattern exactly. Do not perform the IN-01 `gapBand`/`gapBand2` de-duplication cleanup from 25-REVIEW.md while in this file — it is a separate, non-blocking Info-level finding, out of scope for this gap-closure plan (see kept prohibition).
  </action>
  <verify>
    <automated>node --test tests/unit/forecast-blend.test.js</automated>
    <fails_when>non-zero exit, or the summary line reports any failing test — including any pre-existing test in the file, which must still pass unchanged since every existing fixture uses wake times well under 1440 where wrapToDay(x) === x is a no-op</fails_when>
  </verify>
  <acceptance_criteria>
    - `js/lib/forecast-blend.js` exports `wrapToDay` (grep confirms `export function wrapToDay` appears exactly once).
    - `napStartModel1`, `napEndModel1`, and `napEndModel2`'s min/max/median assignments each call `wrapToDay(...)` (grep confirms three new `wrapToDay(` call sites beyond the two pre-existing ones at wake's A2 and bedtime's Model 2, for a total of at least 3 new occurrences).
    - `git diff js/lib/forecast-blend.js` shows zero changed lines inside `stabilityCheck`, `combineModels`, wake's A2 block, or bedtime's Model 2/Model 3 blocks.
    - `node --test tests/unit/forecast-blend.test.js` exits 0 with zero failing tests, including the new "WR-01 midnight-wrap normalization" describe block.
    - `git log` shows a `test(25-07): ...` commit preceding a `feat(25-07): ...` commit for this task.
  </acceptance_criteria>
  <reversibility rating="reversible">Three mechanical additions of an already-proven, already-tested wrap call at three call sites, plus exporting a previously-private pure function whose own behavior is unchanged; reverting is a single-file diff and affects zero existing fixtures (all use wake times where the wrap is a no-op).</reversibility>
  <done>napStart and napEnd predictions built from a late wake time plus a historic gap/duration crossing midnight now correctly detect model overlap and produce a central prediction that lies within the reported band, matching the already-correct behavior wake and bedtime have had since Plans 25-01/25-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | `blendForecast()` continues to consume only the app's own already-validated local `dayRecords`/`settings` state (offline-first, no network I/O, no new user-input parsing, no storage-schema change). This plan corrects internal arithmetic; it crosses no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-14 | Tampering | `wrapToDay()` (now exported from js/lib/forecast-blend.js) | low | accept | Exporting a pure, already-correct arithmetic function (`((m % 1440) + 1440) % 1440`) adds no mutable state and no new input-parsing surface; it operates only on already-computed internal minute integers, never on raw user input directly. `Object.freeze` does not apply to functions and is not a relevant mitigation here. |
| T-25-15 | Denial of Service | napStart/napEnd model combination | low | accept | The fix adds three `O(1)` arithmetic calls; no new loop, recursion, or unbounded input path is introduced. |

</threat_model>

## Artifacts this phase produces (Plan 25-07)

- Modified: `js/lib/forecast-blend.js` — `wrapToDay(m)` changed from private to `export function wrapToDay(m)`; `napStartModel1`, `napEndModel1`, `napEndModel2` all wrap their min/max/median via `wrapToDay()` before `combineModels()`. No other function's signature or logic changes.
- Modified: `tests/unit/forecast-blend.test.js` — new `describe('blendForecast — WR-01 midnight-wrap normalization (gap-closure Plan 25-07)', ...)` block: 6 direct `wrapToDay()` boundary assertions + 2 late-wake integration tests (napStart, napEnd) + the new `centralWithinBand` helper.

<verification>
Run `node --test tests/unit/forecast-blend.test.js` — passes, including the new WR-01 describe block. For full-suite confidence, also run `npm run test:unit` (expect the same 959 baseline plus this plan's new tests, with zero regressions since every existing fixture uses wake times where `wrapToDay(x) === x`).
</verification>

<success_criteria>
- `napStartModel1`, `napEndModel1`, and `napEndModel2` all wrap their raw anchor+gap/duration sums into [0,1440) before `combineModels()`/`stabilityCheck()` runs, matching wake's A2 and bedtime's Models 2/3.
- A late wake time plus a historic gap/duration crossing midnight no longer produces a false "no overlap" verdict or a central prediction outside the reported band.
- `wrapToDay()` is directly unit-tested at its exact [0,1440) boundary.
- 25-VERIFICATION.md's gap 2 (WR-01) is closed, with zero changes to `stabilityCheck()`, `combineModels()`, or the already-correct wake/bedtime wrap call sites.
</success_criteria>

<output>
Create `.planning/phases/25-algorithm-c-settings-modal/25-07-SUMMARY.md` when done.
</output>
