---
phase: 25-algorithm-c-settings-modal
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - js/lib/forecast-blend.js
  - tests/unit/forecast-blend.test.js
  - sw.js
  - tests/unit/sw-precache.test.js
autonomous: true
requirements: [PRED-13, PRED-14, PRED-16]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "blendForecast(dayRecords, settings, activityLog, isNoNapDay) returns { isColdStart, wake, bedtime, napStart, napEnd } — the same top-level keys as forecast()/tifForecast() — so today-screen.js's dispatch ternary can swap between all three algorithms without changing any downstream rendering code (PRED-13)."
    - "Below settings.minDays valid (non-rejected) days, blendForecast returns { isColdStart: true, wake: null, bedtime: null, napStart: null, napEnd: null } via detectColdStart() imported from forecast.js — identical cold-start contract to tifForecast()."
    - "Wake central = average of A1's trimmed median (historic wake-up band) and A2's trimmed median (last-bedtime-anchored sleep-duration band), per D-01."
    - "When A1 and A2's [min,max] intervals overlap — INCLUSIVE of exact touching, matching forecast-tif.js's computeIntersection() convention where only finalStart > finalEnd counts as non-overlap — the reported wake band is their intersection; if the raw central lies outside the intersection, the reported central is pulled toward the intersection's center by blendShrinkage (D-02): final = (1 - shrinkage) * central + shrinkage * center(intersection)."
    - "When A1 and A2 do not overlap, the reported wake band is the union envelope [min(A1.min, A2.min), max(A1.max, A2.max)] and central is left unchanged (D-02) — no optional shrink-toward-closer-interval is applied (CONTEXT.md D-02 resolves NEW_ALG.md's parenthetical 'optionally apply shrinkage' by omission: only the overlap case shrinks)."
    - "When A2 is unavailable (no bedtime ever logged in the window), wake falls back to A1 alone: central=A1.median, min=A1.min, max=A1.max, no stability check applied — mirrors forecast.js's own D-11 'falls back to the hour-band only' contract for the identical no-bedtime edge case."
    - "trimmedBand(sortedValues, trimPct, manualExcludedCount) discards floor(N*trimPct/100) - manualExcludedCount values, split floor(budget/2) off the low end and ceil(budget/2) off the high end (byte-identical budget math to forecast-tif.js's trimmedMinMax), and returns {min, median, max} of the remainder, or null when the trimmed remainder or the input is empty."
    - "stabilityCheck(intervals, central, shrinkage) generalizes unchanged from 2 intervals (wake, this plan) to 3 intervals (bedtime, Plan 25-02) because 1-D interval pairwise-overlap and group-overlap are mathematically equivalent (Helly's theorem in one dimension) — max(...mins) <= min(...maxs) is both the pairwise-overlap test AND the N-way common-intersection test, so no separate 3-way branching is needed."
    - "'./js/lib/forecast-blend.js' is present in sw.js's PRECACHE_LIST (alphabetically between './js/lib/forecast-tif.js' and './js/lib/forecast-utils.js') and asserted present by a new test in tests/unit/sw-precache.test.js."
  artifacts:
    - path: "js/lib/forecast-blend.js"
      provides: "blendForecast() entry point, trimmedBand(), stabilityCheck(), wrapToDay(), BLEND_CONFIG — wake (D-01/D-02) fully implemented; bedtime/napStart/napEnd stubbed to {central:null,min:null,max:null} pending Plan 25-02"
    - path: "tests/unit/forecast-blend.test.js"
      provides: "RED->GREEN coverage for trimmedBand, stabilityCheck, and blendForecast's cold-start gate + wake prediction"
    - path: "sw.js"
      provides: "PRECACHE_LIST entry for forecast-blend.js"
  key_links:
    - from: "js/ui/today-screen.js (Plan 25-04, later wave)"
      to: "js/lib/forecast-blend.js (blendForecast)"
      via: "import + dispatch ternary"
      pattern: "import.*blendForecast.*from.*forecast-blend"
---

<objective>
Create `js/lib/forecast-blend.js` — Algorithm C's entry point — with the shared trim-then-percentile
and interval-stability-check math (D-12, D-13, D-02) and the wake dual-model blend fully implemented
(D-01, D-02, PRED-14). Register the new module in the service worker's precache list.

Purpose: this is the phase's tracer — the thinnest slice that proves `blendForecast()` returns the
exact top-level shape `today-screen.js` needs to swap algorithms transparently (PRED-13), with ONE
event (wake) carrying real dual-model math end-to-end. Plan 25-02 expands the SAME file outward to
bedtime/napStart/napEnd using the identical `trimmedBand`/`stabilityCheck` helpers this plan builds —
nothing in Plan 25-02 introduces a new architectural shape, only more cases of the pattern proven here.

Output: `js/lib/forecast-blend.js` (blendForecast, trimmedBand, stabilityCheck, wrapToDay, BLEND_CONFIG),
`tests/unit/forecast-blend.test.js`, and an updated `sw.js` + `tests/unit/sw-precache.test.js`.
</objective>

<flagged_assumptions>
Two edge-probe categories from the phase's specless-probe-fallback report do not map to a real
behavior in this module and are recorded here rather than forced into a truth:
- PRED-13/14/16 "ordering" probes ("is output order specified/stable when elements compare equal?")
  — do not apply. `trimmedBand`/`stabilityCheck` operate on numeric time values reduced to
  min/median/max; there is no output collection whose element order the caller observes.
- PRED-14 "encoding" probe ("whose definition of length/equality applies — bytes, code points...?")
  — does not apply. All inputs are already-parsed integer minutes-since-midnight; there is no
  string/text-length or byte/codepoint dimension anywhere in this module.

Prohibition-probe recall pass (adversarial "what could this silently become that the author would
NOT want?" question, applied to PRED-13/14/15/16/17/UI-12): this phase is a pure client-side
percentile-math algorithm operating only on the user's own already-logged, already-validated local
sleep events — no new user-facing copy/framing, no protected-attribute-adjacent data, no network
call, no PII surface beyond what Classic/TIF already render. The recall pass surfaced only routine
engineering candidates ("must not mutate dayRecords", "must not throw on empty history" — both
already owned by the edge-probe / cold-start gate above), which Stage 2 correctly drops. Result:
zero genuine values/safety/ethics prohibitions for this phase — the "clean-utility" outcome, not a
skipped step. `must_haves.prohibitions` is empty across all five of this phase's plans for this reason.
</flagged_assumptions>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-algorithm-c-settings-modal/25-CONTEXT.md
@.planning/phases/25-algorithm-c-settings-modal/25-PATTERNS.md
@NEW_ALG.md
@js/lib/forecast.js
@js/lib/forecast-tif.js
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: forecast-blend.js — shared trim/stability helpers + wake dual-model blend (D-01, D-02, D-12, D-13)</name>
  <files>js/lib/forecast-blend.js, tests/unit/forecast-blend.test.js</files>
  <read_first>
    js/lib/forecast-tif.js (lines 1-28: header comment + frozen TIF_CONFIG pattern this file mirrors; lines 41-46: local `extractTime` — forecast-blend.js needs its OWN copy, following the same "own frozen config, own extractTime helper" duplication convention rather than importing from forecast-tif.js — CONTEXT.md's canonical_refs describes forecast-tif.js as a STRUCTURAL model, and its own header states it "imports only what it needs from forecast.js"; lines 68-87: `trimmedMinMax` — the exact budget-split algorithm (`budget = max(0, floor(N*trimPct/100) - manualExcludedCount)`, `low = floor(budget/2)`, `high = ceil(budget/2)`, median = middle element or average of the two middle elements) that this task's local `trimmedBand` reproduces; lines 100-114: `computeIntersection` — the exact "finalStart > finalEnd means no overlap, else overlap" convention this task's `stabilityCheck` reuses for the touching-counts-as-overlap resolution).
    js/lib/forecast.js (lines 56-65: FORECAST_CONFIG / Object.freeze pattern; lines 339-355: `detectColdStart(dayRecords, minDays)` — the exact cold-start gate to reuse via import; lines 714-719: the backward-scan-for-most-recent-bedtime loop `for (let i = window.length-1; i >= 0; i--) { const b = extractTime(window[i].bedtime); if (b) { lastBedtimeHHMM = b; break; } }` — this task's A2 anchor resolution reuses this EXACT loop shape, not `resolveAnchor()`, matching Classic's own D-10 wake-band precedent rather than TIF's prediction-fallback machinery).
    js/lib/metrics.js (lines 43-49: `sleepDuration(day)` — reads `day.wake`/`day.bedtime` from the SAME bucket and returns the wake-minus-bedtime-with-1440-wraparound value; TIF's own wake "sleep-length band" already uses this exact same-record approximation via `import { sleepDuration } from './metrics.js'` — Algorithm C's A2 model reuses the identical import and array-building idiom, not a hand-rolled duration loop).
    .planning/phases/25-algorithm-c-settings-modal/25-CONTEXT.md (D-01, D-02, D-07, D-11, D-12, D-13 — the decisions this task implements; note D-07 confirms `buildNapGapSeries` is available on forecast.js for Plan 25-02, not needed by this task).
    NEW_ALG.md (lines 4-39 — the original algorithm prose D-01/D-02 formalize; read once for the "why", not as an implementation source — CONTEXT.md's D-01/D-02 are the authoritative, already-resolved spec).
  </read_first>
  <behavior>
    - `trimmedBand([400,410,420,430,440], 10, 0)` returns `{min:400, max:440, median:420}` (budget=floor(5*10/100)=0, no trim) — mirrors forecast-tif.test.js's `trimmedMinMax` case 1 byte-for-byte since the budget math is identical.
    - `trimmedBand([400,410,420,430,440,450,460,470,480,490], 10, 0)` returns `{min:400, max:480, median:440}` (budget=1, low=0, high=1 — top value dropped) — mirrors forecast-tif.test.js case 2.
    - `trimmedBand([], 10, 0)` returns `null`; `trimmedBand([400,410,420,430,440], 100, 0)` returns `null` (budget consumes the whole array).
    - `stabilityCheck([{min:390,max:420},{min:400,max:430}], 405, 0.3)` (overlapping: intersection=[400,420], 405 is inside) returns `{min:400, max:420, central:405}` unchanged — central-inside-intersection case.
    - `stabilityCheck([{min:390,max:400},{min:410,max:430}], 395, 0.3)` (overlapping via exact touch is NOT this case — these two do NOT touch/overlap: 400 < 410) returns the union envelope `{min:390, max:430, central:395}` unchanged — no-overlap case, central untouched.
    - `stabilityCheck([{min:390,max:400},{min:400,max:430}], 380, 0.3)` (touching at exactly 400 — inclusive overlap, intersection=[400,400], a single point) with central=380 OUTSIDE [400,400]: `center = 400`; `final = 0.7*380 + 0.3*400 = 386`. Assert `min:400, max:400, central:386`.
    - `stabilityCheck([{min:390,max:420},{min:400,max:440}], 450, 0.5)` (overlapping: intersection=[400,420], central=450 is outside, above): `center=410`; `final = 0.5*450 + 0.5*410 = 430`. Assert `min:400, max:420, central:430`.
    - `detectColdStart`-gated: `blendForecast(dayRecords, {minDays:7, blendWindowDays:90, blendTrimPct:25, blendShrinkage:0.3})` with fewer than 7 non-rejected day records returns `{isColdStart:true, wake:null, bedtime:null, napStart:null, napEnd:null}` — no other fields present (mirrors tifForecast's exact cold-start return shape, not forecast()'s `validDayCount`/`minDaysRemaining` shape).
    - Wake, A1+A2 overlap case: build >= 30 day records (wake 06:00-07:00 spread, bedtime ~20:30 fixed, sleep durations therefore clustering ~9.5h) with `blendWindowDays:90, blendTrimPct:25, blendShrinkage:0.3, minDays:7`. Assert `result.wake.central`, `.min`, `.max` are non-null 'HH:MM' strings, `result.wake.min <= result.wake.central <= result.wake.max` (string comparison is valid here since all three share the same 'HH:MM' zero-padded format and do not cross midnight in this fixture).
    - Wake, A2-unavailable fallback: build a fixture where NO day record in the window has a `.bedtime` field (nap/wake-only fixture). Assert `result.wake.central/min/max` equal A1's own trimmed band converted via `minutesToTime` (i.e., no stability check ran) — compute the expected A1 band from the same raw wake times the test seeds, do not hand-derive a second value.
    - Bedtime/napStart/napEnd stub: on the same non-cold-start fixture, assert `result.bedtime`, `result.napStart`, `result.napEnd` each deep-equal `{central:null, min:null, max:null}` — the explicit Wave-2 placeholder, not `undefined` or a missing key (PRED-13's shape contract must hold even for not-yet-implemented events).
  </behavior>
  <action>
    Create `js/lib/forecast-blend.js` with a header comment mirroring forecast-tif.js's framing: state this is Algorithm C (dual-model blend per NEW_ALG.md / D-01..D-13), a pure function module (no DOM, no clock, no storage), and that it returns the identical top-level prediction shape as `forecast()`/`tifForecast()` (PRED-13). Import `timeToMinutes`, `minutesToTime`, `detectColdStart`, `buildNapGapSeries`, `buildNapDurationSeries` from `./forecast.js` (the last two are unused by this task but import them now so Plan 25-02 does not need to touch the import line) and `sleepDuration`, `dayLength`, `activityAfterNap` from `./metrics.js` (dayLength/activityAfterNap unused until Plan 25-02's bedtime task — same reasoning). Define `const BLEND_CONFIG = Object.freeze({ ROUND_MINUTES: 5 });` (blendWindowDays/blendTrimPct/blendShrinkage come from the `settings` argument, not this config, since they are user-configurable per D-11/D-12/D-13). Add a local `function extractTime(slot)` identical to forecast-tif.js's own (handles null / bare 'HH:MM' string / `{at}` event object).

    Implement and export `trimmedBand(sortedValues, trimPct, manualExcludedCount)`: byte-identical budget/split/median math to forecast-tif.js's `trimmedMinMax` (do not import it — this is forecast-blend.js's own copy per the sibling-module duplication convention already established between forecast.js/forecast-tif.js/metrics.js's `extractTime`). Returns `{min, max, median}` or `null`.

    Implement and export `stabilityCheck(intervals, central, shrinkage)` where `intervals` is an array of `{min, max}` (2 or more elements): compute `interStart = Math.max(...intervals.map(iv => iv.min))` and `interEnd = Math.min(...intervals.map(iv => iv.max))`. If `interStart <= interEnd` (inclusive — touching counts as overlap, matching forecast-tif.js's `computeIntersection` convention where only `finalStart > finalEnd` is non-overlap): compute `center = (interStart + interEnd) / 2`; if `central` is within `[interStart, interEnd]` inclusive, `finalCentral = central`, else `finalCentral = (1 - shrinkage) * central + shrinkage * center`; return `{min: interStart, max: interEnd, central: finalCentral}`. Else (no overlap): return `{min: Math.min(...intervals.map(iv=>iv.min)), max: Math.max(...intervals.map(iv=>iv.max)), central}` (central unchanged, per D-02's explicit resolution — no optional shrink-toward-closer-interval). Document in a comment above this function the Helly's-theorem justification for why this same 2-interval test also correctly resolves Plan 25-02's 3-interval bedtime case (D-06) with zero code changes: 1-D intervals pairwise-overlapping implies they share a common point, so `max(mins) <= min(maxs)` is simultaneously the pairwise test and the group test.

    Add a private `wrapToDay(m)` helper (`((m % 1440) + 1440) % 1440`) for later use by Plan 25-02's anchored duration bands — not exercised by this task's wake logic (wake's A2 anchor + duration never needs wrapping since bedtime-anchored sleep-duration projections land within a normal 0-1440 range once `minutesToTime` is applied, matching forecast.js's own `computeDurationBand` which normalizes via `% 1440` inline rather than a named helper — use the named helper here for reuse by Plan 25-02's bedtime/napEnd anchored bands, which DO need it).

    Implement `export function blendForecast(dayRecords, settings, activityLog = {}, isNoNapDay = false)`. Step 1 — cold-start gate: call `detectColdStart(dayRecords, settings.minDays)`; if `isColdStart`, return `{isColdStart: true, wake: null, bedtime: null, napStart: null, napEnd: null}` (tifForecast's exact shape, not forecast()'s `validDayCount`/`minDaysRemaining` fields — Algorithm C follows the TIF precedent here since PRED-13 only requires shape parity on the non-cold-start branch and CONTEXT.md's canonical_refs name forecast-tif.js, not forecast.js, as the structural model). Step 2 — slice `const window = dayRecords.slice(-(settings.blendWindowDays ?? 90));` then `const acceptedWindow = window.filter(d => !d.rejected); const rejectedInWindow = window.length - acceptedWindow.length;` (mirrors forecast-tif.js's `acceptedWindow`/`rejectedInWindow` exactly — Algorithm C respects the app-wide rejected-day convention even though NEW_ALG.md's original prose does not mention it). Step 3 — compute A1: `const a1Times = acceptedWindow.map(d => extractTime(d.wake)).filter(t => t != null).map(timeToMinutes).sort((a,b)=>a-b); const a1 = trimmedBand(a1Times, settings.blendTrimPct ?? 25, rejectedInWindow);`. Step 4 — resolve `lastBedtimeHHMM` via the backward-scan loop over `window` (not `acceptedWindow` — matches forecast.js's own scan, which does not exclude rejected days for this particular anchor lookup). Step 5 — compute A2 only when `lastBedtimeHHMM` is non-null: `const sleepDurations = acceptedWindow.map(sleepDuration).filter(v => v != null).sort((a,b)=>a-b); const durBand = trimmedBand(sleepDurations, settings.blendTrimPct ?? 25, rejectedInWindow); const a2 = durBand ? { min: timeToMinutes(lastBedtimeHHMM)+durBand.min, max: timeToMinutes(lastBedtimeHHMM)+durBand.max, median: timeToMinutes(lastBedtimeHHMM)+durBand.median } : null;`. Step 6 — combine: if `a1` is null, wake is `{central:null,min:null,max:null}` (no wake history at all — cold-start gate does not guarantee wake-specific data, matching Classic's own `wakeHourResult === null` guard). Else if `a2` is null, wake = `{central: minutesToTime(a1.median), min: minutesToTime(a1.min), max: minutesToTime(a1.max)}` (A1-only fallback, no stability check). Else: `const rawCentral = (a1.median + a2.median) / 2; const sc = stabilityCheck([a1, a2], rawCentral, settings.blendShrinkage ?? 0.3); wake = { central: minutesToTime(sc.central), min: minutesToTime(sc.min), max: minutesToTime(sc.max) };`. Step 7 — return `{isColdStart: false, wake, bedtime: {central:null,min:null,max:null}, napStart: {central:null,min:null,max:null}, napEnd: {central:null,min:null,max:null}}` with a one-line comment `// Plan 25-02 replaces these three stubs with real D-03..D-10 blends`.

    Follow RED->GREEN: write every case in `<behavior>` in `tests/unit/forecast-blend.test.js` first, run `node --test tests/unit/forecast-blend.test.js` and confirm every new test fails (forecast-blend.js does not exist yet), commit `test(25-01): add failing tests for trim/stability helpers and wake blend`; then implement, re-run until green, commit `feat(25-01): implement forecast-blend.js wake dual-model blend`.
  </action>
  <verify>
    <automated>node --test tests/unit/forecast-blend.test.js</automated>
    <fails_when>non-zero exit, or the summary line reports a fail count greater than 0 for any trimmedBand/stabilityCheck/blendForecast test</fails_when>
  </verify>
  <acceptance_criteria>
    - `js/lib/forecast-blend.js` exports `blendForecast`, `trimmedBand`, `stabilityCheck`.
    - `blendForecast` on a cold-start fixture returns exactly `{isColdStart:true, wake:null, bedtime:null, napStart:null, napEnd:null}` (grep the file for this literal return shape in the cold-start branch).
    - `blendForecast` on a non-cold-start fixture returns `bedtime`, `napStart`, `napEnd` each as `{central:null, min:null, max:null}` (not stubbed via `undefined`).
    - `node --test tests/unit/forecast-blend.test.js` exits 0 with zero failing tests.
    - git log shows a `test(25-01): ...` commit preceding a `feat(25-01): ...` commit for this task.
  </acceptance_criteria>
  <done>trimmedBand and stabilityCheck pass all behavior cases in isolation; blendForecast's cold-start gate and wake dual-model blend (overlap-with-shrink, overlap-no-shrink, no-overlap, A2-unavailable) all pass; bedtime/napStart/napEnd stubs are present and typed correctly for Plan 25-02 to replace.</done>
</task>

<task type="auto">
  <name>Task 2: Register forecast-blend.js in the service-worker precache list</name>
  <files>sw.js, tests/unit/sw-precache.test.js</files>
  <read_first>
    sw.js (lines 23-66: PRECACHE_LIST — the alphabetically-ordered "Pure-logic lib" section; `'./js/lib/forecast-tif.js',` at line 43 and `'./js/lib/forecast-utils.js',` at line 44 are the two entries this task's new line goes between).
    tests/unit/sw-precache.test.js (lines 117-119: the `forecast-tif.js` inclusion-test shape to mirror exactly for `forecast-blend.js`).
  </read_first>
  <action>
    In sw.js, insert `'./js/lib/forecast-blend.js',` into PRECACHE_LIST between `'./js/lib/forecast-tif.js',` and `'./js/lib/forecast-utils.js',`. Do not reorder any other entry.

    In tests/unit/sw-precache.test.js, add one new test inside the existing `describe('sw.js PRECACHE_LIST', ...)` block, mirroring the forecast-tif.js test at lines 117-119: `test('contains forecast-blend.js (Algorithm C module)', () => { assert.ok(precacheList.includes('./js/lib/forecast-blend.js'), 'forecast-blend.js missing from PRECACHE_LIST'); });`. The "at least 32 entries" assertion at line 113-115 needs no change (adding one entry only increases the count).
  </action>
  <verify>
    <automated>node --test tests/unit/sw-precache.test.js</automated>
    <fails_when>non-zero exit, or the new forecast-blend.js test reports fail in the summary</fails_when>
  </verify>
  <acceptance_criteria>
    - sw.js's PRECACHE_LIST contains exactly one occurrence of `'./js/lib/forecast-blend.js'`, positioned alphabetically between the forecast-tif.js and forecast-utils.js entries.
    - tests/unit/sw-precache.test.js's new test passes and no pre-existing test in the file regresses.
  </acceptance_criteria>
  <done>forecast-blend.js is precached for offline/PWA use and the precache-completeness test enforces it going forward.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | `blendForecast()` consumes only the app's own already-validated local `dayRecords`/`settings` state (offline-first, no network I/O, no new user-input parsing, no storage-schema change). No new trust boundary is crossed by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Tampering | `BLEND_CONFIG` (js/lib/forecast-blend.js) | low | accept | `Object.freeze()` prevents runtime mutation (same convention as `TIF_CONFIG`/`FORECAST_CONFIG`); no external input can reach this constant. |
| T-25-02 | Denial of Service | `blendForecast()` window slicing | low | accept | `dayRecords.slice(-blendWindowDays)` is bounded by `blendWindowDays`'s validated range (14-180, enforced in Plan 25-03's settings-validate.js RULES) — no unbounded loop is possible regardless of history length. |

</threat_model>

## Artifacts this phase produces (Plan 25-01)

- `blendForecast(dayRecords, settings, activityLog, isNoNapDay)` (js/lib/forecast-blend.js) — Algorithm C entry point; wake fully implemented (D-01/D-02), bedtime/napStart/napEnd stubbed
- `trimmedBand(sortedValues, trimPct, manualExcludedCount)` (js/lib/forecast-blend.js) — local trim-then-percentile helper (D-12)
- `stabilityCheck(intervals, central, shrinkage)` (js/lib/forecast-blend.js) — generalized 2-or-3-interval overlap/shrinkage/union helper (D-02, reused unchanged by Plan 25-02's D-06)
- `wrapToDay(m)` (private, js/lib/forecast-blend.js) — midnight-wrap normalizer for Plan 25-02's anchored duration bands
- `BLEND_CONFIG` (frozen const, js/lib/forecast-blend.js)
- `tests/unit/forecast-blend.test.js` (new test file)
- `'./js/lib/forecast-blend.js'` entry in sw.js PRECACHE_LIST + matching assertion in tests/unit/sw-precache.test.js

<verification>
Run `node --test tests/unit/forecast-blend.test.js tests/unit/sw-precache.test.js` — both pass. Run `npm run test:unit` to confirm no regression in any pre-existing test file.
</verification>

<success_criteria>
- `blendForecast()`'s cold-start gate and wake dual-model blend implement D-01/D-02 exactly, including the overlap-inclusive-touching and no-overlap-no-shrink resolutions.
- `trimmedBand`/`stabilityCheck` are generically reusable (verified by Plan 25-02 consuming them unchanged for 3-band bedtime and 2-band nap predictions).
- sw.js + tests/unit/sw-precache.test.js are updated and passing.
</success_criteria>

<output>
Create `.planning/phases/25-algorithm-c-settings-modal/25-01-SUMMARY.md` when done.
</output>
