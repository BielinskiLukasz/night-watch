---
phase: 25-algorithm-c-settings-modal
plan: 02
type: tdd
wave: 2
depends_on: ["25-01"]
files_modified:
  - js/lib/forecast-blend.js
  - tests/unit/forecast-blend.test.js
autonomous: true
requirements: [PRED-13, PRED-15, PRED-16, PRED-17]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Nap-start blends two models per D-08: Model 1 = wakeAnchor + trimmedBand(buildNapGapSeries(acceptedWindow)) (wake-anchored gap, only when today's wake is actually logged — no predicted-wake fallback, mirroring forecast-tif.js's own wakeAnchorForNap precedent, which is unresolved before nap-start is computed); Model 2 = trimmedBand of raw historic napStart times. Central = average of the two available models' medians; stabilityCheck applies when both are available."
    - "Nap-end blends two models per D-09/D-10: Model 1 = wakeAnchor + trimmedBand(napGaps) + trimmedBand(napDurations) (fully chained, wake-anchored only when today's wake is logged); Model 2 = napStartAnchor + trimmedBand(napDurations), where napStartAnchor is today's ACTUAL logged nap-start if present, else the nap-start prediction's own central value (D-10's explicit actual-else-predicted rule)."
    - "Bedtime blends three models per D-03..D-06: Model 1 = trimmedBand of raw historic bedtimes; Model 2 = todayWakeAnchor + trimmedBand(dayLength per historic day), where todayWakeAnchor is today's actual wake if logged else the wake prediction's own central (extending D-10's actual-else-predicted rule to this anchor, since D-03 does not specify a fallback and the project's established TIF/Classic precedent always resolves an anchor to a predicted value when the actual is not yet available); Model 3 = todayNapEndAnchor + trimmedBand(activityAfterNap per historic day) on nap days, OR — on no-nap days (isNoNapDay=true, D-05) — Model 3 is REPLACED by a raw historic bedtime band computed ONLY from days where napStart is null (never a 2-band collapse when that substitute band is itself available)."
    - "stabilityCheck (from Plan 25-01, unchanged) correctly resolves bedtime's 3-band group via the SAME max(mins)<=min(maxs) test used for wake's 2-band group — Helly's-theorem equivalence, no bedtime-specific branching added."
    - "Any single model that cannot be computed (no data, or an anchor unavailable) is excluded from BOTH the central-average and the stabilityCheck group; when ALL models for an event are unavailable, that event returns {central:null, min:null, max:null} rather than throwing — matching forecast.js/tifForecast's universal null-on-no-data contract, applied uniformly across all four Algorithm C events including the D-05 no-nap-day substitute-unavailable edge."
    - "blendForecast() on a rich fixture (>= blendWindowDays worth of wake+bedtime+napStart+napEnd events) now returns non-null, real {central,min,max} 'HH:MM' predictions for all four of wake/bedtime/napStart/napEnd — Plan 25-01's three stubs are fully replaced, completing PRED-13/PRED-17's full-coverage contract."
  artifacts:
    - path: "js/lib/forecast-blend.js"
      provides: "bedtime (D-03..D-06), napStart (D-07/D-08), napEnd (D-09/D-10) fully implemented — blendForecast() now covers all 4 events"
    - path: "tests/unit/forecast-blend.test.js"
      provides: "RED->GREEN coverage for bedtime's 3-band blend + no-nap-day substitution, napStart/napEnd's 2-band blends, and a full-coverage integration assertion"
  key_links:
    - from: "js/lib/forecast-blend.js (blendForecast bedtime Model 3)"
      to: "js/lib/forecast-blend.js (blendForecast napEnd)"
      via: "function-internal ordering: napStart, then napEnd, then wake (Plan 25-01), then bedtime — bedtime's Model 3 anchor reads napEnd's already-computed central prediction"
      pattern: "napEndPred.*bedtime"
    - from: "js/lib/forecast-blend.js (napStart/napEnd)"
      to: "js/lib/forecast.js (buildNapGapSeries, buildNapDurationSeries)"
      via: "import statement (already added in Plan 25-01, unused until this plan)"
      pattern: "import.*buildNapGapSeries.*buildNapDurationSeries.*from.*forecast"
---

<objective>
Complete `blendForecast()` in `js/lib/forecast-blend.js`: replace Plan 25-01's bedtime/napStart/napEnd
stubs with the real D-03..D-10 multi-band blends, reusing Plan 25-01's `trimmedBand`/`stabilityCheck`
helpers unchanged.

Purpose: this is the phase's pure-logic expansion — every event Algorithm C must cover (PRED-17) now
produces a real prediction, using the SAME architectural pattern (trim-then-percentile band, average-
of-medians central, group stability check) Plan 25-01 already proved on wake. No new helper shape is
introduced; this plan is strictly more CASES of an already-proven pattern.

Output: `js/lib/forecast-blend.js` with all four events implemented, and the completed
`tests/unit/forecast-blend.test.js` covering every model combination and fallback path.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-algorithm-c-settings-modal/25-CONTEXT.md
@js/lib/forecast-blend.js
@js/lib/forecast-tif.js
@js/lib/forecast.js
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Nap-start (D-07/D-08) and nap-end (D-09/D-10) two-model blends</name>
  <files>js/lib/forecast-blend.js, tests/unit/forecast-blend.test.js</files>
  <read_first>
    js/lib/forecast-blend.js (Plan 25-01's `blendForecast`, `trimmedBand`, `stabilityCheck`, `wrapToDay`, `extractTime` — this task edits the function body, replacing the `napStart`/`napEnd` stub lines; it must NOT touch the wake block above them).
    js/lib/forecast-tif.js (lines 540-579: the napStart-prediction block — `wakeAnchorForNap = resolveAnchor('wake', dayRecords, tifPredictions)` called BEFORE `tifPredictions.wake` exists, so it only ever resolves to an ACTUAL logged wake, never a predicted one — this task's napStart Model 1 anchor follows the identical constraint, for the identical reason: napStart is computed before wake in this file too; lines 581-613: the napEnd-prediction block, especially `resolveTodayNapDuration`/`napStartAnchor` resolution precedent for "actual if logged, else the already-computed napStart central" — D-10 states this explicitly for Algorithm C too).
    js/lib/forecast.js (lines 463-490: `buildNapGapSeries(dayRecords)` — computes `(napStart-wake)` per day, midnight-normalized, already exported and imported by Plan 25-01; lines 492-519: `buildNapDurationSeries(dayRecords)` — computes `(napEnd-napStart)` per day, same pattern).
    .planning/phases/25-algorithm-c-settings-modal/25-CONTEXT.md (D-07, D-08, D-09, D-10 — the decisions this task implements).
  </read_first>
  <behavior>
    - Nap-start, both models available: fixture with >= minDays days having wake, napStart (wake-to-napStart gap clustered ~3h), and today's wake logged (last day record's `.wake` set). Assert `result.napStart.central/min/max` are non-null 'HH:MM' strings and `min <= central <= max` (string comparison valid, no midnight crossing in the fixture).
    - Nap-start, Model 1 unavailable (today's wake NOT logged — last day record's `.wake` is null): assert `result.napStart` equals Model 2 alone (raw historic napStart trimmedBand converted via minutesToTime) — no stabilityCheck ran, derive the expected value from the SAME raw times the test seeds rather than hand-computing a second value.
    - Nap-end, both models available: fixture with wake, napStart, napEnd all logged across the window and today's wake logged. Assert `result.napEnd.central/min/max` are non-null 'HH:MM' strings, `min <= central <= max`.
    - Nap-end, Model 2 anchor precedence (D-10): one fixture where today's napStart IS logged (last day record's `.napStart` set) — assert the test can distinguish Model 2 using the ACTUAL logged napStart from a second fixture where it is NOT logged (falls back to `napStartPred.central`) by seeding the two fixtures with deliberately different historic napStart clustering so the two resulting bands are numerically distinguishable, then asserting each result's `napEnd.central` is consistent with its own anchor (do not assert exact minute values — assert the two fixtures produce DIFFERENT `napEnd.central` values, proving the anchor source actually changed the computation).
    - Nap-end, both models unavailable (no wake ever logged AND no napStart ever logged/predicted): assert `result.napEnd` deep-equals `{central:null, min:null, max:null}` — the universal null-on-no-data contract, not a thrown error.
  </behavior>
  <action>
    In `blendForecast`, insert nap-start and nap-end computation BEFORE the existing wake block (matching forecast-tif.js's napStart -> napEnd -> wake -> bedtime ordering — Plan 25-01 put wake first for the tracer; this task reorders by INSERTING nap-start/nap-end ahead of it, since neither depends on wake, and bedtime (Task 2) will depend on both wake and napEnd). Resolve `const todayRecord = dayRecords[dayRecords.length - 1];` once, near the top of the non-cold-start branch, and derive `const todayActualWakeHHMM = extractTime(todayRecord.wake);`, `const todayActualNapStartHHMM = extractTime(todayRecord.napStart);` — both used by this task and reused by Task 2's bedtime.

    Nap-start: `const napGaps = buildNapGapSeries(acceptedWindow);` (imported in Plan 25-01, now used). Model 1 (only when `todayActualWakeHHMM != null` and `napGaps.length > 0`): `const wakeAnchorMin = timeToMinutes(todayActualWakeHHMM); const gapBand = trimmedBand([...napGaps].sort((a,b)=>a-b), settings.blendTrimPct ?? 25, rejectedInWindow); const napStartModel1 = gapBand ? {min: wakeAnchorMin+gapBand.min, max: wakeAnchorMin+gapBand.max, median: wakeAnchorMin+gapBand.median} : null;`. Model 2 (always attempted): `const napStartTimes = acceptedWindow.map(d=>extractTime(d.napStart)).filter(t=>t!=null).map(timeToMinutes).sort((a,b)=>a-b); const napStartModel2 = trimmedBand(napStartTimes, settings.blendTrimPct ?? 25, rejectedInWindow);`. Combine using the SAME "collect available models into an array, average their medians for central, call stabilityCheck only when >= 2 are available, else pass the lone model through untouched, else null-shape" pattern Plan 25-01's wake block already established — write this combination as a small local helper `combineModels(models, shrinkage)` (models: array of non-null `{min,max,median}` objects already filtered to exclude unavailable ones) so Task 2's bedtime (3 models) and this task's napEnd (2 models) reuse it too, rather than duplicating the same if/else three times. `combineModels([])` returns `{central:null,min:null,max:null}` in HH:MM-ready numeric-null form (caller still runs it through the same null-shape branch as wake's A1-null case); `combineModels([m])` returns `{central:m.median, min:m.min, max:m.max}` (no stabilityCheck); `combineModels([m1,m2,...])` computes `rawCentral = mean(medians)` then delegates to `stabilityCheck`.

    Nap-end: `const napDurations = buildNapDurationSeries(acceptedWindow);`. Model 1 (chained, only when `todayActualWakeHHMM != null`): `const gapBand2 = trimmedBand([...napGaps].sort((a,b)=>a-b), trimPct, rejectedInWindow); const durBand2 = trimmedBand([...napDurations].sort((a,b)=>a-b), trimPct, rejectedInWindow); const napEndModel1 = (gapBand2 && durBand2) ? {min: wakeAnchorMin+gapBand2.min+durBand2.min, max: wakeAnchorMin+gapBand2.max+durBand2.max, median: wakeAnchorMin+gapBand2.median+durBand2.median} : null;`. Model 2: resolve `const napStartAnchorHHMM = todayActualNapStartHHMM ?? (napStartResult.central);` (D-10 — `napStartResult` is this task's own already-combined napStart prediction's HH:MM central); when non-null, `const napStartAnchorMin = timeToMinutes(napStartAnchorHHMM); const napEndModel2 = durBand2 ? {min: napStartAnchorMin+durBand2.min, max: napStartAnchorMin+durBand2.max, median: napStartAnchorMin+durBand2.median} : null;` (reuse the SAME `durBand2` computed for Model 1 — it does not depend on the anchor, only on historic durations). Combine via `combineModels`. Convert both napStart and napEnd's combined `{central,min,max}` (numeric minutes, or null) to the HH:MM shape via `minutesToTime` exactly like wake's final step, guarding the null case exactly as Plan 25-01's wake A1-null branch does.

    Follow RED->GREEN: write every case in `<behavior>` first, confirm failure, commit `test(25-02): add failing tests for nap-start/nap-end blends`; implement, confirm pass, commit `feat(25-02): implement nap-start and nap-end two-model blends`.
  </action>
  <verify>
    <automated>node --test tests/unit/forecast-blend.test.js</automated>
    <fails_when>non-zero exit, or the summary line reports a fail count greater than 0 for any napStart/napEnd test</fails_when>
  </verify>
  <acceptance_criteria>
    - `combineModels` exists as a shared local helper used by both napStart and napEnd (grep the file: exactly one definition, at least two call sites).
    - Every behavior case passes; `node --test tests/unit/forecast-blend.test.js` exits 0.
    - git log shows a `test(25-02): ...` commit preceding a `feat(25-02): ...` commit for this task.
  </acceptance_criteria>
  <done>napStart and napEnd both produce real, correctly-anchored two-model blends with graceful degradation to one model or null, matching D-07..D-10 exactly.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Bedtime three-band blend (D-03..D-06) + full four-event integration check</name>
  <files>js/lib/forecast-blend.js, tests/unit/forecast-blend.test.js</files>
  <read_first>
    js/lib/forecast-blend.js (after Task 1: `combineModels`, `todayRecord`, `todayActualWakeHHMM`, the already-computed `wake` and `napEnd` predictions — this task's bedtime block is the LAST computation in the function, after wake and napEnd both exist).
    js/lib/forecast-tif.js (lines 676-705: the bedtime-prediction block — `wakeAnchor2 = resolveAnchor('wake',...)`, `napEndAnchor = resolveAnchor('napEnd',...)`, the day-length band via `dayLength`, the activity-after-nap band via `activityAfterNap` — this task's Model 2/Model 3 mirror these two windows exactly, substituting Algorithm C's own trim/anchor-fallback rules for TIF's `resolveAnchor`).
    js/lib/metrics.js (lines 69-84: `dayLength(day)` = bedtime(day)-wake(day) same-record span; `activityAfterNap(day)` = bedtime(day)-napEnd(day) same-record span — both already imported by Plan 25-01, unused until this task).
    .planning/phases/25-algorithm-c-settings-modal/25-CONTEXT.md (D-03, D-04, D-05, D-06 — the decisions this task implements).
  </read_first>
  <behavior>
    - Bedtime, all three models available, nap day (isNoNapDay=false): fixture with >= minDays days having wake/bedtime/napStart/napEnd, today's wake AND today's napEnd both logged. Assert `result.bedtime.central/min/max` are non-null 'HH:MM' strings.
    - Bedtime, no-nap-day substitution (D-05): same historic fixture, call `blendForecast(dayRecords, settings, {}, true)` (isNoNapDay=true). Assert Model 3 is NOT an AA-anchored band — verify by seeding a fixture where the no-nap-day historic bedtime cluster (days with `napStart:null`) is deliberately far (e.g. 2 hours) from what the AA-band would have produced, then asserting `result.bedtime.central` reflects the substitution (falls within the no-nap-day cluster's range, not the AA-band's range) rather than hand-deriving the exact minute.
    - Bedtime, no-nap-day substitute unavailable (< minDays no-nap-day records exist) AND isNoNapDay=true: assert `blendForecast` does not throw and `result.bedtime` still returns a real prediction from the remaining available models (Model 1 + Model 2 only, or fewer) — the graceful-degradation truth, not a forced 3-band collapse.
    - Bedtime, Model 2/3 anchor fallback: a fixture where today's wake is NOT logged (falls back to `wake.central`) and today's napEnd is NOT logged (falls back to `napEnd.central`, itself possibly null on a thin fixture) — assert no throw; when both fallbacks resolve to real values, Model 2 and Model 3 still contribute normally.
    - Full four-event integration (PRED-13/PRED-17): on a single rich fixture (>= blendWindowDays days, full wake/bedtime/napStart/napEnd coverage, today's wake+napStart+napEnd all logged), assert `blendForecast(...)` returns an object where `wake`, `bedtime`, `napStart`, `napEnd` are ALL non-null `{central, min, max}` HH:MM triples (no stub survives) — a single assertion block proving Plan 25-01's shape contract now holds with real data end-to-end.
  </behavior>
  <action>
    In `blendForecast`, replace the `bedtime` stub (the LAST computation, after wake and napEnd). Resolve `const todayWakeAnchorHHMM = todayActualWakeHHMM ?? wake.central;` and, only when non-null, `const todayWakeAnchorMin = timeToMinutes(todayWakeAnchorHHMM);`. Resolve `const todayActualNapEndHHMM = extractTime(todayRecord.napEnd); const todayNapEndAnchorHHMM = todayActualNapEndHHMM ?? napEnd.central;` and, when non-null, `const todayNapEndAnchorMin = timeToMinutes(todayNapEndAnchorHHMM);`.

    Model 1 (always attempted): `const bedtimeTimes = acceptedWindow.map(d=>extractTime(d.bedtime)).filter(t=>t!=null).map(timeToMinutes).sort((a,b)=>a-b); const bedtimeModel1 = trimmedBand(bedtimeTimes, trimPct, rejectedInWindow);`.

    Model 2 (only when `todayWakeAnchorMin` is resolvable): `const dayLengths = acceptedWindow.map(dayLength).filter(v=>v!=null).sort((a,b)=>a-b); const dlBand = trimmedBand(dayLengths, trimPct, rejectedInWindow); const bedtimeModel2 = dlBand ? {min: wrapToDay(todayWakeAnchorMin+dlBand.min), max: wrapToDay(todayWakeAnchorMin+dlBand.max), median: wrapToDay(todayWakeAnchorMin+dlBand.median)} : null;` (wrap via the Plan 25-01 `wrapToDay` helper since a wake-anchored day-length projection can exceed 1440).

    Model 3 — nap-day branch (`isNoNapDay === false`, only when `todayNapEndAnchorMin` resolvable): `const aaDurations = acceptedWindow.map(activityAfterNap).filter(v=>v!=null).sort((a,b)=>a-b); const aaBand = trimmedBand(aaDurations, trimPct, rejectedInWindow); const bedtimeModel3 = aaBand ? {min: wrapToDay(todayNapEndAnchorMin+aaBand.min), max: wrapToDay(todayNapEndAnchorMin+aaBand.max), median: wrapToDay(todayNapEndAnchorMin+aaBand.median)} : null;`. Model 3 — no-nap-day branch (`isNoNapDay === true`, D-05): `const noNapBedtimeTimes = acceptedWindow.filter(d=>extractTime(d.napStart)==null).map(d=>extractTime(d.bedtime)).filter(t=>t!=null).map(timeToMinutes).sort((a,b)=>a-b); const bedtimeModel3 = trimmedBand(noNapBedtimeTimes, trimPct, 0);` (manualExcludedCount=0 here — this sub-window's own rejected-day count is not separately tracked per D-05's scope; treat the substitute band as freshly built from raw times, matching Model 1's own manualExcluded-vs-window-scope precedent loosely rather than threading a second rejected-count variable through). Document inline that when `bedtimeModel3` is null (thin no-nap history), the function does NOT force a fallback to the AA-band computation — it simply omits Model 3 from the group, per this plan's universal graceful-degradation contract.

    Combine `[bedtimeModel1, bedtimeModel2, bedtimeModel3].filter(m => m != null)` via `combineModels` (from Task 1) exactly as napStart/napEnd do. Convert the combined result to the final HH:MM `bedtime` object via `minutesToTime`, guarding the null-central case identically to wake/napStart/napEnd.

    Follow RED->GREEN: write every case in `<behavior>` first, confirm failure, commit `test(25-02): add failing tests for bedtime three-band blend and full-coverage integration`; implement, confirm pass, commit `feat(25-02): implement bedtime three-band blend, completing all 4 Algorithm C events`.
  </action>
  <verify>
    <automated>npm run test:unit</automated>
    <fails_when>non-zero exit, or the node:test end-of-run summary line reports a fail count greater than 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `blendForecast`'s bedtime block references `combineModels`, `wrapToDay`, `dayLength`, `activityAfterNap` (grep confirms all four identifiers appear in the bedtime section of forecast-blend.js).
    - The no-nap-day substitution branch (`isNoNapDay === true`) is reachable and tested (grep the test file for an `isNoNapDay` `true` call site).
    - The full four-event integration test passes: all four predictions are non-null on the rich fixture.
    - `npm run test:unit` passes with 0 failures.
  </acceptance_criteria>
  <done>Bedtime's three-band blend (including the D-05 no-nap-day substitution and graceful degradation when the substitute itself is thin) is implemented and tested; blendForecast() now fully satisfies PRED-13 (shape parity) and PRED-17 (all 4 events covered) on real data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Same boundary as Plan 25-01 — pure computation over already-validated local `dayRecords`/`settings`. No network, no new input parsing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-03 | Denial of Service | `combineModels` / anchor-fallback chains | low | accept | Every model-availability branch bottoms out in a bounded null-check within one `blendForecast()` call — no recursion, no unbounded loop, no cross-call state. |

</threat_model>

## Artifacts this phase produces (Plan 25-02)

- `combineModels(models, shrinkage)` (private, js/lib/forecast-blend.js) — shared 0/1/N-model combiner reused by napStart, napEnd, and bedtime
- Bedtime, napStart, napEnd full implementations in `blendForecast()` (js/lib/forecast-blend.js) — Plan 25-01's stubs fully replaced
- Extended `tests/unit/forecast-blend.test.js` coverage for all remaining D-03..D-10 model combinations and the full four-event integration assertion

<verification>
Run `npm run test:unit` — full suite passes with 0 failures, including all of `tests/unit/forecast-blend.test.js`.
</verification>

<success_criteria>
- `blendForecast()` covers all 4 events (wake, bedtime, napStart, napEnd) per PRED-17, each with the correct model count and D-03..D-10 anchor/fallback rules.
- The D-05 no-nap-day substitution and its own thin-data degradation are both exercised by a passing test.
- No regression in the full unit suite.
</success_criteria>

<output>
Create `.planning/phases/25-algorithm-c-settings-modal/25-02-SUMMARY.md` when done.
</output>
