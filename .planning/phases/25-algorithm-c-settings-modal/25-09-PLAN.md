---
phase: 25-algorithm-c-settings-modal
plan: 09
type: tdd
wave: 1
depends_on: ["25-08"]
files_modified:
  - js/lib/forecast-blend.js
  - tests/unit/forecast-blend.test.js
autonomous: true
requirements: [PRED-16, PRED-17]
gap_closure: true

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "For ANY raw historic clock-time-of-day sample array in this file (a1Times/wake's A1, napStartTimes/napStart Model 2, bedtimeTimes/bedtime Model 1, noNapBedtimeTimes/bedtime Model 3's no-nap substitute) that legitimately straddles the midnight boundary, the computed band's median lands at the mathematically correct circular center of the sample distribution — not the linear-sort false center (e.g. noon, for a 50/50 23:50/00:10 split) — closing 25-VERIFICATION.md's remaining gap / 25-REVIEW.md's new CR-01 root cause (distinct from the individually-wrapped-interval defect Plan 25-08 already closed)."
    - "combineModels()'s N-model rawCentral (bedtime's 2-3 models, napStart's 2 models, napEnd's 2 models) and wake's own inline 2-model rawCentral are computed via circular-aware averaging (aligning every model's median onto a shared reference before averaging) instead of a plain arithmetic mean — so a correct per-model median is never corrupted again one level up during cross-model combination, the second half of CR-01's root cause."
    - "blendForecast()'s bedtime prediction for the exact 25-REVIEW.md reproduction (20 days, wake fixed 06:00, bedtime alternating 23:50/00:10, no naps ever logged) returns a central that lies within its own reported [min, max] band (via the wrap-aware reading), both via the default isNoNapDay=false 2-model path (historic bedtime band + wake-anchored day-length band) and the isNoNapDay=true 3-model path (adding the no-nap-day bedtime substitute band) — proven via the real public blendForecast() API, not pre-fabricated stabilityCheck() inputs."
    - "The identical circular-median fix applies uniformly to wake's A1 band and napStart's Model 2 band, not just bedtime — proven via dedicated single-model-alone regression fixtures (today's bedtime/wake never logged, forcing the A1-alone / Model-2-alone code paths) so a 4th review round cannot find an un-fixed raw-time-of-day call site this plan missed."
    - "Adjacency (PRED-16 edge-probe, raw-sample case): a raw sample landing exactly at the wrap boundary (0 minutes / midnight) is aligned and included in circularTrimmedBand's computation via the same alignNearReference() tie-break contract Plan 25-08 already established — never silently dropped or double-counted."
    - "Ordering (PRED-16 edge-probe, raw-sample case): circularTrimmedBand's result for a fixed underlying set of raw samples is independent of which sample happens to occupy index 0 (and therefore anchors the alignment reference) — proven via a dedicated order-independence test."
    - "Empty (PRED-16 edge-probe, raw-sample case): circularTrimmedBand([], trimPct, manualExcludedCount) returns null exactly like trimmedBand([], ...) does today, preserving the existing null-on-no-data contract every call site already depends on."
  artifacts:
    - path: "js/lib/forecast-blend.js"
      provides: "Two new exported helpers: circularTrimmedBand(rawValues, trimPct, manualExcludedCount) (self-unwrap/align raw clock-time-of-day samples before delegating to trimmedBand's own unchanged budget/split/median math, then re-wrap the result) and circularMean(values) (align every value onto a shared reference before averaging, then re-wrap). All 4 raw-time-of-day call sites (a1Times, napStartTimes, bedtimeTimes, noNapBedtimeTimes) switched from trimmedBand(sorted) to circularTrimmedBand(raw). Both cross-model rawCentral computations (combineModels(), wake's inline blend) switched from a plain arithmetic mean to circularMean(). stabilityCheck(), selfUnwrapInterval(), alignNearReference(), wrapToDay(), trimmedBand()'s own body, combineModels()'s dispatch/short-circuits, and every anchor+duration model-construction call site (napStartModel1, napEndModel1, napEndModel2, a2, bedtimeModel2, bedtimeModel3's nap-day AA branch) are byte-identical to before this plan."
    - path: "tests/unit/forecast-blend.test.js"
      provides: "New describe('circularTrimmedBand(rawValues, trimPct, manualExcludedCount)', ...) block (4 cases) and describe('circularMean(values)', ...) block (4 cases) proving the two new helpers directly, plus a new describe('blendForecast — CR-01 root-cause circular-median gap-closure (Plan 25-09)', ...) block (4 cases: wake A1-alone, napStart Model-2-alone, bedtime 2-model default, bedtime 3-model isNoNapDay=true) proving the fix via the real public blendForecast() API, not pre-fabricated stabilityCheck() inputs."
  key_links:
    - "circularTrimmedBand()/circularMean() (new, forecast-blend.js) <-> alignNearReference()/wrapToDay() (existing, UNCHANGED, Plan 25-08's own primitives, reused not reimplemented) <-> the 4 raw-time-array call sites + 2 rawCentral call sites (blendForecast()/combineModels(), UPDATED to call the new functions) <-> stabilityCheck() (existing, UNCHANGED, Plan 25-08's already-correct interval comparison, now receiving genuinely-correct inputs for the first time instead of already-wrong scalars it could never repair) — the full pipeline from raw historic sample to final reported band is now circular-aware end-to-end, not just at the final comparison step."
  prohibitions:
    - statement: "MUST NOT modify stabilityCheck()'s own body/JSDoc, selfUnwrapInterval(), or alignNearReference() — Plan 25-08's fix is independently verified correct (hand-traced against all 5 of its own tests by 25-REVIEW.md's re-review); this plan only feeds it genuinely-correct inputs for the first time. Re-touching an already-closed, already-tested surface is out of scope and risks regressing it for no benefit."
      category: safety
      verification: judgment
    - statement: "MUST NOT modify trimmedBand()'s own budget/split/median math or wrapToDay()'s own body — circularTrimmedBand delegates to trimmedBand unchanged (after alignment) and calls wrapToDay unchanged (to re-wrap); every existing duration-based call site (gapBand, durBand2, gapBand2, durBand, dlBand, aaBand — all confirmed non-circular, see prohibition below) depends on trimmedBand's current linear behavior staying exactly as-is."
      category: correctness
      verification: judgment
    - statement: "MUST NOT fix only the 2-3 call sites 25-VERIFICATION.md's artifact list happened to cite by line number (trimmedBand's own math at :70-88, combineModels' averaging at :241-252, wake's inline blend at :440) while leaving any of the 4 actual raw-time-of-day sample arrays (a1Times, napStartTimes, bedtimeTimes, noNapBedtimeTimes) still calling the old linear trimmedBand — all 4 must be switched to circularTrimmedBand in this one plan. This is precisely the narrow point-fix pattern that produced three prior rounds (25-06 -> 25-07 -> 25-08, each closing one layer and a fresh review finding the next); this plan exists specifically to break that pattern."
      category: scope
      verification: judgment
    - statement: "MUST NOT touch any duration-based series or its model-construction call site (buildNapGapSeries, buildNapDurationSeries in forecast.js; sleepDuration, dayLength, activityAfterNap in metrics.js; napStartModel1, napEndModel1, napEndModel2, a2, bedtimeModel2, and bedtimeModel3's nap-day AA branch in forecast-blend.js) — confirmed non-circular by direct inspection: all three metrics.js functions and both forecast.js series builders normalize to always-positive elapsed minutes (`result < 0 ? result + 24*60 : result` / `if (gap < 0) gap += 24*60`) before ever reaching trimmedBand, so they never need circular treatment. Touching them is unrequested scope creep with real regression risk."
      category: scope
      verification: judgment
    - statement: "MUST NOT change combineModels()'s 0/1/N-model dispatch, its short-circuit returns for 0 or 1 models, or its call signature to stabilityCheck() — only the single rawCentral computation line changes (from a plain arithmetic mean to circularMean(...)). Likewise wake's inline blend keeps its own if(a1==null)/else if(a2==null)/else structure and its stabilityCheck([a1,a2], rawCentral, blendShrinkage) call exactly as-is; only the rawCentral line itself changes."
      category: correctness
      verification: judgment
    - statement: "MUST NOT rely solely on pre-fabricated {min,max,median} objects handed directly to stabilityCheck()/combineModels() for this plan's new regression coverage — the bedtime integration tests (and the wake/napStart single-model tests) must exercise the real data flow via the public blendForecast() API. This closes the exact test-coverage gap both 25-REVIEW.md and 25-VERIFICATION.md named against Plan 25-08's own 5 tests (which passed pre-fabricated intervals and bypassed the circular-unaware source path entirely)."
      category: safety
      verification: judgment
---

<objective>
Close 25-VERIFICATION.md's one remaining gap (= 25-REVIEW.md's new CR-01 finding, distinct from the individually-wrapped-interval defect Plan 25-08 already closed): make raw clock-time-of-day aggregation in `js/lib/forecast-blend.js` circular-aware at its SOURCE — the raw-sample median computation and the cross-model central averaging — not just at `stabilityCheck()`'s final band-comparison layer, which Plan 25-08 already correctly hardened but which cannot repair a scalar that arrived already wrong.

Purpose: Plan 25-08 fixed `stabilityCheck()`'s own min/max comparison for a SINGLE interval whose independently-`wrapToDay()`'d fields inverted (min > max). But 25-REVIEW.md's re-review, independently reproduced by 25-VERIFICATION.md via the real public `blendForecast()` API, found the SAME "central outside its own band" symptom still reproducible via a different, upstream root cause: `trimmedBand()`'s sort-then-median treats raw historic clock-time-of-day arrays (bedtime, wake, nap-start) as ordinary linear numbers, so a distribution that legitimately clusters across the midnight boundary (e.g. bedtime alternating 23:50/00:10) sorts into two separated blocks and produces a median nowhere near the true circular center; `combineModels()`'s and wake's own rawCentral averaging compound the error one level up by plain-averaging already-wrong (or even individually-correct-but-oppositely-wrapped) medians. `stabilityCheck()`'s `alignNearReference()` can only shift a value by whole days to the nearest copy of ITSELF — it has no mechanism to correct a scalar that was computed incorrectly before it ever arrived.

This plan audits the ENTIRE file for every raw clock-time-of-day aggregation site (not just the ones 25-VERIFICATION.md's artifact list named) and fixes all of them in one pass, reusing the exact self-unwrap/align-to-reference technique Plan 25-08 already proved correct for intervals — applied upstream, to raw samples and cross-model medians, before they ever reach `stabilityCheck()`.

Output: Two new exported helpers in `js/lib/forecast-blend.js` (`circularTrimmedBand`, `circularMean`), 4 raw-time-array call sites and 2 rawCentral call sites switched to use them, and new RED->GREEN unit + integration test coverage in `tests/unit/forecast-blend.test.js` — including the exact 25-REVIEW.md end-to-end reproduction via the public `blendForecast()` API, asserting `centralWithinBand()` is true.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-algorithm-c-settings-modal/25-VERIFICATION.md
@.planning/phases/25-algorithm-c-settings-modal/25-REVIEW.md
@js/lib/forecast-blend.js
@tests/unit/forecast-blend.test.js
</context>

## Source Coverage Audit

This is a gap-closure plan (`/gsd-plan-phase 25 --gaps`). The source set is 25-VERIFICATION.md's `gaps_remaining` entry (the new circular-median CR-01, distinct from Plan 25-08's already-closed CR-01), cross-referenced against 25-REVIEW.md's independent re-review — not a fresh requirements/research pass. PRED-13/14/15/17/UI-12 were already marked VERIFIED (5/6) and are not reopened here except where this fix's own correctness needs re-asserting.

| SOURCE | ID | Item | Plan | Status | Notes |
|--------|-----|------|------|--------|-------|
| GAP | `gaps_remaining` entry (= 25-REVIEW.md's CR-01: `trimmedBand()`/`combineModels()` not circular-aware) | Raw clock-time-of-day medians and cross-model central averaging treat midnight-straddling data as linear numbers | 25-09 | COVERED | Task 1 fixes the raw-sample median (`circularTrimmedBand`); Task 2 fixes the cross-model averaging (`circularMean`). Both were named as "missing" in 25-VERIFICATION.md's `gaps_remaining.missing` list verbatim. |
| REQ | PRED-16 | Interval stability check (overlap/union/shrinkage) for all events — central must lie in its own reported band | 25-01 (original stabilityCheck), 25-08 (hardened for individually-wrapped intervals), 25-09 (this plan — fixes the upstream inputs stabilityCheck receives) | ALREADY VERIFIED for the interval-comparison layer, NOW FIXED AT THE SOURCE | stabilityCheck's own comparison algorithm (25-08) is correct and untouched; this plan closes the gap by ensuring it is never again handed an already-wrong scalar. |
| REQ | PRED-17 | Algorithm C covers all 4 events (wake, bedtime, nap-start, nap-end) | 25-02 (already VERIFIED, unchanged) | NOT REOPENED, fixed uniformly | `circularTrimmedBand`/`circularMean` are shared functions all 4 events route through (wake and bedtime directly build raw-time models; napStart's Model 2 is a raw-time model; napEnd has no raw-time model at all, confirmed unaffected) — fixing the shared functions once fixes every affected event without touching any event-specific band-construction code. |
| Edge probe | PRED-13 boundary/adjacency/empty/ordering/precision (5 rows) | — | — | REVIEWED — OUT OF SCOPE, not reopened | Concerns `blendForecast()`'s entry-point shape parity with Classic/TIF, already VERIFIED (25-VERIFICATION.md truth #1). Unaffected by an internal fix confined to raw-sample aggregation. |
| Edge probe | PRED-14 adjacency/empty/encoding/ordering (4 rows) | — | — | REVIEWED — OUT OF SCOPE, not reopened | Concerns wake's dual-model blend structure, already VERIFIED (truth #3, PRED-14 SATISFIED). This plan fixes wake's A1 raw-time median and its rawCentral averaging correctness, not the dual-model structure itself. |
| Edge probe | PRED-15 empty/encoding (2 rows) | — | — | REVIEWED — OUT OF SCOPE, not reopened | Concerns bedtime's three-band blend structure, already VERIFIED (PRED-15 SATISFIED). Same rationale as PRED-14 above. |
| Edge probe | PRED-16 adjacency | "When two things are exactly equal or just touch, do they merge, collide, or separate?" | 25-09 | RESOLVED — folded into must_haves truth #5 | A raw sample landing exactly at 0 (midnight) is handled by `alignNearReference`'s existing, unchanged tie-break contract (Plan 25-08) — verified by hand-trace (candidates `value-DAY`/`value`/`value+DAY` resolve deterministically to whichever is nearest the shared reference, identically to any other sample; no special-casing needed or added). |
| Edge probe | PRED-16 empty | "What is the result for empty, single-element, or null input?" | 25-09 | RESOLVED — explicit test, folded into must_haves truth #7 | `circularTrimmedBand([], ...)` returns `null` (Task 1, case 1) — preserves `trimmedBand`'s existing contract every call site depends on. Single-element input is inherited unchanged from `trimmedBand`'s own existing math (no new edge case introduced by the alignment step, which is a no-op for N=1). |
| Edge probe | PRED-16 ordering | "When elements compare equal, is output order specified and stable?" | 25-09 | RESOLVED — explicit test, folded into must_haves truth #6 | `circularTrimmedBand`'s order-independence test (Task 1, case 4) proves the result is identical regardless of which raw sample occupies index 0. |
| Edge probe | PRED-17 adjacency/empty/ordering (3 rows) | — | — | REVIEWED — OUT OF SCOPE, not reopened | These concern per-event coverage completeness (already VERIFIED — all 4 events return real predictions). Fixing the shared `circularTrimmedBand`/`circularMean` functions corrects every affected event uniformly without any event-specific branching, per PRED-17's row above. |
| Edge probe | UI-12 unclassified | "unclassified — review manually" | 25-09 | REVIEWED — OUT OF SCOPE, not reopened | UI-12 concerns the Settings modal's algorithm selector, already VERIFIED. This is a pure-function fix confined to `js/lib/forecast-blend.js`; no UI code is touched. |
| Prohibition recall (specless-probe-fallback §B, "what could this gap silently become") | — | Adversarial pass over the defect | 25-09 | RESOLVED — 6 prohibitions kept | (1) Scope creep back into `stabilityCheck()`/`selfUnwrapInterval()`/`alignNearReference()` (already correct, Plan 25-08). (2) Scope creep into `trimmedBand()`'s own math or `wrapToDay()`'s own body. (3) Narrowing the fix back down to only the 2-3 call sites 25-VERIFICATION.md's artifact list named, missing one of the 4 actual raw-time arrays — the exact recurring failure pattern (25-06->25-07->25-08) this plan exists to break. (4) Scope creep into duration-based series/models (confirmed non-circular by direct inspection of metrics.js/forecast.js). (5) Scope creep into `combineModels()`'s dispatch/short-circuit structure beyond the one `rawCentral` line. (6) New tests that only hand-craft pre-fabricated `{min,max,median}` objects, repeating the exact test-coverage gap 25-REVIEW.md/25-VERIFICATION.md flagged against Plan 25-08. All six captured in `must_haves.prohibitions` above. |

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: circularTrimmedBand() — fix the raw-sample circular median (CR-01 root cause, part 1 of 2)</name>
  <files>js/lib/forecast-blend.js, tests/unit/forecast-blend.test.js</files>
  <read_first>
    js/lib/forecast-blend.js (lines 1-24: top-of-file header comment and "Decisions:" bullet list — add a new bullet here documenting this task's fix, after the existing D-11/D-12/D-13 bullet. Lines 52-88: `trimmedBand(sortedValues, trimPct, manualExcludedCount)` — its exact budget/split/median math, UNCHANGED by this task; `circularTrimmedBand` delegates to it after alignment. Lines 98, 100-112, 114-131: `DAY`, `selfUnwrapInterval`, `alignNearReference` — all already defined and exported/private from Plan 25-08, UNCHANGED, reused as-is (function declarations are hoisted, so textual placement of the new helper relative to these does not matter, but place it after `wrapToDay` for narrative clarity — see action). Lines 205-220: `wrapToDay(m)` — UNCHANGED, reused to re-wrap `circularTrimmedBand`'s result. Lines 322-328 (`napStartTimes` + `napStartModel2`), 391-397 (`a1Times` + `a1`), 461-467 (`bedtimeTimes` + `bedtimeModel1`), 506-513 (`noNapBedtimeTimes` + `bedtimeModel3`'s no-nap-day substitute branch) — the 4 exact call sites this task rewires; each currently builds a raw array via `.map(extractTime).filter(t=>t!=null).map(timeToMinutes).sort((a,b)=>a-b)` then calls `trimmedBand(sortedArray, blendTrimPct, <excludedCount>)`. Confirm the 6 OTHER `trimmedBand(` call sites (lines 312, 349, 355, 414, 475, 497 — all duration-based: gap/duration/sleepDuration/dayLength/activityAfterNap bands) are NOT touched.
    js/lib/metrics.js (lines 43-49 `sleepDuration`, 69-75 `activityAfterNap`, 78-84 `dayLength` — each normalizes to an always-positive elapsed-minutes value via `result < 0 ? result + 24*60 : result`; confirms these are duration series, never raw clock-time-of-day, and are out of scope per the kept prohibition).
    js/lib/forecast.js (lines 479-489 `buildNapGapSeries`, 508-518 `buildNapDurationSeries` — each normalizes via `if (gap < 0) gap += 24*60` / `if (dur < 0) dur += 24*60`; same out-of-scope confirmation as above).
    tests/unit/forecast-blend.test.js (lines 14-18: existing imports — `trimmedBand, stabilityCheck, blendForecast, wrapToDay` from `../../js/lib/forecast-blend.js`; add `circularTrimmedBand` to this import list. Lines 41-46: `centralWithinBand(minStr, maxStr, centralStr)` — the existing wrap-aware helper, reused unchanged by this task's integration tests. Lines 48-53: `BLEND_SETTINGS` fixture. Lines 55-63: `makeDay`/`fmt` and the existing `buildOverlapFixture`/`buildNoBedtimeFixture` helpers — model the 2 new fixture helpers on these. Lines 107-125: the existing `describe('trimmedBand(...)', ...)` block — the new `circularTrimmedBand` describe block is inserted immediately after this block's closing `});`, before `describe('stabilityCheck(...)', ...)` begins at line 131. Lines 258-271: the existing "Model 1 unavailable... falls back to Model 2" napStart test — models this task's own napStart Model-2-alone regression test; confirm it still passes unchanged after this task's edits (its fixture's napStart values, ~09:00-09:55, never approach the midnight boundary, so `circularTrimmedBand` degenerates to `trimmedBand`'s exact old behavior for that fixture). Lines 229-237: the existing "A2-unavailable" wake test — same degeneration check for `a1Times`/`a1`.
    .planning/phases/25-algorithm-c-settings-modal/25-VERIFICATION.md (the `gaps_remaining` entry's `artifacts` list and `missing` list — this task implements the first `missing` item verbatim: "Circular-aware median computation for raw clock-time-of-day samples (self-unwrap+align raw sample array before sorting, then wrap result)").
    .planning/phases/25-algorithm-c-settings-modal/25-REVIEW.md (the CR-01 section's root-cause chain step 1, the `trimmedBand()` hand-trace showing `{min:10, max:1430, median:720}` for a 50/50 split — this task's own test recomputes the equivalent numbers precisely for a matching N=10 fixture; see Behavior below for the exact hand-verified literals).
  </read_first>
  <behavior>
    New `describe('circularTrimmedBand(rawValues, trimPct, manualExcludedCount)', ...)` block (4 cases), inserted after the existing `trimmedBand` describe block:

    Case 1 — `circularTrimmedBand([], 10, 0)` must be exactly `null`. (Preserves `trimmedBand`'s existing empty-input contract — PRED-16 "empty" edge-probe row.)

    Case 2 — no wrap needed, degenerates to plain `trimmedBand`: `circularTrimmedBand([400, 410, 420, 430, 440], 10, 0)` must return exactly `{min: 400, max: 440, median: 420}` — identical to `trimmedBand`'s own first existing test case (line 108-111), proving the alignment step is a true no-op when no sample is near the midnight boundary.

    Case 3 — the CR-01 root-cause repro, exact hand-verified numbers matching 25-REVIEW.md's own illustration: raw input `[1430, 10, 1430, 10, 1430, 10, 1430, 10, 1430, 10]` (10 raw samples, i%2===0 -> 1430 i.e. 23:50, else 10 i.e. 00:10 — the alternating-bedtime pattern), `trimPct=25`, `manualExcludedCount=0`. `circularTrimmedBand(...)` must return exactly `{min: 1430, max: 10, median: 0}`. (Pre-fix comparison, calling the EXISTING unchanged `trimmedBand` directly on the plain-sorted version of this same raw array — `trimmedBand([10,10,10,10,10,1430,1430,1430,1430,1430], 25, 0)` — returns exactly `{min: 10, max: 1430, median: 720}`: median 720 = noon, the exact wrong value 25-REVIEW.md's CR-01 section illustrates by hand. Post-fix: `circularTrimmedBand` aligns every sample onto the same reference frame as `rawValues[0]` (1430) via `alignNearReference` — each `10` shifts to `1450` — sorts the aligned array to `[1430,1430,1430,1430,1430,1450,1450,1450,1450,1450]`, delegates to the UNCHANGED `trimmedBand` (budget=2, low=1, high=1, trimmed=`[1430,1430,1430,1430,1450,1450,1450,1450]`, median=(1430+1450)/2=1440), then re-wraps all three fields via `wrapToDay`: min=wrapToDay(1430)=1430, max=wrapToDay(1450)=10, median=wrapToDay(1440)=0 — the mathematically correct circular center of a symmetric 23:50/00:10 split IS exactly midnight.)

    Case 4 — order-independence: `circularTrimmedBand([10, 1430, 10, 1430, 10, 1430, 10, 1430, 10, 1430], 25, 0)` (case 3's array with every pair swapped, so index 0 is now `10` instead of `1430`) must return the IDENTICAL result: `{min: 1430, max: 10, median: 0}`. (Proves the choice of which raw sample anchors the alignment reference never changes the final wrapped output for a fixed underlying set of samples — resolves the PRED-16 "ordering" edge-probe row for the raw-sample case.)

    New fixture helpers + a new `describe('blendForecast — CR-01 root-cause circular-median gap-closure (Plan 25-09)', ...)` block, inserted at the end of the test file (after the existing final `describe('blendForecast — full four-event integration (PRED-13/PRED-17)', ...)` block) — this task adds ONLY its 2 single-model-alone cases; Task 2 appends its own bedtime cases to the SAME block afterward:

    New fixture `buildMidnightWakeNoBedtimeFixture(n = 20)`: n days, wake alternating `'23:55'`/`'00:05'` (i%2===0 -> '23:55' else '00:05'), bedtime always `null` (A2 unavailable, forcing the A1-alone fallback that never calls `stabilityCheck`/`combineModels` — isolates `circularTrimmedBand`'s fix on `a1Times` alone, with zero dependency on Task 2's `circularMean` fix).

    Case 5 (wake, A1-alone) — `blendForecast(buildMidnightWakeNoBedtimeFixture(20), BLEND_SETTINGS)` must return `result.wake` exactly equal to `{central: '23:55', min: '23:55', max: '00:05'}`. (Hand-verified: `a1Times` = 10x1435 + 10x5 alternating; aligned onto reference 1435, the `5`s shift to `1445`; `trimmedBand` on the sorted-aligned 20-element array with budget=5/low=2/high=3 gives min=1435, max=1445, median=1435 [trimmed length 15 is odd, so median is the single middle element, not an average — falls within the all-1435 block]; wrapped: min=1435, max=5, median=1435. A1-alone fallback uses `a1.median`/`a1.min`/`a1.max` directly, no `stabilityCheck` involved. NOTE: this test MUST assert the exact object, not `centralWithinBand()` — for a single-model fallback, `median` is ALWAYS between `min` and `max` by construction regardless of whether the median value itself is circularly correct, so `centralWithinBand()` would pass vacuously even pre-fix; only an exact-value assertion proves the median moved from the wrong value to the right one. Pre-fix, calling `trimmedBand` on the plain-linear-sorted array instead gives `{min: 5, max: 1435, median: 5}`, i.e. `wake = {central: '00:05', min: '00:05', max: '23:55'}` — a materially different, incorrect central prediction.)

    New fixture `buildMidnightNapStartFixture(n = 20)`: n days, wake fixed `'06:00'` except the LAST day's wake set to `null` (forces `wakeAnchorMin` to be null, so `napStartModel1` stays null regardless of `napGaps` — mirrors the existing "Model 1 unavailable" test's exact technique, lines 258-271), napStart alternating `'23:50'`/`'00:10'` (i%2===0 -> '23:50' else '00:10') for ALL n days including today, bedtime/napEnd always `null`.

    Case 6 (napStart, Model-2-alone) — `blendForecast(buildMidnightNapStartFixture(20), BLEND_SETTINGS)` must return `result.napStart` exactly equal to `{central: '23:50', min: '23:50', max: '00:10'}`. (Hand-verified: `napStartTimes` = 10x1430 + 10x10 alternating [same structure as case 3's N=10 fixture but doubled to N=20]; aligned onto reference 1430, the `10`s shift to `1450`; `trimmedBand` on the sorted-aligned 20-element array with budget=5/low=2/high=3 gives min=1430, max=1450, median=1430 [trimmed length 15, odd, middle element falls within the all-1430 block]; wrapped: min=1430, max=10, median=1430. `napStartModel1` is null [wake not logged today], so `combineModels([napStartModel2], shrinkage)` takes the 1-model short-circuit branch — returns `napStartModel2`'s own median/min/max directly, no `stabilityCheck`/`circularMean` involved. Same exact-value-not-centralWithinBand rationale as case 5 applies. Pre-fix: `{min: 10, max: 1430, median: 10}` -> `napStart = {central: '00:10', min: '00:10', max: '23:50'}`.)
  </behavior>
  <action>
    RED — add the new `describe('circularTrimmedBand(...)', ...)` block (4 cases) after the existing `trimmedBand` describe block, and add `circularTrimmedBand` to the existing import line at the top of the test file. Add the 2 new fixture helpers and the new `describe('blendForecast — CR-01 root-cause circular-median gap-closure (Plan 25-09)', ...)` block (2 cases: case 5 wake, case 6 napStart) at the end of the file, after the existing last describe block. Run `node --test tests/unit/forecast-blend.test.js` and confirm exactly these 6 new cases fail (a `circularTrimmedBand is not defined`/import error is an acceptable RED signal for cases 1-4; cases 5-6 should fail on the exact-value assertion) while all 34 pre-existing tests still pass. Commit `test(25-09): add failing circular-median raw-sample tests`.

    GREEN — in `js/lib/forecast-blend.js`, make exactly these changes:

    1. Immediately after `wrapToDay`'s function definition (after line 220) and before the `combineModels` section header comment (line 222), add a new section comment header ("circularTrimmedBand — exported for unit testing; fixes CR-01's raw-sample root cause, 25-VERIFICATION.md/25-REVIEW.md, Plan 25-09") followed by one new exported function: `circularTrimmedBand(rawValues, trimPct, manualExcludedCount)`. Its body: if `rawValues.length === 0` return `null`; otherwise set `reference` to `rawValues[0]` (the first raw, unsorted sample — mirrors `stabilityCheck`'s own reference convention, Plan 25-08); build `aligned` by mapping every element of `rawValues` through `alignNearReference(value, reference)`, then sort `aligned` ascending; delegate to the UNCHANGED `trimmedBand(aligned, trimPct, manualExcludedCount)`; if that returns `null`, return `null`; otherwise return a new object whose `min`, `max`, and `median` fields are each the delegated result's corresponding field passed through `wrapToDay(...)`. Give it a JSDoc explaining the self-unwrap-and-align-before-sort-and-median technique, citing CR-01/25-VERIFICATION.md/25-REVIEW.md/this plan (25-09) by name, matching the file's existing decision-citation convention, and explicitly noting that the returned band may itself legitimately have `min > max` when the true circular band crosses midnight (same contract `stabilityCheck`'s own output already has).

    2. Add one new bullet to the top-of-file "Decisions:" comment block (after the existing D-11/D-12/D-13 bullet, around line 22-23): note that Plan 25-09 adds `circularTrimmedBand()` to make raw clock-time-of-day median computation circular-aware, closing 25-VERIFICATION.md's remaining CR-01 root cause (the linear-sort-then-median bug distinct from Plan 25-08's individually-wrapped-interval fix).

    3. Rewire the 4 raw-time-of-day call sites to use the new function, removing the now-unnecessary pre-sort in each case (alignment+sort now happens inside `circularTrimmedBand`):
       - Lines 322-328 (`napStartTimes`/`napStartModel2`): drop the trailing `.sort((a, b) => a - b)` from the `napStartTimes` array-building chain (it now ends at `.map(timeToMinutes);`); change `napStartModel2`'s assignment from `trimmedBand(napStartTimes, blendTrimPct, rejectedInWindow)` to `circularTrimmedBand(napStartTimes, blendTrimPct, rejectedInWindow)`.
       - Lines 391-397 (`a1Times`/`a1`): same edit — drop the `.sort(...)`, change `trimmedBand(a1Times, ...)` to `circularTrimmedBand(a1Times, ...)`.
       - Lines 461-467 (`bedtimeTimes`/`bedtimeModel1`): same edit — drop the `.sort(...)`, change `trimmedBand(bedtimeTimes, ...)` to `circularTrimmedBand(bedtimeTimes, ...)`.
       - Lines 506-513 (`noNapBedtimeTimes`/`bedtimeModel3`'s no-nap-day substitute, the `else` branch of the `isNoNapDay` conditional): same edit — drop the `.sort(...)`, change `bedtimeModel3 = trimmedBand(noNapBedtimeTimes, blendTrimPct, 0);` to `bedtimeModel3 = circularTrimmedBand(noNapBedtimeTimes, blendTrimPct, 0);`.

    4. Make ZERO changes to any of the 6 other `trimmedBand(` call sites (lines 312, 349, 355, 414, 475, 497 — all duration-based), to `trimmedBand`'s own body, to `stabilityCheck`/`selfUnwrapInterval`/`alignNearReference`/`wrapToDay`, to `combineModels()` (Task 2 touches this), or to wake's inline blend (Task 2 touches this). Confirm via `git diff js/lib/forecast-blend.js` that the diff touches only: the relocated/added Decisions bullet, the new `circularTrimmedBand` section, and the 4 named call-site edits (each a 2-line change: drop `.sort(...)`, rename the function call).

    Run `node --test tests/unit/forecast-blend.test.js` until all tests (34 pre-existing + 6 new = 40) are green. Commit `feat(25-09): add circularTrimmedBand for circular-aware raw-sample medians`.
  </action>
  <verify>
    <automated>node --test tests/unit/forecast-blend.test.js</automated>
    <fails_when>non-zero exit code, or the summary line reports fewer than 40 passing tests, or any test name containing "circularTrimmedBand" or "CR-01 root-cause circular-median" is missing from the passing count</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "circularTrimmedBand(" js/lib/forecast-blend.js` reports at least 5 (1 `export function circularTrimmedBand` definition + 4 call-site invocations at the rewired napStartModel2/a1/bedtimeModel1/bedtimeModel3 sites).
    - `grep -c "trimmedBand(" js/lib/forecast-blend.js` reports exactly 8 (down from 11 before this task: the 4 rewired call sites are removed, but `circularTrimmedBand`'s own body adds exactly 1 new internal delegating call to `trimmedBand`; note this pattern also matches lowercase `trimmedBand(` inside `circularTrimmedBand(` only if grep is NOT case-sensitive — since `circularTrimmedBand` capitalizes the `T`, a case-sensitive grep for the literal lowercase `trimmedBand(` correctly excludes it).
    - `git diff js/lib/forecast-blend.js` shows zero changed lines inside `stabilityCheck`, `selfUnwrapInterval`, `alignNearReference`, `wrapToDay`'s own body, `trimmedBand`'s own body, `combineModels`, wake's inline blend (lines ~430-443), or any of the 6 unchanged (duration-based) `trimmedBand(` call sites.
    - `node --test tests/unit/forecast-blend.test.js` exits 0 with exactly 40 passing tests (34 pre-existing + 6 new: 4 `circularTrimmedBand` cases + 2 `blendForecast` single-model-alone regression cases), zero failures.
    - `npm run test:unit` exits 0 with zero regressions (expect the 972-test baseline plus this task's 6 new tests, i.e. 978).
    - `git log` shows a `test(25-09): ...` commit preceding a `feat(25-09): ...` commit for this task.
  </acceptance_criteria>
  <reversibility rating="reversible">A pure-function addition (circularTrimmedBand) plus 4 mechanical call-site swaps in one already-isolated, already-unit-tested file, with trimmedBand/stabilityCheck/wrapToDay/combineModels untouched. Reverting is a single-file diff; every pre-existing fixture is unaffected since none of them approach the midnight boundary.</reversibility>
  <done>circularTrimmedBand() correctly computes the circular median/min/max of any raw clock-time-of-day sample array (wrapped or not), all 4 raw-time-of-day call sites use it, and the wake-A1-alone and napStart-Model2-alone single-model regression fixtures prove exact-value correctness via the real blendForecast() API.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: circularMean() — fix cross-model central averaging (CR-01 root cause, part 2 of 2) and close the full end-to-end repro</name>
  <files>js/lib/forecast-blend.js, tests/unit/forecast-blend.test.js</files>
  <read_first>
    js/lib/forecast-blend.js AS MODIFIED BY TASK 1 (re-read the file after Task 1's GREEN commit, not the pre-Task-1 version). Lines 222-252 (`combineModels` section, after Task 1's edits the line numbers shift slightly downward by however many lines Task 1's new `circularTrimmedBand` section added — locate by function name, not by absolute line number): the 0/1/N-model dispatcher; its `models.length === 0`/`=== 1` short-circuit branches, UNCHANGED by this task; the line `const rawCentral = models.reduce((sum, m) => sum + m.median, 0) / models.length;` — the ONE line this task changes, immediately followed by `const sc = stabilityCheck(models, rawCentral, shrinkage);`, UNCHANGED. Wake's inline blend section (originally lines 430-443, locate by the comment "// Step 6 — combine A1 and A2."): the `if (a1 == null) {...} else if (a2 == null) {...} else { const rawCentral = (a1.median + a2.median) / 2; const sc = stabilityCheck([a1, a2], rawCentral, blendShrinkage); ... }` structure — again, the ONE line this task changes is the `rawCentral` assignment; the `if/else if/else` structure and the `stabilityCheck` call are UNCHANGED.
    tests/unit/forecast-blend.test.js AS MODIFIED BY TASK 1 — locate the new `describe('blendForecast — CR-01 root-cause circular-median gap-closure (Plan 25-09)', ...)` block Task 1 added at the end of the file (containing Task 1's 2 fixture helpers and 2 test cases); this task APPENDS 2 more fixture helpers and 2 more `it(...)` cases to the SAME describe block, after Task 1's cases. Also locate Task 1's new `circularTrimmedBand` describe block; this task adds a new sibling `circularMean` describe block immediately after it (still before the existing `stabilityCheck` describe block). The `centralWithinBand(minStr, maxStr, centralStr)` helper (lines 41-46, unaffected by Task 1's edits) is reused for this task's 2 bedtime integration tests.
    .planning/phases/25-algorithm-c-settings-modal/25-VERIFICATION.md (the `gaps_remaining` entry's second `missing` item, implemented verbatim by this task: "Circular-aware central averaging in combineModels() and wake's inline blend (align each model's median onto shared reference before averaging)"; and the third `missing` item, implemented by this task's bedtime tests: "Regression test with bedtime/wake/napStart alternating across midnight (23:50/00:10 fixture), asserting centralWithinBand() is true").
    .planning/phases/25-algorithm-c-settings-modal/25-REVIEW.md (the exact repro command block under "Reproduced end-to-end via the actual public blendForecast() API" — this task's bedtime case 1 uses the identical fixture shape: 20 days, wake fixed 06:00, bedtime alternating 23:50/00:10, napStart/napEnd null, called with `{minDays:7, blendWindowDays:90, blendTrimPct:25, blendShrinkage:0.3}` and no `isNoNapDay` argument).
  </read_first>
  <behavior>
    New `describe('circularMean(values)', ...)` block (4 cases), inserted immediately after Task 1's `circularTrimmedBand` describe block, before the existing `stabilityCheck` describe block:

    Case 1 — no wrap needed, matches plain arithmetic mean: `circularMean([700, 710])` must be exactly `705`.

    Case 2 — 2 values symmetric across midnight: `circularMean([1430, 10])` must be exactly `0`. (Pre-fix, a plain arithmetic mean gives `(1430+10)/2 = 720` [noon] — the exact wrong-direction error 25-REVIEW.md's CR-01 names for combineModels'/wake's own averaging. Post-fix: reference=1430 [first value]; `10` aligns to `1450` [nearest of -1430/10/1450 to 1430]; mean=(1430+1450)/2=1440; `wrapToDay(1440)=0` — the true circular midpoint of 23:50 and 00:10 IS exactly midnight.)

    Case 3 — order-independence: `circularMean([10, 1430])` (case 2's values swapped) must be exactly `0`, identical to case 2. (Proves the internal choice of which value anchors the alignment reference never changes the final wrapped mean.)

    Case 4 — 3 values (bedtime-style group), generalizes with zero branching by count: `circularMean([1430, 1430, 20])` must be exactly `0`. (Pre-fix, a plain arithmetic mean gives `(1430+1430+20)/3 = 960` [16:00] — wildly wrong. Post-fix: reference=1430; the two `1430`s stay unchanged, `20` aligns to `1460` [nearest of -1420/20/1460 to 1430]; mean=(1430+1430+1460)/3=1440; `wrapToDay(1440)=0`. This exactly mirrors the weighted-toward-the-majority-cluster circular mean a human would compute by hand: two samples at 23:50 and one 30 minutes later at 00:20 average to 1/3 of the way from 23:50 toward 00:20, landing at exactly 00:00.)

    New fixture `buildMidnightBedtimeFixture(n = 20)`: n days, wake fixed `'06:00'` for ALL days (including today), bedtime alternating `'23:50'`/`'00:10'` (i%2===0 -> '23:50' else '00:10'), napStart/napEnd always `null` — this is the EXACT shape of 25-REVIEW.md's own CR-01 reproduction fixture.

    Case 5 (bedtime, 2-model default path, the exact 25-REVIEW.md repro) — call `blendForecast(buildMidnightBedtimeFixture(20), BLEND_SETTINGS)` (no `isNoNapDay` argument, so it defaults to `false`; since no naps are ever logged, `napEnd.central` stays `null`, so `bedtimeModel3` [the AA-band branch] stays null too — only `bedtimeModel1` [historic bedtime band] and `bedtimeModel2` [wake-anchored day-length band] combine). Assert `centralWithinBand(result.bedtime.min, result.bedtime.max, result.bedtime.central)` is `true`. Also assert `result.bedtime.central` is a string (sanity, matching the file's existing "returns a valid non-null HH:MM band" test style). PRE-FIX (i.e. with Task 1's `circularTrimmedBand` fix already landed but WITHOUT this task's `circularMean` fix — hand-verify this is genuinely RED before implementing): `bedtimeModel1` = `{min:1430, max:10, median:1430}` (correct, from Task 1's fix — `bedtimeTimes` is the same 20-sample alternating pattern as Task 1's case 3/6, doubled), `bedtimeModel2` = `{min:1430, max:10, median:1430}` (this one was NEVER buggy — wake is held constant at 360 across all days, so `wake-anchor + dayLength` reconstructs `bedtime` exactly). The plain-average `rawCentral = (1430+1430)/2 = 1430` would ALREADY be correct by coincidence in this specific 2-identical-model case — so this test's RED signal comes specifically from confirming it is genuinely GREEN with `circularMean` too (not a regression risk), while the true "before ANY of this plan's fixes" baseline (pre-Task-1 as well) produces `bedtimeModel1 = {min:10, max:1430, median:10}` [Task 1's own bug] feeding a plain-average `rawCentral = (10+1430)/2 = 720`, which — after 25-08's already-correct `stabilityCheck` alignment step — resolves to a central around `08:2x` outside the reported band, matching 25-REVIEW.md's exact `08:25`/`[00:10,00:10]` finding. This test's real value is proving the FULL fix (Task 1 + Task 2 together) reproduces and closes 25-REVIEW.md's exact scenario end-to-end.

    Case 6 (bedtime, 3-model isNoNapDay=true path, exercises bedtimeModel3's noNapBedtimeTimes fix specifically) — call `blendForecast(buildMidnightBedtimeFixture(20), BLEND_SETTINGS, {}, true)` (same fixture, `isNoNapDay=true` this time — since every day already has no nap, `bedtimeModel3`'s no-nap-day substitute pulls from the SAME 20 bedtime records as `bedtimeModel1`, giving 3 models: `bedtimeModel1`, `bedtimeModel2`, `bedtimeModel3`, all `{min:1430, max:10, median:1430}`). Assert `centralWithinBand(result.bedtime.min, result.bedtime.max, result.bedtime.central)` is `true`. This specifically proves `noNapBedtimeTimes`'s `circularTrimmedBand` call site (Task 1) AND `combineModels`'s 3-model `circularMean` averaging (Task 2) both work correctly together in the D-05/D-06 3-model bedtime combine path — distinct from case 5's 2-model default path.
  </behavior>
  <action>
    RED — add the new `describe('circularMean(...)', ...)` block (4 cases) immediately after Task 1's `circularTrimmedBand` describe block, and add `circularMean` to the test file's import line (alongside `circularTrimmedBand` added by Task 1). Append the new fixture `buildMidnightBedtimeFixture` and 2 new `it(...)` cases (case 5, case 6) to the SAME `describe('blendForecast — CR-01 root-cause circular-median gap-closure (Plan 25-09)', ...)` block Task 1 created, after Task 1's 2 cases. Run `node --test tests/unit/forecast-blend.test.js` and confirm exactly these 6 new cases fail while all 40 tests from Task 1 still pass. Commit `test(25-09): add failing circular-mean and full CR-01 repro tests`.

    GREEN — in `js/lib/forecast-blend.js`, make exactly these changes:

    1. Immediately after `circularTrimmedBand`'s closing brace (added by Task 1), add one new exported function: `circularMean(values)`. Its body: set `reference` to `values[0]`; map every element of `values` through `alignNearReference(value, reference)`; sum the aligned values and divide by `values.length`; return that quotient passed through `wrapToDay(...)`. Give it a JSDoc explaining the circular-averaging technique (same `alignNearReference`/`wrapToDay` primitives, reused not reimplemented), citing CR-01/25-VERIFICATION.md/25-REVIEW.md/this plan (25-09) by name, and noting this closes the second (and final) `missing` item from 25-VERIFICATION.md's `gaps_remaining` entry.

    2. Add one new bullet to the top-of-file "Decisions:" comment block (after Task 1's new bullet): note that Plan 25-09 adds `circularMean()` to make `combineModels()`'s and wake's own cross-model central averaging circular-aware, completing the CR-01 fix.

    3. In `combineModels()`, change the line `const rawCentral = models.reduce((sum, m) => sum + m.median, 0) / models.length;` to `const rawCentral = circularMean(models.map(m => m.median));`. Make no other change to `combineModels()` — its 0/1-model short-circuit branches and its `stabilityCheck(models, rawCentral, shrinkage)` call remain exactly as-is.

    4. In wake's inline blend (the `else` branch of the `if (a1 == null) {...} else if (a2 == null) {...} else {...}` structure), change the line `const rawCentral = (a1.median + a2.median) / 2;` to `const rawCentral = circularMean([a1.median, a2.median]);`. Make no other change to this block — its `stabilityCheck([a1, a2], rawCentral, blendShrinkage)` call and the surrounding `if`/`else if` branches remain exactly as-is.

    5. Confirm via `git diff js/lib/forecast-blend.js` (comparing against the post-Task-1 state) that the diff touches only: the new Decisions bullet, the new `circularMean` function, and the 2 named `rawCentral` line replacements — nothing else in `combineModels()`, wake's inline blend, or anywhere else changes.

    Run `node --test tests/unit/forecast-blend.test.js` until all tests (40 from Task 1 + 6 new = 46) are green. Also run `npm run test:unit` for full-suite confidence. Commit `feat(25-09): add circularMean and close CR-01 end-to-end`.
  </action>
  <verify>
    <automated>node --test tests/unit/forecast-blend.test.js</automated>
    <fails_when>non-zero exit code, or the summary line reports fewer than 46 passing tests, or the bedtime "CR-01 root-cause" cases (case 5 / case 6) are missing from the passing count</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "circularMean(" js/lib/forecast-blend.js` reports at least 3 (1 `export function circularMean` definition + 2 call-site invocations: `combineModels`'s rawCentral line and wake's inline blend's rawCentral line).
    - `grep -c "circularMean(models.map(m => m.median))" js/lib/forecast-blend.js` reports exactly 1 (combineModels()'s rawCentral now calls the new circular-aware averager with the exact model-median mapping, replacing its old plain-arithmetic-mean line).
    - `grep -c "circularMean(\[a1.median, a2.median\])" js/lib/forecast-blend.js` reports exactly 1 (wake's inline blend's rawCentral now calls the same new averager with its own two medians, replacing its old plain-arithmetic-mean line).
    - `git diff js/lib/forecast-blend.js` (against the post-Task-1 commit) shows zero changed lines inside `combineModels()`'s dispatch/short-circuit branches, wake's `if(a1==null)/else if(a2==null)` branches, `stabilityCheck`, `selfUnwrapInterval`, `alignNearReference`, `wrapToDay`, `trimmedBand`, `circularTrimmedBand`, or any of the 6 duration-based `trimmedBand(` call sites.
    - `node --test tests/unit/forecast-blend.test.js` exits 0 with exactly 46 passing tests (34 original + 6 from Task 1 + 6 from Task 2), zero failures.
    - `npm run test:unit` exits 0 with zero regressions (expect the 972-test baseline plus 12 new tests across both tasks, i.e. 984).
    - `git log` shows a `test(25-09): ...` commit preceding a `feat(25-09): ...` commit for this task, both after Task 1's two commits.
  </acceptance_criteria>
  <reversibility rating="reversible">A pure-function addition (circularMean) plus 2 single-line replacements inside already-isolated, already-unit-tested functions (combineModels, wake's inline blend), with their dispatch structure and stabilityCheck call sites untouched. Reverting is a single-file diff; every pre-existing fixture whose models never straddle midnight relative to each other produces an identical circularMean/plain-average result, so no regression risk.</reversibility>
  <done>circularMean() correctly computes the circular mean of 2+ already-computed medians, both combineModels() and wake's inline blend use it for rawCentral, and the exact 25-REVIEW.md end-to-end reproduction (bedtime alternating 23:50/00:10, both the default 2-model and isNoNapDay=true 3-model paths) now returns a central that lies within its own reported band via the real public blendForecast() API — closing 25-VERIFICATION.md's last remaining gap for Phase 25.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | `circularTrimmedBand()`/`circularMean()` continue to consume only already-computed internal minute integers derived from the app's own already-validated local `dayRecords`/`settings` state (offline-first, no network I/O, no new user-input parsing, no storage-schema change). This plan corrects internal arithmetic; it crosses no new trust boundary, identical in kind to Plan 25-08's own threat-model entries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-18 | Tampering | `circularTrimmedBand()`/`circularMean()` internals (`js/lib/forecast-blend.js`) | low | accept | Pure arithmetic on already-computed internal minute integers and already-filtered arrays; no new mutable state, no new input-parsing surface, no DOM/storage access added. `Object.freeze` is not applicable to function bodies and is not a relevant mitigation here. |
| T-25-19 | Denial of Service | `circularTrimmedBand`/`circularMean`'s per-call work | low | accept | The fix adds O(N) array maps (align, then delegate to the already-O(N log N) sort inside the unchanged `trimmedBand`) over the same historic-window-bounded arrays (`blendWindowDays`, max 180 per D-11) the existing call sites already iterated; no new loop, recursion, or unbounded input path is introduced. |

</threat_model>

## Artifacts this phase produces (Plan 25-09)

- Modified: `js/lib/forecast-blend.js` — two new exported functions `circularTrimmedBand(rawValues, trimPct, manualExcludedCount)` (Task 1) and `circularMean(values)` (Task 2), placed together in a new section immediately after `wrapToDay` and before `combineModels`. 4 raw-time-of-day call sites (napStartTimes/napStartModel2, a1Times/a1, bedtimeTimes/bedtimeModel1, noNapBedtimeTimes/bedtimeModel3) rewired to `circularTrimmedBand` (Task 1). 2 rawCentral computations (`combineModels()`, wake's inline blend) rewired to `circularMean` (Task 2). Two new bullets added to the top-of-file "Decisions:" comment block. `stabilityCheck()`, `selfUnwrapInterval()`, `alignNearReference()`, `wrapToDay()`, `trimmedBand()`'s own body, `combineModels()`'s dispatch structure, and all 6 duration-based model-construction call sites (napStartModel1, napEndModel1, napEndModel2, a2, bedtimeModel2, bedtimeModel3's nap-day AA branch) are byte-identical to before this plan.
- Modified: `tests/unit/forecast-blend.test.js` — new `describe('circularTrimmedBand(...)', ...)` block (4 cases, Task 1), new `describe('circularMean(...)', ...)` block (4 cases, Task 2), new `describe('blendForecast — CR-01 root-cause circular-median gap-closure (Plan 25-09)', ...)` block (4 cases total: 2 single-model-alone regression cases from Task 1, 2 bedtime multi-model regression cases from Task 2), 4 new fixture helpers (`buildMidnightWakeNoBedtimeFixture`, `buildMidnightNapStartFixture` from Task 1; `buildMidnightBedtimeFixture` from Task 2 — shared by cases 5 and 6). `circularTrimmedBand` and `circularMean` added to the existing import line.

<verification>
Run `node --test tests/unit/forecast-blend.test.js` — passes with exactly 46 tests (34 pre-existing + 12 new across both tasks). For full-suite confidence, also run `npm run test:unit` (expect the 972-test baseline plus these 12 new tests, i.e. 984, with zero regressions since every pre-existing fixture's raw-time arrays and cross-model medians never straddle midnight or land on opposite sides of it).
</verification>

<success_criteria>
- `circularTrimmedBand()` correctly computes the circular median/min/max for ANY raw clock-time-of-day sample array, wrapped or not, for all 4 named call sites (a1Times, napStartTimes, bedtimeTimes, noNapBedtimeTimes).
- `circularMean()` correctly computes the circular mean of 2+ already-computed medians for both `combineModels()`'s N-model path and wake's own 2-model inline path.
- The exact 25-REVIEW.md end-to-end reproduction (20-day bedtime alternating 23:50/00:10, via the real public `blendForecast()` API) returns a `central` that lies within its own reported `[min, max]` band, for both the default `isNoNapDay=false` 2-model path and the `isNoNapDay=true` 3-model path.
- The identical fix is proven for wake's A1 band and napStart's Model 2 band via dedicated single-model-alone regression fixtures, not inferred by extrapolation from the bedtime case alone.
- `stabilityCheck()`, `selfUnwrapInterval()`, `alignNearReference()`, `wrapToDay()`, `trimmedBand()`'s own body, `combineModels()`'s dispatch structure, and all 6 duration-based model-construction call sites are byte-identical to before this plan.
- 25-VERIFICATION.md's `gaps_remaining` entry (PRED-16 interval stability check, root-caused to `trimmedBand`/`combineModels` not being circular-aware) is closed; Phase 25 has zero open verification gaps.
</success_criteria>

<output>
Create `.planning/phases/25-algorithm-c-settings-modal/25-09-SUMMARY.md` when done.
</output>
