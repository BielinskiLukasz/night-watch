---
phase: 25-algorithm-c-settings-modal
plan: 06
type: execute
wave: 1
depends_on: ["25-04"]
files_modified:
  - index.html
  - js/ui/settings-modal.js
  - tests/e2e/settings-modal.spec.js
autonomous: true
requirements: [UI-12]
gap_closure: true

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The Settings modal's Forecast & Prediction fieldset no longer renders a 'noNapBedtimeOffsetMinutes' input — grep across index.html and js/ui/settings-modal.js returns zero matches — so a user who edits that field can no longer have the edit silently discarded on Save (CR-01 / 19-REVIEW.md, closed; restated as 25-VERIFICATION.md gap 1 / 25-REVIEW.md CR-01)."
    - "The eveningHour field immediately preceding the removed block still round-trips correctly through Save -> reload, proving the fieldset edit was surgical and did not collaterally damage a sibling field."
    - "js/lib/db-shape.js's DEFAULT_SETTINGS and js/lib/settings-validate.js's RULES are unchanged by this plan — noNapBedtimeOffsetMinutes stays removed per D-10 (Phase 19); this plan does not reverse that decision, it only finishes removing the orphan UI surface D-10 left behind."
  artifacts:
    - path: "index.html"
      provides: "Forecast & Prediction fieldset with the orphan 'No-nap bedtime offset (min)' label+input removed; eveningHour and targetSleepMinutes labels now adjacent with no gap"
    - path: "js/ui/settings-modal.js"
      provides: "populateForm() and the onClose Save handler's raw-object literal both no longer reference noNapBedtimeOffsetMinutes"
    - path: "tests/e2e/settings-modal.spec.js"
      provides: "New regression test proving the input is gone from the DOM and that the surrounding fieldset still round-trips correctly"
  key_links:
    - "index.html's Forecast & Prediction fieldset <-> js/ui/settings-modal.js's populateForm/onClose <-> js/lib/settings-validate.js's RULES (UNMODIFIED by this plan) — after this fix, every input read by settings-modal.js inside that fieldset has a matching RULES entry; none is silently dropped by validateSettings()."
  prohibitions:
    - statement: "MUST NOT remove, reorder, or modify any other <label>/<input> in the Forecast & Prediction fieldset besides the noNapBedtimeOffsetMinutes block (index.html lines ~302-306) and its two read/populate/collect counterparts in settings-modal.js."
      category: safety
      verification: judgment
    - statement: "MUST NOT re-add noNapBedtimeOffsetMinutes to DEFAULT_SETTINGS (js/lib/db-shape.js) or RULES (js/lib/settings-validate.js) — D-10's removal decision (Phase 19 Plan 02) is not being reversed by this plan; tests/unit/db-shape.test.js and tests/unit/settings-validate.test.js already assert its absence and must keep passing unmodified."
      category: correctness
      verification: judgment
---

<objective>
Close 25-VERIFICATION.md gap 1 (= 25-REVIEW.md CR-01 = the still-unresolved 19-REVIEW.md CR-01): delete the orphan `noNapBedtimeOffsetMinutes` `<input>` from `index.html` and its two dead read/write call sites in `js/ui/settings-modal.js`, so a user can no longer edit a field that Save silently discards without any error.

Purpose: `js/lib/db-shape.js`'s `DEFAULT_SETTINGS` and `js/lib/settings-validate.js`'s `RULES` correctly removed this field in Phase 19 (D-10), but the HTML input and its settings-modal.js glue code were never deleted. `validateSettings()` only copies fields present in `RULES`, so every Save through this still-visible field is a silent, invisible data-loss bug. Phase 25 touched both files (to add the `#blendOptions` fieldset) without cleaning this up, which is why 25-VERIFICATION.md blocks phase sign-off on it.

Output: `index.html` and `js/ui/settings-modal.js` with the orphan field's markup and glue code fully removed, plus a new Playwright regression test in `tests/e2e/settings-modal.spec.js` proving both the removal and that the fix did not collaterally damage the surrounding fieldset.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-algorithm-c-settings-modal/25-VERIFICATION.md
@.planning/phases/25-algorithm-c-settings-modal/25-REVIEW.md
@.planning/phases/19-split-bedtime-wake-anchored-nap/19-REVIEW.md
@index.html
@js/ui/settings-modal.js
</context>

## Source Coverage Audit

This is a gap-closure plan (`/gsd-plan-phase 25 --gaps`). The source set is 25-VERIFICATION.md's gap 1 entry, not a fresh requirements/research pass — PRED-13..17/UI-12 were all already marked VERIFIED by Plans 25-01..25-05 (per 25-VERIFICATION.md's own "Requirements Coverage" table) and are not reopened here.

| SOURCE | ID | Item | Plan | Status | Notes |
|--------|-----|------|------|--------|-------|
| GAP | gap 1 (= CR-01) | Orphan `noNapBedtimeOffsetMinutes` field silently discards user edits | 25-06 Task 1+2 | COVERED | Removal + E2E regression test. |
| GAP | 19-REVIEW.md CR-01 (original) | Same defect, first identified Phase 19, never fixed | 25-06 Task 1 | COVERED | This is the actual, final fix of the Phase 19 blocker; 25-REVIEW.md's CR-01 is a restatement, not a new defect. |
| CONTEXT | D-10 (Phase 19) | `noNapBedtimeOffsetMinutes` removed from DEFAULT_SETTINGS/migration/validator | 25-06 | HONORED, NOT REOPENED | This plan finishes D-10's UI-side cleanup; it does not touch `db-shape.js`/`settings-validate.js`, which already correctly implement D-10. See kept prohibition above. |
| REQ | UI-12 | Settings modal Algorithm selector + context-sensitive fieldsets | 25-04 | ALREADY VERIFIED (not reopened) | This plan edits a field inside the same Forecast & Prediction fieldset UI-12 governs, but does not touch the selector or any `#classicOptions`/`#tifOptions`/`#blendOptions` show/hide logic. |
| Edge probe (specless-probe-fallback) | none applicable | 18-row report scoped to PRED-13..17/UI-12's boundary/adjacency/empty/ordering/precision/encoding categories | — | REVIEWED — NONE APPLY | The removed field carries no numeric boundary, no ordering, no encoding, and no percentile/interval surface of its own — it is dead markup and three lines of `FormData` glue code with zero algorithmic behavior. All 18 rows concern `blendForecast()`'s percentile/interval math (Plan 25-07's territory) or the three-way selector (already verified, Plan 25-04/25-05); none maps to this plan's scope. Recorded here rather than silently dropped, per specless-probe-fallback's scope note. |
| Prohibition recall (specless-probe-fallback §B, scoped to "what could this gap silently become") | — | Adversarial pass over gap 1 | 25-06 | RESOLVED — 2 prohibitions kept | (1) Collateral damage to sibling fields in the same fieldset during the HTML/JS edit. (2) Silently reversing D-10 by re-adding the field to DEFAULT_SETTINGS/RULES instead of deleting the UI surface. Both captured in `must_haves.prohibitions` above. |

<tasks>

<task type="auto">
  <name>Task 1: Delete the orphan noNapBedtimeOffsetMinutes input and its glue code (CR-01)</name>
  <files>index.html, js/ui/settings-modal.js</files>
  <read_first>
    index.html (lines 298-312: the "Evening hour" label at 298-301, immediately followed by the orphan "No-nap bedtime offset (min)" `<label>` block at lines 302-306 — `<input type="number" id="noNapBedtimeOffsetMinutes" name="noNapBedtimeOffsetMinutes" min="0" max="120" step="5">` — immediately followed by the "Sleep target (minutes)" label at lines 307-312. This task deletes exactly the 302-306 block; the 298-301 and 307-312 blocks are untouched and must remain adjacent to each other afterward.)
    js/ui/settings-modal.js (lines 115-118 inside `populateForm(snap)`: `const eveningHourEl = ...` at 115-116 immediately followed by the orphan `const noNapOffsetEl = form.elements.namedItem('noNapBedtimeOffsetMinutes'); if (noNapOffsetEl) noNapOffsetEl.value = String(s.noNapBedtimeOffsetMinutes ?? 30);` at 117-118 — delete only 117-118. Lines 199-212 inside the `onClose` handler's `raw` object literal: `eveningHour: Number(data.get('eveningHour') ?? 18),` at line 206 immediately followed by the orphan `noNapBedtimeOffsetMinutes: Number(data.get('noNapBedtimeOffsetMinutes') ?? 30),` at line 207 — delete only line 207. `intenseDayOffsetMinutes` at line 208 and every other `raw` object key is untouched.)
    .planning/phases/25-algorithm-c-settings-modal/25-REVIEW.md (CR-01 section, lines 59-123 — the exact current diff confirming these are the only three deletions required; the diff there is drawn against the CURRENT file state, more authoritative than 19-REVIEW.md's older line numbers).
    .planning/phases/19-split-bedtime-wake-anchored-nap/19-REVIEW.md (CR-01 section, lines 43-57 — the original Phase 19 finding and fix instruction this plan finally closes; confirms D-10 already removed the field from `DEFAULT_SETTINGS`/`RULES`, so this task is UI-only cleanup with zero schema/validator impact).
  </read_first>
  <action>
    In index.html, delete the 5-line `<label>Sleep target...` — no, delete exactly the `<label>` block that reads "No-nap bedtime offset (min)" and contains `<input type="number" id="noNapBedtimeOffsetMinutes" name="noNapBedtimeOffsetMinutes" min="0" max="120" step="5">` (index.html lines 302-306). Do not touch the "Evening hour" label immediately above it or the "Sleep target (minutes)" label immediately below it — after this edit those two blocks become directly adjacent siblings inside the same fieldset.

    In js/ui/settings-modal.js's `populateForm(snap)` function, delete the two lines `const noNapOffsetEl = form.elements.namedItem('noNapBedtimeOffsetMinutes');` and `if (noNapOffsetEl) noNapOffsetEl.value = String(s.noNapBedtimeOffsetMinutes ?? 30);` (lines 117-118). The `eveningHourEl` population two lines above and the `tifOptionsEl`/`classicOptionsEl`/`blendOptionsEl` lines immediately below are untouched.

    In the same file's `onClose` handler, delete the single line `noNapBedtimeOffsetMinutes: Number(data.get('noNapBedtimeOffsetMinutes') ?? 30),` (line 207) from the `raw` object literal. `eveningHour` (line 206) directly above and `intenseDayOffsetMinutes` (line 208) directly below are untouched.

    Do not touch js/lib/db-shape.js or js/lib/settings-validate.js — D-10 (Phase 19) already correctly removed this field from `DEFAULT_SETTINGS`/`RULES`/migration; re-adding it anywhere would reverse an already-shipped, already-tested decision (see kept prohibition in this plan's frontmatter).
  </action>
  <verify>
    <automated>node --test tests/unit/db-shape.test.js tests/unit/settings-validate.test.js</automated>
    <fails_when>non-zero exit, or the summary line reports any failing test in either file (both files assert noNapBedtimeOffsetMinutes's absence from DEFAULT_SETTINGS/RULES already — this task must not touch either file, so both suites must stay green unmodified)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n noNapBedtimeOffsetMinutes index.html js/ui/settings-modal.js` returns no matches (grep exits 1 — zero occurrences in either file).
    - `git diff index.html` shows exactly one contiguous block removed (the 5-line orphan `<label>` at the old lines 302-306) and no other line changed.
    - `git diff js/ui/settings-modal.js` shows exactly three lines removed (two in `populateForm`, one in the `onClose` `raw` object) and no other line changed.
    - `node --test tests/unit/db-shape.test.js tests/unit/settings-validate.test.js` passes with zero failures.
  </acceptance_criteria>
  <reversibility rating="reversible">A single-file-pair deletion of one dead HTML input and three lines of FormData glue code that were never wired to any schema field; reverting is a plain three-hunk diff with zero downstream contract impact, since DEFAULT_SETTINGS/RULES were unaffected both before and after.</reversibility>
  <done>Grepping the repo for noNapBedtimeOffsetMinutes returns matches ONLY in test files that assert its absence (db-shape.test.js, settings-validate.test.js, forecast.test.js) — zero matches in index.html or js/ui/settings-modal.js. The field can no longer be edited, so it can no longer be silently discarded.</done>
</task>

<task type="auto">
  <name>Task 2: E2E regression test proving removal and fieldset integrity</name>
  <files>tests/e2e/settings-modal.spec.js</files>
  <read_first>
    tests/e2e/settings-modal.spec.js (lines 118-148: the existing `'CFG-02..04, CFG-06..07: forecast-tuning + time/day fields round-trip Save -> reload'` test — this task's new test is inserted immediately after it, before the `'a11y: dialog#settings...'` test at line 150, and reuses the exact same open-modal/`button.settingsTrigger`/`#settings button[type="submit"]`/`page.reload()` idiom; lines 21-25: the `beforeEach` that clears localStorage before every test, so this new test starts from default settings.)
    js/ui/settings-modal.js (Task 1's now-cleaned `populateForm`/`onClose` — confirms `input[name="eveningHour"]` is still a valid, unchanged Playwright locator target after the fix, and that `input[name="noNapBedtimeOffsetMinutes"]` no longer exists anywhere in the rendered form.)
  </read_first>
  <action>
    Add a new test named `'CR-01 gap-closure: noNapBedtimeOffsetMinutes input no longer exists, and eveningHour still round-trips correctly (25-06)'` immediately after the existing `'CFG-02..04, CFG-06..07: forecast-tuning + time/day fields round-trip Save -> reload'` test (before the a11y test). Steps: open Settings (`button.settingsTrigger`); assert `page.locator('#settings input[name="noNapBedtimeOffsetMinutes"]')` has `toHaveCount(0)` — proves the input is gone from the DOM, not merely hidden. Fill `#settings input[name="eveningHour"]` with `'21'` (a value distinct from the default `18`). Click `#settings button[type="submit"]`. Reload the page (`page.reload()`) and reopen Settings. Assert `#settings input[name="eveningHour"]` has value `'21'` (the sibling field the removed block sat directly beneath still round-trips correctly, proving the edit was surgical). Assert `page.locator('#settings input[name="noNapBedtimeOffsetMinutes"]')` STILL has `toHaveCount(0)` after the full Save-> reload cycle (the field was not silently reintroduced by the migration path).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/settings-modal.spec.js</automated>
    <fails_when>non-zero exit, or the summary line reports any failed test in tests/e2e/settings-modal.spec.js</fails_when>
  </verify>
  <acceptance_criteria>
    - A test named containing `'CR-01 gap-closure'` exists in `tests/e2e/settings-modal.spec.js`, placed between the CFG-02..04/06..07 round-trip test and the a11y test.
    - The test asserts `toHaveCount(0)` for `input[name="noNapBedtimeOffsetMinutes"]` both before AND after the Save -> reload cycle.
    - The test asserts `eveningHour` round-trips to `'21'` after reload.
    - `npx playwright test tests/e2e/settings-modal.spec.js` passes with zero failures.
  </acceptance_criteria>
  <reversibility rating="reversible">Adds one new, independent Playwright test to an existing spec file; no production code path is touched by this task.</reversibility>
  <done>A deterministic E2E regression test proves the orphan field is gone from the DOM and that the fieldset around it still round-trips correctly, so CR-01 cannot silently recur.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Settings form input -> `validateSettings()` -> `settings.update()` | Existing, unmodified boundary. This plan REDUCES surface (removes one unvalidated pass-through field) rather than adding any new input path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-13 | Tampering | `js/ui/settings-modal.js` FormData collection | low | accept | Removing the dead `noNapBedtimeOffsetMinutes` read eliminates a class of "read-but-never-validated" field entirely; this task does not add any new `FormData` key, `.value` assignment, or DOM write, and touches only `<input>`/`<label>` markup and existing `.value =` assignment lines — never `innerHTML` (the repo-wide XSS guard in `js/ui/dom.js` is untouched and unaffected). |

No new user input, no new DOM write, no new storage key — this task is a pure subtraction of already-orphaned surface.

</threat_model>

## Artifacts this phase produces (Plan 25-06)

- Modified: `index.html` — orphan `<label>`/`<input id="noNapBedtimeOffsetMinutes">` block removed from the Forecast & Prediction fieldset. No new element.
- Modified: `js/ui/settings-modal.js` — `populateForm()`'s `noNapOffsetEl` population lines removed; the `onClose` handler's `raw` object literal no longer has a `noNapBedtimeOffsetMinutes` key. No new exported symbol; no change to any existing exported symbol's signature.
- New: one Playwright regression test in `tests/e2e/settings-modal.spec.js`.

<verification>
Run `node --test tests/unit/db-shape.test.js tests/unit/settings-validate.test.js` and `npx playwright test tests/e2e/settings-modal.spec.js` — both pass. For full-suite confidence, also run `npm run test:unit` (expect the same 959/959 baseline from 25-VERIFICATION.md — no unit-level surface changed by this plan) and the complete `npx playwright test` (expect the prior 149 plus this plan's 1 new test = 150 passing).
</verification>

<success_criteria>
- `noNapBedtimeOffsetMinutes` no longer appears in index.html or js/ui/settings-modal.js (grep confirms zero hits outside test files that assert its absence).
- The Forecast & Prediction fieldset's remaining fields (eveningHour, targetSleepMinutes, and every other sibling field) round-trip through Save -> reload exactly as before.
- `js/lib/db-shape.js` and `js/lib/settings-validate.js` are untouched — D-10 (Phase 19) is honored, not reversed.
- 25-VERIFICATION.md's gap 1 (CR-01) is closed.
</success_criteria>

<output>
Create `.planning/phases/25-algorithm-c-settings-modal/25-06-SUMMARY.md` when done.
</output>
