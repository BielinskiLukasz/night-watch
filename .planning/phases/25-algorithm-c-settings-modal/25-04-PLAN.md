---
phase: 25-algorithm-c-settings-modal
plan: 04
type: execute
wave: 2
depends_on: ["25-01", "25-03"]
files_modified:
  - index.html
  - js/ui/settings-modal.js
  - js/ui/today-screen.js
autonomous: true
requirements: [UI-12]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Settings modal's Forecast & Prediction fieldset offers a three-option select (Classic/TIF/Algorithm C) with value 'blend' for the new option, positioned at the top of the fieldset — unchanged position from the existing two-option select (D-14, UI-12)."
    - "Exactly one of #classicOptions/#tifOptions/#blendOptions is visible at any time, driven by forecastAlgorithm's current value — verified both at populateForm() time (modal open) and on every 'change' event (D-14)."
    - "#blendOptions contains three inputs (blendWindowDays, blendTrimPct, blendShrinkage) that populate from the settings snapshot on open and round-trip through Save via validateSettings' mode:'save' path exactly like every other numeric setting — no bespoke serialization."
    - "today-screen.js's forecastAlgorithm dispatch becomes a three-way check (classic/tif/blend) that calls blendForecast(forecastDaysOldestFirst, snap, activityLog, isNoNapDay) with the SAME argument shape already passed to tifForecast — no new context object is introduced (PRED-13)."
    - "Predictions returned by blendForecast render through the EXISTING generic 'Classic rendering path' in renderForecastSection (js/ui/today-screen.js's precisionScore/isLowConfidence != null branch), not the TIF-card path — because blendForecast's predictions never carry precisionScore/isLowConfidence/sourceWindows fields, zero changes to renderForecastSection/renderPredictionCard/renderTifNormalCard/renderTifLowConfidenceCard are needed or made by this plan."
  artifacts:
    - path: "index.html"
      provides: "Three-option forecastAlgorithm <select>, new #blendOptions fieldset sub-group with blendWindowDays/blendTrimPct/blendShrinkage inputs"
    - path: "js/ui/settings-modal.js"
      provides: "Three-way populateForm()/_forecastAlgorithmChangeHandler show-hide logic; blend field population + FormData extraction in onClose"
    - path: "js/ui/today-screen.js"
      provides: "blendForecast import + three-way forecastAlgorithm dispatch ternary"
  key_links:
    - from: "index.html (#blendOptions inputs' name attributes)"
      to: "js/ui/settings-modal.js (populateForm / onClose raw object)"
      via: "form.elements.namedItem(fieldName) / new FormData(form).get(fieldName)"
      pattern: "blendWindowDays.*blendTrimPct.*blendShrinkage"
    - from: "js/ui/today-screen.js (forecastAlgorithm dispatch)"
      to: "js/lib/forecast-blend.js (blendForecast)"
      via: "import statement + ternary call"
      pattern: "import.*blendForecast.*from.*lib/forecast-blend"
---

<objective>
Add the three-option (Classic/TIF/Algorithm C) `forecastAlgorithm` selector to the Settings modal with
a new `#blendOptions` fieldset sub-group (D-14, UI-12), and wire `today-screen.js`'s prediction dispatch
to call `blendForecast()` when `forecastAlgorithm === 'blend'`.

Purpose: this is the phase's UI expansion — the user-visible surface that makes Algorithm C selectable
and configurable. It consumes, but does not modify, the pure-logic work Plans 25-01/25-02 already built:
`renderForecastSection`'s existing generic dispatch (keyed on the PRESENCE of TIF-specific fields, not
on `forecastAlgorithm`'s value) already renders Algorithm C's plain `{central,min,max}` shape correctly
with zero changes — confirmed by reading the current dispatch condition before writing this plan.

Output: Updated `index.html` (static markup, following the existing `#tifOptions`/`#classicOptions`
convention), `js/ui/settings-modal.js`, and `js/ui/today-screen.js`.
</objective>

<assumption_delta_decision>
Decision: add-alongside. `forecastAlgorithm` moves from a two-way choice (Classic/TIF) to a three-way
choice (Classic/TIF/Algorithm C), but this is a THIRD independently-addressable option added beside the
existing two, not a promotion that re-architects the setting's shape — it is still a single-value enum
consumed by one dispatch ternary, and each algorithm's settings remain in their own untouched sub-group
(`#classicOptions`/`#tifOptions`/`#blendOptions`). Rationale: user-decided per CONTEXT.md D-14 — "each
algorithm's settings remain in independent sub-groups, no promotion of a unified settings shape." No
existing Classic or TIF field, fieldset, or validation rule changes shape as a result of this plan.
</assumption_delta_decision>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-algorithm-c-settings-modal/25-CONTEXT.md
@index.html
@js/ui/settings-modal.js
@js/ui/today-screen.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: #blendOptions fieldset markup + three-option select in index.html</name>
  <files>index.html</files>
  <read_first>
    index.html (lines 261-324: the full "Group 3: Forecast &amp; Prediction" fieldset — lines 264-270 the `<select name="forecastAlgorithm">` with its two `<option>`s; lines 271-292 `<div id="classicOptions">` with NO `hidden` attribute in the static markup, relying on JS to hide it once `forecastAlgorithm !== 'classic'`; lines 312-323 `<div id="tifOptions" hidden>` — the exact sibling pattern `#blendOptions` follows).
  </read_first>
  <reversibility rating="costly">D-14: the three-way selector + three independently-addressable settings sub-groups is the chosen grouping scheme (touches settings-validate.js, db-shape.js, and this markup) — CONTEXT.md already records the human choosing this exact option, so per the reversibility taxonomy's "gating a decision already made" anti-pattern, this is flagged for the record, not gated behind a checkpoint.</reversibility>
  <action>
    In index.html's `<select name="forecastAlgorithm">` (inside the "Forecast &amp; Prediction" fieldset), add a third `<option value="blend">Algorithm C</option>` after the existing `<option value="tif">TIF</option>` line — "Algorithm C" is the clearest, most literal label (Claude's Discretion per CONTEXT.md; it matches the identical term used throughout CONTEXT.md/ROADMAP.md, avoiding an invented marketing name).

    Immediately after the existing `<div id="tifOptions" hidden>...</div>` block (which closes right before the fieldset's closing `</fieldset>` tag), add a new sibling `<div id="blendOptions" hidden>` containing three `<label>` blocks, following the exact `<label>text<input .../></label>` structure already used inside `#tifOptions`: a "Blend history window (days)" label wrapping `<input type="number" name="blendWindowDays" min="14" max="180" step="1">`; a "Blend trim percentage (0-40)" label wrapping `<input type="number" name="blendTrimPct" min="0" max="40" step="1">`; and a "Blend shrinkage (0.0-1.0)" label wrapping `<input type="number" name="blendShrinkage" min="0" max="1" step="0.05">` (the `step="0.05"` matches D-13's UI-slider-step decision even though this is a plain number input, not a `<input type="range">`, since CONTEXT.md's Claude's Discretion note only decided the settings-modal STRUCTURE, not the input widget type — a numeric spinner is consistent with every other numeric setting in this fieldset, e.g. `maxDelta`/`windowDays`/`trimPct`, none of which use `<input type="range">`).
  </action>
  <verify>
    <automated>node --test tests/integration/security-smoke.test.js</automated>
    <fails_when>non-zero exit — this task adds no JS logic, but confirms the markup change does not break the repo-wide innerHTML/script-src smoke checks</fails_when>
  </verify>
  <acceptance_criteria>
    - `<option value="blend">Algorithm C</option>` exists inside the `forecastAlgorithm` select.
    - `#blendOptions` exists, carries the `hidden` attribute in the static markup, and contains exactly three named inputs: `blendWindowDays`, `blendTrimPct`, `blendShrinkage`.
    - None of the three new inputs duplicates a `name` already used elsewhere in the `#settings` form (grep confirms `blendWindowDays`/`blendTrimPct`/`blendShrinkage` each appear exactly once as a `name=` attribute in index.html).
  </acceptance_criteria>
  <done>The three-way algorithm selector and the Algorithm C settings sub-group both exist in static markup, ready for settings-modal.js to populate and toggle.</done>
</task>

<task type="auto">
  <name>Task 2: Three-way show/hide + field population/extraction in settings-modal.js</name>
  <files>js/ui/settings-modal.js</files>
  <read_first>
    js/ui/settings-modal.js (lines 97-98: `forecastAlgorithmEl.value = s.forecastAlgorithm ?? 'classic';`; lines 113-116: the current two-way `tifOptionsEl.hidden`/`classicOptionsEl.hidden` population-time logic — becomes three-way; lines 143-157: `_forecastAlgorithmChangeHandler` — the current two-way live-toggle logic — becomes three-way; line 188: `forecastAlgorithm: String(data.get('forecastAlgorithm') ?? 'classic'),` inside the `raw` object; lines 189-191: `trimPct`/`precisionTarget`/`tifRollingDays` extraction — the exact `Number(data.get(field) ?? default)` idiom the 3 new fields follow).
  </read_first>
  <action>
    In `populateForm(s)`, add three lines after the existing `tifRollingDaysEl` population block, mirroring its exact `form.elements.namedItem('field')` + null-guard + `String(s.field ?? default)` shape: populate `blendWindowDays` (default `90`), `blendTrimPct` (default `25`), `blendShrinkage` (default `0.3`).

    Replace the two-way hidden-toggle block (`tifOptionsEl.hidden = (s.forecastAlgorithm !== 'tif'); classicOptionsEl.hidden = (s.forecastAlgorithm === 'tif');`) with a three-way version: `const blendOptionsEl = document.getElementById('blendOptions'); if (classicOptionsEl) classicOptionsEl.hidden = (s.forecastAlgorithm !== 'classic'); if (tifOptionsEl) tifOptionsEl.hidden = (s.forecastAlgorithm !== 'tif'); if (blendOptionsEl) blendOptionsEl.hidden = (s.forecastAlgorithm !== 'blend');` — each fieldset's visibility now depends only on whether it matches the CURRENT value, not on excluding one specific other value, so adding a future 4th algorithm would require zero changes to the other three branches' conditions.

    Replace `_forecastAlgorithmChangeHandler`'s body with the same three-way pattern (`const algo = forecastAlgorithmEl.value; ... classicEl.hidden = (algo !== 'classic'); tifOptionsEl.hidden = (algo !== 'tif'); blendEl.hidden = (algo !== 'blend');`), reading `#blendOptions` via `document.getElementById('blendOptions')` alongside the existing `#tifOptions`/`#classicOptions` lookups already in that function.

    In the `onClose` handler's `raw` object, add three lines immediately after the existing `tifRollingDays: Number(data.get('tifRollingDays') ?? 7),` line: `blendWindowDays: Number(data.get('blendWindowDays') ?? 90),`, `blendTrimPct: Number(data.get('blendTrimPct') ?? 25),`, `blendShrinkage: Number(data.get('blendShrinkage') ?? 0.3),` — identical coercion idiom to the 3 sibling TIF fields immediately above them; `validateSettings(raw, {mode:'save'})` (already called unconditionally afterward) performs all range enforcement, so no new validation call is added here.
  </action>
  <verify>
    <automated>node --test tests/integration/security-smoke.test.js</automated>
    <fails_when>non-zero exit, or a new `.innerHTML =` violation is reported</fails_when>
  </verify>
  <acceptance_criteria>
    - `populateForm` sets `.value` on all three new inputs and toggles all three fieldset divs based on `s.forecastAlgorithm`'s exact value (grep confirms `blendOptionsEl` is read and its `.hidden` assigned).
    - `_forecastAlgorithmChangeHandler`'s body references `blendOptionsEl`/`blendEl` (grep confirms the three-way pattern replaced the two-way one — no lingering `=== 'tif'`-only branch remains for `classicOptionsEl`).
    - The `raw` object in `onClose` includes `blendWindowDays`, `blendTrimPct`, `blendShrinkage` keys.
    - Grep js/ui/settings-modal.js for `.innerHTML` shows zero new occurrences.
  </acceptance_criteria>
  <done>Opening Settings with any of the three forecastAlgorithm values shows exactly the matching fieldset; switching the select live-toggles all three; Save round-trips all three new fields through validateSettings.</done>
</task>

<task type="auto">
  <name>Task 3: blendForecast dispatch wiring in today-screen.js</name>
  <files>js/ui/today-screen.js</files>
  <read_first>
    js/ui/today-screen.js (line 49: `import { tifForecast } from '../lib/forecast-tif.js';` — the import this task's new line sits beside; lines 1180-1182: the current two-way ternary `snap.forecastAlgorithm === 'tif' ? tifForecast(...) : forecast(...)`; lines 693-703 `renderForecastSection`'s dispatch — `if (pred.precisionScore != null || pred.isLowConfidence != null) { /* TIF path */ } else { /* Classic path */ }` — read this to CONFIRM (do not modify it) that Algorithm C predictions, which never carry `precisionScore`/`isLowConfidence`, automatically fall into the existing Classic rendering path with zero changes needed here).
  </read_first>
  <action>
    Add `import { blendForecast } from '../lib/forecast-blend.js';` immediately after the existing `import { tifForecast } from '../lib/forecast-tif.js';` line.

    Replace the two-way ternary at lines 1180-1182 with a three-way nested ternary, preserving the exact same argument list already passed to `tifForecast` (no new context object): `snap.forecastAlgorithm === 'tif' ? tifForecast(forecastDaysOldestFirst, snap, activityLog, isNoNapDay) : snap.forecastAlgorithm === 'blend' ? blendForecast(forecastDaysOldestFirst, snap, activityLog, isNoNapDay) : forecast(forecastDaysOldestFirst, snap, forecastContext);` — matching the exact structure documented in 25-PATTERNS.md's "Algorithm dispatch pattern".

    Make NO other change to this file: `renderForecastSection`, `renderPredictionCard`, `renderTifNormalCard`, `renderTifLowConfidenceCard`, the hero-selection logic, and the "Later today" section all already work correctly for Algorithm C's plain `{central,min,max}` prediction shape without modification (confirmed by the `read_first` inspection above) — do not add an `isBlend`/`algorithm === 'blend'` branch anywhere else in this file.
  </action>
  <verify>
    <automated>npm run test:unit</automated>
    <fails_when>non-zero exit, or the node:test end-of-run summary line reports a fail count greater than 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `js/ui/today-screen.js` imports `blendForecast` from `'../lib/forecast-blend.js'`.
    - The three-way ternary calls `blendForecast` with the exact same 4 positional arguments already passed to `tifForecast` (grep confirms `blendForecast(forecastDaysOldestFirst, snap, activityLog, isNoNapDay)`).
    - Grep `js/ui/today-screen.js` for the literal string `'blend'` shows exactly one occurrence (the new ternary branch) — no other file in this task's scope needed an algorithm-specific branch.
    - `npm run test:unit` passes with 0 failures.
  </acceptance_criteria>
  <done>Selecting Algorithm C in Settings and returning to the Today screen renders real Algorithm C predictions through the existing Classic-style prediction cards, with no regression to Classic or TIF rendering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Settings modal form -> forecastAlgorithm dispatch | The `forecastAlgorithm` select's value (`'classic'|'tif'|'blend'`, enum-validated by Plan 25-03's `RULES`) selects which pure function `today-screen.js` calls. An already-validated enum value cannot select an unintended code path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-06 | Tampering (XSS) | `#blendOptions` input population | low | accept | All three new inputs are populated via `.value = String(...)` (never `innerHTML`), identical to every other numeric setting in this modal — `tests/integration/security-smoke.test.js`'s repo-wide `.innerHTML` scan enforces this. |
| T-25-07 | Elevation of Privilege | Three-way `forecastAlgorithm` ternary | low | accept | The ternary's `'blend'` branch is reachable only via the already-`RULES`-validated `forecastAlgorithm` value; there is no way to invoke `blendForecast` with an unvalidated settings object through this dispatch path. |

</threat_model>

## Artifacts this phase produces (Plan 25-04)

- `<option value="blend">Algorithm C</option>` in the `forecastAlgorithm` select (index.html)
- `#blendOptions` fieldset with `blendWindowDays`/`blendTrimPct`/`blendShrinkage` inputs (index.html)
- Three-way `populateForm`/`_forecastAlgorithmChangeHandler` show/hide logic (js/ui/settings-modal.js)
- `blendWindowDays`/`blendTrimPct`/`blendShrinkage` in the `onClose` `raw` extraction object (js/ui/settings-modal.js)
- `blendForecast` import + three-way dispatch ternary (js/ui/today-screen.js)

<verification>
Run `node --test tests/integration/security-smoke.test.js` and `npm run test:unit` — both pass. Manual/E2E confirmation of the three-way visibility toggle and real-prediction rendering is Plan 25-05's scope.
</verification>

<success_criteria>
- The Settings modal is reachable end-to-end with a working three-option selector and a correctly-populated/saved Algorithm C settings sub-group.
- Today screen renders Algorithm C predictions through the unmodified Classic rendering path.
- No regression to Classic or TIF's existing dispatch, rendering, or persistence behavior.
</success_criteria>

<output>
Create `.planning/phases/25-algorithm-c-settings-modal/25-04-SUMMARY.md` when done.
</output>
