---
phase: 25-algorithm-c-settings-modal
plan: 08
type: tdd
wave: 1
depends_on: ["25-07"]
files_modified:
  - js/lib/forecast-blend.js
  - tests/unit/forecast-blend.test.js
autonomous: true
requirements: [PRED-16, PRED-17]
gap_closure: true

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "stabilityCheck(intervals, central, shrinkage) performs genuine circular/modular interval comparison: it self-unwraps any input interval whose min > max (a band that itself crosses the 1440-minute boundary because it was built from independently-wrapToDay()'d fields) and aligns every interval, plus the raw central estimate, onto a shared un-wrapped number line before computing intersection/union — so for ANY combination of 2+ input models where zero, one, or multiple have min > max, the returned central always lies within the returned [min, max] band (reading the output band wrap-aware when it itself legitimately crosses midnight). Closes 25-VERIFICATION.md's remaining gap / the new CR-01 defect from the fresh 25-REVIEW.md."
    - "No input model's min/max signal is ever excluded from stabilityCheck's overlap (interStart/interEnd) or union (min/max) reductions — every interval in the input array participates after alignment, so a wrapped model's true position is never silently dropped from the combined band, which was the specific data-loss symptom 25-REVIEW.md's CR-01 called out (modelA's signal winning neither the Math.min nor Math.max reduction)."
    - "When one model is inverted (crosses midnight) and a sibling model touches it at the exact wrap boundary (an inclusive, single-point overlap), stabilityCheck correctly detects the touch and shrinks central toward that single point — it does not falsely classify the pair as non-overlapping and union them (PRED-16 adjacency edge-probe row, resolved for the circular case)."
    - "stabilityCheck's result for a fixed underlying set of 2+ models is independent of the order those models appear in the input array — reordering only changes which model anchors the internal alignment reference, never the final wrapped output (PRED-16 ordering edge-probe row, resolved for the circular case)."
    - "The circular-interval fix generalizes unchanged from 2 models (wake's A1+A2, napStart's/napEnd's 2-model blends) to 3+ models (bedtime's 3-model blend) — the same Helly's-theorem equivalence the file's existing comment already documents for the non-circular case holds identically after self-unwrap-and-align, with zero branching by interval count."
  artifacts:
    - path: "js/lib/forecast-blend.js"
      provides: "stabilityCheck() hardened with circular/modular interval comparison via two new private helpers (selfUnwrapInterval, alignNearReference); combineModels(), wrapToDay(), trimmedBand(), blendForecast(), and every model-construction call site (wake A1/A2, bedtime Models 1/2/3, napStart/napEnd Models 1/2) are byte-identical to before this plan."
    - path: "tests/unit/forecast-blend.test.js"
      provides: "New 'circular/wrapped intervals — CR-01 gap-closure (Plan 25-08)' nested describe block inside the existing stabilityCheck describe block: 5 new test cases (exact 25-REVIEW.md repro, 2-model true overlap, 3-model true overlap, touching/adjacency at the wrap boundary, order-independence) that fail before the fix and pass after, using stabilityCheck() directly (no day-record fixtures needed)."
  key_links:
    - "stabilityCheck()'s self-unwrap + align-to-reference logic (js/lib/forecast-blend.js) <-> wrapToDay() (same file, UNCHANGED, reused only to re-wrap the final min/max/central into [0,1440) before returning) <-> combineModels() and wake's own direct stabilityCheck() call (same file, UNCHANGED call sites) — every one of the 6 model-combination call sites in the file (wake A1+A2, bedtime's 3 models, napStart's 2 models, napEnd's 2 models) is corrected uniformly by fixing this one shared function, with zero changes to any call site or to how any individual model is built."
  prohibitions:
    - statement: "MUST NOT change combineModels()'s own logic (its 0/1/N-model dispatch, its rawCentral averaging, or its short-circuit returns) — only stabilityCheck()'s internal comparison algorithm changes; combineModels() keeps calling it with the exact same (intervals, central, shrinkage) signature and trusts its corrected output."
      category: safety
      verification: judgment
    - statement: "MUST NOT modify any model-construction call site (wake's A1/A2 at lines ~324-360, bedtime's Models 1/2/3 at lines ~394-446, napStart's Models 1/2 at lines ~236-260, napEnd's Models 1/2 at lines ~277-309) — every one of these already correctly calls wrapToDay() (Plan 25-01/25-02/25-07) on ITS OWN output; this plan's fix lives entirely inside how stabilityCheck COMPARES already-built models, not in how any model is built."
      category: correctness
      verification: judgment
    - statement: "MUST NOT silently drop a model's min/max signal from the union/intersection computation — every interval in the input array must appear in both the self-unwrap step and the final interStart/interEnd/union reductions after alignment; a fix that filters out or short-circuits away from an inverted model instead of correctly aligning it does not satisfy this plan."
      category: correctness
      verification: judgment
    - statement: "MUST NOT address WR-02 (missing E2E round-trip test for the 3 Algorithm C tuning inputs), IN-01 (leftover console.log in the CSV import handler), or IN-02 (stale noNapBedtimeOffsetMinutes literal in two test fixtures) in this plan — all three are separate, Info/Warning-level 25-REVIEW.md findings that are NOT in 25-VERIFICATION.md's gaps_remaining and are explicitly out of scope for this gap-closure round; reviewed and deferred, not silently dropped."
      category: scope
      verification: judgment
---

<objective>
Close the one remaining item in 25-VERIFICATION.md's `gaps_remaining` (= 25-REVIEW.md's new CR-01 finding, distinct from the already-closed orphan-field CR-01): harden `stabilityCheck()` in `js/lib/forecast-blend.js` with genuine circular/modular interval comparison so that combining a model whose independently-`wrapToDay()`'d min/max order inverted (min > max, because its raw pre-wrap band crossed the 1440-minute boundary) with a sibling model no longer produces a reported `central` that falls outside the reported `[min, max]` band, and no longer silently drops the inverted model's signal from the overlap/union computation.

Purpose: `wrapToDay()` is correctly applied independently to each model's own min/median/max (Plans 25-01/25-02/25-07) — safe for a single model in isolation — but `stabilityCheck()` (used by wake's A1+A2 blend, bedtime's 3-model blend, and napStart's/napEnd's 2-model blends, i.e. every Algorithm C event) still compares these already-wrapped bands via plain numeric `Math.max`/`Math.min`, which assumes every input interval lives in the same conventional `min <= max` reference frame. When exactly one combined model's raw band straddled 1440 asymmetrically (so its own wrapped `min > max`) while a sibling model never crossed the boundary, the two are no longer numerically comparable — `stabilityCheck()` has no logic to detect or correct this, so it can report a mathematically inconsistent prediction (central outside its own uncertainty band) and silently discard the inverted model's true position.

Output: `js/lib/forecast-blend.js` with `stabilityCheck()`'s comparison algorithm hardened (self-unwrap + align-to-a-shared-reference + re-wrap-on-return), zero changes to `combineModels()` or any of the 6 model-construction call sites, plus new RED→GREEN unit test coverage in `tests/unit/forecast-blend.test.js` proving the exact 25-REVIEW.md repro, a genuine-overlap case, a 3-model case, a touching/adjacency case, and order-independence.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-algorithm-c-settings-modal/25-VERIFICATION.md
@.planning/phases/25-algorithm-c-settings-modal/25-REVIEW.md
@js/lib/forecast-blend.js
@tests/unit/forecast-blend.test.js
</context>

## Source Coverage Audit

This is a gap-closure plan (`/gsd-plan-phase 25 --gaps`). The source set is 25-VERIFICATION.md's `gaps_remaining` entry (the new circular-interval CR-01), not a fresh requirements/research pass — PRED-13..17/UI-12 were already marked VERIFIED (5/6) by Plans 25-01..25-05/25-07 and are not reopened here except where this fix's own correctness needs re-asserting.

| SOURCE | ID | Item | Plan | Status | Notes |
|--------|-----|------|------|--------|-------|
| GAP | `gaps_remaining` entry (= 25-REVIEW.md's new CR-01, circular-interval defect) | `stabilityCheck()` uses plain numeric `Math.max`/`Math.min` without circular/modular-interval awareness, so a model with independently-wrapped `min > max` can produce a `central` outside the reported band and have its signal silently dropped | 25-08 | COVERED | The one task below fixes `stabilityCheck()` itself; zero changes to `combineModels()` or any of the 6 call sites that build models. |
| REQ | PRED-16 | Interval stability check (overlap/union/shrinkage) for all events | 25-01 (original `stabilityCheck()`), 25-08 (this plan — hardens it for circular/wrapped inputs) | ALREADY VERIFIED, NOW HARDENED | The overlap/union/shrinkage algorithm's non-circular behavior (25-01's own tests) is preserved exactly (re-verified by hand for all 4 pre-existing `stabilityCheck()` tests); this plan only adds correctness for the wrapped-input case those tests never exercised. |
| REQ | PRED-17 | Algorithm C covers all 4 events (wake, bedtime, nap-start, nap-end) | 25-02 (already VERIFIED, unchanged) | NOT REOPENED | `stabilityCheck()` is the single shared function all 4 events route through (wake calls it directly; bedtime/napStart/napEnd via `combineModels()`) — fixing it once fixes all 4 uniformly without touching any event-specific band-construction code. |
| Edge probe (specless-probe-fallback) | PRED-16 adjacency | "When two things are exactly equal or just touch, do they merge, collide, or separate?" | 25-08 | RESOLVED — explicit | New "touching (inclusive overlap) at the wrap boundary" test case (Task 1, case 4). Folded into `must_haves.truths` above. |
| Edge probe | PRED-16 empty | "What is the result for empty, single-element, or null input?" | 25-08 | REVIEWED — structurally unreachable, documented flagged assumption (not fabricated) | `stabilityCheck()` is only ever invoked by `combineModels()` with 2+ models — the 0- and 1-model cases are short-circuited by `combineModels()` BEFORE it ever calls `stabilityCheck()` (unchanged by this plan), and `stabilityCheck()`'s own JSDoc has always documented "2 or more intervals" as its contract. Writing a test that calls `stabilityCheck([], ...)` or `stabilityCheck([oneModel], ...)` would exercise a contract violation no real code path ever reaches, not a meaningful behavior — deliberately not added. |
| Edge probe | PRED-16 ordering | "When elements compare equal, is output order specified and stable?" | 25-08 | RESOLVED — explicit | New "reordering the intervals array produces the same result" test case (Task 1, case 5). Folded into `must_haves.truths` above. |
| Edge probe | PRED-13/14/15/17/UI-12 (all other rows) | — | — | REVIEWED — OUT OF SCOPE, not reopened | These rows concern wake/bedtime shape parity (already VERIFIED), the UI selector (already VERIFIED), or generic band-construction properties of `trimmedBand()`/model-building call sites that Plan 25-01's own prior flagged-assumptions already addressed — none of them are touched by a fix confined to `stabilityCheck()`'s internal comparison algorithm. Recorded here rather than silently dropped. |
| Prohibition recall (specless-probe-fallback §B, "what could this gap silently become") | — | Adversarial pass over the defect | 25-08 | RESOLVED — 4 prohibitions kept | (1) Scope creep into `combineModels()`'s own dispatch/averaging logic. (2) Scope creep into any of the 6 model-construction call sites. (3) A "fix" that filters/discards an inverted model instead of correctly aligning it (re-introducing the exact silent-drop symptom under a different mechanism). (4) Scope creep into WR-02/IN-01/IN-02, separate 25-REVIEW.md findings not in `gaps_remaining`. All four captured in `must_haves.prohibitions` above. |

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Harden stabilityCheck() with circular/modular interval comparison (CR-01 gap-closure)</name>
  <files>js/lib/forecast-blend.js, tests/unit/forecast-blend.test.js</files>
  <read_first>
    js/lib/forecast-blend.js (lines 90-136: the current `stabilityCheck(intervals, central, shrinkage)` — its JSDoc documents "2 or more intervals" and the Helly's-theorem N-way equivalence comment; its body computes `interStart = Math.max(...intervals.map(iv => iv.min))` and `interEnd = Math.min(...intervals.map(iv => iv.max))` with no circular awareness. Lines 138-152: `wrapToDay(m)` — already exported (Plan 25-07), `((m % DAY) + DAY) % DAY`, with `const DAY = 24 * 60;` declared immediately above it. Lines 154-184: `combineModels(models, shrinkage)` — the 0/1/N dispatcher that calls `stabilityCheck()` for 2+ models; this plan makes ZERO changes here, confirm by re-reading its exact current body before and after your edit. Lines 340-360 (wake's A2), 400-415 (bedtime Model 2), 425-446 (bedtime Model 3), 233-272 (napStart), 274-321 (napEnd): the 6 call sites whose already-`wrapToDay()`'d model objects feed into `stabilityCheck()`/`combineModels()` — confirm none of these change.)
    tests/unit/forecast-blend.test.js (lines 131-151: the existing `describe('stabilityCheck(intervals, central, shrinkage)', () => { ... 4 existing it() cases ... })` block — this task's new nested describe is added as a 5th child, immediately after the 4th existing `it('overlapping, central outside (above) → shrinks toward center', ...)` case and its closing `});`, before the outer describe's own closing `});`. Lines 14-18: existing imports — `trimmedBand, stabilityCheck, blendForecast, wrapToDay` from `../../js/lib/forecast-blend.js` and `timeToMinutes, minutesToTime` from `../../js/lib/forecast.js` — no new imports are needed for this task's tests, which call `stabilityCheck()` directly with inline `{min, max}` object literals exactly like the 4 pre-existing cases do.)
    .planning/phases/25-algorithm-c-settings-modal/25-VERIFICATION.md (the `gaps_remaining` frontmatter entry — the exact defect description, artifact line ranges, and "missing" list this task implements verbatim, including its explicit ask for "Numeric (not wrap-tolerant) assertion that min(result.&lt;event&gt;) &lt;= central(result.&lt;event&gt;) &lt;= max(result.&lt;event&gt;)").
    .planning/phases/25-algorithm-c-settings-modal/25-REVIEW.md (the new CR-01 section — the exact repro this task's first test case reproduces verbatim, and the "Fix" paragraph's suggested approach: detect `model.min > model.max`, normalize onto a shared un-wrapped frame, wrap the final result back into [0, 1440)).
  </read_first>
  <behavior>
    All 5 new cases call `stabilityCheck()` directly (no day-record fixtures — `stabilityCheck` is exported specifically for isolated testing and is the single function shared by all 4 Algorithm C events, so proving it correct here proves every event's combine step correct without re-deriving `trimmedBand()`/`combineModels()` pipeline arithmetic through synthetic day records, which 25-07's own SUMMARY documented as fragile/error-prone to hand-derive). Each case asserts the FULL returned object via `assert.deepStrictEqual`, pinning exact expected numbers so a regression to the pre-fix algorithm is caught even when the pre-fix `central` happens to still fall inside its own (wrongly-computed) band.

    Case 1 — exact 25-REVIEW.md CR-01 repro, no true overlap: `stabilityCheck([{min:1400,max:60}, {min:100,max:160}], 70, 0.3)` must return exactly `{min:1400, max:160, central:70}`. (Pre-fix returns `{min:100, max:160, central:70}` — central 70 is below min 100, the exact documented defect. Post-fix, the second model is correctly recognized as living numerically "after" the first model's wrap — self-unwrapping the first model to an internal span of [1400,1500] and aligning the second model to [1540,1600] — the two do not truly overlap once aligned, so the union envelope [1400,160] is reported, legitimately wrapping past midnight; central 70 lies inside the wrap-aware reading of that band, i.e. in [0,160].)

    Case 2 — one inverted model, one normal model, TRUE overlap: `stabilityCheck([{min:1420,max:40}, {min:10,max:30}], 20, 0.3)` must return exactly `{min:10, max:30, central:20}`. (Pre-fix, plain `Math.max(1420,10)=1420 > Math.min(40,30)=30` wrongly concludes no overlap and unions to `{min:10, max:40, central:20}` — central still happens to fall inside that wrong, too-wide band, so only the reported band's width — not a central-outside-band symptom — reveals this specific pre-fix defect, which is exactly why this deepStrictEqual pins the FULL object rather than just checking central containment. Post-fix, both models are aligned onto the same frame — self-unwrapping the first to [1420,1480] — correctly detected as overlapping in [10,30], matching the second model exactly since it is fully contained within the first's true span.)

    Case 3 — 3 intervals (bedtime-style group), one inverted, TRUE overlap: `stabilityCheck([{min:1420,max:40}, {min:10,max:30}, {min:15,max:25}], 20, 0.3)` must return exactly `{min:15, max:25, central:20}`. (Pre-fix unions to `{min:10, max:40, central:20}` for the same reason as case 2. Post-fix, all three are aligned and correctly found to overlap; the reported band exactly matches the third, narrowest model — proves the fix generalizes from 2 to 3+ models with zero branching by count, exactly as the file's existing Helly's-theorem comment claims.)

    Case 4 — one inverted model, one normal model, TOUCHING (inclusive overlap) at the wrap boundary: `stabilityCheck([{min:1430,max:20}, {min:20,max:50}], 1450, 0.3)` must return exactly `{min:20, max:20, central:13}`. (Pre-fix, `Math.max(1430,20)=1430 > Math.min(20,50)=20` wrongly concludes no overlap and unions to `{min:20, max:50, central:1450}` — central 1450 is grossly outside [20,50], the starkest instance of the documented "prediction outside its own band" symptom. Post-fix, both models are aligned — self-unwrapping the first to [1430,1460] and aligning the second to [1460,1490] — correctly found to touch at the single point 1460 (i.e. `min===max` in the aligned frame), and central shrinks 70/30 toward that point: `0.7 * 1450 + 0.3 * 1460 = 1453`, which re-wraps to 13 in the [0,1440) frame.)

    Case 5 — order-independence: `stabilityCheck([{min:100,max:160}, {min:1400,max:60}], 70, 0.3)` (case 1's two models, swapped) must return the identical object as case 1: `{min:1400, max:160, central:70}`. (Proves the internal choice of which interval anchors the alignment reference never changes the final wrapped output for a fixed underlying set of models — resolves the PRED-16 "ordering" edge-probe row.)
  </behavior>
  <action>
    RED — add all 5 cases above as a new nested `describe('circular/wrapped intervals — CR-01 gap-closure (Plan 25-08)', () => { ... })` block inside the existing `describe('stabilityCheck(intervals, central, shrinkage)', ...)` block in `tests/unit/forecast-blend.test.js`, placed immediately after the 4th existing `it(...)` case, before that outer describe's closing `});`. Each `it(...)` should include a one-line comment (as described in Behavior above) explaining the pre-fix vs post-fix expected values so a future reader understands why the exact literal is what it is. No new imports are needed. Run `node --test tests/unit/forecast-blend.test.js` and confirm all 5 new cases fail (and only these 5 — every pre-existing test must still pass, since the pre-fix `stabilityCheck()` is unchanged at this point). Commit `test(25-08): add failing circular-interval stability tests`.

    GREEN — in `js/lib/forecast-blend.js`, make exactly these changes and no others:

    1. Move the `const DAY = 24 * 60;` declaration from its current position (immediately before the `wrapToDay` export, in the comment block that currently reads "wrapToDay — exported for direct unit testing...") to a new position immediately before the `stabilityCheck` section (i.e., right after `trimmedBand`'s closing brace, before the `// --- stabilityCheck` comment header). Do not declare `DAY` twice — remove the old declaration from its current spot; update that spot's surviving comment to note `wrapToDay` now reads the `DAY` constant declared earlier in the file (add one clause noting it's also now used internally by `stabilityCheck()`, Plan 25-08).

    2. Immediately before `stabilityCheck`'s own JSDoc, add two new small private (non-exported) helper functions: `selfUnwrapInterval(iv)`, which returns `iv` unchanged when `iv.min <= iv.max`, and otherwise returns a new object `{ min: iv.min, max: iv.max + DAY }` (reconstructing the interval's true un-wrapped span for a band whose independently-wrapToDay()'d `max` field inverted below its own `min`); and `alignNearReference(value, reference)`, which returns whichever of `value - DAY`, `value`, or `value + DAY` has the smallest absolute distance to `reference` (ties keep `value` itself, i.e. zero shift). Give each a short JSDoc explaining its role in the circular-alignment fix, referencing CR-01/25-VERIFICATION.md/25-REVIEW.md and this plan (25-08) by name, matching the file's existing decision-citation convention.

    3. Rewrite `stabilityCheck`'s body (keep its exact exported signature `stabilityCheck(intervals, central, shrinkage)` and exact return shape `{min, max, central}` unchanged) to: (a) self-unwrap every interval via `intervals.map(selfUnwrapInterval)`; (b) set `reference` to the FIRST self-unwrapped interval's `min`; (c) build `aligned` by, for each self-unwrapped interval, computing `shift = alignNearReference(iv.min, reference) - iv.min` and returning `{min: iv.min + shift, max: iv.max + shift}` (the same shift applied to both fields, preserving the interval's own span exactly); (d) compute `alignedCentral = alignNearReference(central, reference)`; (e) compute `interStart = Math.max(...aligned.map(iv => iv.min))` and `interEnd = Math.min(...aligned.map(iv => iv.max))` exactly as before, but now over the ALIGNED intervals; (f) in the overlap branch (`interStart <= interEnd`), keep the existing shrink-toward-center logic unchanged in shape but operate on `alignedCentral`/`interStart`/`interEnd`, and wrap the THREE returned fields (`min`, `max`, `central`) through `wrapToDay(...)` before returning; (g) in the no-overlap branch, keep the existing union-envelope-with-unchanged-central logic unchanged in shape but compute it over `aligned` and `alignedCentral`, again wrapping all three returned fields through `wrapToDay(...)` before returning. Update `stabilityCheck`'s JSDoc to document the CR-01 hardening: note that inputs may have `min > max` (an internally-wrapped band), describe the self-unwrap-then-align-then-compare-then-rewrap sequence, and explicitly state that no input interval's min/max is ever excluded from the reductions (the specific "signal silently dropped" symptom this closes), and that the RETURNED band may itself legitimately have `min > max` when the true intersection/union crosses midnight — exactly like a single wrapped model already could before this fix, so existing wrap-aware callers (`centralWithinBand()` in the test file) need no changes.

    4. Make ZERO changes to `combineModels()`, `wrapToDay()`'s own body, `trimmedBand()`, `blendForecast()`, or any of the 6 model-construction call sites — confirm this via `git diff js/lib/forecast-blend.js` after the edit (the diff should touch only: the relocated `DAY` declaration, the two new helper functions, `stabilityCheck`'s body and JSDoc, and the surviving comment at `wrapToDay`'s old `DAY`-declaration spot).

    Run `node --test tests/unit/forecast-blend.test.js` until every test (new and pre-existing) is green. Commit `feat(25-08): harden stabilityCheck for circular intervals`.

    No REFACTOR step is needed. Do not perform the WR-02/IN-01/IN-02 fixes from 25-REVIEW.md while in these files — all three are separate, non-blocking findings out of scope for this gap-closure plan (see kept prohibition).
  </action>
  <verify>
    <automated>node --test tests/unit/forecast-blend.test.js</automated>
    <fails_when>non-zero exit, or the summary line reports any failing test — including any pre-existing test in the file, which must still pass unchanged since every existing fixture's models never invert (all realistic historic/anchor times used stay well clear of the 1440 boundary)</fails_when>
  </verify>
  <acceptance_criteria>
    - `js/lib/forecast-blend.js` declares `const DAY = 24 * 60;` exactly once (grep confirms a single occurrence of `const DAY`).
    - `js/lib/forecast-blend.js` defines `selfUnwrapInterval` and `alignNearReference` as new functions, both referenced from inside `stabilityCheck`'s body (grep confirms both identifiers appear).
    - `stabilityCheck`'s exported signature is unchanged: `export function stabilityCheck(intervals, central, shrinkage)` (grep confirms exactly one occurrence, unchanged from before this plan).
    - `git diff js/lib/forecast-blend.js` shows zero changed lines inside `combineModels`, `wrapToDay`'s own function body, `trimmedBand`, `blendForecast`, or any of the wake/bedtime/napStart/napEnd model-construction blocks.
    - `node --test tests/unit/forecast-blend.test.js` exits 0 with zero failing tests, including the 5 new cases in the "circular/wrapped intervals — CR-01 gap-closure (Plan 25-08)" describe block and all 29 pre-existing tests.
    - `npm run test:unit` exits 0 with zero regressions across the full unit/integration suite (expect the prior 967-test baseline plus this plan's 5 new tests, i.e. 972).
    - `git log` shows a `test(25-08): ...` commit preceding a `feat(25-08): ...` commit for this task.
  </acceptance_criteria>
  <reversibility rating="reversible">A pure-function algorithm change confined to one already-isolated, already-unit-tested function (stabilityCheck), with its exact signature and return shape preserved; every caller (combineModels, wake's direct call) is untouched. Reverting is a single-file diff with zero impact on any existing fixture, since none of them ever produce an inverted model.</reversibility>
  <done>stabilityCheck() correctly detects overlap/union for any combination of wrapped and non-wrapped intervals, never drops a model's signal, and always returns a central value that lies within its own reported band (using the wrap-aware reading when that band itself legitimately crosses midnight) — closing 25-VERIFICATION.md's last remaining gap for Phase 25.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | `stabilityCheck()` continues to consume only already-computed internal minute integers derived from the app's own already-validated local `dayRecords`/`settings` state (offline-first, no network I/O, no new user-input parsing, no storage-schema change). This plan corrects internal arithmetic; it crosses no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-16 | Tampering | `stabilityCheck()` internals (`selfUnwrapInterval`, `alignNearReference`, `js/lib/forecast-blend.js`) | low | accept | Pure arithmetic on already-computed internal minute integers; no new mutable state, no new input-parsing surface, no DOM/storage access added. `Object.freeze` is not applicable to function bodies and is not a relevant mitigation here. |
| T-25-17 | Denial of Service | stabilityCheck's per-call comparison work | low | accept | The fix adds O(N) array maps (self-unwrap, align) over the same small (2-3 element) `intervals` array `stabilityCheck` already iterated with `Math.max(...)`/`Math.min(...)`; no new loop, recursion, or unbounded input path is introduced. |

</threat_model>

## Artifacts this phase produces (Plan 25-08)

- Modified: `js/lib/forecast-blend.js` — `stabilityCheck(intervals, central, shrinkage)`'s exported signature and return shape are unchanged; its internal algorithm is rewritten to self-unwrap, align-to-reference, compare, and re-wrap. Two new private helpers added: `selfUnwrapInterval(iv)`, `alignNearReference(value, reference)`. `const DAY = 24 * 60` relocated to before `stabilityCheck` (declared once). `combineModels()`, `wrapToDay()`, `trimmedBand()`, `blendForecast()`, and every model-construction call site are byte-identical to before this plan.
- Modified: `tests/unit/forecast-blend.test.js` — new `describe('circular/wrapped intervals — CR-01 gap-closure (Plan 25-08)', ...)` block (5 new `it(...)` cases) nested inside the existing `describe('stabilityCheck(intervals, central, shrinkage)', ...)` block. No new imports, no new top-level fixture helpers.

<verification>
Run `node --test tests/unit/forecast-blend.test.js` — passes, including the 5 new circular-interval cases and all 29 pre-existing tests. For full-suite confidence, also run `npm run test:unit` (expect the 967-test baseline plus these 5 new tests, i.e. 972, with zero regressions since every existing fixture's models never invert).
</verification>

<success_criteria>
- `stabilityCheck()` correctly self-unwraps and aligns any combination of wrapped (`min > max`) and normal (`min <= max`) input intervals before comparing them, for both 2-model and 3+-model groups.
- A `central` value is never reported outside its own returned `[min, max]` band (reading the band wrap-aware when it itself legitimately crosses midnight), for any of the 5 new test cases, and unchanged for all 4 pre-existing cases.
- No input model's min/max signal is ever excluded from the overlap/union computation.
- `combineModels()` and all 6 model-construction call sites are byte-identical to before this plan — the fix is confined entirely to `stabilityCheck()`'s own comparison algorithm.
- 25-VERIFICATION.md's `gaps_remaining` entry (PRED-16 interval stability check) is closed; Phase 25 has zero open verification gaps.
</success_criteria>

<output>
Create `.planning/phases/25-algorithm-c-settings-modal/25-08-SUMMARY.md` when done.
</output>
