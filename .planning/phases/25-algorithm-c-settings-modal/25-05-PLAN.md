---
phase: 25-algorithm-c-settings-modal
plan: 05
type: execute
wave: 3
depends_on: ["25-02", "25-04"]
files_modified:
  - tests/e2e/algorithm-c.spec.js
autonomous: true
requirements: [UI-12, PRED-13]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "In a real browser, the Settings modal's forecastAlgorithm select shows exactly one of #classicOptions/#tifOptions/#blendOptions at a time across all three values, matching tests/e2e/tif.spec.js's existing two-way toggle test extended to three options (D-14, UI-12)."
    - "In a real browser, selecting Algorithm C and returning to the Today screen renders real, non-placeholder Algorithm C predictions through the pre-existing '.prediction-card' Classic rendering path — never through '.tif-card' — proving PRED-13's shape-parity contract end-to-end, not just at the unit level."
    - "Switching away from Algorithm C back to Classic or TIF removes any Algorithm-C-rendered '.prediction-card' state cleanly (no stale DOM/leftover cards), mirroring tests/e2e/tif.spec.js's existing switch-away regression test."
  artifacts:
    - path: "tests/e2e/algorithm-c.spec.js"
      provides: "E2E coverage for the three-way selector visibility and for real Algorithm C prediction rendering on the Today screen"
  key_links:
    - from: "tests/e2e/algorithm-c.spec.js"
      to: "index.html (#blendOptions) + js/ui/settings-modal.js (three-way toggle) + js/ui/today-screen.js (blendForecast dispatch)"
      via: "Playwright page interactions against the real running app"
      pattern: "forecastAlgorithm.*blend"
---

<objective>
Prove Plan 25-04's UI wiring and Plans 25-01/25-02's algorithm both work end-to-end in a real browser:
the three-way selector's fieldset visibility, and real (non-null) Algorithm C predictions rendering on
the Today screen through the existing Classic-style card path.

Purpose: this is the phase's closing verification layer. Every prior plan's unit/integration tests
prove their own file in isolation; this plan is the only place that proves the full chain — Settings
modal selection -> settings.update() -> today-screen.js's dispatch -> blendForecast() -> rendered DOM
— actually connects, using the SAME wake-only-fixture technique `tests/e2e/tif.spec.js` already
established for TIF (a proven, low-flake pattern for this exact class of assertion).

Output: `tests/e2e/algorithm-c.spec.js`, covering selector visibility and real-data rendering.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-algorithm-c-settings-modal/25-CONTEXT.md
@tests/e2e/tif.spec.js
@tests/e2e/settings-modal.spec.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three-way selector fieldset visibility (D-14)</name>
  <files>tests/e2e/algorithm-c.spec.js</files>
  <read_first>
    tests/e2e/tif.spec.js (lines 92-119: `test.beforeEach` isolation pattern + Test 1's exact two-way toggle assertions — `#tifOptions` hidden/visible via `page.locator('#settings select[name="forecastAlgorithm"]').selectOption(...)` — this task's Test 1 is the SAME test extended to a third option and a third fieldset).
    tests/e2e/settings-modal.spec.js (lines 21-25: the `beforeEach` localStorage-clear-and-reload pattern to reuse verbatim).
  </read_first>
  <action>
    Create `tests/e2e/algorithm-c.spec.js` with a header comment stating its scope (three-way selector visibility + real-data rendering for Algorithm C, Phase 25 / D-14 / UI-12 / PRED-13) and a `test.beforeEach` identical to `tests/e2e/tif.spec.js`'s (`page.goto('/')`, `localStorage.clear()`, `page.reload()`).

    Add `test('three-way algorithm selector shows exactly one fieldset at a time (D-14)', ...)`: open Settings (`button.settingsTrigger`), assert `#classicOptions` is visible and `#tifOptions`/`#blendOptions` are both hidden (default `forecastAlgorithm: 'classic'`); `selectOption('tif')` then assert `#tifOptions` visible, `#classicOptions`/`#blendOptions` hidden; `selectOption('blend')` then assert `#blendOptions` visible, `#classicOptions`/`#tifOptions` hidden; `selectOption('classic')` then assert the original state is restored. Use `page.locator('#settings select[name="forecastAlgorithm"]')` and `await expect(page.locator('#ID')).toBeVisible()` / `.toBeHidden()` exactly as `tests/e2e/tif.spec.js`'s Test 1 does.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/algorithm-c.spec.js -g "three-way algorithm selector"</automated>
    <fails_when>non-zero exit, or the test summary reports this test as failed</fails_when>
  </verify>
  <acceptance_criteria>
    - The test asserts all three fieldsets' visibility state at all four selector positions (classic default, tif, blend, back to classic) — 12 total visibility assertions minimum.
    - `npx playwright test tests/e2e/algorithm-c.spec.js -g "three-way algorithm selector"` passes.
  </acceptance_criteria>
  <done>The three-way selector's mutually-exclusive fieldset visibility is proven in a real browser, matching the established TIF two-way precedent extended to three options.</done>
</task>

<task type="auto">
  <name>Task 2: Real Algorithm C predictions render via the Classic card path, not the TIF path</name>
  <files>tests/e2e/algorithm-c.spec.js</files>
  <read_first>
    tests/e2e/tif.spec.js (lines 17-88: `makeDb`/`makeEvents`/`makeWakeOnlyDb`/`seedAndReload` helpers — copy these into the new file rather than importing across spec files per this project's existing per-spec-file fixture-duplication convention, changing only the settings override passed to `makeWakeOnlyDb`; lines 123-168: Test 2's exact structure — `page.clock.setFixedTime`, seed with the algorithm override, assert `#forecast-cards`/`#cold-start-message` visibility, locate `.later-today-section`, open it, and assert on the wake card inside it; lines 172-195: Test 3's exact "switch away removes cards" structure).
  </read_first>
  <action>
    In the same file, copy `makeDb`/`makeEvents`/`makeWakeOnlyDb`/`seedAndReload` from `tests/e2e/tif.spec.js` verbatim (do NOT change their internals — only this task's own test bodies pass `{ forecastAlgorithm: 'blend' }` instead of `{ forecastAlgorithm: 'tif' }` as the settings override). Do not add `blendWindowDays`/`blendTrimPct`/`blendShrinkage` to the override object — omitting them proves Plan 25-03's migration/validation default-injection path also works end-to-end, not just the algorithm dispatch.

    Add `test('Algorithm C predictions render via .prediction-card, never .tif-card', ...)`: `page.clock.setFixedTime(new Date('2026-06-02T09:00:00'))` (same fixed time as tif.spec.js's Test 2, for the identical reachable-event reasoning documented there); seed with `makeWakeOnlyDb({ forecastAlgorithm: 'blend' })`; assert `#forecast-cards` visible and `#cold-start-message` not visible (32 days > minDays 7); locate `.later-today-section`, open it via its `summary`; assert `#forecast-cards .tif-card` has count 0 (Algorithm C predictions never carry `precisionScore`/`isLowConfidence`, so `renderForecastSection`'s generic dispatch never selects the TIF card renderer); assert at least one `#forecast-cards .prediction-card` exists inside the opened Later-Today section, and that ITS `.time-central` text is NOT the literal em-dash placeholder `'—'` (proving the wake prediction's central time is real data, not a null-fallback render) — locate the wake card specifically via `laterToday.locator('.prediction-card[data-event-type="wake"]')` (the same `data-event-type` attribute `tif.spec.js`'s TIF-card assertions rely on, present on every prediction card regardless of algorithm).

    Add `test('switching from Algorithm C to Classic removes any Algorithm-C-only card state', ...)`: mirroring `tests/e2e/tif.spec.js`'s Test 3 exactly — seed with `forecastAlgorithm: 'blend'`, confirm `#forecast-cards .prediction-card` count is greater than 0, open Settings, `selectOption('classic')`, save, then assert `#forecast-cards` re-renders without error (`#forecast-cards` still visible, no leftover `.tif-card` — there never was one — and no thrown console error, checked via a `page.on('pageerror', ...)` listener registered before the interaction that fails the test if fired).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/algorithm-c.spec.js</automated>
    <fails_when>non-zero exit, or the summary line does not read all tests passed (any "failed" count above 0)</fails_when>
  </verify>
  <acceptance_criteria>
    - Both new tests exist and pass; the full file (including Task 1's test) passes as one suite.
    - The rendering test asserts `.tif-card` count is 0 while `.prediction-card` count is greater than 0 for the SAME `forecastAlgorithm: 'blend'` render.
    - `npx playwright test tests/e2e/algorithm-c.spec.js` exits 0.
  </acceptance_criteria>
  <done>Algorithm C's full chain — selector, dispatch, and rendering — is proven correct in a real Chromium instance, completing the phase's verification layer (PRED-13, PRED-14..17 via the algorithm producing real data, UI-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan adds only test code exercising already-covered boundaries (Plans 25-01..25-04). No new production code, no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-08 | Repudiation | Test-only console-error listener | low | accept | The `page.on('pageerror', ...)` guard exists purely to strengthen this plan's own assertions (fail the test on an unexpected runtime error); it has no production-code footprint. |

</threat_model>

## Artifacts this phase produces (Plan 25-05)

- `tests/e2e/algorithm-c.spec.js` — three-way selector visibility test, real-prediction-rendering test, and switch-away regression test

<verification>
Run `npx playwright test tests/e2e/algorithm-c.spec.js` — all tests pass. Run `npm run test:e2e` (full Playwright suite) to confirm no regression in `tests/e2e/tif.spec.js` or `tests/e2e/settings-modal.spec.js`.
</verification>

<success_criteria>
- The three-way selector's visibility contract (D-14) is proven in a real browser.
- Algorithm C's predictions are proven to render via the pre-existing Classic card path with real data, not a stub, closing PRED-13/PRED-17/UI-12's end-to-end loop for this phase.
- No regression to the existing TIF or Settings-modal E2E suites.
</success_criteria>

<output>
Create `.planning/phases/25-algorithm-c-settings-modal/25-05-SUMMARY.md` when done.
</output>
