---
context: phase
phase: 24-autosave
task: 0
total_tasks: 2
status: not_started
last_updated: 2026-09-17T16:22:26.192Z
---

<current_state>
Phase 24 (Autosave) is in gap-closure. `/gsd-plan-phase 24 --gaps` just completed: `24-VERIFICATION.md` found 2 gaps blocking the phase goal, and a new plan `24-05-PLAN.md` (wave 1, `depends_on: ["24-03"]`, `gap_closure: true`) was created and committed to close them. Execution has NOT started — no task in 24-05 is done yet. Plans 24-01 through 24-04 are already executed and unaffected.
</current_state>

<completed_work>

Completed Tasks (this session, planning only):
- Ran `/gsd-plan-phase 24 --gaps` end-to-end: init, closed-phase gate (phase_status was `Executed`, not `Complete` — gate didn't block), spec-less probe fallback (edge-probe over PLAT-01..04), spawned gsd-planner in gap_closure mode.
- Planner wrote `24-05-PLAN.md` (2 tasks) and updated `ROADMAP.md`'s Phase 24 plan list — committed as `c33cf61`.
- Requirements coverage gate: 4/4 (PLAT-01..04) covered across all 5 plans.
- Decision coverage gate: 11/11 CONTEXT.md decisions covered.
- Recorded planning completion in STATE.md/state.json — committed as `03a3da5` and `af5fd85`.
- plan-checker step was skipped (project config `plan_checker_enabled: false`) — no automated second-opinion review ran on 24-05-PLAN.md.

No execution task has started.
</completed_work>

<remaining_work>

- Task 1 (24-05-PLAN.md): Fix Remove-handler race (Gap A) and gate banner-dismissal on a real pick (Gap B) — both in `js/ui/settings-modal.js`. Not started.
- Task 2 (24-05-PLAN.md): Add 2 new Playwright regression tests to `tests/e2e/settings-autosave.spec.js` (Remove-button flip without reopen; Settings-only pick suppresses banner across reload). Not started.
- After 24-05 executes: phase 24 will need re-verification (`/gsd-verify-work` or the execute-phase post-wave verifier) to confirm both gaps are actually closed.
</remaining_work>

<decisions_made>

- **Gap B fix approach:** chosen to set the `autosaveBannerDismissed` localStorage flag from `settings-modal.js`'s `pickOrChange` handler (Option B), NOT to pass `isConfigured` into `mountTodayScreen` (Option A). Reason: `js/app.js` calls `restoreHandle()` asynchronously then calls `mountTodayScreen(...)` synchronously in the same tick, so `autosaveState.status` is still `'unset'` at every `mountTodayScreen` call — Option A would silently fail for a folder configured in a *prior* session. Option B sidesteps the race entirely and touches only `settings-modal.js` (see `24-05-PLAN.md`'s "Source Coverage Audit" section for full detail).
- **Scope boundary:** `24-REVIEW.md` (code review) independently found the same 2 bugs (its WR-01/WR-02) plus 2 more — WR-03 (IndexedDB connections in `createIndexedDbHandleStore` never closed, no `onblocked` handler) and WR-04 (boot-time `restoreHandle()` failures silently swallowed). These are deliberately OUT of `24-05-PLAN.md`'s scope: gap-closure mode's contract (`gsd-core/references/planner-gap-closure.md`) sources gaps only from `24-VERIFICATION.md`'s `gaps:` block (+ diagnosed UAT.md, which doesn't exist for this phase) — never from REVIEW.md. WR-03/WR-04 need a separate `/gsd-code-review --fix` run or a small follow-up phase; do not fold them into 24-05.
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/phases/24-autosave/24-VERIFICATION.md` — the 2 gaps this plan closes (full reason/artifacts/missing detail for Gap A and Gap B)
2. `.planning/phases/24-autosave/24-05-PLAN.md` — the plan to execute next; read its "Source Coverage Audit" section for the Gap B decision rationale before touching `today-screen.js` or `app.js` (the plan deliberately does NOT touch either file)
3. `.planning/phases/24-autosave/24-REVIEW.md` — for awareness of WR-03/WR-04 (out of scope for this plan, do not fix as part of 24-05)

## Critical Anti-Patterns (do NOT repeat these)
- None discovered this session — planning went cleanly through all gates on the first pass.

## Infrastructure State
- No background servers/watchers running as part of this work.
- Git working tree is clean (only pre-existing, unrelated untracked files: `.planning/milestone.lock`, `.planning/phases/20-nap-probability-redesign/20-UAT.md` — not part of this phase, leave alone).

<context>
Straightforward gap-closure planning session: verification found exactly 2 concrete, well-isolated bugs (both already independently corroborated by code review), the spec-less probe fallback ran (no SPEC.md exists for this phase) and folded PLAT-01..04 edge coverage into the plan's must_haves without duplicating what 24-01..24-04 already covered, and the planner produced one small plan rather than splitting into multiple. Nothing contentious; the only judgment call was the Gap B fix approach, which is documented above and in the plan itself.
</context>

<next_action>
Start with: `/gsd-execute-phase 24 --gaps-only` to execute `24-05-PLAN.md`'s 2 tasks, then re-verify phase 24.
</next_action>
