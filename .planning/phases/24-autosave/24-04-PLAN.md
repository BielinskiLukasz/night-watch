---
phase: 24-autosave
plan: 04
type: execute
wave: 3
depends_on: ["24-02"]
files_modified:
  - js/ui/today-screen.js
  - style.css
  - tests/e2e/autosave-banner.spec.js
autonomous: true
requirements: [PLAT-01, PLAT-04]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The first-launch banner renders at the top of the Today screen (before quickLog, per D-10) only when both autosave.isSupported is true AND localStorage's 'autosaveBannerDismissed' flag is not set."
    - "Clicking 'Set up autosave' calls the injected autosave.pick() and then sets the dismissal flag and hides the banner; clicking 'Dismiss' sets the dismissal flag and hides the banner WITHOUT calling pick() (D-11: dismissal happens on either action, only one of the two also triggers the picker)."
    - "After dismissal (either button), reloading the page does not re-show the banner — the localStorage flag persists across reloads (D-11)."
    - "Every localStorage.getItem/setItem call this task adds is tagged // gsd:allow-storage-local (on the same or immediately preceding line), or tests/integration/security-smoke.test.js's repo-wide storage-seam gate fails the build."
  artifacts:
    - js/ui/today-screen.js (banner rendering)
    - style.css (.autosave-banner rules)
    - tests/e2e/autosave-banner.spec.js
  key_links:
    - "mountTodayScreen's new autosave parameter ({isSupported, pick}) <-> js/app.js's Plan 24-02 wiring — today-screen.js never imports js/lib/autosave.js directly, matching the composition-root discipline used everywhere else in this phase."
---

<objective>
Add a one-time, dismissible first-launch banner at the top of the Today screen that nudges the user to set up autosave (D-10), tracked via a plain localStorage flag (D-11) — the discovery surface for the feature Plans 24-01/24-02/24-03 built.

Purpose: This is the phase's second, independent UI expansion (parallel to Plan 24-03 — different files, same `depends_on: 24-02`). Without it, a Settings-only feature is invisible to users who never open Settings, which the user explicitly flagged as unacceptable for a feature whose entire purpose is closing the app's largest data-loss risk (CONTEXT.md D-10 rationale).

Output: Updated `js/ui/today-screen.js` (banner element + dismissal logic), updated `style.css` (`.autosave-banner` rules, mirroring the existing `#file-note` treatment), and a new `tests/e2e/autosave-banner.spec.js`.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-autosave/24-CONTEXT.md
@js/ui/today-screen.js
@js/app.js
@style.css
@.planning/phases/24-autosave/24-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: First-launch banner in today-screen.js + CSS</name>
  <files>js/ui/today-screen.js, style.css</files>
  <read_first>
    js/ui/today-screen.js (lines 824-890, `mountTodayScreen({root, eventLog, settings, clock})` — the exact function signature this task extends with an `autosave` parameter, and the `root.replaceChildren(quickLog, stageSelectorContainer, nextEventCard, coldStartMsg, forecastCards, toggle, dayList)` call at line 890, which this task must prepend a banner element to).
    js/app.js (lines 216-235: the `FILE_NOTE_KEY` / `location.protocol === 'file:'` block — the project's ONLY existing precedent for a one-time dismissible localStorage-flagged notice, including the exact `// gsd:allow-storage-local` tag placement this task must replicate for every new `localStorage` call).
    tests/integration/security-smoke.test.js (lines 228-249: the storage-seam gate that fails the build if any `localStorage` reference in js/ lacks the `// gsd:allow-storage-local` tag on the same or immediately preceding line — this is a REPO-WIDE test, not phase-scoped, so a missed tag breaks CI regardless of which file it's in).
    style.css (lines 1476-1499: `#file-note` — the flex-row / padding / border / dismiss-btn treatment this task's `.autosave-banner` CSS mirrors, adapted from a fixed id-selector to a reusable class since the banner is built dynamically via `el()`, not static markup).
  </read_first>
  <behavior>
    - `mountTodayScreen({root, eventLog, settings, clock, autosave})` — `autosave` is optional; when omitted (e.g. existing unit/integration tests that don't pass it), no banner logic runs and behavior is unchanged from before this task.
    - When `autosave && autosave.isSupported` is true AND `localStorage.getItem('autosaveBannerDismissed')` is falsy, a `bannerEl` (`className: 'autosave-banner'`) is built containing: a `<p>` with the nudge copy, a "Set up autosave" `<button>`, and a "Dismiss" `<button>`.
    - "Set up autosave" click handler: `await autosave.pick(); localStorage.setItem('autosaveBannerDismissed', '1'); bannerEl.hidden = true;`.
    - "Dismiss" click handler: `localStorage.setItem('autosaveBannerDismissed', '1'); bannerEl.hidden = true;` (no `pick()` call).
    - When `bannerEl` was built, it is prepended as the FIRST argument to the existing `root.replaceChildren(...)` call (before `quickLog`); when it was not built (unsupported or already dismissed), `root.replaceChildren(...)` is called exactly as before this task, unchanged.
  </behavior>
  <action>
    Add `autosave` to `mountTodayScreen`'s destructured parameter list and JSDoc (js/ui/today-screen.js, current signature at line 824). Immediately after the existing `const addEventBtn = el(...)` / `quickLog.appendChild(addEventBtn);` block (lines 877-885), add the banner-construction logic exactly as described in `<behavior>`, tagging BOTH the `localStorage.getItem` call and BOTH `localStorage.setItem` calls with `// gsd:allow-storage-local` on the same line (three tags total, mirroring app.js's `FILE_NOTE_KEY` block at lines 223/230). Change the `root.replaceChildren(quickLog, stageSelectorContainer, nextEventCard, coldStartMsg, forecastCards, toggle, dayList);` line to conditionally prepend `bannerEl` (e.g. build the children array once, unshift `bannerEl` onto it when it exists, then spread into `replaceChildren`).

    In style.css, add a `.autosave-banner` rule block near the existing `#file-note` rules (style.css lines ~1476-1499): flex row, `align-items: center`, `justify-content: space-between`, padding, a light border/background matching `#file-note`'s palette (`background: #f8fafc; border: 1px solid #e2e8f0;`), `border-radius: 0.375rem`, and `margin: 0 0 8px` (it sits inside the Today screen's normal flow, not fixed like `#update-banner`, so no `position: fixed`/body-padding-offset rules are needed). Add a `.autosave-banner button` rule reusing the same `min-height: 44px` tap-target sizing already used elsewhere (e.g. `#update-banner .reload-btn`).
  </action>
  <verify>
    <automated>node --test tests/integration/security-smoke.test.js</automated>
    <fails_when>non-zero exit, or the storage-seam gate ("D-07 storage-seam") test reports a violation</fails_when>
  </verify>
  <acceptance_criteria>
    - Every `localStorage.getItem`/`localStorage.setItem` call added to js/ui/today-screen.js by this task has `// gsd:allow-storage-local` on the same line.
    - `mountTodayScreen` still works with `autosave` omitted entirely (grep confirms the parameter is optional / destructured with no default that throws).
    - `node --test` (full unit+integration suite) reports zero regressions in any existing today-screen.js-dependent test (e.g. tests/integration/today-*.test.js).
  </acceptance_criteria>
  <done>The banner renders only for supported+not-yet-dismissed sessions, both buttons dismiss it, only "Set up autosave" also triggers the picker, and the repo-wide storage-seam smoke test passes.</done>
</task>

<task type="auto">
  <name>Task 2: E2E coverage for the first-launch banner</name>
  <files>tests/e2e/autosave-banner.spec.js</files>
  <read_first>
    tests/e2e/settings-modal.spec.js (lines 21-25: the `beforeEach` localStorage-clear-and-reload isolation pattern this new spec reuses so the dismissal flag starts clean for every test).
    js/app.js (lines 216-235: confirms the existing project convention for asserting a one-time banner's dismiss-and-persist behavior across a `page.reload()`, which this task's third test mirrors for the autosave banner instead of the file-note).
  </read_first>
  <action>
    Create tests/e2e/autosave-banner.spec.js with a `beforeEach` that clears localStorage and reloads (mirroring tests/e2e/settings-modal.spec.js). Write three tests: (1) with `showDirectoryPicker` present (Chromium's default — no mocking needed), the banner (`.autosave-banner`) IS visible on first load; (2) clicking the banner's "Dismiss" button hides it AND, after `page.reload()`, it does not reappear; (3) with `page.addInitScript(() => { delete window.showDirectoryPicker; })` before `page.goto('/')`, the banner is NOT rendered at all (PLAT-04 — unsupported browsers never see the nudge).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/autosave-banner.spec.js</automated>
    <fails_when>non-zero exit, or the summary reports any failed test</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/e2e/autosave-banner.spec.js contains exactly the three tests described (visible-by-default, dismiss-persists-across-reload, hidden-when-unsupported).
    - `npx playwright test tests/e2e/autosave-banner.spec.js` passes with zero failures.
  </acceptance_criteria>
  <done>The banner's appear/dismiss/persist/hide-when-unsupported behavior is proven end-to-end in a real browser.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| localStorage dismissal flag ↔ banner render decision | A same-origin, client-only UI-state flag with no security consequence if tampered with (worst case: banner reappears or stays dismissed one session early). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-09 | Tampering (XSS) | Banner copy rendering | low | mitigate | Static, hardcoded copy via `el({textContent})` — no user-controlled data ever flows into the banner's DOM. |
| T-24-10 | Denial of Service (storage-seam gate) | New `localStorage` calls in today-screen.js | low | mitigate | Every new call is tagged `// gsd:allow-storage-local`; the repo-wide `tests/integration/security-smoke.test.js` gate fails the build otherwise, making an untagged miss impossible to ship unnoticed. |

</threat_model>

## Artifacts this phase produces (Plan 24-04)

- Banner-rendering block inside `mountTodayScreen` (js/ui/today-screen.js) — no new exported symbol, an internal addition to the existing exported `mountTodayScreen` function's parameter contract (`autosave` param).
- `autosaveBannerDismissed` (localStorage key)
- `.autosave-banner` CSS rules (style.css)
- `tests/e2e/autosave-banner.spec.js`

<verification>
Run `npx playwright test tests/e2e/autosave-banner.spec.js` and `node --test tests/integration/security-smoke.test.js` — both pass.
</verification>

<success_criteria>
- The banner appears exactly once per browser profile (until dismissed), never for unsupported browsers, and never blocks normal app use (not a modal).
</success_criteria>

<output>
Create `.planning/phases/24-autosave/24-04-SUMMARY.md` when done.
</output>
