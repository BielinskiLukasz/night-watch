---
phase: 24-autosave
plan: 03
type: execute
wave: 3
depends_on: ["24-02"]
files_modified:
  - index.html
  - js/ui/settings-modal.js
  - tests/e2e/settings-autosave.spec.js
autonomous: true
requirements: [PLAT-01, PLAT-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Backup fieldset renders exactly one of three mutually-exclusive states at any time: unsupported (explanatory note + fallback Export button), unset-or-denied (Choose folder button, plus a revoked-access note ONLY when status is 'denied'), or set (folder name + Change/Remove buttons) — per D-06/D-08/PLAT-04."
    - "Clicking 'Choose folder' or 'Change folder' calls the injected autosave.pick(), then re-renders the Backup section from the fresh autosave.getState() — a successful pick flips the row from unset to set without closing/reopening the modal."
    - "Clicking 'Remove' calls autosave.remove() and re-renders the Backup section back to the unset state."
    - "The fallback Export button (visible only in the unsupported state, D-03) invokes autosave.onExport() — the SAME callback reference the History screen's Export JSON button already uses, not a new/second export code path."
    - "The status line (#autosaveStatus) shows 'Last saved: HH:MM' after a successful autosave, or 'Error: <message>' after a failed one, and NEVER both at once — the most recent outcome fully replaces the previous text (D-09)."
    - "handle.name (the folder's own name only, never a reconstructed path) is the only folder-identifying text rendered, set via textContent (T-07 XSS guard, D-08)."
  artifacts:
    - index.html (Backup fieldset markup)
    - js/ui/settings-modal.js (renderBackupSection / wireBackupButtons)
    - tests/e2e/settings-autosave.spec.js
  key_links:
    - "settings-modal.js's openSettings({...,  autosave}) parameter <-> js/app.js's autosaveActions object from Plan 24-02 — settings-modal.js never imports js/lib/autosave.js directly, it only calls autosave.getState()/pick()/remove()/onExport() on whatever object app.js injects."
    - "index.html's #autosaveUnsupportedRow / #autosaveUnsetRow / #autosaveSetRow / #autosaveRevokedNote / #autosaveFolderName / #autosaveStatus / #autosavePickBtn / #autosaveChangeBtn / #autosaveRemoveBtn / #autosaveExportBtn ids <-> settings-modal.js's renderBackupSection()/wireBackupButtons() — every id referenced in JS must exist in the markup this plan adds, and vice versa."
---

<objective>
Add a fourth-visible "Backup" fieldset to the Settings modal (D-07) with the folder row's four visual states (unsupported / unset / denied / set), a session-scoped status line, and the fallback manual Export button — all wired through the `autosave` deps object Plan 24-02 exposes from `js/app.js`.

Purpose: This is the phase's UI expansion into Settings — the user-visible surface for PLAT-01 (pick a folder) and PLAT-04 (graceful fallback when the File System Access API is unavailable). It consumes, but does not extend, the round trip Plan 24-01 built and Plan 24-02 wired.

Output: Updated `index.html` (static Backup fieldset markup, matching the existing `<dialog id="settings">` convention where "the form markup is static in index.html"), updated `js/ui/settings-modal.js`, and a new `tests/e2e/settings-autosave.spec.js` covering both the supported and unsupported states end-to-end in a real browser.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-autosave/24-CONTEXT.md
@index.html
@js/ui/settings-modal.js
@.planning/phases/24-autosave/24-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backup fieldset markup in index.html</name>
  <files>index.html</files>
  <read_first>
    index.html (lines 213-358: the full `<dialog id="settings">` form — specifically lines 328-344, the "Group 4: Data" import/export fieldset and "Group 5: Stages" fieldset, which sandwich the new Backup fieldset; note the header comment at lines 204-212 documenting the modal's static-markup convention: JS only ever does `.value`/`.checked`/`.textContent` assignment, never builds this markup dynamically).
    style.css (lines 1252-1266: `.importStatus` / `.importStatus:empty` / `.importStatus.error` — reused verbatim for the new status line so this task needs ZERO new CSS; lines confirming `#classicOptions`/`#tifOptions` already toggle visibility via bare `hidden` with no dedicated CSS, the same zero-new-CSS pattern this fieldset follows).
  </read_first>
  <action>
    Insert a new fieldset between the existing "Group 4: Data" fieldset (ends at line ~335) and the "Group 5: Stages" fieldset (starts at line ~339), and relabel the Stages fieldset's leading comment from "Group 5" to "Group 6" so the numbering stays sequential. The new fieldset has `id="backupFieldset"`, a `<legend>Backup</legend>`, and a one-line `<p class="backupHelp">` explaining automatic dated backups to a chosen folder (cite D-07: this is its own fieldset, not folded into Profile/Subject &amp; Display).

    Inside it, add three mutually-exclusive `<div>` state containers, each starting `hidden` (JS decides which one to unhide — mirrors the `#tifOptions`/`#classicOptions` pattern):
    - `<div id="autosaveUnsupportedRow" hidden>` containing a `<p class="hint">` explaining automatic backup is unavailable in this browser, plus `<button type="button" id="autosaveExportBtn" class="btnImport">Export JSON</button>` (D-03/PLAT-04 fallback).
    - `<div id="autosaveUnsetRow" hidden>` containing `<button type="button" id="autosavePickBtn" class="btnImport">Choose folder</button>` and, nested inside it, `<p id="autosaveRevokedNote" class="hint" hidden>` explaining access to the previous folder was revoked (D-06 — shown only when status is 'denied', hidden by default even when this row is visible for the plain 'unset' case).
    - `<div id="autosaveSetRow" hidden>` containing a `<p>Saving to: <strong id="autosaveFolderName"></strong></p>` (D-08 — folder name only, no path), `<button type="button" id="autosaveChangeBtn" class="btnImport">Change folder</button>`, and `<button type="button" id="autosaveRemoveBtn" class="btnImport">Remove</button>`.

    After the three rows, add `<p id="autosaveStatus" class="importStatus" aria-live="polite"></p>` — reuses the EXISTING `.importStatus`/`.importStatus:empty`/`.importStatus.error` CSS rule already defined for the Data fieldset's import status line (style.css lines 1252-1266), so no new CSS is added by this task. None of the new elements have a `name` attribute — they must never be picked up by the Save button's `new FormData(form)` read in the `onClose` handler (mirrors the Stages inline-form inputs' intentional `name`-omission pattern).
  </action>
  <verify>
    <automated>node --test tests/integration/security-smoke.test.js</automated>
    <fails_when>non-zero exit — this task adds no JS logic, but confirms the html-scoped script-src check and the rest of the smoke suite still pass after the markup change</fails_when>
  </verify>
  <acceptance_criteria>
    - `#backupFieldset` exists in index.html, positioned between the Data and Stages fieldsets.
    - All nine new ids listed in this plan's `key_links` are present exactly once each in index.html.
    - None of the nine new elements has a `name` attribute.
    - `#autosaveUnsupportedRow`, `#autosaveUnsetRow`, and `#autosaveSetRow` each carry the `hidden` attribute in the static markup.
  </acceptance_criteria>
  <done>The Backup fieldset markup exists, matches the project's static-markup-plus-JS-toggling convention, and introduces zero new CSS.</done>
</task>

<task type="auto">
  <name>Task 2: renderBackupSection / wireBackupButtons in settings-modal.js</name>
  <files>js/ui/settings-modal.js</files>
  <read_first>
    js/ui/settings-modal.js (lines 60-105: `openSettings({settings, eventLog, storage, id})`'s current parameter list and `populateForm` — this task adds an `autosave` parameter and a sibling render function, following the exact same "read a snapshot, set .hidden/.textContent" style; lines 129-143: the `_forecastAlgorithmChangeHandler` module-level-ref pattern for avoiding listener accumulation on repeated opens — `wireBackupButtons` follows this exact idiom for its four buttons; lines 235-240: the `showStatus`/`textContent`-only status-rendering precedent this task's status-line logic mirrors).
    .planning/phases/24-autosave/24-CONTEXT.md (D-06, D-08, D-09 — exact status-line and denied-state rendering rules).
  </read_first>
  <behavior>
    - `renderBackupSection(state)` reads `#autosaveUnsupportedRow`/`#autosaveUnsetRow`/`#autosaveSetRow`/`#autosaveRevokedNote`/`#autosaveFolderName`/`#autosaveStatus` and sets: `unsupportedRow.hidden = state.supported`; `unsetRow.hidden = !state.supported || state.status === 'granted'`; `setRow.hidden = !state.supported || state.status !== 'granted'`; `revokedNote.hidden = !(state.supported && state.status === 'denied')`; `folderNameEl.textContent = state.folderName || ''`; and the status line per the next bullet.
    - Status line: if `state.error` is set, `statusEl.textContent = 'Error: ' + state.error` and `statusEl.className = 'importStatus error'`; else if `state.lastSavedAt` is set, `statusEl.textContent = 'Last saved: ' + state.lastSavedAt` and `statusEl.className = 'importStatus'`; else `statusEl.textContent = ''` and `statusEl.className = 'importStatus'` — exactly one of the three branches runs (D-09: never both error and last-saved text at once).
    - `wireBackupButtons(autosave)` wires `#autosavePickBtn` and `#autosaveChangeBtn` to the same handler: `async () => { await autosave.pick(); renderBackupSection(autosave.getState()); }`; `#autosaveRemoveBtn` to `() => { autosave.remove(); renderBackupSection(autosave.getState()); }`; `#autosaveExportBtn` to `() => autosave.onExport()`. Each button's handler is stored in a module-level variable and removed before being re-added on every `openSettings` call, exactly like `_forecastAlgorithmChangeHandler`.
    - `openSettings({settings, eventLog, storage, id, autosave})` calls `renderBackupSection(autosave.getState())` and `wireBackupButtons(autosave)` once, near the end of the function (after the existing Stages CRUD wiring), guarded by `if (autosave) { ... }` so the modal still opens correctly in any test/context that omits the new param.
  </behavior>
  <action>
    Add module-level handler-ref variables `_autosavePickHandler`, `_autosaveChangeHandler`, `_autosaveRemoveHandler`, `_autosaveExportHandler` alongside the existing `_csvClickHandler`/`_forecastAlgorithmChangeHandler` block (lines 37-43). Add the `autosave` parameter to `openSettings`'s destructured argument and its JSDoc. Implement `renderBackupSection` and `wireBackupButtons` as private module functions (same visibility level as `mountStagesCrud`/`renderStageList`), and call them from inside `openSettings` exactly as described in `<behavior>`, guarded by `if (autosave) { ... }`.
  </action>
  <verify>
    <automated>node --test tests/integration/security-smoke.test.js</automated>
    <fails_when>non-zero exit, or a new `.innerHTML =` / untagged `localStorage` violation is reported</fails_when>
  </verify>
  <acceptance_criteria>
    - `renderBackupSection` and `wireBackupButtons` both exist in js/ui/settings-modal.js and are called from `openSettings` only when `autosave` is truthy.
    - Grep js/ui/settings-modal.js for `.innerHTML` shows zero new occurrences (folder name and status text are set via `.textContent` only, per T-07/D-08).
    - Every button handler assignment is preceded by a `removeEventListener` call using the same module-level ref variable, mirroring `_forecastAlgorithmChangeHandler`'s pattern (grep for `_autosave.*Handler` shows four ref variables, each used in both a remove and an add call).
  </acceptance_criteria>
  <done>Opening Settings renders the correct Backup row for whatever state autosave.getState() reports, and every button re-renders from a fresh getState() after acting, with no listener accumulation across repeated opens.</done>
</task>

<task type="auto">
  <name>Task 3: E2E coverage for supported and fallback states</name>
  <files>tests/e2e/settings-autosave.spec.js</files>
  <read_first>
    tests/e2e/settings-modal.spec.js (lines 1-40: the `beforeEach` localStorage-clear-and-reload isolation pattern, and the `button.settingsTrigger` → `dialog#settings` open sequence this new spec reuses).
    .planning/BACKLOG.md B-051's implementation notes (documents that Playwright's `page.on('filechooser')` cannot intercept `showDirectoryPicker` — this task instead mocks `window.showDirectoryPicker` via `page.addInitScript` BEFORE the app loads, and uses `navigator.storage.getDirectory()` — the browser's real, dialog-free Origin Private File System root — as a REAL `FileSystemDirectoryHandle` stand-in so the "supported" test exercises the actual saveToDisk write path in a real browser, not a hand-rolled fake).
  </read_first>
  <action>
    Create tests/e2e/settings-autosave.spec.js with two `test.describe` blocks:

    "Unsupported fallback (PLAT-04)": `page.addInitScript(() => { delete window.showDirectoryPicker; })` before `page.goto('/')`, then open Settings and assert `#autosaveUnsupportedRow` is visible, `#autosaveUnsetRow` and `#autosaveSetRow` are hidden, and `#autosaveExportBtn` is visible; clicking `#autosaveExportBtn` triggers a real `download` event (same assertion shape as the existing `tests/e2e/import-export.spec.js` Export JSON test) proving D-03's shared-handler reuse actually fires a download, not a dead button.

    "Supported pick-and-save flow (PLAT-01/PLAT-02)": `page.addInitScript(() => { window.showDirectoryPicker = async () => await navigator.storage.getDirectory(); })` before `page.goto('/')` — OPFS's root directory handle implements the same `getFileHandle`/`createWritable`/`queryPermission` interface as a real user-picked directory, with no permission dialog required, so this exercises the REAL (non-fake) `saveToDisk`/`createIndexedDbHandleStore` code paths end-to-end. Open Settings, click `#autosavePickBtn`, assert the row flips to `#autosaveSetRow` visible with `#autosaveFolderName` non-empty. Close Settings, click a quick-log button on the Today screen to trigger an event-log mutation, wait 600ms (past the 500ms debounce), then via `page.evaluate` read back the written file from `navigator.storage.getDirectory()` (list entries, `getFileHandle(name).getFile().text()`) and assert its parsed JSON contains the just-logged event — confirming the full pick→debounce→write→(real)read-back chain works in a real browser, complementing Plan 24-01's fake-double unit tests. Re-open Settings and assert `#autosaveStatus` reads `Last saved: HH:MM` (regex match, not an exact clock value).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/settings-autosave.spec.js</automated>
    <fails_when>non-zero exit, or the summary line does not read all tests passed (any "failed" count above 0)</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/e2e/settings-autosave.spec.js contains both a supported-state and an unsupported-state test.
    - The supported-state test uses `navigator.storage.getDirectory()`, not a hand-rolled JS object, as the mocked `showDirectoryPicker` return value.
    - `npx playwright test tests/e2e/settings-autosave.spec.js` passes with zero failures.
  </acceptance_criteria>
  <done>Both Settings Backup states are proven end-to-end in a real Chromium instance, including a real (OPFS-backed) file write and read-back — not just unit-level fakes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| User's OS filesystem folder name ↔ Settings DOM | `handle.name` originates from the user's own filesystem (any string the OS allows in a folder name) and is rendered into the modal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-07 | Tampering (XSS) | `#autosaveFolderName` / `#autosaveStatus` rendering | medium | mitigate | Both are set via `.textContent` only (never `innerHTML`) — a folder literally named `<script>` renders as inert text, per CLAUDE.md's XSS guard invariant, enforced by `tests/integration/security-smoke.test.js`'s repo-wide `.innerHTML` scan. |
| T-24-08 | Repudiation | Status line error/last-saved text | low | accept | Purely informational, session-scoped (D-09); no security decision is gated on it. |

</threat_model>

## Artifacts this phase produces (Plan 24-03)

- `#backupFieldset`, `#autosaveUnsupportedRow`, `#autosaveUnsetRow`, `#autosaveSetRow`, `#autosaveRevokedNote`, `#autosaveFolderName`, `#autosaveStatus`, `#autosavePickBtn`, `#autosaveChangeBtn`, `#autosaveRemoveBtn`, `#autosaveExportBtn` (index.html)
- `renderBackupSection(state)`, `wireBackupButtons(autosave)` (js/ui/settings-modal.js)
- `tests/e2e/settings-autosave.spec.js`

<verification>
Run `npx playwright test tests/e2e/settings-autosave.spec.js` and `node --test tests/integration/security-smoke.test.js` — both pass.
</verification>

<success_criteria>
- The Backup fieldset is reachable from the gear icon → Settings, in the correct state for both a supporting and a non-supporting browser.
- The fallback Export button and the "Choose folder" flow are both provably functional in a real browser (E2E), not just wired in source.
</success_criteria>

<output>
Create `.planning/phases/24-autosave/24-03-SUMMARY.md` when done.
</output>
