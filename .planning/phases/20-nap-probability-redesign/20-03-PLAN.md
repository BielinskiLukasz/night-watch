---
phase: 20-nap-probability-redesign
plan: 03
type: execute
gap_closure: true
wave: 3
depends_on: ["20-01", "20-02"]
files_modified:
  - js/ui/today-screen.js
  - js/ui/metrics-screen.js
  - tests/integration/forecast-ordering.test.js
  - tests/unit/time.test.js
autonomous: true
requirements: [NAP-01, NAP-02, NAP-03, NAP-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "today-screen.js's forecast()/tifForecast()/napProbability() calls all receive an oldest-first day-records array (reversed from daysBySubjectiveNight()'s real newest-first output), so forecast.js's/forecast-tif.js's internal window slice (dayRecords.slice(-N)) and 'today' extraction (dayRecords[dayRecords.length-1]) both resolve to the true most-recent day, not the oldest."
    - "forecastDays itself (newest-first, forecastDays[0] === today) is UNCHANGED and still feeds napStreak, renderForecastSection(), and selectNextEvent() — those are order-independent or explicitly rely on index-0-is-today, and must not be reversed."
    - "metrics-screen.js's tifForecast() override call passes the same oldest-first ordering as the fixed today-screen.js, so the Metrics screen's 'Historic ... band' columns stay consistent with the Today screen's historic band (no new divergence introduced as a side effect of the CR-01 fix)."
    - "today-screen.js's todayDateStr (used to find todayDayRecord) is computed from local wall-clock date components (formatLocalISO), never from toISOString() (UTC), so it matches day.date's local-wall-clock convention at every hour and every UTC offset — including evening hours in negative-UTC-offset zones."
    - "A regression test built on eventLog.daysBySubjectiveNight()'s REAL output (not hand-built oldest-first fixtures) proves forecast()'s, tifForecast()'s, and sleepDebtProxy()'s selection/pairing is correct only once reversed, and wrong (selects/pairs the oldest days) when fed the raw newest-first array — so this class of ordering-contract mismatch is caught by CI going forward."
  artifacts:
    - path: "js/ui/today-screen.js"
      provides: "forecastDaysOldestFirst reversal feeding forecast()/tifForecast()/napProbability(); local-date todayDateStr fix via formatLocalISO"
    - path: "js/ui/metrics-screen.js"
      provides: "tifForecast() override call updated to pass reversedDays instead of days, restoring parity with the corrected Today-screen ordering"
    - path: "tests/integration/forecast-ordering.test.js"
      provides: "New regression tests proving forecast()/tifForecast()/sleepDebtProxy() require oldest-first input, using real eventLog.daysBySubjectiveNight() output"
    - path: "tests/unit/time.test.js"
      provides: "New regression test proving a local-date computation (formatLocalISO) diverges correctly from a UTC computation (toISOString) at negative UTC offsets"
  key_links:
    - from: "js/ui/today-screen.js (render)"
      to: "js/lib/forecast.js (forecast, napProbability) / js/lib/forecast-tif.js (tifForecast)"
      via: "forecastDaysOldestFirst argument"
      pattern: "forecastDaysOldestFirst"
    - from: "js/ui/metrics-screen.js (render)"
      to: "js/lib/forecast-tif.js (tifForecast)"
      via: "reversedDays argument"
      pattern: "tifForecast\\(reversedDays"
---

<objective>
Close the two BLOCKER findings from `20-REVIEW.md`'s code review of Plans 20-01/20-02:
CR-01 (`forecast()`/`tifForecast()`/`napProbability()` are fed newest-first day records in
`today-screen.js` when their internal windowing/pairing logic requires oldest-first) and CR-02
(today's date lookup in `today-screen.js` uses UTC `toISOString()` against local-wall-clock
`day.date` keys).

Purpose: both bugs directly undermine the Phase 20 deliverables (nap-probability score,
split-bedtime blend, wake-anchored nap start/end) for realistic production data — any user
with more than one rolling window of history, and any user not at UTC+0.

Output: `today-screen.js` and `metrics-screen.js` correctly order day records for
`forecast()`/`tifForecast()`/`napProbability()`; `today-screen.js` computes today's date from
local wall-clock components; new regression tests pin both fixes using real bucketer output
(not hand-built fixtures) so this class of contract mismatch is caught by CI going forward.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-nap-probability-redesign/20-REVIEW.md
@.planning/phases/20-nap-probability-redesign/20-CONTEXT.md
@js/ui/today-screen.js
@js/ui/metrics-screen.js
@js/lib/forecast.js
@js/lib/forecast-tif.js
@js/lib/metrics.js
@js/lib/day-bucket.js
@js/lib/time.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Fix CR-01: reverse forecastDays to oldest-first before forecast()/tifForecast()/napProbability() (today-screen.js), restore metrics-screen.js parity</name>
  <files>js/ui/today-screen.js, js/ui/metrics-screen.js, tests/integration/forecast-ordering.test.js</files>
  <read_first>
    js/ui/today-screen.js lines 844-953 (render(): forecastDays construction at line 860/867-879, napStreak loop 912-920, napProbability() call 924-930, forecastContext 932-940, tifForecast/forecast dispatch 943-945, renderForecastSection call 952);
    js/ui/metrics-screen.js lines 640-746 (render(): reversedDays at line 666, tifForecast override at lines 704/710-730, its stale "pass `days` NOT reversedDays" comment at lines 725-728);
    js/lib/forecast.js lines 647-664 (window = dayRecords.slice(dayRecords.length-windowDays) — requires oldest-first) and lines 704-709 (lastBedtimeHHMM backward search — requires oldest-first);
    js/lib/forecast-tif.js lines 488-497 (window = dayRecords.slice(-tifRollingDays) — requires oldest-first) and lines 565/595-596/623 (dayRecords[dayRecords.length-1] used as "today" for todaySleepDuration/todayActualNapStart/todayActualWake/bedtimeDayRecord fallback — independently confirms tifForecast() requires oldest-first, since "today" must be the LAST element);
    js/lib/metrics.js lines 417-528 (dayOfWeekAverages: prevDay = dayRecords[i-1], requires oldest-first) and lines 564-586 (sleepDebtProxy: same prevDay pairing, requires oldest-first — its own JSDoc at line 558 says "oldest-first");
    js/lib/day-bucket.js lines 209-235 (bucketBy sorts "newest-first" — the actual real-world output shape daysBySubjectiveNight()/daysByCalendar() return).
  </read_first>
  <behavior>
    Add a new file tests/integration/forecast-ordering.test.js with three regression tests, all
    built on a REAL eventLog (createEventLog + createStorageMemory + createClockFixed, following
    the exact pattern already used in tests/integration/forecast-flow.test.js) and REAL
    eventLog.daysBySubjectiveNight() output — never hand-built oldest-first fixtures — so each
    test exercises the actual newest-first array shape production code receives.

    Shared fixture helper: add 10 consecutive daily wake-only events (no bedtime, no napStart)
    via addAt(eventLog, 'wake', ...) at dates '2025-03-01' through '2025-03-10' (oldest to
    newest), with wake times '06:00', '06:10', '06:20', '06:30', '06:40', '06:50', '07:00',
    '07:10', '07:20', '07:30' respectively (each 10 minutes later than the previous day — a
    clear monotonic recency trend). Call eventLog.daysBySubjectiveNight() once to get `rawDays`
    (the real newest-first array: 2025-03-10 first, 2025-03-01 last) and build `reversedDays`
    as `[...rawDays].reverse()` (oldest-first: 2025-03-01 first, 2025-03-10 last).

    - Test 1 ("forecast() rolling window: real bucketer output must be reversed to select the
      MOST RECENT windowDays days"): call `forecast(reversedDays, { windowDays: 5, minDays: 0,
      maxDelta: 1440 })` and `forecast(rawDays, { windowDays: 5, minDays: 0, maxDelta: 1440 })`.
      Assert `forecast(reversedDays, ...).wake.central === '07:10'` (the correct window is the 5
      MOST RECENT days: 06:50/07:00/07:10/07:20/07:30, median = '07:10') and
      `forecast(rawDays, ...).wake.central === '06:20'` (the buggy window, if fed unreversed
      real output, is the 5 OLDEST days: 06:00/06:10/06:20/06:30/06:40, median = '06:20') —
      proving the two orderings produce materially different, exactly-predictable results, and
      that only the reversed (oldest-first) call matches "most recent 5 days" semantics.
    - Test 2 ("tifForecast() rolling window: same oldest-first requirement as forecast()"):
      call `tifForecast(reversedDays, { minDays: 0, tifRollingDays: 5, trimPct: 0,
      precisionTarget: 60 })` and `tifForecast(rawDays, { minDays: 0, tifRollingDays: 5,
      trimPct: 0, precisionTarget: 60 })`. With no bedtime/napStart data, tifForecast()'s only
      active wake-prediction window is the historic wake-up band computed via a
      no-trim trimmedMinMax over the same 5-day slice as Test 1, so the exact same expected
      values apply: assert `tifForecast(reversedDays, ...).wake.central === '07:10'` and
      `tifForecast(rawDays, ...).wake.central === '06:20'`. This directly answers the code
      review's "verify whether tifForecast() has the same expectation... do not assume; check
      it" instruction — confirmed via its own `dayRecords.slice(-tifRollingDays)` window slice
      AND its `dayRecords[dayRecords.length-1]` "today" extraction (used for
      todaySleepDuration/todayActualNapStart/todayActualWake), both of which require
      oldest-first input identically to forecast().
    - Test 3 ("sleepDebtProxy() overnight pairing: real bucketer output must be reversed for
      correct bedtime-to-wake pairing"): using a SEPARATE 8-day eventLog fixture (dates
      '2025-02-01' through '2025-02-08'), add both a wake and a bedtime event per day:
      2025-02-01 wake 07:00 / bedtime 21:00; 02-02 wake 06:30 / bedtime 21:30; 02-03 wake 07:15
      / bedtime 20:45; 02-04 wake 06:45 / bedtime 22:00; 02-05 wake 07:30 / bedtime 21:15;
      02-06 wake 07:00 / bedtime 20:30; 02-07 wake 06:15 / bedtime 21:45; 02-08 wake 07:45 /
      bedtime 21:00. Get `rawDays8` via eventLog.daysBySubjectiveNight() and
      `reversedDays8 = [...rawDays8].reverse()`. Assert
      `sleepDebtProxy(reversedDays8, 7, 600) === 105` (the correct oldest-first pairing:
      prevDay.bedtime -> day.wake across the 7 qualifying overnight pairs, sum of
      (600-actual) = 105) and `sleepDebtProxy(rawDays8, 7, 600) === 150` (the buggy pairing
      that results from feeding real newest-first output directly, which pairs each day's
      bedtime with the WRONG adjacent day's wake) — pinning the exact numeric divergence this
      class of bug produces, since this is the same prevDay-pairing mechanism
      `dayOfWeekAverages()` uses and `napProbability()`'s sleepDebtSignal (D-09, Plan 20-01)
      depends on.
  </behavior>
  <action>
    Create tests/integration/forecast-ordering.test.js implementing exactly the three tests
    described in <behavior>, importing `forecast` from '../../js/lib/forecast.js',
    `tifForecast` from '../../js/lib/forecast-tif.js', `sleepDebtProxy` from
    '../../js/lib/metrics.js', and `createEventLog`/`createStorageMemory`/`createClockFixed`
    the same way tests/integration/forecast-flow.test.js already does (a fixed clock value is
    irrelevant here since these fixtures only add events at explicit historical dates; any
    fixed 'now' after 2025-03-10 works). Use `node:test`'s `describe`/`it` and
    `node:assert/strict`, matching this repo's existing test-file conventions.

    In js/ui/today-screen.js: immediately after the existing `const todayWeekday = new
    Date().getDay();` line (D-07, Plan 20-02) and before the `napProbability(forecastDays,
    snap, {...})` call, add a new `const forecastDaysOldestFirst = [...forecastDays].reverse();`
    with a comment explaining that forecast()/tifForecast()/napProbability() all require
    oldest-first input (their internal rolling-window slice and "most recent day" extraction
    assume the array's LAST element is the most recent day) while `forecastDays` itself stays
    newest-first because the napStreak loop above it, and the later `renderForecastSection()`
    /`selectNextEvent()` call, both correctly rely on `forecastDays[0]` being today. Change the
    `napProbability(forecastDays, snap, {...})` call's first argument to
    `forecastDaysOldestFirst`. Change the `tifForecast(forecastDays, snap, activityLog,
    isNoNapDay)` call's first argument to `forecastDaysOldestFirst`. Change the `forecast(
    forecastDays, snap, forecastContext)` call's first argument to `forecastDaysOldestFirst`.
    Do NOT change the `renderForecastSection(predictions, snap, forecastDays, ...)` call at the
    end of render() — it must keep receiving the newest-first `forecastDays` because
    `selectNextEvent()` (called inside it) iterates every record's `.allEvents` and compares
    `.at` strings directly, which is order-independent, and any future newest-first-dependent
    logic in that render path must not be silently broken by this fix.

    In js/ui/metrics-screen.js: change the `tifForecast(days, snap, activityLog)` call (inside
    the `if (isTif && tifTrimmedStats)` block) to `tifForecast(reversedDays, snap,
    activityLog)` — `reversedDays` already exists in this function (built earlier via
    `[...days].reverse()` for `aggregateMetrics()`). Rewrite the preceding "IMPORTANT: pass
    `days`... NOT reversedDays" comment block to explain the corrected invariant: today-screen.js
    now also reverses its day-records array before calling tifForecast() (per this same gap
    closure), so this override must pass the same oldest-first ordering to keep matching what
    Today's screen shows — passing the newest-first `days` here would reintroduce exactly the
    divergence this override was originally written to prevent (commit 50d491c).
  </action>
  <reversibility rating="reversible">Both changes correct an internal data-ordering contract already implied by forecast.js/forecast-tif.js/metrics.js's own JSDoc and slicing logic; no external interface, storage schema, or user-facing behavior contract changes — only which historical days feed the existing prediction algorithms.</reversibility>
  <verify>
    <automated>node --test tests/integration/forecast-ordering.test.js && npm run test:unit</automated>
    <fails_when>exit code is non-zero, or any TAP line reads "not ok", for either command</fails_when>
    <human-check>Load the app (npm run serve), log more than 7 days of wake/bedtime/nap history with a clear recent trend (e.g., wake times shifting noticeably later over the most recent 2-3 days), and confirm the Today screen's predictions track the RECENT trend rather than the earliest logged days. If using the TIF algorithm, also open the Metrics screen and confirm its "Historic ... band" columns still match the Today screen's historic band values (no new divergence from the metrics-screen.js parity fix).</human-check>
  </verify>
  <acceptance_criteria>
    - today-screen.js computes `forecastDaysOldestFirst` and passes it (not `forecastDays`) to `napProbability()`, `tifForecast()`, and `forecast()`
    - today-screen.js's `renderForecastSection(...)` call still receives the unreversed `forecastDays`
    - metrics-screen.js's `tifForecast(...)` override call passes `reversedDays`, not `days`
    - tests/integration/forecast-ordering.test.js's three new tests pass with the exact values derived in `<behavior>`
    - `npm run test:unit` passes with no regressions
  </acceptance_criteria>
  <done>forecast()/tifForecast()/napProbability() in today-screen.js all consume an oldest-first day-records array so their rolling windows and "today" extraction resolve to the true most-recent history; metrics-screen.js's TIF historic-band override stays consistent with Today screen's ordering; new regression tests pin both the bug's numeric signature and the fix's correctness using real bucketer output.</done>
</task>

<task type="auto" tdd="true">
  <name>Fix CR-02: today's date lookup must use local wall-clock date, not UTC toISOString()</name>
  <files>js/ui/today-screen.js, tests/unit/time.test.js</files>
  <read_first>
    js/ui/today-screen.js lines 41-43 (formatLocalISO already imported from '../lib/time.js') and lines 893-902 (todayDateStr computation and todayDayRecord lookup);
    js/lib/time.js lines 53-68 (formatLocalISO: local-getter-based, never UTC — the D-16 wall-clock invariant) and its header comment lines 12-15;
    js/lib/day-bucket.js lines 82-85 (calendarKey: `at.slice(0,10)`, a local-wall-clock date prefix — confirms `day.date` values are always local, never UTC).
  </read_first>
  <behavior>
    Add a new test to tests/unit/time.test.js's existing `describe('formatLocalISO', ...)` block
    proving the exact bug class: construct a Date representing 2026-01-15 at 20:00 LOCAL
    wall-clock time in a negative-UTC-offset zone, and show that `formatLocalISO(...).slice(0,
    10)` correctly reports the local calendar date while a UTC-based computation
    (`.toISOString().slice(0, 10)`) would report the NEXT day instead — reproducing the exact
    divergence CR-02 describes ("evening hours in negative-UTC-offset zones"). Since this
    divergence only exists at non-zero UTC offsets (and the test-runner's own machine timezone
    is not guaranteed to be negative-offset), set `process.env.TZ = 'America/New_York'` at the
    top of the test body BEFORE constructing any Date, restore the original `process.env.TZ` in
    a `finally` block, and construct the Date via the local-component constructor
    `new Date(2026, 0, 15, 20, 0)` (year, 0-indexed month, day, hour, minute) — which Node
    interprets using whatever `process.env.TZ` is active at construction time. Assert
    `formatLocalISO(d).slice(0, 10) === '2026-01-15'` and
    `d.toISOString().slice(0, 10) === '2026-01-16'` (20:00 EST on Jan 15 is 01:00 UTC on Jan 16),
    and assert the two computed date strings are NOT equal — pinning both the correct behavior
    and the exact wrongness the removed `toISOString()` call would have produced.
  </behavior>
  <action>
    In tests/unit/time.test.js, add the test described in `<behavior>` inside the existing
    `describe('formatLocalISO', ...)` block, wrapping the TZ mutation and Date construction in a
    try/finally that restores `process.env.TZ` to its original value (captured at the top of the
    test) regardless of assertion outcome, so this test cannot leak a changed timezone into any
    test file that runs after it in the same process.

    In js/ui/today-screen.js: replace the `const todayDateStr = new Date().toISOString().slice(0,
    10);` line with `const todayDateStr = formatLocalISO(new Date()).slice(0, 10);` (keep the
    trailing `// gsd:allow-ui-clock` marker — this is still a display-only, UI-layer clock read,
    just no longer routed through a UTC-producing method). Rewrite the preceding comment to state
    that `formatLocalISO` is required here specifically because `day.date` values are always
    local-wall-clock date strings (per js/lib/day-bucket.js's `calendarKey`/`subjectiveNightKey`),
    and a UTC-based computation would silently mismatch `todayDayRecord` for part of the day at
    any non-zero UTC offset.
  </action>
  <reversibility rating="reversible">Single-expression fix using an already-imported, already-tested helper (formatLocalISO); no schema, API, or interface change.</reversibility>
  <verify>
    <automated>node --test tests/unit/time.test.js && npm run test:unit</automated>
    <fails_when>exit code is non-zero, or any TAP line reads "not ok", for either command</fails_when>
    <human-check>If possible, temporarily set your OS/browser timezone to a negative-UTC-offset zone (e.g., US Eastern), load the app in the evening (local time), log a wake/nap/bedtime event for "today", and confirm the Today screen still finds and reflects today's record (isIntenseToday / nap-start-logged / wake-anchored predictions) rather than silently treating today as having no record.</human-check>
  </verify>
  <acceptance_criteria>
    - today-screen.js's `todayDateStr` is computed via `formatLocalISO(new Date()).slice(0, 10)`, not `.toISOString()`
    - The new tests/unit/time.test.js test demonstrates the exact local-vs-UTC divergence for a negative-UTC-offset evening timestamp and restores `process.env.TZ` afterward
    - `npm run test:unit` passes with no regressions
  </acceptance_criteria>
  <done>today-screen.js's "today" date lookup matches day.date's local-wall-clock convention at every hour and every UTC offset; a regression test pins the exact divergence class this fix eliminates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Both fixes correct internal data-ordering/date-computation contracts using the app's own already-trusted, previously-validated local `dayRecords`/`Date` state (offline-first, no network I/O, no new user-input parsing, no storage schema change). No new trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-05 | Tampering | day-records ordering (today-screen.js / metrics-screen.js) | low | accept | Pure local reordering of the app's own already-validated day records; no external input reaches this path, and the new regression tests pin the corrected contract. |
| T-20-06 | Information Disclosure | today-screen.js's local-date computation | low | accept | `formatLocalISO(new Date())` reads only the device's own local clock (already displayed elsewhere in the app); no network transmission, no new PII surface. |
</threat_model>

<verification>
Run `node --test tests/integration/forecast-ordering.test.js` and `node --test tests/unit/time.test.js` after their respective tasks, then `npm run test:unit` after both tasks. All must exit 0 with no `not ok` lines. Perform both human-check items once at the end (load the app; ideally also check a non-UTC-offset scenario for CR-02).
</verification>

<success_criteria>
- CR-01 closed: forecast()/tifForecast()/napProbability() in today-screen.js all receive oldest-first day records; metrics-screen.js's TIF historic-band override stays consistent with Today screen.
- CR-02 closed: today-screen.js's "today" date lookup uses local wall-clock date (formatLocalISO), never UTC (toISOString).
- New regression tests (tests/integration/forecast-ordering.test.js, tests/unit/time.test.js) pin both fixes using real bucketer output / real timezone-divergence scenarios, not just hand-built fixtures.
- Full `npm run test:unit` suite passes with no regressions.
</success_criteria>

<output>
Create `.planning/phases/20-nap-probability-redesign/20-03-SUMMARY.md` when done
</output>
