---
phase: 20-nap-probability-redesign
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - js/lib/forecast.js
  - js/ui/today-screen.js
  - tests/unit/forecast.test.js
autonomous: true
requirements: [NAP-01, NAP-02, NAP-03, NAP-04]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "forecast.js's PRED-18/19 bedtime-blend branch reads napProbabilityScore.score (not a bare number) at both the not-null guard and the ratio calculation; a bare null context field still falls through to PRED-10/overall exactly as before."
    - "today-screen.js computes todayWeekday (0=Sun..6=Sat via new Date().getDay()) and threads it into the napProbability() call's context, following the same pre-computed-context pattern as todayWakeHHMM/todayNapStartHHMM (Phase 19 D-13)."
    - "Both nap-probability UI render sites (hero card and prediction card) display the percentage from prediction.napProbabilityScore.score, never rendering the literal string '[object Object]%'."
    - "The existing PRED-18/19 blend-test numeric expectations (e.g. '21:20', '21:30' central values) are unchanged after the object-shape migration."
  artifacts:
    - path: "js/lib/forecast.js"
      provides: "D-04 consumption sites updated to read napProbabilityScore.score"
    - path: "js/ui/today-screen.js"
      provides: "todayWeekday computation/threading and .score-based UI rendering (D-04, D-07)"
    - path: "tests/unit/forecast.test.js"
      provides: "Updated blend-test fixtures using the object shape for napProbabilityScore"
  key_links:
    - from: "js/ui/today-screen.js (napProbability call site)"
      to: "js/lib/forecast.js (napProbability)"
      via: "todayWeekday context field"
      pattern: "todayWeekday"
    - from: "js/ui/today-screen.js (UI render sites)"
      to: "napProbability()'s return object"
      via: ".score property access"
      pattern: "napProbabilityScore\\.score"
---

<objective>
Wire the new `napProbability()` object return shape (built in Plan 20-01) into its consumers:
`forecast.js`'s PRED-18/19 bedtime blend and `today-screen.js`'s call site + two UI render sites
(D-03/D-04), plus thread the new `todayWeekday` context field (D-07).

Purpose: `napProbability()` now returns `{score, signalsUsed, confidence}` instead of a bare
number — every reader must switch to `.score` or the app renders `"[object Object]% chance of
nap today"` and the bedtime blend divides `undefined/100` into `NaN`.

Output: `forecast.js` and `today-screen.js` correctly consume the new shape; no visible or
behavioral regression in the bedtime blend or the nap-probability UI text.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-nap-probability-redesign/20-CONTEXT.md
@.planning/phases/20-nap-probability-redesign/20-PATTERNS.md
@js/lib/forecast.js
@js/ui/today-screen.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>forecast.js D-04 consumption sites + blend-test fixture updates</name>
  <files>js/lib/forecast.js, tests/unit/forecast.test.js</files>
  <read_first>js/lib/forecast.js (context destructuring ~line 690-699, bedtime-routing branch ~line 745-800, JSDoc ~line 630-636), tests/unit/forecast.test.js (napProbabilityScore blend tests, describe block covering PRED-18/19 split-bedtime routing), .planning/phases/20-nap-probability-redesign/20-CONTEXT.md D-04</read_first>
  <behavior>
    - When `context.napProbabilityScore` is a real object (e.g. `{score: 70, ...}`), the blend branch fires exactly as it does today (same central/min/max math), reading `.score` instead of the bare value.
    - When `context.napProbabilityScore` is `null` or absent, behavior is unchanged: falls through to PRED-10/overall.
  </behavior>
  <action>
    In `js/lib/forecast.js`: update the JSDoc line documenting `context.napProbabilityScore` (currently describing a bare `number|null`) to describe the new `{score, signalsUsed, confidence}` object shape. Leave the destructuring default as `napProbabilityScore = null` (an omitted/absent context field still defaults to `null` — no change needed there). Change the branch guard from `else if (napProbabilityScore !== null)` to `else if (napProbabilityScore !== null && napProbabilityScore.score !== null)`. Change `const ratio = napProbabilityScore / 100;` to `const ratio = napProbabilityScore.score / 100;`. Update the trailing comment describing the fallthrough condition to mention both the object being absent/null AND `.score` being null.

    In `tests/unit/forecast.test.js`: find the two tests in the PRED-18/19 split-bedtime-blend describe block that currently pass `napProbabilityScore: 70` as a raw number in their `context` fixture (the tests titled "napProbabilityScore=70 with both sub-series >= minDays: blends central proportionally" and "score present but both sub-series thin (< minDays): falls back to overall without offset shift") — change `napProbabilityScore: 70` to `napProbabilityScore: { score: 70, signalsUsed: [], confidence: 'partial' }` in both. Do NOT touch any test that already passes `napProbabilityScore: null` (several occurrences across the PRED-18/19 and PRED-20/21/22 describe blocks) — a bare `null` remains valid input and the updated guard handles it identically to today.
  </action>
  <reversibility rating="costly">D-04: touches already-shipped Phase 19 code (forecast.js's split-bedtime blend). CONTEXT.md rates this costly, not one-way — no checkpoint is inserted.</reversibility>
  <verify>
    <automated>npm run test:unit</automated>
    <fails_when>exit code is non-zero, or the node:test end-of-run summary line reports a fail count greater than 0</fails_when>
  </verify>
  <acceptance_criteria>
    - forecast.js's bedtime-blend branch checks `napProbabilityScore.score !== null` (not a bare `!== null` on the whole value)
    - forecast.js's ratio calculation reads `napProbabilityScore.score`
    - Both updated test fixtures pass an object literal (not a bare number) for `napProbabilityScore`
    - `npm run test:unit` passes
  </acceptance_criteria>
  <done>forecast.js's PRED-18/19 bedtime blend correctly unwraps the object shape at all four D-04 call sites; existing blend-test numeric expectations (e.g. "21:20" central) are unchanged and pass.</done>
</task>

<task type="auto" tdd="true">
  <name>today-screen.js wiring: todayWeekday (D-07) + .score-based UI rendering (D-04)</name>
  <files>js/ui/today-screen.js</files>
  <read_first>js/ui/today-screen.js (napProbability call site + context threading ~line 900-949, hero-card render ~line 150-172, prediction-card render ~line 270-290), .planning/phases/20-nap-probability-redesign/20-CONTEXT.md D-04/D-07, .planning/phases/20-nap-probability-redesign/20-PATTERNS.md today-screen.js section</read_first>
  <behavior>
    - `todayWeekday` is computed once per render pass alongside the existing `currentHour`/`currentMinute` (`new Date().getDay()`, 0=Sun..6=Sat, matching `dayOfWeekAverages()`'s convention) and threaded into the `napProbability()` call's context argument only (not into `forecastContext`, since `forecast()` itself never consumes `todayWeekday`).
    - Both nap-probability render sites display `${prediction.napProbabilityScore.score}% chance of nap today` / `'0% — nap window closed'`, reading `.score`, never the bare object.
  </behavior>
  <action>
    In `js/ui/today-screen.js`: add `const todayWeekday = new Date().getDay(); // gsd:allow-ui-clock` immediately after the existing `currentMinute` computation. Add `todayWeekday` as a new key in the `napProbability(forecastDays, snap, {...})` call's context object, alongside `currentHour, currentMinute, napStreak` — remove `todayWakeHHMM` from THIS specific call's context object (Plan 20-01 dropped it from `napProbability()`'s own destructuring, so passing it here is now inert; `todayWakeHHMM` remains computed and still used elsewhere in `forecastContext` for `forecast()`, unaffected by this change). Then update both UI render sites: change `prediction.napProbabilityScore === 0` to `prediction.napProbabilityScore.score === 0` and `` `${prediction.napProbabilityScore}% chance of nap today` `` to `` `${prediction.napProbabilityScore.score}% chance of nap today` `` — at both the hero-card site and the prediction-card site.
  </action>
  <verify>
    <automated>npm run test:unit</automated>
    <fails_when>exit code is non-zero, or the node:test end-of-run summary line reports a fail count greater than 0 — no dedicated today-screen.js unit tests exist in this codebase; this run is the regression safety net covering forecast.js/metrics.js callers</fails_when>
    <human-check>Load the app (npm run serve), log enough sleep history to clear cold-start, and confirm the Today screen's nap-probability text reads "N% chance of nap today" or "0% — nap window closed" — never "[object Object]% chance of nap today".</human-check>
  </verify>
  <acceptance_criteria>
    - `todayWeekday` is computed via `new Date().getDay()` and passed into the `napProbability()` call's context
    - Both UI render sites read `.score` (not the bare object) in both the `=== 0` check and the percentage interpolation
    - `npm run test:unit` passes with no regressions
  </acceptance_criteria>
  <done>today-screen.js threads todayWeekday into napProbability() (D-07) and both nap-probability render sites correctly display the percentage from the new object shape (D-03/D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan wires an already-computed local value (`napProbability()`'s return object, built entirely from the user's own local `dayRecords`/`settings`) into two existing UI render sites and one existing blend calculation. No new trust boundary, no new user input, no network I/O. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-03 | Tampering | UI render sites (today-screen.js) | low | accept | Render sites use `textContent`-based helpers in `js/ui/dom.js` (existing XSS-guard convention, unchanged by this plan); the interpolated value is a locally-computed integer percentage, never user-controlled string data. |
| T-20-04 | Denial of Service | `napProbabilityScore.score / 100` division (forecast.js) | low | accept | Guarded by the `!== null` check before division; a malformed/absent context object falls through to the existing PRED-10/overall path rather than throwing or producing `NaN` that reaches the UI. |
</threat_model>

## Artifacts this phase produces

- `js/lib/forecast.js` — `napProbabilityScore` context consumption sites (bedtime-blend guard + ratio calc) now read `.score` off the object built in Plan 20-01, instead of treating the field as a bare number
- `js/ui/today-screen.js` — new local `todayWeekday` variable (0=Sun..6=Sat, via `new Date().getDay()`) threaded into the `napProbability()` call's context argument
- `js/ui/today-screen.js` — both nap-probability UI render sites (hero card, prediction card) now read `prediction.napProbabilityScore.score` instead of the bare `prediction.napProbabilityScore`

<verification>
Run `npm run test:unit` after each task; both must exit 0. Manually load the app once at the end (human-check in Task 2) to confirm the nap-probability text renders correctly, since no automated UI test exists for today-screen.js in this codebase.
</verification>

<success_criteria>
- `forecast.js`'s bedtime blend and `today-screen.js`'s call site + both render sites all consume `.score` correctly.
- `todayWeekday` reaches `napProbability()` via the same pre-computed-context pattern as Phase 19's `todayWakeHHMM`.
- `npm run test:unit` passes; nap-probability text never shows `[object Object]`.
</success_criteria>

<output>
Create `.planning/phases/20-nap-probability-redesign/20-02-SUMMARY.md` when done
</output>
