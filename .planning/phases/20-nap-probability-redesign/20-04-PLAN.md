---
phase: 20-nap-probability-redesign
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - js/lib/forecast.js
  - tests/unit/forecast.test.js
autonomous: true
requirements:
  - PRED-18
  - PRED-19
gap_closure: true
gap_ids: [G-20-16]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "Once napProbabilityScore.napWindowClosed is true and today's nap hasn't started, forecast()'s bedtime prediction routes to the pure no-nap-day series (buildBedtimeSeriesNoNapDay) instead of blending in nap-day statistics"
    - "When napWindowClosed is false or absent (nap status still genuinely undetermined), the existing PRED-19/D-05 proportional blend behavior is completely unchanged"
    - "When the no-nap-day sub-window itself is thin (buildBedtimeSeriesNoNapDay returns null), the gate falls through to the existing Step 2 intense-day/overall fallback exactly as the current thin-sub-series fallback already does"
  artifacts:
    - js/lib/forecast.js
    - tests/unit/forecast.test.js
  key_links:
    - "forecast()'s bedtimePred IIFE reads napProbabilityScore.napWindowClosed (already computed by napProbability() per Phase 21 D-01/D-02/D-03) to pick the correct branch"
---

<objective>
Close G-20-16 item 1 (UAT 20-UAT.md, root-cause session .planning/debug/bedtime-nap-same-algo.md): the classic algorithm's PRED-19 bedtime-blend branch in forecast.js currently reads only napProbabilityScore.score, never napProbabilityScore.napWindowClosed, so predictions.bedtime keeps mixing in nap-day statistics even after it is definitively too late for a nap to start today. Gate the blend branch on napWindowClosed so it routes to the pure no-nap-day series once the window has closed.

Purpose: the "undetermined -> blend" design (D-05) has no corresponding "definitively no-nap -> pure no-nap-day series" transition once the clock resolves the uncertainty; this plan adds it.

Output: forecast.js's bedtimePred IIFE gated correctly; new unit tests pinning the closed-window routing; all pre-existing PRED-18/19 tests still passing unmodified.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-nap-probability-redesign/20-UAT.md
@.planning/debug/bedtime-nap-same-algo.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tracer: gate PRED-19 bedtime blend on napWindowClosed — RED then GREEN (G-20-16 item 1)</name>
  <files>js/lib/forecast.js, tests/unit/forecast.test.js</files>
  <read_first>
    - js/lib/forecast.js lines 704-712: destructured context fields (napProbabilityScore default null) feeding the bedtimePred IIFE
    - js/lib/forecast.js lines 758-778: bedtimeTimes + selectBedtime(result) helper, shared by bedtimePred and bedtimeAfterWakePred
    - js/lib/forecast.js lines 782-823: the bedtimePred IIFE itself — Step 1 split-series selection (napStartLogged branch at 784-790, the PRED-19/D-05 blend branch at 791-805 that this task changes), Step 2 intense-day shift, and the final overall fallback
    - js/lib/forecast.js lines 825-834: bedtimeAfterWakePred — the exact buildBedtimeSeriesNoNapDay(window, settings) + selectBedtime(...) call this fix reuses inside the blend branch
    - js/lib/forecast.js lines 910-935: napProbability()'s return-shape docstring — napWindowClosed semantics (decoupled from score, Phase 21 D-01/D-02/D-03)
    - tests/unit/forecast.test.js describe('forecast() split bedtime routing (PRED-18/19)') block (around lines 2693-2784): splitSettings fixture, napDayRecord/noNapDayRecord helpers, and the existing 'napProbabilityScore=70 with both sub-series >= minDays: blends central proportionally' test this fix must not break (it never sets napWindowClosed, so it stays on the unchanged/false path)
  </read_first>
  <behavior>
    - napStartLogged=false, napProbabilityScore={score:70, signalsUsed:[], confidence:'partial', napWindowClosed:true}, both nap-day and no-nap-day sub-series available (>= minDays) -> bedtime.central equals the result of running buildBedtimeSeriesNoNapDay(window, settings) through selectBedtime — NOT the score-blended value the pre-fix code would have produced
    - Identical inputs but napWindowClosed:false (or the field entirely absent, matching every pre-existing fixture in the file) -> completely unchanged: bedtime.central is still the score-blended value; the pre-existing 'blends central proportionally' test keeps passing byte-for-byte
    - napWindowClosed:true but buildBedtimeSeriesNoNapDay(window, settings) itself returns null (thin no-nap sub-window) -> falls through to Step 2 (intense-day check) / overall percentiles exactly as the current "one or both sub-series null" fallback already does — no new dead end, no thrown error
  </behavior>
  <action>
    RED phase — in tests/unit/forecast.test.js, extend the existing describe('forecast() split bedtime routing (PRED-18/19)') block with two new it cases covering the first and third behaviors above (the second behavior is already pinned by the existing 'blends central proportionally' test and needs no new assertion, only confirmation it still passes). Reuse napDayRecord()/noNapDayRecord()/splitSettings exactly as the existing tests do; construct napProbabilityScore literals that explicitly set napWindowClosed: true for the new cases, and reuse the existing 8-record and 4-record fixtures respectively (mirroring the existing 3-nap/3-no-nap and 2-nap/2-no-nap record counts already used in that describe block) so the closed-window case is directly comparable against the existing blend case's expected central value. Run node --test tests/unit/forecast.test.js and confirm the new "napWindowClosed:true" assertion fails against current code (the blend still fires because the branch never reads napWindowClosed).

    GREEN phase — in forecast.js's bedtimePred IIFE, inside the `else if (napProbabilityScore !== null && napProbabilityScore.score !== null)` branch (line 791), add an inner conditional on napProbabilityScore.napWindowClosed before the existing blend logic. When napWindowClosed is true: call buildBedtimeSeriesNoNapDay(window, settings) directly (do not compute napDaySeries at all in this sub-branch — it is no longer needed once the window is closed); if the result is non-null, return selectBedtime(result); if the result is null, do not return anything from this sub-branch (let control fall through to Step 2 / the final overall fallback, exactly like the existing "one or both sub-series null" comment already documents for the undetermined case). When napWindowClosed is false or undefined, keep the existing blend logic (napDaySeries + noNapDaySeries + ratio blend) completely unchanged, character-for-character.

    Re-run node --test tests/unit/forecast.test.js and confirm every test in the file passes, including the untouched pre-existing PRED-18/19 tests and the two new ones.
  </action>
  <verify>
    <automated>node --test tests/unit/forecast.test.js</automated>
    <fails_when>non-zero exit code, or any "not ok" TAP line anywhere in the file, especially inside the 'forecast() split bedtime routing (PRED-18/19)' describe block</fails_when>
  </verify>
  <done>
    forecast()'s bedtime prediction no longer blends in nap-day statistics once napProbabilityScore.napWindowClosed is true and today's nap has not started; the undetermined-blend path (napWindowClosed false/absent) is byte-for-byte unchanged; all pre-existing forecast.test.js assertions pass unmodified alongside the two new ones.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| (none new) | This plan only changes which already-validated internal sub-series a pure function selects; no new external input surface, no new DOM output path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-04-01 | Tampering | forecast.js bedtimePred napWindowClosed gate | low | accept | Operates only on already-schema-validated numeric/HH:MM day-record fields (db-shape.js validation upstream); output still flows only into the existing minutesToTime conversion and the pre-existing XSS-safe textContent rendering path in today-screen.js — no new sink introduced |
</threat_model>

<verification>
Run `node --test tests/unit/forecast.test.js` — all subtests pass, exit 0.
Run `npm run test:unit` for a broader regression sweep (forecast.js is imported by forecast-tif.js, forecast-utils.js, today-screen.js, and metrics-screen.js's override path) to confirm no ripple effects.
Confirm the two new it cases explicitly set `napWindowClosed: true` and assert against `buildBedtimeSeriesNoNapDay`'s own output, not a hand-recomputed literal, so the test stays coupled to the real function.
</verification>

<success_criteria>
The PRED-19 blend branch in forecast.js consults napWindowClosed before blending; closed-window routing is proven by a passing automated test; the pre-existing undetermined-blend behavior is unchanged and still passes its original test.
</success_criteria>

<output>
Create `.planning/phases/20-nap-probability-redesign/20-04-SUMMARY.md` when done
</output>
