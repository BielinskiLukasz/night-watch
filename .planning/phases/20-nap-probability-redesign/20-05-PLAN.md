---
phase: 20-nap-probability-redesign
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - js/lib/forecast-tif.js
  - tests/unit/forecast-tif-nonap.test.js
autonomous: true
requirements:
  - TIF-16
  - PRED-19
gap_closure: true
gap_ids: [G-20-16]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "On a no-nap day (isNoNapDay=true), tifForecast()'s bedtime prediction no longer includes the Activity-after-nap band — it never factors in nap-end-anchored activity statistics for a day that has no nap"
    - "On a no-nap day, a no-nap-day-only historic bedtime band substitutes for the omitted Activity-after-nap band whenever no-nap-day bedtime history is available, mirroring forecast-blend.js's Model 3 substitution exactly (including its no-minDays-gate graceful-degradation contract)"
    - "On a nap day or undetermined day (isNoNapDay=false, the default), the Activity-after-nap band renders exactly as it did before this fix — zero regression"
  artifacts:
    - js/lib/forecast-tif.js
    - tests/unit/forecast-tif-nonap.test.js
  key_links:
    - "tifForecast()'s bedtime block already gates its sibling 'Day-length band' on isNoNapDay (lines ~685-696) — the Activity-after-nap band gate added here follows the identical isNoNapDay parameter, already threaded in via tifForecast(dayRecords, settings, activityLog, isNoNapDay)"
    - "forecast-blend.js lines 552-580 (Model 3) is the reference implementation this fix mirrors"
---

<objective>
Close G-20-16 item 3 (UAT 20-UAT.md, root-cause session .planning/debug/bedtime-nap-same-algo.md): forecast-tif.js's opt-in TIF algorithm adds an "Activity-after-nap band" to its bedtime prediction unconditionally whenever a napEnd anchor resolves — with no isNoNapDay gate at all, unlike its own sibling "Day-length band" a few lines above (which is correctly gated) and unlike forecast-blend.js's Model 3 (which implements the identical no-nap-day substitution correctly). Add the missing gate.

Purpose: on a no-nap day the bedtime intersection should not include a window built from "how long after a nap ends before bedtime" historical durations, since no nap occurred.

Output: forecast-tif.js's bedtime block gated on isNoNapDay with a no-nap-day-only substitute band; new unit tests pinning both the gate and the substitute; all 5 pre-existing forecast-tif-nonap.test.js tests still passing unmodified.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-nap-probability-redesign/20-UAT.md
@.planning/debug/bedtime-nap-same-algo.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tracer: gate Activity-after-nap band on isNoNapDay — RED then GREEN, mirroring forecast-blend.js Model 3 (G-20-16 item 3)</name>
  <files>js/lib/forecast-tif.js, tests/unit/forecast-tif-nonap.test.js</files>
  <read_first>
    - js/lib/forecast-tif.js lines 676-705: the bedtime prediction block — wakeAnchor2/napEndAnchor resolution (677-678), the existing correctly-gated "Day-length band" (685-696, the pattern to mirror), and the ungated "Activity-after-nap band" (699-702, the block this task changes)
    - js/lib/forecast-tif.js lines 168-172: buildHistoricBand(times, trimPct, manualExcluded) — sorts and delegates to trimmedMinMax; returns { min, max, median } or null
    - js/lib/forecast-tif.js line 519: actAfterNap definition (acceptedWindow.map(activityAfterNap).filter(...)) — the data source the ungated band currently uses
    - js/lib/forecast-tif.js line 534: noNapDayWindow definition (acceptedWindow.filter(d => extractTime(d.napStart) === null)) — already computed, reused by the Day-length band's own isNoNapDay gate, and the data source for this fix's substitute band
    - js/lib/forecast-tif.js line 488: tifForecast(dayRecords, settings, activityLog = {}, isNoNapDay = false) — confirms isNoNapDay is already a plain parameter, no new plumbing needed
    - js/lib/forecast-blend.js lines 552-580: Model 3 — the reference implementation. Its else branch (isNoNapDay=true) builds noNapBedtimeTimes by filtering acceptedWindow for extractTime(d.napStart) == null, mapping extractTime(d.bedtime) then timeToMinutes, and passes them through circularTrimmedBand(noNapBedtimeTimes, blendTrimPct, 0) — deliberately with NO minDays gate (per its own docstring: thin history simply omits the band rather than falling back to computing the AA-band anyway)
    - tests/unit/forecast-tif-nonap.test.js in full — makeNoNapFixture(noNapIndices) helper (bedtime is a flat '22:00' for every day, nap days have napStart='13:00'/napEnd='14:30') and defaultNoNapSettings, plus the 5 existing it cases this fix must not break
  </read_first>
  <behavior>
    - isNoNapDay=true, with at least one nap day in the window (so napEndAnchor resolves via resolveAnchor) -> bedtime.sourceWindows must NOT contain a window labelled 'Activity-after-nap band'
    - isNoNapDay=true, with no-nap-day records whose bedtime differs from nap-day records' bedtime -> bedtime.sourceWindows contains a new substitute window (label 'Historic bedtime band (no-nap days)') built only from the no-nap-day bedtimes, and the resulting bedtime.central differs from what it would be if the Activity-after-nap band were still included
    - isNoNapDay=false (regression pin, matching the existing 'isNoNapDay=false -> no-nap substitution absent' test's spirit) -> 'Activity-after-nap band' still appears in bedtime.sourceWindows exactly as before, whenever napEndAnchor resolves
  </behavior>
  <action>
    RED phase — in tests/unit/forecast-tif-nonap.test.js, add a new fixture function (alongside makeNoNapFixture) that varies bedtime between nap-day and no-nap-day records — e.g. nap days keep bedtime '22:00' (matching makeNoNapFixture's existing constant so the two fixtures stay comparable) while no-nap days use a distinctly different bedtime such as '21:00' — with the same napStart/napEnd/wake shape makeNoNapFixture already uses. Add a new describe block with the three it cases from the behavior list above, using tifForecast(fixture, defaultNoNapSettings, {}, true) / (..., false) exactly as the existing tests call it. Run node --test tests/unit/forecast-tif-nonap.test.js and confirm the isNoNapDay=true assertions fail against current code (Activity-after-nap band still present; the new substitute label absent).

    GREEN phase — in forecast-tif.js, wrap the existing block (currently: if napEndAnchor is not null, build actAfterBand from actAfterNap and push it labelled 'Activity-after-nap band') in an `if (!isNoNapDay) { ...exactly the existing block, unchanged... }` / `else { ... }` split, mirroring forecast-blend.js's Model 3 structure exactly. In the new else branch: build a noNapBedtimeTimes array from noNapDayWindow (already defined) by extracting extractTime(d.bedtime) for each record, filtering out nulls, and mapping through timeToMinutes; call buildHistoricBand(noNapBedtimeTimes, trimPct, 0); if the result is non-null, push { label: 'Historic bedtime band (no-nap days)', ...result } into bedtimeLabelledWindows. Deliberately do NOT add a settings.minDays length check on noNapDayWindow before this — forecast-blend.js's Model 3 else-branch has none either, per its own documented graceful-degradation contract (thin history simply omits the substitute band; it never falls back to the AA-band anyway since isNoNapDay is already true).

    Re-run node --test tests/unit/forecast-tif-nonap.test.js and confirm all 5 pre-existing tests plus the 3 new ones pass.
  </action>
  <verify>
    <automated>node --test tests/unit/forecast-tif-nonap.test.js</automated>
    <fails_when>non-zero exit code, or any "not ok" TAP line anywhere in the file</fails_when>
  </verify>
  <done>
    On a no-nap day, tifForecast()'s bedtime prediction no longer includes the Activity-after-nap band; a no-nap-day-only historic bedtime band substitutes when data allows; nap-day/undetermined behavior (isNoNapDay=false) is byte-for-byte unchanged and all pre-existing tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| (none new) | This plan only changes which already-validated internal window a pure function includes in its intersection; no new external input surface, no new DOM output path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-05-01 | Tampering | forecast-tif.js bedtime Activity-after-nap isNoNapDay gate | low | accept | Operates only on already-schema-validated day-record fields; output flows only into buildPrediction's existing intersection arithmetic and the pre-existing XSS-safe rendering path — no new sink introduced |
</threat_model>

<verification>
Run `node --test tests/unit/forecast-tif-nonap.test.js` — all 8 subtests (5 existing + 3 new) pass, exit 0.
Run `npm run test:unit` for a broader regression sweep — forecast-tif.js is also consumed by accuracy-tif.js's backtesting engine (per CLAUDE.md's circular-import guard note) and today-screen.js's TIF dispatch branch.
</verification>

<success_criteria>
forecast-tif.js's bedtime computation gates the Activity-after-nap band on isNoNapDay, mirroring forecast-blend.js's Model 3; the gate and its substitute are proven by passing automated tests; nap-day/undetermined behavior is unchanged.
</success_criteria>

<output>
Create `.planning/phases/20-nap-probability-redesign/20-05-SUMMARY.md` when done
</output>
