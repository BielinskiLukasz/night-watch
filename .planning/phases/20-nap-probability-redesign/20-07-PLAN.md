---
phase: 20-nap-probability-redesign
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - js/ui/accuracy-screen.js
  - style.css
  - tests/e2e/accuracy-screen.spec.js
autonomous: true
requirements:
  - UI-11
gap_closure: true
gap_ids: [G-20-17]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The Accuracy screen's TIF per-day table is wrapped in a horizontally-scrollable container on narrow/mobile viewports, matching the Metrics screen's existing .metricsTableScroll pattern"
  artifacts:
    - js/ui/accuracy-screen.js
    - style.css
  key_links:
    - "renderTifAccuracy wraps buildTifPerDayTable's returned table element in a new .tifPerDayTableScroll div before appending it to the accuracy-section, mirroring metrics-screen.js's tableScroll pattern"
---

<objective>
Close G-20-17 (20-UAT.md test 17: "on mobile accuracy tab table is not displaying correctly, could it be done like metrics (scrollable)"): accuracy-screen.js's renderTifAccuracy appends buildTifPerDayTable's .tifPerDayTable directly into its section with no scroll-container wrapper, unlike metrics-screen.js's equivalent table which is wrapped in .metricsTableScroll (overflow-x/y: auto). Add the missing wrapper and its CSS rule.

Note on tracer-first: this plan opts out of the tracer-first task shape — it is a small, purely additive styling/layout fix to an already-shipped, already-tested table (Phase 23), with no new business logic or data flow, squarely in the "skip TDD" / "styling-only changes" category.

Purpose: match the Metrics screen's already-solved mobile-scroll pattern instead of leaving the newer (Phase 23) Accuracy-screen table without it.

Output: js/ui/accuracy-screen.js wraps the per-day TIF table in a new scroll container; style.css gains a matching CSS rule; new E2E coverage proves the wrapper exists with the correct computed overflow behavior.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-nap-probability-redesign/20-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap the TIF per-day table in a scroll container (mirrors metrics-screen.js .metricsTableScroll)</name>
  <files>js/ui/accuracy-screen.js, style.css</files>
  <read_first>
    - js/ui/accuracy-screen.js lines 563-581: renderTifAccuracy — builds the section, the summary grid, the "Per-Day TIF Windows" h3, then appends buildTifPerDayTable(days, tifBoundsHistory, snap)'s return value directly into section (line 578) with no wrapper
    - js/ui/metrics-screen.js lines 558-563: the tableScroll div created once (className 'metricsTableScroll') and line 744: tableScroll.replaceChildren(table, dowSection) — the exact pattern being mirrored
    - style.css lines 1735-1743: the .metricsTableScroll rule (overflow-x: auto; overflow-y: auto; max-height: calc(100vh - 8rem); margin: 0; padding: 0;) to copy
    - style.css lines 1850-1903: the .tifPerDayTable block this plan's new rule is added near
  </read_first>
  <action>
    In js/ui/accuracy-screen.js's renderTifAccuracy function, after the existing h3 ("Per-Day TIF Windows") is appended to section, create a new div element with className 'tifPerDayTableScroll', append buildTifPerDayTable(days, tifBoundsHistory, snap)'s return value into that div (instead of appending the table directly into section), then append the wrapper div to section. Leave every other part of renderTifAccuracy (the summary grid, the h2/h3 headings, root.replaceChildren(section)) unchanged.

    In style.css, add a new rule immediately adjacent to the existing .tifPerDayTable block (~line 1855) named .tifPerDayTableScroll, copying .metricsTableScroll's declarations verbatim: overflow-x: auto; overflow-y: auto; max-height: calc(100vh - 8rem); margin: 0; padding: 0. Add a one-line comment above it identifying it as the G-20-17 gap-closure counterpart to .metricsTableScroll.
  </action>
  <verify>
    <automated>node --test tests/unit/</automated>
    <fails_when>non-zero exit code (sanity check only — no unit test targets accuracy-screen.js's DOM directly; Task 2's E2E test is the real proof this task is correct)</fails_when>
  </verify>
  <done>
    The TIF per-day table on the Accuracy screen has a .tifPerDayTableScroll ancestor div; style.css defines that class with overflow-x: auto.
  </done>
</task>

<task type="auto">
  <name>Task 2: E2E coverage for the new scroll wrapper</name>
  <files>tests/e2e/accuracy-screen.spec.js</files>
  <read_first>
    - tests/e2e/accuracy-screen.spec.js lines 246-260 (and the surrounding describe block through ~line 333): the existing 'Accuracy screen — Per-Day TIF Windows table (UI-11, D-01..D-08)' describe block, its seeding pattern, and its existing `page.locator('#accuracy-screen .tifPerDayTable')` selector style to reuse
  </read_first>
  <action>
    Extend the existing 'Accuracy screen — Per-Day TIF Windows table' describe block in tests/e2e/accuracy-screen.spec.js with a new test that: seeds the same TIF fixture the describe block's other tests already use, locates page.locator('#accuracy-screen .tifPerDayTableScroll'), asserts it exists and contains a '.tifPerDayTable' child (e.g. via .locator('.tifPerDayTable').toHaveCount(1) scoped to the wrapper), and asserts the wrapper's computed overflow-x style is 'auto' via locator.evaluate(el => getComputedStyle(el).overflowX).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/accuracy-screen.spec.js</automated>
    <fails_when>non-zero exit code, or the new test fails/is flaky</fails_when>
  </verify>
  <done>
    A new passing E2E test proves the .tifPerDayTableScroll wrapper exists, contains the table, and has overflow-x: auto computed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| (none new) | Purely structural DOM wrapper + CSS around an already-existing, already-tested table; no new data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-07-01 | Tampering | .tifPerDayTableScroll wrapper | low | accept | Static wrapper div with no attributes derived from data; the table it wraps already renders exclusively via textContent (unchanged by this plan) |
</threat_model>

<verification>
Run `npx playwright test tests/e2e/accuracy-screen.spec.js` — all subtests including the new one pass, exit 0.
Manually confirm (via the new test's assertions) that .tifPerDayTable is still locatable via `#accuracy-screen .tifPerDayTable` (existing selectors in the same spec file must keep working with the new wrapper in place).
</verification>

<success_criteria>
The Accuracy screen's TIF per-day table scrolls horizontally on narrow viewports via a new .tifPerDayTableScroll wrapper, mirroring the Metrics screen's existing pattern; proven by a passing automated E2E test.
</success_criteria>

<output>
Create `.planning/phases/20-nap-probability-redesign/20-07-SUMMARY.md` when done
</output>
