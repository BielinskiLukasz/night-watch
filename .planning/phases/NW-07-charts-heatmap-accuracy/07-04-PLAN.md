---
phase: NW-07
plan: "04"
type: execute
wave: 3
depends_on:
  - "07-02"
  - "07-03"
files_modified:
  - js/ui/header.js
  - js/ui/bottom-nav.js
  - js/app.js
  - index.html
  - style.css
autonomous: true
requirements:
  - UI-06

must_haves:
  truths:
    - Bottom nav has exactly four tab buttons data-tab="today", "history", "charts", "accuracy" in left-to-right order (D7-03)
    - Each tab button has role="tab", aria-selected="true/false", inline SVG icon + text label, and min-height 44px (D7-02)
    - Clicking any tab hides all screens and shows only the selected one (D7-04)
    - header.js no longer exports setActiveTab and no longer accepts onTabChange parameter (D7-01)
    - The <nav class="tabNav"> element is removed from index.html header (D7-01)
    - #app has padding-bottom of at least 56px so content is not obscured by the fixed bottom nav (D7-04)
    - VALID_TABS in bottom-nav.js is Object.freeze(new Set(['today','history','charts','accuracy'])) (security invariant)
    - Tab IDs that are not in VALID_TABS are ignored silently (security invariant)
  artifacts:
    - js/ui/bottom-nav.js
  key_links:
    - app.js mounts mountBottomNav({ root: getElementById('bottom-nav'), onTabChange })
    - app.js four-screen applyTabVisibility replaces the two-screen version
    - index.html adds sections #charts-screen and #accuracy-screen plus <nav id="bottom-nav">
    - mountHeader in header.js no longer accepts onTabChange
---

<objective>
Deliver the bottom navigation bar and extend app.js to support four screens. This is the navigation slice for UI-06: replaces the header tab nav with a persistent bottom nav bar.

Purpose: All four screens become reachable from any screen. The header simplifies to subject name + Settings gear only. The composition root wires the new screens.

Output: js/ui/bottom-nav.js (new), modified header.js, app.js, index.html, style.css. The Today and History screens continue to work. Charts and Accuracy screen containers exist in DOM but display the mountChartsScreen/mountAccuracyScreen stub outputs (implemented in 07-05/07-06).
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/sleep-tracker/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/sleep-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/NW-07-charts-heatmap-accuracy/07-CONTEXT.md
@.planning/phases/NW-07-charts-heatmap-accuracy/07-RESEARCH.md
@.planning/phases/NW-07-charts-heatmap-accuracy/07-PATTERNS.md
@js/ui/header.js
@js/app.js
@index.html
@style.css
</context>

## Artifacts This Phase (07-04) Produces

New files:
- `js/ui/bottom-nav.js` — exports `mountBottomNav({ root, onTabChange })`

Modified files:
- `js/ui/header.js` — removes VALID_TABS, onTabChange param, setActiveTab export, tabNav wiring
- `js/app.js` — adds four-screen applyTabVisibility, bottom-nav mount, charts/accuracy screen mounts
- `index.html` — removes tabNav from header; adds #charts-screen, #accuracy-screen sections; adds #bottom-nav nav
- `style.css` — adds .bottomNav, updates #app padding-bottom, adds .chartSection/.stageChip/.accuracyGrid stubs

<tasks>

<task type="auto">
  <name>Task 1: Create bottom-nav.js and simplify header.js</name>
  <files>
    js/ui/bottom-nav.js
    js/ui/header.js
  </files>

  <read_first>
    - js/ui/header.js — read in full; identify lines to remove (VALID_TABS, tabNav block, onTabChange param, setActiveTab export) and lines to preserve (h1 subscribe, settingsTrigger click)
    - .planning/phases/NW-07-charts-heatmap-accuracy/07-PATTERNS.md §js/ui/bottom-nav.js — click dispatch pattern, VALID_TABS guard, SVG creation pattern
    - .planning/phases/NW-07-charts-heatmap-accuracy/07-RESEARCH.md §Navigation Refactor Scope §What Changes in header.js
    - .planning/phases/NW-07-charts-heatmap-accuracy/07-CONTEXT.md §D7-01..D7-04
  </read_first>

  <action>
    CREATE js/ui/bottom-nav.js — new file, zero imports.

    File header JSDoc: Phase 7, UI-06, D7-01..D7-04. mountBottomNav({ root, onTabChange }) — renders four-tab bottom navigation bar.

    VALID_TABS: const VALID_TABS = Object.freeze(new Set(['today', 'history', 'charts', 'accuracy']));

    TAB definitions array (module-level): const TABS = Object.freeze([ { id: 'today', label: 'Today', pathD: 'M12 3C10.9 3 10 3.9 10 5s.9 2 2 2 2-.9 2-2-.9-2-2-2zm3.5 7H16a1 1 0 0 1 0 2h-.5l-1 6H9.5l-1-6H8a1 1 0 0 1 0-2h.5L9 8h6l.5 2z' }, { id: 'history', label: 'History', pathD: 'M3 5h14v2H3V5zm0 4h14v2H3V9zm0 4h10v2H3v-2z' }, { id: 'charts', label: 'Charts', pathD: 'M3 18v-6l3-3 4 2 4-5 3 3v9H3z' }, { id: 'accuracy', label: 'Accuracy', pathD: 'M12 2a10 10 0 1 0 0 20A10 10 0 0 0 12 2zm0 3a7 7 0 1 1 0 14A7 7 0 0 1 12 5zm0 2a5 5 0 1 0 0 10A5 5 0 0 0 12 7zm0 2a3 3 0 1 1 0 6 3 3 0 0 1 0-6z' } ]);

    (Claude's discretion for icon path data: the above are placeholder SVG paths; Claude MUST choose clean 24x24 viewBox line-art paths for moon/sleep icon, list icon, bar chart icon, and target/bullseye icon. The paths are static string literals, not user input — safe to use setAttribute.)

    mountBottomNav({ root, onTabChange }): Call root.replaceChildren(). Set root.setAttribute('role', 'tablist'). For each tab in TABS: create button = document.createElement('button'). button.setAttribute('role', 'tab'). button.setAttribute('data-tab', tab.id). button.setAttribute('aria-selected', tab.id === 'today' ? 'true' : 'false'). button.className = 'bottomNavTab'. Build inline SVG: svgNS = 'http://www.w3.org/2000/svg'. svg = document.createElementNS(svgNS,'svg'). svg.setAttribute('viewBox','0 0 24 24'). svg.setAttribute('width','20'). svg.setAttribute('height','20'). svg.setAttribute('aria-hidden','true'). svg.setAttribute('fill','currentColor'). path = document.createElementNS(svgNS,'path'). path.setAttribute('d', tab.pathD). svg.appendChild(path). span = document.createElement('span'). span.textContent = tab.label. button.append(svg, span). root.appendChild(button).

    Add delegated click listener: root.addEventListener('click', (event) => { const btn = event.target.closest('button[data-tab]'); if (!btn || !root.contains(btn)) return; const tabId = btn.getAttribute('data-tab'); if (!VALID_TABS.has(tabId)) return; for (const b of root.querySelectorAll('button[data-tab]')) { b.setAttribute('aria-selected', String(b === btn)); } if (typeof onTabChange === 'function') onTabChange(tabId); });

    Also export: export function setActiveNavTab(root, tabId) { for (const b of root.querySelectorAll('button[data-tab]')) { b.setAttribute('aria-selected', String(b.getAttribute('data-tab') === tabId)); } } — allows app.js to programmatically sync nav state on external tab change.

    MODIFY js/ui/header.js — read the file first, then apply edits using Edit tool (scoped replacements):
    1. Remove the line declaring VALID_TABS (the Set(['today','history']) constant)
    2. Remove the onTabChange parameter from mountHeader's destructuring and JSDoc
    3. Remove the line const tabNav = root.querySelector('nav.tabNav')
    4. Remove the entire if (tabNav) { ... } block (the click-listener wiring block)
    5. Remove the entire export function setActiveTab(root, tabId) { ... } export
    The h1 textContent subscribe and settingsTrigger click handler remain unchanged.
    Result signature: export function mountHeader({ root, settings, onSettings })
  </action>

  <acceptance_criteria>
    - js/ui/bottom-nav.js exists and exports mountBottomNav and setActiveNavTab as named exports
    - grep -c 'VALID_TABS' js/ui/bottom-nav.js outputs a number >= 1 (the frozen Set declaration)
    - grep -c 'setActiveTab' js/ui/header.js outputs 0 (export removed)
    - grep -c 'onTabChange' js/ui/header.js outputs 0 (parameter removed)
    - grep -c 'VALID_TABS' js/ui/header.js outputs 0 (constant removed)
    - node --test tests/unit/ exits 0 (no regression from header.js edits)
  </acceptance_criteria>

  <verify>
    <automated>node -e "import('./js/ui/bottom-nav.js').then(m => { if(typeof m.mountBottomNav!=='function') throw new Error('mountBottomNav not exported'); console.log('OK'); }).catch(e=>{ if(e.code==='ERR_MODULE_NOT_FOUND') throw e; console.log('Import check (non-browser ok):', e.message); })" 2>&1 | head -3</automated>
  </verify>

  <done>
    js/ui/bottom-nav.js exists. header.js no longer exports setActiveTab. header.js mountHeader signature is mountHeader({ root, settings, onSettings }) — no onTabChange. VALID_TABS Set is defined and frozen in bottom-nav.js. The click handler validates tab IDs against VALID_TABS before calling onTabChange.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update index.html, style.css, and app.js for four-screen navigation</name>
  <files>
    index.html
    style.css
    js/app.js
  </files>

  <read_first>
    - index.html — read in full; identify exact location of tabNav nav element to remove; identify where to insert new sections and bottom-nav
    - style.css — read in full; identify .tabNav rules to remove; identify #app rule to update; find insertion point for new bottom-nav and chart styles
    - js/app.js — read in full; identify applyTabVisibility function to replace; identify mountHeader call to update; identify where to add new mounts
    - .planning/phases/NW-07-charts-heatmap-accuracy/07-PATTERNS.md §js/app.js (modify) and §index.html (modify) and §style.css (modify)
    - .planning/phases/NW-07-charts-heatmap-accuracy/07-RESEARCH.md §CSS Patterns Available — .bottomNav CSS, #app padding-bottom
  </read_first>

  <action>
    MODIFY index.html using Edit (scoped):
    1. Remove the entire <nav class="tabNav" ...> element and its child buttons from inside <header class="appHeader">
    2. Inside <main id="app">, after the existing #history-screen section, add: <section id="charts-screen" style="display:none" aria-label="Charts"></section> and <section id="accuracy-screen" style="display:none" aria-label="Accuracy"></section>
    3. After the closing </main> tag (before or after existing dialog elements — after </main> is preferred), add: <nav id="bottom-nav" class="bottomNav" role="tablist" aria-label="Screen navigation"></nav>

    MODIFY style.css using Edit (scoped):
    1. Remove the .tabNav rule block and .tabNav button[role="tab"] rule block. If the .appHeader rule has justify-content: flex-start (added for tabNav spacing), revert it to justify-content: space-between.
    2. Add at the end of the file the following rule blocks (values from RESEARCH.md CSS Patterns Available, per D7-04):
       - .bottomNav: position fixed, bottom 0, left 0, right 0, height 56px, display flex, background #fff, border-top 1px solid #e2e8f0, z-index 100
       - .bottomNav .bottomNavTab (or button[role="tab"] scoped to .bottomNav): flex 1, display flex, flex-direction column, align-items center, justify-content center, gap 2px, min-height 44px, background transparent, border none, cursor pointer, color #64748b, font-size 0.7rem, font-weight 500, padding 4px 0
       - .bottomNav .bottomNavTab[aria-selected="true"]: color #4f46e5
       - #app: add padding-bottom: 64px to the existing #app rule (use Edit to append the property — do not duplicate the selector; if #app already has a padding-bottom, update it)
       - .chartSection: margin-bottom 2rem
       - .chartSection h2: font-size 0.875rem, font-weight 600, color #334155, text-transform uppercase, letter-spacing 0.04em, margin 0 0 0.5rem
       - .chartSvg: width 100%, height auto, display block
       - .stageChip: display inline-flex, align-items center, gap 0.25rem, padding 0.2rem 0.6rem, background #eef2ff, color #4f46e5, border 1px solid #c7d2fe, border-radius 9999px, font-size 0.75rem, font-weight 500, margin-bottom 1rem
       - .accuracyGrid: display grid, grid-template-columns auto 1fr 1fr 1fr, gap 1px, background #e2e8f0, border 1px solid #e2e8f0, border-radius 0.375rem, overflow hidden, font-size 0.875rem
       - .accuracyGrid > *: background #fff, padding 0.5rem 0.75rem
       - .coldStartNote: color #64748b, font-size 0.9rem, padding 2rem 1rem, text-align center

    MODIFY js/app.js using Edit (scoped):
    1. Update import from header.js: remove setActiveTab from the named import. Add imports: import { mountBottomNav } from './ui/bottom-nav.js'; import { mountChartsScreen } from './ui/charts-screen.js'; import { mountAccuracyScreen } from './ui/accuracy-screen.js';
       (Note: charts-screen.js and accuracy-screen.js do not yet exist — these imports will fail at runtime until 07-05/07-06. Add them now so the wiring is complete and discoverable. Alternatively, guard with dynamic import — but static import is cleaner; 07-05/07-06 must be executed before the app runs correctly.)
    2. Add element references after existing ones (getElementById pattern): const chartsScreenEl = document.getElementById('charts-screen'); const accuracyScreenEl = document.getElementById('accuracy-screen'); const bottomNavEl = document.getElementById('bottom-nav');
    3. Replace applyTabVisibility with four-screen version. The SCREENS map uses direct element references (not lazy getters since elements are already captured): const SCREENS = Object.freeze({ today: todayScreenEl, history: historyScreenEl, charts: chartsScreenEl, accuracy: accuracyScreenEl }); function applyTabVisibility() { for (const [tabId, el] of Object.entries(SCREENS)) { if (el) el.style.display = (tabId === activeTab ? '' : 'none'); } } Remove the setActiveTab(headerEl, activeTab) call from the old applyTabVisibility.
    4. Update mountHeader call: remove the onTabChange property from the options object passed to mountHeader.
    5. Add bottom-nav mount call after existing mounts: if (bottomNavEl) mountBottomNav({ root: bottomNavEl, onTabChange: (tabId) => { activeTab = tabId; applyTabVisibility(); } });
    6. Add screen mounts after bottom-nav: if (chartsScreenEl) mountChartsScreen({ root: chartsScreenEl, eventLog, settings }); and if (accuracyScreenEl) mountAccuracyScreen({ root: accuracyScreenEl, eventLog, settings });
    7. Apply initial applyTabVisibility() call remains unchanged (or ensure it fires after all mounts).

    IMPORTANT per D7-04 and RESEARCH Pitfall #5: the padding-bottom on #app must be at least 56px (nav height) + 8px buffer = 64px. Verify this is applied to the element that contains the scrollable content.
  </action>

  <acceptance_criteria>
    - grep -c 'tabNav' index.html outputs 0 (the nav element removed)
    - grep -c 'charts-screen' index.html outputs at least 1 (new section present)
    - grep -c 'accuracy-screen' index.html outputs at least 1 (new section present)
    - grep -c 'bottom-nav' index.html outputs at least 1 (nav element added)
    - grep -c 'bottomNav' style.css outputs at least 1 (CSS rule added)
    - grep -c 'padding-bottom' style.css outputs at least 1 (#app compensation added)
    - grep -c 'setActiveTab' js/app.js outputs 0 (call removed)
    - grep -c 'mountBottomNav' js/app.js outputs at least 1 (mount call added)
    - grep -c 'applyTabVisibility' js/app.js outputs at least 2 (definition + call)
  </acceptance_criteria>

  <verify>
    <automated>node -e "const fs=require('fs'); const html=fs.readFileSync('index.html','utf8'); if(html.includes('tabNav')) throw new Error('tabNav still in index.html'); if(!html.includes('charts-screen')) throw new Error('charts-screen missing'); if(!html.includes('bottom-nav')) throw new Error('bottom-nav missing'); console.log('index.html OK');"</automated>
  </verify>

  <done>
    index.html has no tabNav element. index.html has #charts-screen section, #accuracy-screen section, and #bottom-nav nav. style.css has .bottomNav rule with position:fixed. style.css has #app padding-bottom at least 56px. app.js mounts mountBottomNav with onTabChange callback. app.js applyTabVisibility handles four screens. Existing E2E tests for Today and History screens still pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify four-screen navigation via E2E bottom-nav spec</name>
  <files>tests/e2e/bottom-nav.spec.js</files>

  <read_first>
    - tests/e2e/bottom-nav.spec.js — the stub created in 07-01; review its assertions to confirm they match the implementation
    - tests/e2e/stages.spec.js — Playwright baseline spec for reference on test structure
    - js/ui/bottom-nav.js — review the data-tab attribute names and aria-selected defaults just implemented
  </read_first>

  <action>
    The bottom-nav.spec.js stub from 07-01 may need adjustments to match the exact attribute names and class names used in the implementation. Read the stub, read the implementation, reconcile any mismatch.

    The spec must verify:
    1. The bottom nav renders four buttons: page.locator('#bottom-nav button[data-tab]').count() === 4
    2. Today tab is aria-selected="true" by default: page.locator('#bottom-nav button[data-tab="today"][aria-selected="true"]') is visible
    3. After clicking button[data-tab="charts"], the #charts-screen is visible (display not 'none')
    4. After clicking button[data-tab="accuracy"], the #accuracy-screen is visible
    5. After clicking button[data-tab="history"], #history-screen is visible and #today-screen has display 'none'

    If the stub assertions already match the implementation, no changes are needed. If they don't match (e.g., different selector pattern), update the spec using Edit.

    The spec verifies UI-06 end-to-end: all four screens reachable from any screen via the bottom nav. This is an acceptance test for D7-01..D7-04.

    Do not use fenced code blocks. Reference the exact locator selectors by name in prose.
  </action>

  <acceptance_criteria>
    - npx playwright test tests/e2e/bottom-nav.spec.js exits 0 (all tests pass)
    - The spec file references button[data-tab="today"], button[data-tab="history"], button[data-tab="charts"], button[data-tab="accuracy"]
    - The spec references #charts-screen and #accuracy-screen locators
    - Existing E2E specs (stages.spec.js, history.spec.js, etc.) still pass: npx playwright test --ignore=charts-screen.spec.js --ignore=accuracy-screen.spec.js exits 0
  </acceptance_criteria>

  <verify>
    <automated>npx playwright test tests/e2e/bottom-nav.spec.js --reporter=line 2>&1 | tail -10</automated>
  </verify>

  <done>
    npx playwright test tests/e2e/bottom-nav.spec.js passes all tests. Clicking each of the four tabs in the browser shows the correct screen section. Today and History screens continue to work as before (no regression). The aria-selected attribute updates correctly on tab click.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User click → tab ID extraction | btn.getAttribute('data-tab') produces a string; validated against VALID_TABS before dispatch |
| URL/external → tab navigation | No URL-based tab routing; all tab changes go through the onTabChange callback validated in bottom-nav.js |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04-01 | Tampering | Tab ID injection via forged button click | medium | mitigate | VALID_TABS = Object.freeze(new Set([...])) guard in mountBottomNav click handler — invalid tabIds silently ignored before onTabChange fires |
| T-07-04-02 | Tampering | SVG icon path as user-controllable content | low | accept | Icon path data is static string literals in TABS array — not derived from user input; setAttribute on path d is safe for static content |
| T-07-04-03 | Spoofing | Stage name rendered in stageChip via textContent | low | mitigate | Stage name set via badge.textContent = stage.name — textContent property, never innerHTML (security invariant upheld) |
| T-07-04-SC | Tampering | npm/devDependencies installs | low | accept | No new npm packages; Playwright already in devDependencies |
</threat_model>

<verification>
Full navigation verification:
```
npx playwright test tests/e2e/bottom-nav.spec.js
npx playwright test tests/e2e/history.spec.js tests/e2e/stages.spec.js
node --test tests/unit/
```

All three commands must exit 0.
</verification>

<success_criteria>
- js/ui/bottom-nav.js: mountBottomNav and setActiveNavTab exported; VALID_TABS frozen Set of four tabs; click handler validates tabId against VALID_TABS
- js/ui/header.js: no VALID_TABS, no onTabChange, no setActiveTab export; mountHeader({ root, settings, onSettings }) signature
- index.html: no tabNav nav; has #charts-screen, #accuracy-screen sections; has #bottom-nav nav
- style.css: has .bottomNav with position:fixed; has #app padding-bottom >= 56px
- js/app.js: mounts mountBottomNav; applyTabVisibility handles four screens; no setActiveTab call
- npx playwright test tests/e2e/bottom-nav.spec.js exits 0
- All existing E2E tests pass (no regression on Today/History/Settings/Stages flows)
</success_criteria>

<output>
Create `.planning/phases/NW-07-charts-heatmap-accuracy/07-04-SUMMARY.md` when done.
</output>
