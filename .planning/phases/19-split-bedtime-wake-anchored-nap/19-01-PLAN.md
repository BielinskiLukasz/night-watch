---
phase: 19-split-bedtime-wake-anchored-nap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/lib/forecast.js
  - tests/unit/forecast.test.js
autonomous: true
requirements:
  - PRED-18
  - PRED-20
  - PRED-22

estimate:
  tokens: 65000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "percentileFromArray(values, pct) is exported from forecast.js and returns null for empty input, the interpolated value otherwise (D-16)"
    - "buildNapGapSeries(dayRecords) returns a plain number array of (napStart - wake) minutes, skipping days with null napStart or wake, applying midnight-crossover normalization (PRED-20)"
    - "buildNapDurationSeries(dayRecords) returns a plain number array of (napEnd - napStart) minutes, skipping days with null napEnd or napStart, applying midnight-crossover normalization (PRED-22)"
    - "buildBedtimeSeriesNapDay(dayRecords, settings) returns { min, central, max } as integer minutes for ≥minDays nap-day records, null otherwise (PRED-18, D-02, D-04, D-08)"
    - "buildBedtimeSeriesNoNapDay(dayRecords, settings) returns { min, central, max } as integer minutes for ≥minDays no-nap-day records, null otherwise (PRED-18, D-02, D-04, D-08)"
    - "All five functions are exported from forecast.js and importable by the test runner without error"
    - "All new unit tests pass — node --test tests/unit/forecast.test.js exits 0"
  prohibitions:
    - "noNapBedtimeOffsetMinutes must not appear in any new test's settings object"
    - "No direct new Date() or localStorage calls added to forecast.js"
    - "No innerHTML with user-controlled data in any modified file"
  artifacts:
    - js/lib/forecast.js
    - tests/unit/forecast.test.js
  key_links:
    - "forecast.js export: percentileFromArray, buildNapGapSeries, buildNapDurationSeries, buildBedtimeSeriesNapDay, buildBedtimeSeriesNoNapDay"
    - "Phase 25 forecast-blend.js will import buildNapGapSeries and bedtime series functions from forecast.js — signatures must be stable after this plan"
---

<objective>
Add five new pure-function exports to js/lib/forecast.js that Plan 19-02 will wire into forecast() routing.

Purpose: The split bedtime model and wake-anchored nap predictions require helper functions that extract filtered distributions from dayRecords. This plan implements and unit-tests all five functions using TDD cycles before any routing logic is changed. The tracer proves the export/import chain works end-to-end on the thinnest possible slice (percentileFromArray) before expanding.

Output: Five new exported functions in forecast.js; new describe blocks in forecast.test.js; all existing tests continue to pass.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-split-bedtime-wake-anchored-nap/19-CONTEXT.md
@.planning/phases/19-split-bedtime-wake-anchored-nap/19-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: percentileFromArray — RED test then GREEN implementation (D-16)</name>
  <files>js/lib/forecast.js, tests/unit/forecast.test.js</files>
  <read_first>
    - js/lib/forecast.js lines 114–132: percentile(sorted, p) implementation that percentileFromArray wraps
    - js/lib/forecast.js lines 34–47: exported-functions comment block to update
    - tests/unit/forecast.test.js lines 1–60: import statement shape and describe/it/assert.strictEqual pattern
    - tests/unit/forecast.test.js lines 39–41: makeDay helper used by all new test blocks
  </read_first>
  <action>
    RED phase — in forecast.test.js, extend the existing named import at lines 23–34 to include percentileFromArray (alongside the existing exports). Write a describe('percentileFromArray(values, pct)', () => ...) block with four it cases: (1) empty array returns null; (2) [42] with pct=50 returns 42; (3) [10, 20, 30] with pct=50 returns 20; (4) [30, 10, 20] with pct=50 also returns 20 — verifying the function sorts internally rather than assuming pre-sorted input. Run the test file and confirm it exits non-zero (import fails because export does not exist yet).

    GREEN phase — in forecast.js, add export function percentileFromArray(values, pct) immediately after the percentile() function (~line 133). Implementation: guard falsy or zero-length input → return null. Create sorted = [...values].sort((a, b) => a - b). Delegate to the existing percentile(sorted, pct / 100). Also extend the exported-functions comment block at lines 34–47 to list percentileFromArray(values, pct) → number | null.

    Run tests — confirm exit 0 and four new passing subtests.

    Do NOT include noNapBedtimeOffsetMinutes in any settings literal (per D-10 removal that Plan 19-02 formalises — do not add new references now).
  </action>
  <verify>
    <automated>node --test tests/unit/forecast.test.js</automated>
    <fails_when>non-zero exit code, or any "not ok" TAP line belonging to the percentileFromArray describe block, or the import statement throws a SyntaxError</fails_when>
  </verify>
  <done>
    percentileFromArray is exported from forecast.js; four unit tests pass; existing tests are unaffected; no noNapBedtimeOffsetMinutes reference is introduced.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: buildNapGapSeries + buildNapDurationSeries — RED then GREEN (PRED-20, PRED-22)</name>
  <files>js/lib/forecast.js, tests/unit/forecast.test.js</files>
  <read_first>
    - js/lib/forecast.js lines 349–381: computeDurationBand() — the exact iteration + midnight-crossover normalization pattern to replicate
    - js/lib/forecast.js: extractTime module-private helper — used to extract HH:MM from a dayRecord field (bare string or object with .at)
    - tests/unit/forecast.test.js lines 39–41: makeDay(wake, bedtime, napStart, napEnd, rejected) helper signature
    - .planning/phases/19-split-bedtime-wake-anchored-nap/19-PATTERNS.md: buildNapGapSeries / buildNapDurationSeries pattern excerpts
  </read_first>
  <behavior>
    - buildNapGapSeries([]) returns [] (empty array, not null — callers check length)
    - buildNapGapSeries with records having napStart='11:00' and wake='07:00' returns [240] per day
    - buildNapGapSeries skips records where napStart is null or wake is null
    - buildNapGapSeries applies midnight-crossover: if gap = timeToMinutes(napStart) - timeToMinutes(wake) is negative, adds 1440
    - buildNapDurationSeries([]) returns []
    - buildNapDurationSeries with napEnd='12:30' and napStart='11:00' returns [90] per day
    - buildNapDurationSeries skips records where napEnd or napStart is null
    - buildNapDurationSeries applies midnight-crossover normalization
  </behavior>
  <action>
    RED phase — in forecast.test.js extend the import to include buildNapGapSeries and buildNapDurationSeries. Write describe('buildNapGapSeries(dayRecords)', ...) with it cases matching the behavior block above, using makeDay to create synthetic records. Write describe('buildNapDurationSeries(dayRecords)', ...) similarly. Run tests — expect failure (exports missing).

    GREEN phase — in forecast.js, add export function buildNapGapSeries(dayRecords) after the subWindowBedtime function (~line 424). For each record in dayRecords: call extractTime on napStart and wake fields; skip if either returns null; compute gap = timeToMinutes(napStartStr) - timeToMinutes(wakeStr); if (gap < 0) gap += 1440; push to array. Return the array (never null — empty array when no valid days).

    Add export function buildNapDurationSeries(dayRecords) immediately after. Same pattern: extract napEnd and napStart; compute duration = timeToMinutes(napEndStr) - timeToMinutes(napStartStr); normalize; push; return array.

    Follow the computeDurationBand() pattern at lines 354–366 exactly for the iteration structure. Use extractTime() (module-private) exactly as computeDurationBand does for field extraction. Do not call timeToMinutes on raw event objects — always extract the HH:MM string first via extractTime.

    Update the exported-functions comment block to list both new functions.

    Run tests — confirm all subtests pass.
  </action>
  <acceptance_criteria>
    - describe('buildNapGapSeries') block: all it cases pass
    - describe('buildNapDurationSeries') block: all it cases pass
    - node --test tests/unit/forecast.test.js exits 0
    - buildNapGapSeries and buildNapDurationSeries appear in the export surface of forecast.js (grep "^export function buildNapGapSeries" returns a match)
    - buildNapGapSeries and buildNapDurationSeries return plain number arrays (not objects with min/central/max)
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/unit/forecast.test.js</automated>
    <fails_when>non-zero exit code, or any "not ok" line for buildNapGapSeries or buildNapDurationSeries tests</fails_when>
  </verify>
  <done>
    Both series functions are exported and pass all unit tests including null-skip and midnight-crossover cases. Existing tests unaffected.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: buildBedtimeSeriesNapDay + buildBedtimeSeriesNoNapDay — RED then GREEN (PRED-18, D-01..D-04, D-08)</name>
  <files>js/lib/forecast.js, tests/unit/forecast.test.js</files>
  <read_first>
    - js/lib/forecast.js lines 407–424: subWindowBedtime() — the analog to adapt (filter + calculatePercentiles, no offset shift for these new functions)
    - js/lib/forecast.js: calculatePercentiles signature: (dayRecords, getTimeFn, rejectWeight?) → { min, central, max } | null (returns integer minutes)
    - js/lib/forecast.js: extractTime module-private helper — use same pattern as subWindowBedtime line 411 and 413
    - tests/unit/forecast.test.js lines 39–41: makeDay helper; include bedtime field in synthetic records
    - .planning/phases/19-split-bedtime-wake-anchored-nap/19-PATTERNS.md: buildBedtimeSeriesNapDay/NoNapDay pattern excerpt
  </read_first>
  <behavior>
    - buildBedtimeSeriesNapDay with fewer than minDays nap-day records returns null (cold-start guard, D-08)
    - buildBedtimeSeriesNapDay with exactly minDays nap-day records returns { min, central, max } as integers
    - buildBedtimeSeriesNapDay excludes days where napStart is null
    - buildBedtimeSeriesNapDay passes only the nap-day sub-array to calculatePercentiles — not the full window
    - buildBedtimeSeriesNoNapDay with fewer than minDays no-nap-day records returns null
    - buildBedtimeSeriesNoNapDay with exactly minDays no-nap-day records returns { min, central, max } as integers
    - buildBedtimeSeriesNoNapDay excludes days where napStart is non-null
    - Return shape is { min: number, central: number, max: number } not { min: 'HH:MM', ... } — conversion to HH:MM is the caller's responsibility (D-02)
  </behavior>
  <action>
    RED phase — in forecast.test.js extend the import to include buildBedtimeSeriesNapDay and buildBedtimeSeriesNoNapDay. Write describe blocks for each using makeDay records. The settings argument: use { minDays: 3, windowDays: 7, maxDelta: 60, intenseDayOffsetMinutes: 30, eveningHour: 18 } — do NOT include noNapBedtimeOffsetMinutes. Run tests — expect failure.

    GREEN phase — in forecast.js, add export function buildBedtimeSeriesNapDay(dayRecords, settings) after buildNapDurationSeries. Filter: subWin = dayRecords.filter(d => extractTime(d.napStart) != null). Guard: if (subWin.length < settings.minDays) return null. Return calculatePercentiles(subWin, d => extractTime(d.bedtime)) — which already returns integer-minute { min, central, max } or null (if no bedtime data; caller falls back to overall series).

    Add export function buildBedtimeSeriesNoNapDay(dayRecords, settings). Filter: subWin = dayRecords.filter(d => extractTime(d.napStart) == null). Same minDays guard. Return calculatePercentiles(subWin, d => extractTime(d.bedtime)).

    Update the exported-functions comment block at the top to list both functions.

    Run all tests — confirm exit 0.
  </action>
  <acceptance_criteria>
    - describe('buildBedtimeSeriesNapDay') block: null returned for thin sub-windows, integer-minute object returned for sufficient sub-windows
    - describe('buildBedtimeSeriesNoNapDay') block: same shape assertions
    - grep "^export function buildBedtimeSeriesNapDay" in forecast.js returns a match
    - grep "^export function buildBedtimeSeriesNoNapDay" in forecast.js returns a match
    - node --test tests/unit/forecast.test.js exits 0
    - No noNapBedtimeOffsetMinutes in any new settings literal in forecast.test.js
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/unit/forecast.test.js</automated>
    <fails_when>non-zero exit code, or any "not ok" line for the buildBedtimeSeries test blocks, or settings literals contain noNapBedtimeOffsetMinutes</fails_when>
  </verify>
  <done>
    Both bedtime series functions exported and passing all unit tests. Return shape is integer-minute { min, central, max } | null. No HH:MM conversion inside these functions. All five new exports are present in forecast.js.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eventLog store → forecast.js helpers | dayRecords come from validated localStorage via db-shape.js schema; corrupt values produce null guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01-01 | Tampering | buildNapGapSeries / buildNapDurationSeries | low | accept | Both functions filter on extractTime null-check; non-HH:MM strings return NaN from timeToMinutes but are excluded by the null guard on extractTime; output is a number array used only in percentile arithmetic, never rendered to DOM |
| T-19-01-02 | Tampering | buildBedtimeSeriesNapDay / NoNapDay | low | accept | Delegates to calculatePercentiles which already handles null/empty inputs; return value is integer minutes used only inside forecast() arithmetic, not user-facing until minutesToTime conversion in caller |
</threat_model>

<verification>
Run `node --test tests/unit/forecast.test.js` — all subtests pass, exit 0.
Confirm five new exports appear in forecast.js: percentileFromArray, buildBedtimeSeriesNapDay, buildBedtimeSeriesNoNapDay, buildNapGapSeries, buildNapDurationSeries.
Confirm no noNapBedtimeOffsetMinutes in any test settings object.
Confirm existing tests that use the previous settings shape (minDays, windowDays, maxDelta, intenseDayOffsetMinutes, eveningHour) continue to pass unchanged.
</verification>

<success_criteria>
Five functions exported from forecast.js; all new and existing forecast.test.js subtests exit 0; no scope-reduction patterns (static placeholders, "v1" stubs, hardcoded fallbacks masquerading as real distributions).
</success_criteria>

## Artifacts This Phase Produces

New exported symbols from `js/lib/forecast.js` (stable public API for Phase 25):

| Symbol | Signature | Returns |
|--------|-----------|---------|
| `percentileFromArray` | `(values: number[], pct: number) → number \| null` | interpolated percentile value |
| `buildBedtimeSeriesNapDay` | `(dayRecords, settings) → { min, central, max } \| null` | integer-minute band |
| `buildBedtimeSeriesNoNapDay` | `(dayRecords, settings) → { min, central, max } \| null` | integer-minute band |
| `buildNapGapSeries` | `(dayRecords) → number[]` | wake-to-nap gap minutes |
| `buildNapDurationSeries` | `(dayRecords) → number[]` | nap duration minutes |

<output>
Create `.planning/phases/19-split-bedtime-wake-anchored-nap/19-01-SUMMARY.md` when done.
</output>
