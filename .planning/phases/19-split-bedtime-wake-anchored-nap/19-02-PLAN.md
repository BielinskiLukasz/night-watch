---
phase: 19-split-bedtime-wake-anchored-nap
plan: 02
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified:
  - js/lib/forecast.js
  - js/lib/db-shape.js
  - js/lib/settings-validate.js
  - js/ui/today-screen.js
  - tests/unit/forecast.test.js
autonomous: false
requirements:
  - PRED-18
  - PRED-19
  - PRED-20
  - PRED-21
  - PRED-22

estimate:
  tokens: 75000
  raw_tokens: 37000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "forecast() routes bedtime: napStartLogged → napDay series; napStart not logged + score present + both series non-null → blend per D-05; else → overall calculatePercentiles (PRED-18, PRED-19)"
    - "PRED-11 block (noNapFired = !napStartLogged && currentHour >= eveningHour) is absent from forecast.js (D-09)"
    - "noNapBedtimeOffsetMinutes is absent from DEFAULT_SETTINGS in db-shape.js and from the forward-compat migration block (D-10)"
    - "noNapBedtimeOffsetMinutes validator entry is absent from settings-validate.js (D-10)"
    - "forecast() nap-start: when context.todayWakeHHMM is present and napGapSeries has ≥minDays values, predicts wakeAnchor + P10/P50/P90(gaps); else falls back to time-of-day percentiles (PRED-21, D-14)"
    - "forecast() nap-end: anchors to today's actual napStart (context.todayNapStartHHMM) when logged, else to predicted napStart central; uses P10/P50/P90(napDuration) for band (PRED-22, D-15)"
    - "today-screen.js computes todayWakeHHMM, napProbabilityScore, and todayNapStartHHMM before calling forecast(); all three appear in forecastContext (D-13)"
    - "npm run test:unit exits 0 — all unit and integration tests pass"
  prohibitions:
    - "noNapBedtimeOffsetMinutes must not appear in DEFAULT_SETTINGS after this plan"
    - "No new Date() or localStorage calls added to forecast.js"
    - "No innerHTML with user-controlled data in any modified file"
  artifacts:
    - js/lib/forecast.js
    - js/lib/db-shape.js
    - js/lib/settings-validate.js
    - js/ui/today-screen.js
    - tests/unit/forecast.test.js
  key_links:
    - "context.napProbabilityScore (from today-screen.js) → forecast() split bedtime blend (D-05, D-19)"
    - "context.todayWakeHHMM → buildNapGapSeries → napStart anchor (PRED-21)"
    - "context.todayNapStartHHMM or napStartPred.central → buildNapDurationSeries → napEnd anchor (D-15, PRED-22)"
---

<objective>
Wire the five new helpers from Plan 19-01 into forecast() routing and clean up the superseded PRED-11 infrastructure.

Purpose: The helpers built in Plan 19-01 are not yet used — forecast() still applies the old PRED-11 evening-hour no-nap shift and time-of-day percentiles for nap-start/end. This plan removes PRED-11, replaces it with the split-series bedtime model (PRED-18/19), and replaces time-of-day nap predictions with wake-anchored gap/duration percentiles (PRED-21/22). The schema field noNapBedtimeOffsetMinutes is removed from DEFAULT_SETTINGS and the validator. today-screen.js is updated to supply the three new context fields that forecast() now requires.

Output: forecast() split bedtime routing live; nap predictions wake-anchored; schema cleanup complete; integration tests pass.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-split-bedtime-wake-anchored-nap/19-CONTEXT.md
@.planning/phases/19-split-bedtime-wake-anchored-nap/19-PATTERNS.md
@.planning/phases/19-split-bedtime-wake-anchored-nap/19-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Confirm: Remove PRED-11 block and noNapBedtimeOffsetMinutes setting (D-09, D-10)</name>
  <decision>Proceed with two costly-reversibility removals locked in CONTEXT.md (D-09, D-10)</decision>
  <context>
    D-09: The PRED-11 block (`const noNapFired = !napStartLogged and currentHour >= eveningHour`)
    in forecast() will be permanently deleted. This removes the evening-hour gate that previously
    shifted bedtime predictions on no-nap evenings. The split model (PRED-18/19) supersedes it.
    Restoring requires re-adding the block, its subWindowBedtime call, and all associated tests.

    D-10: noNapBedtimeOffsetMinutes will be deleted from DEFAULT_SETTINGS in db-shape.js, from
    the migrateV1ToV2 forward-compat block, and from the settings-validate.js validator. Users
    who customised this setting lose that value permanently. Existing exported JSON blobs retain
    the orphan key harmlessly until the next export/import cycle.

    Both decisions were locked in 19-CONTEXT.md after discussion. This checkpoint confirms you
    are ready for the implementing tasks that make these removals final.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed with D-09 and D-10 removals</name>
      <pros>Split model captures nap-day vs no-nap-day structure permanently; removes complexity from forecast() routing; noNapBedtimeOffsetMinutes was a crude fixed-offset approximation</pros>
      <cons>Irreversible without code restore; users who set a custom noNapBedtimeOffsetMinutes lose that value</cons>
    </option>
    <option id="abort">
      <name>Abort — keep PRED-11 and noNapBedtimeOffsetMinutes for now</name>
      <pros>Preserves existing behaviour; no user data loss</pros>
      <cons>Cannot implement PRED-18/PRED-19 split model correctly; old code remains active alongside new series functions</cons>
    </option>
  </options>
  <resume-signal>Type "proceed" to continue with Task 2, or "abort" to stop this plan and preserve the existing PRED-11 block</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove PRED-11, schema cleanup, add split bedtime routing (D-09, D-10, D-12, PRED-18, PRED-19)</name>
  <files>js/lib/forecast.js, js/lib/db-shape.js, js/lib/settings-validate.js, tests/unit/forecast.test.js</files>
  <reversibility rating="costly">D-09/D-10: Removes PRED-11 block and noNapBedtimeOffsetMinutes; restoring requires re-adding both the code block and the settings field including schema migration logic</reversibility>
  <read_first>
    - js/lib/forecast.js lines 553–656: full context destructure (line 554) and the bedtimePred IIFE including the PRED-11 block (lines 603–629) and PRED-10 block (lines 631–652) to understand the replacement target
    - js/lib/forecast.js lines 34–47: exported-functions header comment
    - js/lib/db-shape.js line 66: noNapBedtimeOffsetMinutes entry in DEFAULT_SETTINGS; lines 139–140: forward-compat injection block
    - js/lib/settings-validate.js line 63: noNapBedtimeOffsetMinutes validator entry
    - .planning/phases/19-split-bedtime-wake-anchored-nap/19-PATTERNS.md: forecast() bedtime routing replacement excerpt (D-12 order description)
    - tests/unit/forecast.test.js: existing forecast() test blocks (to avoid breaking them and to know where to append new describe block)
  </read_first>
  <behavior>
    - forecast() with napStartLogged=true and ≥minDays nap-day records in the window: bedtime uses buildBedtimeSeriesNapDay result (not the overall series)
    - forecast() with napStartLogged=false and napProbabilityScore=70 and both sub-series having ≥minDays records: bedtime.central = Math.round(0.7 * napDay.central + 0.3 * noNapDay.central); bedtime.min = Math.min(napDay.min, noNapDay.min); bedtime.max = Math.max(napDay.max, noNapDay.max)
    - forecast() with napProbabilityScore=null and napStartLogged=false: bedtime falls back to calculatePercentiles over the full window (D-07)
    - forecast() with napProbabilityScore present but either sub-series returning null (thin data): falls back to calculatePercentiles over full window (D-08)
    - noNapFired branch is not reachable — grep for "noNapFired" in forecast.js returns no match
    - Existing forecast() tests that do NOT reference napStartLogged or currentHour continue to pass without change
    - noNapBedtimeOffsetMinutes is not referenced in forecast.js, db-shape.js, or settings-validate.js after this task
  </behavior>
  <action>
    RED phase — in forecast.test.js append a describe('forecast() split bedtime routing (PRED-18/19)', ...) block with it cases matching the behavior block above. Build synthetic dayRecords using makeDay; set napProbabilityScore in the context argument. Settings object must NOT contain noNapBedtimeOffsetMinutes. Run tests — expect failure because routing not yet implemented.

    GREEN phase (forecast.js):
    1. Extend the context destructure at line 554 to also include: todayWakeHHMM = null, napProbabilityScore = null, todayNapStartHHMM = null.
    2. Inside the bedtimePred IIFE (lines 601–656), REMOVE the entire PRED-11 block: delete the lines containing `const noNapFired`, the `if (noNapFired)` branch and its body. Replace with the D-12 split model:
       - If napStartLogged is true: splitResult = buildBedtimeSeriesNapDay(window, settings). If splitResult is null, fall through to overall calculatePercentiles. Assign selectedResult from splitResult or overall.
       - Else if napProbabilityScore is not null: compute napDaySeries = buildBedtimeSeriesNapDay(window, settings) and noNapDaySeries = buildBedtimeSeriesNoNapDay(window, settings). If both are non-null: blend — central = Math.round((napProbabilityScore / 100) * napDaySeries.central + (1 - napProbabilityScore / 100) * noNapDaySeries.central), min = Math.min(napDaySeries.min, noNapDaySeries.min), max = Math.max(napDaySeries.max, noNapDaySeries.max). Else: fall back to calculatePercentiles(window, d => extractTime(d.bedtime)).
       - Else (score is null): selectedResult = calculatePercentiles(window, d => extractTime(d.bedtime)).
       - After selection: run the probability-band check on bedtimeTimes — same pattern as lines 615–621 (generateProbabilityBand). If band present, return { probabilityBand: band }.
       - Convert to HH:MM: return { central: minutesToTime(selectedResult.central), min: minutesToTime(selectedResult.min), max: minutesToTime(selectedResult.max) }.
       - PRED-10 (isIntenseToday) block stays intact beneath the split-model block — it applies subWindowBedtime for intense days and returns early. This is D-11 (intense day orthogonal to nap status). No change to that block.

    GREEN phase (db-shape.js):
    - Remove the noNapBedtimeOffsetMinutes: 30 entry from DEFAULT_SETTINGS.
    - Remove the two-line migrateV1ToV2 forward-compat block: if (blob.settings && !('noNapBedtimeOffsetMinutes' in blob.settings)) { blob.settings.noNapBedtimeOffsetMinutes = 30; }

    GREEN phase (settings-validate.js):
    - Remove the noNapBedtimeOffsetMinutes: { type: 'integer', min: 0, max: 120 } entry (line 63). No other changes — the circular-import guard (imports DEFAULT_SETTINGS from db-shape.js, not settings.js) remains intact.

    Run tests — confirm exit 0. Verify grep "noNapFired" forecast.js returns 0 matches. Verify grep "noNapBedtimeOffsetMinutes" forecast.js db-shape.js settings-validate.js returns 0 matches.
  </action>
  <acceptance_criteria>
    - node --test tests/unit/forecast.test.js exits 0 with new split routing tests passing
    - grep -c "noNapFired" js/lib/forecast.js returns 0
    - grep -c "noNapBedtimeOffsetMinutes" js/lib/forecast.js returns 0
    - grep -c "noNapBedtimeOffsetMinutes" js/lib/db-shape.js returns 0
    - grep -c "noNapBedtimeOffsetMinutes" js/lib/settings-validate.js returns 0
    - forecast() blend test: given score=70, napDay.central=1200, noNapDay.central=1260, asserted central = Math.round(0.7*1200 + 0.3*1260) = 1238
    - PRED-10 (isIntenseToday) block is present and unchanged in forecast.js — grep "isIntenseToday" returns a match
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/unit/forecast.test.js</automated>
    <fails_when>non-zero exit code, or any "not ok" line for the split bedtime routing describe block, or grep "noNapFired" js/lib/forecast.js produces output</fails_when>
  </verify>
  <done>
    PRED-11 block removed; split bedtime routing live per D-12; noNapBedtimeOffsetMinutes purged from all three files; integration tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wake-anchored nap routing + today-screen.js context wiring (PRED-21, PRED-22, D-13, D-14, D-15)</name>
  <files>js/lib/forecast.js, js/ui/today-screen.js, tests/unit/forecast.test.js</files>
  <read_first>
    - js/lib/forecast.js lines 553–554: context destructure — todayWakeHHMM and todayNapStartHHMM were added in Task 2; verify they are present
    - js/lib/forecast.js lines 657–664: the return block with napStart/napEnd inline forecastEvent calls — these are the replacement targets
    - js/ui/today-screen.js lines 895–940: the forecastContext definition (lines 903–908), the forecast() call (line 913), and the post-forecast napProbability block (lines 915–938) — this entire section is refactored
    - .planning/phases/19-split-bedtime-wake-anchored-nap/19-PATTERNS.md: napProbability context threading excerpt; forecast() return block structure
    - tests/unit/forecast.test.js: the forecast() wake-anchored nap tests to append (new describe block)
  </read_first>
  <behavior>
    - forecast() with todayWakeHHMM='07:00' and buildNapGapSeries returning [90,100,110] (3 values, minDays=3): napStart.central = minutesToTime(420 + percentileFromArray([90,100,110], 50)); napStart.min uses P10; napStart.max uses P90
    - forecast() with todayWakeHHMM=null and sufficient napStart history: napStart falls back to forecastEvent(d => extractTime(d.napStart)) — time-of-day percentiles (D-14)
    - forecast() with todayWakeHHMM present but napGaps.length < minDays: napStart falls back to forecastEvent time-of-day (D-14)
    - forecast() with todayNapStartHHMM='09:15' and buildNapDurationSeries returning [60,70,80]: napEnd.central = minutesToTime(555 + percentileFromArray([60,70,80], 50))
    - forecast() with todayNapStartHHMM=null and napStartPred.central='09:00' (from wake-anchor computation): napEnd anchors to '09:00' central — minutesToTime(540 + P50(durations))
    - forecast() with both todayNapStartHHMM=null and napStartPred.central=null: napEnd falls back to forecastEvent(d => extractTime(d.napEnd))
  </behavior>
  <action>
    RED phase — in forecast.test.js append describe('forecast() wake-anchored nap (PRED-20/21/22)', ...) block with it cases from the behavior block. Pass todayWakeHHMM and todayNapStartHHMM in the context argument. Run tests — expect failure.

    GREEN phase (forecast.js) — in the forecast() function, before the return block (line 657), add wake-anchored nap computation:
    1. const napGaps = buildNapGapSeries(window). Compute napStartPred: if todayWakeHHMM is non-null and napGaps.length >= settings.minDays, then wakeMin = timeToMinutes(todayWakeHHMM) and napStartPred = { central: minutesToTime(wakeMin + Math.round(percentileFromArray(napGaps, 50))), min: minutesToTime(wakeMin + Math.round(percentileFromArray(napGaps, 10))), max: minutesToTime(wakeMin + Math.round(percentileFromArray(napGaps, 90))) }; else napStartPred = forecastEvent(d => extractTime(d.napStart)).
    2. const napDurs = buildNapDurationSeries(window). Resolve anchor: napStartAnchorHHMM = todayNapStartHHMM if non-null, else napStartPred.central if non-null, else null. Compute napEndPred: if napStartAnchorHHMM is non-null and napDurs.length >= settings.minDays, then anchorMin = timeToMinutes(napStartAnchorHHMM) and napEndPred = { central: minutesToTime(anchorMin + Math.round(percentileFromArray(napDurs, 50))), min: minutesToTime(anchorMin + Math.round(percentileFromArray(napDurs, 10))), max: minutesToTime(anchorMin + Math.round(percentileFromArray(napDurs, 90))) }; else napEndPred = forecastEvent(d => extractTime(d.napEnd)).
    3. Update the return block: replace `napStart: forecastEvent(d => extractTime(d.napStart)), napEnd: forecastEvent(d => extractTime(d.napEnd))` with `napStart: napStartPred, napEnd: napEndPred`.

    GREEN phase (today-screen.js) — refactor lines 895–940:
    1. Move the _getSlotTime helper definition to just before the forecastContext block (currently inside the post-forecast if-block at line 919).
    2. Before the forecastContext definition: compute todayWakeHHMM = _getSlotTime(todayDayRecord?.wake ?? null); compute todayNapStartHHMM = todayNapStart ? (todayNapStart.at?.slice(11) ?? null) : null; compute napStreak (existing loop — move here unchanged); compute currentHour = new Date().getHours() (gsd:allow-ui-clock); compute currentMinute = new Date().getMinutes() (gsd:allow-ui-clock); compute napProbabilityScore = napProbability(forecastDays, snap, { currentHour, currentMinute, napStreak, todayWakeHHMM }) — this call moves from post-forecast to pre-forecast.
    3. Add todayWakeHHMM, napProbabilityScore, todayNapStartHHMM to the forecastContext object (per D-13).
    4. After the forecast() call, replace the post-forecast napProbability computation block: keep only `if (predictions.napStart && !predictions.isColdStart) { predictions.napStart.napProbabilityScore = napProbabilityScore; }` — use the already-computed value, do not call napProbability again.
    5. Remove the currentMinute and napStreak variable declarations from the old post-forecast if-block (they are now pre-computed).

    Run npm run test:unit — confirm exit 0.
  </action>
  <acceptance_criteria>
    - node --test tests/unit/forecast.test.js exits 0 with all wake-anchor behavior assertions passing
    - npm run test:unit exits 0 (unit + integration tests including settings-validate, sw-precache)
    - forecast.js return block: grep "forecastEvent(d => extractTime(d.napStart))" returns 0 matches in the return statement itself (the inline call is replaced; the fallback path calls it via napStartPred variable)
    - today-screen.js: forecastContext object contains todayWakeHHMM, napProbabilityScore, todayNapStartHHMM — grep "todayWakeHHMM" js/ui/today-screen.js returns a match inside the forecastContext literal
    - today-screen.js: napProbability is called before the forecast() call — grep line order: napProbability call appears on an earlier line than forecast(forecastDays
    - predictions.napStart.napProbabilityScore is still set post-forecast (UI rendering remains unchanged) — grep "napProbabilityScore" js/ui/today-screen.js returns a match in the post-forecast block
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit</automated>
    <fails_when>non-zero exit code, or any "not ok" line in the output, or "0 subtests passed" for the forecast() wake-anchored nap describe block</fails_when>
  </verify>
  <done>
    Wake-anchored nap-start and nap-end routing live in forecast(); today-screen.js supplies todayWakeHHMM, napProbabilityScore, todayNapStartHHMM to forecastContext before calling forecast(); all unit and integration tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| today-screen.js context → forecast() | napProbabilityScore, todayWakeHHMM, todayNapStartHHMM are caller-supplied; forecast() applies null guards before use |
| localStorage blob → db-shape.js migration | Old blobs with orphan noNapBedtimeOffsetMinutes key are silently ignored on load |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-02-01 | Tampering | forecast() split bedtime blend | low | mitigate | napProbabilityScore clamped to 0–100 by napProbability() before reaching forecast(); null check guards the blend branch (D-07); blend uses Math.round on integer-minute values — no DOM exposure |
| T-19-02-02 | Tampering | today-screen.js context build | low | accept | todayWakeHHMM and todayNapStartHHMM are derived from eventLog store events which go through db-shape.js validation; no user-typed string reaches these fields directly |
| T-19-02-03 | Tampering | db-shape.js orphan field | low | accept | Old exported JSON blobs containing noNapBedtimeOffsetMinutes are loaded without error; the orphan key is never read after migration — no injection vector |
</threat_model>

<verification>
Run `npm run test:unit` — all unit and integration tests pass, exit 0.
Verify `grep -c "noNapFired" js/lib/forecast.js` returns 0.
Verify `grep -c "noNapBedtimeOffsetMinutes" js/lib/forecast.js js/lib/db-shape.js js/lib/settings-validate.js` returns 0 for all three files.
Verify `grep "todayWakeHHMM" js/ui/today-screen.js` shows the field inside the forecastContext literal (not only in the napProbability call).
Confirm that all five new exports from Plan 19-01 appear in the forecast.js export surface.
</verification>

<success_criteria>
forecast() applies the split-series bedtime model (PRED-18/19) and wake-anchored nap predictions (PRED-21/22); PRED-11 block and noNapBedtimeOffsetMinutes are fully removed; today-screen.js supplies the three new context fields before calling forecast(); npm run test:unit exits 0.
</success_criteria>

## Source Coverage Audit

| Source | Item | Status | Plan |
|--------|------|--------|------|
| GOAL | Split bedtime model (buildBedtimeSeriesNapDay, buildBedtimeSeriesNoNapDay) | COVERED | 19-01, 19-02 |
| GOAL | Probability-weighted blending when nap status undetermined | COVERED | 19-02 |
| GOAL | buildNapGapSeries, buildNapDurationSeries in forecast.js | COVERED | 19-01 |
| GOAL | Classic algorithm anchors nap-start to wake time via gap percentiles | COVERED | 19-02 |
| GOAL | Unit tests cover split-series selection and wake-anchor arithmetic | COVERED | 19-01, 19-02 |
| REQ | PRED-18: split bedtime series functions | COVERED | 19-01, 19-02 |
| REQ | PRED-19: blend when nap status undetermined | COVERED | 19-02 |
| REQ | PRED-20: buildNapGapSeries | COVERED | 19-01 |
| REQ | PRED-21: wake-anchored nap-start | COVERED | 19-02 |
| REQ | PRED-22: buildNapDurationSeries + nap-end anchor | COVERED | 19-01, 19-02 |
| CONTEXT | D-01: all 5 functions in forecast.js | COVERED | 19-01 |
| CONTEXT | D-02: return shape integer minutes | COVERED | 19-01 |
| CONTEXT | D-03: routing inline in forecast() | COVERED | 19-02 |
| CONTEXT | D-04: nap day = napStart != null | COVERED | 19-01, 19-02 |
| CONTEXT | D-05: blend formula | COVERED | 19-02 |
| CONTEXT | D-06: PRED-19 fires when napStart not logged (no hour gate) | COVERED | 19-02 |
| CONTEXT | D-07: null score → overall series fallback | COVERED | 19-02 |
| CONTEXT | D-08: sub-series < minDays → null | COVERED | 19-01 |
| CONTEXT | D-09: remove PRED-11 block (costly) | COVERED + checkpoint | 19-02 |
| CONTEXT | D-10: remove noNapBedtimeOffsetMinutes (costly) | COVERED + checkpoint | 19-02 |
| CONTEXT | D-11: PRED-10 intense-day still applies | COVERED | 19-02 |
| CONTEXT | D-12: routing order (split first, PRED-10 stacks) | COVERED | 19-02 |
| CONTEXT | D-13: todayWakeHHMM threading in today-screen.js | COVERED | 19-02 |
| CONTEXT | D-14: no wake anchor → time-of-day percentile fallback | COVERED | 19-02 |
| CONTEXT | D-15: nap-end anchor from actual napStart or predicted | COVERED | 19-02 |
| CONTEXT | D-16: percentileFromArray exported helper | COVERED | 19-01 |

<output>
Create `.planning/phases/19-split-bedtime-wake-anchored-nap/19-02-SUMMARY.md` when done.
</output>
