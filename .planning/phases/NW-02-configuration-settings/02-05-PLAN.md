---
phase: 02-configuration-settings
plan: "05"
type: execute
wave: 3
depends_on: [02-04]
files_modified:
  - js/ui/today-screen.js
  - js/app.js
  - style.css
  - tests/e2e/grouping-toggle.spec.js
  - tests/e2e/regression-phase1.spec.js
autonomous: true
requirements:
  - CFG-08

must_haves:
  truths:
    - "Today screen shows a 'View: Calendar | Sleep cycle' grouping toggle above the day list (D2-16)"
    - "Calendar button has aria-pressed='true' on first load (default groupingMode='calendar' per D2-03)"
    - "Clicking 'Sleep cycle' button fires settings.update({groupingMode:'sleepCycle'}) immediately — no Save step (D2-16 commit-on-click exception)"
    - "After toggling to sleepCycle, day list re-renders via eventLog.daysBySubjectiveNight(settings.get().cutoverHour, 7) (D2-15)"
    - "After toggling to calendar, day list re-renders via eventLog.daysByCalendar(7) (D2-15)"
    - "The cutoverHour used in daysBySubjectiveNight is settings.get().cutoverHour (from user's persisted setting) — NOT a hardcoded 4 (D2-17)"
    - "BUCKET_CONFIG.defaultCutoverHour === 4 is unchanged — injection is at the call site (D2-17, Pitfall #2)"
    - "Event-row times display via formatTime(evt.at, snap.timeFormat) — 24h default renders same as Phase 1 hhmm() output (D2-18)"
    - "settings.subscribe on Today screen re-renders the day list and reflects new groupingMode + cutoverHour changes immediately after Save (D2-09)"
    - "Phase 1 E2E regression suite still passes with default cutoverHour=4 (Nyquist D8)"
  artifacts:
    - path: js/ui/today-screen.js
      provides: "mountTodayScreen now accepts settings parameter; grouping-mode toggle DOM; formatTime integration; subscribe to settings store; bucketer call switched based on groupingMode"
      contains: "daysBySubjectiveNight"
    - path: tests/e2e/grouping-toggle.spec.js
      provides: "CFG-08 user-story E2E — toggle calendar↔sleepCycle, log event at cutover-straddling time, assert grouping changes"
      min_lines: 50
    - path: tests/e2e/regression-phase1.spec.js
      provides: "Phase 1 happy-path re-run with cutoverHour=4 default to confirm no regression (Nyquist D8)"
      min_lines: 20
  key_links:
    - from: js/ui/today-screen.js
      to: js/lib/day-bucket.js
      via: "eventLog.daysBySubjectiveNight(snap.cutoverHour, 7) — cutoverHour from settings, NOT BUCKET_CONFIG constant (D2-17)"
      pattern: "daysBySubjectiveNight.*cutoverHour"
    - from: js/ui/today-screen.js
      to: js/store/settings.js
      via: "settings.subscribe(fn) — re-render on settings change; commit-on-click via settings.update({groupingMode})"
      pattern: "subscribe"
    - from: js/ui/today-screen.js
      to: js/lib/time.js
      via: "import { formatTime } — replaces hhmm(evt.at) with formatTime(evt.at, snap.timeFormat)"
      pattern: "formatTime"
---

<objective>
MVP-CRITICAL PLAN. This is the phase-goal deliverable: the user-story sentence "As a parent tracking a child's sleep, I want to set the day-cutover hour and have it stick across reloads, so that day-grouping matches our household's actual sleep cycle, not a hardcoded default" lands here.

Specifically: the grouping-mode quick-toggle (Calendar | Sleep cycle) appears above the Today list, commits on click, and the day list immediately re-buckets using the user's persisted cutoverHour. The cutoverHour is now wired from settings.get() rather than a hardcoded 4.

Output:
  - js/ui/today-screen.js (extended — grouping toggle, settings subscribe, formatTime, cutoverHour wiring)
  - style.css (grouping toggle styling)
  - tests/e2e/grouping-toggle.spec.js (CFG-08 E2E)
  - tests/e2e/regression-phase1.spec.js (Phase 1 regression guard per Nyquist D8)
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/NW-02-configuration-settings/02-CONTEXT.md
@.planning/phases/NW-02-configuration-settings/02-RESEARCH.md
@.planning/phases/NW-02-configuration-settings/02-PATTERNS.md
@.planning/phases/NW-02-configuration-settings/02-UI-SPEC.md
@js/ui/today-screen.js
@js/lib/day-bucket.js
@js/lib/time.js
@js/app.js
@.planning/phases/NW-02-configuration-settings/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire settings into Today screen — grouping toggle + cutoverHour + formatTime subscriber</name>
  <files>js/ui/today-screen.js, style.css</files>
  <read_first>
    - js/ui/today-screen.js (full file): existing mountTodayScreen({root, eventLog}) — current signature, existing render() function, existing hhmm() private helper, existing dayList construction. Phase 2 extends: add settings parameter, add toggle DOM, subscribe, swap bucketer call, replace hhmm with formatTime. (PATTERNS.md §today-screen.js §Phase 2 extension)
    - js/lib/day-bucket.js: BUCKET_CONFIG.defaultCutoverHour === 4 (frozen) — Phase 2 MUST NOT mutate this constant; inject settings.get().cutoverHour at the call site only (D2-17, Pitfall #2)
    - js/lib/time.js: export formatTime(at, timeFormat) added in Plan 02-03 — replaces the private hhmm(at) helper in today-screen.js
    - 02-CONTEXT.md D2-15: render() reads daysByCalendar(7) when groupingMode==='calendar', daysBySubjectiveNight(cutoverHour, 7) when 'sleepCycle'
    - 02-CONTEXT.md D2-16: grouping toggle commits-on-click (ONLY exception to D2-14 explicit-Save policy); two <button aria-pressed> elements; data-grouping attribute
    - 02-CONTEXT.md D2-17: BUCKET_CONFIG.defaultCutoverHour stays 4; settings injection is AT the call site
    - 02-CONTEXT.md D2-18: formatTime(evt.at, snap.timeFormat) replaces hhmm(evt.at) in event row renderer
    - 02-CONTEXT.md D2-09: settings.subscribe((snap) => { reflectGrouping(snap); render(); }) — both toggle state and day list re-render on ANY settings change
    - 02-RESEARCH.md §Pattern G: two-button toggle with aria-pressed, commits-on-click, subscriber chain handles both aria-pressed and render
    - 02-RESEARCH.md §Anti-patterns: NEVER mutate BUCKET_CONFIG.defaultCutoverHour; subscribers must capture unsubscribe function (manual-entry concern for leaks — Today screen is long-lived so leak is dormant but pattern should be correct)
    - 02-UI-SPEC.md §Grouping-Mode Toggle: div.groupingToggle, role="group", aria-label="Day grouping", button height 40px, active button uses accent color
  </read_first>
  <behavior>
    After the change:
    - mountTodayScreen accepts {root, eventLog, settings} (settings was already forwarded from app.js in Plan 02-03)
    - A div.groupingToggle with two buttons (data-grouping="calendar" and data-grouping="sleepCycle") appears above the dayList section
    - Calendar button has aria-pressed="true" on default load; Sleep cycle has aria-pressed="false"
    - Clicking Sleep cycle: settings.update({groupingMode:'sleepCycle'}) called; subscriber fires; dayList re-renders; Calendar aria-pressed="false", SleepCycle aria-pressed="true"
    - render() calls daysBySubjectiveNight(snap.cutoverHour, 7) in sleepCycle mode — cutoverHour from settings.get(), not hardcoded 4
    - render() calls daysByCalendar(7) in calendar mode
    - Event rows display times via formatTime(evt.at, snap.timeFormat) instead of hhmm(evt.at)
    - BUCKET_CONFIG.defaultCutoverHour still === 4 after this change (grep assertion)
  </behavior>
  <action>
    Modify js/ui/today-screen.js:

    1. Add import { formatTime } from '../lib/time.js';

    2. Change signature: export function mountTodayScreen({ root, eventLog, settings }) — settings parameter now consumed

    3. Build grouping toggle after the existing quickLog row (before dayList):
       const toggle = el('div', {className:'groupingToggle', role:'group', 'aria-label':'Day grouping'});
       const calBtn = el('button', {type:'button', 'data-grouping':'calendar', textContent:'Calendar'});
       const cycBtn = el('button', {type:'button', 'data-grouping':'sleepCycle', textContent:'Sleep cycle'});
       toggle.appendChild(calBtn); toggle.appendChild(cycBtn);
       root.insertBefore(toggle, dayList); (or root.appendChild(toggle) before dayList is appended)

    4. reflectGrouping(snap) helper: for each button in toggle.querySelectorAll('button[data-grouping]') → set aria-pressed to String(btn.getAttribute('data-grouping') === snap.groupingMode)

    5. toggle.addEventListener('click', (event) => { const btn = event.target.closest('button[data-grouping]'); if (!btn) return; const next = btn.getAttribute('data-grouping'); if (next !== settings.get().groupingMode) settings.update({groupingMode: next}); }); — subscriber handles the rest

    6. Update render() function:
       const snap = settings.get();
       const days = snap.groupingMode === 'sleepCycle' ? eventLog.daysBySubjectiveNight(snap.cutoverHour, 7) : eventLog.daysByCalendar(7);
       Replace hhmm(evt.at) with formatTime(evt.at, snap.timeFormat) in the event-row renderer (wherever hhmm is called, pass snap.timeFormat)

    7. Subscribe: const unsub = settings.subscribe((next) => { reflectGrouping(next); render(); });
       Call reflectGrouping(settings.get()) once at mount for initial state.

    8. Initial render() call with settings-aware bucketer.

    Add to style.css:
    .groupingToggle { display:flex; gap:8px; padding:8px 0; }
    .groupingToggle button { padding:8px 16px; min-height:40px; border:1px solid; border-radius:4px; background:transparent; cursor:pointer; }
    .groupingToggle button[aria-pressed="true"] { /* will use --color-accent in Phase 8; for now: font-weight:600 or outline */ font-weight:600; }
  </action>
  <verify>
    <automated>node --test</automated>
  </verify>
  <done>
    - node --test exits 0 — no regression
    - js/ui/today-screen.js contains 'daysBySubjectiveNight' and 'snap.cutoverHour'
    - grep 'BUCKET_CONFIG.defaultCutoverHour' js/lib/day-bucket.js still shows value 4 (not mutated)
    - grep 'formatTime' js/ui/today-screen.js → ≥ 1 (hhmm replaced)
    - App loads in browser: two toggle buttons visible above day list, Calendar active by default
    - Clicking Sleep cycle re-buckets the list immediately (visually verifiable in browser)
  </done>
</task>

<task type="auto">
  <name>Task 2: E2E — CFG-08 grouping toggle user-story + Phase 1 regression guard</name>
  <files>tests/e2e/grouping-toggle.spec.js, tests/e2e/regression-phase1.spec.js</files>
  <read_first>
    - tests/e2e/ existing specs: read manual-entry.spec.js or quick-log.spec.js for beforeEach, page.goto, page.click, page.locator, expect patterns
    - 02-CONTEXT.md D2-27: CFG-08 E2E contract — "toggle to sleep-cycle, log an event at the cutover-straddling time, assert the day-grouping changes"
    - 02-VALIDATION.md §Nyquist Dimension 8: Phase 1 regression test — re-run Phase 1 happy-path with default cutoverHour=4 to confirm daysByCalendar output is unchanged
    - 02-RESEARCH.md §Validation Architecture §Phase Requirements → Test Map CFG-08 row
    - 02-UI-SPEC.md §Grouping-Mode Toggle §Interaction: User clicks "Sleep cycle" → day list re-renders via daysBySubjectiveNight(cutoverHour)
    - The E2E for cutover straddling: log an event with time close to 04:00 (e.g., 03:50 and then another at 04:10 on the same wall-clock day). In sleepCycle mode with cutoverHour=4, 03:50 belongs to the previous subjective night; in calendar mode, both belong to the same date. Assert day headers differ.
  </read_first>
  <action>
    Create tests/e2e/grouping-toggle.spec.js with these scenarios:

    'Calendar mode is active by default (aria-pressed="true")':
    - page.goto('/'); expect button[data-grouping="calendar"] to have attribute aria-pressed with value 'true'; expect button[data-grouping="sleepCycle"] to have attribute aria-pressed with value 'false'

    'Clicking Sleep cycle commits immediately and updates aria-pressed':
    - Click button[data-grouping="sleepCycle"]; expect button[data-grouping="sleepCycle"] aria-pressed==='true'; expect button[data-grouping="calendar"] aria-pressed==='false'

    'Grouping mode persists across reload':
    - Click Sleep cycle; reload; expect button[data-grouping="sleepCycle"] aria-pressed==='true'

    'Grouping mode is also reflected in Settings modal':
    - Click Sleep cycle; open Settings modal; expect select[name="groupingMode"] to have value 'sleepCycle'

    'CFG-08: day-grouping changes when toggling modes with a cutover-straddling event':
    - Set cutoverHour to 4 via Settings modal (default); log an event at '03:50' (use manual entry, today's date minus offset so 03:50 is unambiguously in the past); log another at '05:00' on the same wall-clock date
    - In Calendar mode: both events appear under the SAME day header (same YYYY-MM-DD)
    - Toggle to Sleep cycle: event at 03:50 appears under a DIFFERENT day header than 05:00 (03:50 belongs to the previous subjective night)
    - (If logging past events to achieve the cutover straddling is complex for the E2E, simplify: use the existing daysBySubjectiveNight integration test from Plan 02-02 as the unit-level gate, and use the E2E only for the toggle aria-pressed + persistence behavior. Note this simplification in the spec file with a comment.)

    Create tests/e2e/regression-phase1.spec.js:

    'Phase 1 regression: quick-log four event types still work with default settings':
    - page.goto('/'); Click 'Woke up' → one event row visible; click 'Going to sleep' → two rows; click 'Nap start' → three rows; click 'Nap end' → four rows
    - Reload → four rows still visible (localStorage persistence unchanged)
    - Day grouping uses calendar mode (default) — same behavior as Phase 1 (cutoverHour=4 default produces same output as Phase 1's hardcoded 4)

    Run npx playwright test — all E2E green.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/grouping-toggle.spec.js tests/e2e/regression-phase1.spec.js</automated>
  </verify>
  <done>
    - grouping-toggle.spec.js all scenarios green: aria-pressed behavior, persistence, Settings mirror, cutover-straddling grouping change (or documented simplification)
    - regression-phase1.spec.js green: Phase 1 quick-log + reload still works with default settings
    - npx playwright test (full suite) exits 0 — no existing E2E regression
    - CFG-08 user-story deliverable is verified: day-grouping can be toggled and responds to the persisted cutoverHour setting
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grouping toggle → settings.update() | Commit-on-click path — value is constrained to 'calendar' or 'sleepCycle' by data-grouping attribute, not user-typed input |
| settings.cutoverHour → daysBySubjectiveNight() | Integer 0-23 validated by settings store on load and on save; bucketer receives a trusted integer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-18 | Tampering | grouping toggle commit-on-click | accept | Value is sourced from btn.getAttribute('data-grouping') which is static HTML author-controlled; not user-typed input. Enum validated by settings store's validateSettings(mode:'load') on construction |
| T-2-19 | Tampering | BUCKET_CONFIG mutation | mitigate | D2-17 explicit — plan action states cutoverHour injected at call site only; acceptance_criteria includes grep assertion on BUCKET_CONFIG.defaultCutoverHour; Pitfall #2 documented |
| T-2-20 | Tampering | subscriber re-entry on toggle click | mitigate | Pattern G uses snapshot-before-iterate (const subs = [...subscribers]) in settings store update() — any subscribe() call inside a subscriber callback is safe |
| T-2-21 | Tampering | cross-tab grouping divergence | accept | Single-tab use case per Phase 1 decision; Phase 8 BroadcastChannel |
| T-2-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
Full suite green:

```
node --test && npx playwright test
```

Key source assertions:
- grep -c 'daysBySubjectiveNight' js/ui/today-screen.js → ≥ 1 (bucketer wired to settings)
- grep -c 'snap.cutoverHour' js/ui/today-screen.js → ≥ 1 (from settings, not hardcoded)
- grep -c 'formatTime' js/ui/today-screen.js → ≥ 1 (replaces hhmm)
- grep -c 'defaultCutoverHour = 4' js/lib/day-bucket.js → 1 (unchanged — CRITICAL invariant)
- grep -c 'aria-pressed' js/ui/today-screen.js → ≥ 1 (toggle buttons)
</verification>

<success_criteria>
- Phase goal user-story delivered: grouping mode toggle visible above Today list; toggling to 'Sleep cycle' re-buckets the day list using settings.get().cutoverHour (not hardcoded 4)
- CFG-08 E2E (grouping-toggle.spec.js) all green
- Phase 1 regression guard (regression-phase1.spec.js) all green
- BUCKET_CONFIG.defaultCutoverHour=4 remains unchanged (critical invariant)
- Full test suite (node --test && npx playwright test) exits 0
</success_criteria>

<output>
Create .planning/phases/NW-02-configuration-settings/02-05-SUMMARY.md when done.
</output>
