---
phase: quick-260829-w16
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - js/ui/settings-modal.js
autonomous: true
requirements:
  - TIF-03

estimate:
  tokens: 18000
  raw_tokens: 12000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - Forecast algorithm selector is the first field in the Forecast & Prediction fieldset
    - Classic-only fields (autoOutlier, maxDelta, windowDays, statBlend) are hidden when TIF is selected
    - TIF-only fields (trimPct, tifRollingDays, precisionTarget) are hidden when Classic is selected
    - Shared fields (minDays, eveningHour, noNapBedtimeOffsetMinutes) are always visible
    - The tif-subsection wrapper div and its TIF Algorithm label are gone
  artifacts:
    - index.html (restructured Forecast & Prediction fieldset)
    - js/ui/settings-modal.js (updated populateForm and change handler)
  key_links:
    - populateForm() in settings-modal.js must set classicOptions.hidden on open, mirroring what it does for tifOptions
    - forecastAlgorithmChangeHandler must toggle both classicOptions and tifOptions
---

<objective>
Move the forecastAlgorithm select to the top of the Forecast & Prediction fieldset, wrap classic-only fields in a #classicOptions div, and update the JS show/hide logic to toggle both #classicOptions and #tifOptions based on the selected algorithm.

Purpose: Users switching algorithms see only the relevant fields without scrolling past irrelevant controls.
Output: Restructured fieldset HTML + updated show/hide logic in settings-modal.js.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/my-code/vibe-coding/night-watch/.planning/PROJECT.md
@C:/my-code/vibe-coding/night-watch/.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure Forecast & Prediction fieldset in index.html</name>
  <files>index.html</files>
  <action>
Replace the entire Forecast & Prediction fieldset (lines 254–312) with the restructured version below. The target order and grouping:

1. `<legend>Forecast & Prediction</legend>`
2. Algorithm selector label (moved from tif-subsection to top):
   - `<label>Forecast algorithm <select name="forecastAlgorithm">...</select></label>`
3. `<div id="classicOptions">` wrapping the four classic-only fields in this order:
   - autoOutlier checkbox label
   - maxDelta number input label
   - windowDays number input label
   - statBlend select label
   Close the `</div>` after statBlend.
4. minDays label (shared, outside both divs)
5. eveningHour label (shared, outside both divs — preserve `id="eveningHour"`)
6. noNapBedtimeOffsetMinutes label (shared, outside both divs — preserve `id="noNapBedtimeOffsetMinutes"`)
7. `<div id="tifOptions" hidden>` containing trimPct, tifRollingDays, precisionTarget (same markup as before — preserve `id="tifRollingDays"` on the input)

Remove completely: the `<div class="tif-subsection">` wrapper and the `<p class="tif-subsection-label">TIF Algorithm</p>` paragraph.

Do NOT use innerHTML anywhere. This is a static HTML restructure only — no JS changes in this task.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const fPos=h.indexOf('forecastAlgorithm');const lPos=h.indexOf('Forecast &amp; Prediction');console.assert(fPos>lPos &amp;&amp; fPos&lt;lPos+300,'forecastAlgorithm must appear near top of fieldset');console.assert(h.includes('id=\"classicOptions\"'),'classicOptions div must exist');console.assert(!h.includes('tif-subsection'),'tif-subsection must be removed');console.log('HTML structure OK');"</automated>
  </verify>
  <done>
    - forecastAlgorithm select is the first input element inside the Forecast & Prediction fieldset
    - id="classicOptions" div exists and contains autoOutlier, maxDelta, windowDays, statBlend
    - No div.tif-subsection or p.tif-subsection-label remain in the file
    - id="tifOptions" div still exists with trimPct, tifRollingDays, precisionTarget
    - minDays, eveningHour, noNapBedtimeOffsetMinutes are outside both divs
  </done>
</task>

<task type="auto">
  <name>Task 2: Update settings-modal.js to toggle #classicOptions alongside #tifOptions</name>
  <files>js/ui/settings-modal.js</files>
  <action>
Two targeted edits in js/ui/settings-modal.js:

**Edit A — populateForm():** After the line that sets tifOptionsEl.hidden (around line 94), add parallel logic for classicOptions. The existing block reads:
```
const tifOptionsEl = document.getElementById('tifOptions');
if (tifOptionsEl) tifOptionsEl.hidden = (s.forecastAlgorithm !== 'tif');
```
Extend it to also handle classicOptions:
```
const classicOptionsEl = document.getElementById('classicOptions');
if (classicOptionsEl) classicOptionsEl.hidden = (s.forecastAlgorithm === 'tif');
```
Place this immediately after the tifOptions line, still inside the populateForm function.

**Edit B — _forecastAlgorithmChangeHandler:** The existing handler (around lines 106–109) reads:
```
_forecastAlgorithmChangeHandler = () => {
  tifOptionsEl.hidden = (forecastAlgorithmEl.value !== 'tif');
};
```
Update it to also toggle classicOptions:
```
_forecastAlgorithmChangeHandler = () => {
  const isTif = forecastAlgorithmEl.value === 'tif';
  tifOptionsEl.hidden = !isTif;
  const classicEl = document.getElementById('classicOptions');
  if (classicEl) classicEl.hidden = isTif;
};
```

No other changes to the file. The module-level ref (_forecastAlgorithmChangeHandler), removeEventListener pattern, and all other logic remain untouched.
  </action>
  <verify>
    <automated>npm run test:unit 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    - populateForm() hides #classicOptions when forecastAlgorithm is 'tif' and shows it otherwise
    - The change handler toggles both #classicOptions and #tifOptions on algorithm switch
    - npm run test:unit passes with no new failures
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form inputs → settings store | User-entered algorithm selection reaches store only through validateSettings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-w16-01 | Tampering | forecastAlgorithm select | low | accept | Value is validated by validateSettings before reaching the store; static option list in HTML |
</threat_model>

<verification>
After both tasks:
1. Open Settings modal in browser, confirm forecastAlgorithm select appears at top of Forecast & Prediction fieldset
2. Switch to TIF: classic-only fields (autoOutlier, maxDelta, windowDays, statBlend) disappear; TIF fields appear
3. Switch back to Classic: classic fields reappear; TIF fields hide
4. Shared fields (minDays, eveningHour, noNapBedtimeOffsetMinutes) remain visible in both modes
5. npm run test:unit passes
</verification>

<success_criteria>
- forecastAlgorithm select is visually first in the Forecast & Prediction fieldset
- Selecting TIF hides all classic-only fields and shows TIF-only fields
- Selecting Classic shows all classic-only fields and hides TIF-only fields
- Shared fields are always visible regardless of selection
- tif-subsection div and its label are gone from the DOM
- All unit tests pass
</success_criteria>

<output>
Create `.planning/quick/260829-w16-move-algorithm-selector-to-top-of-foreca/260829-w16-SUMMARY.md` when done
</output>
