---
phase: NW-13
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/lib/db-shape.js
  - js/lib/settings-validate.js
  - js/ui/settings-modal.js
  - index.html
  - js/lib/forecast-tif.js
  - js/ui/today-screen.js
  - tests/integration/forecast-tif.integration.test.js
autonomous: true
requirements:
  - TIF-13

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "tifForecast(dayRecords, settings, activityLog = {}, isNoNapDay = false) — 4-param signature accepted without error (D-06, D-09, D-10, D-15)"
    - "forecast-tif.js slices the last tifRollingDays (not windowDays) records from dayRecords (D-06); default 7 when tifRollingDays absent"
    - "actBeforeNapPerDay is an index-aligned array (same length as the TIF window slice) where activityLog[d.date] overrides activityBeforeNap(d) when non-null (D-09, D-10)"
    - "actBeforeNap (the filtered version used by existing windows) is derived from actBeforeNapPerDay.filter(v => v !== null) (D-09)"
    - "today-screen.js resolves isNoNapDay as (forecastContext.currentHour >= snap.eveningHour) && (todayDayRecord?.napStart == null) and passes it as 4th arg to tifForecast (D-15)"
    - "today-screen.js calls eventLog.getActivityLog() and passes the result as 3rd arg to tifForecast (D-10)"
    - "DEFAULT_SETTINGS in db-shape.js includes tifRollingDays: 7 (D-07)"
    - "db-shape.js additive migration injects tifRollingDays: 7 on blobs that lack the field (D-07)"
    - "settings-validate.js enforces tifRollingDays as integer in range 3–30 (D-07)"
    - "Settings UI shows TIF history window (days) number input with min=3, max=30 in the tifOptions fieldset (D-07)"
    - "When tifRollingDays exceeds the available history length, slice(-N) returns the full available array — no extra gate needed; detectColdStart / minDays handles the true cold-start case (D-08)"
    - "All existing TIF integration tests pass after updating defaultSettings fixture to include tifRollingDays: 7"
  artifacts:
    - "js/lib/db-shape.js — DEFAULT_SETTINGS.tifRollingDays = 7; additive migration block for tifRollingDays"
    - "js/lib/settings-validate.js — tifRollingDays rule { type: 'integer', min: 3, max: 30 }"
    - "js/ui/settings-modal.js — populateForm reads s.tifRollingDays; onClose raw includes tifRollingDays: Number(...)"
    - "index.html — <input type=number id=tifRollingDays name=tifRollingDays min=3 max=30 step=1> in #tifOptions"
    - "js/lib/forecast-tif.js — 4-param signature; tifRollingDays slice; actBeforeNapPerDay array"
    - "js/ui/today-screen.js — isNoNapDay computation + 4-arg tifForecast call"
    - "tests/integration/forecast-tif.integration.test.js — defaultSettings includes tifRollingDays: 7"
  key_links:
    - "today-screen.js → tifForecast: 4-arg call must match new signature exactly"
    - "actBeforeNapPerDay: index-aligned with window[] — index i gives per-day value for window[i], null when unavailable"
    - "tifRollingDays setting: must appear in DEFAULT_SETTINGS, migration, validation, UI, and algorithm"
  prohibitions:
    - "No innerHTML with user-controlled data — settings UI label/input use static HTML in index.html, not dynamic injection"
    - "No build step, no npm dependencies added"
    - "Time strings remain local wall-clock 'HH:MM' — no UTC parsing introduced"
    - "forecast-tif.js must remain pure (no direct clock or localStorage access)"

artifacts_produced:
  - "tifRollingDays setting — DEFAULT_SETTINGS entry, db migration, validation rule, settings UI input"
  - "tifForecast(dayRecords, settings, activityLog = {}, isNoNapDay = false) — extended 4-param signature"
  - "actBeforeNapPerDay — index-aligned per-day actBefore array inside tifForecast"
  - "isNoNapDay resolution — computed in today-screen.js before tifForecast call"
---

<objective>
Tracer plan: wire `tifRollingDays` end-to-end through all five layers (persistence → validation → UI → algorithm → call site) and extend the `tifForecast` signature to accept `activityLog` and `isNoNapDay`. Refactor the per-day `actBeforeNap` computation to produce an index-aligned `actBeforeNapPerDay` array, enabling the ratio windows (Plan 03) and no-nap substitution (Plan 04) to access per-day values without re-computing them.

Purpose: Establishes the architectural skeleton all subsequent TIF-13, TIF-12, and TIF-16 work builds on. Every later plan appends to the signature and uses `actBeforeNapPerDay` — getting the wiring right here prevents cascading rework.

Output: 7 files updated; all existing TIF tests pass; Settings UI shows new field; tifForecast accepts 4 parameters.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/NW-13-tif-algorithm-extensions/13-CONTEXT.md
@.planning/phases/NW-13-tif-algorithm-extensions/13-PATTERNS.md
@js/lib/forecast-tif.js
@js/lib/db-shape.js
@js/lib/settings-validate.js
@js/ui/settings-modal.js
@js/ui/today-screen.js
@tests/integration/forecast-tif.integration.test.js
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tracer: wire tifRollingDays + activityLog + isNoNapDay through all layers (D-06, D-07, D-09, D-10, D-15)</name>
  <files>
    js/lib/db-shape.js,
    js/lib/settings-validate.js,
    js/ui/settings-modal.js,
    index.html,
    js/lib/forecast-tif.js,
    js/ui/today-screen.js,
    tests/integration/forecast-tif.integration.test.js
  </files>

  <read_first>
    - js/lib/db-shape.js — read lines 41–65 (DEFAULT_SETTINGS) and lines 117–139 (additive migration blocks for trimPct, precisionTarget, eveningHour, noNapBedtimeOffsetMinutes) to copy the exact migration pattern (D-07 per 13-PATTERNS.md)
    - js/lib/settings-validate.js — read the rules object near lines 58–62 to locate trimPct and precisionTarget rules (exact insertion point for tifRollingDays rule)
    - js/ui/settings-modal.js — read lines 80–90 (populateForm TIF field reads) and lines 135–145 (onClose raw object) to locate the exact insertion points for tifRollingDays (D-07)
    - index.html — search for id="tifOptions" to locate the TIF fieldset; find trimPct input to insert tifRollingDays input immediately after it
    - js/lib/forecast-tif.js — read the full file (551 lines) focusing on: line 396 (current signature), line 404 (windowDays slice), lines 406–408 (trimPct, precisionTarget extraction), lines 421–427 (actBeforeNap computation), line 457 (actBeforeBand usage)
    - js/ui/today-screen.js — read lines 896–912 (forecastContext block and tifForecast call)
    - tests/integration/forecast-tif.integration.test.js — read lines 53–59 (defaultSettings fixture)
    - .planning/phases/NW-13-tif-algorithm-extensions/13-PATTERNS.md — read sections for TIF-13 (lines 27–59) for exact code patterns
  </read_first>

  <behavior>
    - Calling tifForecast(makeFixture(), { ...defaultSettings, tifRollingDays: 5 }, {}, false) slices only the last 5 records
    - Calling tifForecast(makeFixture(), { ...defaultSettings }, {}, false) uses tifRollingDays=7 (default) not windowDays=30
    - actBeforeNapPerDay has same length as the sliced window array; index i maps to window[i]
    - When activityLog['2024-01-15'] = 120, the day record with date '2024-01-15' uses 120 as its actBefore value, overriding activityBeforeNap(d)
  </behavior>

  <action>
    Layer 1 — db-shape.js (D-07):
    In DEFAULT_SETTINGS (around line 60), add `tifRollingDays: 7` as a new field immediately after the `precisionTarget` line. Use comment `// TIF-13 / D-07: rolling window length for TIF algorithm (valid: 3–30 days)`.
    In the additive migration section (after the Phase 12 intenseDays and noNapBedtimeOffsetMinutes blocks, before `return blob`), add a guard: if blob.settings exists and `'tifRollingDays'` is not in blob.settings, set `blob.settings.tifRollingDays = 7`. Comment: `// Phase 13 forward-compat: inject tifRollingDays for blobs predating Phase 13`.

    Layer 2 — settings-validate.js (D-07):
    Locate the rules object containing `trimPct` and `precisionTarget` entries. Add `tifRollingDays: { type: 'integer', min: 3, max: 30 }` immediately after the `precisionTarget` rule. Comment: `// TIF-13 / D-07`.

    Layer 3a — settings-modal.js (D-07):
    In `populateForm`, after the line that sets `trimPctEl.value`, add: declare `tifRollingDaysEl` by calling `form.elements.namedItem('tifRollingDays')`, then guard: if `tifRollingDaysEl` is truthy, set `tifRollingDaysEl.value` to `String(s.tifRollingDays ?? 7)`.
    In the `onClose` raw object construction (where trimPct and precisionTarget are read), add: `tifRollingDays: Number(data.get('tifRollingDays') ?? 7)`.

    Layer 3b — index.html (D-07):
    In the `#tifOptions` fieldset, immediately after the `trimPct` input element group, add a `<label for="tifRollingDays">` with text "TIF history window (days)" and an `<input type="number" id="tifRollingDays" name="tifRollingDays" min="3" max="30" step="1">`.

    Layer 4 — forecast-tif.js (D-06, D-09, D-10, D-15):
    Change the function signature on line 396 from `export function tifForecast(dayRecords, settings)` to `export function tifForecast(dayRecords, settings, activityLog = {}, isNoNapDay = false)`. Update the JSDoc `@param` list to document `activityLog` (optional object keyed by 'YYYY-MM-DD' date string, values in minutes) and `isNoNapDay` (boolean, caller-resolved: eveningHour passed and napStart null today).
    Change the windowDays slice (line 404) from `dayRecords.slice(-settings.windowDays)` to: declare `const tifRollingDays = settings.tifRollingDays ?? 7`, then `const window = dayRecords.slice(-tifRollingDays)`. Keep the variable name `window` so the rest of the function is unchanged.
    Refactor the actBeforeNap line (line 424) from `window.map(activityBeforeNap).filter(v => v !== null)` into two lines: (1) `const actBeforeNapPerDay = window.map(d => activityLog[d.date] != null ? activityLog[d.date] : activityBeforeNap(d));` and (2) `const actBeforeNap = actBeforeNapPerDay.filter(v => v !== null);`. The `actBeforeNapPerDay` variable retains nulls at their original indices for use in ratio computations (Plan 03); `actBeforeNap` is used by all existing windows unchanged.

    Layer 5 — today-screen.js (D-10, D-15):
    In the block immediately before the `const predictions = ...` line (around line 909), add two lines: `const activityLog = eventLog.getActivityLog();` and `const isNoNapDay = (forecastContext.currentHour >= snap.eveningHour) && (todayDayRecord?.napStart == null);`. Change `tifForecast(forecastDays, snap)` to `tifForecast(forecastDays, snap, activityLog, isNoNapDay)`.

    Integration test fixture:
    In `tests/integration/forecast-tif.integration.test.js`, in the `defaultSettings` object (around line 53–59), add `tifRollingDays: 7` as a new field after `precisionTarget: 60`.
  </action>

  <verify>
    <automated>node --test tests/integration/forecast-tif.integration.test.js</automated>
  </verify>

  <acceptance_criteria>
    - tifForecast.toString() or source contains `activityLog = {}` and `isNoNapDay = false` in its parameter list (new 4-param signature)
    - tifForecast.toString() or source contains `tifRollingDays` and `slice(-tifRollingDays)` (no longer uses slice(-settings.windowDays) for the TIF window)
    - tifForecast.toString() or source contains `actBeforeNapPerDay` (index-aligned array declaration present)
    - `node --test tests/integration/forecast-tif.integration.test.js` exits 0 with all test cases passing
    - `grep -n "tifRollingDays" js/lib/db-shape.js | wc -l` outputs at least 2 (DEFAULT_SETTINGS + migration)
    - `grep -n "tifRollingDays" js/lib/settings-validate.js | wc -l` outputs at least 1
    - `grep -c "tifRollingDays" index.html` outputs at least 2 (id= and name=)
    - `grep -n "tifRollingDays" js/ui/settings-modal.js | wc -l` outputs at least 2 (populate + raw)
    - `grep -n "isNoNapDay" js/ui/today-screen.js | wc -l` outputs at least 2 (declaration + usage in call)
    - `grep -n "getActivityLog" js/ui/today-screen.js | wc -l` outputs at least 1
  </acceptance_criteria>

  <done>
    All 7 files updated per the 5-layer wiring described above; tifForecast accepts 4 params; integration test passes; no other tests regress.
  </done>
</task>

<task type="auto">
  <name>Regression check: full unit test suite passes with new signature (TIF-13)</name>
  <files>tests/unit/forecast-tif.test.js</files>

  <read_first>
    - tests/unit/forecast-tif.test.js — read the full file to check if any test invokes tifForecast directly or references windowDays in a way that breaks with the new signature
    - tests/unit/settings-validate.test.js — verify tifRollingDays validation rule is exercised
  </read_first>

  <action>
    Run `npm run test:unit` to confirm the full unit and integration test suite passes. If settings-validate.test.js does not test the new `tifRollingDays` rule, add two test cases: one asserting `{ tifRollingDays: 2 }` fails validation (below min=3) and one asserting `{ tifRollingDays: 7 }` passes. If db-shape.test.js has a migration fixture that lacks `tifRollingDays`, add a test case asserting the migration injects `tifRollingDays: 7` on a blob that doesn't have it. Do not modify `forecast-tif.test.js` — trimmedMinMax tests are unaffected by the signature change.
  </action>

  <verify>
    <automated>npm run test:unit</automated>
  </verify>

  <acceptance_criteria>
    - `npm run test:unit` exits 0 with no failures
    - settings-validate.test.js includes at least one test covering the tifRollingDays range constraint
    - No test file references `settings.windowDays` when calling tifForecast (the TIF algorithm now ignores windowDays)
  </acceptance_criteria>

  <done>
    Full unit + integration test suite passes (0 failures); tifRollingDays validation and migration coverage present.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings UI → settings-validate.js | tifRollingDays input from user crosses into settings storage |
| activityLog (event store) → tifForecast | Externally-managed date-keyed number map injected into algorithm |
| today-screen.js → tifForecast | isNoNapDay boolean derived from real clock (gsd:allow-ui-clock) injected into pure function |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01-01 | Tampering | tifRollingDays out-of-range (NaN, 0, 31) | medium | mitigate | settings-validate.js enforces `type: 'integer', min: 3, max: 30`; settings-modal.js uses Number() coercion with fallback 7 |
| T-13-01-02 | Tampering | activityLog with non-numeric values (string, NaN) | low | mitigate | actBeforeNapPerDay fallback: `activityLog[d.date] != null` guard; null coerces to activityBeforeNap(d) for NaN-like strings |
| T-13-01-03 | Denial of Service | tifRollingDays=30 on large history | low | accept | 30 records is negligible; realistic load negligible on client-side pure function |
| T-13-01-04 | Information Disclosure | activityLog keys expose date history to tifForecast | low | accept | activityLog is derived from the same event store already available to today-screen.js; no new exposure |
| T-13-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs in this plan — vanilla JS, no new dependencies |
</threat_model>

<verification>
node --test tests/integration/forecast-tif.integration.test.js
npm run test:unit
</verification>

<success_criteria>
- tifForecast has a 4-param signature (dayRecords, settings, activityLog={}, isNoNapDay=false)
- TIF algorithm uses settings.tifRollingDays (default 7) for history slice, not settings.windowDays
- actBeforeNapPerDay is index-aligned with window[] inside tifForecast
- today-screen.js passes activityLog and isNoNapDay to tifForecast
- DEFAULT_SETTINGS and db migration include tifRollingDays: 7
- settings-validate.js enforces integer 3–30 range
- Settings modal saves and restores tifRollingDays; index.html fieldset shows the input
- All existing tests pass (npm run test:unit exits 0)
</success_criteria>

<output>
Create .planning/phases/NW-13-tif-algorithm-extensions/13-01-SUMMARY.md when done.
</output>
