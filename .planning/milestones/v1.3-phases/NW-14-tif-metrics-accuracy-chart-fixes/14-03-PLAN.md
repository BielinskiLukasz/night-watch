---
phase: NW-14
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
  - 14-02
files_modified:
  - js/ui/metrics-screen.js
autonomous: true
requirements:
  - MET-07
  - MET-08
  - MET-09
  - MET-10
  - MET-11

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "COLUMNS constant has exactly 16 entries in D-09 order: Date|Wake|Nap Start|Nap End|Bedtime|Sleep|Nap|Nap Frac|Comb|Day Len|Day/Sleep|→Nap|Nap→|Act|AM/PM|AAS; SAA removed (MET-07, D-09, D-14)"
    - "napFraction, dayToSleepFactor, amPmSplit columns use isRatio:true and format as toFixed(2) (D-13)"
    - "when TIF active (snap.forecastAlgorithm==='tif'), 12 TIF inline columns appear at table right; hidden entirely when TIF off (MET-08, D-11)"
    - "TIF inline column cells show algMin, algMax as formatted times and precisionScore as decimal; null/absent bounds render as '—' (MET-08, D-11)"
    - "when TIF active, 3 TIF aggregate rows (min-TIF, median-TIF, max-TIF) appear after Avg/Min/Max rows; hidden entirely when TIF off (MET-11, D-06, D-07, D-08)"
    - "min-TIF row = avg(algMin) per event type; median-TIF row = avg(central) per event type; max-TIF row = avg(algMax) per event type (D-07)"
    - "null shown when no TIF predictions exist for a given event type in the window (D-08)"
    - "all cell content set via textContent — never innerHTML with computed values (T-11-05)"
    - "[ASSUMPTION MET-11 boundary] when tifBoundsHistory contains no non-null entries for an event type, avg returns null; cell renders as '—'"
    - "[ASSUMPTION MET-11 ordering] min-TIF/median-TIF/max-TIF row order matches D-07 definition — min-TIF first"
    - "[ASSUMPTION MET-08 precision] TIF time columns use formatTime with snap.timeFormat, same as base time columns"
    - "[ASSUMPTION MET-11 adjacency] when exactly one day has TIF bounds for an event type, avg equals that single value"
  artifacts:
    - "COLUMNS constant in metrics-screen.js — 16 entries, new D-09 order"
    - "TIF_COLUMNS constant — 12 entries, all with tif:true, hidden when TIF off"
    - "computeTifBoundsHistory imported from ../lib/accuracy-tif.js"
    - "buildTifAggregateRow(label, tifAvgs, snap) — private helper for min-TIF/median-TIF/max-TIF rows"
    - "render() extended to compute tifBoundsMap and inject TIF column data + TIF aggregate rows"
  prohibitions:
    - "No fenced code blocks (```) in any <action> field"
    - "No innerHTML with user data or computed values — textContent only (T-11-05)"
    - "sleepAfterActivityFactor must be removed from the import list in metrics-screen.js (no longer used in UI)"
    - "SAA column must not appear in COLUMNS — it is fully removed from rendering (D-14)"
    - "TIF inline columns and TIF aggregate rows must use el.hidden = !isTif, not CSS display:none or class toggling"

artifacts_produced:
  - "COLUMNS updated to 16-column D-09 order in js/ui/metrics-screen.js"
  - "TIF_COLUMNS constant (12 entries) in js/ui/metrics-screen.js"
  - "buildTifAggregateRow private helper in js/ui/metrics-screen.js"
  - "render() extended: TIF branch calls computeTifBoundsHistory, merges bounds by date, populates TIF cells and TIF aggregate rows"
---

<objective>
Update `js/ui/metrics-screen.js` to implement three changes from the phase: (1) COLUMNS overhaul from 14→16 columns in D-09 order (MET-07/09/10); (2) 12 TIF inline columns visible when TIF active (MET-08); (3) 3 TIF aggregate rows (min-TIF, median-TIF, max-TIF) visible when TIF active (MET-11).

Purpose: This plan delivers the full Metrics screen overhaul. The tracer task wires the 16-column table end-to-end (users can see Day/Sleep Factor, Nap Frac, AM/PM columns immediately). Expansion tasks add TIF inline data and TIF aggregate rows on top of the proven skeleton.

Output: Updated `metrics-screen.js` with 16-column layout, TIF inline columns, and TIF aggregate rows — all reactive to forecastAlgorithm setting.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/NW-14-tif-metrics-accuracy-chart-fixes/14-CONTEXT.md
@.planning/phases/NW-14-tif-metrics-accuracy-chart-fixes/14-PATTERNS.md
@js/ui/metrics-screen.js
@js/lib/accuracy-tif.js
@.planning/phases/NW-14-tif-metrics-accuracy-chart-fixes/14-01-SUMMARY.md
@.planning/phases/NW-14-tif-metrics-accuracy-chart-fixes/14-02-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: COLUMNS 16-column overhaul — table renders with Day/Sleep, Nap Frac, AM/PM</name>
  <files>js/ui/metrics-screen.js</files>

  <read_first>
    - js/ui/metrics-screen.js — full file (359 lines): current COLUMNS const (lines 39–54); import list (lines 15–27); buildDayRow (lines 160–189); buildAggregateRow (lines 199–222); render() function (lines 276–344); formatCellValue (lines 106–119)
    - 14-PATTERNS.md §"js/ui/metrics-screen.js (MODIFY)" — exact new COLUMNS definition for D-09 order
    - 14-CONTEXT.md §D-09 — 16-column order, D-13 isRatio:true for new columns, D-14 SAA removal
  </read_first>

  <action>
    Perform three targeted changes to metrics-screen.js:

    1. UPDATE the import list (lines 15–27). Remove sleepAfterActivityFactor from the named imports (it is no longer used in the UI after D-14). Keep activityAfterSleepFactor (AAS stays). Add imports of dayToSleepFactor, napFraction, amPmSplit (required for the fallback path check — though aggregateMetrics already returns them, these may be imported for clarity; if they come solely via aggregateMetrics rows, skip adding them to imports). Note: since all new metric values arrive via aggregateMetrics return (rows array), the three new function imports are NOT required for metrics-screen.js — remove only sleepAfterActivityFactor.

    2. REPLACE the COLUMNS constant (lines 39–54) with the 16-entry D-09 order per PATTERNS.md:
       Date (sticky), Wake (isTime), Nap Start (isTime), Nap End (isTime), Bedtime (isTime), Sleep, Nap, Nap Frac (isRatio:true), Comb, Day Len, Day/Sleep (isRatio:true), →Nap, Nap→, Act, AM/PM (isRatio:true), AAS (isRatio:true).
       SAA (sleepAfterActivityFactor) is NOT in this list — it is removed per D-14.
       Update the JSDoc comment above COLUMNS to say "16-column" and reference D-09.

    3. VERIFY that buildDayRow (lines 180–186) and buildAggregateRow (lines 209–220) iterate over COLUMNS dynamically — they do (for loop over COLUMNS.length). No changes needed to those helpers; they pick up the new columns automatically through the COLUMNS array.

    This tracer change is the smallest production-quality modification: 16-column table works end-to-end with no TIF columns yet.
  </action>

  <verify>
    node --test tests/unit/metrics.test.js
    npm run test:unit
  </verify>

  <acceptance_criteria>
    - COLUMNS array has exactly 16 entries (assert by reading file: count objects in the Object.freeze([...]) literal)
    - COLUMNS entries are in exact D-09 order: index 0=date, 7=napFraction, 10=dayToSleepFactor, 14=amPmSplit, 15=activityAfterSleepFactor (AAS)
    - sleepAfterActivityFactor is absent from COLUMNS (no entry with key:'sleepAfterActivityFactor' in COLUMNS)
    - sleepAfterActivityFactor is removed from the import list in the import statement
    - napFraction column has isRatio:true
    - dayToSleepFactor column has isRatio:true
    - amPmSplit column has isRatio:true
    - npm run test:unit exits 0 (no existing tests broken by the COLUMNS change)
  </acceptance_criteria>

  <done>
    The metrics table compiles with 16-column COLUMNS constant. Existing unit tests pass. The three new ratio columns are in the correct positions per D-09. SAA column is absent.
  </done>
</task>

<task type="auto" tdd="false">
  <name>TIF inline columns — 12 hidden columns with TIF bounds per row (MET-08, D-11)</name>
  <files>js/ui/metrics-screen.js</files>

  <read_first>
    - js/ui/metrics-screen.js — current state after tracer task; focus on: render() function (lines 276–344); header row building (lines 309–317); buildDayRow function (lines 160–189); import block at top
    - 14-PATTERNS.md §"js/ui/metrics-screen.js (MODIFY)" — TIF_COLUMNS constant definition and hidden attribute toggling pattern
    - 14-CONTEXT.md §D-11 — 12 TIF column key names, hidden when TIF off
    - js/lib/accuracy-tif.js — computeTifBoundsHistory signature and TifBounds return shape
  </read_first>

  <action>
    Add TIF inline columns to the metrics table. Follow these precise steps:

    1. ADD import at top of metrics-screen.js:
       import { computeTifBoundsHistory } from '../lib/accuracy-tif.js';

    2. ADD the TIF_COLUMNS constant after COLUMNS (before the first private helper function). Define 12 entries for 4 event types × 3 fields:
       event types: wake, napStart, napEnd, bedtime
       fields per type: tif_min (isTime:true, isRatio:false), tif_max (isTime:true), tif_conf (isTime:false, isRatio:true)
       Label shortening per Claude's Discretion: 'W-min','W-max','W-conf', 'NS-min','NS-max','NS-conf', 'NE-min','NE-max','NE-conf', 'B-min','B-max','B-conf'
       All entries include tif:true for identification. Mark Object.freeze on the outer array only (entries are literals).
       Keys: 'wake_tif_min', 'wake_tif_max', 'wake_tif_conf', 'napStart_tif_min', 'napStart_tif_max', 'napStart_tif_conf', 'napEnd_tif_min', 'napEnd_tif_max', 'napEnd_tif_conf', 'bedtime_tif_min', 'bedtime_tif_max', 'bedtime_tif_conf'
       Each entry has: { key, label, isTime, isRatio, tif:true }

    3. IN render() before the table build, determine isTif:
       const isTif = snap.forecastAlgorithm === 'tif';
       When isTif is true, call computeTifBoundsHistory and build a lookup map:
         const activityLog = isTif ? eventLog.getActivityLog() : {};
         const tifBoundsArray = isTif ? computeTifBoundsHistory(days, snap, activityLog) : [];
         Build Map<date, entry>: const tifBoundsMap = new Map(tifBoundsArray.map(e => [e.date, e]));

    4. IN the header row building loop, after iterating COLUMNS, add a second loop over TIF_COLUMNS:
       for (const col of TIF_COLUMNS) {
         const th = document.createElement('th');
         th.textContent = col.label;  // T-11-05 safe (static string)
         th.hidden = !isTif;
         headerRow.appendChild(th);
       }

    5. UPDATE buildDayRow to accept tifBoundsMap and isTif parameters:
       function buildDayRow(dayMetrics, snap, tifBoundsMap, isTif)
       After the existing COLUMNS loop, add TIF cells loop:
         const tifEntry = tifBoundsMap ? tifBoundsMap.get(dayMetrics.date) : null;
         for (const col of TIF_COLUMNS) {
           const td = document.createElement('td');
           td.hidden = !isTif;
           let cellText = '—';
           if (tifEntry) {
             // Parse key: format is '{eventType}_tif_{field}' e.g. 'wake_tif_min'
             // Split at '_tif_': eventType = col.key.split('_tif_')[0]; field = col.key.split('_tif_')[1]
             const parts = col.key.split('_tif_');
             const eventType = parts[0];  // 'wake', 'napStart', 'napEnd', 'bedtime'
             const field = parts[1];       // 'min', 'max', 'conf'
             const bounds = tifEntry[eventType];
             if (bounds) {
               if (field === 'min') cellText = formatTime(bounds.algMin, snap.timeFormat);
               else if (field === 'max') cellText = formatTime(bounds.algMax, snap.timeFormat);
               else if (field === 'conf') cellText = bounds.precisionScore != null ? bounds.precisionScore.toFixed(2) : '—';
             }
           }
           td.textContent = cellText;  // T-11-05: textContent only
           tr.appendChild(td);
         }

    6. UPDATE the call site in render(): change buildDayRow(rows[i], snap) to buildDayRow(rows[i], snap, tifBoundsMap, isTif)

    Note on napStart/napEnd event type key: the TIF_COLUMNS keys use 'napStart_tif_min' etc. The tifEntry from computeTifBoundsHistory has .napStart and .napEnd keys. The split approach above with 'napStart_tif_min'.split('_tif_') gives parts[0]='napStart', parts[1]='min' — correct.

    Note on 'napStart_tif_conf' key split: 'napStart_tif_conf'.split('_tif_') → ['napStart', 'conf'] — correct.

    All td.textContent assignments only. No innerHTML used.
  </action>

  <verify>
    npm run test:unit
  </verify>

  <acceptance_criteria>
    - TIF_COLUMNS constant exists in metrics-screen.js with exactly 12 entries
    - All 12 TIF_COLUMNS entries have tif:true
    - import of computeTifBoundsHistory from '../lib/accuracy-tif.js' is present
    - eventLog.getActivityLog() is called inside render() when isTif is true
    - buildDayRow signature includes tifBoundsMap and isTif parameters
    - TIF header cells are created with th.hidden = !isTif
    - TIF data cells are created with td.hidden = !isTif
    - All TIF cell values set via textContent (grep check: no innerHTML in the TIF column section)
    - npm run test:unit exits 0
  </acceptance_criteria>

  <done>
    When TIF is active, the metrics table shows 12 additional columns at the right displaying algMin, algMax, precisionScore per event type for each row. When TIF is off, all 12 columns are hidden.
  </done>
</task>

<task type="auto" tdd="false">
  <name>TIF aggregate rows — min-TIF, median-TIF, max-TIF rows (MET-11, D-06, D-07, D-08)</name>
  <files>js/ui/metrics-screen.js</files>

  <read_first>
    - js/ui/metrics-screen.js — current state after previous two tasks; focus on: buildAggregateRow function (lines 199–222); summaryTbody building in render() (lines 320–332); the tifBoundsMap already set up in the isTif block from Task 2
    - 14-CONTEXT.md §D-06, D-07, D-08 — three TIF aggregate row definitions; hidden when TIF off; all 4 event types; null when no TIF data
    - 14-PATTERNS.md §"js/ui/metrics-screen.js (MODIFY)" — buildTifAggregateRow pattern
  </read_first>

  <action>
    Add the three TIF aggregate rows after the existing Avg/Min/Max rows in summaryTbody.

    1. ADD private helper function buildTifAggregateRow(label, tifAvgs, snap) after buildAggregateRow:
       tifAvgs is an object with keys for each event type (wake, napStart, napEnd, bedtime), each value being a time string ('HH:MM') or null.
       Build a tr with class 'metrics-summary-row metrics-tif-row'.
       First cell (td): label text (sticky-col), textContent = label.
       Then loop over COLUMNS (indices 1..15) — for each column, if the column key is one of the 4 event types (wake, napStart, napEnd, bedtime), render tifAvgs[col.key] formatted as a time (formatTime(tifAvgs[col.key], snap.timeFormat)) or '—' if null; for all other COLUMNS, render '—'.
       Then loop over TIF_COLUMNS — render '—' for all TIF cells in the aggregate row (TIF aggregate rows don't repeat the individual column bounds).
       All cells via textContent. TIF columns in this row are not hidden (the whole row is hidden together).
       Return tr.

    2. IN render(), after tifBoundsMap is built (isTif block), compute tifAvgs for each of the 3 row labels:
       For the EVENT_TYPES = ['wake','napStart','napEnd','bedtime'], compute averages from tifBoundsArray:
       
       computeTifRowAvg(tifBoundsArray, field):
         field is 'algMin', 'central', or 'algMax'
         Returns object: { wake: 'HH:MM'|null, napStart: ..., napEnd: ..., bedtime: ... }
         For each eventType, collect non-null values from entries where entry[eventType] is non-null and entry[eventType][field] is non-null.
         Average as minutes using timeToMinutes, then convert back to HH:MM using minutesToTime (import from forecast.js or time.js as appropriate — check what's already imported; formatDuration is imported from time.js; if minutesToTime is not in time.js, use the local pattern from charts-screen.js or import from forecast.js).
         If no values for this type, result[eventType] = null.

       NOTE: Import minutesToTime at the top of the file if not already present. The cleanest approach: check if formatTime can accept minutes — it likely expects 'HH:MM'. Import minutesToTime from '../lib/forecast.js' or check if '../lib/time.js' has it. Use the same approach as accuracy-screen.js or charts-screen.js uses for converting minutes back to time strings.
       Alternatively, define a local avgMinutesToTime helper inline in render() that converts array of 'HH:MM' strings to avg 'HH:MM': collect as minutes, average, apply Math.round, return the HH:MM string using the String padStart pattern (hours = Math.floor(avg/60), minutes = avg%60).

       Then build the three row averages:
         const tifMinAvgs    = computeTifRowAvg(tifBoundsArray, 'algMin');
         const tifMedianAvgs = computeTifRowAvg(tifBoundsArray, 'central');
         const tifMaxAvgs    = computeTifRowAvg(tifBoundsArray, 'algMax');

    3. IN render() summaryTbody building, after appending avgRow/minRow/maxRow:
       const minTifRow    = buildTifAggregateRow('min-TIF',    tifMinAvgs,    snap);
       const medianTifRow = buildTifAggregateRow('median-TIF', tifMedianAvgs, snap);
       const maxTifRow    = buildTifAggregateRow('max-TIF',    tifMaxAvgs,    snap);
       minTifRow.hidden    = !isTif;
       medianTifRow.hidden = !isTif;
       maxTifRow.hidden    = !isTif;
       summaryTbody.appendChild(minTifRow);
       summaryTbody.appendChild(medianTifRow);
       summaryTbody.appendChild(maxTifRow);

       When isTif is false (TIF off), tifBoundsArray is [] and tifAvgs will all be null — rows are built but immediately hidden.
  </action>

  <verify>
    npm run test:unit
  </verify>

  <acceptance_criteria>
    - buildTifAggregateRow function exists in metrics-screen.js
    - render() calls buildTifAggregateRow three times (for 'min-TIF', 'median-TIF', 'max-TIF')
    - minTifRow.hidden = !isTif, medianTifRow.hidden = !isTif, maxTifRow.hidden = !isTif
    - tifMinAvgs uses 'algMin' field from tifBoundsArray entries
    - tifMedianAvgs uses 'central' field from tifBoundsArray entries
    - tifMaxAvgs uses 'algMax' field from tifBoundsArray entries
    - when tifBoundsArray has no non-null entries for an event type, the corresponding tifAvgs entry is null and the cell renders '—'
    - all cell content set via textContent (no innerHTML)
    - npm run test:unit exits 0
  </acceptance_criteria>

  <done>
    When TIF is active, the Metrics screen summary section shows three additional rows after Avg/Min/Max: min-TIF, median-TIF, max-TIF, each showing average TIF bounds per event type. When TIF is off, all three rows are hidden.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tifBoundsArray entries → TIF cell rendering | Computed time strings and numbers flow into textContent-only cell setters |
| render() → DOM updates | All updates use textContent; no innerHTML with user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03-01 | XSS (Injection) | TIF cell values rendered to DOM | low | mitigate | All cell content set via td.textContent only — T-11-05 invariant; algMin/algMax are 'HH:MM' strings, precisionScore is a number — no user-controlled string is rendered directly |
| T-14-03-02 | Tampering | Column index drift after COLUMNS change | low | mitigate | buildDayRow and buildAggregateRow iterate over COLUMNS array dynamically (no hardcoded index); COLUMNS change is purely additive/reordering of the array definition |
| T-14-03-03 | Tampering | TIF aggregate row computed from future data (look-ahead) | low | mitigate | computeTifBoundsHistory (Plan 02) enforces look-ahead bias prevention; Plan 03 consumes its output unchanged |
| T-14-03-04 | Tampering | D-09 costly reversibility — column index-dependent tests | low | accept | D-09 is marked "costly" not "one-way" — no checkpoint gate required; existing tests iterate columns dynamically and do not depend on hardcoded column indices |
| T-14-03-SC | Tampering | npm installs | low | accept | No new packages installed |
</threat_model>

<verification>
npm run test:unit
</verification>

<success_criteria>
- COLUMNS constant has exactly 16 entries in D-09 order
- sleepAfterActivityFactor absent from COLUMNS and from the import list
- TIF_COLUMNS constant has exactly 12 entries, all with tif:true
- When snap.forecastAlgorithm==='tif', TIF header cells and data cells are not hidden; when 'classic', they are hidden
- min-TIF, median-TIF, max-TIF rows appended to summaryTbody; each has hidden=!isTif
- TIF aggregate rows show event-type averages for algMin, central, algMax respectively
- npm run test:unit exits 0
</success_criteria>

<output>
Create .planning/phases/NW-14-tif-metrics-accuracy-chart-fixes/14-03-SUMMARY.md when done.
</output>
