---
phase: NW-14
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - js/lib/chart-data.js
  - js/ui/charts-screen.js
  - tests/unit/chart-data.test.js
autonomous: true
requirements:
  - UI-08
  - UI-09
  - UI-10

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "buildTimeBandSeries returns {date, wakeMinutes, bedtimeMinutes, napStartMinutes, napEndMinutes} — one value per slot per subjective day (UI-10, D-17)"
    - "buildTimeBandSeries uses dayRecords.map over pre-bucketed day records — no calendar-date Map or allEvents loop (D-17)"
    - "renderTimeBandChart yScale inverts correctly: earlier times are higher on canvas (UI-08, D-15)"
    - "renderTimeBandChart renders 4 series of dots: wake (#4f46e5), napStart (#f59e0b), napEnd (#fb923c), bedtime (#94a3b8) (UI-09, D-16)"
    - "legend shows 4 colored squares with labels Wake/Nap Start/Nap End/Bedtime (UI-09, D-16)"
    - "hasData guard checks napStartMinutes != null (not bedtimesMinutes.length) (UI-10)"
    - "null-slot days (e.g. no-nap days) skip the nap dot but still render wake and bedtime dots (D-16)"
    - "tests/unit/chart-data.test.js old 'two bedtimes on same calendar date → both in bedtimesMinutes' test is replaced with subjective-night tests verifying the new shape (UI-10, D-17)"
    - "[ASSUMPTION UI-10 no-nap] buildTimeBandSeries returns napStartMinutes: null and napEndMinutes: null for days where the day record has napStart:null"
    - "[ASSUMPTION UI-08 scale] yScale maps 0 minutes (midnight) to the bottom of the plot; 1440 minutes maps to the top; mid-day events appear in the middle"
    - "[ASSUMPTION UI-09 dot radius] 3px dot radius preserved from existing chart code unless PATTERNS.md specifies otherwise"
  artifacts:
    - "buildTimeBandSeries updated in js/lib/chart-data.js — new 4-slot return shape"
    - "renderTimeBandChart updated in js/ui/charts-screen.js — inverted yScale, 4-series loop, 4-row legend"
    - "tests/unit/chart-data.test.js updated — old bedtimesMinutes[] test removed; new napStart/napEnd shape tests added"
  prohibitions:
    - "No fenced code blocks (```) in any <action> field"
    - "buildTimeBandSeries must NOT use a calendar-date Map or iterate allEvents — subjective-night bucketing already done by dayRecords (D-17)"
    - "Do not change the yScale min/max values or plot dimensions — only invert the direction of the formula (D-15)"
    - "Old bedtimesMinutes[] shape must not remain anywhere in chart-data.js or charts-screen.js after this plan"

artifacts_produced:
  - "buildTimeBandSeries with new {date, wakeMinutes, bedtimeMinutes, napStartMinutes, napEndMinutes} shape in js/lib/chart-data.js"
  - "renderTimeBandChart with inverted yScale and 4-series dot rendering in js/ui/charts-screen.js"
  - "Updated tests/unit/chart-data.test.js with new shape assertions; old bedtimesMinutes[] test removed"
---

<objective>
Fix three chart-related bugs in Phase 14: (1) Y-axis inversion so earlier times appear higher (UI-08, D-15); (2) Replace single-color bedtime dots with 4-series colored dots for Wake/Nap Start/Nap End/Bedtime (UI-09, D-16); (3) Fix subjective-night deduplication in buildTimeBandSeries by replacing the calendar-date Map/allEvents loop with a direct dayRecords.map approach (UI-10, D-17). Update tests to verify the new shape.

Purpose: The chart currently shows inverted times (later bedtimes appear higher), a single dot color for all events, and duplicates sleep events that cross midnight on the same calendar date. These fixes together make the Time Band chart useful.

Output: Updated chart-data.js (new return shape), updated charts-screen.js (inverted scale, 4 series), updated chart-data.test.js (new shape tests).
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/NW-14-tif-metrics-accuracy-chart-fixes/14-CONTEXT.md
@.planning/phases/NW-14-tif-metrics-accuracy-chart-fixes/14-PATTERNS.md
@js/lib/chart-data.js
@js/ui/charts-screen.js
@tests/unit/chart-data.test.js
</context>

<tasks>

<task type="tracer">
  <name>Tracer: buildTimeBandSeries new shape + charts-screen 4-series rendering with inverted Y-axis</name>
  <files>js/lib/chart-data.js, js/ui/charts-screen.js</files>

  <read_first>
    - js/lib/chart-data.js — lines 178–211 (buildTimeBandSeries current implementation): current byDate Map + allEvents loop; old return shape {date, wakeMinutes, bedtimeMinutes, bedtimesMinutes[]}; the extractMinutes helper pattern
    - js/ui/charts-screen.js — lines 320–403 (renderTimeBandChart): line 327 (function start); line 335 (hasData check: p.bedtimesMinutes.length > 0); line 356 (current yScale formula); lines 376–392 (dot rendering loop); lines 394–400 (legend)
    - 14-PATTERNS.md §"js/lib/chart-data.js (MODIFY)" — exact new buildTimeBandSeries body
    - 14-PATTERNS.md §"js/ui/charts-screen.js (MODIFY)" — new yScale, 4-series loop, 4-row legend; 4 colors
    - 14-CONTEXT.md §D-15, D-16, D-17 — decisions driving each change
  </read_first>

  <action>
    Apply all three chart fixes in two files as a single production-quality tracer.

    FILE 1: js/lib/chart-data.js — replace buildTimeBandSeries body

    The current implementation (lines 178–211) builds a byDate Map and iterates allEvents, producing the buggy calendar-date grouping. Replace the entire function body with a direct dayRecords.map:

    The new implementation maps over dayRecords (already subjective-day-bucketed by day-bucket.js) and extracts each named slot's minute offset directly:
    - For each dayRecord d: use extractMinutes (already in this file) on d.wake, d.bedtime, d.napStart, d.napEnd
    - Return: { date: d.date, wakeMinutes: extractMinutes(d.wake), bedtimeMinutes: extractMinutes(d.bedtime), napStartMinutes: extractMinutes(d.napStart), napEndMinutes: extractMinutes(d.napEnd) }
    - Filter: keep only entries where at least one of wakeMinutes or bedtimeMinutes is non-null (same as current hasData guard)
    - Note: extractMinutes on a null slot returns null (confirm this is the existing behavior before relying on it — if extractMinutes throws on null, use: d.napStart != null ? extractMinutes(d.napStart) : null)

    Return Array of the new shape. Do not remove extractMinutes — it is still used.
    Remove any code that builds byDate Map or iterates allEvents. Remove the bedtimesMinutes property entirely from the return objects.

    FILE 2: js/ui/charts-screen.js — 3 targeted changes

    Change 1 — yScale (D-15): locate the current yScale definition at line 356:
      Current: const yScale = (minutes) => M.top + (minutes / (24 * 60)) * plotH
      New:     const yScale = (minutes) => M.top + plotH - (minutes / (24 * 60)) * plotH
      This single change inverts the canvas Y direction so earlier times map higher.

    Change 2 — hasData guard (UI-10): locate line 335 or the conditional that checks p.bedtimesMinutes:
      Replace: const hasData = series.some(p => p.bedtimesMinutes.length > 0)
      With:    const hasData = series.some(p => p.wakeMinutes != null || p.bedtimeMinutes != null)
      (Matches the new shape; nap presence is optional — not required for the chart to show data.)

    Change 3 — dot rendering and legend (UI-09, D-16): locate the dot rendering loop (lines 376–392) and legend (lines 394–400) and replace both sections.

    New 4-series definitions (as a local const array):
    const SERIES = [
      { key: 'wakeMinutes',     color: '#4f46e5', label: 'Wake' },
      { key: 'napStartMinutes', color: '#f59e0b', label: 'Nap Start' },
      { key: 'napEndMinutes',   color: '#fb923c', label: 'Nap End' },
      { key: 'bedtimeMinutes',  color: '#94a3b8', label: 'Bedtime' },
    ];

    Dot rendering loop: for each point p in series, for each series s in SERIES:
      const minutes = p[s.key];
      if (minutes == null) continue;  // no-nap days skip nap series
      ctx.beginPath();
      ctx.arc(xPos, yScale(minutes), DOT_RADIUS, 0, Math.PI * 2);
      ctx.fillStyle = s.color;
      ctx.fill();

    (Use DOT_RADIUS as the existing variable name for dot radius — check if it is a const in this file or a literal; if literal 3, keep 3.)

    Legend: Replace the existing single-color legend with 4 rows. For each series s in SERIES:
      draw a filled square (size 10×10 or match existing swatch size) at the current legend position
      draw the label text s.label at the square's right
    Maintain the same x/y layout pattern as the existing legend code (vertical stack, same margin).

    All canvas drawing uses fillStyle and fillRect/arc — no innerHTML involved.
  </action>

  <verify>
    node --test tests/unit/chart-data.test.js
    npm run test:unit
  </verify>

  <acceptance_criteria>
    - buildTimeBandSeries returns objects with {date, wakeMinutes, bedtimeMinutes, napStartMinutes, napEndMinutes} — no bedtimesMinutes property
    - buildTimeBandSeries body uses dayRecords.map — no byDate Map, no allEvents iteration
    - yScale formula in charts-screen.js contains plotH - (minutes / ... — the inversion is present
    - hasData check does NOT reference .bedtimesMinutes
    - SERIES (or equivalent) constant with 4 entries exists in charts-screen.js renderTimeBandChart
    - 4 colors present: #4f46e5, #f59e0b, #fb923c, #94a3b8
    - node --test tests/unit/chart-data.test.js exits 0 (tests not yet updated but old bedtimesMinutes tests may fail — that is resolved in Task 2)
    - npm run test:unit exits 0 OR only chart-data.test.js failures remain (isolated to the outdated test that Task 2 fixes)
  </acceptance_criteria>

  <done>
    buildTimeBandSeries returns the new 4-slot shape. charts-screen.js renders 4-series colored dots with an inverted Y-axis. The tracer is production-quality code; only the outdated chart-data.test.js test (task 2) needs updating.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Update chart-data.test.js — remove old bedtimesMinutes test, add new 4-slot shape tests (UI-10)</name>
  <files>tests/unit/chart-data.test.js</files>

  <read_first>
    - tests/unit/chart-data.test.js — lines 179–240 (buildTimeBandSeries test block): line 179 (block start); line 215 ("two bedtimes on same calendar date → both in bedtimesMinutes" — the OLD test asserting buggy behavior; must be removed); all other tests in the block to understand what stays
    - js/lib/chart-data.js — current state after tracer task (new shape as reference)
    - 14-CONTEXT.md §D-17 — subjective-night definition (cutoverHour determines day boundary)
  </read_first>

  <action>
    Update tests/unit/chart-data.test.js to align with the new buildTimeBandSeries return shape.

    1. LOCATE and REMOVE the test at approximately line 215:
       "two bedtimes on same calendar date → both in bedtimesMinutes"
       This test asserts the OLD calendar-date-bucketing bug. It checks bedtimesMinutes.length > 1. Remove the entire it/test block for this case.

    2. VERIFY existing tests in the buildTimeBandSeries block that test other behaviors — confirm they do not reference bedtimesMinutes. If any do, update them to reference the new shape property names.

    3. ADD new tests for the subjective-night fix and the 4-slot shape:

       Test A — new shape basic: given a single day record with wake:'07:00', bedtime:'22:00', napStart:'13:00', napEnd:'14:30'; buildTimeBandSeries returns array where result[0].napStartMinutes equals 780 (13×60) and result[0].napEndMinutes equals 870 (14×60+30).

       Test B — no-nap day: given a day record with napStart:null, napEnd:null; result[0].napStartMinutes is null and result[0].napEndMinutes is null; result[0].wakeMinutes and result[0].bedtimeMinutes are still non-null numbers.

       Test C — subjective-night (replacing the removed test): given two day records with dates 'YYYY-MM-01' and 'YYYY-MM-02', each with distinct wake/bedtime values; buildTimeBandSeries returns exactly 2 entries — one per day record — confirming no calendar-date duplication.

       Fixtures: use the existing makeDay or makeRecord helper pattern in the test file. If buildTimeBandSeries takes (dayRecords, settings), use a settings object with appropriate cutoverHour (e.g. 4).

    4. RUN npm run test:unit — all tests must pass.
  </action>

  <verify>
    node --test tests/unit/chart-data.test.js
    npm run test:unit
  </verify>

  <acceptance_criteria>
    - No test in chart-data.test.js references .bedtimesMinutes
    - Test for "two bedtimes on same calendar date → both in bedtimesMinutes" is absent
    - New test for napStartMinutes/napEndMinutes values on a nap day is present
    - New test for napStartMinutes === null on a no-nap day is present
    - New test verifying exactly 2 results for 2 day records (subjective-night dedup) is present
    - node --test tests/unit/chart-data.test.js exits 0
    - npm run test:unit exits 0
  </acceptance_criteria>

  <done>
    chart-data.test.js tests the new 4-slot shape. The old bedtimesMinutes[] test is gone. Three new tests verify the subjective-night fix and null handling. Full test suite passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dayRecords (pre-bucketed) → buildTimeBandSeries | Trust boundary: day-bucket.js has already resolved calendar/subjective-night ambiguity; chart-data.js just maps, it does not re-bucket |
| Canvas rendering (ctx.arc/fillStyle) → display | Canvas draws computed numbers — no user-supplied strings touch the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-05-01 | Tampering | buildTimeBandSeries shape change breaks consumers | low | mitigate | Only consumer is renderTimeBandChart in charts-screen.js — updated in the same tracer task; no other file uses the bedtimesMinutes shape (grep confirms) |
| T-14-05-02 | Tampering | yScale inversion introduces visual regression in other chart types | low | mitigate | yScale is defined as a local const inside renderTimeBandChart scope — not shared with other canvas functions; only affects the Time Band chart |
| T-14-05-03 | Tampering | null napStartMinutes causes arc(null) NaN on canvas | low | mitigate | Dot rendering loop uses explicit null guard: if (minutes == null) continue; NaN cannot reach ctx.arc |
| T-14-05-04 | Tampering | Old test asserting calendar-date duplication blocks CI | low | mitigate | Task 2 removes the old test and adds correct shape tests before marking the plan done |
| T-14-05-SC | Tampering | npm installs | low | accept | No new packages — vanilla JS constraint |
</threat_model>

<verification>
node --test tests/unit/chart-data.test.js
npm run test:unit
</verification>

<success_criteria>
- buildTimeBandSeries returns {date, wakeMinutes, bedtimeMinutes, napStartMinutes, napEndMinutes} — no bedtimesMinutes
- yScale formula contains the inversion: plotH - (minutes / (24*60)) * plotH
- 4 colored dot series rendered: wake=#4f46e5, napStart=#f59e0b, napEnd=#fb923c, bedtime=#94a3b8
- hasData guard does not reference bedtimesMinutes
- chart-data.test.js: old bedtimesMinutes test removed; 3 new shape tests present
- npm run test:unit exits 0
</success_criteria>

<output>
Create .planning/phases/NW-14-tif-metrics-accuracy-chart-fixes/14-05-SUMMARY.md when done.
</output>
