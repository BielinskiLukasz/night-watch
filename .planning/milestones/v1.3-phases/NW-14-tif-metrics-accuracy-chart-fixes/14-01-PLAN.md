---
phase: NW-14
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - js/lib/metrics.js
  - tests/unit/metrics.test.js
autonomous: true
requirements:
  - MET-07
  - MET-09
  - MET-10

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "dayToSleepFactor(day) returns dayLength(day)/sleepDuration(day); null when either is null or sleepDuration===0 (D-12)"
    - "napFraction(day) returns napDuration(day)/combinedSleepNap(day); null on no-nap days (napStart==null || napEnd==null), or when combinedSleepNap is null or 0 (D-12)"
    - "amPmSplit(day) returns activityBeforeNap(day)/activityAfterNap(day); null on no-nap days (napStart==null && napEnd==null) or when activityAfterNap is 0 (D-12)"
    - "aggregateMetrics computes avg/min/max for dayToSleepFactor (validRows), napFraction (napRows), amPmSplit (napRows) (D-14)"
    - "aggregateMetrics does NOT call aggregateMetric for sleepAfterActivityFactor — only the per-row field value is retained for backward compat (D-14)"
    - "sleepAfterActivityFactor remains exported and its direct-call tests still pass — only removed from aggregateMetrics computation (D-14)"
    - "all three new functions format as bare decimal ratios, not rounded — consistent with activityAfterSleepFactor (D-13)"
    - "[ASSUMPTION MET-07 adjacency] when multiple rows share the same dayToSleepFactor value, aggregateMetric min/max returns the first match (findIndex stops at first)"
    - "[ASSUMPTION MET-09 boundary] napFraction when combinedSleepNap===0 returns null (division-by-zero guard same as activityAfterSleepFactor)"
    - "[ASSUMPTION MET-10 boundary] amPmSplit when activityAfterNap===0 returns null; also null when activityAfterNap is the full dayLength on a no-nap day (napStart+napEnd both null guard)"
  artifacts:
    - "dayToSleepFactor — new named export in js/lib/metrics.js"
    - "napFraction — new named export in js/lib/metrics.js"
    - "amPmSplit — new named export in js/lib/metrics.js"
    - "aggregateMetrics — updated: three new fields added to rows, avg, min, max; saaRows derivation and aggregateMetric('sleepAfterActivityFactor') call removed"
    - "tests/unit/metrics.test.js — three new describe blocks for new functions + one new describe block for aggregateMetrics updated fields"
  prohibitions:
    - "No fenced code blocks (```) in any <implementation> prose"
    - "metrics.js must NOT import from forecast-tif.js — circular-import guard (CLAUDE.md §Pitfalls)"
    - "sleepAfterActivityFactor must remain exported and callable; existing SAA tests must still pass"
    - "No new npm dependencies"

artifacts_produced:
  - "dayToSleepFactor(day) → number|null — exported from js/lib/metrics.js"
  - "napFraction(day) → number|null — exported from js/lib/metrics.js"
  - "amPmSplit(day) → number|null — exported from js/lib/metrics.js"
  - "aggregateMetrics updated return shape: rows/avg/min/max include dayToSleepFactor, napFraction, amPmSplit; avg/min/max no longer include sleepAfterActivityFactor"
  - "tests/unit/metrics.test.js — extended with new test groups"
---

<objective>
TDD plan — add three new ratio metric functions to `js/lib/metrics.js` (MET-07, MET-09, MET-10 per D-12) and update `aggregateMetrics` to compute aggregates for them while removing `sleepAfterActivityFactor` from the aggregate computation (D-14). The function stays exported; only its aggregation is removed.

Purpose: `dayToSleepFactor` replaces SAA in the Metrics table (MET-07). `napFraction` and `amPmSplit` are new columns (MET-09, MET-10). Building and testing these pure functions before the UI layer (Plan 03) ensures the data model is correct.

Output: Three new exported functions; updated `aggregateMetrics`; extended test coverage. Plan 03 (metrics-screen.js COLUMNS update) depends on this plan.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/NW-14-tif-metrics-accuracy-chart-fixes/14-CONTEXT.md
@.planning/phases/NW-14-tif-metrics-accuracy-chart-fixes/14-PATTERNS.md
@js/lib/metrics.js
@tests/unit/metrics.test.js
</context>

<feature>
  <name>dayToSleepFactor, napFraction, amPmSplit — new ratio metrics (D-12, D-13, D-14)</name>
  <files>js/lib/metrics.js, tests/unit/metrics.test.js</files>

  <read_first>
    - js/lib/metrics.js — read full file (317 lines); focus on: activityAfterSleepFactor (lines 133–138) for the null-guard/division-by-zero pattern to replicate; aggregateMetrics lines 172–317 for the rows.push structure and the aggregateMetric helper; saaRows derivation (line 313) to locate and remove
    - tests/unit/metrics.test.js — read full file; focus on: makeDay helper (lines 35–37); describe block structure; existing SAA describe block (lines 294–400) to understand test shape without replicating it for the new functions
    - 14-PATTERNS.md — §"js/lib/metrics.js (MODIFY)" for exact new function bodies and aggregateMetrics update pattern
  </read_first>

  <behavior>
    dayToSleepFactor cases (MET-07, D-12):

    dayToSleepFactor(makeDay('07:30', '22:00', '13:00', '14:30'))
      dayLength = 22:00-07:30 = 870 min, sleepDuration = 07:30+1440-22:00+0 = 570 min
      result ≈ 1.526 (non-null, typeof number)

    dayToSleepFactor(makeDay('07:30', null, null, null))
      sleepDuration = null → result = null

    dayToSleepFactor(makeDay(null, '22:00', null, null))
      dayLength = null → result = null

    dayToSleepFactor where sleepDuration computes to 0 (edge: wake===bedtime in minutes)
      → result = null (division-by-zero guard)

    napFraction cases (MET-09, D-12):

    napFraction(makeDay('07:00', '22:00', '13:00', '14:30'))
      napDuration = 90 min; sleepDuration pairs with prev day so combinedSleepNap depends on context —
      but in the unit helper makeDay the sleep is computed directly: use a day where sleepDuration is known.
      Test fixture: wake='07:00', bedtime='22:00', napStart='13:00', napEnd='14:30'
      napDuration = 90; combinedSleepNap (sleepDur + napDur) — for unit test, sleepDuration uses
      wake-bedtime same day wrap: 420-1320 = -900+1440=540; combined=540+90=630; napFraction=90/630≈0.143

    napFraction(makeDay('07:00', '22:00', null, null))
      napStart==null → napDuration = null → result = null (no-nap day)

    napFraction(makeDay('07:00', '22:00', '13:00', null))
      napEnd==null → napDuration = null → result = null

    napFraction edge: combinedSleepNap===0 → result = null

    amPmSplit cases (MET-10, D-12):

    amPmSplit(makeDay('07:00', '22:00', '13:00', '14:30'))
      activityBeforeNap = 13:00-07:00 = 360 min
      activityAfterNap = 22:00-14:30 = 450 min
      360/450 = 0.8

    amPmSplit(makeDay('07:00', '22:00', null, null))
      napStart==null && napEnd==null → result = null (no-nap day)

    amPmSplit edge: activityAfterNap===0 → result = null

    aggregateMetrics update (D-14):
    Given a 3-day fixture where all days have full nap + activity data:
      result.avg.dayToSleepFactor is non-null number
      result.avg.napFraction is non-null number
      result.avg.amPmSplit is non-null number
      result.avg.sleepAfterActivityFactor is undefined (key absent from avg object)
      result.rows[0].sleepAfterActivityFactor is still a number or null (row value kept for backward compat)

    Note — [ASSUMPTION MET-07 ordering]: when days have equal dayToSleepFactor values, findIndex returns the first; this matches existing aggregateMetric behavior for all other metrics.
    Note — [ASSUMPTION MET-09 precision]: JavaScript float division does not lose precision at the minute granularity used by these metrics.
  </behavior>

  <implementation>
    RED — add four new describe blocks to tests/unit/metrics.test.js:

    1. describe('dayToSleepFactor(day)', ...) — add after the existing sleepAfterActivityFactor block.
       Import dayToSleepFactor from '../../js/lib/metrics.js' (add to existing import statement).
       Tests: normal nap day (assert typeof number and approx value); null bedtime → null; null wake → null.

    2. describe('napFraction(day)', ...) — import napFraction.
       Tests: nap day (assert approx value); no-nap day (napStart null → null); napEnd null → null.

    3. describe('amPmSplit(day)', ...) — import amPmSplit.
       Tests: nap day with both activity segments (assert value === 0.8 exactly for 360/450); no-nap day (null napStart+napEnd → null).

    4. describe('aggregateMetrics — updated fields (D-14)', ...) after the existing aggregateMetrics block.
       Build a 3-day fixture where all days have wake, bedtime, napStart, napEnd set.
       Call aggregateMetrics(days) and assert:
         assert.ok(typeof result.avg.dayToSleepFactor === 'number', 'dayToSleepFactor aggregated')
         assert.ok(typeof result.avg.napFraction === 'number', 'napFraction aggregated')
         assert.ok(typeof result.avg.amPmSplit === 'number', 'amPmSplit aggregated')
         assert.strictEqual(result.avg.sleepAfterActivityFactor, undefined, 'SAA not in avg (D-14)')
         assert.ok('sleepAfterActivityFactor' in result.rows[0], 'SAA still in per-row data')

    Run: node --test tests/unit/metrics.test.js — new tests MUST fail (functions don't exist yet).
    Commit: test(NW-14-01): add failing tests for dayToSleepFactor, napFraction, amPmSplit, aggregateMetrics update

    GREEN — in js/lib/metrics.js:

    1. After sleepAfterActivityFactor function (~line 152), add three new exported functions.
       dayToSleepFactor: get dl = dayLength(day) and sd = sleepDuration(day); if dl==null || sd==null || sd===0, return null; return dl/sd.
       napFraction: get nd = napDuration(day) and cs = combinedSleepNap(day); if nd==null || cs==null || cs===0, return null; return nd/cs.
       amPmSplit: get before = activityBeforeNap(day) and after = activityAfterNap(day); if day.napStart==null && day.napEnd==null, return null; if before==null || after==null || after===0, return null; return before/after.

    2. In aggregateMetrics rows.push({...}) block (around line 226), add three new fields:
         dayToSleepFactor: dayToSleepFactor(day),
         napFraction: napFraction(day),
         amPmSplit: amPmSplit(day),
       Keep sleepAfterActivityFactor: sleepAfterActivityFactor(day, prevDay) in the row (backward compat).

    3. In the aggregate section (~lines 302–314):
       Add after aggregateMetric('activityAfterSleepFactor', validRows):
         aggregateMetric('dayToSleepFactor', validRows)
         aggregateMetric('napFraction', napRows)
         aggregateMetric('amPmSplit', napRows)
       Remove these lines entirely (D-14):
         const saaRows = validRows.filter((r, i) => i > 0 && r.sleepAfterActivityFactor !== null);
         aggregateMetric('sleepAfterActivityFactor', saaRows);

    Run: node --test tests/unit/metrics.test.js — ALL tests including new ones MUST pass.
    Commit: feat(NW-14-01): add dayToSleepFactor, napFraction, amPmSplit; remove SAA from aggregateMetrics (MET-07/09/10, D-12/D-14)

    REFACTOR — add JSDoc comments to the three new functions following the style of activityAfterSleepFactor:
    @param {object} day day record (with wake, bedtime, napStart, napEnd slots)
    @returns {number|null} ratio, or null when required slots are absent or denominator is 0
    Also add a one-line comment above the rows.push additions: "D-12 new ratio fields".
    Run: npm run test:unit — MUST pass.
    Commit: refactor(NW-14-01): add JSDoc to dayToSleepFactor, napFraction, amPmSplit
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| day record slots → ratio functions | Computed numeric results from pre-bucketed day data; no external input |
| ratio functions → aggregateMetrics | Pure numbers flow into aggregate computation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01-01 | Tampering | Division-by-zero in ratio functions | low | mitigate | All three functions guard denominator===0 explicitly and return null — same pattern as activityAfterSleepFactor |
| T-14-01-02 | Tampering | NaN leaking into aggregateMetric accumulator | low | mitigate | aggregateMetric filters with .filter(v => v !== null && v !== undefined); ratio functions never return NaN because null-guards prevent all NaN-producing paths |
| T-14-01-03 | Tampering | sleepAfterActivityFactor accidentally removed from per-row data | low | mitigate | rows.push() retains sleepAfterActivityFactor field; only the aggregate call is removed; existing test at line 475 of metrics.test.js asserts it is present in rows |
| T-14-01-SC | Tampering | npm installs | low | accept | No new packages — vanilla JS project constraint |
</threat_model>

<verification>
node --test tests/unit/metrics.test.js
npm run test:unit
</verification>

<success_criteria>
- dayToSleepFactor(normalNapDay) returns a non-null number; dayToSleepFactor(noBedtime) returns null
- napFraction(napDay) returns a non-null number; napFraction(noNapDay) returns null
- amPmSplit(napDay) returns 0.8 for the 360/450 fixture; amPmSplit(noNapDay) returns null
- aggregateMetrics(3dayFixture).avg.dayToSleepFactor is a non-null number
- aggregateMetrics(3dayFixture).avg.sleepAfterActivityFactor is undefined (removed from aggregation)
- aggregateMetrics(3dayFixture).rows[0].sleepAfterActivityFactor is still present (backward compat)
- sleepAfterActivityFactor imported directly still returns correct values in all existing tests
- node --test tests/unit/metrics.test.js exits 0
- npm run test:unit exits 0
</success_criteria>

<output>
Create .planning/phases/NW-14-tif-metrics-accuracy-chart-fixes/14-01-SUMMARY.md when done.
</output>
