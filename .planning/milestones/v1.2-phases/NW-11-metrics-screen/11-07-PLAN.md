---
phase: 11
plan: 07
type: execute
wave: 2
gap_closure: true
gap_ids: [G-NW-11-9]
depends_on: [11-04, 11-05]
files_modified: [tests/e2e/metrics.spec.js, js/ui/metrics-screen.js]
autonomous: false
requirements: [MET-01, MET-02, MET-03, MET-04, MET-05, MET-06]

must_haves:
  truths:
    - "Stage badge shows 'Viewing: {stage name}' above the metrics table when an active stage is selected"
    - "Stage badge is hidden when no stage is active (All) or remains hidden in the default state"
    - "E2E test verifies the badge visibility flow: create stage → select from Today screen dropdown → verify badge on Metrics screen"
  artifacts:
    - "tests/e2e/metrics.spec.js with new MET-06 test for stage badge visibility (currently deferred)"
    - "js/ui/metrics-screen.js with verified renderStageBadge() implementation"
  key_links:
    - "Today screen stage dropdown → activeStageId setting update"
    - "activeStageId setting → renderStageBadge reads and displays badge"
    - "Badge hidden attribute lifecycle → removed when activeStageId exists, set when null"
---

<objective>
Add E2E test for stage badge visibility on Metrics screen (MET-06, previously deferred) and verify the badge rendering logic correctly reads and displays the active stage.

Purpose: Users can confirm that selecting a stage from the Today screen dropdown makes the stage badge appear on the Metrics screen, filtering data to show only that stage's activity.
Output: E2E test in metrics.spec.js + verified badge implementation in metrics-screen.js
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/e2e/metrics.spec.js
@js/ui/metrics-screen.js
@js/ui/today-screen.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add E2E test for stage badge visibility (MET-06)</name>
  <files>tests/e2e/metrics.spec.js</files>
  <action>Add a new test to tests/e2e/metrics.spec.js (replacing or extending the deferred MET-06 test placeholder). The test should:
1. Start the app and log at least one sleep event
2. Open Settings and create a new stage (e.g. "Work Week") with an active period covering logged dates
3. Navigate to Today screen and select the stage from the stage dropdown (this sets activeStageId in settings)
4. Navigate to Metrics screen
5. Verify the stage badge element is visible (not hidden) and displays text like "Viewing: Work Week"
6. Verify the metrics table displays only data scoped to that stage (if stage filtering is implemented; otherwise just verify the badge shows)
7. Select "All" from the dropdown on Today screen
8. Verify the badge on Metrics screen is hidden again (or shows "All")

Use Playwright `page.locator()` to find the badge element (likely a <span> or <div> with a class like `.stageBadge` or similar) and assert on visibility and textContent. The test should not make assumptions about DOM structure; use the same selectors that metrics-screen.js uses to render the badge.
  </action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/metrics.spec.js --grep "MET-06|stage.*badge" 2>&1 | grep -c "1 passed"</automated>
  </verify>
  <done>E2E test MET-06 is implemented and passes. The test verifies the full flow: create stage → select from Today screen → badge appears on Metrics screen → deselect → badge hidden.</done>
</task>

<task type="auto">
  <name>Task 2: Verify renderStageBadge implementation (G-NW-11-9 logic)</name>
  <files>js/ui/metrics-screen.js</files>
  <action>Review the renderStageBadge() function in metrics-screen.js to ensure it:
1. Reads the activeStageId from the current settings snapshot (likely from a settings store call or callback)
2. Queries the stages list to find the stage name by activeStageId
3. Sets or removes the badge element's `hidden` attribute based on whether activeStageId is null/undefined
4. Updates the badge's textContent to "Viewing: {stageName}" when a stage is active
5. Properly handles the case where activeStageId is set but the corresponding stage no longer exists (show the badge with the ID, or handle gracefully)

If renderStageBadge() is not yet implemented or is incomplete, implement it following the pattern established in today-screen.js or other UI modules. Ensure the function is called whenever settings change (e.g. after the user changes the stage dropdown on Today screen and navigates back to Metrics).

Verify that the badge element (e.g. `#stageBadge`, `.metrics-badge`, etc.) exists in the HTML template (index.html or metrics-screen.html if separate) and has the correct CSS class and initial hidden state.
  </action>
  <verify>
    <automated>grep -c "renderStageBadge" js/ui/metrics-screen.js</automated>
  </verify>
  <done>renderStageBadge() is implemented and correctly reads activeStageId from settings, queries the stage name, and toggles the badge visibility and text. The function is called on metrics screen load and when settings change. E2E test verifies the behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User settings (activeStageId) | Badge displays the user's selected stage; settings trust is assumed (settings stored locally) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-07-01 | — | stage badge UI | low | accept | Badge display is read-only UI; no validation needed beyond settings consistency |
</threat_model>

<verification>
1. Run `npm run test:e2e -- tests/e2e/metrics.spec.js --grep "MET-06|badge"` — test passes
2. Manually test: Open app → log an event → create a stage via Settings → select stage from Today screen dropdown → navigate to Metrics screen → verify badge shows "Viewing: {stage name}" → select "All" → verify badge hidden
</verification>

<success_criteria>
- E2E test MET-06 (stage badge visibility) is implemented and passes
- renderStageBadge() correctly reads activeStageId and displays the badge with the stage name
- Badge is hidden when activeStageId is null or when "All" is selected
- Full flow (create stage → select → verify badge → deselect → badge hidden) works end-to-end
</success_criteria>

<output>
Create `.planning/phases/NW-11-metrics-screen/11-07-SUMMARY.md` after execution
</output>
