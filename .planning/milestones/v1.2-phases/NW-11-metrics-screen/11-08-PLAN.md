---
phase: 11
plan: 08
type: execute
wave: 1
gap_closure: true
gap_ids: [G-NW-11-17, G-NW-11-19, G-NW-11-20]
depends_on: []
files_modified: [style.css, js/ui/today-screen.js, js/ui/metrics-screen.js]
autonomous: true
requirements: [MET-01, MET-02, MET-03, MET-04, MET-05, MET-06]

must_haves:
  truths:
    - "Today screen action buttons (Woke up, Nap start, Nap end, Going to sleep) are centered horizontally"
    - "Today screen button order is Woke up → Nap start → Nap end → Going to sleep (bedtime moved after nap)"
    - "All screens use the full available width with minimal side padding (no large white gutters)"
    - "Metrics table column order is Date, Wake, Nap Start, Nap End, Bedtime, Sleep, Nap, ... (Bedtime after Nap End)"
  artifacts:
    - "style.css with centered .quickLog and reduced body padding or increased #app max-width"
    - "js/ui/today-screen.js with reordered BUTTONS array"
    - "js/ui/metrics-screen.js with reordered COLUMNS array"
  key_links:
    - ".quickLog justify-content → centers action buttons"
    - "BUTTONS array order → button render sequence"
    - "COLUMNS array order → table column sequence"
    - "body padding or #app max-width → layout width on wider screens"
---

<objective>
Fix three cosmetic/UI layout issues: center and reorder Today screen action buttons, reduce side gutters on all screens, and correct Metrics table column order.

Purpose: Improve visual balance and usability on wider screens; match intended column sequence (time events before metrics).
Output: style.css with layout adjustments; today-screen.js with reordered buttons; metrics-screen.js with reordered columns
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@style.css
@js/ui/today-screen.js
@js/ui/metrics-screen.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Center action buttons and reorder on Today screen (G-NW-11-17)</name>
  <files>style.css, js/ui/today-screen.js</files>
  <action>Make two changes to center the action buttons and reorder them:

**In style.css:**
- Locate the `.quickLog` rule (currently has `display: flex; flex-wrap: wrap; gap: 12px; margin-bottom: 1.5rem;`)
- Add `justify-content: center;` to center the buttons horizontally

**In js/ui/today-screen.js:**
- Locate the BUTTONS export constant (lines ~48-52)
- Reorder the array from: wake, bedtime, napStart, napEnd
- To: wake, napStart, napEnd, bedtime
- This moves "Going to sleep" (bedtime) to the end, after the two nap buttons

The BUTTONS array is the single source of truth for button order; no other reordering needed. The render loop (lines ~641-649) uses this order directly.</action>
  <verify>
    <automated>grep -c "justify-content: center" style.css</automated>
  </verify>
  <done>The .quickLog CSS rule includes justify-content: center. The BUTTONS array in today-screen.js is reordered to wake → napStart → napEnd → bedtime. Action buttons on the Today screen display centered and in the new sequence when the app runs.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce side gutters on all screens (G-NW-11-19)</name>
  <files>style.css</files>
  <action>Reduce the body padding to minimize white gutters on wider screens. Locate the `body` rule (currently has `padding: 1.5rem;`). Change the padding to:

`padding: 1.5rem 0.75rem;` (top/bottom: 1.5rem, left/right: 0.75rem)

OR, alternatively, increase the `#app` max-width from `32rem` (512px) to `48rem` (768px) to use more horizontal space.

Choose the first approach (reduce body padding) as it's the minimal, targeted fix. This reduces horizontal padding from 24px to 12px on each side.</action>
  <verify>
    <automated>grep -c "padding: 1.5rem 0.75rem" style.css</automated>
  </verify>
  <done>The body padding is reduced to 1.5rem top/bottom and 0.75rem left/right, eliminating large white side gutters on wider screens. Content on all screens (Today, History, Charts, Accuracy, Metrics) now uses the full available width more effectively.</done>
</task>

<task type="auto">
  <name>Task 3: Reorder Bedtime column in Metrics table (G-NW-11-20)</name>
  <files>js/ui/metrics-screen.js</files>
  <action>Reorder the COLUMNS array in metrics-screen.js to move Bedtime after Nap End.

Current order (lines 39-54):
  date, wake, bedtime, napStart, napEnd, sleep, nap, combined, dayLen, actBefore, actAfter, activity, AAS, SAA

New order:
  date, wake, napStart, napEnd, bedtime, sleep, nap, combined, dayLen, actBefore, actAfter, activity, AAS, SAA

Swap the bedtime entry (currently at index 2) to after napEnd (currently at index 4). Move the bedtime object definition to between napEnd and sleepDuration in the array literal. This ensures the Metrics table displays time columns (Wake, Nap Start, Nap End, Bedtime) in chronological sequence before computed metrics.</action>
  <verify>
    <automated>awk '/const COLUMNS/,/Object\.freeze\(\[/' js/ui/metrics-screen.js | grep -n "napEnd\|bedtime" | awk -F: '{print $1}' | (read napEnd_line; read bedtime_line; [ "$bedtime_line" -gt "$napEnd_line" ] && echo "order-ok")</automated>
  </verify>
  <done>The COLUMNS array in metrics-screen.js is reordered. Bedtime now appears after Nap End in the column sequence. The Metrics table displays columns in the correct order: Date, Wake, Nap Start, Nap End, Bedtime, then metric columns (Sleep, Nap, Combined, etc.).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan modifies UI layout and button order; no security concerns |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-08-01 | — | UI layout | low | accept | Button reordering and CSS layout changes do not expose user data or interact with sensitive logic |
</threat_model>

<verification>
1. Run `npm run serve` and open http://localhost:8081
2. Navigate to Today screen and verify action buttons are centered and in order: Woke up → Nap start → Nap end → Going to sleep
3. Verify all screens (Today, History, Charts, Accuracy, Metrics) have reduced side padding and use more horizontal space (no large white gutters)
4. Navigate to Metrics screen and verify column headers appear in order: Date, Wake, Nap Start, Nap End, Bedtime, Sleep, Nap, Comb, Day Len, →Nap, Nap→, Act, AAS, SAA
</verification>

<success_criteria>
- Today screen action buttons are horizontally centered
- Button order is Woke up | Nap start | Nap end | Going to sleep
- All screens have minimal side padding (0.75rem per side instead of 1.5rem)
- Metrics table column order is: Date, Wake, Nap Start, Nap End, Bedtime, then remaining columns
- Visual layout is balanced and uses full screen width effectively on wider devices
</success_criteria>

<output>
Create `.planning/phases/NW-11-metrics-screen/11-08-SUMMARY.md` after execution
</output>
