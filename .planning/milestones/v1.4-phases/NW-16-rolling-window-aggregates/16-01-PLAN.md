---
phase: 16-rolling-window-aggregates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/ui/metrics-screen.js
  - style.css
  - tests/e2e/metrics.spec.js
autonomous: true
requirements:
  - MET-09
  - MET-10

estimate:
  tokens: 65000
  raw_tokens: 43000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - Three distinct aggregate tbodies appear above per-day rows in order (D-06): "7-day rolling", "14-day rolling", "All-time"
    - Each rolling section renders Min/Avg/Max rows computed from the N most recent non-rejected stage-filtered days (MET-09, MET-10)
    - Exactly 7 non-rejected days available: 7-day section header shows "7-day rolling" with no day-count note (D-09)
    - Exactly 6 non-rejected days available: 7-day section header reads "7-day rolling (6 days available)"; all value cells show em-dash (D-09, D-10)
    - Zero non-rejected days available: both rolling sections render all three rows with em-dash values and no JS errors (D-10)
    - 14 or more non-rejected days available: both sections show computed aggregate values with no cold-start notes
    - Stage filter applied before slicing — nonRejectedDays derived from stage-filtered reversedDays, not from allDays (D-08)
    - TIF bound columns in rolling rows always show em-dash regardless of TIF toggle (D-05)
    - Min/Avg/Max rows in rolling sections always rendered — never conditionally omitted (D-10)
  artifacts:
    - buildSectionHeaderRow(label, colCount) private helper in js/ui/metrics-screen.js
    - buildRollingSection(nDays, label, nonRejectedDays, snap, isTif) private helper in js/ui/metrics-screen.js
    - .metrics-section-header CSS rule in style.css
    - render() restructured to produce sevenDayTbody + fourteenDayTbody + allTimeTbody + daysTbody in that order (D-01, D-06, D-07)
    - Playwright tests covering MET-09 and MET-10 boundary conditions in tests/e2e/metrics.spec.js
  key_links:
    - nonRejectedDays must come from reversedDays.filter(r => !r.rejected) after stage filter is applied — NOT from allDays
    - buildRollingSection passes nonRejectedDays.slice(-nDays) to aggregateMetrics (oldest-first slice of the N most recent)
    - Each rolling aggregate row receives TIF_COLUMNS.length placeholder td cells appended after buildAggregateRow cells to prevent column-count mismatch when TIF is active
  prohibitions:
    - Must NOT compute rolling aggregates from allDays before stage filter — only from already-filtered reversedDays
    - Must NOT hide or omit Min/Avg/Max rows when fewer than N days are available — rows always render (D-10)
    - Must NOT place TIF-computed values in rolling section cells — TIF placeholder cells always show em-dash (D-05)
---

<objective>
Add 7-day and 14-day rolling aggregate sections to the Metrics screen. Each section shows Min/Avg/Max rows for all base metric columns, computed from the N most recent non-rejected stage-filtered days. The current single summaryTbody is replaced by three visually-distinct tbodies (7-day rolling, 14-day rolling, All-time) ordered most-recent-first above the per-day history rows.

Purpose: Users gain an immediately actionable short-term trend view without leaving the Metrics screen.
Output: Modified metrics-screen.js with two new helpers and restructured render(); .metrics-section-header CSS rule in style.css; Playwright tests for MET-09 and MET-10 boundary conditions.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/NW-16-rolling-window-aggregates/16-CONTEXT.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: 7-day rolling tbody wired end-to-end with helpers, CSS, and All-time section label</name>
  <read_first>
    js/ui/metrics-screen.js — full file: COLUMNS array (18 entries, indices 0–17), TIF_COLUMNS array (12 entries), buildAggregateRow signature and cell-construction logic (lines ~254–277), buildTifAggregateRow (lines ~348–375), render() summaryTbody construction (lines ~529–563), table appendChild sequence at end of render()
    style.css — .metricsTable th rule (background-color: #f1f5f9, font-weight: 600, font-size: 0.8rem, padding: 8px 12px, border-bottom: 1px solid #e2e8f0) — this is the visual baseline .metrics-section-header must mirror; also .metrics-summary-tbody and .metrics-summary-row rules
    tests/e2e/metrics.spec.js — full file: data-seeding pattern via page.evaluate + localStorage.setItem('nightwatch:db', JSON.stringify(seedDb)) + page.reload(), selector conventions (.metricsTable, [data-tab="metrics"])
    .planning/phases/NW-16-rolling-window-aggregates/16-CONTEXT.md — decisions D-01 through D-10, section label exact strings (D-02), cold-start format (D-09)
  </read_first>
  <files>js/ui/metrics-screen.js, style.css, tests/e2e/metrics.spec.js</files>
  <action>
    Read all three target files in full before writing a single line. Then implement the following.

    In js/ui/metrics-screen.js, add two private helpers immediately after the buildTifAggregateRow function and before the Public API comment block:

    Helper 1 — buildSectionHeaderRow(label, colCount):
    Creates and returns a tr. Inside the tr, create one td with: class "metrics-section-header", colspan set to colCount (a number), textContent set to label. Label is always a hardcoded static string — textContent only, never innerHTML (T-11-05). colCount must be COLUMNS.length + TIF_COLUMNS.length (= 30) so the row spans all columns including TIF inline columns.

    Helper 2 — buildRollingSection(nDays, label, nonRejectedDays, snap, isTif):
    Step 1: available = nonRejectedDays.length. These days are already stage-filtered AND rejection-filtered by the caller — do NOT filter again inside this function (D-08).
    Step 2: headerLabel = (available < nDays) ? (label + ' (' + available + ' days available)') : label. Exactly this format per D-09; the string is hardcoded structure with a numeric count — textContent safe.
    Step 3: slice = nonRejectedDays.slice(-nDays). When nonRejectedDays is empty, slice is []. When nonRejectedDays.length <= nDays, slice is all of nonRejectedDays. This preserves oldest-first order for aggregateMetrics.
    Step 4: result = aggregateMetrics(slice). Returns { rows, avg, min, max }. When slice is [], all avg/min/max values are null and buildCell will render em-dash for each.
    Step 5: Create tbody; add classList entries 'metrics-summary-tbody' and 'metrics-rolling-tbody'.
    Step 6: Append buildSectionHeaderRow(headerLabel, COLUMNS.length + TIF_COLUMNS.length) as the first child of tbody.
    Step 7: Build three rows per D-10 (always render, even with 0 days):
      minRow = buildAggregateRow('Min', result.min, snap)
      avgRow = buildAggregateRow('Average', result.avg, snap)
      maxRow = buildAggregateRow('Max', result.max, snap)
    Step 8: For each of the three rows, append TIF_COLUMNS.length (= 12) placeholder td elements. Each td: textContent = '—', hidden = !isTif. This prevents column-count mismatch when TIF is active (D-05). Append placeholders AFTER the row is returned by buildAggregateRow (the row tr already has 18 cells; append 12 more to it directly via row.appendChild(td)).
    Step 9: Append minRow, avgRow, maxRow to tbody.
    Step 10: Return tbody.

    Modify render() in js/ui/metrics-screen.js:
    Locate the line: const metricsResult = aggregateMetrics(reversedDays);
    Immediately after that line, add: const nonRejectedDays = reversedDays.filter(r => !r.rejected);
    This derives nonRejectedDays from reversedDays (already stage-filtered via filterDayRecordsByStage), NOT from allDays — per D-08 prohibition.

    Locate the summaryTbody construction block. Change it as follows:
    - Keep the variable name summaryTbody but rename its role to the all-time tbody.
    - Before appending minRow to summaryTbody, prepend a section-header row for the all-time label: create it with buildSectionHeaderRow('All-time', COLUMNS.length + TIF_COLUMNS.length) and call summaryTbody.appendChild(sectionHeaderRow) FIRST, then append minRow, avgRow, maxRow, then the TIF rows. This ensures 'All-time' is the first row in the all-time tbody (D-02).

    Add the 7-day rolling tbody immediately before the table.appendChild(summaryTbody) call:
      const sevenDayTbody = buildRollingSection(7, '7-day rolling', nonRejectedDays, snap, isTif);

    Update the table's tbody append sequence so that D-06 order is: thead → sevenDayTbody → summaryTbody (all-time) → daysTbody. The 14-day tbody slot between sevenDayTbody and summaryTbody will be filled by Task 2.

    In style.css, add the following CSS rule block after the existing .metricsTable tbody.metrics-summary-tbody block (after line ~1727). The rule mirrors the thead th styling (D-03):

    .metricsTable td.metrics-section-header {
      background-color: #f1f5f9;
      font-weight: 600;
      font-size: 0.8rem;
      text-transform: uppercase;
      color: #334155;
      padding: 6px 12px;
      letter-spacing: 0.05em;
      border-top: 2px solid #cbd5e1;
    }

    This applies to section-header td elements inside .metricsTable. The uppercase is applied by CSS — the textContent stays lowercase ('7-day rolling', 'All-time') per D-02.

    In tests/e2e/metrics.spec.js, add a new describe block 'Metrics Screen: Rolling Window Aggregates (MET-09, MET-10)' after the existing describe block. Inside it, add test 'MET-09: 7-day rolling section appears above per-day rows':
    - beforeEach: navigate to app, wait for [data-tab="today"]
    - Seed localStorage with 8 days of wake+bedtime pairs spanning dates '2025-01-01' through '2025-01-08'. For each date YYYY-MM-DD, add two events: { type: 'wake', at: 'YYYY-MM-DDT08:00' } and { type: 'bedtime', at: 'YYYY-MM-DDT22:00' }. Use version: 2, settings: { cutoverHour: 4, timeFormat: '24h', maxDelta: 30, minDays: 1, windowDays: 7, statBlend: 'median', autoOutlier: false, groupingMode: 'calendar', rejectedDays: [], stages: [], activeStageId: null, forecastAlgorithm: 'classic' }. Use page.evaluate to call localStorage.setItem and page.reload() + page.waitForSelector('[data-tab="today"]').
    - Navigate to metrics tab, wait for .metricsTable to be visible.
    - Assert: page.locator('.metrics-rolling-tbody').first() is visible.
    - Assert: page.locator('.metrics-rolling-tbody').first().locator('.metrics-section-header') has textContent that includes 'day rolling' (case-insensitive check since CSS uppercases display; use .toContainText('day rolling', { ignoreCase: true }) or check textContent directly for '7-day rolling').
    - Assert: page.locator('.metrics-rolling-tbody').first().locator('.metrics-summary-row').count() resolves to 3.
  </action>
  <acceptance_criteria>
    - buildSectionHeaderRow function exists in metrics-screen.js after buildTifAggregateRow
    - buildRollingSection function exists in metrics-screen.js after buildSectionHeaderRow
    - render() contains the line: const nonRejectedDays = reversedDays.filter(r => !r.rejected);
    - render() contains: buildRollingSection(7, '7-day rolling', nonRejectedDays, snap, isTif)
    - render() table construction appends sevenDayTbody before summaryTbody
    - summaryTbody contains a buildSectionHeaderRow call with label 'All-time'
    - style.css contains rule for .metricsTable td.metrics-section-header with background-color: #f1f5f9
    - Playwright test 'MET-09: 7-day rolling section appears above per-day rows' passes
    - npx playwright test tests/e2e/metrics.spec.js exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/metrics.spec.js --reporter=line 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    buildSectionHeaderRow and buildRollingSection helpers exist in metrics-screen.js. render() extracts nonRejectedDays from stage-filtered reversedDays, creates a 7-day rolling tbody via buildRollingSection(7, ...), and adds a section-header row to the all-time summaryTbody. Table order is: thead → sevenDayTbody → summaryTbody (all-time) → daysTbody. .metrics-section-header CSS rule is in style.css. Playwright test with 8-day seeded data confirms the 7-day rolling section renders with 3 aggregate rows.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Expand: 14-day rolling tbody + comprehensive boundary tests (MET-09, MET-10)</name>
  <read_first>
    js/ui/metrics-screen.js — after Task 1 changes: current buildRollingSection signature, render() tbody append sequence, location where fourteenDayTbody must be inserted (between sevenDayTbody and summaryTbody per D-06)
    tests/e2e/metrics.spec.js — after Task 1 changes: existing MET-09 test and data-seeding pattern to replicate for boundary tests
  </read_first>
  <files>js/ui/metrics-screen.js, tests/e2e/metrics.spec.js</files>
  <behavior>
    Test 1 (cold-start 7-day): seed 6 non-rejected days. 7-day rolling section header textContent is '7-day rolling (6 days available)'. Three aggregate rows exist. 14-day section header contains '6 days available'.
    Test 2 (cold-start 14-day only): seed 13 non-rejected days. 7-day section header is '7-day rolling' (no day-count note). 14-day section header contains '13 days available'.
    Test 3 (both sections full): seed 15 non-rejected days. Neither rolling section header contains 'days available'. Both sections have 3 aggregate rows.
    Test 4 (zero days): seed DB with no events (events: []). Metrics screen shows empty state — no .metricsTable rendered — no JS errors in console.
    Test 5 (TIF cells hidden when TIF off): seed 8 days with forecastAlgorithm: 'classic'. All td elements inside rolling aggregate rows that are positioned after the 18th cell are hidden (hidden attribute present). Assert count of hidden tds in first rolling tbody first aggregate row is 12 (= TIF_COLUMNS.length).
  </behavior>
  <action>
    RED PHASE first: add Test 1 through Test 5 (per behavior block above) to the 'Rolling Window Aggregates' describe block in tests/e2e/metrics.spec.js. Run npx playwright test tests/e2e/metrics.spec.js to confirm the 5 new tests fail (the 14-day tbody does not exist yet — Test 1, 2, 3 fail on 14-day assertions; Test 4 may pass if zero days shows empty state; Test 5 verifies hidden-cell count). Confirm failures before proceeding.

    GREEN PHASE — implement in js/ui/metrics-screen.js render():
    After the line that builds sevenDayTbody, add:
      const fourteenDayTbody = buildRollingSection(14, '14-day rolling', nonRejectedDays, snap, isTif);
    Update the table tbody append sequence per D-06 order: thead → sevenDayTbody → fourteenDayTbody → summaryTbody (all-time) → daysTbody.

    Data-seeding guidance for test cases (follow page.evaluate + localStorage.setItem pattern):
    - For N days: build events array with N pairs. For day index i (0-based), date = '2025-01-' + String(i+1).padStart(2,'0'). Events: { type: 'wake', at: date + 'T08:00' } and { type: 'bedtime', at: date + 'T22:00' }. cutoverHour: 4, rejectedDays: [], version: 2, forecastAlgorithm: 'classic' (or 'tif' for Test 5 when checking TIF cells).
    - Test 4 (zero days): events: [] in seedDb. Expected: .metricsTable not present; empty state p.emptyState visible.
    - Test 5 (TIF cells): seed 8 days with forecastAlgorithm: 'classic'. After navigating to metrics, locate all td elements in the first .metrics-rolling-tbody row that has class 'metrics-summary-row'. Using page.evaluate, count how many td elements in that row have the hidden attribute set. Assert count equals 12 (TIF_COLUMNS.length per D-05).

    For Test 1 and Test 2 cold-start assertions: .metrics-rolling-tbody nth(0) locator for 7-day section, nth(1) for 14-day section. Check .metrics-section-header textContent for the cold-start note. Assertion: use page.locator('.metrics-rolling-tbody').nth(0).locator('.metrics-section-header').textContent() and assert it contains '(6 days available)' for Test 1.

    After GREEN: run npm test (full suite) to ensure no regressions. Confirm unit tests pass (js/lib/ unchanged, no new imports needed in metrics.js).
  </action>
  <acceptance_criteria>
    - render() contains: const fourteenDayTbody = buildRollingSection(14, '14-day rolling', nonRejectedDays, snap, isTif);
    - render() table appends tbodies in order: sevenDayTbody → fourteenDayTbody → summaryTbody → daysTbody
    - Test 1 (6 days): 7-day header textContent is '7-day rolling (6 days available)' — Playwright assertion passes
    - Test 2 (13 days): 7-day header is '7-day rolling' (exact, no note); 14-day header contains '(13 days available)' — both Playwright assertions pass
    - Test 3 (15 days): neither rolling section header contains 'days available' — Playwright assertion passes
    - Test 4 (0 days): .metricsTable is not present in DOM; no JS console errors — Playwright assertion passes
    - Test 5 (TIF cells hidden): count of hidden td elements in first rolling aggregate row is 12 — Playwright assertion passes
    - npm test exits 0 (unit, integration, and e2e all pass)
  </acceptance_criteria>
  <verify>
    <automated>npm test 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    fourteenDayTbody built by buildRollingSection(14, '14-day rolling', ...) inserted between sevenDayTbody and summaryTbody. Table order: thead → 7-day tbody → 14-day tbody → All-time tbody → days tbody. All five Playwright boundary tests pass. Full test suite (npm test) is green. MET-09 and MET-10 requirements satisfied: 7-day and 14-day rolling aggregate sections operational with correct data, cold-start notes when insufficient data, and visual distinction via section-header rows (D-02, D-03, D-06, D-07, D-09, D-10).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aggregateMetrics output → DOM cells | Numeric aggregate values rendered via formatCellValue (textContent) — no user-controlled strings enter section header labels or cell content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Information Disclosure | Rolling aggregate display | low | accept | Aggregates show the same data already visible in per-day rows; no new data class exposed; all cell content rendered via textContent per T-11-05 |
| T-16-02 | Denial of Service | buildRollingSection with 0 available days | low | mitigate | aggregateMetrics([]) returns all-null result; D-10 guarantees Min/Avg/Max rows always appended; formatCellValue(null, ...) renders em-dash — no throw path exists |
| T-16-SC | Tampering | npm/pip/cargo installs | high | accept | No new packages installed in this phase; node_modules unchanged |
</threat_model>

<verification>
1. npm run test:unit passes — no changes to js/lib/ modules
2. npx playwright test tests/e2e/metrics.spec.js passes — existing MET-01..MET-06 tests plus 6 new rolling-window tests all green
3. npm test (full suite) passes — no regressions in unit, integration, or e2e
4. Visual inspection: Metrics screen in browser shows three section-header rows ("7-DAY ROLLING", "14-DAY ROLLING", "ALL-TIME" — CSS uppercased) above per-day rows
5. sw-precache.test.js passes — no new app-shell files added, PRECACHE_LIST unchanged
</verification>

<success_criteria>
- MET-09: 7-day rolling aggregate section (Min/Avg/Max) renders for all base metric columns from 7 most recent non-rejected stage-filtered days
- MET-10: 14-day rolling aggregate section renders with same structure; all three aggregate sections (7-day, 14-day, all-time) are visually distinguished by .metrics-section-header rows with section labels per D-02
- Cold-start (D-09, D-10): fewer than N non-rejected days shows day-count note in header; all three aggregate rows always rendered regardless of data availability
- Probe edges covered: exactly-7, exactly-6, zero-day, 14-day-full, 13-day-cold-start boundary cases verified by Playwright
- Stage filter precedes slicing — nonRejectedDays derived from stage-filtered reversedDays (D-08)
- TIF placeholder cells in rolling rows prevent column-count mismatch; all TIF cells show em-dash (D-05)
- Full test suite green
</success_criteria>

## Artifacts This Phase Produces

| Artifact | Type | Location |
|----------|------|----------|
| buildSectionHeaderRow(label, colCount) | Private JS function | js/ui/metrics-screen.js |
| buildRollingSection(nDays, label, nonRejectedDays, snap, isTif) | Private JS function | js/ui/metrics-screen.js |
| .metrics-section-header | CSS rule | style.css |
| .metrics-rolling-tbody | CSS class (applied by buildRollingSection) | js/ui/metrics-screen.js |
| MET-09 Playwright test | Test case | tests/e2e/metrics.spec.js |
| MET-10 boundary Playwright tests (5 cases) | Test cases | tests/e2e/metrics.spec.js |

<output>
Create `.planning/phases/NW-16-rolling-window-aggregates/16-01-SUMMARY.md` when done
</output>
