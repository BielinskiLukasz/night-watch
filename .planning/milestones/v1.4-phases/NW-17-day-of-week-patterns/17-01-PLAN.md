---
phase: NW-17
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/lib/metrics.js
  - tests/unit/metrics.test.js
  - js/lib/db-shape.js
  - js/lib/settings-validate.js
  - tests/unit/settings-validate.test.js
  - js/ui/metrics-screen.js
  - style.css
  - index.html
  - js/ui/settings-modal.js
  - tests/e2e/metrics.spec.js
autonomous: true
requirements:
  - MET-11
  - MET-12

estimate:
  tokens: 78000
  raw_tokens: 44000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "dayOfWeekAverages(dayRecords) exported from js/lib/metrics.js groups pre-filtered day records by weekday using wake date via new Date(dateStr + 'T00:00').getDay(), returns array of 7 entries (indexes 0=Sun..6=Sat) with activityBeforeNap, activityAfterNap, napDuration, sleepDuration averages as numbers or null — verified by unit tests (MET-11, D-04, D-06)"
    - "Nap-related averages (activityBeforeNap/MA, activityAfterNap/AA, napDuration) exclude no-nap days (day.napStart === null per D-02); a weekday with only no-nap days returns null for those three columns (D-01, D-03)"
    - "Sleep duration in dayOfWeekAverages() is paired via prevDay.bedtime to day.wake (same overnight-pairing as aggregateMetrics), not from same-day bedtime — consistent with the Metrics table Sleep column (D-06)"
    - "The Metrics screen render() appends a <details class='metrics-dow-section'> element with NO 'open' attribute (collapsed by default) and <summary> textContent exactly 'Day-of-Week Patterns' to tableScroll after the main metrics table (D-08, D-11, D-12, D-13, D-14, MET-12)"
    - "The DoW <details> section contains a standalone 5-column table (Weekday | MA | AA | Nap | Sleep) with 7 rows ordered per snap.firstDayOfWeek — 'monday' yields Mon-first [1..6,0], 'sunday' yields [0..6]; null cells render as the em-dash character (MET-12, D-09)"
    - "DEFAULT_SETTINGS.firstDayOfWeek = 'monday'; RULES.firstDayOfWeek validates as enum {monday, sunday}; the Settings modal Subject & Display fieldset contains a <select name='firstDayOfWeek'> dropdown populated and saved by settings-modal.js (D-10)"
    - "buildDowSection(nonRejectedDays, snap) receives nonRejectedDays — the same stage-filtered + rejected-excluded array used by rolling sections — so active-stage scoping is automatic without additional filtering (D-07, MET-12 success criterion 3)"
  artifacts:
    - "js/lib/metrics.js — exports dayOfWeekAverages(dayRecords)"
    - "js/lib/db-shape.js — DEFAULT_SETTINGS.firstDayOfWeek = 'monday'"
    - "js/lib/settings-validate.js — RULES.firstDayOfWeek enum {monday, sunday}"
    - "js/ui/metrics-screen.js — buildDowSection() private helper + tableScroll.replaceChildren(table, dowSection) in render()"
    - "style.css — .metrics-dow-section and .metrics-dow-table CSS classes"
    - "index.html — <select name='firstDayOfWeek'> in Subject & Display fieldset"
    - "tests/unit/metrics.test.js — describe('dayOfWeekAverages(dayRecords)') with 7+ test cases covering all ROADMAP success criteria"
    - "tests/unit/settings-validate.test.js — describe for firstDayOfWeek validation"
    - "tests/e2e/metrics.spec.js — MET-11/MET-12 E2E coverage"
  key_links:
    - "buildDowSection(nonRejectedDays, snap) calls dayOfWeekAverages(nonRejectedDays) and reads snap.firstDayOfWeek ?? 'monday' for row ordering"
    - "render() in metrics-screen.js: tableScroll.replaceChildren(table, dowSection) places DoW section after main table inside the scroll container"
    - "settings-modal.js populateForm() reads snap.firstDayOfWeek; onClose raw object includes firstDayOfWeek from FormData"
---

<objective>
Wire the complete Day-of-Week Patterns feature for Phase 17.

Task 1 (tracer/TDD): Implement `dayOfWeekAverages()` in metrics.js via RED-GREEN TDD, add `firstDayOfWeek` to the settings schema, wire a collapsible "Day-of-Week Patterns" section into the Metrics screen, and add matching CSS.

Task 2 (settings UI): Add `firstDayOfWeek` dropdown to the Settings modal HTML and JS wiring, plus validation unit tests.

Task 3 (E2E): Full Playwright coverage for the DoW section — presence, row count, empty-weekday rendering, and re-render collapse reset.

Purpose: Users see per-weekday sleep rhythm averages at a glance; first-day-of-week is configurable.
Output: Working collapsible DoW table in the Metrics screen, backed by a tested pure function and settings control.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/NW-17-day-of-week-patterns/17-CONTEXT.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: dayOfWeekAverages() TDD + firstDayOfWeek schema + DoW section + CSS</name>
  <files>js/lib/metrics.js, tests/unit/metrics.test.js, js/lib/db-shape.js, js/lib/settings-validate.js, js/ui/metrics-screen.js, style.css</files>

  <read_first>
    js/lib/metrics.js — read extractDate() (lines 31-36), extractTime() (lines 23-28), aggregateMetrics() (lines 230-360+) for the prevDay-pairing pattern and calculateOvernightSleep inline helper; read the full export list at the bottom; add dayOfWeekAverages after aggregateMetrics
    js/lib/db-shape.js — read DEFAULT_SETTINGS object fully (lines 41-66) and the JSDoc @type comment (lines 22-40); add firstDayOfWeek after intenseDayOffsetMinutes
    js/lib/settings-validate.js — read the RULES object (lines 43-65); add firstDayOfWeek after intenseDayOffsetMinutes
    js/ui/metrics-screen.js — read: (a) imports block (lines 17-24, add dayOfWeekAverages to the metrics.js import); (b) render() function from line 522 to end (locate tableScroll.replaceChildren call at line 684); (c) buildRollingSection helper (lines 422-475) for helper structure reference; (d) formatCellValue helper (lines 135-148) for the null-to-dash pattern
    style.css — read lines 1649-1766 (existing metrics CSS) for class naming and padding conventions
    tests/unit/metrics.test.js — read entire file; locate the import list (lines 17-31) to add dayOfWeekAverages; understand the makeDay helper; add a new makeEventDay helper and describe block
  </read_first>

  <behavior>
    TDD expectations — write these tests FIRST in tests/unit/metrics.test.js, run to confirm RED, then implement GREEN:

    - Empty input: dayOfWeekAverages([]) returns exactly 7 entries; every entry has activityBeforeNap, activityAfterNap, napDuration, sleepDuration all null; entry at index 1 has label 'Mon'
    - Single Monday nap-day: one record with wake {at:'2025-01-06T07:00'}, napStart {at:'2025-01-06T12:00'} (300 min from wake), napEnd {at:'2025-01-06T14:00'} (120 min nap), bedtime {at:'2025-01-06T22:00'}; prevDay has bedtime {at:'2025-01-05T22:30'} (510 min sleep); index-1 entry (Mon) has activityBeforeNap=300, napDuration=120, sleepDuration=510; all other 6 entries have all-null metrics
    - Single Monday no-nap day: wake {at:'2025-01-13T07:00'}, bedtime {at:'2025-01-13T22:00'}, napStart null; index-1 (Mon) activityBeforeNap null, activityAfterNap null, napDuration null; sleepDuration non-null (paired with prevDay.bedtime)
    - Mix: two Monday records — one nap-day (MA=300, nap=120), one no-nap-day; nap column average comes from nap-day only (napDuration=120 not averaged with null); sleepDuration averages both
    - No-date record: wake is a bare 'HH:MM' string (not an event object with .at); extractDate returns null; that record contributes to no weekday bucket
    - Thursday (index 4) and Saturday (index 6) records → bucketed correctly; Monday (index 1) stays null
    - Two records same weekday: activityBeforeNap averages to Math.round of the mean (duration rounding per aggregateMetrics pattern)
  </behavior>

  <action>
    RED phase: Add `dayOfWeekAverages` to the import line in tests/unit/metrics.test.js (the existing import from metrics.js). Add a `makeEventDay(wakeAt, bedtimeAt, napStartAt, napEndAt)` helper that returns an object where each non-null argument becomes `{ at: argValue }` under the corresponding slot key, and `rejected: false`. Add `describe('dayOfWeekAverages(dayRecords)', () => { ... })` with the 7 test cases listed in the behavior block — use 2025-01-06 (Monday), 2025-01-13 (Monday), 2025-01-09 (Thursday), 2025-01-11 (Saturday) for date fixtures. Run `node --test tests/unit/metrics.test.js 2>&1 | tail -20` and confirm all new tests fail.

    GREEN phase — dayOfWeekAverages in js/lib/metrics.js: Add the export after aggregateMetrics. The function iterates dayRecords with index access to get prevDay (same as aggregateMetrics). For each record, call `extractDate(day.wake)` and skip records where the result is null. Compute weekday via `new Date(dateStr + 'T00:00').getDay()` — index 0=Sun, 1=Mon, 2=Tue, 3=Wed, 4=Thu, 5=Fri, 6=Sat. This is wall-clock local time, never UTC. Maintain 7 per-weekday accumulator objects each holding sum and count for each metric. For nap-related metrics (activityBeforeNap, activityAfterNap, napDuration), only accumulate when `day.napStart != null` (D-02). For sleepDuration, pair with prevDay.bedtime using an inline `calcSleep(bedStr, wakeStr)` helper identical in logic to the one inside aggregateMetrics (timeToMinutes subtraction with +1440 correction). For other metrics, call the existing module helpers (dayLength, totalActivity, combinedSleepNap, activityAfterSleepFactor, dayToSleepFactor, napFraction, amPmSplit, maSleepRatio, maNapRatio) per D-05 and accumulate into the bucket. After iterating, produce the 7-entry return array using `Array.from({length:7}, (_, i) => ({ weekday: i, label: DAY_LABELS[i], activityBeforeNap: avg_or_null, ... }))` where duration averages are `Math.round(sum / count)` and ratio averages are `sum / count`. DAY_LABELS is a module-local constant `['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat']`. Run `node --test tests/unit/metrics.test.js 2>&1 | tail -20` and confirm all tests pass.

    db-shape.js: In DEFAULT_SETTINGS, after the `intenseDayOffsetMinutes` line, add `firstDayOfWeek: 'monday'` with a comment referencing D-10 and MET-12. Update the JSDoc @type to add `firstDayOfWeek: string`.

    settings-validate.js: In RULES, after `intenseDayOffsetMinutes`, add `firstDayOfWeek: { type: 'enum', values: new Set(['monday', 'sunday']) }` with a comment referencing D-10.

    metrics-screen.js — three changes: (a) Extend the `import { aggregateMetrics }` line at the top to also import `dayOfWeekAverages` from '../lib/metrics.js'. (b) Add `buildDowSection(nonRejectedDays, snap)` as a private helper function before `mountMetricsScreen`. The helper: reads `const firstDay = snap.firstDayOfWeek ?? 'monday'`; calls `dayOfWeekAverages(nonRejectedDays)` to get a 7-entry array; rotates the array so the correct day leads (if 'monday': `[...entries.slice(1), entries[0]]`; if 'sunday': keep as-is); creates a `<details>` element with className 'metrics-dow-section' and NO `open` attribute — this implements D-12 and D-14; creates `<summary>` with textContent 'Day-of-Week Patterns' — exactly this string per D-13; creates `<table className='metrics-dow-table'>` with `<thead>` containing one `<tr>` with five `<th>` elements: 'Weekday', 'MA', 'AA', 'Nap', 'Sleep' (all via textContent); creates `<tbody>` with one `<tr>` per entry using `entry.label` for the first cell and `formatDuration(value)` for the four metric cells, falling back to the em-dash character (U+2014) when the value is null; all cell values set via `.textContent` only (XSS guard per T-11-05); appends tbody to table, appends table to details, returns details element. (c) In render(), locate `tableScroll.replaceChildren(table)` at the bottom and replace it with two lines: `const dowSection = buildDowSection(nonRejectedDays, snap);` followed by `tableScroll.replaceChildren(table, dowSection);`.

    style.css: After the `.emptyState` rule (around line 1760), add a clearly commented block of CSS for the DoW section. `.metrics-dow-section` gets `margin-top: 1rem` and `overflow-x: auto`. `.metrics-dow-section summary` gets `cursor: pointer; padding: 8px 12px; background-color: #f1f5f9; font-weight: 600; font-size: 0.9rem; color: #334155; border: 1px solid #e2e8f0; border-radius: 4px; list-style: none; user-select: none;`. `.metrics-dow-section[open] summary` gets `border-radius: 4px 4px 0 0`. `.metrics-dow-table` gets `border-collapse: collapse; font-size: 0.9rem; font-variant-numeric: tabular-nums; width: 100%; border: 1px solid #e2e8f0; border-top: none`. `.metrics-dow-table th, .metrics-dow-table td` get `padding: 8px 12px; border-bottom: 1px solid #e2e8f0; text-align: right`. `.metrics-dow-table th` gets `background-color: #f1f5f9; font-weight: 600; font-size: 0.8rem`. `.metrics-dow-table th:first-child, .metrics-dow-table td:first-child` get `text-align: left; font-weight: 600`.
  </action>

  <verify>
    <automated>node --test tests/unit/metrics.test.js 2>&1 | tail -20</automated>
    <fails_when>non-zero exit or any "not ok" line in the output</fails_when>
  </verify>

  <acceptance_criteria>
    - `node --test tests/unit/metrics.test.js` exits 0 with all tests passing including the new dayOfWeekAverages describe block (7+ test cases)
    - `grep -c "dayOfWeekAverages" js/lib/metrics.js` outputs a count of 2 or more (definition + export keyword)
    - `grep "firstDayOfWeek" js/lib/db-shape.js` shows the field present in DEFAULT_SETTINGS
    - `grep "firstDayOfWeek" js/lib/settings-validate.js` shows the rule present in RULES
    - `grep -c "buildDowSection" js/ui/metrics-screen.js` outputs 2 or more (definition + call site)
    - `grep "replaceChildren(table, dowSection)" js/ui/metrics-screen.js` finds the updated call
    - `grep -c "metrics-dow-section" style.css` outputs 3 or more (multiple rules target this class)
    - Opening the Metrics screen in a browser (after seeding data) shows a collapsed details element below the main table; its summary reads "Day-of-Week Patterns"; expanding it shows a 5-column 7-row table
  </acceptance_criteria>

  <done>dayOfWeekAverages() passes all unit tests; firstDayOfWeek schema additions are in place; DoW section wired end-to-end in the Metrics screen; CSS renders the section correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: firstDayOfWeek Settings UI — HTML dropdown, modal wiring, and validation unit tests (per D-10)</name>
  <files>index.html, js/ui/settings-modal.js, tests/unit/settings-validate.test.js</files>

  <read_first>
    index.html — read the "Subject and Display" fieldset (lines 219-232); insert firstDayOfWeek select after the timeFormat label block before the closing fieldset tag
    js/ui/settings-modal.js — read populateForm() function (lines 69-97) to see where to add the firstDayOfWeek populate call; read the onClose raw object construction (lines 132-154) to see where to add firstDayOfWeek extraction from FormData
    tests/unit/settings-validate.test.js — read the tail of the file (last 30 lines) to understand where to append a new describe block; note the valid() helper that spreads DEFAULT_SETTINGS
  </read_first>

  <behavior>
    TDD expectations for settings-validate.test.js — write these first, confirm RED, then verify GREEN after Task 1 schema changes:

    - valid({ firstDayOfWeek: 'monday' }) in mode:'save' → ok: true
    - valid({ firstDayOfWeek: 'sunday' }) in mode:'save' → ok: true
    - valid({ firstDayOfWeek: 'saturday' }) in mode:'save' → ok: false, errors contains entry with field 'firstDayOfWeek'
    - valid({ firstDayOfWeek: '' }) in mode:'save' → ok: false
    - mode:'load' with invalid value 'wednesday' → ok: true (lenient), normalized.firstDayOfWeek === 'monday' (resets to default)
  </behavior>

  <action>
    tests/unit/settings-validate.test.js: Append a new `describe('validateSettings — firstDayOfWeek (D-10)', () => { ... })` block before the existing "default mode" describe. Write the 5 test cases from the behavior block using the existing `valid()` helper. Run `node --test tests/unit/settings-validate.test.js 2>&1 | tail -20` before making any other changes and confirm the new tests fail (RED). They will pass automatically after Task 1 adds firstDayOfWeek to RULES and DEFAULT_SETTINGS — no code change needed in settings-validate.js itself for this task.

    index.html: In the "Subject and Display" fieldset, after the closing tag of the `<label>` block for timeFormat and before `</fieldset>`, insert a new label element. The label text is 'First day of week'. Inside the label, a `<select>` element with `name="firstDayOfWeek"` and `id="firstDayOfWeek"`. The select contains two option elements: `value="monday"` with display text 'Monday' and `value="sunday"` with display text 'Sunday'. This matches the existing `<label><select>` pattern used for timeFormat directly above.

    js/ui/settings-modal.js populateForm(s): After the `tifRollingDaysEl` block (reading `s.tifRollingDays`), add a block that reads `form.elements.namedItem('firstDayOfWeek')` into a local constant and, if the element exists, sets its `.value` to `s.firstDayOfWeek ?? 'monday'`.

    js/ui/settings-modal.js onClose raw object: In the raw object literal passed to `validateSettings`, after the `intenseDays` line, add `firstDayOfWeek: String(data.get('firstDayOfWeek') ?? 'monday')`. This is consistent with the string coercion pattern used for other enum fields (forecastAlgorithm, statBlend, groupingMode, timeFormat).
  </action>

  <verify>
    <automated>node --test tests/unit/settings-validate.test.js 2>&1 | tail -20</automated>
    <fails_when>non-zero exit or any "not ok" line in the output</fails_when>
  </verify>

  <acceptance_criteria>
    - `node --test tests/unit/settings-validate.test.js` exits 0 with all tests passing including the new firstDayOfWeek describe block (5 test cases)
    - `grep "firstDayOfWeek" index.html` shows the select element present in the Subject and Display fieldset
    - `grep "firstDayOfWeek" js/ui/settings-modal.js` appears at least twice (populateForm + onClose raw)
    - Opening the Settings modal in a browser shows a "First day of week" dropdown with Monday and Sunday options
    - Selecting Sunday, saving, and reopening the modal shows Sunday as the selected value
    - After saving firstDayOfWeek='sunday', the Metrics screen DoW section re-renders with Sunday as the first row (via the synchronous settings.update → render subscription chain)
  </acceptance_criteria>

  <done>Settings modal shows firstDayOfWeek dropdown; saving it persists the value and triggers Metrics re-render; validation unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: E2E tests for Day-of-Week Patterns (MET-11, MET-12)</name>
  <files>tests/e2e/metrics.spec.js</files>

  <read_first>
    tests/e2e/metrics.spec.js — read entire file to understand the seedDb pattern (settings fields required, event shape), the page seeding idiom (localStorage.setItem + reload), and the tail of the file to find where to append the new describe block
  </read_first>

  <action>
    Append a new `test.describe('Metrics Screen Day-of-Week Patterns (MET-11, MET-12)', () => { ... })` block at the end of tests/e2e/metrics.spec.js. The describe block contains 3 tests. All tests use `test.beforeEach` to navigate to the app, or inline seeding per the existing pattern in the file.

    All seedDb objects for this describe must include `firstDayOfWeek: 'monday'` in the settings object alongside the other required fields (cutoverHour: 4, timeFormat: '24h', maxDelta: 30, minDays: 1, windowDays: 7, statBlend: 'median', autoOutlier: false, groupingMode: 'calendar', rejectedDays: [], stages: [], activeStageId: null, forecastAlgorithm: 'classic').

    Test 1 `'MET-12/basic: DoW section present, collapsed by default, expands to 7 rows'`:
    Seed 14 days covering dates 2025-04-01 through 2025-04-14 (these span Mon through Sun twice). For each day add a wake event and a bedtime event. For 7 of the days (odd-numbered days) also add napStart and napEnd events (napStart at 12:00, napEnd at 13:30 on the same date). Navigate to the Metrics tab and wait for `.metricsTable`. Assert that `page.locator('.metrics-dow-section')` has count 1. Assert the details element does NOT have the 'open' attribute (collapsed by default per D-12). Click the summary child of the details element. Assert the details element now HAS the 'open' attribute. Assert `page.locator('.metrics-dow-table tbody tr')` has count 7. Assert no console errors (register an error listener before seeding).

    Test 2 `'MET-12/empty-weekdays: only-Monday data — other weekdays render em-dash without errors'`:
    Seed records for exactly two Mondays: 2025-01-06 and 2025-01-13. Each day has wake, bedtime, napStart (12:00), and napEnd (13:30). Navigate to Metrics, open the DoW section (click summary). Verify the Monday row (first row when firstDayOfWeek is 'monday') has a non-dash value in at least one metric cell. Verify the remaining 6 rows each contain at least one td with textContent equal to the em-dash character. Use page.evaluate to count em-dash cells in the non-first rows and assert the count is 24 (6 rows * 4 metric columns). Assert no JS errors.

    Test 3 `'MET-14/rerender-collapse: DoW section resets to collapsed after re-render'`:
    Seed 8 days (dates 2025-06-01 through 2025-06-08). Navigate to Metrics, open DoW section (click summary), assert it is open. Trigger a re-render by calling `settings.update` via page.evaluate that changes a non-DoW setting (e.g., timeFormat from '24h' to '12h' and back). Because settings.subscribe fires render() synchronously, the DOM is rebuilt. Assert the details element no longer has the 'open' attribute after re-render. Alternatively, reload the page (which always resets state) and assert the section is collapsed on first load.
  </action>

  <verify>
    <automated>npx playwright test tests/e2e/metrics.spec.js --reporter=line 2>&1 | tail -20</automated>
    <fails_when>non-zero exit or any "FAILED" line in the output</fails_when>
  </verify>

  <acceptance_criteria>
    - All 3 new Playwright tests pass with no failures
    - All pre-existing Playwright tests in metrics.spec.js continue to pass (no regressions)
    - Test 1 confirms the DoW section is collapsed by default and expandable to exactly 7 rows
    - Test 2 confirms non-recorded weekdays render the em-dash character without JS errors
    - Test 3 confirms the section returns to collapsed state on re-render
    - `npm test 2>&1 | tail -30` exits 0 (full suite: unit + integration + E2E)
  </acceptance_criteria>

  <done>E2E coverage complete for MET-11 and MET-12; full npm test suite passes with no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings modal form input → settings store | firstDayOfWeek enum string from untrusted HTML form |
| localStorage nightwatch:db → settings store | firstDayOfWeek loaded from persisted storage (may be externally modified) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering | settings-modal.js firstDayOfWeek form value | medium | mitigate | validateSettings RULES.firstDayOfWeek enum check rejects any value not in {monday, sunday} before settings.update() is called; only result.normalized reaches the store (T-2-14) |
| T-17-02 | Tampering | localStorage nightwatch:db firstDayOfWeek field | low | mitigate | mode:'load' lenient validation silently resets invalid values to DEFAULT_SETTINGS.firstDayOfWeek ('monday'), so a tampered stored value cannot cause runtime errors |
| T-17-03 | Information Disclosure | dayOfWeekAverages() output rendered to DoW table | low | accept | All DoW table cell content is set via textContent only (per existing XSS guard T-11-05 in metrics-screen.js); weekday labels are from a frozen constant; metric values are numbers or null; no user-controlled strings are interpolated into HTML |
| T-17-SC | Tampering | npm/pip/cargo installs | N/A | accept | No new package-manager installs in this phase; no legitimacy audit needed |
</threat_model>

<verification>
After all 3 tasks complete:

Full test suite: `npm test 2>&1 | tail -30` — expects zero failures.

Manual smoke check:
- Navigate to Metrics screen (requires seeded data) — a collapsed "Day-of-Week Patterns" details element appears below the main metrics table
- Click the summary to expand — a 5-column, 7-row table shows Mon through Sun with MA, AA, Nap, Sleep averages
- Weekdays with no data show em-dashes in all metric cells
- Open Settings modal — "First day of week" dropdown is present in the Subject and Display section
- Change to Sunday and save — Metrics screen re-renders with Sunday as the first row in the DoW table
- Reload page — DoW section is collapsed again (re-render reset per D-14)
</verification>

<success_criteria>
- MET-11: `dayOfWeekAverages(dayRecords)` exported from `js/lib/metrics.js`, groups by weekday via wake date, excludes no-nap days from nap columns, returns null for empty weekdays — unit tests prove all four ROADMAP success-criterion behaviors
- MET-12: "Day-of-Week Patterns" collapsible section in Metrics screen, 7 rows Mon-Sun (or Sun-Mon), 4 metric columns, stage-scoped via nonRejectedDays, em-dash for empty cells, resets to collapsed on re-render — E2E tests prove all three screen-level behaviors
- D-01 through D-14 from 17-CONTEXT.md each implemented as specified
- `npm test` passes — no regressions in the existing test suite
</success_criteria>

<output>
Create `.planning/phases/NW-17-day-of-week-patterns/17-01-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

| Symbol | File | Type | Consumed By |
|--------|------|------|-------------|
| `dayOfWeekAverages(dayRecords)` | `js/lib/metrics.js` | exported function | `buildDowSection()` in metrics-screen.js; future MET-16 TIF source window |
| `DEFAULT_SETTINGS.firstDayOfWeek` | `js/lib/db-shape.js` | settings field | settings-validate.js RULES, settings-modal.js, metrics-screen.js snap |
| `RULES.firstDayOfWeek` | `js/lib/settings-validate.js` | validation rule | settings-modal.js onClose validation path |
| `buildDowSection(nonRejectedDays, snap)` | `js/ui/metrics-screen.js` | private helper | render() inside mountMetricsScreen |
| `.metrics-dow-section` | `style.css` | CSS class | applied to the `<details>` wrapper element |
| `.metrics-dow-table` | `style.css` | CSS class | applied to the 5-column weekday averages table |
| `firstDayOfWeek` select control | `index.html` | UI control | openSettings() in settings-modal.js |

## Source Audit

| Source | Item | Status |
|--------|------|--------|
| GOAL | "Users can inspect per-weekday averages for MA, AA, nap duration, and sleep duration in a collapsible Metrics screen section" | COVERED — Task 1 wires end-to-end, Task 3 verifies E2E |
| REQ MET-11 | `dayOfWeekAverages()` function, non-rejected grouping, null for empty weekdays | COVERED — Task 1 TDD |
| REQ MET-12 | Collapsible DoW section, 7 rows, Mon-Sun, stage-scoped | COVERED — Tasks 1 + 3 |
| CONTEXT D-01/D-02/D-03 | No-nap exclusion from nap columns | COVERED — Task 1 behavior block |
| CONTEXT D-04 | dayOfWeekAverages lives in metrics.js | COVERED — Task 1 |
| CONTEXT D-05 | Function computes all Metrics columns (not just MET-11 four) | COVERED — Task 1 action |
| CONTEXT D-06 | Wake-date attribution | COVERED — Task 1 action |
| CONTEXT D-07 | Pure function, caller pre-filters | COVERED — Task 1 (buildDowSection passes nonRejectedDays) |
| CONTEXT D-08 | DoW section after main table (standalone, below) | COVERED — Task 1 metrics-screen wiring |
| CONTEXT D-09 | Standalone 5-column table | COVERED — Task 1 buildDowSection |
| CONTEXT D-10 | firstDayOfWeek setting (db-shape, validate, Settings UI) | COVERED — Tasks 1 + 2 |
| CONTEXT D-11 | HTML details/summary for collapse | COVERED — Task 1 buildDowSection |
| CONTEXT D-12 | No open attribute (collapsed by default) | COVERED — Task 1 + Task 3 Test 1 |
| CONTEXT D-13 | Label exactly "Day-of-Week Patterns" | COVERED — Task 1 action |
| CONTEXT D-14 | Reset to collapsed on every re-render | COVERED — Task 1 (replaceChildren rebuilds DOM) + Task 3 Test 3 |
| DEFERRED | Persist open/closed state across re-renders | EXCLUDED per 17-CONTEXT.md Deferred Ideas |
| DEFERRED | firstDayOfWeek for any weekday (Mon-Sun, 7 options) | EXCLUDED per 17-CONTEXT.md Deferred Ideas |
