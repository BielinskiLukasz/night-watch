---
phase: 18-sleep-debt-proxy
plan: 18-03
type: execute
wave: 2
depends_on:
  - 18-01
  - 18-02
files_modified:
  - js/ui/metrics-screen.js
  - tests/e2e/metrics.spec.js
autonomous: true
requirements:
  - MET-14

estimate:
  tokens: 65000
  raw_tokens: 33000
  tasks: 2
  confidence: med

must_haves:
  goal: "Users can see a rolling 7-day accumulated sleep deficit column in the Metrics per-day table and all three aggregate sections"
  truths:
    - "COLUMNS in metrics-screen.js includes { key: 'sleepDebt', label: 'S.Debt', isTime: false, isRatio: false } (MET-14)"
    - "Each per-day row object has a sleepDebt field computed by sleepDebtProxy over non-rejected days up to and including that row's date (MET-14)"
    - "Cold-start rows (fewer than 7 qualifying prior records) show — in the Sleep Debt column via buildCell null handling (MET-14)"
    - "All-time aggregate section shows Sleep Debt avg, min { value, date }, max { value, date } (MET-14)"
    - "7-day and 14-day rolling aggregate sections show Sleep Debt avg, min, max (MET-14)"
    - "sleepDebtProxy is imported from ../lib/metrics.js in metrics-screen.js (D-08)"
    - "snap.settings.targetSleepMinutes is passed as the third argument to sleepDebtProxy everywhere it is called (D-01)"
    - "All cell content set via textContent — no innerHTML for debt values (XSS guard T-11-05)"
    - statement: "MET-14 ordering: aggregate min/max date attribution is stable — first occurrence wins on ties"
      flagged: true
  artifacts:
    - "js/ui/metrics-screen.js — updated COLUMNS array, sleepDebt row augmentation, aggregate augmentation, buildRollingSection augmentation, sleepDebtProxy import"
    - "tests/e2e/metrics.spec.js — updated column count assertion and S.Debt column presence check"
  key_links:
    - "COLUMNS array (metrics-screen.js line ~42) drives thead, buildAggregateRow, buildRollingSection — adding one entry propagates automatically to those renderers"
    - "buildCell(value, col, snap, minMaxVal) handles null → '—' and calls formatDuration when isTime:false isRatio:false — no changes needed there"
    - "sleepDebtProxy from 18-01 must be exported before this plan runs (Wave 2 dependency)"
    - "snap.settings.targetSleepMinutes from 18-02 must exist in DEFAULT_SETTINGS before this plan runs (Wave 2 dependency)"
---

<objective>
Add the Sleep Debt (S.Debt) column to the Metrics screen by extending COLUMNS, augmenting per-day row objects with sleepDebt values, and augmenting aggregate objects so all three aggregate sections (all-time, 7-day rolling, 14-day rolling) include debt avg/min/max. Update the E2E smoke test to account for the new column.

Purpose: Delivers the visible user-facing feature — a rolling 7-day sleep deficit signal in the Metrics table.
Output: A new column visible in every section of the Metrics screen; E2E test green.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/my-code/vibe-coding/night-watch/.planning/PROJECT.md
@C:/my-code/vibe-coding/night-watch/.planning/ROADMAP.md
@C:/my-code/vibe-coding/night-watch/.planning/phases/18-sleep-debt-proxy/18-CONTEXT.md
@C:/my-code/vibe-coding/night-watch/.planning/phases/18-sleep-debt-proxy/18-PATTERNS.md
@C:/my-code/vibe-coding/night-watch/.planning/phases/18-sleep-debt-proxy/18-01-SUMMARY.md
@C:/my-code/vibe-coding/night-watch/.planning/phases/18-sleep-debt-proxy/18-02-SUMMARY.md
</context>

<tasks>

<task id="1" type="auto">
  <name>Add Sleep Debt column to metrics-screen.js (COLUMNS, row augmentation, aggregates)</name>
  <read_first>
    - js/ui/metrics-screen.js (full file — understand: COLUMNS frozen array at line ~42, import block at line ~17, buildCell at line ~162, buildDayRow at line ~198, buildAggregateRow at line ~254, buildRollingSection at line ~403, the main render loop at line ~629 where aggregateMetrics is called and rows are iterated)
    - js/lib/metrics.js (confirm sleepDebtProxy is exported after Plan 18-01)
    - .planning/phases/18-sleep-debt-proxy/18-CONTEXT.md (decisions D-04, D-05, D-06, D-07, D-08 and integration point description)
    - .planning/phases/18-sleep-debt-proxy/18-PATTERNS.md (COLUMNS entry to add, per-day debt computation pattern, aggregate augmentation pattern, import addition)
  </read_first>
  <action>
    Make four targeted changes to js/ui/metrics-screen.js:

    CHANGE 1 — Import sleepDebtProxy:
    In the import block at the top of the file, add sleepDebtProxy to the named imports from '../lib/metrics.js'. The existing import already brings in aggregateMetrics and dayOfWeekAverages from that module — add sleepDebtProxy to the same destructured list.

    CHANGE 2 — Extend COLUMNS array:
    In the frozen COLUMNS array (line ~42), add a new entry after the combinedSleepNap entry (index 8, per PATTERNS.md grouping logic):
      { key: 'sleepDebt', label: 'S.Debt', isTime: false, isRatio: false }
    Position: after combinedSleepNap and before dayLength, so related "total sleep" columns are grouped together. This propagates the column automatically to thead, buildAggregateRow, and buildRollingSection — those functions iterate COLUMNS without special-casing.

    CHANGE 3 — Augment per-day rows in the main render loop:
    Locate the main render path (line ~635) where aggregateMetrics(reversedDays) is called and rows are destructured as const { rows, avg, min, max } = metricsResult.

    After destructuring, add a loop that augments each rows[i] with a sleepDebt value. For row at index i (rows are oldest-first, matching the order of reversedDays):
    - Compute the slice of non-rejected days up to and including position i: take reversedDays.slice(0, i + 1), then filter out entries where rejected is true.
    - Call sleepDebtProxy(nonRejectedUpToI, 7, snap.settings.targetSleepMinutes).
    - Assign the result (may be null) to rows[i].sleepDebt.

    Then augment avg, min, max with sleepDebt aggregate values derived from the per-day sleepDebt values already set on rows:
    - Collect non-null sleepDebt values from rows (only non-rejected rows — check rows[i].rejected before including).
    - avg.sleepDebt = non-null values exist ? Math.round(sum / count) : null.
    - min.sleepDebt = non-null values exist ? { value: minimum, date: the date string from the rows entry with the minimum } : null. Use the 'date' field that aggregateMetrics already populates on each row, or fall back to the dateStr from reversedDays at the same index. Follow the existing { value, date } shape that buildAggregateRow expects (line ~270 check: if typeof value === 'object' && 'value' in value).
    - max.sleepDebt = same pattern as min but for the maximum.
    - On ties for min/max, the first occurrence wins (stable: iterate oldest-first, update only if strictly less/greater).

    CHANGE 4 — Augment rolling aggregate rows in buildRollingSection:
    Locate buildRollingSection (line ~403). After aggregateMetrics(slice) is called and its result is destructured, add the same per-row sleepDebt augmentation and aggregate augmentation pattern used in Change 3 above.

    In buildRollingSection, the non-rejected slice is already available as a local variable (the function receives nonRejectedDays and takes .slice(-nDays) of it). Use that slice directly as the dayRecords for sleepDebtProxy calls — all records in the rolling slice are already non-rejected. For row at index i within the rolling-section rows array, call sleepDebtProxy(rollingSlice.slice(0, i + 1), 7, snap.settings.targetSleepMinutes). Then compute avg/min/max for sleepDebt across the rolling section rows.

    Verify buildCell handles null sleepDebt values correctly: buildCell already renders '—' when value is null and isTime is false, isRatio is false — no change needed there.

    No new CSS classes are needed unless a debt-specific color is desired (Claude's discretion — if applying a color class for positive/negative values, add a minimal conditional class on the td after buildCell returns it; use textContent for value, classList for styling).
  </action>
  <acceptance_criteria>
    - COLUMNS has 19 entries (was 18); S.Debt appears between Comb and Day Len.
    - sleepDebtProxy is imported from ../lib/metrics.js in metrics-screen.js.
    - Per-day rows array has sleepDebt field on each element (null for cold-start rows, number for qualifying rows).
    - avg.sleepDebt, min.sleepDebt { value, date }, max.sleepDebt { value, date } are computed and set on the all-time aggregate object.
    - buildRollingSection similarly augments its local result with sleepDebt aggregate values.
    - All three aggregate sections (7-day rolling, 14-day rolling, all-time) render an S.Debt cell in their Avg/Min/Max rows.
    - Cold-start per-day rows (fewer than 7 qualifying prior records) show — in the S.Debt column.
    - No regression in existing Metrics screen rendering.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/unit/metrics.test.js 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, or any test in metrics.test.js fails (regression check — metrics-screen.js imports from metrics.js, so an import error surfaces here)</fails_when>
  </verify>
</task>

<task id="2" type="auto">
  <name>Update E2E test for new S.Debt column in metrics.spec.js</name>
  <read_first>
    - tests/e2e/metrics.spec.js (full file — understand column count assertions, column label checks, and seed data shape used in the existing tests; identify every assertion that counts columns or checks header labels)
    - js/ui/metrics-screen.js (confirm the final COLUMNS count after Task 1 so the assertion is grounded)
  </read_first>
  <action>
    Update tests/e2e/metrics.spec.js to account for the new S.Debt column.

    Locate every assertion that checks the number of table header cells or the list of column labels. COLUMNS now has 19 entries (plus TIF_COLUMNS), so any hardcoded count must be updated.

    Add or update an assertion that checks 'S.Debt' appears as a column header in the metrics table. Pattern: find the th with text 'S.Debt' using a text locator and assert it is visible.

    If the seed database in the existing tests does not have enough historical data to trigger a non-null sleepDebt (i.e., fewer than 7 qualifying records), assert that the S.Debt cells render '—' (em-dash) for all rows — this is the correct cold-start behavior per MET-14. Do not assert a specific numeric debt value in the E2E test; debt values depend on seed data and targetSleepMinutes, making the test brittle.

    The seed data in MET-02/MET-03 test includes targetSleepMinutes in its settings object — if it does not, add targetSleepMinutes: 600 to the seeded settings object so the setting is present (without it, the app would fall back to DEFAULT_SETTINGS which is 600 anyway, but explicit is safer).

    Run the full E2E spec after changes to confirm no regressions.
  </action>
  <acceptance_criteria>
    - metrics.spec.js column count assertions are updated to match the new total (19 COLUMNS + TIF_COLUMNS).
    - At least one assertion checks that an 'S.Debt' column header is visible in the metrics table.
    - The spec passes end-to-end against the running app.
    - No existing MET-01 through MET-06 assertions are broken.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/metrics.spec.js --reporter=line 2>&1 | tail -20</automated>
    <fails_when>non-zero exit code, or any test in metrics.spec.js reports "failed", or "0 passed" in the summary line</fails_when>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| metrics-screen.js → DOM | All cell values rendered via buildCell which uses textContent — debt values are computed numbers, never user-supplied strings |
| snap.settings.targetSleepMinutes → sleepDebtProxy | The value flows from validated settings store; out-of-range values are already rejected by settings-validate.js before reaching the store |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-06 | Tampering | COLUMNS extension breaking buildAggregateRow | low | mitigate | buildAggregateRow iterates COLUMNS by index (i=1 start) — adding an entry is safe; existing test suite catches any regression |
| T-18-07 | Information Disclosure | sleepDebt null shown as blank instead of cold-start indicator | low | mitigate | buildCell already renders null as '—' for non-time non-ratio columns; no special case needed |
| T-18-08 | Denial of Service | per-row sleepDebtProxy called O(n) times in render loop | low | accept | n is bounded by the number of days (typically < 1000); each call is O(windowDays) = O(7); total O(7n) is negligible for this data size |
</threat_model>

<verification>
Unit regression check:
  node --test tests/unit/metrics.test.js 2>&1 | tail -20

E2E spec:
  npx playwright test tests/e2e/metrics.spec.js --reporter=line 2>&1 | tail -20

Both must pass with no failures.
</verification>

<success_criteria>
- S.Debt column header appears in the Metrics table (MET-14)
- Per-day rows show '—' for cold-start and a signed minute integer for qualifying rows (MET-14)
- All-time, 7-day rolling, and 14-day rolling aggregate sections each show Sleep Debt avg/min/max (MET-14)
- E2E spec passes; unit metrics test has no regressions
- snap.settings.targetSleepMinutes feeds the debt computation throughout (D-01)
</success_criteria>

<output>
Create .planning/phases/18-sleep-debt-proxy/18-03-SUMMARY.md when done.
</output>
