---
phase: 18-sleep-debt-proxy
plan: 18-02
type: tdd
wave: 1
depends_on: []
files_modified:
  - js/lib/db-shape.js
  - js/lib/settings-validate.js
  - js/ui/settings-modal.js
  - index.html
  - tests/unit/settings-validate.test.js
autonomous: true
requirements:
  - MET-13

estimate:
  tokens: 55000
  raw_tokens: 28000
  tasks: 2
  confidence: high

must_haves:
  goal: "targetSleepMinutes is a validated setting wired end-to-end: schema, validation, and Settings modal UI with median hint"
  truths:
    - "targetSleepMinutes: 600 is present in DEFAULT_SETTINGS in js/lib/db-shape.js (D-01)"
    - "targetSleepMinutes is present in the JSDoc @type block of DEFAULT_SETTINGS (D-02)"
    - "RULES in settings-validate.js includes targetSleepMinutes: { type: 'integer', min: 1, max: 1440 } (D-02)"
    - "validateSettings in mode:'save' rejects targetSleepMinutes values outside 1-1440 (D-02)"
    - "validateSettings in mode:'load' resets out-of-range targetSleepMinutes to 600 silently (D-02)"
    - "Settings modal HTML has a number input named targetSleepMinutes with min=1, max=1440, step=5 (D-02)"
    - "Settings modal JS populates the input with snap.settings.targetSleepMinutes on open (D-02)"
    - "Settings modal JS reads targetSleepMinutes from the form and includes it in the saved settings object (D-02)"
    - "Settings modal shows an inline hint 'Your median: Xh Ym' derived from combinedSleepNap median of snap.days (D-03)"
    - "Median hint is rendered via textContent only — no innerHTML (XSS guard)"
    - statement: "targetSleepMinutes must not be stored as float — must remain integer minutes"
      status: unverified
      flagged: true
  prohibitions:
    - statement: "targetSleepMinutes must not be stored as float — must remain integer minutes"
      status: unverified
      flagged: true
  artifacts:
    - "js/lib/db-shape.js — targetSleepMinutes: 600 in DEFAULT_SETTINGS"
    - "js/lib/settings-validate.js — targetSleepMinutes rule in RULES"
    - "js/ui/settings-modal.js — input population on open, raw-object read on save, median hint"
    - "index.html — number input + small hint element inside existing numeric-settings fieldset"
    - "tests/unit/settings-validate.test.js — new test cases for targetSleepMinutes"
  key_links:
    - "DEFAULT_SETTINGS (db-shape.js) → RULES (settings-validate.js) → populateForm / onClose (settings-modal.js) → HTML input (index.html)"
    - "combinedSleepNap imported into settings-modal.js to compute median hint (D-03)"
    - "formatDuration imported into settings-modal.js to render hint as 'Xh Ym' (D-03)"
---

<objective>
Apply the settings field trio pattern to add targetSleepMinutes (default 600, valid 1-1440) to the schema, validator, and Settings modal UI, including the all-time combinedSleepNap median hint.

Purpose: Provides the per-day sleep target that sleepDebtProxy() (Plan 18-01) and the Metrics screen render (Plan 18-03) will read from snap.settings.targetSleepMinutes.
Output: A validated setting wired from DEFAULT_SETTINGS through the form to the settings store, with a median hint rendered on modal open.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/my-code/vibe-coding/night-watch/.planning/PROJECT.md
@C:/my-code/vibe-coding/night-watch/.planning/ROADMAP.md
@C:/my-code/vibe-coding/night-watch/.planning/phases/18-sleep-debt-proxy/18-CONTEXT.md
@C:/my-code/vibe-coding/night-watch/.planning/phases/18-sleep-debt-proxy/18-PATTERNS.md
</context>

<tasks>

<task id="1" type="tdd">
  <name>TDD — targetSleepMinutes validation rule in settings-validate.js</name>
  <read_first>
    - js/lib/db-shape.js (full file — understand DEFAULT_SETTINGS shape, JSDoc typedef block at lines 22-41, existing fields including firstDayOfWeek at line 67; this is where targetSleepMinutes: 600 will be added)
    - js/lib/settings-validate.js (full file — understand RULES object, integer rule pattern for windowDays and trimPct at lines 52-58, checkField dispatcher; targetSleepMinutes rule will be added after firstDayOfWeek)
    - tests/unit/settings-validate.test.js (full file — understand describe groups, the valid() helper, and how the test count assertion for RULES fields works so it can be updated)
    - .planning/phases/18-sleep-debt-proxy/18-CONTEXT.md (decisions D-01, D-02)
    - .planning/phases/18-sleep-debt-proxy/18-PATTERNS.md (settings-validate.js section showing the exact RULES entry to add)
  </read_first>
  <action>
    RED — Write failing tests in tests/unit/settings-validate.test.js:

    Update the 'has entries for all N field names' assertion count: change the expected count from 22 to 23 (adding targetSleepMinutes). Update the expected field name list in that test to include 'targetSleepMinutes'.

    Add a new describe block for targetSleepMinutes validation. The block must cover:
    1. mode:'save' accepts 1 (minimum valid value).
    2. mode:'save' accepts 1440 (maximum valid value).
    3. mode:'save' accepts 600 (the default).
    4. mode:'save' rejects 0 — below minimum — errors array is non-empty.
    5. mode:'save' rejects 1441 — above maximum — errors array is non-empty.
    6. mode:'save' rejects a non-integer such as 600.5 — errors array is non-empty.
    7. mode:'load' resets an out-of-range value (e.g., 0) to DEFAULT_SETTINGS.targetSleepMinutes (600) silently.

    Run the tests — the updated field count assertion and all new targetSleepMinutes assertions MUST fail.

    Commit: test(18-02): add failing tests for targetSleepMinutes validation

    GREEN — Implement in two files:

    File 1 — js/lib/db-shape.js:
    - Extend the JSDoc @type typedef block with "targetSleepMinutes: number," in alphabetical order among the existing numeric fields (or after firstDayOfWeek, which is the most recent addition).
    - Append targetSleepMinutes: 600 to DEFAULT_SETTINGS before the closing Object.freeze parenthesis. Add an inline comment: "// MET-13 / D-01: per-day sleep target in minutes (default 10h)". Pattern mirrors firstDayOfWeek at line 67.

    File 2 — js/lib/settings-validate.js:
    - Append to RULES: targetSleepMinutes: { type: 'integer', min: 1, max: 1440 } with an inline comment: "// MET-13 / D-02: positive integer, 1 min to 24h". Pattern mirrors windowDays (line 52) and trimPct (line 58). No new case in checkField needed — type:'integer' already handled.

    Run the tests — all assertions in the new targetSleepMinutes describe block AND the updated field count assertion MUST pass. All prior describe blocks must also pass.

    Commit: feat(18-02): add targetSleepMinutes to DEFAULT_SETTINGS and RULES
  </action>
  <acceptance_criteria>
    - DEFAULT_SETTINGS.targetSleepMinutes === 600 (number, integer).
    - RULES.targetSleepMinutes is { type: 'integer', min: 1, max: 1440 }.
    - validateSettings in mode:'save' accepts 1, 600, 1440 without errors.
    - validateSettings in mode:'save' rejects 0, 1441, and non-integers with non-empty errors array.
    - validateSettings in mode:'load' resets out-of-range values to 600.
    - RULES field count test passes with the updated count.
    - No regression in existing settings-validate test suite.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/unit/settings-validate.test.js 2>&1 | tail -20</automated>
    <fails_when>non-zero exit code, or "0 passed" in the summary line, or any test in the targetSleepMinutes describe block reports "failed"</fails_when>
  </verify>
</task>

<task id="2" type="auto">
  <name>Wire targetSleepMinutes input + median hint in Settings modal</name>
  <read_first>
    - js/ui/settings-modal.js (full file — understand populateForm function, the onClose raw-object, the guard pattern for newer fields, existing numeric input population pattern; look for where firstDayOfWeek is populated to insert targetSleepMinutes after it)
    - index.html (search for the numeric-settings fieldset, approximately lines 295-310 based on PATTERNS.md; identify the exact insertion point for the new label/input/small elements)
    - js/lib/metrics.js (check if combinedSleepNap is exported — needed for median hint computation)
    - js/lib/time.js (check if formatDuration is exported — needed to render "Xh Ym" hint text)
    - .planning/phases/18-sleep-debt-proxy/18-CONTEXT.md (decision D-03: inline hint "Your median: Xh Ym")
    - .planning/phases/18-sleep-debt-proxy/18-PATTERNS.md (settings-modal.js section showing populateForm addition, onClose raw-object addition, and median hint computation pattern)
  </read_first>
  <action>
    Modify three files — settings-modal.js, index.html, and optionally add imports.

    File 1 — js/ui/settings-modal.js:

    Imports: if combinedSleepNap is not already imported, add it to the import from '../lib/metrics.js'. If formatDuration is not already imported, add it to the import from '../lib/time.js'. Use the same guard pattern as other newer fields.

    In populateForm (or the equivalent function that populates form inputs on modal open):
    - After the firstDayOfWeek population line, add a guarded line that reads form.elements.namedItem('targetSleepMinutes') and sets its value to String(s.targetSleepMinutes ?? 600). Pattern: identical to the firstDayOfWeek line above it.
    - After setting the input value, compute the median hint:
      a. Get the hint element by ID 'targetSleepMedianHint' using getElementById.
      b. If the element exists, map snap.days (or the days array from the snap/deps object available at this scope) through combinedSleepNap, filter out null results, sort ascending, and pick the middle element (Math.floor(vals.length / 2)).
      c. If at least one valid value exists, set hintEl.textContent to 'Your median: ' + formatDuration(median). Otherwise set hintEl.textContent to ''.
      d. All text assignment via .textContent only — never .innerHTML (XSS guard T-18-03).
      e. If snap.days is not directly available in populateForm's scope, pass it through the existing function parameter chain (check the function signature and what the caller passes).

    In the onClose raw-object (the object built when the save button is clicked):
    - After the firstDayOfWeek line, add: targetSleepMinutes: Number(data.get('targetSleepMinutes') ?? 600). This converts the form string value to a number. Pattern mirrors the other integer fields in the raw object.

    File 2 — index.html:

    Inside the existing numeric-settings fieldset (near lines 295-310), add a label element containing:
    - Text node "Sleep target (minutes)"
    - input type="number" with id="targetSleepMinutes", name="targetSleepMinutes", min="1", max="1440", step="5"
    - small element with id="targetSleepMedianHint" and class="hint" (empty initially; JS populates it)

    Insert the label after the existing fieldset entries for numeric settings, in a position consistent with how other numeric settings are laid out in the fieldset. No new fieldset — inline with existing entries per D-02.

    No test file for this task — manual verification via the E2E spec in Plan 18-03 will confirm the input renders and the hint appears.
  </action>
  <acceptance_criteria>
    - index.html contains an input with name="targetSleepMinutes", type="number", min="1", max="1440", step="5" inside the existing numeric settings fieldset.
    - index.html contains a small element with id="targetSleepMedianHint" adjacent to the input.
    - settings-modal.js populates the targetSleepMinutes input with the current setting value on open.
    - settings-modal.js sets targetSleepMedianHint.textContent to "Your median: Xh Ym" (or empty string when no data), using textContent only.
    - settings-modal.js includes targetSleepMinutes: Number(...) in the raw settings object on save.
    - combinedSleepNap and formatDuration are imported in settings-modal.js (or already present).
    - No innerHTML assignment for the hint (XSS guard).
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/unit/settings-validate.test.js 2>&1 | tail -20</automated>
    <fails_when>non-zero exit code (regression in validation suite); this task has no dedicated automated test — regression in the validation unit suite indicates an import or module error was introduced</fails_when>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → Settings modal form | targetSleepMinutes input accepts free text from the user; validated by settings-validate.js before persistence |
| settings-modal.js → DOM | median hint text is derived from computed data and rendered via textContent only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-03 | Tampering | targetSleepMinutes form input | low | mitigate | settings-validate.js enforces integer type and 1–1440 bounds in mode:'save' before any persistence |
| T-18-04 | Information Disclosure | targetSleepMedianHint DOM element | low | mitigate | median hint rendered via .textContent only; formatDuration returns plain string with no HTML; XSS guard maintained |
| T-18-05 | Tampering | targetSleepMinutes stored as float | low | mitigate | onClose uses Number() which may produce float; if form input has step=5 and min=1 integers only, validate as integer in mode:'save' per RULES type:'integer' |
</threat_model>

<verification>
Unit suite passes with no regression:
  node --test tests/unit/settings-validate.test.js 2>&1 | tail -20

Manual smoke check (deferred to Plan 18-03 E2E): open the Settings modal and verify the Sleep target input appears with a median hint populated from stored data.
</verification>

<success_criteria>
- targetSleepMinutes: 600 in DEFAULT_SETTINGS (MET-13, D-01)
- RULES entry for targetSleepMinutes with type:'integer', min:1, max:1440 (D-02)
- validateSettings mode:'save' rejects out-of-range values; mode:'load' resets them to 600
- Settings modal HTML input present with correct attributes (D-02)
- populateForm and onClose wired for the new field (D-02)
- Median hint rendered from combinedSleepNap data via textContent (D-03)
- Two atomic commits from Task 1: failing test commit, then passing implementation commit
</success_criteria>

<output>
Create .planning/phases/18-sleep-debt-proxy/18-02-SUMMARY.md when done.
</output>
