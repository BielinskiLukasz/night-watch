---
phase: 18-sleep-debt-proxy
plan: 18-01
type: tdd
wave: 1
depends_on: []
files_modified:
  - js/lib/metrics.js
  - tests/unit/metrics.test.js
autonomous: true
requirements:
  - MET-13

estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 1
  confidence: high

must_haves:
  goal: "sleepDebtProxy() is a correct, tested, exported pure function in js/lib/metrics.js"
  truths:
    - "sleepDebtProxy(dayRecords, windowDays, targetSleepMinutes) is exported from js/lib/metrics.js (D-08)"
    - "returns null when fewer than windowDays non-rejected records with non-null combinedSleepNap exist (D-07)"
    - "returns the signed integer sum of (targetSleepMinutes - combinedSleepNap(day)) over the last windowDays qualifying records (D-04, D-06)"
    - "negative values are preserved — surplus sleep reduces the rolling sum; no clamping at zero (D-06)"
    - "days with null combinedSleepNap are excluded and do not count toward windowDays (D-05)"
    - "the input dayRecords array is not mutated (pure function contract, D-08)"
    - statement: "MET-13 unclassified edge: review manually — no explicit acceptance criterion could be derived"
      flagged: true
    - statement: "MET-14 boundary: sleepDebtProxy behavior at exactly windowDays qualifying records returns a value, not null"
      flagged: true
    - statement: "MET-14 adjacency: when combinedSleepNap equals targetSleepMinutes exactly, debt for that day is 0"
      flagged: true
    - statement: "MET-14 empty: sleepDebtProxy([]) returns null — empty input has fewer than windowDays qualifying records"
      flagged: true
    - statement: "MET-14 precision: sleep debt values are integer minutes — all inputs are integer minutes, no float rounding needed"
      flagged: true
  prohibitions:
    - statement: "sleepDebtProxy must not modify its input dayRecords array"
      status: unverified
      flagged: true
  artifacts:
    - "js/lib/metrics.js — exports sleepDebtProxy"
    - "tests/unit/metrics.test.js — describe block for sleepDebtProxy covering all edge cases"
  key_links:
    - "combinedSleepNap(day) (metrics.js:87) — the per-day actual sleep value used in debt formula"
    - "sleepDebtProxy called by metrics-screen.js Plan 18-03 to populate per-day and aggregate debt values"
---

<objective>
Implement sleepDebtProxy(dayRecords, windowDays, targetSleepMinutes) as a TDD exercise, adding it to js/lib/metrics.js and unit-testing it in tests/unit/metrics.test.js.

Purpose: Establishes the core pure function that Plan 18-03 will call to populate each per-day debt cell and the rolling aggregate rows in the Metrics screen.
Output: A tested, exported function and a new describe block in the existing unit test file.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/my-code/vibe-coding/night-watch/.planning/PROJECT.md
@C:/my-code/vibe-coding/night-watch/.planning/ROADMAP.md
@C:/my-code/vibe-coding/night-watch/.planning/phases/18-sleep-debt-proxy/18-CONTEXT.md
@C:/my-code/vibe-coding/night-watch/.planning/phases/18-sleep-debt-proxy/18-PATTERNS.md
</context>

<tasks>

<task id="1" type="tdd">
  <name>TDD — sleepDebtProxy() pure function in js/lib/metrics.js</name>
  <read_first>
    - js/lib/metrics.js (full file — understand module header, existing exports, combinedSleepNap at line ~87, aggregateMetrics at line ~230, dayOfWeekAverages at line ~417; these three are the calling-convention patterns to follow)
    - tests/unit/metrics.test.js (full file — understand import list, makeDay helper, existing describe groups; add the new describe block after the last existing one)
    - .planning/phases/18-sleep-debt-proxy/18-CONTEXT.md (decisions D-04 through D-08)
    - .planning/phases/18-sleep-debt-proxy/18-PATTERNS.md (function signature pattern and null-exclusion rolling slice pattern for sleepDebtProxy)
  </read_first>
  <action>
    RED — Write the failing tests first in tests/unit/metrics.test.js:

    Add "sleepDebtProxy" to the import list at the top of the test file (alongside the existing exports). Then add a new describe('sleepDebtProxy', ...) block after the existing describe groups. The block must cover:

    1. Returns null when given an empty array (MET-14 empty edge, D-07).
    2. Returns null when given fewer than windowDays records with non-null combinedSleepNap — e.g., 3 valid days with windowDays=7 (D-07 cold-start).
    3. Returns a numeric value (not null) when given exactly windowDays records with non-null combinedSleepNap — at the boundary (MET-14 boundary).
    4. Returns the correct signed sum when all days have the same sleep as target — result is 0 (MET-14 adjacency: each day contributes 0 deficit).
    5. Returns a positive value when all days sleep less than target — deficit scenario (D-06).
    6. Returns a negative value when all days sleep more than target — surplus scenario, no clamping (D-06).
    7. Excludes days with null combinedSleepNap from the rolling window and from the count toward windowDays (D-05): given 6 valid days + 1 null day = 7 total records, windowDays=7, result must be null because only 6 qualify.
    8. Takes only the last windowDays qualifying records when the array is larger (rolling slice): given 10 valid days, windowDays=7, only the last 7 contribute to the sum.
    9. Does not mutate the input array (prohibitions check: assert dayRecords.length unchanged and reference equality of elements after the call).

    Use makeDay(wake, bedtime, napStart, napEnd) for synthetic records. For cases involving null combinedSleepNap, pass makeDay(null, null, null, null) or omit bedtime/wake so the helper returns null.

    Run the tests — all assertions in the new describe block MUST fail (import of sleepDebtProxy will cause an error or named export will be undefined).

    Commit: test(18-01): add failing tests for sleepDebtProxy

    GREEN — Implement sleepDebtProxy in js/lib/metrics.js:

    Add the function after the existing exports (after dayOfWeekAverages). Export it with the `export` keyword. The function receives three parameters: dayRecords (array), windowDays (number), targetSleepMinutes (number).

    Implementation contract (per D-04, D-05, D-06, D-07, D-08, PATTERNS.md):
    - combinedSleepNap is already imported/available in the module — call it on each day.
    - Collect qualifying records: filter dayRecords keeping only days where combinedSleepNap(day) is not null, then take the last windowDays entries via .slice(-windowDays).
    - If the filtered-then-sliced array has fewer than windowDays elements, return null.
    - Otherwise return the sum of (targetSleepMinutes minus combinedSleepNap(day)) across those qualifying records. Use reduce. Do not round — inputs are integer minutes so the result is already an integer.
    - Do not mutate the input: .filter and .slice produce new arrays; the original dayRecords is untouched.

    Add a JSDoc block above the function following the pattern of dayOfWeekAverages (line ~417): describe the caller pre-filter contract (stage + rejected exclusion), the null-exclusion rule (D-05), the sign convention (D-06), and the null return condition (D-07). Reference parameters with @param tags and @returns.

    Run the tests — all assertions in the new describe block MUST pass.

    Commit: feat(18-01): implement sleepDebtProxy in metrics.js

    REFACTOR — Review for clarity only if needed:
    - Confirm the JSDoc is accurate.
    - Confirm variable names inside the function are self-documenting (validDays, windowDays, targetSleepMinutes).
    - No logic changes needed unless a test is still failing.

    Commit if changed: refactor(18-01): clean up sleepDebtProxy
  </action>
  <acceptance_criteria>
    - sleepDebtProxy is exported from js/lib/metrics.js and importable by name.
    - All test cases in the new describe('sleepDebtProxy') block pass.
    - Existing tests in metrics.test.js continue to pass (no regression).
    - The function does not mutate its input dayRecords array.
    - Returns null for empty input and for fewer-than-windowDays qualifying records.
    - Returns 0 when actual sleep equals target exactly for all windowDays records.
    - Returns positive values for deficit, negative values for surplus — no clamping.
    - Null combinedSleepNap days are excluded from window count and sum.
    - Only the last windowDays qualifying records contribute (rolling slice, not cumulative).
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/unit/metrics.test.js 2>&1 | tail -20</automated>
    <fails_when>non-zero exit code, or "0 passed" in the summary line, or any test in the sleepDebtProxy describe block reports "failed"</fails_when>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → sleepDebtProxy | caller is responsible for pre-filtering rejected days and stage-scoped days before passing dayRecords |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | sleepDebtProxy input dayRecords | low | mitigate | pure function uses .filter().slice() — never mutates input; mutation test in unit suite |
| T-18-02 | Information Disclosure | sleepDebtProxy returning non-null on cold-start | low | mitigate | explicit validDays.length < windowDays → null guard per D-07 |
</threat_model>

<verification>
Run full unit suite to confirm no regression: node --test tests/unit/metrics.test.js 2>&1 | tail -20

All describe groups in the file must show "passed"; the new sleepDebtProxy group must show all cases green.
</verification>

<success_criteria>
- sleepDebtProxy exported from js/lib/metrics.js with correct three-parameter signature (MET-13, D-08)
- All 9 unit test assertions in the new describe block pass
- No existing test regressions
- Two atomic commits: failing test commit, then passing implementation commit
</success_criteria>

<output>
Create .planning/phases/18-sleep-debt-proxy/18-01-SUMMARY.md when done.
</output>
