---
phase: NW-15
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - js/ui/metrics-screen.js
  - tests/unit/settings-validate.test.js
autonomous: true
requirements:
  - FIX-03
  - FIX-04
  - FIX-05

estimate:
  tokens: 22000
  raw_tokens: 14000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - metrics-screen.js render() does not invoke tifForecast a second time; TIF
      event-time strings come from data already computed in the same render cycle (FIX-03)
    - The computeTifTrimmedStats comment accurately describes that metric rows may contain
      bare HH:MM strings, so the raw.length > 5 guard is not dead code (FIX-04)
    - The settings-validate.test.js test description for tifRollingDays upper-bound reads
      'rejects 91 (above max=90)' and the full suite passes (FIX-05)
  artifacts:
    - js/ui/metrics-screen.js (second tifForecast call block removed)
    - js/ui/metrics-screen.js (computeTifTrimmedStats comment updated)
    - tests/unit/settings-validate.test.js (stale '31' description corrected to '91')
  key_links:
    - tifForecast import in metrics-screen.js (may be removable after FIX-03 if unused)
    - computeTifTrimmedStats raw.length > 5 guard must not be removed after comment fix
---

<objective>
Remove a redundant tifForecast invocation from metrics-screen.js render(), correct a
misleading comment in computeTifTrimmedStats, and fix a stale test description in
settings-validate.test.js.

Purpose: FIX-03 eliminates double TIF computation on every metrics render cycle.
FIX-04 ensures maintainers understand the raw.length > 5 guard is live code (not dead
code) because metric rows may carry bare HH:MM strings, not only ISO strings. FIX-05
corrects a test description that contradicts the value being tested, preventing future
confusion about the tifRollingDays upper bound.

Output: Patched js/ui/metrics-screen.js with the redundant call removed and the comment
corrected; patched tests/unit/settings-validate.test.js with the accurate description.
</objective>

<execution_context>
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/night-watch/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove redundant tifForecast call from render() (FIX-03)</name>
  <files>js/ui/metrics-screen.js</files>
  <read_first>
    - js/ui/metrics-screen.js — read the full render() function (starting at 'const render = () =>').
      Identify the block that calls tifForecast a second time (after tifTrimmedStats is computed)
      to override the 4 event-time columns with historic band values. Note which lines it spans.
    - js/ui/metrics-screen.js — read the import statement at the top to see whether tifForecast
      appears there alongside trimmedMinMax.
    - js/ui/metrics-screen.js — read computeTifTrimmedStats to confirm it already handles time
      columns correctly (the raw.length > 5 guard converts both bare HH:MM and ISO strings
      before calling timeToMinutes). This confirms the override block is redundant.
  </read_first>
  <action>
    Delete the entire conditional block inside render() that:
      (a) declares a currentForecast variable by calling tifForecast on reversedDays, and
      (b) iterates over HISTORIC_LABELS to override tifTrimmedStats.min/median/max for the
          four event-time column keys (wake, napStart, napEnd, bedtime).

    The block begins with a comment about overriding the 4 event-time columns and ends with
    the closing brace of its if-statement. Delete the comment, the if-statement, and all
    code inside it. Do not touch any other part of render().

    After deleting the block, check whether tifForecast is referenced anywhere else in
    metrics-screen.js. If tifForecast is no longer referenced, remove it from the import
    statement at the top of the file (the import that also pulls in trimmedMinMax). Keep
    trimmedMinMax in the import. Do not remove any other import. (FIX-03)
  </action>
  <verify>
    <automated>node --test tests/unit/ 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - The render() function in js/ui/metrics-screen.js contains exactly zero calls to
      tifForecast. Confirmed by: grep -c 'tifForecast' js/ui/metrics-screen.js returns 0
      or 1 (only 1 if still in the import line and only if it cannot be removed, but the
      preferred outcome is 0 since the import should be cleaned up too).
    - trimmedMinMax remains imported and used in metrics-screen.js.
    - npm run test:unit exits 0 (no regressions).
    - npm run test:e2e is not required for this task but must not be broken by the removal
      (the Metrics screen still renders correctly since tifTrimmedStats values are unchanged).
  </acceptance_criteria>
  <done>
    render() calls tifForecast zero times. tifForecast import removed if unused. Unit
    tests green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix misleading comment in computeTifTrimmedStats (FIX-04)</name>
  <files>js/ui/metrics-screen.js</files>
  <read_first>
    - js/ui/metrics-screen.js — read computeTifTrimmedStats in full (the function that
      builds minMap, medianMap, maxMap). Locate the single-line comment immediately before
      the `const mins = window.map(r => {` expression inside the isTime branch. Read the
      guard expression `raw.length > 5 ? raw.slice(11) : raw` to understand what both
      branches handle.
  </read_first>
  <action>
    Replace the single-line comment that precedes the `const mins = window.map(...)` call
    inside the `if (col.isTime)` branch of computeTifTrimmedStats. The replacement comment
    must convey all of the following:

    - Metric rows may contain bare HH:MM strings OR full ISO timestamp strings.
    - The raw.length > 5 guard extracts the HH:MM slice from ISO strings and passes bare
      strings through unchanged.
    - The guard is live code, not dead code: bare HH:MM values do appear in practice.

    Keep the comment to one or two lines. Do not change any code in the function — only
    the comment text. (FIX-04)
  </action>
  <verify>
    <automated>node --test tests/unit/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The comment immediately before `const mins = window.map(r => {` no longer states
      that rows store only full ISO strings.
    - The updated comment mentions that bare HH:MM strings may appear and explains the
      raw.length > 5 guard handles both forms.
    - The raw.length > 5 conditional expression is unchanged in the source.
    - npm run test:unit exits 0.
  </acceptance_criteria>
  <done>
    The comment in computeTifTrimmedStats accurately describes both bare-string and ISO-string
    inputs, making the raw.length > 5 guard's purpose clear to future readers.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix stale tifRollingDays test description (FIX-05)</name>
  <files>tests/unit/settings-validate.test.js</files>
  <read_first>
    - tests/unit/settings-validate.test.js — read lines 715–735 to see the three
      tifRollingDays validation tests (below-min, above-max, non-integer). Confirm the
      it() description at line 724 and the tifRollingDays value used in its body.
  </read_first>
  <action>
    On the line that contains the it() description for the tifRollingDays upper-bound test,
    change the description string so the value shown in the description matches the value
    passed to validateSettings in that test's body. The test body uses tifRollingDays: 91.
    The description must read 'rejects 91 (above max=90)' in the mode:'save' portion.
    Do not change any other test, any assertion, or any other line. One string change only.
    (FIX-05)
  </action>
  <verify>
    <automated>node --test tests/unit/settings-validate.test.js 2>&1 | grep -E '(pass|fail|rejects 91)'</automated>
  </verify>
  <acceptance_criteria>
    - The it() description at the tifRollingDays upper-bound test reads exactly:
      "rejects 91 (above max=90) in mode:'save'"
    - node --test tests/unit/settings-validate.test.js exits 0.
    - npm run test:unit exits 0 (no regressions elsewhere).
  </acceptance_criteria>
  <done>
    The tifRollingDays upper-bound test description matches the tested value (91). Full
    unit suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| metrics-screen render() | No new external data introduced; this plan only removes code and fixes a comment/string |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-15-03 | Tampering | tifForecast removal side-effects | low | accept | computeTifTrimmedStats already computes the same columns; no data path is widened or narrowed |
| T-15-04 | Information Disclosure | stale test description | low | accept | Test description fix (FIX-05) has no runtime security impact; affects only test readability |
</threat_model>

<verification>
node --test tests/unit/settings-validate.test.js
node --test tests/unit/
npm run test:unit
</verification>

<success_criteria>
1. grep -c 'tifForecast' js/ui/metrics-screen.js returns 0 (redundant call and import removed,
   FIX-03) or 1 if only the import line remains and tifForecast is used elsewhere in the module
2. The comment in computeTifTrimmedStats before the isTime branch mins-array mentions bare HH:MM
   strings and the raw.length > 5 guard's purpose (FIX-04)
3. The tifRollingDays upper-bound test description reads 'rejects 91 (above max=90)' (FIX-05)
4. npm run test:unit exits 0
</success_criteria>

<output>
Create .planning/phases/NW-15-tif-engine-bug-fixes/15-02-SUMMARY.md when done.
</output>
