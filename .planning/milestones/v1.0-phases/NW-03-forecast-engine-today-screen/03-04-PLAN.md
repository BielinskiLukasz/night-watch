---
phase: 03-forecast-engine-today-screen
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - js/ui/today-screen.js
  - js/app.js
  - index.html
  - style.css
  - tests/e2e/forecast.spec.js
autonomous: true
requirements:
  - PRED-01
  - PRED-02
  - PRED-03
  - PRED-04
  - PRED-05
  - PRED-06
  - UI-01
  - UI-02

must_haves:
  truths:
    - "Today screen renders four prediction cards (wake, bedtime, nap start, nap end) with central time and min/max band"
    - "Next-event card appears above the four predictions and is visually prominent"
    - "When cold-start gate is active, a message replaces prediction cards"
    - "When band width exceeds maxDelta, the prediction card shows a probability table instead of min/max"
    - "User logs a new event via quick-log button and predictions update in real-time without reload"
    - "Missed predictions (central time in past) are grayed out with a 'Missed by Xmin' label"
  artifacts:
    - path: "js/ui/today-screen.js"
      provides: "Today screen component with forecast card rendering"
      exports: ["mountTodayScreen"]
    - path: "index.html"
      provides: "DOM structure for forecast cards, next-event hero, and day list"
      contains: "<div id=\"forecast-cards\">" 
    - path: "style.css"
      provides: "Visual styling for prediction cards, probability tables, and hero next-event card"
      contains: ".prediction-card"
    - path: "tests/e2e/forecast.spec.js"
      provides: "End-to-end tests for forecast UI rendering and reactive updates"
      contains: "test('land on today"

  key_links:
    - from: "js/ui/today-screen.js"
      to: "js/lib/forecast.js"
      via: "import forecast, selectNextEvent"
      pattern: "from.*forecast"
    - from: "js/ui/today-screen.js"
      to: "js/store/event-log.js"
      via: "subscribe to changes"
      pattern: "eventLog.subscribe"
    - from: "tests/e2e/forecast.spec.js"
      to: "index.html"
      via: "navigate and interact"
      pattern: "page.goto|getByRole"
---

<objective>
Render the forecast algorithm output as interactive UI cards and verify end-to-end user flow.

**Purpose:** Waves 1–2 delivered the pure algorithm and integration tests. Wave 3 brings predictions to the screen. Today screen subscribes to eventLog and settings changes, calls forecast(), and renders four prediction cards plus a hero "next event" card. When band width exceeds maxDelta, a card switches to probability-table layout. When cold-start gate is active, a message replaces cards. E2E tests verify the full user flow: land on Today, log events, see predictions update live.

**Output:** Fully functional Today screen with all four prediction cards, next-event hero, and cold-start gating. E2E tests validate user-visible behavior.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/projects/sleep-tracker/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/projects/sleep-tracker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/NW-03-forecast-engine-today-screen/03-RESEARCH.md
@.planning/phases/NW-03-forecast-engine-today-screen/03-CONTEXT.md
@.planning/phases/NW-01-log-persist/01-05-SUMMARY.md
@.planning/phases/NW-02-configuration-settings/02-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend index.html with forecast card DOM structure</name>
  <files>index.html</files>
  <action>
    Add a new section to index.html (after the header strip) with:
    - Container `<div id="next-event-card"></div>` for the hero card (will be populated by today-screen.js)
    - Container `<section id="forecast-cards" class="forecast-grid"></section>` for the four prediction cards (grid layout, 2x2 or 1x4 depending on screen size, to be styled in CSS)
    - Placeholder for cold-start message: `<div id="cold-start-message"></div>` (shown/hidden by JS)
    - Static skeleton: no content in these divs yet (will be populated by JS on mount)
    
    Keep existing structure intact (header, quick-log buttons, today's event list). Position forecast section above the day-list per D3-07 (hero card layout).
    
    Reference Phase 2 (02-04-SUMMARY.md) for the header structure already in place. Forecast section goes between quick-log buttons and the today-event list.
  </action>
  <verify>
    <automated>grep -c "id=\"forecast-cards\"" index.html</automated>
  </verify>
  <done>
    index.html updated with forecast card containers. DOM structure in place but empty (JS will populate). Page still loads and renders quick-log buttons and today-list.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CSS styling for prediction cards and probability-band tables</name>
  <files>style.css</files>
  <action>
    Add CSS rules for:
    - `.forecast-grid` container — flexbox or CSS grid layout, responsive (2x2 on desktop, 1x4 stacked on mobile)
    - `.prediction-card` base class — border, padding, gap spacing, background color (subtle dark theme per Phase 1 aesthetic)
    - `.prediction-card.probability-band` variant — layout for probability table (ul/li or grid for time + percentage rows)
    - `.prediction-card.missed` variant — lower opacity, different text color (grayed out per D3-11), small "Missed by Xmin" label styling
    - `.next-event-hero` — larger, more prominent than the four cards, positioned above them, distinct background/border
    - `.cold-start-message` — centered text, encouraging tone, clear font weight
    - `.event-label` — event type label (wake, bedtime, etc.) at the top of each card
    - `.time-central` — large, prominent central time
    - `.time-band` — min/max display (smaller than central)
    - `.prob-list li` — individual P(event by T) = X% rows
    
    Use calm, minimal dark theme matching Phase 1/2 (references: mindful-breathing accent colors, muted palette, high contrast for readability). Ensure 5-minute precision is visually consistent with Phase 1 time formatting.
  </action>
  <verify>
    <automated>grep -c "\.prediction-card" style.css</automated>
  </verify>
  <done>
    CSS updated with prediction-card styling, probability-band variant, missed variant, next-event hero, and cold-start message. All selector rules present. Page renders with styled forecast containers.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement today-screen.js with forecast rendering and reactive subscription</name>
  <files>js/ui/today-screen.js</files>
  <action>
    Refactor `js/ui/today-screen.js` (already contains quick-log buttons and day list from Phase 1) to add:
    
    - Import forecast and selectNextEvent from `js/lib/forecast.js`
    - Create internal function `renderNextEventCard(prediction)` — if prediction is null, return empty or "no prediction" message; otherwise render:
      ```html
      <div class="next-event-hero">
        <p class="event-type">{prediction.type}</p>
        <p class="time-central">{formatted central time}</p>
        <p class="time-band">{formatted min} – {formatted max}</p>
        {if missed: <span class="missed-label">Missed by {delta}min</span>}
      </div>
      ```
    
    - Create function `renderPredictionCard(prediction, eventType)` — renders either min/max or probability-band view:
      ```html
      <div class="prediction-card {is-missed ? 'missed' : ''}">
        <p class="event-label">{eventType}</p>
        {if prediction.probabilityBand:
          <ul class="prob-list">
            {prediction.probabilityBand.map(({time, prob}) => 
              <li>P({eventType} by {time}) = {prob}%</li>
            )}
          </ul>
        else:
          <p class="time-central">{central}</p>
          <p class="time-band">{min} – {max}</p>
        }
        {if isMissed: <span class="missed-label">Missed by {minutesDelta}min</span>}
      </div>
      ```
    
    - Create function `renderColdStartMessage(minDaysRemaining)` — returns centered message:
      ```html
      <div id="cold-start-message" class="cold-start-message">
        <p>Not enough data yet.</p>
        <p>Log {minDaysRemaining} more day{minDaysRemaining === 1 ? '' : 's'} to see predictions.</p>
      </div>
      ```
    
    - Create main function `renderForecastSection(predictions, settings, dayRecords)`:
      - If predictions.isColdStart is true: show cold-start message, hide forecast cards
      - Else: hide cold-start message, render four cards (wake, bedtime, napStart, napEnd)
      - Get next-event via selectNextEvent(predictions, dayRecords)
      - Render next-event hero card above the four cards
    
    - Modify the module's mount/render function to:
      - Subscribe to eventLog changes (per Phase 2 pattern, eventLog.subscribe(renderFunction))
      - Subscribe to settings changes (per Phase 2 pattern, settings.subscribe(renderFunction))
      - In the render callback:
        - Get days via eventLog.daysBySubjectiveNight(settings.get().cutoverHour, settings.get().windowDays)
        - Call forecast(days, settings.get())
        - Call renderNextEventCard() and renderForecastSection()
      - Initial render on mount
    
    - Use formatTime() helper from Phase 2 (js/lib/time-format.js or similar) to respect user's 24h/12h format setting
    - Helper to calculate "missed" status: if central time is before current hour (e.g., forecast was 07:00 but it's 08:15), mark as missed and calculate delta
    - Helper to format min/max display: e.g., "06:30 – 07:00" on one line
    
    Keep existing quick-log buttons and day-list rendering unchanged. Wire forecast above the day-list per D3-07.
  </action>
  <verify>
    <automated>grep -c "renderForecastSection\|renderPredictionCard\|renderNextEventCard" js/ui/today-screen.js</automated>
  </verify>
  <done>
    today-screen.js updated with forecast rendering functions. Subscribers wired to eventLog and settings. Forecast cards render on mount and reactively update. Cold-start gating working.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify composition root (js/app.js) wires everything together</name>
  <files>js/app.js</files>
  <action>
    Review and update `js/app.js` to ensure:
    - forecast function is imported from `js/lib/forecast.js`
    - mountTodayScreen() is called with all required parameters: { eventLog, settings, targetElement }
    - No breaking changes from Phase 2 composition (eventLog and settings stores already created and available)
    - If any new dependencies are needed (e.g., formatTime helper), import them
    
    Typical composition looks like:
    ```javascript
    import { createEventLog } from './store/event-log.js';
    import { createSettingsStore } from './store/settings.js';
    import { forecast, selectNextEvent } from './lib/forecast.js';
    import { mountTodayScreen } from './ui/today-screen.js';
    
    const storage = createStorageLocal();
    const eventLog = createEventLog({ storage });
    const settings = createSettingsStore({ storage, eventLog });
    
    mountTodayScreen({
      eventLog,
      settings,
      targetElement: document.querySelector('#app')
    });
    ```
    
    Verify no circular dependencies or missing imports.
  </action>
  <verify>
    <automated>grep -c "mountTodayScreen\|forecast\|selectNextEvent" js/app.js</automated>
  </verify>
  <done>
    js/app.js updated with forecast imports and composition. All wiring in place. App loads and renders Today screen with forecast cards.
  </done>
</task>

<task type="auto">
  <name>Task 5: E2E test — land on Today, see cold-start message, log 7 events, see predictions</name>
  <files>tests/e2e/forecast.spec.js</files>
  <action>
    Create `tests/e2e/forecast.spec.js` with Playwright tests covering:
    
    **Test 1: "Land on Today, see cold-start message when < minDays"**
    - Navigate to app
    - Assert cold-start message visible: "Not enough data yet"
    - Assert "Log 7 more days" message (minDays default 7, 0 events = 7 remaining)
    - Assert four prediction cards NOT visible
    
    **Test 2: "Log 7 events on different days (one per day), prediction cards appear"**
    - Log event 1 (wake 06:30) for day 1
    - Verify message updates: "Log 6 more days" (reactive)
    - Log 6 more events (bedtime, nap, etc.) on days 2–7
    - After 7th event, prediction cards appear (cards visible, message hidden)
    - Verify wake card shows central time + min/max band
    
    **Test 3: "Log new event via quick-log button, predictions update in real-time"**
    - Start from state where cold-start is satisfied (7+ events logged)
    - Record initial prediction value (e.g., wake central time = "06:45")
    - Click "Woke up" quick-log button, which logs event at current time
    - Wait for DOM to update (waitForSelector or similar)
    - Verify prediction changed (central time updated to reflect new event)
    - No page reload should have occurred
    
    **Test 4: "Probability-band fallback when ±delta exceeds maxDelta"**
    - Log 7 events with high variance (e.g., wake times: 06:00, 06:15, 06:30, 07:00, 07:15, 07:30, 08:00 = 120-min span)
    - maxDelta = 30 by default, so 120 > 30, should trigger probability band
    - Verify wake card shows probability table (P(wake by 06:30) = X%, etc.)
    - NOT showing "06:15 – 07:45" min/max band
    
    **Test 5: "Next-event card is visible and shows most imminent event"**
    - Log events such that last event = bedtime
    - Verify next-event card is present and visible above the four cards
    - Verify next-event card shows wake time (per D3-10 priority: bedtime → wake is next)
    - Card styling is distinct/prominent (larger font, different background color)
    
    **Test 6: "Missed predictions are grayed out with label"**
    - Manually set an event 2 hours in the past (if possible via time mocking, or rely on current time)
    - Forecast a wake time at that past hour
    - Verify prediction card has class="missed"
    - Verify "Missed by Xmin" label visible
    - Card opacity is reduced
    
    Use Playwright's built-in page navigation, locators (getByRole, getByText), and waitForSelector for reactive updates.
  </action>
  <verify>
    <automated>grep -c "test(" tests/e2e/forecast.spec.js</automated>
  </verify>
  <done>
    E2E test file created with 6 comprehensive tests. Tests cover cold-start, prediction cards, reactive updates, probability-band fallback, next-event hero, and missed-prediction styling. All tests written and pass (green) on local app instance.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Forecast data flows from validated event-log + settings stores to DOM. No untrusted input.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Repudiation | Reactive forecast updates on user action | accept | All state changes flow through eventLog.subscribe() / settings.subscribe() — all mutations are logged at store layer |
| T-03-09 | Information Disclosure | Forecast card displays user's sleep history | accept | User's own data displayed to user only |

</threat_model>

<verification>
**E2E test command:** `playwright test tests/e2e/forecast.spec.js`

**Verification checklist:**
- [ ] Cold-start message appears when < minDays
- [ ] Message updates reactively as events are logged
- [ ] Prediction cards appear when minDays threshold reached
- [ ] Four prediction cards (wake, bedtime, nap start, nap end) render with central time + min/max band
- [ ] Next-event hero card is visible and shows correct priority
- [ ] Quick-log button triggers reactive forecast re-render
- [ ] Probability-band fallback appears when band width > maxDelta
- [ ] Missed predictions are grayed out and labeled
- [ ] No page reload during reactive updates

**Nyquist check:** All forecast card selectors verified to exist in DOM (grep for id="forecast-cards", class="prediction-card", etc.)
</verification>

<success_criteria>
- User lands on Today and sees cold-start message (until minDays events logged)
- After minDays events, four prediction cards appear with central time + band
- Next-event hero card visible above the four cards
- Quick-log button triggers reactive forecast re-render (no reload)
- Probability-band view appears when band width exceeds maxDelta
- Missed predictions are visually distinct (grayed, labeled)
- All E2E tests pass (6/6 green)
- CSS styling matches Phase 1/2 calm dark theme

</success_criteria>

<output>
Create `.planning/phases/NW-03-forecast-engine-today-screen/03-04-SUMMARY.md` when done.

Document:
- Files modified (index.html, style.css, js/ui/today-screen.js, js/app.js, tests/e2e/forecast.spec.js)
- E2E test delta (0 → 6 specs)
- CSS class names added for prediction cards, hero card, cold-start message
- Any styling adjustments made after initial implementation (responsive layout tweaks, color/spacing refinements)
- Note: Missed-prediction styling per D3-11 implemented; Phase 8 may revisit visibility rules
</output>
