---
plan_id: 01-03
phase: 1
plan: 3
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - index.html
  - js/ui/today-screen.js
  - js/ui/dom.js
  - js/store/event-log.js
  - tests/integration/event-log.test.js
  - tests/e2e/quick-log.spec.js
autonomous: true
requirements:
  - LOG-02
  - LOG-03
  - LOG-04
  - LOG-07
  - LOG-08
  - LOG-09
mitigates:
  - T-01
  - T-05
  - T-07

must_haves:
  truths:
    # Vertical slice extension: 4 quick-log buttons + day-grouped event list
    - "User can tap 'Going to sleep' to record a bedtime event at clock.now() rounded to 5 minutes (LOG-02)"
    - "User can tap 'Nap start' to record a napStart event at clock.now() rounded to 5 minutes (LOG-03)"
    - "User can tap 'Nap end' to record a napEnd event at clock.now() rounded to 5 minutes (LOG-04)"
    - "Events are rendered in a day-grouped list, newest day first, last 7 calendar days visible (D-10, D-15, LOG-08 calendar grouping)"
    - "Day headers show calendar date only — no cutover-hint tooltip or annotation in Phase 1 (D-17 affirmative)"
    - "Each rendered event row shows 'HH:MM  <Event type>' using textContent only (D-12, T-07 XSS mitigation)"
    - "A day with two napStart events renders the second one in a faint 'extra nap' row (LOG-09 / T-06 surfacing read from dayRecord.extraNaps)"
    - "Double-clicking 'Woke up' produces exactly one event (T-05 / Pitfall #5: 300ms debounce on the click handler)"
    - "Store addEvent rejects all four non-valid types with /Invalid event type/ (T-01 complete coverage of VALID_TYPES guard)"
  artifacts:
    - path: "index.html"
      provides: "Four quick-log buttons + empty day-list mount point"
      contains: 'data-log="bedtime"'
    - path: "js/ui/today-screen.js"
      provides: "mountTodayScreen renders 4 buttons + day-grouped list reading from eventLog.daysByCalendar(7); delegated click; 300ms debounce per button"
      exports: ["mountTodayScreen"]
      min_lines: 60
    - path: "js/ui/dom.js"
      provides: "el(tag, props, children), clear(node), $(sel, root) helpers — no framework"
      exports: ["el", "clear", "$"]
    - path: "js/store/event-log.js"
      provides: "createEventLog exposes daysByCalendar(limit) delegating to lib/day-bucket"
      exports: ["createEventLog"]
    - path: "tests/integration/event-log.test.js"
      provides: "Coverage for all four valid types + invalid type rejection + daysByCalendar delegation"
    - path: "tests/e2e/quick-log.spec.js"
      provides: "Playwright spec clicking each of 4 buttons + double-click idempotency assertion"
  key_links:
    - from: "js/ui/today-screen.js"
      to: "js/store/event-log.js"
      via: "eventLog.daysByCalendar(7) + eventLog.addEvent(type)"
      pattern: "daysByCalendar\\(7\\)"
    - from: "js/store/event-log.js"
      to: "js/lib/day-bucket.js"
      via: "import { daysByCalendar }"
      pattern: "from '\\.\\./lib/day-bucket\\.js'"
    - from: "js/ui/today-screen.js"
      to: "js/ui/dom.js"
      via: "import { el, clear }"
      pattern: "from '\\./dom\\.js'"
---

<objective>
Extend the Walking Skeleton's single-button slice into the full Phase 1 Today screen: four quick-log buttons (Woke up / Going to sleep / Nap start / Nap end) + a day-grouped event list rendered from `daysByCalendar(7)`. After this plan, the user can log all four event types and see them grouped by calendar date with the LOG-09 extra-nap warning rendered as a faint row.

Purpose: Land LOG-02/03/04 (the other three quick-log buttons), LOG-08 (calendar grouping in the UI per D-11), LOG-09 (extraNaps surfaced visibly per Plan 02's read-side enforcement), and harden the click handler against double-click duplicates (T-05 / Pitfall #5). The slice now does what a user calls "logging sleep events" — Plan 04 only adds the manual entry / edit / delete affordances on top.

Output: Updated `index.html` with all four buttons, `js/ui/today-screen.js` rendering the day-grouped list, a tiny `js/ui/dom.js` helper module, store exposing `daysByCalendar(limit)`, integration tests covering all four valid types, and a Playwright `quick-log.spec.js` exercising each button + the double-click idempotency case.
</objective>

## Phase Goal

**As a** sleep-tracker user (the primary subject), **I want to** tap any of four quick-log buttons (Woke up / Going to sleep / Nap start / Nap end) and immediately see the event in a day-grouped list, **so that** I can capture every sleep event in a single tap and visually confirm what I've logged today without leaving the home screen.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/NW-01-log-persist/01-CONTEXT.md
@.planning/phases/NW-01-log-persist/01-RESEARCH.md
@.planning/phases/NW-01-log-persist/01-PATTERNS.md
@.planning/phases/NW-01-log-persist/01-VALIDATION.md
@.planning/phases/NW-01-log-persist/01-SKELETON.md
@.planning/phases/NW-01-log-persist/01-01-SUMMARY.md
@.planning/phases/NW-01-log-persist/01-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend store.daysByCalendar passthrough + integration tests for all four valid types</name>
  <files>js/store/event-log.js, tests/integration/event-log.test.js</files>
  <read_first>
    - .planning/phases/NW-01-log-persist/01-CONTEXT.md §decisions D-01 (event shape with 4 valid types), D-08 (two day-grouping functions are exposed on the store per RESEARCH §Pattern 5), D-10 (7-day default window)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Pattern 5 (event-log store full body — daysByCalendar and daysBySubjectiveNight already shown)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Code Examples §Integration test example
    - js/store/event-log.js (Plan 01 — currently exposes addEvent + listEvents; this task adds daysByCalendar + daysBySubjectiveNight passthrough)
    - js/lib/day-bucket.js (Plan 02 — daysByCalendar + daysBySubjectiveNight implemented)
    - tests/integration/event-log.test.js (Plan 01 — extend, don't rewrite)
  </read_first>
  <behavior>
    RED → GREEN.

    RED — extend tests/integration/event-log.test.js with these assertions:
    - 'addEvent("bedtime") persists with type "bedtime"' (LOG-02)
    - 'addEvent("napStart") persists with type "napStart"' (LOG-03)
    - 'addEvent("napEnd") persists with type "napEnd"' (LOG-04)
    - 'addEvent rejects each of: "snore", "", null, undefined, "WAKE" (case-sensitive)' (T-01 complete coverage)
    - 'log.daysByCalendar() returns an array of day records' (delegation passthrough)
    - 'log.daysByCalendar(3) returns at most 3 day records' (limit propagation)
    - 'after adding events on three calendar dates, daysByCalendar(2) returns the 2 newest dates' (newest-first ordering preserved across the seam)

    GREEN — extend js/store/event-log.js so the factory returns an object that also exposes:
      daysByCalendar(limit) { return _daysByCalendar(db.events, limit); }
      daysBySubjectiveNight(cutoverHour = 4, limit) { return _daysBySubjectiveNight(db.events, cutoverHour, limit); }
    importing `daysByCalendar as _daysByCalendar, daysBySubjectiveNight as _daysBySubjectiveNight` from '../lib/day-bucket.js'.

    The cutoverHour default of 4 matches D-18 (Phase 1 hardcoded; Phase 2 wires user config). Document this in a comment so Phase 2 finds the seam.
  </behavior>
  <action>
    RED commit: `test(NW-01): LOG-02,LOG-03,LOG-04 add failing integration assertions for all valid types + daysByCalendar passthrough`. Run `node --test tests/integration/event-log.test.js` and capture the failure.

    GREEN: edit js/store/event-log.js to (a) import the two bucketer functions from '../lib/day-bucket.js' (with rename to avoid name collision with the returned object's methods), (b) add daysByCalendar(limit) and daysBySubjectiveNight(cutoverHour=4, limit) methods to the returned object.

    Run `node --test`. Must be green.

    GREEN commit: `feat(NW-01): LOG-02,LOG-03,LOG-04,LOG-08 expose daysByCalendar+daysBySubjectiveNight on store per D-08`.

    Verify the existing Plan 01 + Plan 02 tests still pass (`node --test`).
  </action>
  <verify>
    <automated>
      node --test tests/integration/event-log.test.js
      AND grep -q "daysByCalendar" js/store/event-log.js
      AND grep -q "daysBySubjectiveNight" js/store/event-log.js
      AND grep -q "from '../lib/day-bucket.js'" js/store/event-log.js
    </automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/integration/event-log.test.js` exits 0 with at least 10 assertions passing (3 valid types + 5 invalid type rejections + daysByCalendar delegation + limit + ordering)
    - js/store/event-log.js source imports from '../lib/day-bucket.js'
    - js/store/event-log.js exposes daysByCalendar(limit) and daysBySubjectiveNight(cutoverHour, limit) (verified via `node -e` smoke importing createEventLog and checking the returned object)
    - All four valid types ('wake', 'bedtime', 'napStart', 'napEnd') round-trip through addEvent + listEvents with their exact spelling preserved
    - Invalid types ('snore', '', null, undefined, 'WAKE') each throw an error matching /Invalid event type/ (T-01)
  </acceptance_criteria>
  <done>
    Integration suite green; Plan 01 reload spec still passes; commit applied.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build js/ui/dom.js helpers + extend today-screen.js to 4 buttons + day-grouped list</name>
  <files>js/ui/dom.js, js/ui/today-screen.js, index.html</files>
  <read_first>
    - .planning/phases/NW-01-log-persist/01-CONTEXT.md §decisions D-10 (single scrollable screen, 4 buttons + day-grouped list), D-11 (calendar grouping on UI), D-12 (per-event affordances inline — but [edit]/[×] is Plan 04, this plan only renders the static row), D-15 (7-day window), D-17 (NO cutover-hint tooltip in Phase 1 — list header shows the calendar date only)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Architecture Patterns (system diagram showing today-screen.js role)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Anti-Patterns ("Mutating store-returned arrays" — never mutate daysByCalendar result; "innerHTML with user data" — never)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Common Pitfalls #5 (double-click idempotency: 300ms debounce on buttons recommended for Phase 1)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Security Domain (V5 Input Validation + XSS via innerHTML — textContent only for user data; T-07)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Open Questions #4 (7-day window counts back from clock.now() — but in this plan we just pass limit=7 to the store, which already uses event.at string sort, so the "from now" semantic is implicit through the data shape)
    - .planning/phases/NW-01-log-persist/01-PATTERNS.md "js/ui/today-screen.js" section (createElement/textContent/appendChild idiom from mindful-breathing lines 1267-1310)
    - .planning/phases/NW-01-log-persist/01-PATTERNS.md "js/ui/dom.js" section (sketch of el/clear/$ helpers — NOT a framework)
    - .planning/phases/NW-01-log-persist/01-PATTERNS.md Pattern C (textContent over innerHTML)
    - ../mindful-breathing/index.html lines 1267-1310 (createElement idiom verbatim)
    - js/ui/today-screen.js (Plan 01 — extend, replacing the 1-button stub with the 4-button + list renderer)
    - index.html (Plan 01 — replace the single button with the 4-button row + day list mount)
  </read_first>
  <behavior>
    Build js/ui/dom.js helpers (per PATTERNS.md §dom.js sketch):
    - el(tag, props = {}, children = []): creates an element, sets className/textContent/data-*/other DOM props (NEVER innerHTML for property assignment) — when a prop key starts with 'data-' use setAttribute, when key === 'onClick' attach an event listener
    - clear(node): node.replaceChildren() (safer than innerHTML='')
    - $(selector, root = document): querySelector wrapper

    Replace today-screen.js with a full-slice implementation:
    - Render four buttons in a `<div class="quickLog">` with data-log attributes 'wake', 'bedtime', 'napStart', 'napEnd' and human labels 'Woke up', 'Going to sleep', 'Nap start', 'Nap end'
    - Render a `<section class="dayList">` containing one `<article class="day">` per dayRecord from eventLog.daysByCalendar(7)
    - Each article header is `<h3>` with the calendar date as a plain string ONLY (e.g. "2026-05-26" or "Tue 26 May") — D-17 explicitly rules out a cutover-hint tooltip or annotation in Phase 1
    - Each article header uses textContent only (no innerHTML)
    - Each event row is `<li>` containing `<time>HH:MM</time>` + `<span>${eventTypeLabel}</span>` (textContent for both; no innerHTML)
    - Render extraNaps as a faint row at the bottom of the article with `<li class="extraNap">` + textContent 'Extra nap: HH:MM' (LOG-09 surfacing)
    - Wire a single delegated click listener on the quickLog div: on click of a button with data-log, call eventLog.addEvent(type), then re-render the day list
    - Debounce each button: track `lastClickAt[type]`; if Date.now() - lastClickAt[type] < 300ms, ignore (Pitfall #5 mitigation, T-05). NOTE: this is the ONE exception to "no Date.now() outside clock adapter" — it's a debounce timestamp, not domain time. Document the exception in a top-of-function comment, and use `performance.now()` instead so the grep gate for `new Date()` remains clean.
    - Map type → label: { wake: 'Wake', bedtime: 'Bedtime', napStart: 'Nap start', napEnd: 'Nap end' } as an Object.freeze'd config

    Update index.html: replace the Plan 01 single button with `<main id="app"></main>` (Plan 01 should already have this mount point). Today-screen builds the entire UI under that mount.
  </behavior>
  <action>
    Create js/ui/dom.js per PATTERNS.md sketch — el, clear, $. Keep it under 30 lines. NO reactivity, no virtual DOM. If the file exceeds 30 lines, you are building a framework — stop.

    Replace js/ui/today-screen.js. Use createElement (via el()) + textContent + appendChild throughout. Use `clear(listRoot)` before each re-render. Map dayRecord.allEvents into `<li>` rows; map dayRecord.extraNaps into a separate `<li class="extraNap">` block.

    Wire debounce: at the top of the click handler, use `performance.now()` instead of `Date.now()` so the grep gate stays clean. Store `const lastClickAt = {};` inside the closure returned by mountTodayScreen so it survives re-renders but is per-mount.

    Update index.html: confirm `<main id="app"></main>` is the mount point. If Plan 01 mounted into document.body, fix to mount into `document.getElementById('app')`. Add minimal CSS rules in style.css for `.quickLog { display: flex; gap: 12px }`, `.day { margin-bottom: 16px }`, `.extraNap { opacity: 0.6 }`.

    Anti-patterns to avoid:
    - DO NOT assign `.innerHTML = somethingDerivedFromEvents` (T-07)
    - DO NOT mutate the array returned by `eventLog.daysByCalendar(7)` (RESEARCH §Anti-Patterns)
    - DO NOT call `new Date()` (use `performance.now()` for the debounce — this preserves the clock-adapter invariant from Plan 01)
    - DO NOT inline the four button definitions four times — loop over a frozen `BUTTONS = [{ type: 'wake', label: 'Woke up' }, ...]` config

    Commit: `feat(NW-01): LOG-02,LOG-03,LOG-04,LOG-08,LOG-09 four quick-log buttons + day-grouped list with extraNaps surfacing per D-10,D-11,D-12,D-15`.
  </action>
  <verify>
    <automated>
      node -e "import('./js/ui/dom.js').then(m=>{['el','clear','$'].forEach(k=>{if(typeof m[k]!=='function')process.exit(1)})})"
      AND grep -E 'data-log="(wake|bedtime|napStart|napEnd)"' index.html | wc -l | awk '{if($1&lt;1)exit 1}'
      AND grep -E "BUTTONS|button.*data-log" js/ui/today-screen.js
      AND ! grep -E "\.innerHTML\s*=\s*[^\"]" js/ui/today-screen.js
      AND ! grep -E "new Date\(\)" js/ui/today-screen.js
    </automated>
  </verify>
  <acceptance_criteria>
    - js/ui/dom.js exports el, clear, $ (smoke import passes)
    - js/ui/dom.js is fewer than 30 non-comment lines (anti-framework constraint)
    - js/ui/today-screen.js source contains four type strings: 'wake', 'bedtime', 'napStart', 'napEnd' (the BUTTONS config)
    - js/ui/today-screen.js does NOT contain `.innerHTML = ` followed by a non-empty-string (T-07 enforcement; the only acceptable innerHTML usage is `el.innerHTML = ""` for clearing, but we use `clear()` instead)
    - js/ui/today-screen.js does NOT contain `new Date()` (clock-seam invariant preserved; debounce uses `performance.now()`)
    - js/ui/today-screen.js source contains `daysByCalendar(7)` (D-10/D-15 7-day window literal)
    - js/ui/today-screen.js source contains `extraNap` (LOG-09 surfacing observable in UI code)
    - js/ui/today-screen.js source contains `performance.now()` (T-05 debounce uses non-domain clock)
    - All Plan 01 and Plan 02 tests still pass (`node --test` exits 0)
  </acceptance_criteria>
  <done>
    Manual smoke: open `http://localhost:8080` (via `node scripts/serve.js`), click each of the four buttons, see four events grouped under today's date. Plan 01's reload.spec.js still passes against the new UI (LOG-01 row visibility wording may need a small update to find 'Wake' label — verify and patch if needed; the spec uses `getByRole('button', { name: /woke up/i })` which still matches).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Playwright quick-log.spec.js covering all 4 buttons + double-click idempotency</name>
  <files>tests/e2e/quick-log.spec.js</files>
  <read_first>
    - .planning/phases/NW-01-log-persist/01-CONTEXT.md §decisions D-10 (four quick-log buttons), D-12 (per-event affordances — visible labels)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Code Examples §E2E test example (verbatim spec for click + reload + double-click)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Common Pitfalls #5 (double-click idempotency rationale)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Common Pitfalls #8 (Playwright storage isolation — beforeEach must clear localStorage and reload)
    - .planning/phases/NW-01-log-persist/01-VALIDATION.md §Per-Task Verification Map (LOG-01..04 row)
    - tests/e2e/reload.spec.js (Plan 01 — extend the pattern; do NOT rewrite that file)
    - js/ui/today-screen.js (just landed in Task 2 — read the button labels for selector accuracy)
  </read_first>
  <behavior>
    Create tests/e2e/quick-log.spec.js with these specs (all under one file; beforeEach clears localStorage and reloads per Pitfall #8):

    - 'click "Going to sleep" records a bedtime event visible in the list' — LOG-02
    - 'click "Nap start" records a napStart event visible in the list' — LOG-03
    - 'click "Nap end" records a napEnd event visible in the list' — LOG-04
    - 'click each of the four buttons sequentially → four events visible' — combination smoke
    - 'double-click "Woke up" within 300ms → exactly one event' (T-05 / Pitfall #5)
    - 'a second "Nap start" on the same calendar date renders as an extraNap row' — LOG-09 / T-06 UI surfacing

    Use `page.getByRole('button', { name: /going to sleep/i })` etc. so selectors are role-based (accessible) per Plan 01's pattern. List assertions use `page.getByRole('listitem')` filtered by text. The extraNap assertion uses `page.locator('.extraNap')` since the row carries a distinguishing class.
  </behavior>
  <action>
    Write tests/e2e/quick-log.spec.js with the six specs above. Each spec is self-contained — `beforeEach` clears localStorage and reloads, so no state leaks between tests (Pitfall #8).

    For the double-click spec, use `await btn.click({ clickCount: 2, delay: 50 })` then `await page.waitForTimeout(400)` to step past the debounce window. Assert exactly one `<li>` row with text matching `/Wake/`.

    For the extraNap spec, click "Nap start" twice (separated by a 400ms wait to bypass debounce — confirming two events ARE created when intentionally spaced). Then assert one `.extraNap` row is rendered (Plan 02's read-side filter + Plan 03's UI surfacing combined).

    Run `npx playwright test tests/e2e/quick-log.spec.js` (with `node scripts/serve.js` running). All specs must pass.

    Commit: `test(NW-01): LOG-02,LOG-03,LOG-04,LOG-09 e2e quick-log + double-click idempotency + extraNap surfacing per Pitfall #5,#8`.
  </action>
  <verify>
    <automated>
      node scripts/serve.js &amp; SERVER_PID=$!; sleep 1; npx playwright test tests/e2e/quick-log.spec.js tests/e2e/reload.spec.js; PW_EXIT=$?; kill $SERVER_PID; exit $PW_EXIT
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/e2e/quick-log.spec.js` exits 0 with all 6 specs passing
    - Plan 01's reload.spec.js still passes (no regression from UI replacement)
    - tests/e2e/quick-log.spec.js contains `localStorage.clear()` in a beforeEach (Pitfall #8)
    - tests/e2e/quick-log.spec.js contains an assertion of exactly one event after a double-click (T-05 / Pitfall #5)
    - tests/e2e/quick-log.spec.js contains an assertion against `.extraNap` selector (LOG-09 UI surfacing)
  </acceptance_criteria>
  <done>
    Full E2E suite green; full `npm test` green (unit + integration + E2E).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM click event → js/ui/today-screen.js handler | `event.target.dataset.log` could be tampered (devtools) — store's VALID_TYPES is the authoritative gate |
| eventLog.daysByCalendar return value → DOM render | Must use textContent for every event field (T-07 — though Phase 1 data is well-formed enum + regex'd timestamp, enforce by convention) |
| Rapid user clicks → store.addEvent | T-05: debounce at UI level (300ms per button) — Pitfall #5 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01 | Tampering (V5) | js/store/event-log.js addEvent (full coverage) | mitigate | Integration test in Task 1 asserts all four valid types accepted AND five invalid inputs rejected (snore, empty, null, undefined, wrong-case 'WAKE'). VALID_TYPES set is the gate. |
| T-05 | Business Logic (V11) | js/ui/today-screen.js click handler | mitigate | 300ms per-button debounce using `performance.now()` (not `Date.now()` — keeps clock-seam invariant). E2E test in Task 3 double-clicks 'Woke up' and asserts exactly one event. |
| T-07 | Tampering / XSS (V5) | js/ui/today-screen.js render | mitigate | All event fields rendered via `textContent` (through `el()` helper or directly). No `.innerHTML = ` with derived data. Grep gate in `<acceptance_criteria>` enforces invariant. Phase 1 data is enum + regex'd timestamp (low risk), but convention is enforced now to make Phase 5's CSV import safe-by-default. |
</threat_model>

<verification>
- `npm test` exits 0 (chained node --test + npx playwright test, all suites green)
- `grep -E "data-log=\"(wake|bedtime|napStart|napEnd)\"" index.html` finds all 4 attributes
- `grep -E "\.innerHTML\s*=\s*[^\"]" js/ui/today-screen.js` returns no matches (T-07 enforcement)
- `grep -E "new Date\(\)" js/ui/today-screen.js` returns no matches (clock-seam invariant preserved through Plan 03)
- `grep -q "performance.now()" js/ui/today-screen.js` (T-05 debounce uses non-domain clock)
- `grep -q "extraNap" js/ui/today-screen.js` (LOG-09 surfacing in UI)
- Manual smoke: click each of 4 buttons; see 4 distinct events; double-click any button; see only one extra event from the double-click
</verification>

<success_criteria>
After Plan 03:
1. LOG-02, LOG-03, LOG-04 — all three remaining quick-log buttons work and are tested via E2E.
2. LOG-08 (calendar-day grouping in the UI per D-11) is observable: events appear under their calendar date.
3. LOG-09 (at most one nap surfaced per day) is observable: a second napStart renders as `.extraNap`.
4. T-01 (invalid types rejected) has full coverage of all four valid + several invalid cases.
5. T-05 (double-click idempotency) is mitigated and tested.
6. T-07 (XSS via innerHTML) is mitigated by convention and grep gate.
7. Plan 01's reload.spec.js still passes — no regression.
8. The slice now does what a user calls "Phase 1 logging" — only manual entry / edit / delete remains (Plan 04).
</success_criteria>

<output>
Create `.planning/phases/NW-01-log-persist/01-03-SUMMARY.md` when done, listing:
- New E2E specs added + pass counts
- UI screenshot path if captured (optional)
- Any selector ambiguity discovered during testing (e.g. button labels colliding)
- Confirmation Plan 01's reload spec still green
</output>
