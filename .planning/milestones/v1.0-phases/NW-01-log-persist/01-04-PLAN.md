---
plan_id: 01-04
phase: 1
plan: 4
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - index.html
  - js/store/event-log.js
  - js/ui/today-screen.js
  - js/ui/manual-entry.js
  - tests/integration/event-log.test.js
  - tests/integration/manual-entry.test.js
  - tests/e2e/manual-entry.spec.js
autonomous: true
requirements:
  - LOG-05
  - LOG-06
  - LOG-07
mitigates:
  - T-02
  - T-05
  - T-07

must_haves:
  truths:
    # Manual entry / edit / delete slice — completes Phase 1 user-visible behavior
    - "User can open a modal via '+ Add event' at the bottom of the day list (D-10 modal trigger)"
    - "User can submit the modal with Date + Hour + Minute + Type to record an event for today OR any past day (LOG-05)"
    - "Modal Hour input is 0-23, Minute input is 0-55 step=5, Type input is 4-option select (D-13, D-14, T-02 V5 input bounds)"
    - "Modal silently rounds Minute to nearest 5 on save if user types a non-5-multiple (D-13 normalization, RESEARCH §Open Question #2 recommendation)"
    - "Each event row exposes [edit] and [×] affordances; click [edit] opens the modal pre-filled, click [×] confirms via window.confirm() then deletes (D-12, RESEARCH §Open Question #3 native confirm acceptable)"
    - "Editing an event mutates the existing record in place — events.length is unchanged after edit (D-03 mutate-in-place; T-05/Pitfall #6 mitigation)"
    - "Deleting an event removes it from the list AND from localStorage immediately (LOG-06; D-05 invariant holds)"
    - "Modal uses native <dialog> + showModal() — focus trap, ESC-to-close, aria-modal automatic (RESEARCH §Pattern 6, V5+V14 zero-deps accessibility)"
    - "All modal field values rendered via .value property assignments — never innerHTML interpolation (T-07 mitigation)"
  artifacts:
    - path: "js/ui/manual-entry.js"
      provides: "openManualEntry({ mode, existing, onSave }) using native <dialog>.showModal(); silently rounds minute on save"
      exports: ["openManualEntry"]
      min_lines: 60
    - path: "js/store/event-log.js"
      provides: "addEventAt(type, atString), editEvent(eventId, patch), deleteEvent(eventId) — all mutate-in-place per D-03"
      exports: ["createEventLog"]
    - path: "index.html"
      provides: "<dialog id='manualEntry'> with form fields per D-13/D-14 (date input + HH/MM number inputs + 4-option type select) + Add Event trigger button"
      contains: '<dialog id="manualEntry"'
    - path: "js/ui/today-screen.js"
      provides: "Renders [edit] [×] on each event row + '+ Add event' button; delegated handlers wire to manual-entry openModal + store editEvent/deleteEvent"
    - path: "tests/integration/manual-entry.test.js"
      provides: "addEventAt round-trip, editEvent mutate-in-place (events.length unchanged), deleteEvent removal"
    - path: "tests/e2e/manual-entry.spec.js"
      provides: "Open modal → submit → event appears; edit row → modal pre-filled → save → only one event; delete row → row gone after confirm"
  key_links:
    - from: "js/ui/today-screen.js"
      to: "js/ui/manual-entry.js"
      via: "import { openManualEntry }; openManualEntry({ mode, existing, onSave: (data) => eventLog.addEventAt(...) | eventLog.editEvent(...) })"
      pattern: "openManualEntry\\("
    - from: "js/ui/manual-entry.js"
      to: "<dialog id='manualEntry'>"
      via: "document.getElementById('manualEntry').showModal()"
      pattern: "showModal\\("
    - from: "js/store/event-log.js"
      to: "js/lib/time.js"
      via: "addEventAt parses atString with parseLocalISO then re-rounds with roundTo5"
      pattern: "parseLocalISO|roundTo5"
---

<objective>
Complete the Phase 1 vertical slice by adding manual entry / edit / delete affordances. Users can now log an event for any past day (back-fill), edit an existing event's time/type, and delete events — all without leaving the Today screen. The modal uses native `<dialog>` (no library) and the store gains `addEventAt`, `editEvent`, `deleteEvent` methods that all mutate in place per D-03.

Purpose: Land LOG-05 (manual entry / edit) and LOG-06 (delete), the last two Phase 1 user-visible behaviors. Mitigate T-05 / Pitfall #6 (edit-creates-duplicate bug) at both the store level (integration test asserts events.length unchanged) and the UI level (modal distinguishes 'add' vs 'edit' mode via explicit parameter).

Output: A native `<dialog>`-based modal, three new store methods, [edit] / [×] affordances on each event row, integration coverage for the mutation methods, and a Playwright spec exercising the full modal flow.
</objective>

## Phase Goal

**As a** sleep-tracker user (the primary subject), **I want to** add events for past days, edit a logged event's time when I mis-tapped, and delete events I logged by accident, **so that** my historical log stays accurate and I can back-fill the days I forgot to log on the spot.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/NW-01-log-persist/01-CONTEXT.md
@.planning/phases/NW-01-log-persist/01-RESEARCH.md
@.planning/phases/NW-01-log-persist/01-PATTERNS.md
@.planning/phases/NW-01-log-persist/01-VALIDATION.md
@.planning/phases/NW-01-log-persist/01-SKELETON.md
@.planning/phases/NW-01-log-persist/01-01-SUMMARY.md
@.planning/phases/NW-01-log-persist/01-02-SUMMARY.md
@.planning/phases/NW-01-log-persist/01-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD addEventAt + editEvent (mutate-in-place) + deleteEvent in the store</name>
  <files>js/store/event-log.js, tests/integration/event-log.test.js, tests/integration/manual-entry.test.js</files>
  <read_first>
    - .planning/phases/NW-01-log-persist/01-CONTEXT.md §decisions D-01 (event shape), D-03 (mutate-in-place — no audit trail), D-05 (localStorage byte-for-byte equals canonical JSON — invariant after edit/delete), D-13 (modal save dispatches add-or-edit)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Pattern 5 (event-log store body — full addEventAt, editEvent, deleteEvent shown)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Common Pitfalls #6 (edit-creates-duplicate — write test FIRST asserting events.length unchanged after edit)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Failure Categories #3 (edit creates duplicate)
    - .planning/phases/NW-01-log-persist/01-PATTERNS.md "js/store/event-log.js" section (full rules + Pattern 5 anchor)
    - js/store/event-log.js (Plan 01 + Plan 03 state — currently exposes addEvent, listEvents, daysByCalendar, daysBySubjectiveNight; extend with addEventAt, editEvent, deleteEvent)
    - js/lib/time.js (Plan 02 — parseLocalISO + roundTo5 + formatLocalISO all in place)
    - tests/integration/event-log.test.js (Plan 01 + Plan 03 — extend)
  </read_first>
  <behavior>
    RED → GREEN → REFACTOR.

    RED — write tests in tests/integration/manual-entry.test.js (new file, dedicated to manual-entry flow) plus extend tests/integration/event-log.test.js:

    addEventAt:
    - 'addEventAt("wake", "2026-05-25T06:33") persists with at "2026-05-25T06:35" (round-to-nearest applied to typed input)'
    - 'addEventAt throws /Invalid event type/ on bad type (T-01 reused)'
    - 'addEventAt throws on malformed at-string "2026/05/25T06:35" (T-02 reused via parseLocalISO)'
    - 'addEventAt accepts a past date (LOG-05 back-fill)'

    editEvent (Pitfall #6 — MUST write first):
    - 'editEvent mutates in place — events.length unchanged after edit' (T-05 / Pitfall #6 the canonical integration test)
    - 'editEvent re-rounds at on save (typed 06:33 saves as 06:35)'
    - 'editEvent rejects invalid type (T-01)'
    - 'editEvent throws /not found/ when id absent'
    - 'editEvent preserves id'

    deleteEvent:
    - 'deleteEvent removes the event from listEvents'
    - 'deleteEvent returns false when id absent (idempotent)'
    - 'deleteEvent persists — after a fresh createEventLog over the same storage, the deleted event is gone (D-05 invariant)'

    GREEN — extend js/store/event-log.js. Add these methods to the object returned by createEventLog (refer to RESEARCH §Pattern 5 for the exact body of each):

    addEventAt(type, atString):
    - if (!VALID_TYPES.has(type)) throw new Error(`Invalid event type: ${type}`)
    - const at = formatLocalISO(roundTo5(parseLocalISO(atString)))
    - mint id, push, persist, return event

    editEvent(eventId, patch):
    - find index by id; throw /not found/ if missing
    - const next = { ...events[i], ...patch }
    - if (!VALID_TYPES.has(next.type)) throw
    - next.at = formatLocalISO(roundTo5(parseLocalISO(next.at)))
    - events[i] = next  // mutate-in-place at SAME index (D-03)
    - persist; return next

    deleteEvent(eventId):
    - find index by id; if absent return false
    - events.splice(i, 1)
    - persist; return true

    Imports at top: extend Plan 01's import to include `parseLocalISO` from '../lib/time.js'.

    REFACTOR — extract `persist()` helper into a private function inside createEventLog (it may already exist from Plan 01). Add a top-of-file comment block citing D-03 (mutate-in-place) and Pitfall #6 (the integration test is the regression guard).
  </behavior>
  <action>
    RED commit: `test(NW-01): LOG-05,LOG-06 + Pitfall #6 add failing integration tests for addEventAt+editEvent+deleteEvent`.

    Run `node --test tests/integration/`. Confirm failures (methods don't exist).

    GREEN: implement the three methods in js/store/event-log.js per RESEARCH §Pattern 5. Run `node --test`. Must be green.

    GREEN commit: `feat(NW-01): LOG-05,LOG-06 addEventAt+editEvent (mutate-in-place)+deleteEvent per D-03,D-13`.

    REFACTOR — confirm persist() is shared, header comment references D-03 + Pitfall #6, no duplication of the type+at validation block. Re-run tests; green.

    REFACTOR commit: `refactor(NW-01): share persist() and document D-03 mutate-in-place invariant`.

    Anti-patterns to avoid:
    - DO NOT splice + push when editing — must mutate at the same index (D-03; Pitfall #6 root cause)
    - DO NOT skip re-rounding `at` on edit — the user may type 06:33; the store re-rounds to 06:35 per LOG-07
    - DO NOT silently coerce non-string `eventId` — let `findIndex` return -1 and throw /not found/
    - DO NOT add an audit-trail / tombstone array — explicit D-03 rejection
  </action>
  <verify>
    <automated>
      node --test tests/integration/event-log.test.js tests/integration/manual-entry.test.js
      AND grep -q "addEventAt" js/store/event-log.js
      AND grep -q "editEvent" js/store/event-log.js
      AND grep -q "deleteEvent" js/store/event-log.js
      AND grep -E "events\.splice\(i," js/store/event-log.js
      AND ! grep -E "events\.push.*editEvent|push.*after.*editEvent" js/store/event-log.js
    </automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/integration/event-log.test.js` AND `node --test tests/integration/manual-entry.test.js` both exit 0
    - At least 12 new assertions pass across the two integration files (4 addEventAt + 5 editEvent + 3 deleteEvent)
    - The Pitfall #6 test (`editEvent ... events.length unchanged`) explicitly passes — search the integration test files for that assertion text
    - js/store/event-log.js exports a createEventLog whose returned object has methods: addEvent, addEventAt, editEvent, deleteEvent, listEvents, daysByCalendar, daysBySubjectiveNight (verified by `node -e` import + Object.keys check)
    - js/store/event-log.js source contains `events.splice(i, 1)` (deleteEvent removes by index, mutate-in-place; D-03)
    - js/store/event-log.js source contains `events[i] = next` (editEvent assigns at same index — NOT splice+push; Pitfall #6 mitigation visible)
    - js/store/event-log.js header comment cites D-03 and Pitfall #6
  </acceptance_criteria>
  <done>
    Integration suite green; Plan 01/02/03 tests still pass; refactor commit applied.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Native <dialog> manual entry + edit + delete UI</name>
  <files>index.html, js/ui/manual-entry.js, js/ui/today-screen.js</files>
  <read_first>
    - .planning/phases/NW-01-log-persist/01-CONTEXT.md §decisions D-10 (+Add event button at bottom of list), D-12 (per-event [edit] [×] affordances), D-13 (modal dialog with Date+Hour+Minute+Type, silently rounds minute), D-14 (two-number HH+MM picker, no <input type="time">)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Pattern 6 (native <dialog> HTML + showModal pattern)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Don't Hand-Roll (native <dialog> for focus trap, ESC, aria-modal — DO NOT roll a div overlay)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Common Pitfalls #6 (edit-creates-duplicate at UI level — explicit `mode: 'add' | 'edit'` parameter)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Open Questions #2 (silently round minute on save + grey hint label — recommended)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Open Questions #3 (native window.confirm() acceptable for delete confirmation in Phase 1)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Security Domain (V5 Input Validation — modal form bounds enforce client-side validation; store re-validates server-side equivalent)
    - .planning/phases/NW-01-log-persist/01-PATTERNS.md "js/ui/manual-entry.js" section
    - .planning/phases/NW-01-log-persist/01-PATTERNS.md "js/ui/today-screen.js" section (delegated [edit] / [×] handler pattern)
    - js/ui/today-screen.js (Plan 03 — extend to add the [edit][×] affordances + Add event button)
    - js/ui/dom.js (Plan 03 — reuse el/clear/$ helpers)
    - index.html (Plan 03 — extend with <dialog> block)
  </read_first>
  <behavior>
    index.html — append a `<dialog id="manualEntry">` block under `<main id="app">`. The dialog body is a `<form method="dialog">` per RESEARCH §Pattern 6:
    - `<h2 id="manualEntryTitle">Add event</h2>` (title text swapped by JS for edit mode)
    - `<label>Date <input type="date" name="date" required></label>`
    - `<label>Hour <input type="number" name="hour" min="0" max="23" required></label>`
    - `<label>Minute <input type="number" name="minute" min="0" max="55" step="5" required></label>`
    - `<small class="hint">Times are stored in 5-minute increments.</small>` (Open Question #2 grey hint)
    - `<label>Type <select name="type" required>` with 4 options (wake, bedtime, napStart, napEnd) using lowercase camelCase values matching D-01
    - `<menu><button type="button" id="manualCancel" formnovalidate>Cancel</button><button type="submit" value="save">Save</button></menu>`

    js/ui/manual-entry.js — export `openManualEntry({ mode, existing, onSave })` where:
    - mode: 'add' | 'edit' (REQUIRED — explicit per Pitfall #6 mitigation)
    - existing: { id, type, at } when mode === 'edit'; null/undefined when mode === 'add'
    - onSave: callback receiving { type, at } (already in canonical 'YYYY-MM-DDTHH:MM' form, rounded)

    Behavior:
    - Get dialog by id; get form via querySelector
    - Set title h2.textContent to 'Add event' or 'Edit event'
    - If editing, pre-fill date (existing.at.slice(0,10)), hour (parseInt existing.at.slice(11,13)), minute (parseInt existing.at.slice(14,16)), type (existing.type) — direct property assignments via .value (NEVER innerHTML)
    - If adding, default date to today (computed via `document.defaultView.Intl` — actually use `new Intl.DateTimeFormat('en-CA').format(new Date())` returns YYYY-MM-DD, BUT note this calls `new Date()` indirectly. Better: pass `today` as a parameter from the caller who reads it from `eventLog`'s injected clock, or accept that the modal directly reads `new Date()` for the default date prefill ONLY — add an inline comment exempting this single `new Date()` reference as the UI default-prefill, NOT domain time. Domain time still flows through clock-system.js. Document the exemption.)
    - On submit (form's `close` event with returnValue === 'save'): read FormData, normalize Minute = Math.round(rawMinute / 5) * 5 (silent normalize per Open Question #2 + LOG-07), construct atString = `${date}T${HH}:${MM}` with HH/MM zero-padded, invoke onSave({ type, at: atString })
    - On cancel button click, call dlg.close('cancel')
    - Use `dlg.showModal()` (focus trap, ESC, aria-modal automatic — RESEARCH §Pattern 6)
    - Reset form on close (so re-opening starts clean for next add)

    js/ui/today-screen.js — extend Plan 03's renderer:
    - Each event row now includes `<button class="rowEdit" data-event-id="${id}">edit</button>` and `<button class="rowDel" data-event-id="${id}">×</button>` (textContent only; data-event-id attribute)
    - At the bottom of the day list (or at the bottom of `<main>`), render a `<button id="addEventBtn">+ Add event</button>`
    - Wire delegated click listeners on the day list root:
      - .rowEdit click → find event by id from eventLog.listEvents(), openManualEntry({ mode: 'edit', existing, onSave: (patch) => { eventLog.editEvent(existing.id, patch); rerender(); } })
      - .rowDel click → if (window.confirm(`Delete this event at ${event.at}?`)) { eventLog.deleteEvent(eventId); rerender(); } — native confirm per Open Question #3
    - Wire #addEventBtn click → openManualEntry({ mode: 'add', existing: null, onSave: ({ type, at }) => { eventLog.addEventAt(type, at); rerender(); } })

    NEVER use innerHTML to render the row affordances — use `el('button', { className: 'rowEdit', 'data-event-id': id, textContent: 'edit' })`.
  </behavior>
  <action>
    Edit index.html: append the `<dialog id="manualEntry">` block per the `<behavior>` HTML above. Place it inside `<body>` AFTER `<main id="app">` but BEFORE the `<script type="module">` tag. Add minimal CSS in style.css for the dialog (padding, max-width, button spacing).

    Create js/ui/manual-entry.js implementing `openManualEntry({ mode, existing, onSave })`. The function signature MUST be exactly `openManualEntry({ mode, existing, onSave })` — `mode` is required and validated at function entry with `if (mode !== 'add' && mode !== 'edit') throw new Error('mode must be "add" or "edit"')`. This explicit-mode parameter is the architectural mitigation for Pitfall #6 (the UI cannot accidentally call addEventAt when it should be calling editEvent).

    For the date default-prefill, the modal calls `new Date()` ONLY when mode === 'add' AND the existing date input is empty. Add the EXACT inline comment (verbatim, this exact string): `// gsd:allow-ui-clock — UI default-prefill of today's date is non-domain; domain time flows through clock-system.js`. Plan 05 Task 2 greps for the literal tag `// gsd:allow-ui-clock`; any variation will fail Plan 05's security-smoke gate and force a revision cycle.

    Extend js/ui/today-screen.js to (a) render [edit] [×] buttons in each row via el(), (b) render the '+ Add event' button below the day list, (c) wire delegated handlers. Each affordance uses textContent for labels.

    Run `npm test` to confirm all unit + integration + the new Playwright spec from Task 3 pass.

    Commit: `feat(NW-01): LOG-05,LOG-06 manual entry modal + edit + delete affordances per D-10,D-12,D-13,D-14`.
  </action>
  <verify>
    <automated>
      grep -q '&lt;dialog id="manualEntry"' index.html
      AND grep -E 'min="0" max="23"' index.html
      AND grep -E 'min="0" max="55" step="5"' index.html
      AND grep -q "showModal" js/ui/manual-entry.js
      AND grep -q "mode === 'edit'\|mode === \"edit\"" js/ui/manual-entry.js
      AND grep -q "Math.round" js/ui/manual-entry.js
      AND grep -q "addEventBtn\|+ Add event" js/ui/today-screen.js
      AND grep -q "rowEdit" js/ui/today-screen.js
      AND grep -q "rowDel" js/ui/today-screen.js
      AND grep -q "window.confirm" js/ui/today-screen.js
      AND ! grep -E "\.innerHTML\s*=\s*[^\"]" js/ui/manual-entry.js
    </automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `<dialog id="manualEntry">` (native dialog element; T-07 zero-deps accessibility)
    - index.html contains the three form fields with proper bounds: date input, hour input min=0 max=23, minute input min=0 max=55 step=5 (D-14 + V5 input bounds)
    - index.html does NOT contain `<input type="time">` (D-14 explicit rejection of native time picker)
    - js/ui/manual-entry.js calls `.showModal()` (RESEARCH §Pattern 6 + V14 zero-deps modal accessibility)
    - js/ui/manual-entry.js validates the `mode` parameter (Pitfall #6 mitigation visible in code)
    - js/ui/manual-entry.js contains `Math.round` for minute normalization (LOG-07 silent rounding + Open Question #2)
    - js/ui/manual-entry.js does NOT contain `.innerHTML = ` with a non-empty string (T-07 enforcement)
    - js/ui/today-screen.js renders both `rowEdit` and `rowDel` buttons (D-12 affordances)
    - js/ui/today-screen.js wires `window.confirm` before deleteEvent (Open Question #3 native confirm acceptable)
    - js/ui/today-screen.js renders a `+ Add event` button (D-10 trigger)
    - All Plan 01/02/03 tests still pass (`npm test` chained suite green; no regression)
  </acceptance_criteria>
  <done>
    Manual smoke: open `http://localhost:8080`; click `+ Add event`; modal appears (focus trapped, ESC works); fill in Date/Hour/Minute/Type; click Save; event appears in list. Click [edit] on a row; modal pre-fills; change minute; click Save; same row updates in place (no duplicate). Click [×] on a row; native confirm; click OK; row disappears. Reload page; mutations persist (D-05 invariant).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Playwright manual-entry.spec.js (add via modal, edit no-duplicate, delete flow)</name>
  <files>tests/e2e/manual-entry.spec.js</files>
  <read_first>
    - .planning/phases/NW-01-log-persist/01-CONTEXT.md §decisions D-13 (modal flow)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Common Pitfalls #6 (edit creates duplicate — E2E regression guard)
    - .planning/phases/NW-01-log-persist/01-RESEARCH.md §Common Pitfalls #8 (Playwright storage isolation — beforeEach)
    - .planning/phases/NW-01-log-persist/01-VALIDATION.md §Per-Task Verification Map (LOG-05, LOG-06 rows)
    - js/ui/manual-entry.js + js/ui/today-screen.js (Task 2 — selectors)
  </read_first>
  <behavior>
    tests/e2e/manual-entry.spec.js with these specs (beforeEach clears localStorage + reloads per Pitfall #8):

    1. 'open modal via + Add event, submit a past-day wake event, verify it appears in the list under the correct date' (LOG-05)
    2. 'submit modal with minute=33 (non-5-multiple) — event saved with minute=35 (silent rounding per Open Question #2)' (LOG-07)
    3. 'edit an existing event — events.length stays at 1 after save' (LOG-05 + Pitfall #6 E2E regression guard)
    4. 'click [×] on a row, accept native confirm → row disappears AND reload confirms persistence of the delete' (LOG-06)
    5. 'cancel button in modal → no event added' (modal cancel path)
    6. 'press ESC in modal → no event added (native <dialog> ESC-to-close + cancel semantic)' (RESEARCH §Pattern 6)

    For spec 4 (delete), the native confirm dialog must be auto-accepted in Playwright. Use:
    ```
    page.on('dialog', d => d.accept());
    ```
    set in the test beforeEach OR before the click.

    For spec 6 (ESC), use `await page.keyboard.press('Escape')`. The native dialog's ESC behavior leaves returnValue empty, which the manual-entry.js close handler should treat as cancel (not save).
  </behavior>
  <action>
    Create tests/e2e/manual-entry.spec.js with the six specs. Each spec sets up its own preconditions (most need a pre-existing event before edit/delete — log one via the quick-log button).

    For spec 3 (edit-no-duplicate), the assertion sequence is:
    1. Click 'Woke up' → one event row
    2. Click [edit] on that row → modal opens, pre-filled
    3. Change the Minute input to '40'
    4. Click Save
    5. Assert exactly ONE list item containing 'Wake' (events.length === 1)
    6. Reload the page
    7. Assert still exactly ONE list item (D-05 invariant + D-03 mutate-in-place verified end-to-end)

    For spec 4 (delete), use:
    ```
    page.on('dialog', dialog => dialog.accept());
    await page.locator('.rowDel').first().click();
    await expect(page.locator('.rowDel')).toHaveCount(0);
    await page.reload();
    await expect(page.locator('.rowDel')).toHaveCount(0);
    ```

    Run `npx playwright test tests/e2e/manual-entry.spec.js`. All 6 specs must pass.

    Commit: `test(NW-01): LOG-05,LOG-06 e2e manual entry+edit-no-duplicate+delete per Pitfall #6,#8`.
  </action>
  <verify>
    <automated>
      node scripts/serve.js &amp; SERVER_PID=$!; sleep 1; npx playwright test tests/e2e/manual-entry.spec.js tests/e2e/quick-log.spec.js tests/e2e/reload.spec.js; PW_EXIT=$?; kill $SERVER_PID; exit $PW_EXIT
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/e2e/manual-entry.spec.js` exits 0 with all 6 specs passing
    - The edit-no-duplicate spec (#3) is named so a grep can find it: `grep -q "events.length\|duplicate\|stays at 1" tests/e2e/manual-entry.spec.js` (T-05 / Pitfall #6 regression guard)
    - tests/e2e/manual-entry.spec.js sets `page.on('dialog', d => d.accept())` for the delete spec (native confirm handling)
    - tests/e2e/manual-entry.spec.js contains an ESC keyboard press assertion (Pattern 6 native dialog behavior)
    - All E2E specs from Plans 01/03 still pass (reload.spec.js + quick-log.spec.js)
  </acceptance_criteria>
  <done>
    Full E2E suite green (3 spec files: reload, quick-log, manual-entry — total ~12-15 specs). Full `npm test` chained suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Modal form inputs (date/hour/minute/type) → js/store/event-log.js addEventAt/editEvent | Untrusted user input; modal client-validates bounds (V5), store re-validates via VALID_TYPES + parseLocalISO regex (T-02 reused) |
| event-id from DOM attribute → js/store/event-log.js editEvent/deleteEvent | data-event-id can be tampered via devtools; store throws /not found/ on mismatch (idempotent failure) |
| Modal mode parameter ('add' vs 'edit') → onSave dispatch | T-05 / Pitfall #6: explicit mode prevents UI from accidentally calling addEventAt when editEvent is intended |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02 | Tampering (V5) | js/store/event-log.js addEventAt + editEvent | mitigate | Both call parseLocalISO (regex-strict) on the at string before persisting. Integration test asserts addEventAt throws on '2026/05/25T06:35' (wrong separator). Reuses Plan 02's parseLocalISO gate. |
| T-05 | Business Logic (V11) | js/ui/manual-entry.js + onSave dispatch | mitigate | `mode` parameter is required and validated at openManualEntry entry. UI dispatches to addEventAt (mode='add') or editEvent (mode='edit') based on this explicit mode — never on `existing ? edit : add` (which is brittle per Pitfall #6). Integration test + E2E test both assert events.length unchanged after edit. |
| T-07 | Tampering / XSS (V5) | js/ui/manual-entry.js + js/ui/today-screen.js [edit][×] affordances | mitigate | All form value assignments use `.value =` (property, not innerHTML). All row affordance labels use `textContent` via el() helper. Grep gate enforces invariant. |
</threat_model>

<verification>
- `npm test` exits 0 — full suite (3 unit files + 3 integration files + 3 E2E specs)
- `grep -q "showModal" js/ui/manual-entry.js` (native dialog API)
- `grep -E "mode === 'add'|mode === \"add\"" js/ui/manual-entry.js` (Pitfall #6 explicit mode dispatch)
- `grep -E "events\.splice|events\[i\] = next" js/store/event-log.js` (D-03 mutate-in-place semantics in both delete and edit paths)
- `grep -q "window.confirm" js/ui/today-screen.js` (Open Question #3 native confirm)
- `grep -RnE "\.innerHTML\s*=\s*[^\"]" js/` returns no matches (T-07 enforcement project-wide)
- Manual smoke: full modal flow (add past-day, edit, delete) works end-to-end
</verification>

<success_criteria>
After Plan 04:
1. LOG-05 — manual entry / edit via modal works for current and past days.
2. LOG-06 — delete works with native confirm + persists across reload.
3. LOG-07 — minute auto-normalization in modal silently rounds non-5-multiples.
4. T-02 — modal's at-string flows through parseLocalISO; bad input rejected.
5. T-05 / Pitfall #6 — edit-creates-duplicate is mitigated by (a) explicit mode parameter in modal, (b) mutate-in-place in store, (c) integration test asserting events.length unchanged, (d) E2E test confirming the same.
6. T-07 — no innerHTML with user-derived data anywhere in `js/`.
7. All Plan 01/02/03 tests still pass — no regression.
8. Phase 1 user-visible behavior is COMPLETE; only Plan 05 hardening remains.
</success_criteria>

<output>
Create `.planning/phases/NW-01-log-persist/01-04-SUMMARY.md` when done, listing:
- Store methods added (addEventAt, editEvent, deleteEvent) with line counts
- Modal flow specs added (LOG-05/LOG-06 coverage)
- Pitfall #6 mitigation evidence (the failing test asserting events.length and its passing fix commit hashes)
- Confirmation that all 9 test files now exist (3 unit + 3 integration + 3 E2E) per the testing pyramid
</output>
