---
phase: 02-configuration-settings
plan: "06"
type: execute
wave: 3
depends_on: [02-04, 02-05]
files_modified:
  - js/ui/manual-entry.js
  - style.css
  - tests/e2e/settings-modal.spec.js
autonomous: true
requirements:
  - CFG-09

must_haves:
  truths:
    - "In 24h mode (default), manual-entry HH input has min='0' max='23' and no AM/PM select visible (D2-20)"
    - "In 12h mode, manual-entry HH input has min='1' max='12' and a <select name='ampm'> sibling is visible with options AM/PM (D2-20)"
    - "Toggling to 12h in Settings → Save → manual-entry modal already-open picker reconfigures to 12h shape (D2-19)"
    - "12h → 24h toggle while manual-entry is open converts existing HH+AMPM value to 24h integer and writes it back (e.g., '2' PM → '14') (D2-19)"
    - "24h → 12h toggle while manual-entry is open converts existing HH value to h12+ampm (e.g., '14' → '2' PM) (D2-19)"
    - "Clamping rule: value '23' in 24h mode → on toggle to 12h → HH='11', AMPM='PM' (to12h(23)={h12:11,ampm:'PM'}) — no special clamping code needed, arithmetic handles it"
    - "12h mode Save: to24h(hStr, ampm) converts form values to 24h before calling validate({hourStr,...}); existing validate() logic is unchanged (D2-20)"
    - "Manual-entry subscriber captures unsubscribe() and calls it on modal close — no accumulating listeners across repeated opens (RESEARCH §Anti-patterns)"
    - "Internal event storage remains canonical 'YYYY-MM-DDTHH:MM' (24h ISO) regardless of timeFormat setting"
    - "Event rows in Today list already use formatTime(evt.at, snap.timeFormat) from Plan 02-05 — no additional change needed here (CFG-09 display surface covered)"
  artifacts:
    - path: js/ui/manual-entry.js
      provides: "openManualEntry accepts settings parameter; applyTimeFormat(snap) reconfigures HH input and AM/PM select; subscriber registered on open, unsubscribed on close; 12h→24h conversion via to24h before validate()"
      contains: "applyTimeFormat"
    - path: tests/e2e/settings-modal.spec.js
      provides: "Extended with CFG-09 E2E: toggle 12h → manual-entry shows AM/PM select; toggle 24h → AM/PM gone; time format persists after reload"
      min_lines: 20
  key_links:
    - from: js/ui/manual-entry.js
      to: js/lib/time.js
      via: "import { to24h, to12h } — conversion helpers used in applyTimeFormat and submit handler"
      pattern: "to24h|to12h"
    - from: js/ui/manual-entry.js
      to: js/store/settings.js
      via: "settings.subscribe(applyTimeFormat) registered on open; unsubscribed on modal close"
      pattern: "subscribe"
---

<objective>
Final Phase 2 plan — wires time format (24h/12h) into the manual-entry modal's time picker. After this plan, toggling to 12h in Settings causes the HH input to switch to 1-12 range and a new AM/PM select to appear; switching back removes it. The internal canonical storage format ('YYYY-MM-DDTHH:MM') is unchanged.

This plan completes CFG-09's three display surfaces: (1) event rows in Today list — done in Plan 02-05 via formatTime; (2) manual-entry modal picker — this plan; (3) day headers remain ISO YYYY-MM-DD (timeFormat-independent per D2-18).

Output:
  - js/ui/manual-entry.js (extended — settings parameter, applyTimeFormat, subscriber, 12h submit conversion)
  - style.css (AM/PM select styling if needed)
  - tests/e2e/settings-modal.spec.js (extended — CFG-09 E2E specs appended)
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/NW-02-configuration-settings/02-CONTEXT.md
@.planning/phases/NW-02-configuration-settings/02-RESEARCH.md
@.planning/phases/NW-02-configuration-settings/02-PATTERNS.md
@.planning/phases/NW-02-configuration-settings/02-UI-SPEC.md
@js/ui/manual-entry.js
@js/lib/time.js
@tests/e2e/settings-modal.spec.js
@.planning/phases/NW-02-configuration-settings/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend openManualEntry with 12h picker — applyTimeFormat subscriber + submit conversion</name>
  <files>js/ui/manual-entry.js, style.css</files>
  <read_first>
    - js/ui/manual-entry.js (full file): existing openManualEntry({eventLog, clock}) — current signature, form elements, submit handler with validate({date, hourStr, minuteStr, type}, {now}), error rendering. Phase 2 adds settings parameter and extends the time picker section. (PATTERNS.md §manual-entry.js §Phase 2 extension)
    - js/lib/time.js: to24h(hStr, ampm) and to12h(h24) exports added in Plan 02-03 — import these
    - js/ui/dom.js: el(tag, attrs) helper — use to create the <select name="ampm"> dynamically
    - 02-CONTEXT.md D2-20: HH input becomes min="1" max="12" in 12h mode + <select name="ampm"> sibling; native <input type="time"> REJECTED (same rationale as D-14); internal storage unchanged
    - 02-CONTEXT.md D2-19: when modal open and settings change (timeFormat toggle) → re-render time picker via subscriber, preserve partial values (convert), do NOT close/re-open the modal
    - 02-RESEARCH.md §Pattern I: applyTimeFormat(snap) implementation — conditional rendering Track 1 (create/remove ampmSelect dynamically, convert existing values)
    - 02-RESEARCH.md §Pitfall #6: 24→12 toggle while '14' in HH → to12h(14)={h12:2,ampm:'PM'} → write '2' + 'PM'; if value is empty or non-numeric, skip conversion
    - 02-RESEARCH.md §Anti-patterns: settings.subscribe without unsubscribe — manual-entry opens/closes repeatedly; MUST capture unsubscribe() and call it on dlg close
    - 02-RESEARCH.md §Pattern I §Submit flow: if 12h mode, read hourStr from HH input, ampm from ampmSelect, call to24h(hourStr, ampm) → hourStr24, pass to existing validate({date, hourStr:hourStr24, minuteStr, type}, {now})
    - 02-CONTEXT.md D2-18: day headers stay ISO YYYY-MM-DD — no change to day header rendering in this plan
  </read_first>
  <behavior>
    After the change:
    - openManualEntry({eventLog, clock, settings}) — settings accepted
    - applyTimeFormat(snap) is called at modal open with current settings.get()
    - If snap.timeFormat === '12h': hourInput.min='1', hourInput.max='12'; if ampmSelect not present, create <select name="ampm"> with AM/PM options and insert after hourInput; if hourInput.value is a valid 24h integer, convert via to12h and write h12+ampm back; if empty, skip conversion
    - If snap.timeFormat === '24h': hourInput.min='0', hourInput.max='23'; if ampmSelect is present, remove it; if hourInput.value is a valid 12h integer and ampmSelect was present, convert via to24h and write back; if empty, skip
    - settings.subscribe(applyTimeFormat) registered on open; returned unsubscribe fn stored in a variable
    - On dlg 'close' event: call unsubscribe() before doing anything else — prevents listener accumulation across repeated modal opens
    - Submit handler (when returnValue==='save'): if timeFormat==='12h', hourStr = String(to24h(form.elements.namedItem('hour').value, form.elements.namedItem('ampm').value)); else hourStr = form.elements.namedItem('hour').value; then call existing validate({date, hourStr, minuteStr, type}, {now}) — existing validator is unchanged

    NOTE: In practice, opening Settings while manual-entry is already open is prevented by the UI flow (Settings is in the header, which is outside the modal, but the user cannot interact with it while a modal is open due to native <dialog> blocking). D2-19 mandates subscriber support regardless — the subscriber fires when Settings modal Save fires after manual-entry was already open. The re-render is safe because the subscriber only touches the time picker elements, not the date/type fields.
  </behavior>
  <action>
    Modify js/ui/manual-entry.js:

    1. Add imports at top: import { to24h, to12h } from '../lib/time.js'; import { el } from './dom.js';

    2. Change signature: export function openManualEntry({ eventLog, clock, settings })

    3. Inside openManualEntry, after getting form references, identify:
       - hourInput = form.elements.namedItem('hour') (existing)
       - minuteInput = form.elements.namedItem('minute') (existing)
       - let ampmSelect = null; (track dynamically created select)
       - let unsubSettings = null; (track subscriber unsubscribe)

    4. Define applyTimeFormat(snap):
       - if snap.timeFormat === '12h':
         - hourInput.setAttribute('min','1'); hourInput.setAttribute('max','12');
         - if ampmSelect is null: ampmSelect = el('select', {name:'ampm'}); ampmSelect.appendChild(el('option',{value:'AM',textContent:'AM'})); ampmSelect.appendChild(el('option',{value:'PM',textContent:'PM'})); hourInput.insertAdjacentElement('afterend', ampmSelect);
         - if hourInput.value is non-empty and parseInt(hourInput.value,10) is a valid 24h int (0-23): const {h12,ampm} = to12h(parseInt(hourInput.value,10)); hourInput.value=String(h12); ampmSelect.value=ampm;
       - if snap.timeFormat === '24h':
         - hourInput.setAttribute('min','0'); hourInput.setAttribute('max','23');
         - if ampmSelect is not null: const h24Val = ampmSelect && hourInput.value ? to24h(hourInput.value, ampmSelect.value) : null; ampmSelect.remove(); ampmSelect = null; if (h24Val !== null) hourInput.value = String(h24Val);

    5. Call applyTimeFormat(settings.get()) immediately after form references are ready (modal is populated but not yet shown)

    6. unsubSettings = settings.subscribe(applyTimeFormat);

    7. In the dlg 'close' event handler (the existing onClose), at the TOP before any returnValue check: if (unsubSettings) { unsubSettings(); unsubSettings = null; }
       This prevents accumulating subscribers across repeated opens.

    8. In the submit path (when returnValue==='save', after reading FormData):
       let hourStr = String(data.get('hour') ?? '');
       if (settings.get().timeFormat === '12h' && ampmSelect) {
         const h24 = to24h(hourStr, String(data.get('ampm') ?? ''));
         hourStr = String(h24);
       }
       Then pass hourStr to the existing validate() call (unchanged).

    Add minimal CSS to style.css if needed for ampm select sizing (inherit input height: select[name="ampm"] { height: inherit; min-height:44px; padding:0 8px; })

    Run node --test to confirm no regression.
  </action>
  <verify>
    <automated>node --test</automated>
  </verify>
  <done>
    - node --test exits 0 — no regression on existing Phase 1 + Phase 2 tests
    - js/ui/manual-entry.js contains 'applyTimeFormat'
    - js/ui/manual-entry.js contains 'to24h' and 'to12h' imports
    - js/ui/manual-entry.js contains 'unsubSettings' and 'unsubSettings()' call in close handler
    - In browser: switching to 12h in Settings then opening manual-entry shows HH 1-12 range + AM/PM select
  </done>
</task>

<task type="auto">
  <name>Task 2: E2E — CFG-09 12h picker in manual-entry + time format persistence</name>
  <files>tests/e2e/settings-modal.spec.js</files>
  <read_first>
    - tests/e2e/settings-modal.spec.js: existing E2E from Plan 02-04 — read current spec to understand structure before APPENDING new scenarios (do NOT rewrite existing specs)
    - 02-CONTEXT.md D2-27: CFG-09 E2E — "toggle 12h and assert modal picker shape changes"
    - 02-VALIDATION.md §Phase Requirements → Test Map CFG-09 row: "integration + E2E — unit/time.test.js extended, npx playwright test e2e/settings-modal.spec.js"
    - 02-RESEARCH.md §Pitfall #6: 24→12 conversion of partially-entered values — E2E can cover the basic case (no value entered, just format toggle), leave the partial-value conversion as a dogfooding observation
    - 02-UI-SPEC.md §Time Format Impact: 12h mode event rows show 'H:MM AM/PM', 24h shows 'HH:MM'
  </read_first>
  <action>
    APPEND new describe/test blocks to tests/e2e/settings-modal.spec.js (do not overwrite existing tests):

    'CFG-09: switching to 12h — manual-entry HH input becomes 1-12 range':
    - Open Settings; select timeFormat '12-hour'; click Save
    - Open manual-entry modal (click '+ Add event' or gear triggers it — use the existing add-event button)
    - Expect HH input (form.elements.namedItem('hour') or input[name="hour"]) to have attribute min='1' and max='12'
    - Expect select[name="ampm"] to be visible with options 'AM' and 'PM'

    'CFG-09: switching back to 24h — AM/PM select disappears':
    - Ensure timeFormat is 12h; open manual-entry; confirm AM/PM select present
    - Close manual-entry; open Settings; select timeFormat '24-hour'; Save
    - Open manual-entry again; expect input[name="hour"] to have min='0' max='23'
    - Expect select[name="ampm"] to NOT be present in DOM

    'CFG-09: time format persists after reload':
    - Set timeFormat to 12h; reload; open manual-entry; expect input[name="hour"] min='1' max='12'; expect select[name="ampm"] visible

    'CFG-09: 12h event times display in H:MM AM/PM format in Today list':
    - Set timeFormat to 12h; log an event via quick-log (Woke up); expect the event row time to match pattern /\d{1,2}:\d{2} (AM|PM)/ (regex assertion)

    'CFG-09: 24h event times display in HH:MM format in Today list':
    - Set timeFormat to 24h (or reload with default); log an event; expect event row time to match pattern /\d{2}:\d{2}/ (two-digit hour, no AM/PM)

    Run npx playwright test tests/e2e/settings-modal.spec.js — all specs green (new + existing from Plan 02-04).
    Run npx playwright test — full E2E suite green.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/settings-modal.spec.js</automated>
  </verify>
  <done>
    - All new CFG-09 E2E specs pass (12h picker shape, 24h revert, persistence, display format)
    - Existing CFG-01 and save/cancel specs from Plan 02-04 still pass (append-only, no regressions introduced)
    - npx playwright test (full suite) exits 0
    - CFG-09 is fully covered: 12h picker shape in manual-entry + event-row time format in Today list
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manual-entry 12h submit path | User enters HH 1-12 + AM/PM → to24h() converts to 24h integer before validate() and before canonical storage |
| subscriber lifecycle | settings.subscribe registered on open; must unsubscribe on close to prevent listener accumulation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-22 | Tampering | 12h submit conversion | mitigate | to24h(hStr, ampm) throws on invalid hour (0, 13+) or invalid ampm — existing validate() then catches and surfaces the error; canonical storage remains 24h ISO (D-05 invariant preserved) |
| T-2-23 | Tampering | subscriber accumulation | mitigate | unsubSettings() called at the TOP of the close handler before any returnValue check — safe even if modal is cancelled or ESC'd |
| T-2-24 | Tampering | AM/PM select injection | mitigate | ampmSelect created via el(tag, attrs) with explicit value/textContent — no innerHTML; select values are 'AM' and 'PM' (author-controlled, not user-typed) |
| T-2-25 | Tampering | 12h picker value conversion edge cases | mitigate | to12h/to24h edge cases (12 AM, 12 PM) pinned by unit tests in Plan 02-03 (Pitfall #4); conversion skipped if hourInput.value is empty or non-numeric |
| T-2-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
Full suite green:

```
node --test && npx playwright test
```

Key source assertions:
- grep -c 'applyTimeFormat' js/ui/manual-entry.js → ≥ 2 (definition + call)
- grep -c 'to24h' js/ui/manual-entry.js → ≥ 1 (submit conversion)
- grep -c 'to12h' js/ui/manual-entry.js → ≥ 1 (24→12 toggle conversion)
- grep -c 'unsubSettings' js/ui/manual-entry.js → ≥ 2 (assignment + call in close handler)
- grep -v 'innerHTML' js/ui/manual-entry.js → no innerHTML in modified sections (T-07)
</verification>

<success_criteria>
- CFG-09 fully delivered: 12h picker shape in manual-entry (HH 1-12 + AM/PM select) + event row times in Today list (formatTime from Plan 02-05) + persistence after reload
- Subscriber accumulation prevented: unsubSettings() called in close handler (any outcome — save, cancel, ESC)
- 12h→24h→12h round-trip preserves time value (to12h/to24h arithmetic correctness pinned by unit tests)
- Internal storage remains canonical 24h ISO 'YYYY-MM-DDTHH:MM' regardless of timeFormat
- Full test suite (node --test && npx playwright test) exits 0
- All 8 in-phase CFG-* requirements (CFG-01..04, CFG-06..09) delivered across plans 02-01 through 02-06
</success_criteria>

<output>
Create .planning/phases/NW-02-configuration-settings/02-06-SUMMARY.md when done.
</output>
