---
phase: 02-configuration-settings
plan: "03"
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - js/store/event-log.js
  - js/app.js
  - js/lib/time.js
  - tests/unit/time.test.js
autonomous: true
requirements:
  - CFG-08
  - CFG-09

must_haves:
  truths:
    - "js/store/event-log.js has SCHEMA_VERSION===2 and calls migrateV1ToV2 before the version check (D2-05, PATTERNS.md §event-log.js)"
    - "event-log persist() re-reads settings slice from storage before save to avoid cross-store stale-write (Pitfall #1 symmetric fix)"
    - "js/app.js constructs createSettingsStore on the SAME storage instance as createEventLog (D2-08); settings wired into mountTodayScreen and mountHeader (D2-09)"
    - "formatTime('2026-05-01T03:50', '24h') === '03:50' (CFG-09)"
    - "formatTime('2026-05-01T03:50', '12h') === '3:50 AM' (CFG-09)"
    - "to12h(0) → {h12:12, ampm:'AM'} (midnight edge case, Pitfall #4)"
    - "to12h(12) → {h12:12, ampm:'PM'} (noon edge case, Pitfall #4)"
    - "to24h('12','AM') === 0 (midnight edge case); to24h('12','PM') === 12 (noon)"
    - "BUCKET_CONFIG.defaultCutoverHour remains 4 — cutoverHour injection happens at the daysBySubjectiveNight call site, NOT by mutating the constant (D2-17, Pitfall #2)"
  artifacts:
    - path: js/store/event-log.js
      provides: "SCHEMA_VERSION bumped to 2; migrateV1ToV2 called on load; persist() re-reads settings before save"
      contains: "SCHEMA_VERSION = 2"
    - path: js/app.js
      provides: "createSettingsStore constructed; mountHeader and mountTodayScreen receive settings parameter"
      contains: "createSettingsStore"
    - path: js/lib/time.js
      provides: "formatTime(at, timeFormat), to24h(hStr, ampm), to12h(h24) exported"
      exports: [formatTime, to24h, to12h]
    - path: tests/unit/time.test.js
      provides: "Extended with 12h/24h conversion boundary tests (12 AM/PM edge, all 4 boundary hours)"
      min_lines: 40
  key_links:
    - from: js/store/event-log.js
      to: js/lib/db-shape.js
      via: "import { migrateV1ToV2 } from '../lib/db-shape.js'"
      pattern: "migrateV1ToV2"
    - from: js/app.js
      to: js/store/settings.js
      via: "import { createSettingsStore } from './store/settings.js'"
      pattern: "createSettingsStore"
    - from: js/ui/today-screen.js
      to: js/store/settings.js
      via: "settings parameter passed through mountTodayScreen({root, eventLog, settings})"
      pattern: "settings"
---

<objective>
Wire the settings store into the composition root and raise the event-log schema version to 2. Also extends js/lib/time.js with the three time-format helpers (formatTime, to24h, to12h) that Plan 02-05 and 02-06 will consume. No new UI visible yet — the settings store is now wired at the root level and time helpers exist, but Today screen and manual-entry modal consume them in later plans.

Purpose: Unblock Plan 02-04 (UI shell) by ensuring the composition root exports settings to all UI mounts. Also ensures the event-log schema migration path is in place so Phase 1 dogfooders can migrate v1→v2 blobs when Phase 2 ships.

Output:
  - js/store/event-log.js (modified — SCHEMA_VERSION 1→2, migrateV1ToV2 call)
  - js/app.js (modified — settings store construction + wiring)
  - js/lib/time.js (modified — formatTime, to24h, to12h added)
  - tests/unit/time.test.js (extended — 12h conversion boundaries)
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/NW-02-configuration-settings/02-CONTEXT.md
@.planning/phases/NW-02-configuration-settings/02-RESEARCH.md
@.planning/phases/NW-02-configuration-settings/02-PATTERNS.md
@js/store/event-log.js
@js/app.js
@js/lib/time.js
@tests/unit/time.test.js
@.planning/phases/NW-02-configuration-settings/02-01-SUMMARY.md
@.planning/phases/NW-02-configuration-settings/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Raise event-log schema to v2 + wire settings store into composition root</name>
  <files>js/store/event-log.js, js/app.js</files>
  <read_first>
    - js/store/event-log.js lines 36–65: SCHEMA_VERSION constant + load + version check that throws — this is the two-line diff (PATTERNS.md §event-log.js §Current code)
    - js/store/event-log.js lines 123–145: persist() function — needs re-read-before-save to pick up settings changes from the settings store (Pitfall #1 symmetric fix)
    - js/app.js lines 11–21: composition root — PATTERNS.md §app.js §Current code + §Phase 2 extension
    - 02-CONTEXT.md D2-08: SAME storage instance, settings constructed first (or order does not matter — both call migrateV1ToV2 independently)
    - 02-RESEARCH.md §Pattern C: minimal diff for event-log.js — import migrateV1ToV2, import DEFAULT_SETTINGS, call migrateV1ToV2 before version check
    - 02-RESEARCH.md §Pitfall #1: event-log's persist() must re-read settings from fresh blob before save (mirror of settings.js update())
    - 02-RESEARCH.md §Pitfall #2: BUCKET_CONFIG.defaultCutoverHour MUST NOT be mutated — cutoverHour injection is at the call site in today-screen.js (D2-17), NOT here
  </read_first>
  <behavior>
    - After the change, createEventLog on a v1 blob succeeds (no throw)
    - After the change, createEventLog on a v2 blob succeeds
    - After the change, createEventLog on a v3 blob throws "Unsupported schema version: 3"
    - js/app.js imports createSettingsStore; constructs settings = createSettingsStore({storage}); passes settings to mountTodayScreen (signature update — today-screen.js need not consume settings yet, just accept it)
    - BUCKET_CONFIG.defaultCutoverHour === 4 still (grep assertion)
  </behavior>
  <action>
    Modify js/store/event-log.js:
    1. Add import: import { migrateV1ToV2, DEFAULT_SETTINGS } from '../lib/db-shape.js';
    2. Change SCHEMA_VERSION constant from 1 to 2.
    3. Replace the null-check + version-throw block with: let db = migrateV1ToV2(storage.load(), DEFAULT_SETTINGS); if (db.version !== SCHEMA_VERSION) { throw new Error('Unsupported schema version: ' + db.version); }
       (Migration runs before the version check so v1 blobs succeed; v3+ blobs still throw.)
    4. In the persist() function (storage.save call), add re-read-before-save: const fresh = storage.load(); if (fresh && fresh.version === 2 && fresh.settings) { db.settings = fresh.settings; } storage.save(db);
       This is the symmetric fix for Pitfall #1 — event-log picks up any settings the settings-store wrote between event-log's load and this save.

    Modify js/app.js:
    1. Add import: import { createSettingsStore } from './store/settings.js';
    2. Add import: import { mountHeader } from './ui/header.js'; (header.js does not exist yet — this import will be wired in Plan 02-04; add it now so the composition root is complete, and guard: if (typeof mountHeader === 'function') mountHeader({...}) — or simply leave the call commented with a TODO comment for Plan 02-04 to uncomment)
    3. Construct settings = createSettingsStore({ storage }); BEFORE eventLog construction (D2-08 order).
    4. Pass settings to mountTodayScreen: mountTodayScreen({ root: document.getElementById('app'), eventLog, settings }); — today-screen.js will accept settings in Plan 02-05 (the signature extension is forward-compatible; unused parameters are silently ignored).

    Run existing tests to confirm no regression: node --test
  </action>
  <verify>
    <automated>node --test</automated>
  </verify>
  <done>
    - node --test exits 0 — all Phase 1 tests still pass (schema bump is backward-compatible via migration)
    - grep -c 'SCHEMA_VERSION = 2' js/store/event-log.js → 1
    - grep -c 'migrateV1ToV2' js/store/event-log.js → ≥ 1
    - grep -c 'createSettingsStore' js/app.js → ≥ 1
    - grep 'BUCKET_CONFIG.defaultCutoverHour' js/lib/day-bucket.js still outputs 4 (not mutated)
    - Integration tests (02-02) still pass: node --test tests/integration/
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend time.js with formatTime / to24h / to12h + tests for boundary cases</name>
  <files>js/lib/time.js, tests/unit/time.test.js</files>
  <read_first>
    - js/lib/time.js lines 1–89: existing exports (roundTo5, formatLocalISO, parseLocalISO, hhmm) — string-based arithmetic, no Date construction (Pitfall #3). New exports extend this file with the same discipline.
    - tests/unit/time.test.js: existing test suite structure — read first to understand describe() nesting and assert style; new tests go in a new describe('12h conversion') block
    - 02-PATTERNS.md §time.js: formatTime, to24h, to12h recommended implementations (string-slice for formatTime, arithmetic for to24h/to12h)
    - 02-RESEARCH.md §Pattern H: formatTime(at, timeFormat) — string-based, slices at.slice(11,13) and at.slice(14,16), never constructs a Date
    - 02-RESEARCH.md §Pattern I: to24h(hStr, ampm) and to12h(h24) — explicit conditionals for 12/AM and 12/PM edges (Pitfall #4)
    - 02-RESEARCH.md §Pitfall #4: 12 AM = 00, 12 PM = 12 — naïve modulo arithmetic fails; use explicit conditionals
    - 02-CONTEXT.md D2-18: formatTime governs event row display; D2-20: to24h/to12h govern manual-entry modal conversion
  </read_first>
  <behavior>
    Table-driven tests covering all specified boundary cases:
    - formatTime('2026-05-01T03:50', '24h') === '03:50'
    - formatTime('2026-05-01T18:25', '24h') === '18:25'
    - formatTime('2026-05-01T00:00', '12h') === '12:00 AM' (midnight)
    - formatTime('2026-05-01T12:00', '12h') === '12:00 PM' (noon)
    - formatTime('2026-05-01T03:50', '12h') === '3:50 AM'
    - formatTime('2026-05-01T23:58', '12h') === '11:58 PM'
    - to12h(0) → {h12:12, ampm:'AM'}
    - to12h(12) → {h12:12, ampm:'PM'}
    - to12h(3) → {h12:3, ampm:'AM'}
    - to12h(15) → {h12:3, ampm:'PM'}
    - to12h(23) → {h12:11, ampm:'PM'}
    - to12h(1) → {h12:1, ampm:'AM'}
    - to12h(-1) → throws Error
    - to12h(24) → throws Error
    - to24h('12','AM') === 0
    - to24h('12','PM') === 12
    - to24h('3','AM') === 3
    - to24h('3','PM') === 15
    - to24h('11','PM') === 23
    - to24h('1','AM') === 1
    - to24h('0','AM') → throws Error (0 is invalid in 12h)
    - to24h('13','AM') → throws Error (13 is invalid in 12h)
    - to24h('3','XM') → throws Error (invalid AM/PM)
  </behavior>
  <action>
    Add to tests/unit/time.test.js: new describe('formatTime') and describe('to24h / to12h') blocks with table-driven assert.strictEqual / assert.deepStrictEqual cases covering all boundary cases above. Run node --test tests/unit/time.test.js — new tests MUST FAIL (functions not yet added).

    Add to js/lib/time.js (append to existing file):

    formatTime(at, timeFormat) export:
    - Slice hh = at.slice(11, 13), mm = at.slice(14, 16)
    - If timeFormat === '24h': return `${hh}:${mm}`
    - Parse h24 = parseInt(hh, 10); ampm = h24 < 12 ? 'AM' : 'PM'; h12 = h24 === 0 ? 12 : h24 > 12 ? h24 - 12 : h24
    - Return `${h12}:${mm} ${ampm}`
    - Never construct a Date object (DST safety per Pitfall #3)

    to12h(h24) export:
    - Validate: Number.isInteger(h24) && h24 >= 0 && h24 <= 23; throw on failure
    - ampm = h24 < 12 ? 'AM' : 'PM'; h12 = h24 === 0 ? 12 : h24 > 12 ? h24 - 12 : h24
    - Return { h12, ampm }

    to24h(hStr, ampm) export:
    - h = parseInt(hStr, 10); validate: Number.isFinite(h) && h >= 1 && h <= 12; throw on failure
    - If ampm === 'AM': return h === 12 ? 0 : h
    - If ampm === 'PM': return h === 12 ? 12 : h + 12
    - Throw on invalid ampm

    Run node --test tests/unit/time.test.js — all MUST PASS.
    Run node --test — full suite MUST PASS.

    Commit: "feat(NW-02): formatTime + to24h/to12h helpers (CFG-09, D2-18, D2-20) — Pitfall #4 edges pinned"
  </action>
  <verify>
    <automated>node --test tests/unit/time.test.js</automated>
  </verify>
  <done>
    - All new time.test.js cases pass (boundary cases for 12 AM, 12 PM, midnight, noon)
    - formatTime, to24h, to12h are exported from js/lib/time.js
    - No Date constructor used in any of the three new functions (grep -v 'new Date(' js/lib/time.js shows no new instances in the new functions)
    - node --test (full suite) exits 0 — no Phase 1 regression
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage v1 blob → event-log v2 constructor | Migration path now covers the version check |
| composition root wiring | settings store instance shared between event-log (same storage) and UI mounts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-09 | Tampering | event-log schema bump | mitigate | migrateV1ToV2 runs BEFORE version check — v1 blobs succeed; v3+ blobs throw (preventing silent version downgrade) |
| T-2-10 | Tampering | event-log persist() cross-store race | mitigate | persist() re-reads settings slice from fresh blob before save — symmetric fix to Pitfall #1; settings cannot be overwritten by an event-log write |
| T-2-11 | Tampering | time format 12h arithmetic | mitigate | Explicit conditionals for h24===0 (midnight) and h24===12 (noon) — Pitfall #4; table-driven tests pin all 4 boundary hours (0, 1, 12, 23) |
| T-2-12 | Tampering | BUCKET_CONFIG mutation temptation | mitigate | D2-17 explicit: BUCKET_CONFIG.defaultCutoverHour stays 4; cutoverHour injected at call site in today-screen.js (Plan 02-05). This plan does NOT touch day-bucket.js. |
| T-2-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
Full suite green:

```
node --test
```

Expected: all Phase 1 tests + Phase 2 Plans 01-02 tests + new time.test.js extensions. Exit code 0.

Key source assertions:
- grep -c 'SCHEMA_VERSION = 2' js/store/event-log.js → 1
- grep -c 'migrateV1ToV2' js/store/event-log.js → ≥ 1
- grep -c 'createSettingsStore' js/app.js → ≥ 1
- grep -c 'export.*formatTime' js/lib/time.js → ≥ 1
- grep -c 'export.*to24h' js/lib/time.js → ≥ 1
- grep -c 'export.*to12h' js/lib/time.js → ≥ 1
- grep -c 'defaultCutoverHour = 4' js/lib/day-bucket.js → 1 (unchanged)
</verification>

<success_criteria>
- node --test exits 0 with all Phase 1 + Phase 2 (plans 01-03) tests
- Event-log loads v1 blobs by migrating silently (SCHEMA_VERSION 1→2 tested in integration suite)
- formatTime/to24h/to12h pass all boundary cases including 12 AM/PM edge (Pitfall #4)
- Composition root wires settings store to mountTodayScreen (settings parameter forwarded)
- BUCKET_CONFIG.defaultCutoverHour=4 unchanged — cutoverHour wired at call site in Plan 02-05
</success_criteria>

<output>
Create .planning/phases/NW-02-configuration-settings/02-03-SUMMARY.md when done.
</output>
