---
phase: NW-07
plan: "03"
type: tdd
wave: 2
depends_on:
  - "07-01"
files_modified:
  - js/lib/chart-data.js
  - tests/unit/chart-data.test.js
autonomous: true
requirements:
  - UI-04

must_haves:
  truths:
    - buildSleepLengthSeries returns null sleepHours when wake or bedtime slot is missing
    - buildHeatmapData fills calendar gaps between first and last date with null sleepHours
    - buildHeatmapData assigns dayOfWeek 0 for Monday, 6 for Sunday (ISO day-of-week)
    - buildNapStats returns napDayPct === 0 and null avgNapStartHHMM when no nap days exist
    - buildActivityCorrelation returns [] when activityLog is empty
    - CHART_CONFIG and HEATMAP_CONFIG are Object.freeze()'d at module top
    - All date arithmetic in buildHeatmapData uses nextDayStr tagged gsd:allow-ui-clock
    - "node --test tests/unit/chart-data.test.js" exits 0 (GREEN)
  artifacts:
    - js/lib/chart-data.js
  key_links:
    - chart-data.js imports { timeToMinutes } from './forecast.js' for minute extraction
    - buildHeatmapData uses nextDayStr() local helper (tagged gsd:allow-ui-clock) for date iteration
    - buildSleepLengthSeries computes sleepHours as (wakeMinutes - bedtimeMinutes + 1440) % 1440 / 60
---

<objective>
Implement js/lib/chart-data.js using strict RED→GREEN→REFACTOR TDD cycles, satisfying the chart-data stubs from 07-01. These are the pure data-transform helpers that feed all five visualizations on the Charts screen (D7-05..D7-11, UI-04).

Purpose: Charts screen needs pre-computed data series in typed shapes before any SVG rendering happens. TDD ensures the transform contracts are correct and regression-safe.

Output: js/lib/chart-data.js with five named exports; all chart-data.test.js stubs passing GREEN.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/sleep-tracker/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/sleep-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/NW-07-charts-heatmap-accuracy/07-CONTEXT.md
@.planning/phases/NW-07-charts-heatmap-accuracy/07-RESEARCH.md
@.planning/phases/NW-07-charts-heatmap-accuracy/07-PATTERNS.md
@js/lib/forecast.js
@js/lib/time.js
@tests/unit/chart-data.test.js
</context>

## Artifacts This Phase (07-03) Produces

New files:
- `js/lib/chart-data.js` — exports five pure transform functions

Symbols exported:
- `buildSleepLengthSeries(dayRecords)` → `Array<{ date:string, sleepHours:number|null, rejected:boolean }>`
- `buildHeatmapData(dayRecords)` → `Array<{ date:string, sleepHours:number|null, dayOfWeek:number, weekIndex:number }>`
- `buildTimeBandSeries(dayRecords)` → `Array<{ date:string, wakeMinutes:number|null, bedtimeMinutes:number|null }>`
- `buildNapStats(dayRecords)` → `{ napDayPct:number, avgNapStartHHMM:string|null, avgNapLengthMin:number|null }`
- `buildActivityCorrelation(dayRecords, activityLog)` → `Array<{ activityScore:number, sleepHours:number }>`

Module-private helpers:
- `CHART_CONFIG` — Object.freeze with heatmap color thresholds (HEATMAP_COLORS, TARGET_SLEEP_MIN, TARGET_SLEEP_MAX)
- `computeSleepHours(dayRecord)` → number|null (wake and bedtime both present)
- `extractMinutes(event)` → number|null (extracts HH:MM from event.at and converts)
- `nextDayStr(dateStr)` → string (date increment, gsd:allow-ui-clock tagged)
- `getISODayOfWeek(dateStr)` → 0-6 (Mon=0, Sun=6)
- `getWeekIndex(dateStr, firstDateStr)` → integer week offset

<feature>
  <name>chart-data — five pure transform helpers for Phase 7 charts</name>
  <files>js/lib/chart-data.js, tests/unit/chart-data.test.js</files>

  <behavior>
    RED phase: node --test tests/unit/chart-data.test.js fails with ERR_MODULE_NOT_FOUND (already established in 07-01).

    GREEN phase — implement to satisfy all stubs:

    buildSleepLengthSeries(dayRecords):
    - Empty input → []
    - Day with wake at '07:00' and bedtime at '22:00' (previous day) → sleepHours === 9.0. Computed as: wakeMinutes=420, bedtimeMinutes=1320. (420 - 1320 + 1440) % 1440 = 540 minutes = 9 hours.
    - Day with null wake → { date, sleepHours: null, rejected: false }
    - Returns array preserving input order (or sort by date — document choice in JSDoc)

    buildHeatmapData(dayRecords):
    - Empty input → []
    - Two records with dates '2025-01-01' and '2025-01-03' → 3 cells, middle cell has date '2025-01-02' and sleepHours === null
    - Cell for '2025-01-06' (a known Monday) → dayOfWeek === 0
    - weekIndex increases monotonically from 0
    - Gap-filling uses nextDayStr(dateStr) loop from first to last date
    - byDate lookup: missing dates map to null sleepHours

    buildTimeBandSeries(dayRecords):
    - Day with wake event → wakeMinutes is a number in 0-1439
    - Day with null wake → wakeMinutes is null
    - Day with null bedtime → bedtimeMinutes is null
    - Returns array of { date, wakeMinutes, bedtimeMinutes }

    buildNapStats(dayRecords):
    - No nap days → { napDayPct: 0, avgNapStartHHMM: null, avgNapLengthMin: null }
    - 1 nap day in 5 total days → napDayPct === 20 (Math.round(1/5*100))
    - 1 nap day with napStart at '2025-01-01T13:00' and napEnd at '2025-01-01T14:30' → avgNapLengthMin === 90
    - avgNapStartHHMM format: 'HH:MM' string (minutes-back-to-time conversion)

    buildActivityCorrelation(dayRecords, activityLog):
    - Empty activityLog {} → []
    - Day with date '2025-01-01', sleepHours > 0, and activityLog['2025-01-01'] = 5 → { activityScore: 5, sleepHours: [computed] }
    - Day not in activityLog → excluded from result
    - Day where sleepHours is null → excluded from result (cannot plot without both axes)
    - Returns array sorted by activityScore ascending (for consistent scatter plot rendering)

    REFACTOR: extract computeSleepHours, extractMinutes, nextDayStr, getISODayOfWeek, getWeekIndex as module-private helpers after all tests pass.

    nextDayStr(dateStr) uses: new Date(dateStr + 'T00:00'), d.setDate(d.getDate()+1), d.toISOString().slice(0,10). Tag the line with inline comment: gsd:allow-ui-clock — calendar display arithmetic, not domain time storage.

    getISODayOfWeek(dateStr): new Date(dateStr + 'T00:00').getDay() returns 0=Sun..6=Sat. Convert to ISO: (getDay() + 6) % 7 gives 0=Mon..6=Sun. Tag with gsd:allow-ui-clock.

    getWeekIndex(dateStr, firstDateStr): Use the formula from RESEARCH.md — count days since first date's Monday. firstDow = getISODayOfWeek(firstDateStr). daysBetween = (new Date(dateStr+'T00:00') - new Date(firstDateStr+'T00:00')) / 86400000. weekIndex = Math.floor((daysBetween + firstDow) / 7). Tag with gsd:allow-ui-clock.
  </behavior>

  <implementation>
    File header: JSDoc comment citing UI-04, D7-05..D7-11, PLAT-11. Note: all functions are pure (zero DOM, zero I/O, zero localStorage).

    CHART_CONFIG at top: Object.freeze({ HEATMAP_COLORS: Object.freeze({ missing:'#e2e8f0', short:'#c7d2fe', target:'#4f46e5', long:'#3730a3' }), TARGET_SLEEP_MIN: 8, TARGET_SLEEP_MAX: 10 }).

    Import: import { timeToMinutes } from './forecast.js'; — for extractMinutes helper.

    extractMinutes(event): null guard, then extract at.slice(-5) if at.length > 5 else at directly, then call timeToMinutes(hhmm). Return result.

    computeSleepHours(dayRecord): if (!dayRecord.wake || !dayRecord.bedtime) return null. wakeMin = extractMinutes(dayRecord.wake); bedMin = extractMinutes(dayRecord.bedtime). if either is null return null. hours = ((wakeMin - bedMin + 1440) % 1440) / 60. Return hours (may be 0 for same-time edge case — acceptable).

    buildSleepLengthSeries: map dayRecords to { date: d.date, sleepHours: computeSleepHours(d), rejected: !!d.rejected }. Filter/sort is not required — preserve input order. Return the mapped array.

    buildHeatmapData: early return [] if empty. Sort by date. Build byDate Map. Iterate from sorted[0].date to last date using nextDayStr loop. Push { date: cur, sleepHours: byDate.has(cur) ? computeSleepHours(byDate.get(cur)) : null, dayOfWeek: getISODayOfWeek(cur), weekIndex: getWeekIndex(cur, sorted[0].date) }. Return cells array.

    buildTimeBandSeries: map dayRecords to { date: d.date, wakeMinutes: d.wake ? extractMinutes(d.wake) : null, bedtimeMinutes: d.bedtime ? extractMinutes(d.bedtime) : null }.

    buildNapStats: filter napDays = dayRecords.filter(d => d.napStart). napDayPct = dayRecords.length ? Math.round(napDays.length / dayRecords.length * 100) : 0. If no napDays return { napDayPct: 0, avgNapStartHHMM: null, avgNapLengthMin: null }. avgStartMin = average of extractMinutes(d.napStart) over napDays. avgNapLengthMin = average of (extractMinutes(d.napEnd) - extractMinutes(d.napStart) + 1440) % 1440 over napDays with both events. Convert avgStartMin back to 'HH:MM' string: String(Math.floor(avgStartMin/60)).padStart(2,'0') + ':' + String(Math.round(avgStartMin%60)).padStart(2,'0').

    buildActivityCorrelation: corr = []. For each [date, score] of Object.entries(activityLog): find day = dayRecords.find(d => d.date === date). If no day or computeSleepHours(day) is null, skip. Push { activityScore: score, sleepHours: computeSleepHours(day) }. Return corr.sort((a,b) => a.activityScore - b.activityScore).

    Commit: GREEN 'feat(07-03): implement chart-data pure transforms [UI-04, D7-05..D7-11]'.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dayRecords → chart-data functions | Typed objects from store; no raw user strings interpolated into output |
| activityLog keys → buildActivityCorrelation | Keys are date strings from CSV import; values are numbers. Uses Object.entries() — no for..in prototype risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03-01 | Tampering | activityLog keys in buildActivityCorrelation | low | mitigate | Use Object.entries(activityLog) not for..in; validates typeof score === 'number' before pushing (RESEARCH §Security Known Threat Patterns) |
| T-07-03-02 | Tampering | Date arithmetic gsd:allow-ui-clock | low | accept | nextDayStr/getISODayOfWeek use Date only for calendar display, not domain time storage; tagged with gsd:allow-ui-clock |
| T-07-03-SC | Tampering | npm installs | low | accept | No new packages; pure module with one in-tree import |
</threat_model>

<verification>
```
node --test tests/unit/chart-data.test.js
```
Must exit 0 (all 10+ tests green). Then:
```
node --test tests/unit/
```
All existing tests still pass (no regression).

Spot checks:
- grep -c 'document\.' js/lib/chart-data.js — must be 0
- grep -c 'Object.freeze' js/lib/chart-data.js — must be at least 1 (CHART_CONFIG)
- grep -c 'gsd:allow-ui-clock' js/lib/chart-data.js — must be at least 3 (nextDayStr, getISODayOfWeek, getWeekIndex)
</verification>

<success_criteria>
- js/lib/chart-data.js exists and exports buildSleepLengthSeries, buildHeatmapData, buildTimeBandSeries, buildNapStats, buildActivityCorrelation as named exports
- CHART_CONFIG is Object.freeze()'d at module top with HEATMAP_COLORS sub-object also frozen
- All 10+ tests in chart-data.test.js pass green
- buildHeatmapData([ makeDay('2025-01-01'), makeDay('2025-01-03') ]).length === 3 (gap filled)
- buildNapStats([]) returns { napDayPct: 0, avgNapStartHHMM: null, avgNapLengthMin: null }
- No DOM access in chart-data.js
- nextDayStr, getISODayOfWeek, and getWeekIndex are all tagged with gsd:allow-ui-clock comment
- Existing unit test suite green: node --test tests/unit/ exits 0
</success_criteria>

<output>
Create `.planning/phases/NW-07-charts-heatmap-accuracy/07-03-SUMMARY.md` when done.
</output>
