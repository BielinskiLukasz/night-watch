---
phase: NW-07
plan: "05"
type: execute
wave: 4
depends_on:
  - "07-04"
files_modified:
  - js/ui/charts-screen.js
  - tests/e2e/charts-screen.spec.js
autonomous: true
requirements:
  - UI-04

must_haves:
  truths:
    - mountChartsScreen renders a cold-start card when validCount < minDays (D7-01 discretion)
    - Five visualization sections render in order when data is sufficient — Sleep Length, Time Bands, Calendar Heatmap, Nap Pattern, Activity Correlation (D7-05)
    - Calendar heatmap cells are SVG <rect> elements with color from CHART_CONFIG.HEATMAP_COLORS (D7-06)
    - Nap pattern section is an HTML stats card not an SVG chart (D7-07)
    - Activity correlation section is hidden when Object.keys(activityLog).length < minDays (D7-11)
    - Stage badge shows "Viewing [Stage Name]" when activeStageId is non-null (D7-18)
    - filterDayRecordsByStage is called with THREE arguments — (allDays, snap.stages || [], snap.activeStageId) — never two (RESEARCH Pitfall 1)
    - All SVG text elements use .textContent property to set stage names and axis labels — never setAttribute (RESEARCH Pitfall 3)
    - mountChartsScreen returns { unsubscribe() } (same pattern as mountHistoryScreen)
  artifacts:
    - js/ui/charts-screen.js
  key_links:
    - charts-screen.js imports buildSleepLengthSeries, buildHeatmapData, buildTimeBandSeries, buildNapStats, buildActivityCorrelation from ../lib/chart-data.js
    - charts-screen.js imports filterDayRecordsByStage from ../lib/stages.js
    - charts-screen.js imports formatTime from ../lib/time.js for axis tick labels
    - All SVG elements created via document.createElementNS(SVG_NS, tag) — dom.js el() not used for SVG
---

<objective>
Implement js/ui/charts-screen.js — the Charts screen with five stacked SVG visualizations plus the stage badge and cold-start gate. This completes UI-04 and the Charts portion of Phase 7.

Purpose: Users see sleep-length trends, time-band distributions, a GitHub-style calendar heatmap, nap pattern stats, and activity correlation all in one scrollable screen.

Output: js/ui/charts-screen.js with mountChartsScreen; Playwright E2E spec passing for UI-04 behaviors.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/sleep-tracker/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/sleep-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/NW-07-charts-heatmap-accuracy/07-CONTEXT.md
@.planning/phases/NW-07-charts-heatmap-accuracy/07-RESEARCH.md
@.planning/phases/NW-07-charts-heatmap-accuracy/07-PATTERNS.md
@js/ui/history-screen.js
@js/ui/today-screen.js
@js/lib/chart-data.js
@js/lib/stages.js
@js/lib/forecast.js
@js/lib/time.js
</context>

## Artifacts This Phase (07-05) Produces

New files:
- `js/ui/charts-screen.js` — exports `mountChartsScreen({ root, eventLog, settings })`

Symbols exported:
- `mountChartsScreen({ root, eventLog, settings })` → `{ unsubscribe() }`

Module-private constants/helpers:
- `SVG_NS` = 'http://www.w3.org/2000/svg'
- `svgEl(tag, attrs)` — local SVG element factory using createElementNS
- `CHART_MARGINS` = Object.freeze({ top:10, right:16, left:32, bottom:24 })
- `renderColdStart(root, remaining)` — renders cold-start card paragraph
- `renderStageBadge(badge, snap)` — updates badge visibility and textContent
- `renderSleepLengthChart(sectionEl, days, snap)` — SVG polyline chart with Y-axis ticks
- `renderTimeBandChart(sectionEl, days, snap)` — SVG scatter with wake/bedtime circles
- `renderHeatmap(sectionEl, days)` — SVG rect grid (GitHub-style)
- `renderNapPattern(sectionEl, stats)` — HTML stats card (no SVG)
- `renderActivityCorrelation(sectionEl, days, activityLog, snap)` — SVG scatter, conditional

<tasks>

<task type="auto">
  <name>Task 1: Implement mountChartsScreen with all five visualizations</name>
  <files>js/ui/charts-screen.js</files>

  <read_first>
    - js/ui/history-screen.js — mount/subscribe/replaceChildren/unsubscribe pattern to mirror exactly
    - js/ui/today-screen.js lines 620-645 — filterDayRecordsByStage three-arg call site (verified)
    - js/lib/chart-data.js — function signatures and return shapes just implemented in 07-03
    - js/lib/stages.js — filterDayRecordsByStage(dayRecords, stages, activeStageId) three-arg signature
    - js/lib/time.js — formatTime(atString, timeFormat) signature (takes full ISO string)
    - .planning/phases/NW-07-charts-heatmap-accuracy/07-RESEARCH.md §SVG Chart Rendering Approach, §Calendar Heatmap Layout Algorithm
    - .planning/phases/NW-07-charts-heatmap-accuracy/07-PATTERNS.md §js/ui/charts-screen.js
    - .planning/phases/NW-07-charts-heatmap-accuracy/07-CONTEXT.md §D7-05..D7-11, D7-17..D7-19
  </read_first>

  <action>
    CREATE js/ui/charts-screen.js. File header JSDoc: Phase 7, UI-04, D7-05..D7-11, D7-17..D7-19.

    Imports: import { filterDayRecordsByStage } from '../lib/stages.js'; import { buildSleepLengthSeries, buildHeatmapData, buildTimeBandSeries, buildNapStats, buildActivityCorrelation } from '../lib/chart-data.js'; import { formatTime } from '../lib/time.js';

    Module-level constants: SVG_NS = 'http://www.w3.org/2000/svg'. CHART_MARGINS = Object.freeze({ top:10, right:16, left:32, bottom:24 }). SLEEP_LEN_SVG = Object.freeze({ w:600, h:180 }). TIME_BAND_SVG = Object.freeze({ w:600, h:200 }). HEATMAP_CFG = Object.freeze({ cellSize:11, cellGap:2, rowOffset:18, colOffset:4 }). HEATMAP_COLORS = Object.freeze({ missing:'#e2e8f0', short:'#c7d2fe', target:'#4f46e5', long:'#3730a3' }). STAGE_BOUNDARY_STROKE = '#94a3b8'.

    Local svgEl helper: function svgEl(tag, attrs={}) { const el = document.createElementNS(SVG_NS, tag); for (const [k,v] of Object.entries(attrs)) el.setAttribute(k,v); return el; }

    renderColdStart(root, remaining): root.replaceChildren(); const p = document.createElement('p'); p.className = 'coldStartNote'; p.textContent = 'Not enough history to show charts — keep logging! (' + remaining + ' more day(s) needed)'; root.appendChild(p);

    renderStageBadge(badge, snap): if (snap.activeStageId) { const stage = (snap.stages||[]).find(s=>s.id===snap.activeStageId); badge.hidden = !stage; if (stage) badge.textContent = 'Viewing: ' + stage.name; } else { badge.hidden = true; }

    renderSleepLengthChart(sectionEl, days, snap): Build SVG with viewBox '0 0 600 180' and width '100%'. Plot sleep hours per day as a polyline. X-scale: evenly spaced across plotW. Y-scale: auto-scaled to data range with 10% padding (D7-09). Use svgEl('polyline') for the line. Add 3-4 Y-axis tick labels using svgEl('text') with .textContent = hours.toFixed(1) + 'h'. Add svgEl('text') for axis title 'Sleep Length (h)' with .textContent. Stage boundary dashed lines when !snap.activeStageId (D7-19): for each stage transition, add svgEl('line') with stroke-dasharray '4 3'. Stage name label via svgEl('text') with .textContent = stage.name. Section heading h2.textContent = 'Sleep Length'.

    renderTimeBandChart(sectionEl, days, snap): Build SVG with viewBox '0 0 600 200'. Y-axis = hours 0-24 (inverted: Y increases downward in SVG, map 0h→MARGIN.top, 24h→MARGIN.top+plotH). X-axis = date index. For each day: wake circle = svgEl('circle') with cy = yScale(wakeMinutes/60), fill '#4f46e5'. Bedtime circle = svgEl('circle') fill '#94a3b8'. Forecast min/max band: if forecast available for that day (D7-10 discretion — omit forecast bands in v1 to avoid complexity; just plot actual wake/bedtime points). Y-axis ticks: 0,6,12,18,24h labels. Section heading h2.textContent = 'Wake & Bedtime Bands'.

    renderHeatmap(sectionEl, days): cells = buildHeatmapData(days). If empty, show placeholder. Total weeks = max weekIndex + 1. SVG width = colOffset + (weeks * (cellSize + cellGap)). SVG height = rowOffset + (7 * (cellSize + cellGap)). For each cell: x = HEATMAP_CFG.colOffset + cell.weekIndex*(HEATMAP_CFG.cellSize+HEATMAP_CFG.cellGap). y = HEATMAP_CFG.rowOffset + cell.dayOfWeek*(HEATMAP_CFG.cellSize+HEATMAP_CFG.cellGap). Color = cell.sleepHours === null ? HEATMAP_COLORS.missing : (cell.sleepHours < 8 ? HEATMAP_COLORS.short : cell.sleepHours <= 10 ? HEATMAP_COLORS.target : HEATMAP_COLORS.long). rect = svgEl('rect', { x:String(x), y:String(y), width:String(HEATMAP_CFG.cellSize), height:String(HEATMAP_CFG.cellSize), fill:color, rx:'2' }). svg.appendChild(rect). Day labels (Mon-Sun) on left column: for i in 0..6, label using svgEl('text') with .textContent = ['M','T','W','T','F','S','S'][i]. Section heading h2.textContent = 'Sleep Calendar'.

    renderNapPattern(sectionEl, stats): Build HTML card (div.napPatternCard). Three stat items: pct days with nap (stats.napDayPct.toFixed(0) + '%'), avg nap start (stats.avgNapStartHHMM ?? '—'), avg nap length (stats.avgNapLengthMin ? Math.floor(stats.avgNapLengthMin/60) + 'h ' + (stats.avgNapLengthMin%60) + 'm' : '—'). All values via element.textContent. Section heading h2.textContent = 'Nap Pattern'.

    renderActivityCorrelation(sectionEl, days, activityLog, snap): points = buildActivityCorrelation(days, activityLog). Hide sectionEl if points.length < snap.minDays (D7-11). Otherwise: SVG scatter with x=activityScore, y=sleepHours. Both axes auto-scaled. Circles via svgEl('circle'). Section heading h2.textContent = 'Activity vs Sleep'.

    mountChartsScreen({ root, eventLog, settings }): root.replaceChildren(). Create permanent containers: stageBadge (p.stageChip, hidden), and five section elements (div.chartSection each). root.replaceChildren(stageBadge, section1, section2, section3, section4, section5).

    render(): snap = settings.get(). allDays = eventLog.daysBySubjectiveNight(snap.cutoverHour). days = snap.activeStageId ? filterDayRecordsByStage(allDays, snap.stages||[], snap.activeStageId) : allDays. validCount = days.filter(d=>!d.rejected).length. if (validCount < snap.minDays) { renderColdStart(root, snap.minDays - validCount); return; } renderStageBadge(stageBadge, snap). Also get activityLog from eventLog.get() or from the db blob — check how today-screen.js accesses it. Render each section: clear each section's SVG/content then call the render sub-function. [If eventLog does not expose activityLog directly, check import-export.js or db-shape.js for how to access it — the activityLog is stored in the db blob per D5-17.]

    Return { unsubscribe() { unsubLog(); unsubSettings(); } } after wiring render() to eventLog.subscribe(render) and settings.subscribe(render).

    CRITICAL SVG TEXT INVARIANT: Every SVG <text> element that displays a user-controlled string (stage names, subject name) MUST set content via .textContent property, not via setAttribute. Axis labels and legend labels are numeric or static strings — they may use .textContent directly with safe numeric/literal values.

    filterDayRecordsByStage MUST be called with THREE arguments: (allDays, snap.stages||[], snap.activeStageId). NEVER two. (RESEARCH Pitfall 1 — verified from js/lib/stages.js source.)

    formatTime usage: when displaying axis tick times (for time-band chart), call formatTime(fullISOString, snap.timeFormat). formatTime takes a full ISO string 'YYYY-MM-DDTHH:MM', not a bare HH:MM. For numeric minute-to-display conversion, build a synthetic ISO string or use Math.floor/modulo directly for display without formatTime.
  </action>

  <acceptance_criteria>
    - js/ui/charts-screen.js exists and exports mountChartsScreen as a named export
    - grep -c 'filterDayRecordsByStage' js/ui/charts-screen.js outputs at least 1
    - grep -c 'snap.stages' js/ui/charts-screen.js outputs at least 1 (three-arg call confirmed)
    - grep -c 'innerHTML' js/ui/charts-screen.js outputs 0 (no innerHTML)
    - grep -c 'createElementNS' js/ui/charts-screen.js outputs at least 5 (multiple SVG elements)
    - grep -c 'Object.freeze' js/ui/charts-screen.js outputs at least 3 (CHART_MARGINS, HEATMAP_CFG, HEATMAP_COLORS)
    - grep -c 'unsubscribe' js/ui/charts-screen.js outputs at least 1 (return shape)
    - grep -c '\.textContent' js/ui/charts-screen.js outputs at least 5 (text security invariant)
  </acceptance_criteria>

  <verify>
    <automated>node -e "const fs=require('fs'); const src=fs.readFileSync('js/ui/charts-screen.js','utf8'); if(src.includes('innerHTML')) throw new Error('innerHTML found'); if(!src.includes('createElementNS')) throw new Error('no createElementNS'); console.log('Security check OK');"</automated>
  </verify>

  <done>
    js/ui/charts-screen.js exists. mountChartsScreen is exported. All five visualization sections render when validCount >= minDays. Cold-start card renders when data is insufficient. Stage badge shows/hides correctly. No innerHTML anywhere in the file. All SVG text labels use .textContent property.
  </done>
</task>

<task type="auto">
  <name>Task 2: Finalize charts-screen E2E spec and run full suite</name>
  <files>tests/e2e/charts-screen.spec.js</files>

  <read_first>
    - tests/e2e/charts-screen.spec.js — the stub from 07-01; update assertions to match implementation
    - tests/e2e/bottom-nav.spec.js — navigation pattern for switching to charts tab
    - tests/e2e/stages.spec.js — import flow to get enough data for non-cold-start path
    - .planning/phases/NW-07-charts-heatmap-accuracy/07-CONTEXT.md §D7-05 — chart order and section headings
  </read_first>

  <action>
    Update tests/e2e/charts-screen.spec.js to test the implemented charts screen. The spec must cover:

    1. "cold-start: charts screen shows cold-start card when no data logged" — navigate to charts tab via #bottom-nav button[data-tab="charts"] click; expect page.locator('.coldStartNote') to be visible; expect page.locator('.chartSection') to have count 0.

    2. "charts screen section present after navigating to charts tab" — navigate to charts tab; #charts-screen is visible (display not none); page.locator('#charts-screen') is attached to DOM.

    3. (Optional, if the test environment can load fixture data) "charts screen renders sleep length section heading after import" — import a small CSV fixture, navigate to charts tab, expect page.getByText('Sleep Length') to be visible.

    Navigation pattern: use page.locator('#bottom-nav button[data-tab="charts"]').click() for navigation. This is the production navigation path, not a direct style manipulation.

    If the E2E environment cannot reliably import data (due to FileReader or file system constraints in headless), focus on the cold-start path which requires no data. The two cold-start assertions are sufficient for regression guarding UI-04.

    Update the spec using Edit (scoped) if the stub already has the right structure, or rewrite it if needed. Ensure the spec file uses the same beforeEach pattern as other E2E specs.
  </action>

  <acceptance_criteria>
    - npx playwright test tests/e2e/charts-screen.spec.js exits 0
    - The spec references #bottom-nav button[data-tab="charts"] for navigation
    - The spec references .coldStartNote for cold-start assertion
    - npx playwright test (full suite except accuracy-screen.spec.js) exits 0 — no regression
  </acceptance_criteria>

  <verify>
    <automated>npx playwright test tests/e2e/charts-screen.spec.js tests/e2e/bottom-nav.spec.js --reporter=line 2>&1 | tail -10</automated>
  </verify>

  <done>
    npx playwright test tests/e2e/charts-screen.spec.js exits 0. The Charts screen cold-start behavior is verified by E2E. Full E2E suite (excluding accuracy-screen.spec.js, which is addressed in 07-06) passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stage name → SVG text element | User-defined stage name from settings; must go through .textContent property only |
| activityLog keys → scatter plot | Keys are date strings from CSV import; values are numeric scores |
| days array → chart functions | Typed objects from store; numeric SVG attributes are derived from computed numbers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05-01 | Tampering | Stage name in SVG text element | high | mitigate | All SVG <text> content for stage names set via .textContent = stage.name (property), never via setAttribute or innerHTML (RESEARCH Pitfall #3, ASVS V5) |
| T-07-05-02 | Tampering | activityLog key used as display | low | mitigate | Activity scores are typed as numbers; date keys from activityLog are not rendered to DOM — only the numeric score is used as SVG attribute value |
| T-07-05-03 | Tampering | Subject name in chart title | medium | mitigate | Subject name in chart aria-label or title element must use .textContent or setAttribute on aria-label (static string context) — never innerHTML |
| T-07-05-04 | Information Disclosure | Cold-start remaining count | low | accept | Cold-start message shows minDays - validCount (a calculated integer) — no sensitive data exposed |
| T-07-05-SC | Tampering | npm installs | low | accept | No new npm packages; Playwright already in devDependencies |
</threat_model>

<verification>
```
node --test tests/unit/
npx playwright test tests/e2e/charts-screen.spec.js tests/e2e/bottom-nav.spec.js
```
Both commands must exit 0.

Security checks:
```
node -e "const s=require('fs').readFileSync('js/ui/charts-screen.js','utf8'); const n=(s.match(/innerHTML/g)||[]).length; if(n>0) throw new Error('innerHTML count: '+n); console.log('innerHTML=0 OK');"
node -e "const s=require('fs').readFileSync('js/ui/charts-screen.js','utf8'); const n=(s.match(/createElementNS/g)||[]).length; if(n<5) throw new Error('createElementNS count too low: '+n); console.log('createElementNS OK: '+n);"
```
</verification>

<success_criteria>
- js/ui/charts-screen.js exists with mountChartsScreen export and { unsubscribe } return
- Five visualization functions implemented: sleep-length line chart, time-band scatter, calendar heatmap, nap pattern card, activity correlation scatter
- Cold-start card renders when validCount < minDays — no crash
- Activity correlation section hidden when activityLog has fewer than minDays entries
- Stage badge shows "Viewing [Stage Name]" when stage is active (per D7-18)
- filterDayRecordsByStage called with three args everywhere
- All SVG text content via .textContent property — zero innerHTML
- npx playwright test tests/e2e/charts-screen.spec.js exits 0
- Full unit suite: node --test tests/unit/ exits 0
</success_criteria>

<output>
Create `.planning/phases/NW-07-charts-heatmap-accuracy/07-05-SUMMARY.md` when done.
</output>
