---
phase: NW-07
plan: "02"
type: tdd
wave: 2
depends_on:
  - "07-01"
files_modified:
  - js/lib/accuracy.js
  - tests/unit/accuracy.test.js
autonomous: true
requirements:
  - UI-05

must_haves:
  truths:
    - computeAccuracy(dayRecords, settings) is a pure function with zero DOM and zero I/O
    - Empty dayRecords returns AccuracyResult with all totals and counts at zero
    - Look-ahead bias is prevented — history passed to forecast() is always sorted.slice(0, i)
    - Nap rows count only days where napStart or napEnd is logged (D7-15)
    - pct fields are 0-100 numbers (percentages), not 0-1 fractions
    - filterDayRecordsByStage is NOT called inside accuracy.js — caller filters before passing dayRecords
    - "node --test tests/unit/accuracy.test.js" exits 0 (GREEN)
  artifacts:
    - js/lib/accuracy.js
  key_links:
    - accuracy.js imports { forecast, timeToMinutes } from './forecast.js'
    - computeAccuracy is the sole named export; all helpers are module-private
    - AccuracyResult shape exactly matches D7-14 grid: { wake, bedtime, napStart, napEnd } each with { total, withinDelta:{count,pct}, withinHalfDelta:{count,pct}, insideBand:{count,pct} }
---

<objective>
Implement js/lib/accuracy.js using strict RED→GREEN→REFACTOR TDD cycles, satisfying the stubs created in 07-01. This is the pure backtesting engine for the Accuracy screen (D7-16, UI-05).

Purpose: Accuracy screen needs a pure, unit-tested function that retroactively scores all historical forecasts. TDD ensures the I/O contract is pinned before DOM wiring.

Output: js/lib/accuracy.js with computeAccuracy(dayRecords, settings) → AccuracyResult; all accuracy.test.js stubs passing GREEN.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/sleep-tracker/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/sleep-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/NW-07-charts-heatmap-accuracy/07-CONTEXT.md
@.planning/phases/NW-07-charts-heatmap-accuracy/07-RESEARCH.md
@.planning/phases/NW-07-charts-heatmap-accuracy/07-PATTERNS.md
@js/lib/forecast.js
@js/lib/stages.js
@tests/unit/accuracy.test.js
</context>

## Artifacts This Phase (07-02) Produces

New files:
- `js/lib/accuracy.js` — exports `computeAccuracy(dayRecords, settings) → AccuracyResult`

Symbols exported:
- `computeAccuracy(dayRecords, settings)` — named export, the sole public API

Module-private (not exported):
- `ACCURACY_CONFIG` — Object.freeze({ EVENT_TYPES: [...], NAP_TYPES: new Set([...]) })
- `extractActualMinutes(event)` — extracts HH:MM from event.at (ISO or bare HH:MM), returns integer minutes
- `timeToMinutes(hhmm)` — re-exported from forecast.js via import, not re-declared
- `buildAccuracyResult(counters)` — converts raw counters to AccuracyResult with pct fields

<feature>
  <name>computeAccuracy — retroactive backtesting engine</name>
  <files>js/lib/accuracy.js, tests/unit/accuracy.test.js</files>

  <behavior>
    RED phase (stubs already written in 07-01): run node --test tests/unit/accuracy.test.js → fails with ERR_MODULE_NOT_FOUND.

    GREEN phase — implement js/lib/accuracy.js to satisfy all stubs:

    1. Empty dayRecords → { wake:{total:0,...}, bedtime:{total:0,...}, napStart:{total:0,...}, napEnd:{total:0,...} }
       Each event type has: total:0, withinDelta:{count:0,pct:0}, withinHalfDelta:{count:0,pct:0}, insideBand:{count:0,pct:0}

    2. Fewer than minDays+1 records → loop body never executes → all totals zero

    3. Perfect prediction (actual.at === predicted central, delta === 0) → withinDelta.pct === 100, withinHalfDelta.pct === 100, insideBand.pct === 100

    4. Prediction at exactly maxDelta boundary (delta === maxDelta) → withinDelta.pct === 100, withinHalfDelta.pct === 0 (because delta > maxDelta/2)

    5. Prediction outside maxDelta (delta > maxDelta) → withinDelta.pct === 0

    6. Nap rows: a day with napStart:null does NOT increment napStart.total

    7. When forecast() returns isColdStart:true for a history slice → that day is skipped (total unchanged)

    8. All pct fields are Math.round(count/total*100) — integer 0-100, never NaN (guard total===0 → pct:0)

    LOOP INVARIANT (look-ahead bias prevention, RESEARCH Pitfall #2):
      for (let i = minDays; i < sorted.length; i++) {
        const history = sorted.slice(0, i);   // only BEFORE day i
        const actual  = sorted[i];             // the day being scored
        const pred = forecast(history, settings);
        if (pred.isColdStart) continue;
        // score per event type
      }

    NAP DAY COUNTING (D7-15):
      Only increment napStart.total / napEnd.total when actual[type] is non-null for that type.
      Per-day: iterate EVENT_TYPES; for napStart/napEnd, skip if actual.napStart and actual.napEnd are both null.

    CROSS-MIDNIGHT NOTE (RESEARCH Risk 2): time comparison is naive minutes-since-midnight (0-1439). This is a known v1 limitation. Add a JSDoc comment on extractActualMinutes noting the midnight-wrap limitation.

    BAND MODE: when pred[type].probabilityBand is present, check if actualMinutes falls between min and max of the band times. withinDelta and withinHalfDelta are NOT incremented in band mode.

    REFACTOR: extract buildAccuracyResult(counters) private helper; extract extractActualMinutes(event) private helper. ACCURACY_CONFIG frozen at module top.
  </behavior>

  <implementation>
    File header: JSDoc comment citing UI-05, D7-12..D7-16, PLAT-11.

    Import at top of file: import { forecast, timeToMinutes } from './forecast.js';

    ACCURACY_CONFIG frozen object: EVENT_TYPES array ['wake','bedtime','napStart','napEnd'], NAP_TYPES = new Set(['napStart','napEnd']).

    extractActualMinutes(event): null guard → if (!event || !event.at) return null. Extract HH:MM from event.at: if event.at.length > 5, take event.at.slice(-5); else use event.at directly. Then call timeToMinutes(hhmm). Return the result.

    buildAccuracyResult(counters): for each type in EVENT_TYPES, compute pct fields as: total === 0 ? 0 : Math.round(count/total*100). Return the four-key object.

    computeAccuracy(dayRecords, settings): destructure { minDays, maxDelta } from settings. Build sorted copy: [...dayRecords].sort by date string lexicographic. Initialize counters object. Loop for (let i = minDays; i < sorted.length; i++). Call forecast(sorted.slice(0, i), settings). Skip if pred.isColdStart. For each type in EVENT_TYPES: get actualEvent = actual[type]. If null, continue. If NAP_TYPES has type and actual.napStart === null and actual.napEnd === null, continue. Get prediction = pred[type]. If no prediction or no central and no probabilityBand, continue. Increment counters[type].total. Get actualMinutes via extractActualMinutes(actualEvent); if null, continue. If probabilityBand: compute bandMin/Max from prediction.probabilityBand.map(e=>timeToMinutes(e.time)); if actualMinutes in [bandMin,bandMax], increment insideBand. Else (central mode): delta = Math.abs(actualMinutes - timeToMinutes(prediction.central)); if delta <= maxDelta, increment withinDelta; if delta <= maxDelta/2, increment withinHalfDelta; check prediction.min/max for insideBand. Return buildAccuracyResult(counters).

    Commit sequence: RED commit 'test(07-02): add failing stubs for computeAccuracy [already in 07-01]', then GREEN commit 'feat(07-02): implement computeAccuracy pure backtesting engine [UI-05, D7-16]'.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dayRecords input → computeAccuracy | Caller passes day records from eventLog; no direct user string interpolation in pure function |
| forecast() output → counter increment | forecast() output is consumed numerically; no DOM or I/O exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02-01 | Tampering | accuracy.js input | low | accept | Pure function; dayRecords are typed objects from controlled store, not raw user strings; no DOM in this module |
| T-07-02-02 | Information Disclosure | pct calculation NaN | low | mitigate | Guard: total === 0 → pct = 0 (no NaN leaks to UI) |
| T-07-02-SC | Tampering | npm installs | low | accept | No new packages; node:test is built-in; forecast.js is in-tree |
</threat_model>

<verification>
```
node --test tests/unit/accuracy.test.js
```
Must exit 0 with all tests passing (GREEN). Then verify no regression:
```
node --test tests/unit/
```
All existing tests (forecast, stages, db-shape, etc.) still pass.
</verification>

<success_criteria>
- js/lib/accuracy.js exists and exports computeAccuracy as a named export
- ACCURACY_CONFIG is Object.freeze()'d at module top
- All 8+ tests in accuracy.test.js pass (node --test tests/unit/accuracy.test.js exits 0)
- computeAccuracy([],{minDays:7,maxDelta:30,windowDays:7}) returns object with wake.total===0 (verifiable inline)
- No DOM access (grep -c 'document\.' js/lib/accuracy.js === 0)
- No localStorage access (grep -c 'localStorage' js/lib/accuracy.js === 0)
- Existing unit tests unaffected: node --test tests/unit/ exits 0
</success_criteria>

<output>
Create `.planning/phases/NW-07-charts-heatmap-accuracy/07-02-SUMMARY.md` when done.
</output>
