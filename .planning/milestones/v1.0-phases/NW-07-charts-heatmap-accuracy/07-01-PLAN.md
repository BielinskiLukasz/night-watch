---
phase: NW-07
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/accuracy.test.js
  - tests/unit/chart-data.test.js
  - tests/e2e/bottom-nav.spec.js
  - tests/e2e/charts-screen.spec.js
  - tests/e2e/accuracy-screen.spec.js
autonomous: true
requirements:
  - UI-04
  - UI-05
  - UI-06

must_haves:
  truths:
    - Five failing test stubs exist for accuracy.test.js covering computeAccuracy I/O contracts
    - Five failing test stubs exist for chart-data.test.js covering each transform function
    - Three Playwright E2E stub files exist for bottom-nav, charts-screen, and accuracy-screen
    - "node --test tests/unit/accuracy.test.js" exits non-zero (RED state confirmed)
    - "node --test tests/unit/chart-data.test.js" exits non-zero (RED state confirmed)
  artifacts:
    - tests/unit/accuracy.test.js
    - tests/unit/chart-data.test.js
    - tests/e2e/bottom-nav.spec.js
    - tests/e2e/charts-screen.spec.js
    - tests/e2e/accuracy-screen.spec.js
  key_links:
    - accuracy.test.js imports from js/lib/accuracy.js (not yet created — should produce MODULE_NOT_FOUND)
    - chart-data.test.js imports from js/lib/chart-data.js (not yet created — should produce MODULE_NOT_FOUND)
    - E2E stubs must reference correct page locators matching future DOM: #bottom-nav, #charts-screen, #accuracy-screen
---

<objective>
Create failing test stub files (RED state) that define the I/O contracts for the two new pure-logic modules and the three new UI screens before any implementation exists.

Purpose: Establish RED state for TDD discipline (PLAT-11). The test files encode the required contracts and acceptance criteria that the subsequent implementation plans must satisfy. E2E stubs provide regression guards for UI-06, UI-04, UI-05.

Output: Five new test files — two unit stubs (accuracy.test.js, chart-data.test.js) and three E2E stubs (bottom-nav.spec.js, charts-screen.spec.js, accuracy-screen.spec.js). No production code yet.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/sleep-tracker/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/sleep-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/NW-07-charts-heatmap-accuracy/07-CONTEXT.md
@.planning/phases/NW-07-charts-heatmap-accuracy/07-RESEARCH.md
@.planning/phases/NW-07-charts-heatmap-accuracy/07-PATTERNS.md
@tests/unit/forecast.test.js
@tests/unit/stages.test.js
@tests/e2e/stages.spec.js
</context>

## Artifacts This Phase (07-01) Produces

New files:
- `tests/unit/accuracy.test.js` — failing stubs for `computeAccuracy(dayRecords, settings)`
- `tests/unit/chart-data.test.js` — failing stubs for `buildSleepLengthSeries`, `buildHeatmapData`, `buildTimeBandSeries`, `buildNapStats`, `buildActivityCorrelation`
- `tests/e2e/bottom-nav.spec.js` — Playwright stub for UI-06 navigation
- `tests/e2e/charts-screen.spec.js` — Playwright stub for UI-04 charts
- `tests/e2e/accuracy-screen.spec.js` — Playwright stub for UI-05 accuracy grid

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create failing unit stubs for accuracy.js (RED)</name>
  <files>tests/unit/accuracy.test.js</files>

  <read_first>
    - tests/unit/forecast.test.js — existing unit test structure and import conventions to mirror
    - tests/unit/stages.test.js — pattern for pure-function test layout
    - .planning/phases/NW-07-charts-heatmap-accuracy/07-RESEARCH.md §Accuracy Backtesting Design — AccuracyResult shape and edge-case list
    - .planning/phases/NW-07-charts-heatmap-accuracy/07-CONTEXT.md §D7-12..D7-16 — locked decisions
  </read_first>

  <behavior>
    - import { computeAccuracy } from '../../js/lib/accuracy.js' — module does not exist yet; import must fail (RED)
    - Test "empty dayRecords → all totals zero": computeAccuracy([], { minDays:7, maxDelta:30, windowDays:7 }) returns { wake:{total:0,...}, bedtime:{total:0,...}, napStart:{total:0,...}, napEnd:{total:0,...} }
    - Test "fewer than minDays+1 records → loop never runs → all totals zero": 3-element dayRecords with minDays=7 returns all zeros
    - Test "perfect prediction within max_delta → 100% withinDelta": single day with wake prediction central 07:00, actual wake 07:00; withinDelta.pct === 100
    - Test "prediction at exactly max_delta boundary → 100% withinDelta, 0% withinHalfDelta": delta === maxDelta exactly
    - Test "prediction outside max_delta → 0% withinDelta": delta > maxDelta on all rows
    - Test "nap rows skip no-nap days → napStart.total counts only days with napStart": 2 total days, 1 with nap → napStart.total === 1
    - Test "isColdStart forecast result → row skipped, total unchanged": inject scenario where forecast() returns isColdStart:true mid-loop
    - Test "pct values are 0-100 numbers, not fractions": all pct fields are percentages not decimals
  </behavior>

  <action>
    Write tests/unit/accuracy.test.js using node:test and node:assert. Mirror the header comment style of forecast.test.js (phase, requirement IDs, decisions). Import path: import { computeAccuracy } from '../../js/lib/accuracy.js'.

    Structure: one top-level describe('computeAccuracy — UI-05, D7-12..D7-16') with nested it() blocks for each behavior listed. Use minimal synthetic dayRecords fixtures — avoid loading the full test database. Fixtures need only the fields that computeAccuracy reads: date (YYYY-MM-DD string), wake/bedtime/napStart/napEnd (null or { at: 'YYYY-MM-DDTHH:MM' }), rejected (boolean).

    Helper fixture builder pattern (seen in forecast.test.js): function makeDay(date, { wake, bedtime, napStart, napEnd, rejected }) returning a minimal record. Use this for all test cases.

    For the "perfect prediction" test, the fixture needs at least minDays+1 records. Use minDays=2 in the settings fixture for brevity — set { minDays:2, maxDelta:30, windowDays:7 }. Days 0 and 1 are history; day 2 is the actual. Day 2's wake event.at = 'YYYY-MM-DDTHH:MM' where HH:MM exactly matches what forecast() would predict. (Note: the test does NOT need to pin forecast() output — it only needs to exercise the counter increment logic. Use a controlled fixture where the delta is zero.)

    Because the module does not exist yet, all tests MUST fail at import time — confirm this is acceptable. Write the test file to be correct once the module exists, not to work around the missing module.

    Do NOT use fenced code blocks in this action. Describe the structure in prose with identifiers.
  </action>

  <verify>
    <automated>node --test tests/unit/accuracy.test.js 2>&1 | grep -E "(ERR_MODULE_NOT_FOUND|FAIL|Error)" | head -5</automated>
  </verify>

  <done>
    tests/unit/accuracy.test.js exists with at least 8 it() blocks inside a describe block. Running "node --test tests/unit/accuracy.test.js" exits with non-zero status due to ERR_MODULE_NOT_FOUND (js/lib/accuracy.js does not exist). All existing tests in tests/unit/ still pass: "node --test tests/unit/ --ignore=tests/unit/accuracy.test.js" exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create failing unit stubs for chart-data.js (RED)</name>
  <files>tests/unit/chart-data.test.js</files>

  <read_first>
    - tests/unit/forecast.test.js — module structure and describe/it conventions
    - .planning/phases/NW-07-charts-heatmap-accuracy/07-RESEARCH.md §TDD Opportunities — chart-data.js test case list
    - .planning/phases/NW-07-charts-heatmap-accuracy/07-PATTERNS.md §js/lib/chart-data.js — return shapes for all 5 functions
    - .planning/phases/NW-07-charts-heatmap-accuracy/07-CONTEXT.md §D7-05..D7-11
  </read_first>

  <behavior>
    - import { buildSleepLengthSeries, buildHeatmapData, buildTimeBandSeries, buildNapStats, buildActivityCorrelation } from '../../js/lib/chart-data.js' — module does not exist yet
    - buildSleepLengthSeries test "empty → []": buildSleepLengthSeries([]) deep equals []
    - buildSleepLengthSeries test "day with wake + bedtime → sleepHours computed": fixture with wake 07:00, bedtime previous day 22:00 → sleepHours === 9
    - buildSleepLengthSeries test "day with missing wake → sleepHours null": fixture with null wake → entry has sleepHours === null
    - buildHeatmapData test "empty → []": buildHeatmapData([]) deep equals []
    - buildHeatmapData test "gap filling: 3 days with 1 missing middle → 3 cells, middle has sleepHours null": dates ['2025-01-01','2025-01-03'] as input → 3 cells returned
    - buildHeatmapData test "dayOfWeek assignment: 2025-01-06 is Monday → dayOfWeek === 0": known Monday date maps to 0 (ISO Mon=0)
    - buildNapStats test "no nap days → napDayPct === 0, avgNapStartHHMM null, avgNapLengthMin null": all days have napStart:null
    - buildNapStats test "one nap day in 5 → napDayPct === 20": 1 of 5 days has napStart
    - buildActivityCorrelation test "empty activityLog → []": buildActivityCorrelation(days, {}) deep equals []
    - buildTimeBandSeries test "day with wake + bedtime → wakeMinutes and bedtimeMinutes present": correct minute values
  </behavior>

  <action>
    Write tests/unit/chart-data.test.js using node:test and node:assert. Import path: import { buildSleepLengthSeries, buildHeatmapData, buildTimeBandSeries, buildNapStats, buildActivityCorrelation } from '../../js/lib/chart-data.js'.

    Use one top-level describe('chart-data transforms — UI-04, D7-05..D7-11') with nested describe blocks per function. Each nested describe has 2-3 it() blocks covering the behaviors listed.

    Fixture helper: a makeDay(date, overrides) function building a minimal record with date, wake, bedtime, napStart, napEnd fields where each is null or { id, type, at: 'YYYY-MM-DDTHH:MM' }. Use short ISO dates (2025-01-01 etc.) for clarity.

    For buildHeatmapData gap-fill test: pass two records with dates '2025-01-01' and '2025-01-03'. Assert the returned array has length 3, the middle cell has date '2025-01-02' and sleepHours === null.

    For buildNapStats tests: use simple day arrays; fixture with 5 days, 1 having napStart event at '2025-01-01T13:00' and napEnd at '2025-01-01T14:30' → napDayPct === 20, avgNapLengthMin === 90.

    The module does not exist — all tests fail at import. This is the expected RED state.
  </action>

  <verify>
    <automated>node --test tests/unit/chart-data.test.js 2>&1 | grep -E "(ERR_MODULE_NOT_FOUND|FAIL|Error)" | head -5</automated>
  </verify>

  <done>
    tests/unit/chart-data.test.js exists with at least 10 it() blocks across 5 nested describe blocks. Running "node --test tests/unit/chart-data.test.js" exits non-zero (ERR_MODULE_NOT_FOUND for js/lib/chart-data.js). All other unit tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Playwright E2E stub specs for UI-06, UI-04, UI-05</name>
  <files>
    tests/e2e/bottom-nav.spec.js
    tests/e2e/charts-screen.spec.js
    tests/e2e/accuracy-screen.spec.js
  </files>

  <read_first>
    - tests/e2e/stages.spec.js — Playwright spec structure, baseURL pattern, page.goto usage
    - tests/e2e/history.spec.js — locator patterns (getByRole, locator('#id'))
    - .planning/phases/NW-07-charts-heatmap-accuracy/07-RESEARCH.md §Validation Architecture — phase requirement to test map
    - .planning/phases/NW-07-charts-heatmap-accuracy/07-CONTEXT.md §D7-01..D7-04 (nav), D7-14 (accuracy grid)
  </read_first>

  <action>
    Create three Playwright E2E spec files. All three specs must import from @playwright/test, use test.describe, and reference DOM elements by their Phase 7 IDs. They will fail at execution time because the production DOM does not yet have #bottom-nav, #charts-screen, or #accuracy-screen — this is expected (RED state for E2E guards).

    bottom-nav.spec.js: One describe('Bottom navigation — UI-06, D7-01..D7-04') with tests:
    - "renders four tab buttons": page.locator('#bottom-nav button[data-tab]').count() resolves to 4
    - "Today tab is active by default": page.locator('#bottom-nav button[data-tab="today"][aria-selected="true"]') is visible
    - "clicking Charts tab shows charts screen": click button[data-tab="charts"], then page.locator('#charts-screen') has display not 'none'
    - "clicking Accuracy tab shows accuracy screen": click button[data-tab="accuracy"], then page.locator('#accuracy-screen') has display not 'none'
    - "clicking History tab shows history screen and hides today screen": locator check

    charts-screen.spec.js: One describe('Charts screen — UI-04, D7-05..D7-11') with tests:
    - "shows cold-start card when insufficient data": navigate to charts tab, element with class coldStartNote is visible
    - "charts section present after switching to charts tab": page.locator('#charts-screen') is attached to DOM

    accuracy-screen.spec.js: One describe('Accuracy screen — UI-05, D7-12..D7-16') with tests:
    - "shows cold-start card when insufficient data": navigate to accuracy tab, coldStartNote visible
    - "accuracy screen present in DOM": page.locator('#accuracy-screen') is attached

    All three files must use test.beforeEach to navigate to the app (same pattern as existing E2E specs — check the stages.spec.js for the exact baseURL reference). Use page.goto('/') or the established pattern from existing specs.

    The test bodies should use await expect(...).toBeVisible() or .toHaveCount() — standard Playwright assertions. Tests will fail because the production DOM lacks these elements, which is the intended RED state.

    Do not include test.skip() calls — specs must actively attempt to run and fail.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs'); ['tests/e2e/bottom-nav.spec.js','tests/e2e/charts-screen.spec.js','tests/e2e/accuracy-screen.spec.js'].forEach(f => { if(!fs.existsSync(f)) throw new Error('Missing: '+f); console.log('OK:', f); })"</automated>
  </verify>

  <done>
    Three E2E spec files exist. Each contains at least 1 test.describe block and at least 1 it/test block with a Playwright assertion. Files parse without syntax errors: "node --input-type=module" syntax check passes for all three. The files reference DOM IDs #bottom-nav, #charts-screen, and #accuracy-screen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture → module import | Node.js module resolution; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01-01 | Tampering | Test fixture data | low | accept | Test fixtures are hardcoded literals — no user input in test stubs |
| T-07-01-SC | Tampering | npm/devDependencies | low | accept | No new packages installed in this plan; node:test is built-in |
</threat_model>

<verification>
Run after all tasks complete:

```
node --test tests/unit/accuracy.test.js 2>&1 | grep -E "ERR_MODULE_NOT_FOUND"
node --test tests/unit/chart-data.test.js 2>&1 | grep -E "ERR_MODULE_NOT_FOUND"
node --test tests/unit/ --ignore=accuracy.test.js --ignore=chart-data.test.js 2>&1 | tail -5
```

First two commands must show ERR_MODULE_NOT_FOUND (RED state). Third command must show all passing (no regression on existing tests).
</verification>

<success_criteria>
- tests/unit/accuracy.test.js: exists, 8+ it() blocks, fails with ERR_MODULE_NOT_FOUND for js/lib/accuracy.js
- tests/unit/chart-data.test.js: exists, 10+ it() blocks, fails with ERR_MODULE_NOT_FOUND for js/lib/chart-data.js
- tests/e2e/bottom-nav.spec.js: exists, 5 test cases referencing #bottom-nav
- tests/e2e/charts-screen.spec.js: exists, 2 test cases referencing #charts-screen
- tests/e2e/accuracy-screen.spec.js: exists, 2 test cases referencing #accuracy-screen
- Existing unit tests (all files except accuracy.test.js and chart-data.test.js) pass green
</success_criteria>

<output>
Create `.planning/phases/NW-07-charts-heatmap-accuracy/07-01-SUMMARY.md` when done.
</output>
