---
phase: NW-08
plan: 04
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - index.html
  - style.css
  - js/ui/charts-screen.js
  - js/ui/settings-modal.js
autonomous: true
requirements:
  - PLAT-06

must_haves:
  truths:
    - All inline SVG icons in index.html (bottom-nav tabs, quick-log buttons, settings trigger) use viewBox="0 0 24 24" with stroke-width="1.5" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round"
    - Settings gear in header is replaced with a stroke-based SVG icon (not the existing filled-path Material icon)
    - style.css contains @keyframes drawLine (stroke-dashoffset 1→0) and @keyframes fadeInEl (opacity 0→1)
    - style.css contains .chart-line class (stroke-dasharray 1, stroke-dashoffset 1, animation drawLine 300ms)
    - style.css contains .chart-el-enter class (opacity 0, animation fadeInEl 200ms)
    - charts-screen.js polyline elements have pathLength="1" attribute and class "chart-line"
    - charts-screen.js heatmap rect elements have class "chart-el-enter"
    - Settings modal fieldsets in index.html are reorganized into five groups per D8-14 and UI-SPEC
    - style.css contains Settings modal group spacing rules (#settings fieldset + fieldset margin-top, legend styling)
    - style.css contains typography refinements ensuring only 400 and 600 weights are used; no font-size outside the four declared roles (36px/20px/16px/14px)
  artifacts:
    - index.html (modified — SVG icons unified, settings fieldsets reorganized)
    - style.css (modified — chart SVG keyframes, chart-line/chart-el-enter classes, settings modal group CSS, typography refinement)
    - js/ui/charts-screen.js (modified — polyline pathLength="1" + class chart-line, heatmap rect class chart-el-enter)
    - js/ui/settings-modal.js (modified — DOM selectors updated if fieldset structure changed in index.html)
  key_links:
    - CSS .chart-line class in style.css must be applied via polyline.classList.add('chart-line') in charts-screen.js
    - CSS .chart-el-enter class in style.css must be applied via rect.classList.add('chart-el-enter') in charts-screen.js
    - Settings fieldset id="stagesFieldset" must be preserved for settings-modal.js querySelector('#stagesFieldset') to continue working
    - Icon viewBox="0 0 24 24" in index.html bottom-nav must match the 22×22px rendered size in UI-SPEC
---

<objective>
Visual identity polish: unify all inline SVG icons to a consistent stroke-based style (D8-10), add chart SVG draw-in and fade-in animations (D8-12), reorganize the Settings modal into five logical groups (D8-14), and tighten typography (D8-13). No new user-facing features — pure visual refinement.

Purpose: PLAT-06 — visual identity, calm/dark/minimal register. CSS @keyframes for chart animations; SVG icon unification; Settings modal regrouping per UI-SPEC.md.

Output: index.html with unified SVG icons and regrouped settings; style.css with chart animations and settings modal styles; charts-screen.js with animation class assignments
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/sleep-tracker/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/sleep-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/NW-08-pwa-platform-hardening/08-CONTEXT.md
@.planning/phases/NW-08-pwa-platform-hardening/08-RESEARCH.md
@.planning/phases/NW-08-pwa-platform-hardening/08-PATTERNS.md
@.planning/phases/NW-08-pwa-platform-hardening/08-UI-SPEC.md
@.planning/phases/NW-08-pwa-platform-hardening/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify SVG icons and reorganize Settings modal fieldsets in index.html</name>
  <files>index.html, js/ui/settings-modal.js</files>
  <read_first>
    - index.html (full file — read the existing Settings gear SVG path at lines 33–35, the bottom-nav SVG icons rendered by js/ui/bottom-nav.js, the quick-log buttons at today-screen section, and the entire settings dialog fieldset structure at lines 172–260)
    - js/ui/bottom-nav.js (read full file — identifies the TABS array with pathD values for all four bottom-nav icons and any inline SVG attributes; Phase 8 unifies these into the stroke-based style — the pathD values remain, only stroke/fill/viewBox attributes change)
    - js/ui/settings-modal.js (full file — locate all DOM queries using fieldset selectors or element names; confirm #stagesFieldset id is queried at settings-modal.js; confirm form.querySelector('[name="..."]') pattern is used for all input reads — these are name-based and unaffected by fieldset regrouping)
    - .planning/phases/NW-08-pwa-platform-hardening/08-UI-SPEC.md (Icon Style Contract table, Settings Modal Groups section)
    - .planning/phases/NW-08-pwa-platform-hardening/08-PATTERNS.md (settings-modal.js section — D8-14 proposed regrouping, JS query pattern to preserve)
  </read_first>
  <action>
    **Part A — Settings gear icon replacement in index.html:**
    The existing settings trigger uses a filled-path Material icon. Replace the inner SVG element (the viewBox="0 0 24 24" width="20" height="20" svg with the filled gear path) with a stroke-based gear SVG. Use the Feather Icons gear/settings path (Apache 2.0): a circle at center (cx=12 cy=12 r=3) plus an outer gear ring path. The exact stroke-based gear SVG: viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true", with a circle element cx=12 cy=12 r=3, and a path element with the gear outline d value. A suitable gear path (12-point, Lucide/Feather compatible): "M12 2a10 10 0 0 1 .324 0l.293.016.293.016.293.016A3 3 0 0 1 15 4.83l.009.02a3 3 0 0 0 3.83 1.42l.02-.009A3 3 0 0 1 22 7.83V8v.16A3 3 0 0 1 19.16 11l-.02.009a3 3 0 0 0-1.42 3.83l.009.02A3 3 0 0 1 16.17 18l-.16.009A3 3 0 0 1 13 15.16l-.009-.02a3 3 0 0 0-3.83-1.42l-.02.009A3 3 0 0 1 6 11.83V11.67A3 3 0 0 1 8.83 9l.02-.009a3 3 0 0 0 1.42-3.83l-.009-.02A3 3 0 0 1 11.83 2H12Z". ALTERNATIVE if the above is too complex: use Lucide Settings2 or a simple six-point star gear — the critical requirement is stroke-based (fill="none", stroke="currentColor", stroke-width="1.5"). If in doubt, use any standard Feather/Lucide gear SVG path. The key constraint from UI-SPEC: viewBox="0 0 24 24", stroke-width="1.5", stroke-linecap="round", stroke-linejoin="round", fill="none".

    **Part B — Settings modal fieldsets reorganization in index.html:**
    Replace the existing five fieldsets (Profile, Time & Day, Forecast tuning, Import/Export, Stages) with the five-group layout from UI-SPEC.md D8-14. Preserve ALL existing input elements, their name attributes, and IDs — only the grouping structure (which fieldset they live in) and the legend text changes. The new structure:
    1. Fieldset legend "Subject & Display": subjectName input, timeFormat select
    2. Fieldset legend "Day Structure": cutoverHour input, groupingMode select
    3. Fieldset legend "Forecast & Prediction": autoOutlier checkbox, maxDelta input, minDays input, windowDays input, statBlend select
    4. Fieldset legend "Data": importHelp paragraph, importCsvBtn button, importJsonBtn button, importStatus paragraph
    5. Fieldset id="stagesFieldset" legend "Stages": stagesHelp paragraph, stagesList div, addStageBtn button
    CRITICAL: preserve id="stagesFieldset" on the last fieldset — settings-modal.js queries this ID. Preserve all existing id attributes on buttons and input elements. Preserve the settingsErrors output element and the menu/buttons at the bottom of the form — these are outside the fieldsets and must not move.

    **Part C — If settings-modal.js has any selectors that target the fieldset legend text or fieldset position (not just input names or IDs):** Read settings-modal.js first to confirm. If any selector references fieldset legend content as text (e.g., querySelector('legend')) rather than by input name or element ID, update that selector to remain valid after the regrouping. The form.querySelector('[name="fieldName"]') pattern is name-based and unaffected.
  </action>
  <verify>
    <automated>node -e "const src=require('fs').readFileSync('index.html','utf8'); console.assert(!src.includes('fill=\"currentColor\"'),'no filled icons (except SVG defs)'); const gearFilled='M19.14 12.94'; console.assert(!src.includes(gearFilled),'old gear path removed'); console.assert(src.includes('stroke-width=\"1.5\"'),'stroke-width 1.5'); console.assert(src.includes('stroke-linecap=\"round\"'),'rounded linecap'); console.log('index.html icons OK')"</automated>
    <automated>node -e "const src=require('fs').readFileSync('index.html','utf8'); console.assert(src.includes('Subject &amp; Display')||src.includes('Subject &amp;amp; Display')||src.includes('Subject \\u0026 Display')||src.includes('Subject & Display'),'subject display group'); console.assert(src.includes('Day Structure'),'day structure group'); console.assert(src.includes('Forecast &amp; Prediction')||src.includes('Forecast & Prediction'),'forecast group'); console.assert(src.includes('id=\"stagesFieldset\"'),'stages fieldset id preserved'); console.assert(src.includes('name=\"subjectName\"'),'subjectName preserved'); console.assert(src.includes('name=\"cutoverHour\"'),'cutoverHour preserved'); console.assert(src.includes('name=\"autoOutlier\"'),'autoOutlier preserved'); console.log('settings fieldsets OK')"</automated>
  </verify>
  <done>Settings gear SVG replaced with stroke-based icon (fill="none", stroke="currentColor", stroke-width="1.5"); settings modal has five logical groups per UI-SPEC D8-14; all input name attributes preserved; id="stagesFieldset" preserved; settings-modal.js selectors verified to still work</done>
</task>

<task type="auto">
  <name>Task 2: Add chart SVG animations to style.css and wire animation classes in charts-screen.js</name>
  <files>style.css, js/ui/charts-screen.js</files>
  <read_first>
    - style.css (read the current end of file — append after the Phase 8 section added in Plan 08-03 Task 2, or after the last existing rule)
    - js/ui/charts-screen.js (full file — locate all polyline element creation calls and rect element creation calls; understand the svgEl() helper signature; find the buildSleepLength chart function where the main line polyline is created, and the heatmap function where rect elements are created)
    - .planning/phases/NW-08-pwa-platform-hardening/08-PATTERNS.md (charts-screen.js section — exact code showing pathLength="1" and classList.add('chart-line'); also rect.classList.add('chart-el-enter'))
    - .planning/phases/NW-08-pwa-platform-hardening/08-RESEARCH.md (Pattern 5 SVG draw-in with pathLength, Code Examples SVG draw-in animation)
    - .planning/phases/NW-08-pwa-platform-hardening/08-UI-SPEC.md (Chart SVG Transitions section — drawLine from var(--line-length) to 0, 300ms; fadeInEl 200ms ease-in)
  </read_first>
  <action>
    **Part A — Append chart animation CSS to style.css:**
    After the Phase 8 section (already added in Plan 08-03), add a subsection comment "/* Phase 8 — Chart SVG animations (D8-12) */". Add these rules:

    @keyframes drawLine with from { stroke-dashoffset: 1 } and to { stroke-dashoffset: 0 } (using the pathLength="1" normalization technique from RESEARCH.md Pattern 5 — avoids any JS getTotalLength() call).

    .chart-line with stroke-dasharray: 1, stroke-dashoffset: 1, animation: drawLine 300ms ease-out forwards.

    @keyframes fadeInEl with from { opacity: 0 } and to { opacity: 1 }.

    .chart-el-enter with opacity: 0, animation: fadeInEl 200ms ease-in forwards.

    **Part B — Wire animation classes in charts-screen.js:**
    Read charts-screen.js to find: (1) where polyline elements are created for the sleep-length line chart (the main data line polyline, not axis lines or grid lines), and (2) where rect elements are created for the calendar heatmap.

    For the sleep-length line chart polyline: after the svgEl('polyline', { ... }) call that creates the main data line (the one that gets points= set from the series data), add polyline.setAttribute('pathLength', '1') and polyline.classList.add('chart-line'). Do NOT add these to axis lines, tick marks, or grid lines — only to the primary data visualization polyline(s).

    For the heatmap rect elements: after each svgEl('rect', { ... }) call that creates a heatmap cell (the colored cells, not background or grid), add rect.classList.add('chart-el-enter').

    Do not add animation classes to SVG text elements, axis line elements, or structural elements (the SVG root, groups, or labels). The animation should apply only to the data visualization elements that change when data changes.

    Security note: classList.add() is a safe DOM API — no string interpolation, no innerHTML.

    **Part C — Typography refinement in style.css:**
    Read the current style.css to find any font-size values outside the four declared roles (36px/2.25rem, 20px/1.25rem, 16px/1rem, 14px/0.875rem) and any font-weight values other than 400 and 600. Append a comment "/* Phase 8 — Typography normalization (D8-13) */" and add any necessary overrides to bring outliers within the four-role system. For example: if any element uses font-weight: 500 or 700, add an override to normalize it to 400 or 600. If any element uses a font-size like 12px or 18px, add an override to the nearest role size. Do not remove existing rules — add targeted overrides only. If the audit finds no outliers (all sizes and weights already match the four-role system), document this with a comment: "/* D8-13 audit: all existing font-size and font-weight values conform to the four-role system — no overrides needed */"
  </action>
  <verify>
    <automated>node -e "const src=require('fs').readFileSync('style.css','utf8'); console.assert(src.includes('@keyframes drawLine'),'drawLine keyframes'); console.assert(src.includes('@keyframes fadeInEl'),'fadeInEl keyframes'); console.assert(src.includes('.chart-line'),'chart-line class'); console.assert(src.includes('.chart-el-enter'),'chart-el-enter class'); console.assert(src.includes('stroke-dasharray: 1'),'dasharray 1'); console.assert(src.includes('drawLine 300ms'),'drawLine duration'); console.assert(src.includes('fadeInEl 200ms'),'fadeIn duration'); console.log('style.css chart animations OK')"</automated>
    <automated>node -e "const src=require('fs').readFileSync('js/ui/charts-screen.js','utf8'); console.assert(src.includes('pathLength'),'pathLength attribute'); console.assert(src.includes('chart-line'),'chart-line class'); console.assert(src.includes('chart-el-enter'),'chart-el-enter class'); console.assert(!src.includes('innerHTML'),'no innerHTML'); console.log('charts-screen.js animation wiring OK')"</automated>
  </verify>
  <done>style.css has drawLine and fadeInEl @keyframes; .chart-line and .chart-el-enter classes defined; charts-screen.js sleep-length polyline has pathLength="1" and class chart-line; heatmap rects have class chart-el-enter; no innerHTML used; typography D8-13 audit complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SVG innerHTML (icon replacement) | New icons use createElementNS or are static inline SVG — no dynamic innerHTML for icons |
| CSS animation | Pure CSS — no JS eval; classList.add() is safe DOM API |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04-01 | Tampering | SVG icon replacement via innerHTML | high | mitigate | Icons are static inline SVG in index.html (HTML author-controlled); no dynamic icon generation via innerHTML; charts-screen.js uses createElementNS exclusively (D7-08 invariant preserved) |
| T-08-04-02 | Tampering | settings-modal.js DOM selectors broken after fieldset restructure | medium | mitigate | All settings input reads use form.querySelector('[name="fieldName"]') — name-based, unaffected by fieldset regrouping; id="stagesFieldset" preserved; verify selectors before committing |
| T-08-04-03 | Denial of Service | CSS animation causing layout reflow on every tab switch | low | accept | opacity-only transitions do not trigger layout reflow (GPU composited); stroke-dashoffset animation is SVG paint-only; no layout thrashing |
</threat_model>

<verification>
- index.html settings trigger: old filled gear path "M19.14 12.94..." is GONE
- index.html settings trigger: new SVG has fill="none", stroke="currentColor", stroke-width="1.5"
- index.html settings fieldsets: five groups with legends "Subject & Display", "Day Structure", "Forecast & Prediction", "Data", "Stages"
- index.html: id="stagesFieldset" still present on last fieldset
- style.css: @keyframes drawLine with stroke-dashoffset: 1 to 0
- style.css: .chart-line { stroke-dasharray: 1; stroke-dashoffset: 1; animation: drawLine 300ms ease-out forwards }
- style.css: @keyframes fadeInEl opacity 0 to 1
- style.css: .chart-el-enter { opacity: 0; animation: fadeInEl 200ms ease-in forwards }
- charts-screen.js: polyline.setAttribute('pathLength', '1') present
- charts-screen.js: polyline.classList.add('chart-line') present
- charts-screen.js: rect.classList.add('chart-el-enter') present (heatmap cells)
- No innerHTML in charts-screen.js (existing invariant preserved)
</verification>

<success_criteria>
- All inline SVG icons unified to stroke-based style (viewBox 0 0 24 24, stroke-width 1.5, fill none, stroke currentColor, linecap round, linejoin round)
- Settings modal reorganized into five logical groups per UI-SPEC D8-14; all input names/IDs preserved
- Chart animations: @keyframes drawLine and fadeInEl in style.css; .chart-line on sleep-length polyline; .chart-el-enter on heatmap rects
- pathLength="1" on animated polylines (no JS getTotalLength needed)
- D8-13 typography audit complete: all font-size and font-weight values conform to four-role system
</success_criteria>

<output>
Create `.planning/phases/NW-08-pwa-platform-hardening/08-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Modified files:
- `index.html` — unified stroke-based SVG icons; five-group Settings modal fieldset structure
- `style.css` — @keyframes drawLine, @keyframes fadeInEl, .chart-line, .chart-el-enter, settings modal group spacing, typography normalization
- `js/ui/charts-screen.js` — pathLength="1" + class chart-line on data polylines; class chart-el-enter on heatmap rects
- `js/ui/settings-modal.js` — verified/updated DOM selectors if fieldset structure changed

New CSS classes:
- `.chart-line` — SVG polyline draw-in animation (stroke-dasharray 1, drawLine 300ms)
- `.chart-el-enter` — SVG element fade-in animation (opacity, fadeInEl 200ms)

New CSS @keyframes:
- `drawLine` — stroke-dashoffset 1→0
- `fadeInEl` — opacity 0→1
